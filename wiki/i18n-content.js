/**
 * @file Wiki body-content translations for Simplified Chinese, Vietnamese, Korean, and Spanish.
 * Keys are whitespace-normalized English innerHTML; values keep inline tags.
 * 'plain' fills heading/label gaps in script.js's T (existing T wins).
 * @author Son Nguyen <hoangson091104@gmail.com>
 */
window.__WIKI_CONTENT_I18N = {
  zh: {
    "<span class=\"caption-icon\">⌘</span> <span><strong>Command Palette</strong> — <code>Cmd/Ctrl+K</code> from anywhere opens the dashboard's only launcher. One query resolves nine groups: recent commands, the nine pages, live server-side session search, the current page's own actions, every known project directory, page sub-views and list filters, all 13 Settings sections, all 12 Agent Config tabs, and preference, data-scope and language actions. Matching is subsequence-based with the matched characters highlighted</span>":
      '<span class="caption-icon">⌘</span> <span><strong>命令面板</strong> —— 在任意位置按 <code>Cmd/Ctrl+K</code> 打开仪表盘唯一的启动器。一次查询解析九个分组：最近命令、九个页面、实时服务端会话搜索、当前页面自身的操作、每一个已知的项目目录、页面子视图与列表筛选、全部 13 个设置分区、全部 12 个 Agent 配置标签页，以及偏好、数据范围与语言相关的动作。匹配采用子序列方式并高亮命中字符</span>',
    "Command Palette": "命令面板",
    "Press <strong>Cmd/Ctrl+K</strong> anywhere to open a launcher over the whole dashboard. One query resolves nine groups: recently run commands; the current page's own actions; every known project directory; the nine sidebar routes matched on their translated labels, so it works in every locale; live session search backed by <code>/api/sessions?q=</code>, debounced and scoped to the active machine and provider rather than a stale client-side index of thousands of sessions; every page sub-view and list filter; all thirteen Settings sections; all twelve Agent Config tabs; and actions for preferences, data scope, language, and page operations. Ranking is subsequence matching with the matched characters highlighted, so <code>mcp</code> finds MCP servers and <code>kbrd</code> finds Kanban Board, and a leading <code>&gt;</code>, <code>@</code>, or <code>#</code> narrows to actions, pages, or sessions. Arrows, Home/End, PageUp/PageDown and Tab move; Enter opens; Escape closes; a failed session query degrades quietly instead of blocking the palette. Destructive operations are deliberately absent — the palette navigates to them, never performs them.":
      "在任意位置按 <strong>Cmd/Ctrl+K</strong> 即可打开覆盖整个仪表盘的启动器。一次输入解析九组结果：最近运行的命令；当前页面自身的操作；每一个已知的项目目录；按已翻译标签匹配的九个侧边栏路由，因此在任何语言下都可用；由 <code>/api/sessions?q=</code> 支撑的实时会话搜索，带防抖并限定在当前机器与提供方范围内，而不是在客户端保留数千条会话的陈旧索引；每个页面子视图与列表筛选；全部十三个设置分区；全部十二个 Agent 配置标签页；以及用于偏好、数据范围、语言和页面操作的动作。排序采用子序列匹配并高亮命中字符，因此 <code>mcp</code> 能找到 MCP servers，<code>kbrd</code> 能找到 Kanban Board；开头的 <code>&gt;</code>、<code>@</code> 或 <code>#</code> 可缩小到动作、页面或会话。方向键、Home/End、PageUp/PageDown 与 Tab 用于移动，Enter 打开，Escape 关闭；会话查询失败时会安静降级，而不会阻塞面板。破坏性操作被刻意排除 —— 面板只跳转到它们，绝不代为执行。",
    "Serve-stale window (ms) for per-transcript task-progress summaries behind <code>include_task_progress</code> list requests and the session-detail <code>todo_snapshot</code>. A transcript being actively appended to rarely hits the size+mtime cache key, so without this floor every list reload fully re-parses each multi-MB live transcript. <code>0</code> restores immediate re-parse on every change.":
      "\u6309\u8f6c\u5f55\u7ef4\u5ea6\u7684\u4efb\u52a1\u8fdb\u5ea6\u6458\u8981\u7684\u8fc7\u671f\u53ef\u7528\u7a97\u53e3\uff08\u6beb\u79d2\uff09\uff0c\u4f5c\u7528\u4e8e <code>include_task_progress</code> \u5217\u8868\u8bf7\u6c42\u4ee5\u53ca\u4f1a\u8bdd\u8be6\u60c5\u7684 <code>todo_snapshot</code>\u3002\u6b63\u5728\u6301\u7eed\u8ffd\u52a0\u7684\u8f6c\u5f55\u51e0\u4e4e\u65e0\u6cd5\u547d\u4e2d size+mtime \u7f13\u5b58\u952e\uff0c\u82e5\u65e0\u6b64\u4e0b\u9650\uff0c\u6bcf\u6b21\u5217\u8868\u5237\u65b0\u90fd\u4f1a\u5b8c\u6574\u91cd\u65b0\u89e3\u6790\u6570 MB \u7684\u6d3b\u8dc3\u8f6c\u5f55\u3002\u8bbe\u4e3a <code>0</code> \u53ef\u6062\u590d\u6bcf\u6b21\u53d8\u66f4\u7acb\u5373\u91cd\u65b0\u89e3\u6790\u3002",
    "Closes an idle MCP HTTP or SSE session after this long with no request, reclaiming its server; <code>0</code> disables reaping":
      "空闲的 MCP HTTP 或 SSE 会话在此时长内没有请求后关闭，并回收其 Server；<code>0</code> 表示禁用回收。",
    "Every transport reclaims its own resources. A stdio server self-terminates once its host process is gone — stdin closing, or the process being re-parented away from the parent it started under — so a host that dies without signalling leaves no orphan behind. An HTTP session is released by <code>DELETE /mcp</code>, by an SSE disconnect, or by an idle sweep governed by <code>MCP_HTTP_SESSION_TIMEOUT_MS</code>, because terminating a session is optional in the protocol and a vanished client would otherwise hold its server forever.":
      "每种传输方式都会回收各自的资源。stdio Server 会在其宿主进程消失后自行终止——stdin 关闭，或进程被重新挂到启动时父进程以外的父进程之下——因此宿主未发送信号就崩溃时不会留下孤儿进程。HTTP 会话会通过 <code>DELETE /mcp</code>、SSE 断开连接，或由 <code>MCP_HTTP_SESSION_TIMEOUT_MS</code> 控制的空闲清理来释放；因为协议中终止会话是可选的，否则消失的客户端会永久占用其 Server。",
    "One-time automatic repair of token totals inflated before usage was reconciled per <code>message.id</code>. <code>replaceTokenUsage</code> is a high-water mark, so the parser fix alone can never lower a historical total. Marker-gated, deferred off the boot path, skipped while another dashboard shares the data directory, and it snapshots the old rows to <code>token_usage_pre_repair</code> first. <code>0</code> skips it \u2014 repair manually with <code>npm run repair-tokens</code>.":
      "\u4e00\u6b21\u6027\u81ea\u52a8\u4fee\u590d\u5728\u7528\u91cf\u6309 <code>message.id</code> \u5bf9\u8d26\u4e4b\u524d\u88ab\u865a\u9ad8\u7684 Token \u603b\u8ba1\u3002<code>replaceTokenUsage</code> \u662f\u9ad8\u6c34\u4f4d\u6807\u8bb0\uff0c\u56e0\u6b64\u4ec5\u9760\u89e3\u6790\u5668\u4fee\u590d\u6c38\u8fdc\u65e0\u6cd5\u4e0b\u8c03\u5386\u53f2\u603b\u8ba1\u3002\u8be5\u4fee\u590d\u6709\u6807\u8bb0\u6587\u4ef6\u3001\u5ef6\u540e\u4e8e\u542f\u52a8\u8def\u5f84\u3001\u5f53\u53e6\u4e00\u4e2a Dashboard \u5171\u7528\u6570\u636e\u76ee\u5f55\u65f6\u4f1a\u8df3\u8fc7\uff0c\u5e76\u4f1a\u5148\u5c06\u65e7\u884c\u5feb\u7167\u5230 <code>token_usage_pre_repair</code>\u3002\u8bbe\u4e3a <code>0</code> \u53ef\u8df3\u8fc7 \u2014\u2014 \u6539\u7528 <code>npm run repair-tokens</code> \u624b\u52a8\u4fee\u590d\u3002",
    "<strong>Monitor</strong> combines overview stats, active agent cards, collapsible subagent trees, recent activity, and the same task-progress previews used by Sessions. <strong>Health</strong> summarizes system score, storage, cache, errors, tool use, subagent effectiveness, model tokens, and compactions. Both tabs refresh every 5 seconds through WebSocket push, while the activity feed adapts its item count to the available viewport height.":
      "<strong>Monitor</strong> 汇总概览统计、活动 Agent 卡片、可折叠子 Agent 树、最近活动，以及与 Sessions 相同的任务进度预览。<strong>Health</strong> 概括系统评分、存储、缓存、错误、工具使用、子 Agent 有效性、模型 Token 和上下文压缩。两个标签页每 5 秒通过 WebSocket 推送刷新，活动流的条目数会适配可用视口高度。",
    "Switch between <strong>Agent</strong> and <strong>Session</strong> swim lanes for working, waiting, completed, error, and abandoned states. Yellow Waiting cards identify work paused for user input, a permission gate, or the next prompt; hover badges and column headers for the exact reason and lifecycle rules. Cards show model, cumulative cost, and current tool, while WebSocket updates keep lane counts synchronized with the live event store.":
      "在 <strong>Agent</strong> 与 <strong>Session</strong> 泳道间切换，查看工作中、等待、已完成、错误和已放弃状态。黄色 Waiting 卡片标识因用户输入、权限门槛或下一条 Prompt 而暂停的工作；悬停徽标和列标题可查看具体原因与生命周期规则。卡片显示模型、累计成本和当前工具，WebSocket 更新则使泳道计数与实时事件存储保持同步。",
    "Server-side rules detect event patterns, inactivity, stuck agents, and token thresholds with per-session cooldown dedup. Fired alerts appear in a live feed and can reach 14 built-in providers or a generic JSON endpoint with optional HMAC-SHA256 signing and custom headers. Delivery stays fail-safe through bounded timeouts, retries, secret redaction, test probes, and per-target logs. Configure rules and channels together in <strong>Settings → Alerts</strong>.":
      "服务端规则通过按会话冷却去重检测事件模式、无活动、卡住的 Agent 和 Token 阈值。触发的告警会出现在实时流中，并可发送到 14 个内置 Provider 或支持可选 HMAC-SHA256 签名和自定义 Header 的通用 JSON 端点。有限超时、重试、Secret 脱敏、测试探针和按目标日志共同确保投递故障安全。可在 <strong>Settings → Alerts</strong> 中统一配置规则和渠道。",
    "Electron packages CCAM as a macOS <code>.dmg</code> and Windows installer or portable <code>.exe</code>. The app embeds the Express server in-process and renders the React client in a native window, with tray controls, application menus, login startup, and a single-instance lock. Closing the window leaves the local server running, and first boot installs Claude Code hooks automatically so events flow without a repository checkout.":
      "Electron 将 CCAM 打包为 macOS <code>.dmg</code>，以及 Windows 安装包或便携式 <code>.exe</code>。应用在进程内嵌入 Express Server，并在原生窗口中渲染 React Client，同时提供托盘控制、应用菜单、登录时启动和单实例锁。关闭窗口后本地 Server 继续运行，首次启动还会自动安装 Claude Code Hook，无需检出仓库即可接收事件。",
    "Compare model tokens, tool frequency, agent types, session outcomes, and total input, output, and cache spend. A 52-week activity heatmap and 30-day cost and session sparklines reveal longer trends, while WebSocket refresh and a live indicator keep current values clear. Responsive charts adapt to mobile screens; long legends paginate with localized controls without changing compact legends.":
      "比较模型 Token、工具频率、Agent 类型、会话结果，以及输入、输出和缓存总成本。52 周活动热图与 30 天成本和会话迷你趋势图展示长期变化，WebSocket 刷新和实时指示器则保持当前值清晰。响应式图表适配移动屏幕；较长图例使用本地化控件分页，紧凑图例保持不变。",
    "Measure subagent effectiveness, workflow patterns, model delegation, error propagation, concurrency, and complexity in one view. Expand detected patterns for their full step chain, statistics, and loop-aware narrative; info popovers explain each headline metric, and the status filter applies across the page. Data-driven legends paginate when needed so every label remains reachable without making chart cards excessively tall.":
      "在一个视图中衡量子 Agent 有效性、工作流模式、模型委派、错误传播、并发和复杂度。展开检测到的模式即可查看完整步骤链、统计数据和循环感知说明；信息弹层会解释各项核心指标，状态筛选作用于整个页面。数据驱动的图例会按需分页，让每个标签都可访问，同时避免图表卡片过高。",
    "Start Claude Code or Codex from a streaming browser chat, using multi-turn, headless, or resumable sessions. Re-attachment reconciles memory with the saved transcript, while slash-command completion, file references, token context, and effort controls bring core TUI workflows to the dashboard. Codex uses a native interactive <code>app-server</code> thread with provider approval, sandbox, and signed-in model discovery; same-origin checks protect every spawn.":
      "通过流式浏览器聊天启动 Claude Code 或 Codex，可使用多回合、Headless 或可恢复会话。重新连接会将内存与已保存的 Transcript 对齐，斜杠命令补全、文件引用、Token 上下文和 Effort 控制则把核心 TUI 工作流带到 Dashboard。Codex 使用原生交互式 <code>app-server</code> 线程，并提供 Provider 审批、Sandbox 和已登录模型发现；同源检查会保护每次启动。",
    "Open any session for overview counters, top-tool and subagent breakdowns, a collapsible agent tree, and a chronological event timeline. When task state exists, an owner-aware panel adds a segmented donut, active task, completion bar, and paginated rows. The conversation view renders Markdown, highlighted code, tool blocks, slash-command output, renames, and correctly attributed queued messages. Waiting sessions explain why and for how long; export the record as JSON or share its permalink.":
      "打开任意会话即可查看概览计数、Top 工具与子 Agent 分布、可折叠 Agent 树和按时间排序的事件线。存在任务状态时，带归属信息的面板会显示分段环形图、当前任务、完成进度条和分页任务行。对话视图可渲染 Markdown、高亮代码、工具区块、斜杠命令输出、重命名，以及正确归属的排队消息。等待中的会话会说明原因和时长；你也可以导出 JSON 记录或共享永久链接。",
    "Background sync keeps new Claude project folders and session files visible after the one-time startup import. An immediate sweep, debounced filesystem watcher, and optional 30-second safety poll share one mtime cache and coalesce overlapping work; set <code>DASHBOARD_SESSION_SYNC_MS</code> to tune or disable only the poll. Each sweep parses only changed transcripts, skips unchanged imports, and broadcasts session and main-agent updates so the UI refreshes live across macOS, Windows, and Linux.":
      "后台同步会在一次性启动导入后继续发现新的 Claude 项目目录和会话文件。即时扫描、带防抖的文件系统 watcher 与可选的 30 秒安全轮询共享一个 mtime 缓存，并合并重叠工作；可通过 <code>DASHBOARD_SESSION_SYNC_MS</code> 调整或仅禁用轮询。每次扫描只解析已变化的 Transcript、跳过未变化的导入，并广播会话和主 Agent 更新，让 UI 在 macOS、Windows 和 Linux 上实时刷新。",
    "Configure per-model input, output, cache, and Codex reasoning rates in Settings, then see consistent totals across Sessions, Session Detail, Analytics, Kanban, and agent cards. Compaction-aware accounting preserves usage across context compression, imports reuse the same pricing table, and rate edits recalculate stored sessions. Subagent cards show only their own transcript cost while main agents retain session totals. GPT pricing distinguishes standard, long-context, and Fast modes, leaving unpublished tiers explicitly unpriced.":
      "在 Settings 中配置各模型的输入、输出、缓存和 Codex 推理费率，即可在 Sessions、Session Detail、Analytics、Kanban 和 Agent 卡片中查看一致的成本总计。支持压缩感知的核算会跨上下文压缩保留用量，导入复用同一价格表，费率修改后会重新计算已存储会话。子 Agent 卡片只显示自身 Transcript 成本，主 Agent 则保留会话总成本。GPT 定价区分标准、长上下文和 Fast 模式，未公布的档位会明确保持未定价。",
    "Explore Claude Code configuration in 12 tabs covering skills, subagents, commands, styles, plugins, MCP, hooks, settings, memory, keybindings, and statusline scripts. The page resolves <code>/config</code> values across scopes, searches project memory, links <code>MEMORY.md</code> entries to their source facts, and shows plugin authorship. Low-risk text, memory, and keybinding files support create, edit, and delete with timestamped backups; plugins, MCP, hooks, and live settings remain read-only with copyable CLI guidance.":
      "通过 12 个标签页浏览 Claude Code 配置，涵盖技能、子 Agent、命令、样式、插件、MCP、Hook、设置、内存、快捷键和状态栏脚本。页面会跨作用域解析 <code>/config</code> 值、搜索项目内存、将 <code>MEMORY.md</code> 条目链接到来源事实，并显示插件作者。低风险的文本、内存和快捷键文件支持创建、编辑和删除，且操作前会生成带时间戳的备份；插件、MCP、Hook 和实时设置保持只读，并提供可复制的 CLI 指引。",
    "Browse every recorded session in a <strong>server-paginated</strong> table with fast search, project and status filters, sorting, cost, model, duration, and agent counts. The first page can show a temporary local Codex startup row until its durable session arrives. Rows with task state add a compact progress donut and owner-aware preview; stale trackers disappear when newer work no longer uses them or a turn ends unfinished, while completed progress remains available. Open any durable row for its conversation, events, and agent hierarchy.":
      "在<strong>服务端分页</strong>表格中浏览所有已记录会话，并使用快速搜索、项目与状态筛选、排序、成本、模型、持续时间和 Agent 数量。第一页可在持久会话到达前显示临时的本机 Codex 启动行。具有任务状态的行会显示紧凑进度环和带归属信息的预览；当新工作不再使用 tracker，或回合以未完成状态结束时，旧 tracker 会消失，而已完成进度仍会保留。打开任意持久会话行即可查看对话、事件和 Agent 层级。",
    "Reads Claude and Codex transcripts incrementally, keeping long-running sessions fast while powering conversation history, live titles, prompt previews, attachments, and turn statistics. Renames and recent human prompts flow to session and agent cards in real time, older conversation pages load on demand, and duplicate Codex records collapse into one readable turn. Stable transcript identities also prevent replayed events from inflating totals, while provider-aware process matching keeps historical sessions from appearing active.":
      "增量读取 Claude 和 Codex Transcript，在保持长时间会话快速的同时，为对话历史、实时标题、Prompt 预览、附件和回合统计提供支持。重命名与近期用户 Prompt 会实时显示在会话和 Agent 卡片上，较早的对话页面按需加载，重复的 Codex 记录会合并为一个清晰回合。稳定的 Transcript 标识还可防止重放事件抬高总数，而 Provider 感知的进程匹配可避免历史会话被误显示为 active。",
    "Stable transcript identities keep Claude <code>TurnDuration</code> events replay-safe and repair legacy duplicate totals. On supported local hosts, exact open-rollout probing also prevents historical Codex sessions that share a project directory from appearing as active agents. Node launcher/native-child pairs are collapsed first, so each interactive TUI produces one card.":
      "稳定的 Transcript 标识使 Claude <code>TurnDuration</code> 事件可安全重放，并修复旧版本的重复总计。在受支持的本地主机上，精确的开放 rollout 探测还会防止共享项目目录的历史 Codex 会话显示为 active Agent。Node 启动器与原生子进程会先合并，因此每个交互式 TUI 只生成一张卡片。",
    '<span class="caption-icon">📋</span> <span><strong>Task Progress · Overview</strong> — Dashboard agent cards and Sessions rows reuse the same compact completion donut beside status; hover or focus opens an owner-aware preview of current work and task states</span>':
      '<span class="caption-icon">📋</span> <span><strong>任务进度 · 概览</strong> — Dashboard Agent 卡片与 Sessions 行在状态旁复用同一个紧凑完成度环形图；悬停或聚焦可打开带归属信息的当前工作与任务状态预览</span>',
    '<span class="caption-icon">✅</span> <span><strong>Task Progress · Session Detail</strong> — the full owner-aware tracker combines a segmented completion donut, active task, completion bar, owner breakdown, and a task list paginated at 10 rows per page</span>':
      '<span class="caption-icon">✅</span> <span><strong>任务进度 · 会话详情</strong> — 完整的带归属信息任务跟踪器包含分段完成度环形图、当前任务、完成进度条、归属统计，以及每页 10 行的任务列表</span>',
    "Two tabs: <strong>Monitor</strong> shows overview stats, active agent cards with collapsible subagent hierarchy, and a recent activity feed whose item count fills available viewport height. When an active session exposes task state, its card shows the same completion donut as the Sessions table immediately before the status badge; hover or focus opens the detailed task preview. <strong>Health</strong> renders a composite system health score ring, storage engine donut chart, cache/error/success gauges, tool invocation bars, subagent effectiveness ratios, model token distribution, and compaction stats. Both tabs auto-refresh every 5 seconds via WebSocket push so the view is always current without manual reload.":
      "两个标签页：<strong>Monitor</strong> 显示概览统计、带可折叠子 Agent 层级的活动 Agent 卡片，以及根据可用视口高度填充条目数的最近活动流。当活动会话暴露任务状态时，其卡片会在状态徽标前显示与 Sessions 表格相同的完成度环形图；悬停或聚焦可打开详细任务预览。<strong>Health</strong> 显示综合系统健康评分环、存储引擎环形图、缓存/错误/成功仪表盘、工具调用条、子 Agent 有效性比率、模型 Token 分布和上下文压缩统计。两个标签页都通过 WebSocket 推送每 5 秒自动刷新，无需手动重载即可保持最新。",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. When observable task state exists, an owner-aware progress panel appears directly below the overview with a segmented donut, active task, completion bar, owner breakdown, and dense task rows paginated at 10 tasks per page. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "通过可折叠的 Agent 层级树和完整的按时间排序事件线深入查看单个会话，时间线显示每次工具调用的名称和摘要。顶部概览面板显示事件、工具调用、子 Agent、上下文压缩、错误和持续时间的统计卡片。Top 工具使用条和子 Agent 类型分布便于快速判断结构。当存在可观察的任务状态时，概览下方会显示带归属信息的进度面板，包含分段环形图、当前任务、完成进度条、归属统计，以及每页 10 项的紧凑任务行。对话查看器渲染带语法高亮的 Markdown、按工具样式化的区块、带已捕获 TUI 输出的斜杠命令胶囊，以及内联会话重命名标记。回合中输入的消息（Claude 仍在工作时排队）会显示在 Claude 实际接收的位置，框架通知归属为 System。可将整个会话导出为 JSON，或共享永久链接进行异步审查。当会话等待你的输入时，页头下方的黄色横幅会标明原因和等待时长。",
    '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — live overview of active sessions and agents. Cards with task state show a progress donut beside status with the same detailed preview as Sessions. Stats tiles, collapsible subagent hierarchy cards, and a recent activity feed auto-refresh via WebSocket</span>':
      '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — 活动会话与 Agent 的实时概览。有任务状态的卡片会在状态旁显示进度环，并提供与 Sessions 相同的详细预览。统计卡片、可折叠的子 Agent 层级卡片和最近活动流通过 WebSocket 自动刷新</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles, an owner-aware task-progress panel with 10 tasks per page when available, top-tool usage, subagent breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>会话详情 · Agent</strong> — 概览统计、可用时显示每页 10 项且带归属信息的任务进度面板、Top 工具使用、子 Agent 分布，以及可折叠的父子 Agent 层级树</span>',
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. The first page also shows the same local, in-memory Codex startup row used by Dashboard and Kanban. It appears immediately but stays non-navigable until a durable session ID replaces it, without changing durable totals or pagination. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, estimated cost, and — when the provider exposed task state — a compact completion donut beside the status badge. Hovering or focusing the donut opens a viewport-clamped preview with current work, owner labels, source, and up to five tasks. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "<strong>服务端分页</strong>表格展示所有已记录会话，每页只获取当前切片，因此无论会话数量多少，成本计算范围都保持受控。第一页还会显示与 Dashboard 和 Kanban 相同的本机内存 Codex 启动行。该行会立即出现，但在持久会话 ID 替换它之前不可跳转，并且不会改变持久总数或分页。服务端以 300 毫秒防抖对 <code>id</code>、<code>name</code> 和 <code>cwd</code> 执行不区分大小写的搜索；状态筛选可与搜索组合以精确缩小范围。每行显示会话真实名称（从 Transcript 实时同步：优先使用 <code>/rename</code> 或 <code>claude -n</code> 标题，其次自动标题，再其次首条用户 Prompt，最后回退到短 ID）、状态徽标、Agent 数量、持续时间、模型、预估成本；当 Provider 暴露任务状态时，状态徽标旁还会显示紧凑的完成度环形图。悬停或聚焦该环形图会打开受视口约束的预览，显示当前工作、归属标签、来源以及最多五项任务。点击任意持久会话行可进入包含对话 Transcript 和 Agent 层级的完整会话详情。",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. When observable task state exists, an owner-aware progress panel appears directly below the overview with a segmented donut, active task, completion bar, owner breakdown, and dense task rows. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "通过可折叠的 Agent 层级树和完整的按时间排序事件线深入查看单个会话，时间线显示每次工具调用的名称和摘要。顶部概览面板显示事件、工具调用、子 Agent、上下文压缩、错误和持续时间的统计卡片。Top 工具使用条和子 Agent 类型分布便于快速判断结构。当存在可观察的任务状态时，概览下方会显示带归属信息的进度面板，包含分段环形图、当前任务、完成进度条、归属统计和紧凑任务行。对话查看器渲染带语法高亮的 Markdown、按工具样式化的区块、带已捕获 TUI 输出的斜杠命令胶囊，以及内联会话重命名标记。回合中输入的消息（Claude 仍在工作时排队）会显示在 Claude 实际接收的位置，框架通知归属为 System。可将整个会话导出为 JSON，或共享永久链接进行异步审查。当会话等待你的输入时，页头下方的黄色横幅会标明原因和等待时长。",
    '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — searchable, filterable, server-paginated table. Rows with task state add a progress donut beside status; hover or focus opens a detailed task preview. Click any row to drill into session detail</span>':
      '<span class="caption-icon">📂</span> <span><strong>会话</strong> — 可搜索、可筛选、服务端分页的表格。有任务状态的行会在状态旁显示进度环；悬停或聚焦可打开详细任务预览。点击任意行进入会话详情</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles, an owner-aware task-progress panel when available, top-tool usage, subagent breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>会话详情 · Agent</strong> — 概览统计、可用时显示带归属信息的任务进度面板、Top 工具使用、子 Agent 分布，以及可折叠的父子 Agent 层级树</span>',
    "A fresh interactive Codex process first appears as an in-memory Waiting card, including while Codex is still at its trust screen or first prompt and has no stable session ID. The card is local-only, non-navigable, excluded from durable APIs unless <code>include_transient=true</code>, and removed on process exit. A <code>SessionStart</code> hook, native live-thread row, or rollout then creates the durable session and main-agent rows without copying the temporary card into history. If the user selects an existing thread in Codex's Resume picker, that exact Codex PID opens the resumed rollout or writer lock immediately. CCAM uses that signal to switch from the temporary card to the durable resumed session before the first new message.":
      "新的交互式 Codex 进程会先显示为仅存在于内存中的等待卡片，即使 Codex 仍停留在信任确认界面或首个提示符，尚未获得稳定的 session ID。该卡片仅限本机、不可跳转，除非使用 <code>include_transient=true</code>，否则不会出现在持久 API 中，并会在进程退出时移除。随后 <code>SessionStart</code> Hook、原生 live-thread 行或 rollout 会创建持久会话和主 agent 行，而不会把临时卡片复制到历史记录中。如果用户在 Codex 的 Resume 选择器中选择已有线程，对应的 Codex PID 会立即打开已恢复的 rollout 或 writer lock。CCAM 使用该信号在首条新消息发送前从临时卡片切换到持久的已恢复会话。",
    "<strong>React 19</strong>": "<strong>React 19</strong>",
    "<strong>Vite 7</strong>": "<strong>Vite 7</strong>",
    "<strong>React Router 8</strong>": "<strong>React Router 8</strong>",
    "The OCI runtime is non-root, uses Tini as PID 1, includes Git, OpenSSH, and SQLite, and becomes read-only except for persistent data/config volumes and tmpfs. Docker Compose and Podman Compose use the same file.":
      "OCI 运行时以非 root 用户运行，使用 Tini 作为 PID 1，包含 Git、OpenSSH 与 SQLite，并且除持久化数据/配置卷和 tmpfs 外，根文件系统均为只读。Docker Compose 与 Podman Compose 使用同一份文件。",
    "Dashboard, MCP, Nginx, Prometheus, and Grafana bind host loopback by default. Nginx proxies the UI, authenticated API, and WebSocket, while hooks, metrics, and MCP remain blocked at the edge unless explicitly enabled.":
      "Dashboard、MCP、Nginx、Prometheus 与 Grafana 默认仅绑定宿主机 loopback。Nginx 代理 UI、认证 API 与 WebSocket，而 Hook、指标和 MCP 默认在边缘被阻止，除非显式启用。",
    "Run exactly one dashboard container per data volume. Install hooks on the host. Remote hook destinations must use HTTPS plus <code>CCAM_HOOK_TOKEN</code>.":
      "每个数据卷必须只运行一个 Dashboard 容器。请在宿主机安装 Hook。远程 Hook 目标必须使用 HTTPS，并提供 <code>CCAM_HOOK_TOKEN</code>。",
    "The dashboard image runs as UID/GID 1000 with a read-only root filesystem, dropped capabilities, <code>no-new-privileges</code>, health checks, and persistent <code>/app/data</code> plus <code>/app/config</code> volumes. Claude and Codex homes mount read-only at <code>/home/node/.claude</code> and <code>/home/node/.codex</code>.":
      "Dashboard 镜像以 UID/GID 1000 运行，根文件系统只读，丢弃所有 capability，启用 <code>no-new-privileges</code> 与健康检查，并持久化 <code>/app/data</code> 和 <code>/app/config</code> 卷。Claude 与 Codex home 分别只读挂载到 <code>/home/node/.claude</code> 和 <code>/home/node/.codex</code>。",
    'The optional <code>agent-runtime</code> image target adds pinned Claude Code and Codex CLIs for container-native Run Agent workflows. Cloud deployments use one Recreate-managed dashboard replica on a retained ReadWriteOnce PVC through Helm, Kustomize, or Terraform-to-existing-Kubernetes. See <a href="../DEPLOYMENT.md">DEPLOYMENT.md</a>.':
      '可选的 <code>agent-runtime</code> 镜像目标加入固定版本的 Claude Code 与 Codex CLI，用于容器原生 Run Agent 工作流。云部署通过 Helm、Kustomize 或面向现有 Kubernetes 的 Terraform，在保留的 ReadWriteOnce PVC 上运行一个由 Recreate 管理的 Dashboard 副本。参见 <a href="../DEPLOYMENT.md">DEPLOYMENT.md</a>。',
    "Protects MCP <code>/mcp</code>, <code>/sse</code>, and <code>/messages</code>; <code>/health</code> remains available to probes":
      "保护 MCP <code>/mcp</code>、<code>/sse</code> 和 <code>/messages</code>；<code>/health</code> 仍可供探针访问",
    "File-backed dashboard REST and WebSocket token for Docker or Kubernetes secrets":
      "供 Docker 或 Kubernetes Secret 使用的文件型 Dashboard REST 与 WebSocket Token",
    "Independent credential for authenticated remote <code>/api/hooks/*</code> ingestion":
      "用于认证远程 <code>/api/hooks/*</code> 采集的独立凭据",
    "Writable dotenv path used when Settings persists Claude or Codex home overrides":
      "Settings 持久化 Claude 或 Codex home 覆盖值时使用的可写 dotenv 路径",
    "Optional remote hook destination; non-loopback URLs require HTTPS and a hook token":
      "可选的远程 Hook 目标；非 loopback URL 必须使用 HTTPS 和 Hook Token",
    "Rootless Docker and Podman stacks cover the dashboard, authenticated MCP, Nginx, Prometheus, and Grafana. Read-only root filesystems, file-backed secrets, health checks, WebSocket proxying, private metrics, backup tooling, and one-writer SQLite safeguards ship together.":
      "Rootless Docker 与 Podman 栈覆盖 Dashboard、认证 MCP、Nginx、Prometheus 和 Grafana，并一起提供只读根文件系统、文件型 Secret、健康检查、WebSocket 代理、私有指标、备份工具与单 writer SQLite 保护。",
    "hooks, metrics, MCP blocked by default": "Hook、指标和 MCP 默认被阻止",
    "private bearer scrape": "私有 Bearer 抓取",
    "password file": "密码文件",
    "One SQLite writer": "一个 SQLite writer",
    "Base URL used by the local MCP server when calling dashboard APIs. Direct loopback HTTP is allowed with a bearer token; tokenized container-host aliases require HTTPS.":
      "本地 MCP 服务器调用仪表板 API 时使用的基础 URL。直接回环 HTTP 可携带 Bearer Token；带 Token 的容器主机别名必须使用 HTTPS。",
    "Bearer token for a protected dashboard. Falls back to <code>DASHBOARD_API_TOKEN</code>.":
      "受保护仪表板的 Bearer Token。未设置时回退到 <code>DASHBOARD_API_TOKEN</code>。",
    "Restore one export bundle up to 25 MiB (multipart <code>file</code> or JSON <code>{ path }</code>). The restore is idempotent and non-destructive, so it can consolidate machines.":
      "恢复一个不超过 25 MiB 的导出包（multipart <code>file</code> 或 JSON <code>{ path }</code>）。恢复过程幂等且非破坏性，因此可用于合并多台机器的数据。",
    "All dashboard requests reject redirects. Direct loopback HTTP may carry the dashboard bearer token, while tokenized container-host aliases require HTTPS. History uploads are capped at 50 MiB per file and 100 MiB per call, binary responses at 10 MiB, and backup restore at 25 MiB. Only GET requests retry automatically.":
      "所有仪表板请求都会拒绝重定向。直接回环 HTTP 可携带仪表板 Bearer Token，而带 Token 的容器主机别名必须使用 HTTPS。历史上传限制为单文件 50 MiB、每次调用合计 100 MiB，二进制响应限制为 10 MiB，备份恢复限制为 25 MiB。只有 GET 请求会自动重试。",
    "Configuration file reads canonicalize the target and allowed roots, so symlinks cannot escape the trusted Claude directories.":
      "配置文件读取会规范化目标路径和允许的根目录，因此符号链接无法逃逸受信任的 Claude 目录。",
    "Codex preview targets are canonicalized before containment checks. Edits reject symlinked path components and refuse content containing <code>[redacted]</code>.":
      "Codex 预览目标会在包含检查前规范化。编辑操作拒绝含符号链接的路径组件，并拒绝包含 <code>[redacted]</code> 的内容。",
    "Redirects are rejected rather than forwarding credentials or signed headers to another URL.":
      "系统会拒绝重定向，而不是把凭据或签名请求头转发到另一个 URL。",
    "Hosted providers require HTTPS. Generic and n8n targets may use HTTP for local or self-hosted receivers.":
      "托管提供方必须使用 HTTPS。Generic 和 n8n 目标可为本地或自托管接收方使用 HTTP。",
    "Stakeholder-ready executive, cost, reliability, and workflow reports with explicit scope and evidence":
      "面向利益相关者的执行摘要、成本、可靠性和工作流报告，保留明确范围与证据",
    "Long Analytics legends use compact pages with localized controls. Legends that fit on one page stay unchanged.":
      "较长的 Analytics 图例使用带本地化控件的紧凑分页。能放入一页的图例保持不变。",
    "Data-driven Workflows legends also paginate when labels would make a chart card too tall, while keeping every label reachable.":
      "当标签会让图表卡片过高时，数据驱动的 Workflows 图例也会分页，同时保证每个标签都可访问。",
    "First-run readiness follows the selected scope: Claude Code requires Claude hooks, Codex requires Codex hooks, and Both requires both. Ready selections open the dashboard immediately; otherwise only missing selected providers appear in setup.":
      "首次运行的就绪检查遵循所选范围：Claude Code 需要 Claude Hook，Codex 需要 Codex Hook，选择两者则两套都需要。已就绪的选择会立即打开仪表板；否则设置中只显示缺失的已选提供方。",
    "Local MCP sidecar with three transport modes and 97 typed tools across 16 domain modules. It covers the complete supported app surface, including scoped data, transcripts and images, Claude/GPT pricing, workflows, alerts, webhooks, imports and restore, Claude/Codex config, Run Agent, remote sources, push, and maintenance. Every transport shares one validated catalog with bearer-token support and tiered mutation/destructive gates.":
      "本地 MCP sidecar 支持三种传输模式，并在 16 个领域模块中提供 97 个类型化工具。它覆盖应用支持的完整操作面，包括带作用域的数据、Transcript 与图片、Claude/GPT 定价、工作流、告警、Webhook、导入与恢复、Claude/Codex 配置、Run Agent、远程数据源、推送和维护。所有传输共享同一套已验证目录，并支持 Bearer Token 与分层变更/破坏性门控。",
    "One shared marketplace ships 14 plugins for Claude Code and Codex, with 66 packaged skills, 18 Claude subagents, 34 Claude commands, 3 CLI helpers, and 2 MCP-enabled plugins. Every skill includes canonical frontmatter and OpenAI metadata. The skills.sh CLI discovers 76 repository skills from the same public source. Install with <code>claude plugin marketplace add</code>, <code>codex plugin marketplace add</code>, or <code>npx skills add</code>.":
      "同一个 Marketplace 为 Claude Code 和 Codex 提供 14 个插件，其中包含 66 个打包技能、18 个 Claude 子 Agent、34 个 Claude 命令、3 个 CLI 辅助工具和 2 个启用 MCP 的插件。每个技能都包含规范的 Frontmatter 和 OpenAI 元数据。skills.sh CLI 可从同一公开源发现 76 个仓库技能。可使用 <code>claude plugin marketplace add</code>、<code>codex plugin marketplace add</code> 或 <code>npx skills add</code> 安装。",
    "The Agent Monitor ships one shared marketplace containing 14 plugins for Claude Code and Codex, 66 packaged plugin skills, 18 Claude subagents, 34 Claude commands, 3 CLI helpers, 3 hook configurations, and 2 MCP-enabled plugins. The skills.sh CLI discovers 76 total repository skills. Product-specific manifests stay separate while skill instructions and validation remain shared.":
      "Agent Monitor 提供一个共享 Marketplace，其中包含面向 Claude Code 和 Codex 的 14 个插件、66 个打包插件技能、18 个 Claude 子 Agent、34 个 Claude 命令、3 个 CLI 辅助工具、3 个 Hook 配置以及 2 个启用 MCP 的插件。skills.sh CLI 可发现仓库中的全部 76 个技能。产品专用 Manifest 保持独立，而技能说明与验证流程保持共享。",
    "Each plugin carries product-specific Claude Code and Codex manifests. The catalogs live at <code>.claude-plugin/marketplace.json</code> and <code>.agents/plugins/marketplace.json</code>. Each plugin directory contains:":
      "每个插件都包含产品专用的 Claude Code 和 Codex Manifest。Catalog 位于 <code>.claude-plugin/marketplace.json</code> 和 <code>.agents/plugins/marketplace.json</code>。每个插件目录包含：",
    "Safe monitored Claude Code/Codex launch, follow-up, stop, resume, and run history":
      "安全地监控 Claude Code/Codex 的启动、跟进、停止、恢复和运行历史。",
    "Alert rules, webhook providers, push notifications, and SSH remote collection":
      "告警规则、Webhook 提供方、推送通知和 SSH 远程采集。",
    "Claude/Codex config, hooks, provider imports, backup restore, updates, and MCP operations":
      "Claude/Codex 配置、Hook、提供方导入、备份恢复、更新和 MCP 运维。",
    "Remote sessions never emit live hooks to this dashboard, so a healthy Claude Code or Codex provider mirror remains exempt from the local process-liveness reap and watchdog transcript scan. Every successful provider sync still reconciles live status from the matching mirrored transcript: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime); within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) keeps the session <code>active</code>, otherwise it is reconciled to <code>completed</code>. If that provider's SSH sync fails, is unavailable, or is stranded in <code>syncing</code> longer than <code>DASHBOARD_STALE_MINUTES</code> — and an active session is itself older than that same window — the ordinary stale-session sweep marks it <code>abandoned</code> and completes its agents. A later fresh mirror reactivates a session that is still writing, so unavailable providers never leave permanent Waiting cards.":
      "远程会话从不向此仪表盘发送实时 Hook，因此健康的 Claude Code 或 Codex provider 镜像仍不参与本地进程存活回收和 watchdog transcript 扫描。每次成功的 provider 同步都会从对应的镜像 transcript 协调实时状态：活动性根据 <b>JSONL 内最新事件时间戳</b>（回退到镜像 mtime）判断；位于 <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code>（默认 10 分钟）内会使会话保持 <code>active</code>，否则协调为 <code>completed</code>。若该 provider 的 SSH 同步失败、不可用，或在 <code>syncing</code> 中停滞超过 <code>DASHBOARD_STALE_MINUTES</code>，且活跃会话本身也超过同一窗口，常规 stale-session 扫描会将其标记为 <code>abandoned</code> 并完成其 agents。后续的新鲜镜像会重新激活仍在写入的会话，因此不可用 provider 不会留下永久的 Waiting 卡片。",
    '<span class="caption-icon">🛰️</span> <span><strong>Settings · Remote Data Sources</strong> — pull Claude Code and Codex activity from other machines over SSH: optionally set independent Remote Claude home and Remote Codex home paths, test each provider, sync manually or on a background poller, and switch the global data scope between local, all sources, or a specific machine with per-session source badges</span>':
      '<span class="caption-icon">🛰️</span> <span><strong>设置 · 远程数据源</strong> — 通过 SSH 从其他机器拉取 Claude Code 和 Codex 活动：可选地分别设置远程 Claude 主目录和远程 Codex 主目录，逐个测试 provider、手动或通过后台轮询器同步，并在本地、全部数据源或指定机器之间切换全局数据范围，每个会话都带有来源徽章</span>',
    '<span class="caption-icon">🧰</span> <span><strong>Codex Config Explorer</strong> — the Codex workspace brings together <code>config.toml</code>, account models, profiles, MCP servers, projects, skills, hooks, rules, plugins, and instructions. Edit supported user-managed files with timestamped backups; <code>config.toml</code> is edit-only.</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Codex 配置浏览器</strong> — Codex 工作区集中提供 <code>config.toml</code>、账户模型、配置文件、MCP 服务器、项目、技能、Hook、规则、插件和指令。支持编辑用户维护的文件，并自动创建带时间戳的备份；<code>config.toml</code> 始终仅可编辑。</span>',
    "Claude Code and Codex cards show a compact two-row history of their latest distinct human prompts beneath the provider-native title, so a short friendly name or terse follow-up never hides the active task. Claude refreshes this context from its local transcript cache during live hooks, imports, and watchdog sweeps; Codex refreshes it from rollout records and falls back to persisted <code>user_message</code> events for older imports. The transcript renders persisted PNG/JPEG/GIF/WebP attachments for Claude Code and Codex when available, while duplicate Codex response/event copies collapse into one human turn.":
      "Claude Code 和 Codex 卡片都会在各自提供方原生标题下显示最近不同用户提示的紧凑两行历史，因此简短友好的名称或简短跟进都不会隐藏当前任务。Claude 会在实时 Hook、导入和 watchdog 扫描期间从本地 transcript 缓存刷新该上下文；Codex 会从 rollout 记录刷新，并为旧导入回退到持久化的 <code>user_message</code> 事件。Transcript 会在可用时渲染 Claude Code 和 Codex 已持久化的 PNG/JPEG/GIF/WebP 附件，并将 Codex 重复的 response/event 副本合并为一条用户回合。",
    "<strong>Run Agent</strong> now begins by choosing Claude Code or Codex. Codex starts a native interactive <code>app-server</code> thread, offers provider-native approval and sandbox controls, and discovers models directly from the signed-in CLI so new releases appear without a dashboard update.":
      "<strong>运行 Agent</strong>现在会先选择 Claude Code 或 Codex。Codex 会启动原生交互式 <code>app-server</code> 线程，提供提供方原生审批与沙箱控制，并直接从已登录 CLI 发现模型，因此新版本无需更新仪表盘即可出现。",
    "The same page also has a <strong>Codex</strong> switch. Its workspace shows defaults, the model cache, profiles, MCP servers, projects, skills, rules, hooks, installed plugins, and instruction files from <code>CODEX_HOME</code>. Plugin cards use <code>codex plugin list</code> rather than exposing cache folders. Secret-like TOML and JSON values are redacted in previews; an explicit local editor supports <code>config.toml</code>, <code>hooks.json</code>, user rules, skills, and instruction files with atomic saves and timestamped backups. It warns that syntax is not validated, and <code>codex_config_changed</code> refreshes the view when those files change.":
      "同一页面还有 <strong>Codex</strong> 切换项。其工作区会显示 <code>CODEX_HOME</code> 中的默认值、模型缓存、配置文件、MCP 服务器、项目、技能、规则、Hook、已安装插件和指令文件。插件卡片使用 <code>codex plugin list</code>，不会暴露缓存文件夹。预览中的类似密钥的 TOML 和 JSON 值会脱敏；显式本地编辑器支持 <code>config.toml</code>、<code>hooks.json</code>、用户规则、技能和指令文件，并以原子保存和带时间戳的备份保护。它会提示不会验证语法，文件变化时 <code>codex_config_changed</code> 会刷新视图。",
    "The same page also has a <strong>Codex</strong> switch. Its workspace shows defaults, the full local account model catalog, profiles, MCP servers, projects, skills, rules, hooks, installed plugins, and instruction files from <code>CODEX_HOME</code>. The model catalog is read apart from the small redacted-preview limit, so long account instructions cannot make the Models tab report zero. Create a standard named <code>&lt;name&gt;.config.toml</code> profile in place; Codex overlays it only for <code>codex --profile &lt;name&gt;</code>, and its card copies that exact command in one click. Plugin cards use <code>codex plugin list</code> rather than exposing cache folders. Secret-like TOML and JSON values are redacted in previews; an explicit local editor supports <code>config.toml</code>, profile overlays, <code>hooks.json</code>, user rules, skills, and instruction files with atomic saves and timestamped backups. User-maintained profiles, hooks, rules, skills, and instructions also have confirmed backup-backed <strong>View source / Copy path / Edit / Delete</strong> actions; a skill deletion preserves its whole directory, while <code>config.toml</code> stays edit-only. It warns that syntax is not validated, and <code>codex_config_changed</code> refreshes the view when those files change.":
      "同一页面还有 <strong>Codex</strong> 切换项。其工作区会显示 <code>CODEX_HOME</code> 中的默认值、完整的本地账户模型目录、配置文件、MCP 服务器、项目、技能、规则、Hook、已安装插件和指令文件。模型目录与较小的脱敏预览限制分开读取，因此很长的账户指令不会让 Models 标签页错误显示为零。可直接创建标准的命名 <code>&lt;name&gt;.config.toml</code> 配置文件；Codex 仅在使用 <code>codex --profile &lt;name&gt;</code> 时叠加它，配置文件卡片可一键复制该准确命令。插件卡片使用 <code>codex plugin list</code>，不会暴露缓存文件夹。预览中的类似密钥的 TOML 和 JSON 值会脱敏；显式本地编辑器支持 <code>config.toml</code>、配置文件叠加、<code>hooks.json</code>、用户规则、技能和指令文件，并以原子保存和带时间戳的备份保护。用户维护的配置文件、Hook、规则、技能和指令还提供经确认、带备份的 <strong>查看源文件 / 复制路径 / 编辑 / 删除</strong> 操作；删除技能会保留其整个目录，而 <code>config.toml</code> 始终只能编辑。它会提示不会验证语法，文件变化时 <code>codex_config_changed</code> 会刷新视图。",
    '<span class="caption-icon">🧰</span> <span><strong>Agent Config</strong> — switch between the editable Claude Code explorer and a live Codex workspace for defaults, models, profiles, MCP, projects, skills, rules, hooks, plugins, and instructions. Previews redact secret-like values; user-maintained files receive backup-backed edits.</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Agent 配置</strong> — 可在可编辑的 Claude Code 浏览器与实时 Codex 工作区之间切换，查看默认值、模型、配置文件、MCP、项目、技能、规则、Hook、插件和指令。预览会脱敏；用户维护的文件可获得带备份的编辑。</span>',
    '<span class="caption-icon">▶️</span> <span><strong>Run Agent</strong> — choose Claude Code or Codex each time you open the launcher. Claude supports Conversation or Headless mode; Codex starts a native interactive thread with approval and sandbox controls.</span>':
      '<span class="caption-icon">▶️</span> <span><strong>运行 Agent</strong> — 每次打开启动器时选择 Claude Code 或 Codex。Claude 支持对话或单次模式；Codex 启动带审批与沙箱控制的原生交互线程。</span>',
    '<span class="caption-icon">💬</span> <span><strong>Run Agent · live stream</strong> — Claude stream-json and Codex app-server events render together as chat, including reasoning, commands, file changes, and tool activity. Dashboard Runs lets you re-attach later.</span>':
      '<span class="caption-icon">💬</span> <span><strong>运行 Agent · 实时流</strong> — Claude stream-json 与 Codex app-server 事件会以聊天形式一同呈现，包括推理、命令、文件变更和工具活动。Dashboard Runs 支持稍后重新连接。</span>',
    "Codex also reads its native session index, so <code>/rename</code> titles reach session and agent cards in real time. Its conversation reader includes human turns plus <code>exec</code> custom-tool calls and outputs, and loads older pages when you reach the top of the transcript.":
      "Codex 还会读取其原生会话索引，因此 <code>/rename</code> 标题会实时显示在会话和 agent 卡片上。其对话读取器包含用户回合以及 <code>exec</code> 自定义工具调用和输出，并会在到达 transcript 顶部时加载更早的页面。",
    "Optional Codex home. Settings saves this dashboard-only override, re-arms live rollout watching, and immediately scans the new <code>sessions/</code> tree.":
      "可选的 Codex 主目录。设置会保存此仪表盘专用覆盖、重新启用实时 rollout 监控，并立即扫描新的 <code>sessions/</code> 树。",
    "<strong>Dashboard Data</strong> lets you switch the entire app between Claude Code, Codex, or both. Sessions, agents, events, token totals, workflows, analytics, and costs refetch immediately. The same page saves separate Claude Code and Codex homes; saving the Codex home re-arms live rollout watching and performs a fresh scan.":
      "<strong>仪表盘数据</strong>允许你在 Claude Code、Codex 或两者之间切换整个应用。会话、agent、事件、token 总数、工作流、分析和成本会立即重新获取。同一页面可分别保存 Claude Code 和 Codex 主目录；保存 Codex 主目录会重新启用实时 rollout 监控并执行一次全新扫描。",
    "The manual installer is interactive: Claude Code starts selected; use arrow keys, <kbd>Space</kbd>, then <kbd>Enter</kbd> to choose Claude Code, Codex (beta), or both. If a selected dashboard hook is already present, it warns before replacing only that entry and preserves unrelated hooks. The same guided installer is available from <strong>Settings → Hook Configuration</strong>. On the first dashboard visit, choose a data source, then use the provider-locked live-monitoring setup gate to install the selected hooks in place and review its output. You can also explicitly confirm hooks are already installed before continuing.":
      "手动安装器是交互式的：默认选中 Claude Code；使用方向键、<kbd>空格</kbd> 和 <kbd>Enter</kbd> 选择 Claude Code、Codex（测试版）或两者。若已存在所选的仪表盘 Hook，安装器会在仅替换该条目前发出警告，并保留无关 Hook。同一引导式安装器也可在 <strong>设置 → Hook 配置</strong> 中使用。首次进入仪表板时，请选择数据来源，然后使用与提供方绑定的实时监控设置引导，在原处安装所选 Hook 并查看输出。您也可以明确确认 Hook 已安装后再继续。",
    "Codex rollouts are accounted separately with fresh, cached, cache-write, output, and reasoning-output deltas. The GPT rate card distinguishes standard short context (≤272K input tokens), long context, and Fast mode; unavailable published tiers remain explicitly unpriced until you set a rate.":
      "Codex rollout 会按新的输入、缓存输入、缓存写入、输出和推理输出增量分别计费。GPT 费率表区分标准短上下文（输入 token ≤272K）、长上下文和 Fast 模式；尚未公布价格的层级会明确显示为未定价，直到你设置费率。",
    "Codex rollout records are the source of truth for live cards: <code>user_message</code> and <code>task_started</code> mark the main agent working; <code>task_complete</code> keeps the session active while showing Waiting; and <code>turn_aborted</code> shows interrupted Waiting. Newest rollouts are processed first, and an unreadable historical file is retried without delaying the rest.":
      "Codex rollout 记录是实时卡片的唯一事实来源：<code>user_message</code> 和 <code>task_started</code> 将主 agent 标记为工作中；<code>task_complete</code> 保持会话 active，同时显示等待中；<code>turn_aborted</code> 显示中断的等待中。最新 rollout 优先处理，无法读取的历史文件会重试且不延迟其他文件。",
    "Codex <code>response_item</code> tool calls are indexed exactly once through a separate rollout cursor, so provider-scoped Workflows shows recorded tool flows, session drill-ins, token/model totals, and <code>context_compacted</code> counts without replaying lifecycle or token accounting. Dynamic Workflows stays hidden in a Codex-only scope because its on-disk journals are a Claude Code feature.":
      "Codex <code>response_item</code> 工具调用通过独立 rollout cursor 精确索引一次，因此按 provider 范围的 Workflows 会显示已记录的工具流、会话 drill-in、Token/模型总计和 <code>context_compacted</code> 次数，而不会重放生命周期或 Token 计费。Dynamic Workflows 在仅 Codex 范围内保持隐藏，因为其磁盘 journal 是 Claude Code 功能。",
    "Optional Codex home. Defaults to <code>CODEX_HOME</code> or <code>~/.codex</code>.":
      "可选的 Codex 主目录。默认使用 <code>CODEX_HOME</code> 或 <code>~/.codex</code>。",
    "Safety-net interval (ms) for incremental Codex rollout scanning. Default <code>4000</code>; <code>0</code> disables polling but keeps the watcher.":
      "增量扫描 Codex rollout 的兜底间隔（毫秒）。默认 <code>4000</code>；<code>0</code> 会禁用轮询但保留文件监视器。",
    "How long a hook-only Codex session may leave a reported-finished turn unanswered before its <code>SessionEnd</code> is presumed lost. A run started with <code>codex exec --ephemeral</code> writes no rollout, so it has neither a transcript mtime nor an open file for the liveness probes — the fallback that matters most on Windows, where both probes are unavailable. Only a session already parked by a real <code>Stop</code> hook qualifies, because <code>SessionEnd</code> follows <code>Stop</code> within a few hundred milliseconds. Silence never qualifies: a rollout-less run emits no hooks at all while a tool runs, so an idle timer would complete a live CI build. A session still running self-heals on its next hook.":
      "仅靠 hook 的 Codex 会话在已报告结束的回合迟迟得不到响应时，可等待多久才判定其 <code>SessionEnd</code> 已丢失。以 <code>codex exec --ephemeral</code> 启动的运行不会写入 rollout，因此既没有 transcript mtime，也没有可供存活探测使用的打开文件——这一兜底在两种探测均不可用的 Windows 上尤为重要。只有已被真实 <code>Stop</code> hook 停放的会话才符合条件，因为 <code>SessionEnd</code> 会在 <code>Stop</code> 之后几百毫秒内到达。静默永远不算条件：没有 rollout 的运行在工具执行期间完全不发出 hook，空闲计时器会误将正在运行的 CI 构建判定为已完成。实际仍在运行的会话会在下一个 hook 时自动修复。",
    '<span class="caption-icon">📊</span> <span><strong>Observability · Grafana</strong> — default home dashboard <em>CCAM — Overview</em> (four boards auto-provisioned): live fleet snapshot, database totals, breakdown charts, and rates from <code>/api/metrics</code> scrapes. The npm-only local helper uses <code>admin</code> / <code>admin</code>; container stacks read the admin password from <code>deployments/secrets/grafana-admin-password</code>.</span>':
      '<span class="caption-icon">📊</span> <span><strong>可观测性 · Grafana</strong> — 默认首页仪表盘 <em>CCAM — Overview</em>（自动配置四个看板）：实时舰队快照、数据库累计总量、分解图与速率，来自 <code>/api/metrics</code> 抓取。仅 npm 本地辅助栈使用 <code>admin</code> / <code>admin</code>；容器栈从 <code>deployments/secrets/grafana-admin-password</code> 读取管理员密码。</span>',
    '<span class="caption-icon">🔥</span> <span><strong>Observability · Prometheus console</strong> — pre-built landing page at <code>/consoles/index.html</code> with live metric cards, session/token tables, and drill-down links into the Graph UI (Prometheus 3.x compatible — queries the HTTP API directly)</span>':
      '<span class="caption-icon">🔥</span> <span><strong>可观测性 · Prometheus 控制台</strong> — <code>/consoles/index.html</code> 预置落地页：实时指标卡片、会话/Token 表，以及钻取到 Graph UI 的链接（兼容 Prometheus 3.x — 直接查询 HTTP API）</span>',
    '<span class="caption-icon">📈</span> <span><strong>Observability · Prometheus Graph</strong> — run PromQL against scraped CCAM series (e.g. <code>sum(ccam_sessions)</code>, <code>ccam_events_total</code>, <code>rate(ccam_tokens_total[5m])</code>) with starter expressions from the CCAM console and recording rules in <code>monitoring/prometheus/ccam-rules.yml</code></span>':
      '<span class="caption-icon">📈</span> <span><strong>可观测性 · Prometheus Graph</strong> — 对已抓取的 CCAM 序列运行 PromQL（如 <code>sum(ccam_sessions)</code>、<code>ccam_events_total</code>、<code>rate(ccam_tokens_total[5m])</code>），入门表达式见 CCAM 控制台与 <code>monitoring/prometheus/ccam-rules.yml</code> 记录规则</span>',
    '<span class="caption-icon">📊</span> <span><strong>Grafana · CCAM — Overview</strong> — default home dashboard after login on port 3000: fleet snapshot, database totals, breakdown charts, and rates from live <code>/api/metrics</code> scrapes — no sample or synthetic data. The npm-only local helper uses <code>admin</code> / <code>admin</code>; container stacks use <code>deployments/secrets/grafana-admin-password</code>.</span>':
      '<span class="caption-icon">📊</span> <span><strong>Grafana · CCAM — Overview</strong> — 登录后端口 3000 的默认首页：舰队快照、数据库累计、分解图与速率，全部来自实时 <code>/api/metrics</code> 抓取 — 无示例或合成数据。仅 npm 本地辅助栈使用 <code>admin</code> / <code>admin</code>；容器栈使用 <code>deployments/secrets/grafana-admin-password</code>。</span>',
    '<span class="caption-icon">🔥</span> <span><strong>Prometheus · CCAM console</strong> — static HTML at <code>/consoles/index.html</code> (Prometheus 3.x compatible): live metric cards, session/token tables, and one-click Graph drill-down links that query the Prometheus HTTP API directly</span>':
      '<span class="caption-icon">🔥</span> <span><strong>Prometheus · CCAM 控制台</strong> — <code>/consoles/index.html</code> 静态 HTML（兼容 Prometheus 3.x）：实时指标卡片、会话/Token 表，以及一键钻取 Graph 的链接，直接查询 Prometheus HTTP API</span>',
    '<span class="caption-icon">📈</span> <span><strong>Prometheus · Graph</strong> — ad-hoc PromQL against scraped CCAM series; starter expressions ship in the CCAM console and <code>monitoring/README.md</code>, with derived rates in <code>monitoring/prometheus/ccam-rules.yml</code></span>':
      '<span class="caption-icon">📈</span> <span><strong>Prometheus · Graph</strong> — 对已抓取 CCAM 序列运行即席 PromQL；入门表达式见 CCAM 控制台与 <code>monitoring/README.md</code>，派生速率见 <code>monitoring/prometheus/ccam-rules.yml</code></span>',
    "CCAM exposes a read-only <strong>Prometheus text-exposition</strong> endpoint at <code>GET /api/metrics</code> — sessions and agents by status, cumulative events and tokens, connected WebSocket clients, configured remote sources, process uptime/RSS, and build version — so the same dashboard you use for Claude Code monitoring can be scraped into your own observability stack. A turnkey stack lives in <code>monitoring/</code>: npm-managed binaries (no Homebrew/apt) or optional Docker Compose, with Grafana datasource and four dashboards auto-provisioned on first start.":
      "CCAM 在 <code>GET /api/metrics</code> 提供只读的 <strong>Prometheus 文本暴露</strong> 端点 — 按状态的 sessions/agents、累计事件与 token、已连接 WebSocket 客户端、已配置远程源、进程运行时长/RSS 与构建版本 — 因此用于 Claude Code 监控的同一仪表盘可被抓取到你自己的可观测性栈。<code>monitoring/</code> 中有开箱即用组合：npm 管理二进制（无需 Homebrew/apt）或可选 Docker Compose，首次启动自动配置 Grafana 数据源与四个仪表盘。",
    "<strong>Quick start (npm):</strong> run the dashboard on loopback, then <code>npm run monitoring:install</code> once and <code>npm run monitoring:up</code>. Open Grafana at <code>http://localhost:3000</code> and Prometheus at <code>http://localhost:9090</code>. The pre-built CCAM console is at <code>http://localhost:9090/consoles/index.html</code> (also reachable from the Prometheus UI under <strong>Consoles</strong>). For an all-Docker path use <code>npm run monitoring:docker:up</code> or <code>npm run docker:full:up</code> for dashboard + Prometheus + Grafana together. Verify with <code>npm run monitoring:verify</code> — the <code>ccam</code> scrape target should read <strong>UP</strong>.":
      "<strong>快速开始（npm）：</strong>在回环地址运行仪表盘，然后执行一次 <code>npm run monitoring:install</code> 与 <code>npm run monitoring:up</code>。Grafana：<code>http://localhost:3000</code>，Prometheus：<code>http://localhost:9090</code>。预置 CCAM 控制台：<code>http://localhost:9090/consoles/index.html</code>（也可在 Prometheus UI 的 <strong>Consoles</strong> 下打开）。全 Docker 路径：<code>npm run monitoring:docker:up</code> 或 <code>npm run docker:full:up</code>。用 <code>npm run monitoring:verify</code> 验证 — <code>ccam</code> 抓取目标应为 <strong>UP</strong>。",
    "Four Grafana boards": "四个 Grafana 看板",
    "<strong>CCAM — Overview</strong> (default home), <strong>CCAM — Sessions &amp; Agents</strong>, <strong>CCAM — Tokens &amp; Events</strong>, and <strong>CCAM — Platform</strong> — all provisioned from <code>monitoring/grafana/dashboards/</code> with PromQL against your live scrape. The npm-only local helper uses <code>admin</code> / <code>admin</code> via <code>monitoring/grafana.defaults.env</code>. Container stacks read the password from <code>deployments/secrets/grafana-admin-password</code>.":
      "<strong>CCAM — Overview</strong>（默认首页）、<strong>CCAM — Sessions &amp; Agents</strong>、<strong>CCAM — Tokens &amp; Events</strong> 与 <strong>CCAM — Platform</strong> — 均从 <code>monitoring/grafana/dashboards/</code> 配置，PromQL 针对实时抓取。仅 npm 本地辅助栈通过 <code>monitoring/grafana.defaults.env</code> 使用 <code>admin</code> / <code>admin</code>；容器栈从 <code>deployments/secrets/grafana-admin-password</code> 读取密码。",
    "Prometheus console": "Prometheus 控制台",
    "Prometheus 3.x dropped the legacy console template libraries, so CCAM ships a <strong>static HTML console</strong> at <code>monitoring/prometheus/consoles/index.html</code> that fetches <code>/api/v1/query</code> directly — live cards, session tables, and Graph links without Go-template errors on <code>/consoles/index.html</code>.":
      "Prometheus 3.x 移除了旧版控制台模板库，因此 CCAM 在 <code>monitoring/prometheus/consoles/index.html</code> 提供<strong>静态 HTML 控制台</strong>，直接请求 <code>/api/v1/query</code> — 实时卡片、会话表与 Graph 链接，<code>/consoles/index.html</code> 不再出现 Go 模板错误。",
    "Recording rules": "记录规则",
    "<code>monitoring/prometheus/ccam-rules.yml</code> derives convenient rates and aggregates from raw <code>ccam_*</code> series (e.g. <code>rate(ccam_tokens_total[5m])</code>) so Grafana panels and ad-hoc Graph queries stay fast without hand-rolling every expression.":
      "<code>monitoring/prometheus/ccam-rules.yml</code> 从原始 <code>ccam_*</code> 序列派生便捷速率与聚合（如 <code>rate(ccam_tokens_total[5m])</code>），使 Grafana 面板与即席 Graph 查询无需手写每条表达式。",
    "Scrape &amp; auth guard": "抓取与认证防护",
    "Loopback scrapes (<code>127.0.0.1:4820</code>) work out of the box. When Prometheus runs in Docker and reaches the host via <code>host.docker.internal</code>, add that host to <code>DASHBOARD_ALLOWED_HOSTS</code> or the scrape returns <code>403 EBADHOST</code>. If <code>DASHBOARD_TOKEN</code> is set, scrapers must send the same bearer token.":
      "回环抓取（<code>127.0.0.1:4820</code>）开箱即用。Prometheus 在 Docker 中通过 <code>host.docker.internal</code> 访问主机时，须将该主机加入 <code>DASHBOARD_ALLOWED_HOSTS</code>，否则返回 <code>403 EBADHOST</code>。若设置了 <code>DASHBOARD_TOKEN</code>，抓取器须发送相同 bearer token。",
    "Full reference": "完整参考",
    'Commands, Docker vs npm paths, dashboard UIDs, starter PromQL, and troubleshooting (Grafana login reset, target DOWN) are documented in <a href="../monitoring/README.md"><code>monitoring/README.md</code></a> and the <a href="#api-reference">API reference</a> entry for <code>GET /api/metrics</code>.':
      '命令、Docker 与 npm 路径、仪表盘 UID、入门 PromQL 与故障排查（Grafana 登录重置、目标 DOWN）见 <a href="../monitoring/README.md"><code>monitoring/README.md</code></a> 与 <a href="#api-reference">API 参考</a> 中的 <code>GET /api/metrics</code> 条目。',
    "Pull Claude Code history from other machines over SSH. Each remote's <code>~/.claude/projects</code> tree is mirrored via <code>scp</code> into a sandboxed staging directory, fed through the same importer as local history, and every session is tagged with its source. A background poller keeps remote data near-real-time, a global data-scope selector (Local only / All / a specific source) narrows the whole app, and sessions from a remote show a source badge. Auth defers entirely to the host's SSH stack — no passwords or secrets are stored. Managed in <b>Settings → Remote Data Sources</b>.":
      "通过 SSH 从其他机器拉取 Claude Code 历史。每个远程主机的 <code>~/.claude/projects</code> 目录树通过 <code>scp</code> 镜像到沙箱暂存目录,经由与本地历史相同的导入器处理,每个会话都会标记其来源。后台轮询器让远程数据保持近实时,全局数据范围选择器(仅本机 / 全部 / 指定来源)可缩小整个应用的范围,来自远程的会话会显示来源徽章。认证完全委托给主机的 SSH 栈——不存储任何密码或密钥。在 <b>Settings → Remote Data Sources</b> 中管理。",
    "Remote Data Sources": "远程数据源",
    "One remote source can independently import <b>Claude Code</b>, <b>Codex</b>, or both. Codex mirrors <code>~/.codex/sessions</code> and its lightweight <code>session_index.jsonl</code> title index; the connection test and sync result show a separate state for each provider, so a missing provider never hides fresh data from the other.":
      "一个远程来源可独立导入 <b>Claude Code</b>、<b>Codex</b> 或两者。Codex 会镜像 <code>~/.codex/sessions</code> 及其轻量 <code>session_index.jsonl</code> 标题索引；连接测试和同步结果会显示每个提供方各自的状态，因此缺失一个提供方不会隐藏另一个提供方的新鲜数据。",
    "List, add, and test SSH data sources; sync one source (or all when no id is given); and remove a source, optionally purging the sessions it imported":
      "列出、添加并测试 SSH 数据源;同步单个来源(未提供 id 时同步全部);以及移除某个来源,可选地清除它导入的会话",
    "Beyond local history, the dashboard can pull Claude Code activity from <b>other machines over SSH</b> and fold it into the same views as your local sessions. Each remote's <code>~/.claude/projects</code> tree is mirrored via <code>scp</code> (or <code>wsl.exe</code> + <code>tar</code> when Claude runs in WSL on a Windows SSH host) into a sandboxed staging directory, fed through the exact same importer used for local history, and every imported session is tagged with the source it came from. A background poller (<code>DASHBOARD_REMOTE_SYNC_MS</code>, default <code>15000</code> ms) keeps remote data near-real-time — adding or re-enabling a source also triggers an immediate pull — and successful syncs broadcast <code>remote_data.updated</code> plus per-session <code>session_created</code> / <code>session_updated</code> frames. A global <b>data scope</b> selector lets you narrow the entire app to local sessions only, all sources, or a specific machine. Sources are managed in <b>Settings → Remote Data Sources</b>.":
      "除了本地历史,仪表盘还可以 <b>通过 SSH 从其他机器</b> 拉取 Claude Code 活动,并将其融入与本地会话相同的视图中。每个远程主机的 <code>~/.claude/projects</code> 目录树通过 <code>scp</code> 镜像(Windows SSH 上 Claude 在 WSL 中时使用 <code>wsl.exe</code> + <code>tar</code>)到沙箱暂存目录,经由与本地历史完全相同的导入器处理,每个导入的会话都会标记其来源。后台轮询器(<code>DASHBOARD_REMOTE_SYNC_MS</code>,默认 <code>15000</code> ms)保持近实时——添加或重新启用来源也会立即同步——成功同步会广播 <code>remote_data.updated</code> 以及逐会话的 <code>session_created</code> / <code>session_updated</code> 帧。全局 <b>数据范围</b> 选择器可将整个应用缩小到仅本地会话、全部来源或某台特定机器。来源在 <b>Settings → Remote Data Sources</b> 中管理。",
    "Each remote is mirrored over SSH, imported through the local pipeline, and tagged by source — the poller (default 15 s) keeps it near-real-time":
      "每个远程主机通过 SSH 镜像、经由本地流水线导入并按来源标记——轮询器(默认 15 秒)让其保持近实时",
    "Add a source with an SSH destination — either a <code>user@host</code> pair or an alias defined in your <code>~/.ssh/config</code> — plus an optional port, identity file, and remote home directory. Use <b>Test connection</b> to verify reachability before saving, then <b>Sync now</b> to pull immediately, or let the background poller do it on its own schedule. Removing a source can optionally purge the sessions it imported.":
      "添加来源时提供一个 SSH 目标——可以是 <code>user@host</code> 组合,也可以是你 <code>~/.ssh/config</code> 中定义的别名——外加可选的端口、身份文件和远程主目录。保存前用 <b>Test connection</b> 验证可达性,然后用 <b>Sync now</b> 立即拉取,或让后台轮询器按自己的计划完成。移除来源时可选地清除它导入的会话。",
    "A global data-scope selector narrows the whole app: choose <b>Local only</b>, <b>All</b> sources, or one specific machine. The scope flows into every data view through a <code>sources</code> query parameter on the sessions, events, agents, stats, and analytics endpoints, and sessions imported from a remote carry a <b>source badge</b> so their origin is always visible.":
      "全局数据范围选择器可缩小整个应用的范围:选择 <b>Local only</b>、<b>All</b> 来源,或某台特定机器。该范围通过 sessions、events、agents、stats 和 analytics 端点上的 <code>sources</code> 查询参数流入每个数据视图,来自远程的会话会带有 <b>source badge</b>,使其来源始终可见。",
    "Authentication defers entirely to the host's own SSH stack — <code>~/.ssh/config</code>, <code>ssh-agent</code>, on-disk keys, and <code>known_hosts</code>. The dashboard stores <b>no passwords or secrets</b>; it only records the SSH destination and connection options you supply. <code>StrictHostKeyChecking</code> is left at its default, so the remote host must already be present in <code>known_hosts</code> — an unknown host fails the connection rather than being trusted blindly.":
      "认证完全委托给主机自身的 SSH 栈——<code>~/.ssh/config</code>、<code>ssh-agent</code>、磁盘上的密钥以及 <code>known_hosts</code>。仪表盘 <b>不存储任何密码或密钥</b>,只记录你提供的 SSH 目标和连接选项。<code>StrictHostKeyChecking</code> 保持默认值,因此远程主机必须已存在于 <code>known_hosts</code> 中——未知主机会导致连接失败,而不会被盲目信任。",
    "Interval for the background remote poller (add/re-enable also syncs immediately); <code>0</code> disables the poller while leaving manual sync available":
      "后台远程轮询器的间隔(添加或重新启用来源也会立即同步);<code>0</code> 会禁用轮询器,但仍保留手动同步",
    "Ceiling on a single scp-and-import run before it is aborted":
      "单次 scp 与导入运行在被中止前的上限",
    "Timeout for the <b>Test connection</b> probe": "<b>Test connection</b> 探测的超时时间",
    "Freshness window (10 min) for a Remote Data Source session's live status — judged from the newest JSONL event timestamp in the mirror (mtime fallback); remote sessions receive no live hooks, so this replaces the local liveness and stale sweeps (which skip them)":
      "远程数据源会话实时状态的新鲜度窗口(10 分钟)——依据镜像 JSONL 中最新事件时间戳判断(回退到镜像 mtime);远程会话不接收实时 hook,因此这会取代本地的存活与陈旧扫描(它们会跳过远程会话)",
    "Freshness window (10 min) for a Remote Data Source session's live status — judged from the newest JSONL event timestamp in the mirror (mtime fallback); healthy sources stay out of local liveness/stale checks, while errored or stale-syncing sources fall back to the global abandon sweep":
      "远程数据源会话实时状态的新鲜度窗口（10 分钟）——依据镜像 JSONL 中最新事件时间戳判断（回退到镜像 mtime）；健康来源不参与本地存活/陈旧检查，而出错或长期 syncing 的来源会回退到全局放弃扫描",
    "Because a remote session is pulled over SSH, it never emits live hooks to this dashboard, so remote sessions (<code>source</code> ≠ <code>local</code>) are excluded from every local liveness and stale sweep — the process liveness reap, the watchdog transcript scan, the startup cleanup, and the periodic abandon sweep all skip them. Instead, a remote session's live status is reconciled from the mirrored transcript on every sync: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime). If the last event is within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) the session stays <code>active</code>; once the mirror stops advancing for longer than that window, the session is reconciled to <code>completed</code>. This keeps a still-running remote session from being wrongly shown as Completed.":
      "由于远程会话是通过 SSH 拉取的,它从不向本仪表盘发送实时 hook,因此远程会话(<code>source</code> ≠ <code>local</code>)会被排除在每一次本地存活与陈旧扫描之外——进程存活回收、看门狗 transcript 扫描、启动清理以及周期性放弃扫描都会跳过它们。取而代之的是,远程会话的实时状态会在每次同步时从镜像 transcript 进行调和:活动性依据 <b>JSONL 内最新事件时间戳</b> 判断(回退到镜像 mtime)。若最后一条事件在 <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code>(默认 10 分钟)内,会话保持 <code>active</code>;一旦镜像在超过该窗口的时间内不再推进,会话就会被调和为 <code>completed</code>。这可以避免仍在运行的远程会话被错误地显示为 Completed。",
    "Remote sessions never emit live hooks to this dashboard, so a healthy source remains exempt from the local process-liveness reap and watchdog transcript scan. Every successful sync still reconciles live status from the mirrored transcript: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime); within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) keeps the session <code>active</code>, otherwise it is reconciled to <code>completed</code>. If an SSH sync fails — or a source is stranded in <code>syncing</code> longer than <code>DASHBOARD_STALE_MINUTES</code> — and an active session is itself older than that same window, the ordinary stale-session sweep marks it <code>abandoned</code> and completes its agents. A later fresh mirror reactivates a session that is still writing, so unavailable remotes never leave permanent Waiting cards.":
      "远程会话从不向本仪表盘发送实时 hook，因此健康来源仍被排除在本地进程存活回收和看门狗 transcript 扫描之外。每次成功同步仍会从镜像 transcript 对实时状态进行协调：活动性依据 <b>JSONL 内最新事件时间戳</b>（回退到镜像 mtime）判断；位于 <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code>（默认 10 分钟）内会使会话保持 <code>active</code>，否则会协调为 <code>completed</code>。若 SSH 同步失败——或来源在 <code>syncing</code> 状态停滞超过 <code>DASHBOARD_STALE_MINUTES</code>——且活跃会话本身也超过同一窗口，常规陈旧会话扫描会将其标记为 <code>abandoned</code> 并完成其 agents。后续的新鲜镜像会重新激活仍在写入的会话，因此不可用的远程来源不会留下永久的等待中卡片。",
    "List configured sources with their status": "列出已配置的来源及其状态",
    "Add a source — SSH destination plus optional port, identity file, and remote home":
      "添加来源——SSH 目标外加可选的端口、身份文件和远程主目录",
    "Edit an existing source": "编辑现有来源",
    "Remove a source; optionally purge the sessions it imported":
      "移除某个来源;可选地清除它导入的会话",
    "Test SSH reachability without importing anything": "测试 SSH 可达性而不导入任何内容",
    "Trigger an immediate scp-and-import for that source": "为该来源触发一次立即的 scp 与导入",
    "The <code>sources</code> query parameter is also accepted on <code>/api/sessions</code>, <code>/api/events</code>, <code>/api/agents</code>, <code>/api/stats</code>, and <code>/api/analytics</code> to scope any view to <code>local</code>, <code>all</code>, or a comma-separated list of source IDs.":
      "<code>sources</code> 查询参数在 <code>/api/sessions</code>、<code>/api/events</code>、<code>/api/agents</code>、<code>/api/stats</code> 和 <code>/api/analytics</code> 上同样受支持,可将任意视图限定为 <code>local</code>、<code>all</code> 或以逗号分隔的来源 ID 列表。",
    "Sync progress and connection state broadcast on <code>/ws</code> as <code>remote_source.status</code> messages, so the Settings panel and the source badges update live as a remote connects, syncs, or fails.":
      "同步进度和连接状态会作为 <code>remote_source.status</code> 消息在 <code>/ws</code> 上广播,因此当远程主机连接、同步或失败时,设置面板和来源徽章会实时更新。",
    "The same operations are available from the terminal via <code>ccam remote-sources</code>, with subcommands <code>list</code>, <code>add</code>, <code>test &lt;id&gt;</code>, <code>sync [id]</code> (all sources when no id is given), and <code>rm &lt;id&gt; [--purge]</code> to remove a source and optionally drop the sessions it imported.":
      "相同的操作也可以在终端通过 <code>ccam remote-sources</code> 完成,其子命令包括 <code>list</code>、<code>add</code>、<code>test &lt;id&gt;</code>、<code>sync [id]</code>(未提供 id 时同步全部来源),以及 <code>rm &lt;id&gt; [--purge]</code>——移除某个来源并可选地删除它导入的会话。",
    "Local-only by default": "默认仅限本机访问",
    "The image bakes <code>DASHBOARD_HOST=0.0.0.0</code> (the server binds loopback by default, but a container's loopback is a separate namespace the published port cannot reach) and <code>DASHBOARD_DATA_DIR=/app/data</code> (the read-only <code>~/.claude</code> mount cannot hold the SQLite data dir). The trust boundary is the host port publish: the examples use <code>-p 127.0.0.1:4820:4820</code>, so the dashboard is local-only. To expose it on a LAN, publish on <code>0.0.0.0</code> (<code>-p 4820:4820</code>) and set <code>DASHBOARD_TOKEN</code>.":
      "镜像内置了 <code>DASHBOARD_HOST=0.0.0.0</code>（服务器默认绑定回环地址，但容器的回环地址是独立命名空间，发布的端口无法访问它）和 <code>DASHBOARD_DATA_DIR=/app/data</code>（只读的 <code>~/.claude</code> 挂载无法存放 SQLite 数据目录）。信任边界是主机端口发布：示例使用 <code>-p 127.0.0.1:4820:4820</code>，因此仪表盘仅限本机访问。若要在局域网中暴露，请发布到 <code>0.0.0.0</code>（<code>-p 4820:4820</code>）并设置 <code>DASHBOARD_TOKEN</code>。",
    "The CLI also manages the server itself: <code>ccam status</code> shows an up/down indicator and <code>ccam start</code> boots a detached production server. When the server is down, read-only commands fall back to reading <code>data/dashboard.db</code> directly under an explicit offline banner, while live or server-computed commands refuse with the specific reason.":
      "CLI \u4e5f\u80fd\u7ba1\u7406\u670d\u52a1\u5668\u672c\u8eab\uff1a<code>ccam status</code> \u663e\u793a\u8fd0\u884c/\u505c\u6b62\u6307\u793a\u5668\uff0c<code>ccam start</code> \u5728\u540e\u53f0\u62c9\u8d77\u5206\u79bb\u7684\u751f\u4ea7\u670d\u52a1\u5668\u3002\u670d\u52a1\u5668\u672a\u8fd0\u884c\u65f6\uff0c\u53ea\u8bfb\u547d\u4ee4\u4f1a\u5728\u660e\u786e\u7684\u79bb\u7ebf\u6a2a\u5e45\u4e0b\u56de\u9000\u4e3a\u76f4\u63a5\u8bfb\u53d6 <code>data/dashboard.db</code>\uff0c\u800c\u5b9e\u65f6\u6216\u9700\u8981\u670d\u52a1\u5668\u8ba1\u7b97\u7684\u547d\u4ee4\u4f1a\u7ed9\u51fa\u5177\u4f53\u539f\u56e0\u5e76\u62d2\u7edd\u6267\u884c\u3002",
    "The full dashboard feature surface is also available from any terminal through the dependency-free <code>ccam</code> CLI (<code>bin/ccam.js</code>). It is linked automatically by <code>npm run setup</code> (via a fail-soft <code>npm link</code>), discovers the running server through the same <code>~/.claude/.agent-dashboard.json</code> registry the hook handler uses (override with <code>CLAUDE_DASHBOARD_PORT</code> / <code>DASHBOARD_PORT</code>), and renders a full terminal UI — box-drawn tables, status icons, inline bar charts, and agent trees — that degrades to plain text when piped (<code>--no-color</code> / <code>NO_COLOR</code> / <code>FORCE_COLOR</code> respected), so it is equally at home in an interactive shell, <code>grep</code> pipelines, or CI.":
      "仪表盘的完整功能面同样可以在任意终端中使用——通过零依赖的 <code>ccam</code> CLI（<code>bin/ccam.js</code>）。它由 <code>npm run setup</code> 自动链接（通过失败即降级的 <code>npm link</code>），并通过与 hook 处理器相同的 <code>~/.claude/.agent-dashboard.json</code> 注册表发现正在运行的服务器（可用 <code>CLAUDE_DASHBOARD_PORT</code> / <code>DASHBOARD_PORT</code> 覆盖）；并渲染完整的终端 UI——框线表格、状态图标、内联条形图和代理树——被管道接收时自动降级为纯文本（支持 <code>--no-color</code> / <code>NO_COLOR</code> / <code>FORCE_COLOR</code>）——因此它同样适用于交互式 shell、<code>grep</code> 管道或 CI。",
    "For a live monitoring session, <code>ccam repl</code> (aliases <code>shell</code> / <code>i</code>) opens an interactive shell where you type commands without the <code>ccam</code> prefix — with a CCAM welcome banner, tab-completion, persisted arrow-key history, a live server-status prompt (<code>●</code> up / <code>○</code> offline), a grouped <code>help</code> / <code>help &lt;cmd&gt;</code> menu, and a <code>watch [secs] &lt;cmd&gt;</code> built-in that auto-refreshes any command. Each entered line runs as an isolated child process, so an offline refusal or a blocking <code>tail</code> never takes the shell down.":
      "为了持续监控，<code>ccam repl</code>（别名 <code>shell</code> / <code>i</code>）会打开一个交互式 shell，你可以在其中输入命令而无需 <code>ccam</code> 前缀——带有 CCAM 欢迎横幅、Tab 补全、可持久化的方向键历史、实时服务器状态提示符（<code>●</code> 在线 / <code>○</code> 离线）、分组的 <code>help</code> / <code>help &lt;cmd&gt;</code> 菜单，以及可自动刷新任意命令的 <code>watch [秒] &lt;命令&gt;</code> 内置命令。每一行都作为独立子进程运行，因此离线拒绝或阻塞的 <code>tail</code> 都不会拖垮 shell。",
    Group: "分组",
    Commands: "命令",
    "What you get": "提供的能力",
    Monitoring: "监控",
    "Reachability, totals and status distributions, the Kanban board rendered as text columns, and a live event feed that polls for new rows every 2 seconds":
      "可达性检查、总量与状态分布、以文本列渲染的 Kanban 看板，以及每 2 秒轮询新行的实时事件流",
    "Up/down indicator, a background server start that waits for health, and the interactive <code>ccam repl</code> shell — tab-completion, persisted history, a live server-status prompt, and a <code>watch</code> auto-refresh":
      "上线/下线指示器、在后台启动服务器并等待其健康就绪，以及交互式 <code>ccam repl</code> shell——Tab 补全、可持久化历史、实时服务器状态提示符，以及 <code>watch</code> 自动刷新",
    Data: "数据",
    "Filterable tables plus a per-session deep dive with an indented agent tree, per-session cost, and the most recent events":
      "可筛选的表格，外加按会话的深入视图：缩进的 agent 树、单会话成本以及最近的事件",
    Insights: "洞察",
    "Token totals and top tools, workflow-intelligence stats with detected patterns, dynamic Workflow-tool runs, and the per-model cost breakdown, which can be scoped to a single session with <code>--session</code>, surfaces server-tool surcharges (web search / code execution), warns about models with usage but no matching pricing rule (priced at $0, excluded from the total), and points to <code>ccam pricing set</code>":
      "Token 总量与最常用工具、带已检测模式的工作流智能统计、动态 Workflow 工具运行，以及按模型的成本细分——可用 <code>--session</code> 限定到单个会话，会显示服务器工具附加费用（网页搜索 / 代码执行），当某个模型有用量却没有匹配的定价规则时会发出警告（按 $0 计价并从总额中排除），并提示用 <code>ccam pricing set</code> 修复",
    "Alerts &amp; Webhooks": "告警 &amp; Webhook",
    "The fired-alert feed with acknowledge / acknowledge-all, the rule list, and synthetic test deliveries against any webhook target":
      "触发告警流（支持单条/全部确认）、规则列表，以及对任意 webhook 目标的合成测试投递",
    Pricing: "价格",
    "Full pricing-rule CRUD with per-mtok rates — including 1-hour cache-write, fast-tier, and time-limited intro rates — straight from the terminal, with Fast In/Out and Intro In/Out columns in the list view":
      "完整的定价规则增删改查（按每百万 token 费率）——包括 1 小时缓存写入、fast 层级以及限时 intro 费率——直接在终端完成，列表视图带有 Fast In/Out 与 Intro In/Out 列",
    Import: "导入",
    "The same idempotent ingestion pipeline the Settings page uses, reporting imported / backfilled / skipped / error counts":
      "与 Settings 页面相同的幂等摄取管道，报告 imported / backfilled / skipped / error 计数",
    Administration: "管理",
    "Connectivity and hook diagnosis, raw system info, full JSON export, session cleanup, hook reinstallation, an update check that prints how far behind the canonical remote you are plus the copy-paste update command, and a guarded wipe that refuses to run without <code>--yes</code>":
      "连接与 hook 诊断、原始系统信息、完整 JSON 导出、会话清理、hook 重新安装、一个会打印你落后规范远程多少提交并给出可复制更新命令的更新检查，以及一个没有 <code>--yes</code> 就拒绝执行的受保护清空操作",
    "Safe by default": "默认安全",
    "Read commands only issue GETs; mutating commands map one-to-one to explicit dashboard actions; and the single destructive command, <code>clear-data</code>, always refuses to run without an explicit <code>--yes</code> flag. Exit codes are script-friendly: 0 on success, 1 on any failure. See <code>docs/CLI.md</code> for the complete reference.":
      "读取类命令只发出 GET；变更类命令与仪表盘上的显式操作一一对应；唯一的破坏性命令 <code>clear-data</code> 在没有显式 <code>--yes</code> 标志时始终拒绝执行。退出码对脚本友好：成功为 0，任何失败为 1。完整参考见 <code>docs/CLI.md</code>。",
    "The whole dashboard as terminal commands — <code>ccam stats</code>, <code>kanban</code>, <code>tail</code>, <code>session &lt;id&gt;</code>, <code>analytics</code>, <code>alerts</code>, <code>pricing</code>, <code>import</code>, <code>doctor</code>, <code>export</code> and more. Zero dependencies, linked by <code>npm run setup</code>, auto-discovers the running server, renders a full terminal UI (box-drawn tables, status icons, inline bar charts, agent trees), and pipes cleanly (colors off when not a TTY). The one destructive command is gated behind <code>--yes</code>.":
      "把整个仪表盘变成终端命令——<code>ccam stats</code>、<code>kanban</code>、<code>tail</code>、<code>session &lt;id&gt;</code>、<code>analytics</code>、<code>alerts</code>、<code>pricing</code>、<code>import</code>、<code>doctor</code>、<code>export</code> 等等。零依赖，由 <code>npm run setup</code> 链接，自动发现正在运行的服务器，渲染完整的终端 UI（框线表格、状态图标、内联条形图、代理树），管道输出干净（非 TTY 时关闭颜色）。唯一的破坏性命令由 <code>--yes</code> 把关。",
    '<span class="caption-icon">📡</span> Live dashboard — real-time agent cards, stats, and activity feed':
      '<span class="caption-icon">📡</span> 实时仪表盘 — 实时的 agent 卡片、统计数据和活动流',
    "Claude Code Agent Monitor integrates with Claude Code through its native hook system. When Claude Code performs any action — tool use, session start, subagent orchestration, session end — it fires a hook that calls a small Node.js script bundled with this project. That script forwards the event over HTTP to the dashboard server, which stores it in SQLite and broadcasts it to the browser over WebSocket.":
      "Claude Code Agent Monitor 通过 Claude Code 的原生 hook 系统与之集成。当 Claude Code 执行任何操作时——工具使用、会话开始、subagent 编排、会话结束——它都会触发一个 hook，调用本项目附带的一个小型 Node.js 脚本。该脚本通过 HTTP 将事件转发给仪表盘服务器，服务器将其存入 SQLite 并通过 WebSocket 广播到浏览器。",
    "End-to-end data pipeline from Claude Code to the browser":
      "从 Claude Code 到浏览器的端到端数据管道",
    "Local-first by design": "本地优先的设计理念",
    "The server binds <code>127.0.0.1</code> (loopback) by default, so it is not network-reachable and everything runs on your machine. No data leaves your system. No API keys. No external services. Exposing it more widely is opt-in via <code>DASHBOARD_HOST</code> and should be paired with <code>DASHBOARD_TOKEN</code>.":
      "服务器默认绑定 <code>127.0.0.1</code>（回环地址），因此它不可通过网络访问，一切都在你的机器上运行。没有数据离开你的系统。无需 API 密钥。无需外部服务。如需更大范围地暴露，则需通过 <code>DASHBOARD_HOST</code> 主动开启，并应搭配 <code>DASHBOARD_TOKEN</code> 一起使用。",
    "Every feature is driven by real hook events — nothing is hardcoded or simulated in production mode.":
      "每项功能都由真实的 hook 事件驱动——在生产模式下没有任何内容是硬编码或模拟的。",
    "Two tabs: <strong>Monitor</strong> shows overview stats, active agent cards with collapsible subagent hierarchy, and a recent activity feed whose item count fills available viewport height. <strong>Health</strong> renders a composite system health score ring, storage engine donut chart, cache/error/success gauges, tool invocation bars, subagent effectiveness ratios, model token distribution, and compaction stats. Both tabs auto-refresh every 5 seconds via WebSocket push so the view is always current without manual reload.":
      "两个标签页：<strong>Monitor</strong> 显示概览统计、带可折叠 subagent 层级的活动 agent 卡片，以及一个其条目数量会填满可用视口高度的最近活动流。<strong>Health</strong> 渲染一个综合系统健康评分环、存储引擎环形图、缓存/错误/成功仪表盘、工具调用条形图、subagent 有效性比率、模型 token 分布以及压缩统计。两个标签页都通过 WebSocket 推送每 5 秒自动刷新一次，因此无需手动重新加载视图始终保持最新。",
    "Toggle between <strong>Agents</strong> (Working / Waiting / Completed / Error) and <strong>Sessions</strong> (Active / Waiting / Completed / Error / Abandoned) swim lanes. A yellow <strong>Waiting</strong> column flags items sitting on the user — fresh prompt, between turns, or permission gate. Hover any column header for lifecycle tooltips explaining each state transition. Cards surface model name, cumulative cost, and the current tool being called. Counts update in real time via WebSocket so the board is always in sync with the live event store. Hover a Waiting badge to see why — needs input, turn done, at prompt, or interrupted.":
      "在 <strong>Agents</strong>（Working / Waiting / Completed / Error）和 <strong>Sessions</strong>（Active / Waiting / Completed / Error / Abandoned）泳道之间切换。黄色的 <strong>Waiting</strong> 列标记出等待用户的条目——刚收到的提示、回合之间，或权限关卡。将鼠标悬停在任意列标题上可查看解释每次状态转换的生命周期提示。卡片会展示模型名称、累计成本以及当前正在调用的工具。计数通过 WebSocket 实时更新，因此看板始终与实时事件存储保持同步。将鼠标悬停在 Waiting 徽章上即可查看原因——需要输入、回合结束、等待提示或已中断。",
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, and estimated cost. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "<strong>服务端分页</strong>的表格，列出每一个已记录的会话——每一页只获取它对应的那一段数据，因此无论存在多少会话，成本计算都保持有界。针对 <code>id</code>、<code>name</code> 和 <code>cwd</code> 的不区分大小写搜索在服务端运行，并带有 300 ms 防抖；状态过滤器可与搜索组合以实现精确缩小范围。每一行显示会话的真实名称（从 transcript 实时同步——<code>/rename</code> 或 <code>claude -n</code> 标题，否则使用自动标题，再否则使用首条用户 prompt，并回退到短 ID）、状态徽章、agent 数量、时长、模型和预估成本。点击任意行即可深入查看完整的会话详情视图，包含对话记录和 agent 层级。",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "针对单个会话的深入剖析，配有可折叠的 agent 层级树和一条完整的按时间排序的事件时间线，显示每一次工具调用的名称和摘要。顶部的概览面板展示事件、工具调用、subagent、压缩、错误和时长的磁贴计数器。顶部工具使用条形图和 subagent 类型细分让你快速读取分布情况。对话查看器渲染带语法高亮的 markdown、按工具分类的样式化代码块、带其捕获的 TUI 输出的斜杠命令气泡，以及内联的会话重命名标记。在回合进行中输入的消息（Claude 仍在工作时排队）会出现在 Claude 实际接收它们的位置，来自框架的通知则归属为 System。可将整个会话导出为 JSON，或分享永久链接以供异步审阅。当会话在等待你时，标题下方的黄色横幅会注明原因以及已等待的时长。",
    "A rules-based alerting engine evaluates the live event stream server-side: <strong>event pattern</strong> (match event type / tool / summary text, optionally N matches within a time window), <strong>inactivity</strong>, <strong>stuck agent</strong>, and <strong>token threshold</strong> — each with per-(rule, session, agent) cooldown dedup. Fired alerts surface in a live feed and fan out to <strong>14 first-class webhook providers</strong> — Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream — plus any generic JSON endpoint (with optional HMAC-SHA256 signing and custom headers). Delivery is detached and fail-safe with a request timeout, bounded retry/backoff, secret redaction, a one-click test probe, and a per-target delivery log. Rules and channels are managed together in <strong>Settings → Alerts</strong>.":
      "一个基于规则的告警引擎在服务端评估实时事件流：<strong>事件模式</strong>（匹配事件类型 / 工具 / 摘要文本，可选地在某个时间窗口内匹配 N 次）、<strong>不活动</strong>、<strong>卡住的 agent</strong> 以及 <strong>token 阈值</strong>——每一种都按（规则、会话、agent）维度进行冷却去重。触发的告警会出现在实时动态流中，并扇出到 <strong>14 个一流的 webhook 提供方</strong>——Slack、Discord、Teams、Google Chat、Mattermost、Rocket.Chat、Telegram、PagerDuty、Opsgenie、Splunk On-Call、Zapier、Make、n8n、Pipedream——外加任意通用 JSON 端点（可选 HMAC-SHA256 签名和自定义 header）。投递是分离且故障安全的，带有请求超时、有界的重试/退避、密钥脱敏、一键测试探针，以及按目标维度的投递日志。规则和通道在 <strong>Settings → Alerts</strong> 中统一管理。",
    "A native desktop app — a macOS <code>.app</code> (shipped as a <code>.dmg</code>) and a Windows <code>.exe</code> (NSIS installer plus a no-install portable build) — built with Electron 35. It <strong>embeds the Express server in-process</strong> — <code>require()</code>-ing <code>server/index.js</code> directly, with no child process and no IPC — and renders the built React client in a <code>BrowserWindow</code>. Adds a menu-bar / notification-area (tray) icon, a native application menu, auto-start at login (macOS Login Items via <code>SMAppService</code>; Windows per-user <code>HKCU\\…\\Run</code>), and a single-instance lock. Closing the window hides it while the server keeps running, and the app auto-installs Claude Code hooks on first boot so an install-only user gets events flowing without a checkout.":
      "一个原生桌面应用——一个 macOS <code>.app</code>（以 <code>.dmg</code> 形式发布）和一个 Windows <code>.exe</code>（NSIS 安装程序外加一个免安装的便携版本）——使用 Electron 35 构建。它 <strong>将 Express 服务器嵌入到同一进程中</strong>——直接 <code>require()</code> 引入 <code>server/index.js</code>，没有子进程也没有 IPC——并在 <code>BrowserWindow</code> 中渲染已构建的 React 客户端。它还添加了菜单栏 / 通知区域（托盘）图标、原生应用程序菜单、登录时自动启动（macOS 通过 <code>SMAppService</code> 使用登录项；Windows 使用按用户的 <code>HKCU\\…\\Run</code>），以及单实例锁。关闭窗口会将其隐藏而服务器继续运行，并且应用会在首次启动时自动安装 Claude Code hook，因此仅安装应用的用户无需检出代码即可让事件开始流入。",
    "Real-time streaming event log showing tool calls, agent state changes, errors, and compaction events as they arrive. Pause/resume with automatic buffering, paginated history for scrollback, and auto-scrolling to the latest entry. Click any row to expand its full hook payload inline. A dedicated <strong>Session →</strong> button navigates directly to session detail without collapsing the expanded state. Every entry carries the same status badge the Dashboard and Session Detail show for that event — one shared mapping that understands both Claude and Codex event types — and rows are grouped by session for quick scanning of concurrent work.":
      "实时流式事件日志，在工具调用、agent 状态变化、错误和压缩事件到达时即刻显示它们。支持带自动缓冲的暂停/恢复、用于回溯的分页历史，以及自动滚动到最新条目。点击任意行即可就地展开其完整的 hook 负载。一个专用的 <strong>Session →</strong> 按钮可直接跳转到会话详情，而不会折叠已展开的状态。每个条目都带有 Dashboard 与会话详情为该事件显示的同一状态徽章——一套同时理解 Claude 与 Codex 事件类型的共享映射——并按会话分组，以便快速浏览并发进行的工作。",
    "Token usage breakdown by model with stacked bar charts, tool frequency rankings, agent type distribution donuts, and session outcome pie charts. A 52-week activity heatmap aligned by day-of-week shows density with hover tooltips. 30-day sparkline trends track cost and session volume at a glance. The cost summary panel totals input, output, and cache spend across all models. A live/offline indicator and auto-refresh via WebSocket keep everything current. All charts are responsive and adapt to mobile viewports.":
      "以堆叠条形图按模型细分 token 使用量，工具使用频率排名、agent 类型分布环形图，以及会话结果饼图。一个按星期对齐的 52 周活动热力图通过悬停提示显示密度。30 天迷你折线趋势让你一目了然地跟踪成本和会话量。成本汇总面板汇总所有模型的输入、输出和缓存开销。实时/离线指示器以及通过 WebSocket 的自动刷新让一切保持最新。所有图表都是响应式的，可适配移动端视口。",
    "Every UI update is pushed over a persistent WebSocket with sub-5 ms dispatch latency — zero polling anywhere. If the connection drops, automatic 2-second reconnect kicks in while a ping/pong heartbeat detects stale connections early. A sidebar indicator turns green/red so you always know whether you're live. The WebSocket carries typed JSON envelopes for new events, session updates, agent transitions, compaction results, and import progress — all parsed into the same eventBus the REST layer uses.":
      "每次 UI 更新都通过持久化的 WebSocket 推送，分发延迟低于 5 ms——任何地方都不需要轮询。若连接断开，会自动在 2 秒内重连，同时通过 ping/pong 心跳尽早检测失效连接。侧栏指示灯会变为绿色/红色，让你随时知道是否处于实时连接状态。WebSocket 承载带类型的 JSON 信封，用于新事件、会话更新、智能体转换、压缩结果以及导入进度——全部解析进 REST 层所使用的同一个 eventBus。",
    "Standalone CLI statusline for Claude Code that prints model name, user, working directory, git branch, and a color-coded context-window bar (green → yellow → red). Token counts show input (green ↑), output (cyan ↓), and cache (dim) separately. Session cost in USD shifts color by configurable thresholds. ANSI-colored output updates on every turn. Python-based with a thin shell wrapper — drop it into your prompt or tmux status line. Works with any terminal emulator that supports 256-color ANSI.":
      "为 Claude Code 提供的独立 CLI 状态栏，会打印模型名称、用户、工作目录、git branch，以及一条彩色编码的上下文窗口条（green → yellow → red）。Token 计数分别显示输入（green ↑）、输出（cyan ↓）和缓存（dim）。以 USD 计的会话成本会按可配置的阈值改变颜色。ANSI 着色输出会在每个回合更新。基于 Python，并带有一个轻量的 shell 包装器——将其放入你的提示符或 tmux 状态栏即可。可与任何支持 256 色 ANSI 的终端模拟器配合使用。",
    "Import existing Claude Code or Codex sessions from three sources — rescan the selected default <code>~/.claude/projects</code> or <code>~/.codex/sessions</code> folder, scan any absolute path on disk, or drag-drop <code>.jsonl</code>, <code>.zip</code>, <code>.tar.gz</code>, and <code>.gz</code> archives through <b>Settings → Import History</b>. All paths use each provider's live ingestion pipeline, so imported tokens, per-model cost, and tool data match real-time capture exactly. Codex uploads are snapshotted before temporary files are cleaned up, and re-imports are idempotent.":
      "可从三种来源导入已有的 Claude Code 或 Codex 会话——重新扫描所选默认 <code>~/.claude/projects</code> 或 <code>~/.codex/sessions</code> 文件夹、扫描磁盘上的任意绝对路径，或通过 <b>Settings → Import History</b> 拖放 <code>.jsonl</code>、<code>.zip</code>、<code>.tar.gz</code> 和 <code>.gz</code> 归档文件。所有路径都使用各提供方的实时摄取管线，因此导入的 token、按模型计的成本和工具数据与实时捕获完全一致。Codex 上传会在清理临时文件之前被快照，重复导入保持幂等。",
    "The startup auto-import of <code>~/.claude/projects</code> is one-time and marker-gated, so a project folder created after first launch — whose sessions never flow through hooks (for example with host-only hooks disabled) — would stay invisible until a manual rescan. A background sync closes that gap with three triggers sharing one mtime cache and a single coalesced sweep: an immediate sweep at startup, a debounced <code>fs.watch</code> (recursive on macOS and Windows; root plus immediate child folders on Linux to avoid the userland recursive-watcher hazard) that fires the moment a new session file or project folder appears, and a periodic safety-net poll tunable via <code>DASHBOARD_SESSION_SYNC_MS</code> (default <code>30000</code> ms; <code>0</code> disables the poll while leaving the watcher running). Each sweep re-parses only files whose mtime advanced and broadcasts <code>session_created</code> / <code>session_updated</code> (plus the main agent) so the UI refreshes live, while an already-imported unchanged session is skipped without re-parsing.":
      "对 <code>~/.claude/projects</code> 的启动时自动导入是一次性的，并由标记控制，因此首次启动之后创建的项目文件夹——其会话从不经由 hook 流入（例如禁用了仅主机的 hook）——在手动重新扫描之前都不会显示。一个后台同步通过三个共享同一个 mtime 缓存和单次合并扫描的触发器弥补这一缺口：启动时的一次立即扫描、一个去抖动的 <code>fs.watch</code>（在 macOS 和 Windows 上递归；在 Linux 上监听根目录加各个直接子文件夹，以规避用户态递归监听器的隐患），它会在出现新的会话文件或项目文件夹的那一刻触发，以及一个可通过 <code>DASHBOARD_SESSION_SYNC_MS</code> 调节的周期性安全网轮询（默认 <code>30000</code> ms；<code>0</code> 会禁用轮询但保留监听器）。每次扫描仅重新解析 mtime 已推进的文件，并广播 <code>session_created</code> / <code>session_updated</code>（外加主 agent），从而让 UI 实时刷新；而对于已导入且未变更的会话则会被跳过，不再重新解析。",
    "Incremental JSONL reader shared across the hook handler, compaction scanner, conversation viewer, and import pipeline. Byte-offset tracking skips already-parsed content; cache hits short-circuit disk I/O so even sessions with tens of thousands of turns stay fast. It also extracts the live session title (<code>custom-title</code> / <code>ai-title</code>) so renames surface in real time, plus the first user prompt as a fallback descriptor for placeholder-named sessions and agents.":
      "增量式 JSONL 读取器，在 hook 处理器、压缩扫描器、对话查看器和导入管道之间共享。字节偏移跟踪会跳过已解析的内容；缓存命中会短路磁盘 I/O，因此即便是有数万个回合的会话也能保持快速。它还会提取实时的会话标题（<code>custom-title</code> / <code>ai-title</code>），使重命名能实时呈现，并提取首条用户 prompt，作为占位命名的会话与 agent 的后备描述符。",
    "LRU eviction of cold session buffers plus a tail-cap on per-entry growable arrays (turn durations, API errors, compaction entries). A session that runs for days cannot grow a single cache entry without bound, and each entry stores its parsed result only once — no shadow copy.":
      "对冷会话缓冲区进行 LRU 淘汰，并对每个条目的可增长数组（回合时长、API 错误、压缩条目）设置尾部上限。运行数天的会话也无法让任何单个缓存条目无限增长，而且每个条目只存储一次其解析结果——没有影子副本。",
    "The periodic compaction sweep reads each active session's transcript path directly from <code>sessions.transcript_path</code> (a partial index covers exactly those rows), so the work is O(active sessions) instead of a <code>json_extract</code> scan over the whole events table.":
      "周期性的压缩扫描直接从 <code>sessions.transcript_path</code> 读取每个活跃会话的 transcript 路径（一个部分索引恰好覆盖这些行），因此其工作量为 O(活跃会话数)，而不是对整个事件表进行 <code>json_extract</code> 扫描。",
    "Collapsible parent–child agent tree rendered on both Dashboard and Session Detail. Agents with subagents display expand/collapse chevrons; leaf agents show a dot indicator. The tree auto-expands when any child transitions to active and correctly tracks backgrounded subagents without premature completion. Depth is unlimited — deeply nested chains render as indented rows with connecting lines. Each node shows model, current tool, status badge, and cumulative token cost for tracing spend down the spawn chain.":
      "可折叠的父子智能体树，同时在 Dashboard 和 Session Detail 上渲染。带有子智能体的智能体会显示展开/折叠的箭头；叶子智能体显示一个圆点指示符。当任意子节点转为活跃时，树会自动展开，并能正确跟踪后台运行的子智能体而不会过早标记完成。深度不受限制——深层嵌套的链会渲染为带连接线的缩进行。每个节点都会显示模型、当前工具、状态徽章以及累计 token 成本，便于沿生成链追踪开销。",
    "Per-model cost estimation with configurable pricing rules — set input, output, and cache-read rates per model variant through the Settings UI. View total and per-session breakdowns on Sessions, Session Detail, and Analytics. Compaction- aware token accounting preserves baselines across context compressions so no usage is silently dropped. Cost chips appear on Kanban cards, session rows, and the sidebar summary. Subagent cards show each subagent's own cost, computed from that subagent's transcript token usage priced at current rates, so a subagent card no longer misleadingly reads as if it cost the whole session; main-agent cards still show the session total. Pricing changes retroactively recalculate all stored sessions, and imports apply the same rate table.":
      "按模型估算成本，配以可配置的定价规则——通过 Settings UI 为每个模型变体设置输入、输出和缓存读取的费率。可在 Sessions、Session Detail 和 Analytics 上查看总计及按会话的明细。具备压缩感知的 token 计量会在上下文压缩过程中保留基线，因此不会有任何用量被悄悄丢弃。成本标签会出现在看板卡片、会话行和侧栏汇总中。subagent 卡片会显示该 subagent 自身的成本，它按当前费率对该 subagent transcript 的 token 用量计算得出，因此 subagent 卡片不会再误导性地显示得好像它花掉了整个会话的费用；主 agent 卡片仍显示会话总计。定价变更会追溯重算所有已存储的会话，导入也会套用同一张费率表。",
    "Model pricing editor with per-token rate configuration for every Claude variant. Hook installation status with one-click reinstall and per-hook health checks. Full JSON data export covering sessions, agents, events, tokens, and pricing rules. Session cleanup controls to abandon stale sessions or purge old data by age. Browser notification preferences with per-event toggles. A system information panel shows database row counts, file sizes, server uptime, and WebSocket connection status at a glance.":
      "模型定价编辑器，可为每个 Claude 变体配置按 token 计的费率。Hook 安装状态，支持一键重装以及对每个 hook 的健康检查。完整的 JSON 数据导出，涵盖会话、智能体、事件、token 和定价规则。会话清理控件，可放弃陈旧会话或按时长清除旧数据。浏览器通知偏好，支持按事件切换。系统信息面板可一目了然地显示数据库行数、文件大小、服务器运行时间以及 WebSocket 连接状态。",
    "Local MCP sidecar with three transport modes — stdio for Claude Code native integration, HTTP+SSE for remote clients, and an interactive REPL for ad-hoc terminal queries. Exposes 25 typed tools across 6 domains: sessions, agents, events, analytics, settings, and system health. Every mutation is gated behind a tiered policy so nothing dangerous fires without opt-in. Retry-aware API access handles transient failures. Runs as a standalone Node process with no Docker or cloud dependency.":
      "本地 MCP 边车，提供三种传输模式——用于 Claude Code 原生集成的 stdio、用于远程客户端的 HTTP+SSE，以及用于临时终端查询的交互式 REPL。在 6 个领域中暴露 25 个带类型的工具：会话、智能体、事件、分析、设置和系统健康。每个写操作都受分级策略把关，因此任何危险操作都不会在未经选择启用的情况下触发。具备重试感知的 API 访问可处理瞬时故障。作为独立的 Node 进程运行，不依赖 Docker 或云。",
    "Instruction, skills, rules, and custom-agent layers for both Claude Code and Codex. Path-scoped rules target backend, frontend, MCP, and docs directories with context-appropriate guidelines. Reusable skills cover onboarding, feature shipping, live-issue debugging, release-readiness, and MCP operations. Specialized subagents for backend, frontend, and MCP code review run in parallel with focused tooling. Everything lives in <code>.claude/</code> and is version-controlled alongside the codebase.":
      "为 Claude Code 和 Codex 同时提供指令、技能、规则以及自定义智能体层。按路径作用域的规则会针对 backend、frontend、MCP 和 docs 目录，配以贴合上下文的准则。可复用的技能涵盖入门引导、功能发布、实时问题调试、发布就绪检查以及 MCP 运维。用于 backend、frontend 和 MCP 代码评审的专用子智能体会借助聚焦的工具并行运行。所有内容都位于 <code>.claude/</code> 中，并与代码库一起进行版本管理。",
    "D3.js-powered visualizations: an agent orchestration DAG showing spawn patterns across sessions, a tool-execution Sankey diagram mapping tool-to- tool transitions, and a directed pipeline graph with frequency labels. Every chart title carries an info icon that opens a popover explaining what it shows and how to read it. Hovering nodes, edges, and bars surfaces tooltips with share-of-source percentages, success-rate buckets, and timing patterns. All labels are translated to English, Vietnamese, and Chinese.":
      "由 D3.js 驱动的可视化：一个展示跨会话生成模式的智能体编排 DAG、一个映射工具到工具转换的工具执行 Sankey 图，以及一个带频率标签的有向管道图。每个图表标题都带有一个信息图标，点击可打开弹出框，说明它展示了什么以及如何解读。悬停在节点、边和条形上会浮现工具提示，显示来源占比、成功率分桶和时序模式。所有标签都已翻译为英语、越南语和中文。",
    "Subagent effectiveness scorecards with success-rate rings and day-of-week sparklines. Auto-detected workflow patterns expand on click into a detail panel with the full step chain, stats grid, and a narrative with loop detection. Model delegation flow, error propagation bars with API error cards, concurrency swim-lanes, and complexity bubble charts round out the view. Six headline stat cards each include an info popover explaining the metric and its current value. Status filter applies globally.":
      "子智能体有效性记分卡，配有成功率环形图和按星期几的迷你折线图。自动检测出的工作流模式可点击展开为详情面板，包含完整的步骤链、统计网格以及带循环检测的叙述。模型委派流、带 API 错误卡片的错误传播条、并发泳道以及复杂度气泡图共同充实了该视图。六张头条统计卡片各自带有一个信息弹出框，说明该指标及其当前值。状态筛选器全局生效。",
    "Searchable session selector with pagination to explore any session's agent tree, tool-call timeline, and event sequence. The detail page opens with a live-updating overview — tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and subagent breakdown give quick reads. The conversation viewer renders markdown with syntax highlighting. Cross-filter from DAG nodes, run compaction analysis, or export as JSON — all with real-time WebSocket auto-refresh.":
      "可搜索的会话选择器，带分页，用于探索任意会话的智能体树、工具调用时间线和事件序列。详情页打开时会呈现一个实时更新的概览——针对事件、工具调用、子智能体、压缩、错误和时长的磁贴计数器。顶级工具使用条和子智能体细分可供快速浏览。对话查看器会渲染带语法高亮的 markdown。可从 DAG 节点进行交叉筛选、运行压缩分析或导出为 JSON——所有这些都带有实时的 WebSocket 自动刷新。",
    "Persistent browser notifications via Web Push (VAPID) for real-time alerts even when the tab is not focused or the browser is backgrounded. Includes macOS audio support so notifications are audible alongside system sounds. Per-event toggles let you choose which events fire — session starts, completions, errors, compactions, or agent spawns. Server-side subscription management ensures one push per event per browser. Works on Chrome, Edge, Firefox, and Safari 17+ with graceful degradation elsewhere.":
      "通过 Web Push（VAPID）提供持久化的浏览器通知，即便标签页未聚焦或浏览器处于后台也能实时提醒。包含 macOS 音频支持，因此通知会与系统声音一并发出。按事件的开关让你选择哪些事件触发——会话开始、完成、错误、压缩或智能体生成。服务端的订阅管理确保每个事件在每个浏览器上只推送一次。可在 Chrome、Edge、Firefox 和 Safari 17+ 上运行，在其他环境下则会优雅降级。",
    "Ready-to-use Dockerfile and docker-compose.yml for one-command deployment. Supports both Docker and Podman with persistent volume mounts for the SQLite database and hook data. Configurable port mapping via environment variables and a health-check endpoint the container runtime can poll. Multi-stage build keeps the image lean — only production deps and the compiled bundle ship. Run <code>docker compose up -d --build</code> and the dashboard is live with zero additional setup or configuration required.":
      "开箱即用的 Dockerfile 和 docker-compose.yml，支持一条命令部署。同时支持 Docker 和 Podman，为 SQLite 数据库和 hook 数据提供持久化的卷挂载。可通过环境变量配置端口映射，并提供一个容器运行时可轮询的健康检查端点。多阶段构建让镜像保持精简——只发布生产依赖和编译后的产物。运行 <code>docker compose up -d --build</code>，仪表盘即可上线，无需任何额外的安装或配置。",
    "Official Claude Code plugin marketplace shipping 10 plugins with 53 skills, 14 agents, 30 slash commands, 3 CLI tools, 3 hook configs, and 1 MCP server. Deep analytics with compaction-aware baselines, productivity automation, developer diagnostics, AI-powered workflow intelligence, and dashboard MCP integration. Five newer plugins go further: <code>ccam-cost-guard</code> (budget guardrails, spend forecasts, and cost-threshold alerts), <code>ccam-sessions</code> (session forensics — search, timeline, and transcript replay), <code>ccam-workflows</code> (multi-agent orchestration and fleet intelligence), <code>ccam-quality</code> (reliability and SLO checks), and <code>ccam-config</code> (Claude Code config and memory governance). Install with <code>claude plugin install</code> — no restart needed. Each listing shows author, license, homepage, and per-skill contribution breakdown. The Config Explorer's Plugins tab surfaces installed plugins with live status.":
      "官方 Claude Code 插件市场，发布 10 个插件，包含 53 个技能、14 个智能体、30 个斜杠命令、3 个 CLI 工具、3 个 hook 配置以及 1 个 MCP 服务器。具备压缩感知基线的深度分析、效率自动化、开发者诊断、AI 驱动的工作流智能以及仪表盘 MCP 集成。五个较新的插件更进一步：<code>ccam-cost-guard</code>（预算护栏、支出预测与成本阈值告警）、<code>ccam-sessions</code>（会话取证——搜索、时间线与 transcript 回放）、<code>ccam-workflows</code>（多智能体编排与机群智能）、<code>ccam-quality</code>（可靠性与 SLO 检查）以及 <code>ccam-config</code>（Claude Code 配置与记忆治理）。使用 <code>claude plugin install</code> 安装——无需重启。每个条目都会显示作者、许可证、主页以及按技能的贡献细分。Config Explorer 的 Plugins 标签页会展示已安装的插件及其实时状态。",
    "Spawn <code>claude</code> subprocesses straight from the dashboard with a chat-style streaming UI — multi-turn <b>Conversation</b> or single-shot <b>Headless</b> mode. One-click <b>Resume</b> on any past conversation spawns <code>claude --resume</code> seeded with the prior transcript. Re- attach reconciles in-memory logs with the on-disk JSONL so navigating away never loses history. Slash-command autocomplete, file references, live token/context-window meter, and a thinking-effort dial bring TUI parity to the browser. Same-origin guard blocks drive-by spawns.":
      "直接从仪表盘以聊天式流式 UI 生成 <code>claude</code> 子进程——可选多轮的 <b>Conversation</b> 或单次的 <b>Headless</b> 模式。对任意过往对话一键 <b>Resume</b>，会以先前的 transcript 作为种子生成 <code>claude --resume</code>。重新接入会将内存中的日志与磁盘上的 JSONL 进行协调，因此离开页面也绝不会丢失历史。斜杠命令自动补全、文件引用、实时的 token/上下文窗口计量表以及思考强度旋钮，让浏览器具备与 TUI 同等的体验。同源保护可阻止顺带发起的恶意生成。",
    "A 12-tab inspector at <code>/cc-config</code> for everything Claude Code knows about: skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts. The Settings tab leads with a Current configuration summary of the options <code>/config</code> controls — model, verbose, theme, output style, auto-compact, notifications — resolved across scopes. The Memory tab surfaces both the user and project <code>CLAUDE.md</code> files and the per-project file-based memory store — every auto-memory <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+), grouped by project, searchable, and editable. Create, edit, and delete the low-risk text-file surfaces with mandatory timestamped backups before every write. Plugins, MCP, hooks, and live settings stay read-only with explainer banners and copy-able CLI commands. Per-plugin contribution breakdowns show author and license.":
      "位于 <code>/cc-config</code> 的 12 个标签页的检查器，涵盖 Claude Code 所知的一切：技能、子智能体、斜杠命令、输出样式、插件、市场、MCP 服务器、hook、设置、记忆、键位绑定和状态栏脚本。设置标签页以一个当前配置摘要为首，跨作用域解析 <code>/config</code> 控制的选项——model、verbose、主题、输出样式、自动压缩、通知。记忆标签页同时呈现用户与项目的 <code>CLAUDE.md</code> 文件，以及按项目划分的基于文件的记忆存储——位于 <code>~/.claude/projects/&lt;slug&gt;/memory/</code> 下的每一个自动记忆 <code>*.md</code> 文件（一个 <code>MEMORY.md</code> 索引外加每条记忆事实一个文件，通常 100 多个），按项目分组、可搜索且可编辑。可创建、编辑和删除低风险的文本文件区域，每次写入前都会强制进行带时间戳的备份。插件、MCP、hook 和实时设置保持只读，并配有说明横幅和可复制的 CLI 命令。按插件的贡献细分会显示作者和许可证。",
    "Mobile-first layouts with stacking grids, horizontally scrollable tables, and a collapsible sidebar that auto-hides below 1400 px. All pages adapt from phone to ultrawide with consistent navigation and readable typography. Kanban columns stack vertically on narrow screens, analytics charts reflow to single-column, and the activity feed stays fully swipeable. Touch targets meet 44 px minimum. Dark theme renders consistently across iOS Safari, Chrome, and Firefox with no flash of unstyled content.":
      "移动优先的布局，采用堆叠网格、可横向滚动的表格，以及在低于 1400 px 时自动隐藏的可折叠侧栏。所有页面都能从手机到超宽屏自适应，并保持一致的导航和可读的排版。在窄屏上看板列会纵向堆叠，分析图表会重排为单列，而活动信息流则保持完全可滑动。触摸目标满足最小 44 px。深色主题在 iOS Safari、Chrome 和 Firefox 上渲染一致，且不会出现无样式内容的闪烁。",
    "Visualize parallel agent execution with a Gantt-style timeline showing overlapping subagent lifetimes, tool-call concurrency windows, and wait gaps. Color-coded bars distinguish working, waiting, and errored states so bottlenecks are immediately visible. Hover any bar for exact timestamps and duration. Zoom and pan across long-running sessions with hundreds of agents. The timeline shares the Workflows status filter so you can isolate active, completed, or errored sessions without leaving the view.":
      "用甘特图风格的时间线可视化并行的智能体执行过程，展示相互重叠的子智能体生命周期、工具调用的并发窗口以及等待间隙。彩色编码的条形区分工作中、等待中和出错状态，使瓶颈一目了然。悬停任意条形可查看精确的时间戳和时长。可在拥有数百个智能体的长时间运行会话中缩放和平移。该时间线共用 Workflows 的状态筛选器，因此你无需离开该视图即可隔离活跃、已完成或出错的会话。",
    "Professional VS Code extension with a real-time Activity Bar sidebar showing active sessions, agent counts, and recent events without leaving your editor. A status bar pulse monitor surfaces connection health and the latest event type at a glance. Deep navigation links open any session or analytics view directly in your browser. An embedded webview renders the full dashboard inside a VS Code tab with WebSocket push, theme sync, and responsive layout. Install from the marketplace or build from source.":
      "专业的 VS Code 扩展，配有实时的 Activity Bar 侧栏，无需离开编辑器即可显示活跃会话、智能体数量和近期事件。状态栏的脉冲监视器可一目了然地展示连接健康状况和最新的事件类型。深度导航链接可直接在浏览器中打开任意会话或分析视图。内嵌的 webview 会在 VS Code 标签页中渲染完整的仪表盘，支持 WebSocket 推送、主题同步和响应式布局。可从市场安装或从源码构建。",
    "Trace how errors cascade across agents and tool calls with a directed graph showing failure origins, retry paths, and recovery points. Each node displays the agent or tool that errored, the error message, and whether a retry succeeded or propagated upstream. Pinpoint root causes in deeply nested subagent chains. Horizontal bar charts rank the most error-prone tools and models. API error cards group failures by HTTP status and endpoint. Filter by session, time range, or error severity to narrow the view.":
      "用一张有向图追踪错误如何在智能体和工具调用之间级联，展示故障源头、重试路径和恢复点。每个节点都会显示出错的智能体或工具、错误消息，以及重试是成功还是向上游传播。可在深层嵌套的子智能体链中精确定位根因。横向条形图会对最易出错的工具和模型进行排名。API 错误卡片会按 HTTP 状态和端点对故障进行分组。可按会话、时间范围或错误严重程度筛选以缩小视图。",
    'Three independent PWAs — dashboard, landing page, and wiki — each with its own Web App Manifest and Service Worker. Install to your home screen or dock for a standalone, chrome-less experience. SVG icons with <code>sizes="any"</code> and iOS standalone meta tags included.':
      '三个独立的 PWA——仪表盘、着陆页和 wiki——各自拥有自己的 Web App Manifest 和 Service Worker。可安装到你的主屏幕或程序坞，获得独立、无浏览器边框的体验。包含带 <code>sizes="any"</code> 的 SVG 图标以及 iOS standalone 元标签。',
    "The dashboard SW serves Vite's hashed <code>/assets/*</code> bundles cache-first (URLs are immutable per build) and treats everything else as network-first with cache fallback. Explicit <code>Cache-Control</code> headers on the production Express static middleware reinforce the policy, so a rebuild replaces the in-browser code without a hard refresh.":
      "仪表盘的 SW 以缓存优先的方式提供 Vite 带哈希的 <code>/assets/*</code> 产物（这些 URL 在每次构建中都是不可变的），并将其他所有内容视为网络优先并以缓存作为回退。生产环境 Express 静态中间件上显式的 <code>Cache-Control</code> 头进一步强化了该策略，因此重新构建无需强制刷新即可替换浏览器中的代码。",
    "A <code>controllerchange</code> listener in <code>client/src/main.tsx</code> reloads the page exactly once when a new SW takes over an already-controlled page. First installs do not reload, so the very first visit is never interrupted.":
      "<code>client/src/main.tsx</code> 中的一个 <code>controllerchange</code> 监听器会在新的 SW 接管一个已受控页面时，恰好重新加载页面一次。首次安装不会重新加载，因此首次访问绝不会被打断。",
    '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — live overview of active sessions and agents. Stats tiles, collapsible subagent hierarchy cards, and a recent activity feed. Auto-refreshes every 5 s via WebSocket</span>':
      '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — 活动会话与 agent 的实时概览。统计卡片、可折叠的子 agent 层级卡片，以及最近活动信息流。通过 WebSocket 每 5 s 自动刷新</span>',
    '<span class="caption-icon">🩺</span> <span><strong>Dashboard · Health</strong> — composite health score ring, storage engine donut, cache/error/success gauges, tool invocation bars, subagent effectiveness, and model token distribution</span>':
      '<span class="caption-icon">🩺</span> <span><strong>Dashboard · Health</strong> — 综合健康评分环、存储引擎环形图、缓存/错误/成功仪表盘、工具调用柱状图、子 agent 有效性，以及模型 token 分布</span>',
    '<span class="caption-icon">📋</span> <span><strong>Kanban Board (agents)</strong> — agents swim-laned by status: Working, Waiting, Completed, Error. Cards show model, cost, and current tool call. Yellow column flags agents waiting on user input</span>':
      '<span class="caption-icon">📋</span> <span><strong>Kanban Board（agent）</strong> — agent 按状态分泳道排列：Working、Waiting、Completed、Error。卡片显示模型、成本和当前工具调用。黄色列标记正在等待用户输入的 agent</span>',
    '<span class="caption-icon">🗂️</span> <span><strong>Kanban Board (sessions)</strong> — sessions swim-laned across 5 columns: Active, Waiting, Completed, Error, Abandoned. Each card shows agent count, duration, model, and cumulative cost</span>':
      '<span class="caption-icon">🗂️</span> <span><strong>Kanban Board（session）</strong> — session 横跨 5 列分泳道排列：Active、Waiting、Completed、Error、Abandoned。每张卡片显示 agent 数量、持续时间、模型和累计成本</span>',
    '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — searchable, filterable, server-paginated table. Each row shows status badge, agent count, duration, model, and cost. Click any row to drill into session detail</span>':
      '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — 可搜索、可筛选、服务端分页的表格。每行显示状态徽章、agent 数量、持续时间、模型和成本。点击任意一行可深入查看会话详情</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles (events, tool calls, subagents, errors, duration), top-tool usage bars, subagent type breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — 概览卡片（事件、工具调用、子 agent、错误、持续时间）、最常用工具使用柱状图、子 agent 类型细分，以及可折叠的父子 agent 层级树</span>',
    '<span class="caption-icon">💬</span> <span><strong>Session Detail · Conversation</strong> — full transcript viewer with markdown rendering, syntax-highlighted code blocks, per-tool sections, and collapsible thinking blocks</span>':
      '<span class="caption-icon">💬</span> <span><strong>Session Detail · Conversation</strong> — 完整的会话记录查看器，支持 markdown 渲染、语法高亮的代码块、按工具分区，以及可折叠的思考块</span>',
    '<span class="caption-icon">🔬</span> <span><strong>Session Detail · Timeline</strong> — chronological event timeline with multi-dimension filters, color-coded entries by type, expandable hook payloads, and direct links to the owning session and agent</span>':
      '<span class="caption-icon">🔬</span> <span><strong>Session Detail · Timeline</strong> — 按时间顺序排列的事件时间线，支持多维度筛选、按类型颜色编码的条目、可展开的 hook 负载，以及指向所属会话和 agent 的直接链接</span>',
    '<span class="caption-icon">📰</span> <span><strong>Activity Feed</strong> — real-time streaming event log with pause/resume buffering, multi-dimension filters, expandable hook payloads, color-coded entries, and per-row session navigation buttons</span>':
      '<span class="caption-icon">📰</span> <span><strong>Activity Feed</strong> — 实时流式事件日志，支持暂停/恢复缓冲、多维度筛选、可展开的 hook 负载、颜色编码的条目，以及每行的会话导航按钮</span>',
    '<span class="caption-icon">📊</span> <span><strong>Analytics</strong> — token usage by model, tool frequency bars, 52-week activity heatmap, 30-day sparkline trends, session outcome donuts, and cost summary with WebSocket auto-refresh</span>':
      '<span class="caption-icon">📊</span> <span><strong>Analytics</strong> — 按模型统计的 token 用量、工具使用频率柱状图、52 周活动热力图、30 天迷你折线趋势图、会话结果环形图，以及通过 WebSocket 自动刷新的成本汇总</span>',
    '<span class="caption-icon">🔀</span> <span><strong>Workflows</strong> — D3.js agent orchestration DAG, tool-execution Sankey diagram, directed pipeline graph, effectiveness scorecards, concurrency swim-lanes, and complexity bubble charts</span>':
      '<span class="caption-icon">🔀</span> <span><strong>Workflows</strong> — D3.js agent 编排 DAG、工具执行桑基图、有向流水线图、有效性评分卡、并发泳道，以及复杂度气泡图</span>',
    '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — 12-tab inspector for skills, subagents, slash commands, plugins, MCP servers, hooks, settings, memory, keybindings, and statusline. Safe edits with backups</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — 12 个标签页的检查器，涵盖 skill、子 agent、斜杠命令、plugin、MCP 服务器、hook、设置、记忆、键位绑定和状态栏。安全编辑并带备份</span>',
    '<span class="caption-icon">▶️</span> <span><strong>Run Claude</strong> — spawn or resume Claude subprocesses from the browser. Pick Conversation or Headless mode, set cwd, model, permission level, and thinking effort. Same-origin guard included</span>':
      '<span class="caption-icon">▶️</span> <span><strong>Run Claude</strong> — 在浏览器中启动或恢复 Claude 子进程。选择 Conversation 或 Headless 模式，设置 cwd、模型、权限级别和思考强度。内置同源防护</span>',
    '<span class="caption-icon">💬</span> <span><strong>Run Claude · live stream</strong> — character-by-character streaming output. Tool uses, tool results, and thinking blocks are collapsible. Active runs switcher juggles multiple sessions</span>':
      '<span class="caption-icon">💬</span> <span><strong>Run Claude · live stream</strong> — 逐字符的流式输出。工具调用、工具结果和思考块均可折叠。活动运行切换器可同时管理多个会话</span>',
    '<span class="caption-icon">⚙️</span> <span><strong>Settings</strong> — model pricing editor with per-token rates, hook installation status, JSON data export, session cleanup controls, browser notification toggles, and system info panel with DB stats</span>':
      '<span class="caption-icon">⚙️</span> <span><strong>Settings</strong> — 模型定价编辑器（含每 token 费率）、hook 安装状态、JSON 数据导出、会话清理控制、浏览器通知开关，以及含数据库统计的系统信息面板</span>',
    "This chart tracks how interest in Claude Code Agent Monitor has grown over time. The curve keeps climbing as more developers discover the project, share it, and use it in real workflows. Each new star is a small vote of confidence from the community.":
      "此图表追踪人们对 Claude Code Agent Monitor 的关注度如何随时间增长。随着越来越多的开发者发现该项目、分享它并在真实工作流中使用它，曲线持续攀升。每一颗新的 star 都是社区投出的一份小小的信任票。",
    '<span class="caption-icon">⭐</span> <span> Enjoying the project? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer" >Give it a star on GitHub</a > and help more builders discover it. </span>':
      '<span class="caption-icon">⭐</span> <span> 喜欢这个项目吗？ <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer" >在 GitHub 上给它点个 star</a >，帮助更多开发者发现它。 </span>',
    "Hook Type": "Hook 类型",
    Trigger: "触发条件",
    "Dashboard Action": "仪表盘动作",
    "Claude Code session begins": "Claude Code 会话开始",
    "Creates session and main agent. Stamps <code>awaiting_input_since</code> (with <code>awaiting_reason</code> = <code>session_start</code>) so the row lands in <strong>Waiting</strong> from the start (the CLI is at a prompt) — except a <code>compact</code>-source SessionStart (mid-turn auto-compaction), which leaves the flag untouched so a working session stays <strong>Active</strong>. Reactivates resumed sessions. Abandons orphaned sessions with no activity for <code>DASHBOARD_STALE_MINUTES</code> (default 180).":
      "创建会话和主 agent。打上 <code>awaiting_input_since</code>（附带 <code>awaiting_reason</code> = <code>session_start</code>）时间戳，使该行从一开始就落入 <strong>Waiting</strong>（CLI 处于提示符状态）——但 <code>compact</code> 来源的 SessionStart（回合中自动压缩）不改动标志，使正在工作的会话保持 <strong>Active</strong>。重新激活已恢复的会话。将在 <code>DASHBOARD_STALE_MINUTES</code>（默认 180）期间无活动的孤立会话标记为放弃。",
    "User hits enter on a prompt": "用户在提示符处按下回车",
    'Clears the waiting flag and promotes the main agent to <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >. The only reliable signal that text-only assistant turns have started — they emit no <code>PreToolUse</code> before <code>Stop</code>.':
      '清除等待标志，并将主 agent 提升为 <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >。这是纯文本助手轮次已开始的唯一可靠信号——它们在 <code>Stop</code> 之前不会发出 <code>PreToolUse</code>。',
    "Agent begins using a tool": "Agent 开始使用某个工具",
    'Clears the waiting flag, sets agent → <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >, <code>current_tool</code> set. If tool is <code>Agent</code>, subagent record created.':
      '清除等待标志，将 agent 置为 → <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >，并设置 <code>current_tool</code>。如果工具是 <code>Agent</code>，则创建子 agent 记录。',
    "Tool execution completes": "工具执行完成",
    'Clears the waiting flag (covers permission-prompt approvals mid-tool). <code>current_tool</code> cleared. Agent stays <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >.':
      '清除等待标志（涵盖工具执行中途的权限提示批准）。清除 <code>current_tool</code>。agent 保持 <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >。',
    "Claude finishes a turn": "Claude 完成一个轮次",
    'Non-error: main agent → <code>waiting</code> — UI shows <span class="status-chip chip-waiting" ><span class="chip-dot"></span>Waiting</span > until the next user input. <code>stop_reason=error</code>: marks the agent and session <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >. Background subagents keep running.':
      '非错误情况：主 agent → <code>waiting</code> — UI 显示 <span class="status-chip chip-waiting" ><span class="chip-dot"></span>Waiting</span >，直到下一次用户输入。<code>stop_reason=error</code>：将该 agent 和会话标记为 <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >。后台子 agent 继续运行。',
    "Background agent finished": "后台 agent 已完成",
    "Matched subagent → <span class=\"status-chip chip-completed\" ><span class=\"chip-dot\"></span>Completed</span >. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session's <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent's own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks. The same scan also rebuilds the nested-subagent hierarchy — it repoints each subagent's <code>parent_agent_id</code> to the true spawner recovered from the transcript's Task tool result (<code>toolUseResult.agentId</code>), so subagents that spawn their own subagents nest correctly instead of flattening under main.":
      '匹配到的子 agent → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >。刻意 <strong>不</strong> 清除等待标志——后台子 agent 的完成并不能说明人类用户的任何情况。同时启动一次发后即忘的 JSONL 扫描（<code>scanAndImportSubagents</code>），它会遍历会话的 <code>subagents/agent-*.jsonl</code> 文件，按 <code>tool_use_id</code> 将 <code>tool_use</code> ↔ <code>tool_result</code> 块配对，并在每个子 agent 自身的 <code>agent_id</code> 下为每个工具发出 <code>PreToolUse</code> + <code>PostToolUse</code> 事件——从而呈现子 agent 内部进行的、从不触发任何 hook 的工具调用。同一次扫描还会重建嵌套子 agent 的层级——它把每个子 agent 的 <code>parent_agent_id</code> 重新指向从 transcript 的 Task 工具结果(<code>toolUseResult.agentId</code>)中恢复出的真正 spawn 方,因此自己再 spawn 子 agent 的子 agent 会正确嵌套,而不会平铺到 main 之下。',
    "Agent sends notification": "Agent 发送通知",
    'Event logged to activity feed. Permission/input-prompt patterns (e.g. "needs your permission", "waiting for your input") set the agent to <code>waiting</code> and stamp <code>awaiting_input_since</code> (with <code>awaiting_reason</code> = <code>notification</code>). Compaction-related notifications tagged as <code>Compaction</code> events. Triggers a browser notification if enabled.':
      '事件记录到活动信息流。权限/输入提示模式（例如 "needs your permission"、"waiting for your input"）将 agent 置为 <code>waiting</code> 并打上 <code>awaiting_input_since</code>（附带 <code>awaiting_reason</code> = <code>notification</code>）时间戳。与压缩相关的通知会被标记为 <code>Compaction</code> 事件。如果已启用，则触发浏览器通知。',
    "<code>/compact</code> detected in JSONL": "在 JSONL 中检测到 <code>/compact</code>",
    'Creates a compaction subagent → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Detected via <code>isCompactSummary</code> entries in the transcript. Token baselines preserve pre-compaction totals. Periodic scanner (cadence ~¼ of <code>DASHBOARD_STALE_MINUTES</code>) catches compactions when no hooks fire.':
      '创建一个压缩子 agent → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >。通过会话记录中的 <code>isCompactSummary</code> 条目检测。Token 基线会保留压缩前的总量。当没有 hook 触发时，周期性扫描器（节奏约为 <code>DASHBOARD_STALE_MINUTES</code> 的 ¼）会捕获压缩事件。',
    "API error detected in transcript": "在会话记录中检测到 API 错误",
    "Extracted from JSONL during history import, real-time transcript scanning, or the error detection watchdog. Captures quota limits, rate limits, auth failures, and other API errors. <strong>Immediately marks sessions and agents as error</strong> — previously recorded as events without changing status.":
      "在历史导入、实时会话记录扫描或错误检测看门狗过程中从 JSONL 中提取。捕获配额上限、速率限制、认证失败及其他 API 错误。<strong>立即将会话和 agent 标记为错误</strong>——此前仅记录为事件而不改变状态。",
    "Turn cancelled by the user (<code>Esc</code>)": "用户取消了该轮次（<code>Esc</code>）",
    "Synthesized by the watchdog because pressing <code>Esc</code> fires no hook. Recovered either from the transcript <code>[Request interrupted by user]</code> marker (flagged as <code>pendingInterrupt</code>) or, when <code>Esc</code> preceded any output and left no marker, from the idle-working timeout (<code>DASHBOARD_WORKING_IDLE_SECONDS</code>, default 120). Moves the session to <strong>Waiting</strong> — the same state a normal <code>Stop</code> produces.":
      "由看门狗合成，因为按下 <code>Esc</code> 不会触发任何 hook。要么从会话记录中的 <code>[Request interrupted by user]</code> 标记恢复（标记为 <code>pendingInterrupt</code>），要么在 <code>Esc</code> 发生于任何输出之前且未留下标记时，从空闲工作超时恢复（<code>DASHBOARD_WORKING_IDLE_SECONDS</code>，默认 120）。将会话置为 <strong>Waiting</strong>——与正常 <code>Stop</code> 产生的状态相同。",
    "Per-turn timing recorded": "记录每个轮次的计时",
    "Extracted from JSONL turn boundaries. Records the duration of each assistant turn for latency analysis.":
      "从 JSONL 的轮次边界中提取。记录每个助手轮次的持续时间，用于延迟分析。",
    "Claude Code CLI process exits": "Claude Code CLI 进程退出",
    'Drops the waiting flag. If the session is already in <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >, the error state is preserved; otherwise marks all agents and the session as <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Evicts the session\'s transcript from the shared cache.':
      '清除等待标志。如果会话已处于 <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >，则保留错误状态；否则将所有 agent 和该会话标记为 <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >。将该会话的会话记录从共享缓存中逐出。',
    "Clone the repository to your machine": "将仓库克隆到你的机器上",
    "Run <code>npm run setup</code> to install all dependencies":
      "运行 <code>npm run setup</code> 以安装所有依赖",
    "Run <code>npm run dev</code> — server + client launch automatically":
      "运行 <code>npm run dev</code> — 服务端 + 客户端将自动启动",
    "Start a new Claude Code session — events appear in real-time":
      "启动一个新的 Claude Code 会话——事件会实时出现",
    "A multi-stage <code>Dockerfile</code> and <code>docker-compose.yml</code> are included. You can run the monitor with either Docker or Podman and keep the SQLite database in a named volume.":
      "已包含一个多阶段的 <code>Dockerfile</code> 和 <code>docker-compose.yml</code>。你可以使用 Docker 或 Podman 运行该监控器，并将 SQLite 数据库保存在命名卷中。",
    "Hooks auto-install in local mode": "本地模式下自动安装 Hooks",
    "When you run the server directly on the host with <code>npm run dev</code> or <code>npm start</code>, it automatically writes Claude Code hook entries to <code>~/.claude/settings.json</code>. If you run the dashboard in Docker or Podman, install hooks from the host with <code>npm run install-hooks</code> after the container is up, then restart Claude Code. The installer refuses to run inside a container (issue #193) so it never writes a container-internal handler path into a bind-mounted host <code>~/.claude</code>; override with <code>CCAM_ALLOW_CONTAINER_HOOKS=1</code> only if Claude Code itself runs in the container.":
      "当你使用 <code>npm run dev</code> 或 <code>npm start</code> 直接在主机上运行服务器时，它会自动将 Claude Code hook 条目写入 <code>~/.claude/settings.json</code>。如果你在 Docker 或 Podman 中运行仪表盘，请在容器启动后从主机使用 <code>npm run install-hooks</code> 安装 hooks，然后重启 Claude Code。安装程序会拒绝在容器内运行（issue #193），因此绝不会将容器内部的处理器路径写入被绑定挂载的主机 <code>~/.claude</code>；仅当 Claude Code 本身在容器中运行时，才使用 <code>CCAM_ALLOW_CONTAINER_HOOKS=1</code> 覆盖。",
    "This repository also ships a local MCP server under <code>mcp/</code> and extension scaffolding for both Claude Code and Codex. These are optional for the dashboard UI, but recommended for complete local-agent workflows. The MCP server supports stdio (for host integration), HTTP+SSE (for remote clients), and an interactive REPL (for operator debugging).":
      "本仓库还在 <code>mcp/</code> 下附带了一个本地 MCP 服务器，以及面向 Claude Code 和 Codex 的扩展脚手架。对仪表盘界面而言这些是可选的，但建议用于完整的本地 agent 工作流。该 MCP 服务器支持 stdio（用于主机集成）、HTTP+SSE（用于远程客户端）和交互式 REPL（用于运维调试）。",
    "After starting a Claude Code session, you should see:":
      "启动一个 Claude Code 会话后，你应该会看到：",
    Page: "页面",
    Expected: "预期结果",
    Sessions: "会话",
    'Your session listed with status <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> (a fresh CLI is sitting at the prompt) — flips to <span class="status-chip chip-active"><span class="chip-dot"></span>Active</span> the moment Claude starts a turn':
      '你的会话以状态 <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> 列出（一个全新的 CLI 正停留在提示符处）——一旦 Claude 开始一个回合，状态便会切换为 <span class="status-chip chip-active"><span class="chip-dot"></span>Active</span>',
    "Kanban Board": "看板",
    "A <em>Main Agent</em> card in the <strong>Waiting</strong> column until you type your first message; flips to <em>Working</em> on <code>UserPromptSubmit</code> / <code>PreToolUse</code> and back to <em>Waiting</em> after each <code>Stop</code>":
      "在你输入第一条消息之前，<strong>Waiting</strong> 列中会有一张 <em>Main Agent</em> 卡片；在 <code>UserPromptSubmit</code> / <code>PreToolUse</code> 时切换为 <em>Working</em>，并在每次 <code>Stop</code> 后切回 <em>Waiting</em>",
    "Activity Feed": "活动信息流",
    'Events streaming in; click any row to expand payload, use "Session →" to drill into session details':
      "事件持续流入；点击任意行可展开负载，使用 “Session →” 可深入查看会话详情",
    Dashboard: "仪表盘",
    "Stats updating in real-time": "统计数据实时更新",
    "Start server before Claude Code": "在 Claude Code 之前先启动服务器",
    "Hooks only fire to a running server. If Claude Code was already running when you started the dashboard, restart the Claude Code session.":
      "Hooks 只会向正在运行的服务器触发。如果你启动仪表盘时 Claude Code 已经在运行，请重启该 Claude Code 会话。",
    Variable: "变量",
    Default: "默认值",
    Description: "说明",
    "Port the Express server listens on": "Express 服务器监听的端口",
    "Port used by the hook handler to reach the server (for custom port setups)":
      "hook 处理器用来访问服务器的端口（用于自定义端口配置）",
    "Base URL used by the local MCP server when calling dashboard APIs":
      "本地 MCP 服务器调用仪表盘 API 时使用的基础 URL",
    "MCP transport mode: <code>stdio</code>, <code>http</code>, <code>repl</code>":
      "MCP 传输模式：<code>stdio</code>、<code>http</code>、<code>repl</code>",
    "Port for the MCP HTTP+SSE server (only when <code>MCP_TRANSPORT=http</code>)":
      "MCP HTTP+SSE 服务器的端口（仅当 <code>MCP_TRANSPORT=http</code> 时）",
    "Bind address for the MCP HTTP server": "MCP HTTP 服务器的绑定地址",
    "Path to the SQLite database file": "SQLite 数据库文件的路径",
    "Set to <code>0</code> to disable the dead-session liveness reap — the watchdog completes active local sessions only when their matching <code>claude</code> or <code>codex</code> process is gone; auto-disabled on Windows and in containers. Sessions forwarded from another machine (household hooks) report a non-POSIX <code>cwd</code> and are auto-skipped, so a mixed local + forwarded deployment no longer needs this off.":
      "设为 <code>0</code> 可禁用死亡会话的存活性回收——看门狗仅在匹配的本地 <code>claude</code> 或 <code>codex</code> 进程已退出时，才会完成活动会话；在 Windows 和容器内自动禁用。从其他机器转发而来的会话（household hooks）会报告非 POSIX 的 <code>cwd</code>，因此会被自动跳过，所以本地 + 转发混合部署不再需要关闭此项。",
    "Idle gate for watchdog-tick liveness reaps — the transcript must not have been written for at least this long (last hook write is the fallback clock); startup passes skip the gate":
      "看门狗节拍存活性回收的空闲门槛——transcript 必须至少有这么长时间未被写入（无 transcript 时以最后一次 hook 写入为后备时钟）；启动时的回收会跳过该门槛",
    "Idle-working timeout the watchdog uses to recover an <code>Esc</code> cancel that left no transcript marker":
      "看门狗用于恢复未在会话记录中留下标记的 <code>Esc</code> 取消的空闲工作超时",
    "Poll interval for the background sync of <code>~/.claude/projects</code>; <code>0</code> disables the poll but keeps the filesystem watcher":
      "对 <code>~/.claude/projects</code> 进行后台同步的轮询间隔；<code>0</code> 会禁用轮询但保留文件系统监听器",
    "Set to <code>production</code> to serve built client from <code>client/dist/</code>":
      "设为 <code>production</code> 可从 <code>client/dist/</code> 提供已构建的客户端",
    "The server writes the following to <code>~/.claude/settings.json</code> on every startup:":
      "服务器在每次启动时会向 <code>~/.claude/settings.json</code> 写入以下内容：",
    "Existing hooks are preserved. The installer only adds or updates entries containing <code>hook-handler.js</code>.":
      "现有的 hooks 会被保留。安装程序只会添加或更新包含 <code>hook-handler.js</code> 的条目。",
    Script: "脚本",
    Command: "命令",
    "Install all dependencies (server + client)": "安装所有依赖项（服务器 + 客户端）",
    "Start server + client in development mode with hot reload":
      "以带热重载的开发模式启动服务器 + 客户端",
    "Start only the Express server with <code>--watch</code>":
      "仅启动带 <code>--watch</code> 的 Express 服务器",
    "Start only the Vite dev server": "仅启动 Vite 开发服务器",
    "TypeScript check + Vite production build to <code>client/dist/</code>":
      "TypeScript 检查 + Vite 生产构建到 <code>client/dist/</code>",
    "Start Express in production mode serving built client":
      "以生产模式启动 Express 并提供已构建的客户端",
    "Manually write Claude Code hooks to <code>~/.claude/settings.json</code>":
      "手动将 Claude Code hooks 写入 <code>~/.claude/settings.json</code>",
    "Insert demo sessions, agents, and events (8 sessions / 23 agents / 106 events)":
      "插入演示用的会话、agent 和事件（8 个会话 / 23 个 agent / 106 个事件）",
    "Refresh token totals for imported sessions from their transcripts. Never lowers an existing total":
      "从 Transcript 刷新已导入会话的 Token 总计。不会降低已有的总计",
    "Re-derive non-workflow token totals for Claude sessions whose transcripts are still on disk and reset the compaction baselines, preserving workflow and Codex rows. One-time repair for databases inflated by the old per-record usage sum; stop the dashboard before running it":
      "\u4e3a Transcript \u4ecd\u5728\u78c1\u76d8\u4e0a\u7684 Claude \u4f1a\u8bdd\u91cd\u65b0\u63a8\u5bfc\u975e workflow \u7684 Token \u603b\u8ba1\u5e76\u5c06\u4e0a\u4e0b\u6587\u538b\u7f29\u57fa\u7ebf\u6e05\u96f6\uff0c\u540c\u65f6\u4fdd\u7559 workflow \u4e0e Codex \u884c\u3002\u8fd9\u662f\u9488\u5bf9\u56e0\u65e7\u7248\u6309\u8bb0\u5f55\u7d2f\u52a0\u7528\u91cf\u800c\u865a\u9ad8\u7684\u6570\u636e\u5e93\u7684\u4e00\u6b21\u6027\u4fee\u590d\uff1b\u8fd0\u884c\u524d\u8bf7\u5148\u505c\u6b62 Dashboard",
    "Import historical Claude Code sessions from <code>~/.claude</code> with deep JSONL extraction (API errors, turn durations, thinking blocks, subagent data)":
      "从 <code>~/.claude</code> 导入历史 Claude Code 会话，并进行深度 JSONL 提取（API 错误、回合时长、思考块、subagent 数据）",
    "Delete all data from the database (keeps schema)": "删除数据库中的所有数据（保留表结构）",
    "Run all server and client tests": "运行所有服务器和客户端测试",
    "Server integration tests only (Node built-in test runner)":
      "仅运行服务器集成测试（Node 内置测试运行器）",
    "Client unit tests only (Vitest + Testing Library)":
      "仅运行客户端单元测试（Vitest + Testing Library）",
    "Install dependencies for the local MCP package under <code>mcp/</code>":
      "为 <code>mcp/</code> 下的本地 MCP 包安装依赖项",
    "Type-check MCP source without emitting build output":
      "对 MCP 源码进行类型检查，但不产出构建输出",
    "Compile MCP server into <code>mcp/build/</code>":
      "将 MCP 服务器编译到 <code>mcp/build/</code>",
    "Start MCP server (stdio transport — for MCP hosts)":
      "启动 MCP 服务器（stdio 传输——用于 MCP 主机）",
    "Start MCP HTTP+SSE server on port 8819 (Streamable HTTP + legacy SSE)":
      "在端口 8819 上启动 MCP HTTP+SSE 服务器（Streamable HTTP + 传统 SSE）",
    "Start interactive MCP REPL with tab completion and colored output":
      "启动交互式 MCP REPL，支持 Tab 补全和彩色输出",
    "Run MCP server in dev mode with <code>tsx</code> (stdio)":
      "使用 <code>tsx</code> 以开发模式运行 MCP 服务器（stdio）",
    "Run MCP HTTP server in dev mode with <code>tsx</code>":
      "使用 <code>tsx</code> 以开发模式运行 MCP HTTP 服务器",
    "Run MCP REPL in dev mode with <code>tsx</code>":
      "使用 <code>tsx</code> 以开发模式运行 MCP REPL",
    "Build MCP container image with Docker (<code>agent-dashboard-mcp:local</code>)":
      "使用 Docker 构建 MCP 容器镜像（<code>agent-dashboard-mcp:local</code>）",
    "Build MCP container image with Podman (<code>localhost/agent-dashboard-mcp:local</code>)":
      "使用 Podman 构建 MCP 容器镜像（<code>localhost/agent-dashboard-mcp:local</code>）",
    "Run MCP server unit tests": "运行 MCP 服务器单元测试",
    "Install Electron + electron-builder under <code>desktop/</code>; rebuilds <code>better-sqlite3</code> for Electron's ABI. Preflights the native <code>better-sqlite3</code> build; prints actionable setup help (incl. a no-toolchain alternative) on failure":
      "在 <code>desktop/</code> 下安装 Electron + electron-builder；为 Electron 的 ABI 重新构建 <code>better-sqlite3</code>。会预检原生 <code>better-sqlite3</code> 的构建；失败时打印可操作的设置帮助（包括一个无需工具链的替代方案）",
    "Prebuild guard + <code>tsc</code> compile of the Electron main process into <code>desktop/out/</code>":
      "预构建检查 + 使用 <code>tsc</code> 将 Electron 主进程编译到 <code>desktop/out/</code>",
    "Build, then launch the desktop app against <code>desktop/out/main.js</code>":
      "先构建，然后基于 <code>desktop/out/main.js</code> 启动桌面应用",
    "Desktop smoke test — spawn Electron and probe <code>/api/health</code>":
      "桌面冒烟测试——启动 Electron 并探测 <code>/api/health</code>",
    "Build <strong>both</strong> per-arch DMGs (arm64 + x64). Correct for release — slower (packages each arch)":
      "构建<strong>两个</strong>各架构 DMG（arm64 + x64）。适用于发布——较慢（会分别打包每个架构）",
    "Build an Apple-Silicon-only DMG — fast (~1 min), recommended for a single machine":
      "构建仅适用于 Apple Silicon 的 DMG——速度快（约 1 分钟），推荐用于单台机器",
    "Build an Intel-only DMG — fast (macOS host)":
      "构建仅适用于 Intel 的 DMG——速度快（macOS 主机）",
    "Build one merged universal DMG (arm64 + x86_64 in a single file). Optional, slowest — not what the release ships":
      "构建一个合并的通用（universal）DMG（arm64 + x86_64，单个文件）。可选，最慢——不是发布版本附带的内容",
    "Build the Windows NSIS installer <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> (Windows host)":
      "构建 Windows NSIS 安装程序 <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>（Windows 主机）",
    "Build the no-install portable <code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code> (Windows host)":
      "构建免安装的便携版 <code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>（Windows 主机）",
    "Regenerate <code>desktop/assets/icon.ico</code> from <code>icon.png</code> (PowerShell + .NET; Windows host)":
      "从 <code>icon.png</code> 重新生成 <code>desktop/assets/icon.ico</code>（PowerShell + .NET；Windows 主机）",
    "Format all files with Prettier": "使用 Prettier 格式化所有文件",
    "Check formatting without writing": "检查格式但不写入",
    "Core dashboard telemetry is composed of three processes (Claude hook source, dashboard server, browser UI). When the local MCP sidecar is enabled, it integrates with the same dashboard API via stdio, HTTP+SSE, or interactive REPL transport.":
      "核心仪表盘遥测由三个进程组成（Claude 钩子源、仪表盘服务器、浏览器 UI）。当启用本地 MCP 辅助进程时，它会通过 stdio、HTTP+SSE 或交互式 REPL 传输方式集成到同一个仪表盘 API。",
    "Full system architecture — Claude Code process → Hook Layer → Server → Browser":
      "完整系统架构 — Claude Code 进程 → 钩子层 → 服务器 → 浏览器",
    "Agent status transitions driven by hook events. <code>waiting</code> is a real persisted status — agents start as <code>waiting</code> and return to it after each turn. Error recovery requires active user retry (<code>UserPromptSubmit</code> or <code>PreToolUse</code>). A background watchdog detects API errors in transcripts every 15 s. The same watchdog also recovers <code>Esc</code>-cancelled turns — via either the transcript <code>[Request interrupted by user]</code> marker or the idle-working timeout when <code>Esc</code> preceded any output — and moves the session to Waiting.":
      "由钩子事件驱动的 agent 状态转换。<code>waiting</code> 是一个真实的持久化状态 — agent 以 <code>waiting</code> 状态开始，并在每一轮之后返回该状态。错误恢复需要用户主动重试（<code>UserPromptSubmit</code> 或 <code>PreToolUse</code>）。后台看门狗每隔 15 s 在记录中检测 API 错误。同一个看门狗还会恢复被 <code>Esc</code> 取消的轮次——通过会话记录中的 <code>[Request interrupted by user]</code> 标记，或在 <code>Esc</code> 发生于任何输出之前时通过空闲工作超时——并将会话置为 Waiting。",
    "Session status lifecycle. <code>waiting</code> is a UI overlay — persisted as <code>active</code> with <code>awaiting_input_since</code> set and <code>awaiting_reason</code> recording why (<code>session_start</code>, <code>stop</code>, <code>notification</code>, or <code>interrupted</code>). <code>SessionEnd</code> preserves error state. Error recovery requires <code>UserPromptSubmit</code> or <code>PreToolUse</code>. The watchdog also recovers <code>Esc</code>-cancelled turns (marker or idle-timeout path) and moves the session to Waiting. The dashboard surfaces the reason on every Waiting badge (hover tooltip, plus an inline chip on the Sessions table and session detail header) and in a waiting-for-input banner on the session detail page.":
      "会话状态生命周期。<code>waiting</code> 是一个 UI 叠加层 — 实际持久化为 <code>active</code> 并设置了 <code>awaiting_input_since</code>，同时用 <code>awaiting_reason</code> 记录原因（<code>session_start</code>、<code>stop</code>、<code>notification</code> 或 <code>interrupted</code>）。<code>SessionEnd</code> 会保留错误状态。错误恢复需要 <code>UserPromptSubmit</code> 或 <code>PreToolUse</code>。看门狗还会恢复被 <code>Esc</code> 取消的轮次（标记路径或空闲超时路径）并将会话置为 Waiting。仪表盘会在每个 Waiting 徽章上（悬停提示，会话列表与会话详情页头部还会显示内联小徽章）以及会话详情页的等待输入横幅中展示该原因。",
    "Complete event ingestion from hook fire to browser re-render":
      "从钩子触发到浏览器重新渲染的完整事件摄取流程",
    "Initial load + WebSocket subscription lifecycle": "初始加载 + WebSocket 订阅生命周期",
    "Server module dependency graph": "服务器模块依赖关系图",
    Module: "模块",
    Responsibility: "职责",
    "Express app setup, middleware (CORS, JSON 1MB limit), route mounting, static serving in production, HTTP server, auto-hook installation on startup":
      "Express 应用设置、中间件（CORS、JSON 1MB 限制）、路由挂载、生产环境中的静态资源服务、HTTP 服务器、启动时自动安装钩子",
    "SQLite connection, WAL/FK pragmas, schema migrations (<code >CREATE TABLE IF NOT EXISTS</code >), all prepared statements as a reusable <code>stmts</code> object. Tries <code>better-sqlite3</code> first, falls back to built-in <code>node:sqlite</code> via <code>compat-sqlite.js</code>":
      "SQLite 连接、WAL/FK pragma、schema 迁移（<code >CREATE TABLE IF NOT EXISTS</code >）、所有预编译语句作为可复用的 <code>stmts</code> 对象。优先尝试 <code>better-sqlite3</code>，并通过 <code>compat-sqlite.js</code> 回退到内置的 <code>node:sqlite</code>",
    "Compatibility wrapper giving Node.js built-in <code>node:sqlite</code> (<code>DatabaseSync</code>) the same API as <code>better-sqlite3</code> — pragma, transaction, prepare. Used as automatic fallback on Node 22+":
      "兼容性封装层，让 Node.js 内置的 <code>node:sqlite</code>（<code>DatabaseSync</code>）拥有与 <code>better-sqlite3</code> 相同的 API — pragma、transaction、prepare。在 Node 22+ 上用作自动回退方案",
    "WebSocket server on <code>/ws</code> path, 30s ping/pong heartbeat, typed <code>broadcast(type, data)</code> function":
      "位于 <code>/ws</code> 路径的 WebSocket 服务器、30s ping/pong 心跳、带类型的 <code>broadcast(type, data)</code> 函数",
    "Core event processing inside SQLite transactions. Auto-creates sessions/agents. Switch-case dispatch by hook type. Extracts token usage from Stop events.":
      "在 SQLite 事务内进行核心事件处理。自动创建会话/agent。按钩子类型进行 switch-case 分派。从 Stop 事件中提取 token 使用量。",
    "CRUD with pagination. GET includes agent count via LEFT JOIN. POST is idempotent on session ID.":
      "带分页的 CRUD。GET 通过 LEFT JOIN 包含 agent 数量。POST 对会话 ID 是幂等的。",
    "CRUD with status/session_id filtering. PATCH broadcasts <code>agent_updated</code>.":
      "带 status/session_id 过滤的 CRUD。PATCH 会广播 <code>agent_updated</code>。",
    "Read-only event listing with session_id filter and pagination.":
      "只读的事件列表，支持 session_id 过滤和分页。",
    "Single aggregate query — total/active counts, status distributions, WS connection count.":
      "单条聚合查询 — 总数/活跃数、状态分布、WS 连接数。",
    "Extended analytics — token totals, tool usage counts, daily event/session trends, agent type distribution, event type breakdown, average events per session.":
      "扩展分析 — token 总量、工具使用次数、每日事件/会话趋势、agent 类型分布、事件类型细分、每个会话的平均事件数。",
    "Model pricing CRUD (list, upsert, delete). Per-session and global cost calculation with pattern-based model matching and specificity sorting.":
      "模型定价 CRUD（list、upsert、delete）。基于模式的模型匹配和特异性排序，进行单会话和全局成本计算。",
    "System info (DB size, row counts, hook status, server uptime). Data export as JSON. Session cleanup (abandon stale active sessions, purge old completed sessions). Clear all data. Reset pricing to defaults. Reinstall hooks.":
      "系统信息（数据库大小、行数、钩子状态、服务器运行时间）。以 JSON 形式导出数据。会话清理（弃置陈旧的活跃会话、清除旧的已完成会话）。清除所有数据。将定价重置为默认值。重新安装钩子。",
    "Aggregate workflow visualization data (agent orchestration, tool transitions, collaboration networks, workflow patterns, model delegation, error propagation, concurrency, session complexity, compaction impact). Accepts <code>?status=active|completed</code> filter. Per-session drill-in with agent tree, tool timeline, and events.":
      "聚合工作流可视化数据（agent 编排、工具转换、协作网络、工作流模式、模型委派、错误传播、并发、会话复杂度、压缩影响）。接受 <code>?status=active|completed</code> 过滤器。支持按会话深入查看，包含 agent 树、工具时间线和事件。",
    "React component tree": "React 组件树",
    Purpose: "用途",
    "<code>lib/api.ts</code>": "<code>lib/api.ts</code>",
    "Typed fetch wrapper — one method per REST endpoint. All return typed promises.":
      "类型化的 fetch 封装——每个 REST 端点对应一个方法。全部返回类型化的 promise。",
    "<code>lib/types.ts</code>": "<code>lib/types.ts</code>",
    "TypeScript interfaces: <code>Session</code>, <code>Agent</code>, <code>DashboardEvent</code>, <code>Stats</code>, <code>Analytics</code>, <code>WSMessage</code>, plus all workflow-related types (<code>WorkflowData</code>, <code>SessionDrillIn</code>, etc). Status config maps.":
      "TypeScript 接口：<code>Session</code>、<code>Agent</code>、<code>DashboardEvent</code>、<code>Stats</code>、<code>Analytics</code>、<code>WSMessage</code>，以及所有与工作流相关的类型（<code>WorkflowData</code>、<code>SessionDrillIn</code> 等）。还有状态配置映射。",
    "<code>lib/eventBus.ts</code>": "<code>lib/eventBus.ts</code>",
    "Set-based pub/sub. <code>subscribe(fn)</code> returns an unsubscribe function for clean useEffect teardown.":
      "基于 Set 的发布/订阅。<code>subscribe(fn)</code> 返回一个取消订阅函数，便于 useEffect 干净地清理。",
    "<code>lib/format.ts</code>": "<code>lib/format.ts</code>",
    "Date/time formatting helpers — relative time, duration, ISO display.":
      "日期/时间格式化辅助函数——相对时间、时长、ISO 显示。",
    "<code>hooks/useWebSocket.ts</code>": "<code>hooks/useWebSocket.ts</code>",
    "Auto-reconnecting WebSocket React hook. 2-second reconnect interval. Publishes messages to eventBus.":
      "自动重连的 WebSocket React hook。2 秒重连间隔。将消息发布到 eventBus。",
    "The dashboard is a Progressive Web App with its own <code>manifest.json</code> and Service Worker (<code>client/public/sw.js</code>). The landing page and wiki are also independent PWAs with separate manifests and service workers.":
      "该仪表盘是一个渐进式 Web 应用，拥有自己的 <code>manifest.json</code> 和 Service Worker（<code>client/public/sw.js</code>）。落地页和 wiki 也是独立的 PWA，各自拥有独立的 manifest 和 service worker。",
    Surface: "界面",
    Manifest: "Manifest",
    "Service Worker": "Service Worker",
    Strategy: "策略",
    "<code>client/public/manifest.json</code>": "<code>client/public/manifest.json</code>",
    "<code>client/public/sw.js</code>": "<code>client/public/sw.js</code>",
    "Precaches app shell. Cache-first for static assets (JS/CSS bundles). Network-first for navigation with offline fallback. Skips <code>/api/*</code>, <code>/ws</code>, and Vite HMR. Preserves push notification handlers.":
      "预缓存应用外壳。静态资源（JS/CSS 包）采用缓存优先策略。导航采用网络优先策略并提供离线回退。跳过 <code>/api/*</code>、<code>/ws</code> 和 Vite HMR。保留推送通知处理程序。",
    "Landing page": "落地页",
    "<code>manifest.json</code> (root)": "<code>manifest.json</code>（根目录）",
    "<code>sw.js</code> (root)": "<code>sw.js</code>（根目录）",
    "Precaches HTML shell, favicon, OG image. Lazy-caches screenshot PNGs on first view. Network-first HTML, cache-first assets.":
      "预缓存 HTML 外壳、favicon、OG 图片。首次查看时惰性缓存截图 PNG。HTML 采用网络优先，资源采用缓存优先。",
    Wiki: "Wiki",
    "<code>wiki/manifest.json</code>": "<code>wiki/manifest.json</code>",
    "<code>wiki/sw.js</code>": "<code>wiki/sw.js</code>",
    "Precaches <code>index.html</code>, <code>style.css</code>, <code>script.js</code>. Fully offline after one visit.":
      "预缓存 <code>index.html</code>、<code>style.css</code>、<code>script.js</code>。访问一次后即可完全离线使用。",
    'All three SWs call <code>skipWaiting()</code> on install and delete stale caches on activate (keyed by version strings like <code>dashboard-v1</code>). Manifests use SVG icons (<code>favicon.svg</code>) with <code>sizes="any"</code>. iOS standalone mode is enabled via <code>apple-mobile-web-app-capable</code> meta tags.':
      '三个 SW 都在安装时调用 <code>skipWaiting()</code>，并在激活时删除过期缓存（以诸如 <code>dashboard-v1</code> 之类的版本字符串作为键）。manifest 使用 SVG 图标（<code>favicon.svg</code>）并带有 <code>sizes="any"</code>。通过 <code>apple-mobile-web-app-capable</code> meta 标签启用 iOS 独立模式。',
    "The client deliberately avoids Redux / Zustand / Context. Each page owns its data and lifecycle. WebSocket events trigger a reload or append — no complex state merging.":
      "客户端刻意避免使用 Redux / Zustand / Context。每个页面拥有自己的数据和生命周期。WebSocket 事件触发重新加载或追加——没有复杂的状态合并。",
    "No global store — by design": "没有全局存储——这是有意为之的设计",
    "There is no cross-page shared state. Each page fetches and owns exactly the data it displays. This simplifies debugging and avoids stale-closure hazards that are common with global stores in long-running WebSocket apps.":
      "不存在跨页面共享的状态。每个页面只获取并拥有它所显示的数据。这简化了调试，并避免了在长时间运行的 WebSocket 应用中使用全局存储时常见的过期闭包隐患。",
    Index: "索引",
    Table: "表",
    "Column(s)": "列",
    "Fast agent lookup by session": "按会话快速查找代理",
    "Kanban column queries": "看板列查询",
    "Session detail event list": "会话详情事件列表",
    "Filter events by type": "按类型过滤事件",
    "Activity feed ordering": "活动流排序",
    "Status filter on sessions page": "会话页面上的状态过滤",
    "Default sort order": "默认排序顺序",
    Pragma: "Pragma",
    Value: "数值",
    Rationale: "理由",
    "Concurrent reads during writes. Far better for read-heavy dashboards.":
      "在写入期间支持并发读取。对于读取密集型仪表盘要好得多。",
    "Referential integrity — prevents orphaned agents/events.":
      "引用完整性——防止出现孤立的代理/事件。",
    "Wait up to 5s for write lock instead of failing immediately under load.":
      "等待写锁最多 5 秒，而不是在负载下立即失败。",
    'All endpoints return JSON. Errors follow <code>{ "error": { "code", "message" } }</code>. The OpenAPI 3.0 spec comprehensively documents every backend route - parameters, request/response schemas, field descriptions, and examples. It is served at <code>/api/openapi.json</code> (with a committed <code>openapi.yaml</code> mirror), rendered as interactive Swagger UI at <code>/api/docs</code>, and as a clean, read-optimized ReDoc reference at <code>/api/redoc</code>. ReDoc is self-hosted, so it works fully offline.':
      '所有端点都返回 JSON。错误遵循 <code>{ "error": { "code", "message" } }</code> 格式。OpenAPI 3.0 规范全面记录了每个后端路由——参数、请求/响应模式、字段说明以及示例。它在 <code>/api/openapi.json</code> 提供（并附带提交到仓库的 <code>openapi.yaml</code> 镜像），在 <code>/api/docs</code> 渲染为交互式 Swagger UI，并在 <code>/api/redoc</code> 渲染为简洁、便于阅读的 ReDoc 参考文档。ReDoc 为本地自托管，因此可完全离线使用。',
    '<span class="caption-icon">📘</span> <span>Swagger UI at <code>/api/docs</code> — auto-generated interactive playground for every REST endpoint. Try-it-out forms, request/response schema, auth headers, and curl snippets</span>':
      '<span class="caption-icon">📘</span> <span>位于 <code>/api/docs</code> 的 Swagger UI——为每个 REST 端点自动生成的交互式演练场。包含试用表单、请求/响应模式、认证标头以及 curl 代码片段</span>',
    Property: "属性",
    Path: "路径",
    Protocol: "协议",
    "Standard WebSocket (RFC 6455)": "标准 WebSocket（RFC 6455）",
    Heartbeat: "心跳",
    "Server pings every 30s — clients that don't pong are terminated":
      "服务器每隔 30s ping 一次——未回应 pong 的客户端将被终止",
    Reconnect: "重连",
    "Client retries every 2 seconds on disconnect": "断开连接后客户端每隔 2 秒重试一次",
    "Client WebSocket auto-reconnect state machine": "客户端 WebSocket 自动重连状态机",
    "<code>scripts/hook-handler.js</code> is a minimal, fail-safe forwarder. It always exits 0 so it can never block Claude Code regardless of server state.":
      "<code>scripts/hook-handler.js</code> 是一个极简的故障安全转发器。它始终以 0 退出，因此无论服务器处于何种状态都绝不会阻塞 Claude Code。",
    "hook-handler.js flow — always exits 0, never blocks Claude Code":
      "hook-handler.js 流程——始终以 0 退出，绝不阻塞 Claude Code",
    "Hook installation is idempotent — safe to run multiple times":
      "Hook 安装是幂等的——可以安全地多次运行",
    '<span class="caption-icon">📥</span> <span>Settings → Import History — rescan default paths, set a custom directory, or drag-and-drop <code>.gz</code> archives. Progress bar and result card show counts for every run</span>':
      '<span class="caption-icon">📥</span> <span>Settings → Import History——重新扫描默认路径、设置自定义目录，或拖放 <code>.gz</code> 归档文件。进度条和结果卡片会显示每次运行的计数</span>',
    "The dashboard ships with a first-class <b>history importer</b> that backfills sessions, agents, events, tokens, and costs from Claude Code JSONL transcripts. Live hook ingestion and manual import share the exact same parser — <code>parseSessionFile</code> + <code>importSession</code> in <code>scripts/import-history.js</code> — which is the architectural contract that guarantees imported token totals and cost values are identical to those captured in real time. Re-imports are idempotent: session IDs are the dedup key and compaction <code>baseline_*</code> columns preserve pre-compaction token totals.":
      "仪表盘内置了一流的<b>历史导入器</b>，可从 Claude Code 的 JSONL 转录文件回填会话、智能体、事件、token 和成本。实时 hook 摄取与手动导入共用完全相同的解析器——<code>scripts/import-history.js</code> 中的 <code>parseSessionFile</code> + <code>importSession</code>——这是一项架构契约，保证导入的 token 总量和成本值与实时捕获的完全一致。重新导入是幂等的：会话 ID 是去重键，而压缩后的 <code>baseline_*</code> 列会保留压缩前的 token 总量。",
    "All three modes funnel into the same parser and DB transaction — imported numbers match live capture bit-for-bit":
      "三种模式都汇入同一个解析器和数据库事务——导入的数字与实时捕获逐位一致",
    "Upload path: multipart → safe extract → walk → parse → import — every temp dir reclaimed in <code>finally</code>":
      "上传路径：multipart → 安全解压 → 遍历 → 解析 → 导入——每个临时目录都在 <code>finally</code> 中被回收",
    "The <code>baseline_*</code> columns make cost monotonic under re-imports — compacted sessions retain pre-compaction usage for billing":
      "<code>baseline_*</code> 列使成本在重新导入时保持单调——被压缩的会话会保留压缩前的用量以供计费",
    Layout: "布局",
    Example: "示例",
    Handling: "处理方式",
    "Default Claude Code": "默认 Claude Code",
    "Session transcript — extracts tokens, compactions, tool uses, turn durations":
      "会话转录——提取 token、压缩、工具使用和回合时长",
    "Default subagent": "默认子智能体",
    "Paired with parent on discovery via <code>findSessionSubagents</code>":
      "在发现时通过 <code>findSessionSubagents</code> 与父级配对",
    "Alternative subagent": "备用子智能体",
    "Paired with parent on discovery (second layout probed automatically)":
      "在发现时与父级配对（自动探测第二种布局）",
    "Orphan subagent": "孤立子智能体",
    "No parent JSONL in source, but <code>sid</code> exists in DB":
      "源中没有父级 JSONL，但 <code>sid</code> 存在于数据库中",
    "<code>importFromDirectory</code> probes both layouts; attaches if the parent is found":
      "<code>importFromDirectory</code> 会探测两种布局；若找到父级则进行关联",
    "Flat JSONL drop": "扁平 JSONL 投放",
    "Recognized as a loose session transcript": "被识别为松散的会话转录",
    Archives: "归档文件",
    "Extracted into a per-request temp dir, then walked by the same importer":
      "解压到每个请求的临时目录中，然后由同一个导入器遍历",
    "Single-file gzip": "单文件 gzip",
    "Gunzipped in streaming mode with running byte-counter size cap":
      "以流式模式解压 gzip，并使用运行中的字节计数器进行大小上限控制",
    Threat: "威胁",
    Mitigation: "缓解措施",
    "Path traversal via archive entries": "通过归档条目进行的路径穿越",
    "<code>archive.safeJoin</code> resolves under the extraction root; any <code>..</code> or absolute path returns <code>null</code>":
      "<code>archive.safeJoin</code> 在解压根目录下解析；任何 <code>..</code> 或绝对路径都会返回 <code>null</code>",
    "Zip / tar / gzip bombs": "Zip / tar / gzip 炸弹",
    "<code>MAX_EXTRACT_BYTES</code> (default 4 GB) enforced by running byte counter; aborts with <code>ExtractionLimitError</code> → HTTP 413":
      "由运行中的字节计数器强制执行 <code>MAX_EXTRACT_BYTES</code>（默认 4 GB）；以 <code>ExtractionLimitError</code> → HTTP 413 中止",
    "Per-file upload size abuse": "针对单个文件上传大小的滥用",
    "multer <code>limits.fileSize = MAX_UPLOAD_BYTES</code> (default 1 GB)":
      "multer <code>limits.fileSize = MAX_UPLOAD_BYTES</code>（默认 1 GB）",
    "Too many files per request": "每个请求的文件数过多",
    "multer <code>limits.files = MAX_UPLOAD_FILES</code> (default 2000)":
      "multer <code>limits.files = MAX_UPLOAD_FILES</code>（默认 2000）",
    "Unsupported file types": "不受支持的文件类型",
    "<code>fileFilter</code> drops them early and reports them in <code>rejected_files[]</code>":
      "<code>fileFilter</code> 会及早丢弃它们，并在 <code>rejected_files[]</code> 中报告",
    "Concurrent upload temp-dir collisions": "并发上传时的临时目录冲突",
    "Per-request temp dir on <code>req._ccamUploadDir</code>; created in multer <code>destination</code>, reclaimed in <code>finally</code>":
      "在 <code>req._ccamUploadDir</code> 上为每个请求设置临时目录；在 multer <code>destination</code> 中创建，在 <code>finally</code> 中回收",
    "Arbitrary absolute path on <code>scan-path</code>": "<code>scan-path</code> 上的任意绝对路径",
    "Validated: must be absolute (after <code>~</code> expansion), exist, and be a directory":
      "经过校验：必须是绝对路径（在 <code>~</code> 展开后）、存在且为目录",
    "Relative / traversal paths on <code>scan-path</code>":
      "<code>scan-path</code> 上的相对路径 / 穿越路径",
    "Rejected with <code>INVALID_INPUT</code>": "以 <code>INVALID_INPUT</code> 拒绝",
    "Maximum size per uploaded file on <code>/api/import/upload</code>":
      "<code>/api/import/upload</code> 上每个上传文件的最大大小",
    "Maximum files per upload request": "每个上传请求的最大文件数",
    "Ceiling on total uncompressed bytes from any single archive (zip-bomb defense)":
      "对任何单个归档文件解压后总字节数的上限（zip 炸弹防御）",
    "Every import emits <code>import.progress</code> messages on <code>/ws</code>. Messages are throttled to at most one every ~150 ms to avoid flooding the channel on multi-thousand-session imports; the terminal <code>complete</code> and <code>error</code> frames are never throttled.":
      "每次导入都会在 <code>/ws</code> 上发出 <code>import.progress</code> 消息。消息被节流为最多每 ~150 ms 一条，以避免在数千会话的导入中淹没通道；终止帧 <code>complete</code> 和 <code>error</code> 永远不会被节流。",
    "Phases: <code>start</code> → <code>scan</code> → <code>extract</code> (upload only) → <code>parse</code> → <code>complete</code>, with <code>error</code> / <code>extract_error</code> replacing <code>complete</code> on failure.":
      "各阶段：<code>start</code> → <code>scan</code> → <code>extract</code>（仅上传时）→ <code>parse</code> → <code>complete</code>，失败时由 <code>error</code> / <code>extract_error</code> 取代 <code>complete</code>。",
    "In addition to dashboard telemetry, this project includes a production-grade local MCP server and complete extension scaffolding for both Claude Code and Codex. This gives agents a richer local tool surface while keeping all execution local-first. The MCP server supports three transport modes: stdio for host integration, HTTP+SSE for remote clients, and an interactive REPL for operator debugging.":
      "除了仪表盘遥测之外，本项目还包含一个生产级的本地 MCP 服务器，以及面向 Claude Code 和 Codex 的完整扩展脚手架。这为智能体提供了更丰富的本地工具面，同时保持所有执行都以本地优先。MCP 服务器支持三种传输模式：用于宿主集成的 stdio、用于远程客户端的 HTTP+SSE，以及用于操作者调试的交互式 REPL。",
    '<span class="caption-icon">🔧</span> MCP Server REPL — interactive tool invocation terminal with colored JSON output, argument prompts, error formatting, and session-aware context for rapid testing':
      '<span class="caption-icon">🔧</span> MCP 服务器 REPL——交互式工具调用终端，具有彩色 JSON 输出、参数提示、错误格式化和会话感知上下文，便于快速测试',
    "Local extension architecture: host instructions + skills + multi-transport MCP sidecar":
      "本地扩展架构：宿主指令 + 技能 + 多传输 MCP 边车",
    "The <code>mcp/</code> package exposes dashboard-oriented tools for AI agents across three transport modes. Mutation and destructive operations are policy-gated by environment variables and disabled by default. HTTP mode serves both Streamable HTTP (protocol 2025-11-25) and legacy SSE (protocol 2024-11-05). REPL mode provides tab-completed interactive tool invocation with colored output and JSON syntax highlighting.":
      "<code>mcp/</code> 包通过三种传输模式向 AI 智能体公开面向仪表盘的工具。变更和破坏性操作由环境变量进行策略门控，并且默认禁用。HTTP 模式同时提供 Streamable HTTP（协议 2025-11-25）和旧版 SSE（协议 2024-11-05）。REPL 模式提供带 Tab 补全的交互式工具调用，并带有彩色输出和 JSON 语法高亮。",
    Component: "组件",
    Location: "位置",
    Notes: "备注",
    "MCP source": "MCP 源码",
    "TypeScript server, tools, policy guards, transport layer, CLI UI":
      "TypeScript 服务器、工具、策略守卫、传输层、CLI UI",
    "MCP build output": "MCP 构建产物",
    "Compiled JavaScript runtime for all transport modes":
      "适用于所有传输模式的已编译 JavaScript 运行时",
    "MCP docs": "MCP 文档",
    "Tool catalog, architecture diagrams, host integration examples, REPL guide":
      "工具目录、架构图、宿主集成示例、REPL 指南",
    "Transport layer": "传输层",
    "HTTP+SSE server, interactive REPL, tool handler collector":
      "HTTP+SSE 服务器、交互式 REPL、工具处理器收集器",
    "CLI UI": "CLI UI",
    "ANSI banner, colors, formatter with tables, boxes, JSON highlighting":
      "ANSI 横幅、配色、带表格、方框和 JSON 高亮的格式化器",
    "Runtime commands": "运行时命令",
    "Start MCP in stdio, HTTP+SSE, or REPL mode (production or dev)":
      "以 stdio、HTTP+SSE 或 REPL 模式启动 MCP（生产或开发）",
    Target: "目标",
    Files: "文件",
    "Claude Code": "Claude Code",
    "Persistent project instructions + path-scoped coding rules":
      "持久化的项目指令 + 按路径限定的编码规则",
    "Claude Code Skills": "Claude Code 技能",
    "Reusable workflows (onboarding, shipping, MCP ops, live debugging)":
      "可复用的工作流（上手引导、发布、MCP 运维、实时调试）",
    "Claude Code Subagents": "Claude Code 子代理",
    "Specialized reviewers for backend, frontend, and MCP code paths":
      "针对后端、前端和 MCP 代码路径的专用审查器",
    "Codex Base Instructions": "Codex 基础指令",
    "Project-wide guidance + execution policy defaults": "项目级指导 + 执行策略默认值",
    "Codex Skills": "Codex 技能",
    "Task-specific skills aligned to this repository": "与本仓库对齐的特定任务技能",
    "Codex Agents": "Codex 代理",
    "Reusable custom-agent templates for implementation and review":
      "用于实现和审查的可复用自定义代理模板",
    Role: "作用",
    "Receives Claude hook payloads over stdin and forwards them to dashboard API":
      "通过 stdin 接收 Claude 钩子负载并将其转发到仪表板 API",
    "Writes/updates hook registration in <code>~/.claude/settings.json</code>":
      "在 <code>~/.claude/settings.json</code> 中写入/更新钩子注册",
    "Batch history importer used by server startup auto-import, the <code>/api/import/*</code> routes, and the <code>import-history</code> CLI. Exposes <code>importAllSessions()</code> for the default projects dir and the generalized <code>importFromDirectory(dbModule, rootDir, {onProgress})</code> which walks any directory recursively, classifies session vs subagent JSONLs (probes both <code>&lt;proj&gt;/&lt;sid&gt;/subagents/*</code> and <code>&lt;proj&gt;/subagents/&lt;sid&gt;/*</code> layouts), and funnels everything through the shared <code>parseSessionFile</code> + <code>importSession</code> pipeline — identical to live ingest. <b>Re-import is fully incremental</b>: a per-event-type high-water mark (<code>MAX(created_at) GROUP BY event_type</code> for the session) drives <code>ts &gt; cutoff[type]</code> dedup for Stop / PostToolUse / TurnDuration / ToolError, so long-running sessions whose transcripts grow across multiple days keep receiving new events on every re-run. <code>sessions.ended_at</code> is rolled forward when the JSONL has progressed past the stored value, and message-count metadata is refreshed on every pass. Session-ID dedup and <code>baseline_*</code> preservation keep token totals stable. Extracts tokens, API errors, turn durations, thinking blocks, usage extras, and per-subagent breakdowns":
      "批量历史导入器，用于服务器启动时的自动导入、<code>/api/import/*</code> 路由以及 <code>import-history</code> CLI。为默认项目目录暴露了 <code>importAllSessions()</code>，并提供通用的 <code>importFromDirectory(dbModule, rootDir, {onProgress})</code>，它会递归遍历任意目录，区分会话与子代理 JSONL（同时探测 <code>&lt;proj&gt;/&lt;sid&gt;/subagents/*</code> 和 <code>&lt;proj&gt;/subagents/&lt;sid&gt;/*</code> 两种布局），并将一切汇入共享的 <code>parseSessionFile</code> + <code>importSession</code> 流水线——与实时摄取完全一致。<b>重新导入是完全增量的</b>：按事件类型划分的高水位线（针对会话的 <code>MAX(created_at) GROUP BY event_type</code>）驱动 Stop / PostToolUse / TurnDuration / ToolError 的 <code>ts &gt; cutoff[type]</code> 去重，因此那些转录内容跨多天增长的长时间运行会话会在每次重新运行时持续接收新事件。当 JSONL 进展超过已存储的值时，<code>sessions.ended_at</code> 会向前滚动，且每一遍都会刷新消息计数元数据。会话 ID 去重和 <code>baseline_*</code> 保留使令牌总量保持稳定。提取令牌、API 错误、轮次时长、思考块、用量附加项以及按子代理划分的明细",
    "Express router for Import History. Four endpoints: <code>GET /api/import/guide</code> (OS-aware instructions + default-dir stats), <code>POST /api/import/rescan</code> (default <code>~/.claude/projects</code>), <code>POST /api/import/scan-path</code> (arbitrary absolute dir with <code>~</code> expansion), <code>POST /api/import/upload</code> (multer multipart). Each request uses a per-request temp dir reclaimed in <code>finally</code>. Progress broadcast as throttled <code>import.progress</code> WebSocket messages. Limits tunable via <code>CCAM_IMPORT_MAX_BYTES</code>, <code>CCAM_IMPORT_MAX_FILES</code>, <code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>":
      "用于导入历史的 Express 路由。四个端点：<code>GET /api/import/guide</code>（识别操作系统的说明 + 默认目录统计）、<code>POST /api/import/rescan</code>（默认 <code>~/.claude/projects</code>）、<code>POST /api/import/scan-path</code>（带 <code>~</code> 展开的任意绝对目录）、<code>POST /api/import/upload</code>（multer multipart）。每个请求使用一个按请求划分、在 <code>finally</code> 中回收的临时目录。进度通过限流的 <code>import.progress</code> WebSocket 消息广播。限制可通过 <code>CCAM_IMPORT_MAX_BYTES</code>、<code>CCAM_IMPORT_MAX_FILES</code>、<code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code> 调整",
    "Safe archive extraction: <code>.zip</code> via <code>adm-zip</code>, <code>.tar</code>/<code>.tar.gz</code>/<code>.tgz</code> via <code>tar</code>, plain <code>.gz</code> streaming via <code>zlib</code>. Every entry validated through <code>safeJoin</code> which rejects absolute paths and <code>..</code> traversal before any bytes are written. Enforces a hard <code>MAX_EXTRACT_BYTES</code> cap (default 4 GB) with <code>ExtractionLimitError</code> surfaced as HTTP 413 — defense against zip/tar/gzip bombs":
      "安全的归档解压：<code>.zip</code> 通过 <code>adm-zip</code>，<code>.tar</code>/<code>.tar.gz</code>/<code>.tgz</code> 通过 <code>tar</code>，纯 <code>.gz</code> 通过 <code>zlib</code> 流式处理。每个条目都经过 <code>safeJoin</code> 校验，在写入任何字节之前拒绝绝对路径和 <code>..</code> 穿越。强制执行硬性的 <code>MAX_EXTRACT_BYTES</code> 上限（默认 4 GB），并以 HTTP 413 形式抛出 <code>ExtractionLimitError</code>——用于防御 zip/tar/gzip 炸弹",
    "Loads deterministic demo data for testing and demos": "为测试和演示加载确定性的演示数据",
    "Removes persisted rows while preserving schema": "在保留数据库结构的同时移除已持久化的行",
    "The Agent Monitor ships with an official Claude Code plugin marketplace containing ten production-ready plugins (53 skills, 14 agents, 30 slash commands, 3 CLI tools, 3 hook configs, and 1 MCP server). These extend Claude Code with skills, agents, hooks, CLI tools, and MCP integration — all grounded in the real data model (token tracking with compaction baselines, cost calculation via pattern-matched pricing rules, workflow intelligence with 11 datasets per session, and session metadata including thinking blocks, turn counts, and inference geography).":
      "Agent Monitor 附带一个官方的 Claude Code 插件市场，包含十个生产就绪的插件（53 个技能、14 个智能体、30 个斜杠命令、3 个 CLI 工具、3 个 hook 配置以及 1 个 MCP 服务器）。它们通过技能、代理、钩子、CLI 工具和 MCP 集成来扩展 Claude Code——全部基于真实的数据模型构建（带压缩基线的令牌跟踪、通过模式匹配定价规则进行的成本计算、每个会话包含 11 个数据集的工作流智能，以及包含思考块、轮次计数和推理地理位置的会话元数据）。",
    Plugin: "插件",
    Skills: "技能",
    Agent: "Agent",
    "CLI Tools": "CLI 工具",
    Focus: "侧重点",
    "Token usage (4 types + baselines), cost via pricing engine, daily trends, productivity scoring":
      "令牌用量（4 种类型 + 基线）、通过定价引擎计算的成本、每日趋势、生产力评分",
    "Standup reports, sprint tracking, workflow optimization via 11 workflow intelligence datasets":
      "站会报告、冲刺跟踪、通过 11 个工作流智能数据集进行的工作流优化",
    "Session debugging, hook diagnostics, data export (JSON/CSV), system health":
      "会话调试、钩子诊断、数据导出（JSON/CSV）、系统健康状况",
    "Pattern detection via tool flow transitions, anomaly alerting, optimization, session comparison":
      "通过工具流转换进行的模式检测、异常告警、优化、会话对比",
    "Budget guardrails: set budgets, forecast week/month-end spend, cost-threshold alerts, model-routing savings (fail-safe Stop hook)":
      "预算护栏：设置预算、预测周末/月末支出、成本阈值告警、模型路由节省（故障安全的 Stop hook）",
    "Session forensics: search, timeline, transcript replay, per-cwd rollup, cleanup":
      "会话取证：搜索、时间线、transcript 回放、按 cwd 汇总、清理",
    "Multi-agent orchestration &amp; fleet intelligence: DAG map, delegation audit, concurrency, error propagation, fleet runs (11-dataset workflow intelligence API)":
      "多智能体编排与机群智能：DAG 图、委派审计、并发、错误传播、机群运行（11 个数据集的工作流智能 API）",
    "Reliability &amp; SLOs: error scan, API-error report, hook-failure audit, SLO check, regression alert":
      "可靠性与 SLO：错误扫描、API 错误报告、hook 失败审计、SLO 检查、回归告警",
    "Claude Code config &amp; memory governance: config audit, memory review, skill/MCP/hook inventory (via the Config Explorer API)":
      "Claude Code 配置与记忆治理：配置审计、记忆审查、技能/MCP/hook 清点（通过 Config Explorer API）",
    "Dashboard connector with MCP integration and one-line metric summaries":
      "带 MCP 集成的仪表板连接器以及单行指标摘要",
    "Each plugin follows the official Claude Code plugin specification. The marketplace manifest at <code>.claude-plugin/marketplace.json</code> catalogs all ten plugins. Each plugin directory contains:":
      "每个插件都遵循官方的 Claude Code 插件规范。位于 <code>.claude-plugin/marketplace.json</code> 的市场清单编录了全部十个插件。每个插件目录包含：",
    "All plugins query the Agent Monitor API at <code>http://localhost:4820</code>. Key capabilities they leverage:":
      "所有插件都在 <code>http://localhost:4820</code> 查询 Agent Monitor API。它们所利用的关键能力：",
    Capability: "能力",
    Details: "详情",
    "Token tracking": "令牌跟踪",
    "4 types (input, output, cache_read, cache_write) + 4 compaction baselines per model per session":
      "4 种类型（input、output、cache_read、cache_write）+ 每个会话每个模型 4 个压缩基线",
    "Cost calculation": "成本计算",
    "<code>(tokens / 1M) × rate_per_mtok</code> for each type; longest pattern match wins":
      "每种类型为 <code>(tokens / 1M) × rate_per_mtok</code>；最长模式匹配胜出",
    "Session metadata": "会话元数据",
    "Event types": "事件类型",
    "Workflow intelligence": "工作流智能",
    "11 datasets: stats, orchestration (DAG), toolFlow, effectiveness, patterns, modelDelegation, errorPropagation, concurrency, complexity, compaction, cooccurrence":
      "11 个数据集：stats、orchestration（DAG）、toolFlow、effectiveness、patterns、modelDelegation、errorPropagation、concurrency、complexity、compaction、cooccurrence",
    "Agent hierarchy": "代理层级",
    "Recursive parent/child tree with subagent_type, depth tracking via recursive CTE":
      "带 subagent_type 的递归父/子树，通过递归 CTE 进行深度跟踪",
    '📖 Full documentation: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/docs/PLUGINS.md"><code>docs/plugins.md</code></a>':
      '📖 完整文档：<a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/docs/PLUGINS.md"><code>docs/plugins.md</code></a>',
    '<span class="caption-icon">🖥️</span> Statusline — always-visible bar showing context window usage, token counts, active model, git branch, and session ID. Configurable segments with theme support':
      '<span class="caption-icon">🖥️</span> 状态栏——始终可见的栏，显示上下文窗口用量、令牌计数、活动模型、git 分支和会话 ID。可配置的分段并支持主题',
    "The <code>statusline/</code> directory contains a standalone CLI statusline for Claude Code — completely independent of the web dashboard. It renders a color-coded bar at the bottom of the Claude Code terminal showing context window usage, per-direction token counts, session cost in USD, and git branch.":
      "<code>statusline/</code> 目录包含一个用于 Claude Code 的独立 CLI 状态栏——完全独立于 Web 仪表板。它在 Claude Code 终端底部渲染一个带颜色编码的栏，显示上下文窗口用量、按方向划分的令牌计数、以 USD 计的会话成本以及 git 分支。",
    Segment: "分段",
    Source: "来源",
    "Color Logic": "颜色逻辑",
    "Always cyan": "始终为青色",
    "Always green": "始终为绿色",
    "Always yellow, <code>~</code> prefix for home": "始终为黄色，主目录加 <code>~</code> 前缀",
    "Always magenta, hidden outside git repos": "始终为洋红色，在 git 仓库之外隐藏",
    "Green &lt; 50%, Yellow 50–79%, Red ≥ 80%": "&lt; 50% 为绿色，50–79% 为黄色，≥ 80% 为红色",
    "Green <code>↑</code> input, cyan <code>↓</code> output, dim <code>c</code> cache reads":
      "绿色 <code>↑</code> 为输入，青色 <code>↓</code> 为输出，暗色 <code>c</code> 为缓存读取",
    "Green &lt; $5, Yellow $5–$20, Red ≥ $20 (shown on API and subscription plans)":
      "&lt; $5 为绿色，$5–$20 为黄色，≥ $20 为红色（在 API 和订阅计划上显示）",
    "Add this to <code>~/.claude/settings.json</code>:":
      "将此内容添加到 <code>~/.claude/settings.json</code>：",
    "No dependencies required": "无需任何依赖",
    "The statusline uses only Python 3.6+ stdlib (<code>sys</code>, <code>json</code>, <code>os</code>, <code>subprocess</code>). It fails silently on empty input or JSON errors and never blocks Claude Code.":
      "该状态栏仅使用 Python 3.6+ 标准库（<code>sys</code>、<code>json</code>、<code>os</code>、<code>subprocess</code>）。在空输入或 JSON 错误时会静默失败，且绝不会阻塞 Claude Code。",
    '<span class="caption-icon">🔌</span> Sidebar — live health, analytics, and deep navigation links':
      '<span class="caption-icon">🔌</span> 侧边栏 — 实时健康状态、分析和深度导航链接',
    "The <b>Claude Code Agent Monitor</b> is a premium, high-fidelity extension designed to minimize context switching for AI engineers. It brings the full power of the dashboard directly into VS Code, allowing you to monitor complex subagent orchestration without ever leaving your active code file.":
      "<b>Claude Code Agent Monitor</b> 是一款高品质、高保真的扩展，旨在为 AI 工程师减少上下文切换。它将仪表盘的全部能力直接带入 VS Code，让你无需离开当前代码文件即可监控复杂的子代理编排。",
    "A dedicated Activity Bar view that performs background polling every 5 seconds. Includes a real-time <b>Agent Health</b> monitor tracking all 5 states (Working, Connected, Idle, Completed, Error) with native VS Code theme-aware icons and colors.":
      "一个专属的活动栏视图，每 5 秒进行一次后台轮询。包含一个实时的 <b>Agent Health</b> 监视器，跟踪全部 5 种状态（Working、Connected、Idle、Completed、Error），并采用原生的、感知 VS Code 主题的图标和颜色。",
    "Aggregates data from multiple API endpoints to display high-signal metrics directly in the sidebar: <ul style=\"margin-top: 8px; color: var(--text-muted); font-size: 13px; list-style-type: '→ '; padding-left: 15px;\"> <li><b>Token Consumption</b>: Scaled tracking from 1k to 1.0B+ tokens.</li> <li><b>Live Cost Estimates</b>: Automatic USD cost calculation based on model pricing rules.</li> <li><b>Event Frequency</b>: Total events, daily sessions, and subagent spawning rates.</li> </ul>":
      "聚合来自多个 API 端点的数据，直接在侧边栏中显示高信号指标： <ul style=\"margin-top: 8px; color: var(--text-muted); font-size: 13px; list-style-type: '→ '; padding-left: 15px;\"> <li><b>Token Consumption</b>：从 1k 到 1.0B+ tokens 的分级跟踪。</li> <li><b>Live Cost Estimates</b>：根据模型定价规则自动计算美元成本。</li> <li><b>Event Frequency</b>：总事件数、每日会话数和子代理生成速率。</li> </ul>",
    "<b>Token Consumption</b>: Scaled tracking from 1k to 1.0B+ tokens.":
      "<b>Token Consumption</b>：从 1k 到 1.0B+ tokens 的分级跟踪。",
    "<b>Live Cost Estimates</b>: Automatic USD cost calculation based on model pricing rules.":
      "<b>Live Cost Estimates</b>：根据模型定价规则自动计算美元成本。",
    "<b>Event Frequency</b>: Total events, daily sessions, and subagent spawning rates.":
      "<b>Event Frequency</b>：总事件数、每日会话数和子代理生成速率。",
    "Renders the full React application within a native webview tab. Supports <b>Deep Linking</b>: one-click jump from the sidebar directly to specific views like the <i>Kanban Board</i>, <i>Analytics Hub</i>, or your <i>Last 10 Sessions</i>.":
      "在原生 webview 标签页中渲染完整的 React 应用。支持 <b>Deep Linking</b>：从侧边栏一键直达特定视图，例如 <i>Kanban Board</i>、<i>Analytics Hub</i> 或你的 <i>Last 10 Sessions</i>。",
    "Seamlessly scans ports <code>5173</code> (Vite Dev) and <code>4820</code> (Production) on localhost. Automatically toggles between <b>Online</b> and <b>Offline</b> modes in the sidebar as you start or stop your local server.":
      "无缝扫描 localhost 上的端口 <code>5173</code>（Vite Dev）和 <code>4820</code>（Production）。当你启动或停止本地服务器时，会在侧边栏中自动切换 <b>Online</b> 和 <b>Offline</b> 模式。",
    "<strong>Zero-Config Setup</strong>": "<strong>零配置安装</strong>",
    "The extension is designed to be plug-and-play. Once your server is running, the extension automatically discovers the API and begins streaming telemetry — no manual URL configuration required.":
      "该扩展设计为即插即用。一旦服务器运行，扩展会自动发现 API 并开始流式传输遥测数据 — 无需手动配置 URL。",
    '📖 Full developer guide: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/vscode-extension/README.md"><code>vscode-extension/README.md</code></a>':
      '📖 完整开发者指南：<a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/vscode-extension/README.md"><code>vscode-extension/README.md</code></a>',
    "The dashboard ships as an optional <strong>native desktop application</strong> — a <code>desktop/</code> workspace that wraps the existing server and client into a macOS <code>.app</code> (distributed as a <code>.dmg</code>) and a Windows <code>.exe</code> (an NSIS installer plus a no-install portable build) you install once and forget. <code>desktop/</code> is a sibling workspace to <code>client/</code>, <code>server/</code>, <code>mcp/</code>, and <code>vscode-extension/</code>, built with <strong>Electron 35</strong>. It <strong>embeds the Express server in-process</strong> — it <code>require()</code>s <code>server/index.js</code> directly in the same Node runtime as the Electron main process (no child process, no IPC) — and renders the already-built React client in a <code>BrowserWindow</code>. Everything you see in the browser at <code>localhost:4820</code> lives inside this window, with native OS lifecycle on top.":
      "仪表盘还以可选的<strong>原生桌面应用</strong>形式发布 — 一个 <code>desktop/</code> 工作区，将现有的服务器和客户端封装为 macOS <code>.app</code>（以 <code>.dmg</code> 形式分发）和 Windows <code>.exe</code>（一个 NSIS 安装程序外加一个免安装的便携版构建），你只需安装一次即可。<code>desktop/</code> 是 <code>client/</code>、<code>server/</code>、<code>mcp/</code> 和 <code>vscode-extension/</code> 的同级工作区，使用 <strong>Electron 35</strong> 构建。它<strong>在进程内嵌入 Express 服务器</strong> — 它在与 Electron 主进程相同的 Node 运行时中直接 <code>require()</code> <code>server/index.js</code>（没有子进程，没有 IPC） — 并在 <code>BrowserWindow</code> 中渲染已经构建好的 React 客户端。你在浏览器中通过 <code>localhost:4820</code> 看到的一切都存在于这个窗口内，并在其之上叠加了原生的操作系统生命周期。",
    '<span class="caption-icon">🍎🪟</span> <span>The full dashboard, natively on macOS <strong>and</strong> Windows — same React client, same Express server, real <code>BrowserWindow</code>. Menu-bar / notification-area (tray) icon included. Shipped as a macOS DMG and a Windows EXE (macOS shown) — see <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a>.</span>':
      '<span class="caption-icon">🍎🪟</span> <span>完整的仪表盘，原生运行于 macOS <strong>和</strong> Windows — 相同的 React 客户端，相同的 Express 服务器，真正的 <code>BrowserWindow</code>。包含菜单栏 / 通知区域（托盘）图标。以 macOS DMG 和 Windows EXE 形式发布（图中所示为 macOS） — 参见 <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a>。</span>',
    '<span class="caption-icon">🪟</span> <span>The same dashboard as a native <strong>Windows</strong> app — real <code>BrowserWindow</code> with the native Windows window menu, live Activity Feed, and the Tabby companion. A notification-area (system tray) icon sits beside the clock for quick access.</span>':
      '<span class="caption-icon">🪟</span> <span>同一个仪表盘作为原生 <strong>Windows</strong> 应用 — 真正的 <code>BrowserWindow</code>，带有原生 Windows 窗口菜单、实时 Activity Feed 以及 Tabby 伴侣。通知区域（系统托盘）图标位于时钟旁边，便于快速访问。</span>',
    "<strong>One-line mental model</strong>": "<strong>一句话心智模型</strong>",
    "<em>Electron is a window onto the same code.</em> The desktop app does not reimplement the dashboard — it hosts the exact server and client the standalone deployment runs. The only change outside <code>desktop/</code> is a behavior-preserving refactor of <code>server/index.js</code>: its post-listen bootstrap was extracted into an exported <code>startBackgroundServices()</code> so the embedded server runs exactly what <code>node server/index.js</code> runs.":
      "<em>Electron 只是通向同一套代码的一扇窗。</em> 桌面应用并没有重新实现仪表盘 — 它托管的正是独立部署所运行的同一套服务器和客户端。<code>desktop/</code> 之外唯一的改动是对 <code>server/index.js</code> 进行了一次保持行为不变的重构：它在监听之后的引导逻辑被提取为一个导出的 <code>startBackgroundServices()</code>，因此嵌入式服务器运行的内容与 <code>node server/index.js</code> 运行的内容完全一致。",
    "The Electron main process hosts the embedded server <em>and</em> manages the window, tray, and menus. The renderer is just Chromium loading <code>http://127.0.0.1:&lt;port&gt;</code> — the same origin a normal browser would use.":
      "Electron 主进程托管嵌入式服务器，<em>同时</em>管理窗口、托盘和菜单。渲染进程只是加载 <code>http://127.0.0.1:&lt;port&gt;</code> 的 Chromium — 与普通浏览器使用的源相同。",
    "The desktop app embeds the Express server in-process — no child process, no IPC":
      "桌面应用在进程内嵌入 Express 服务器 — 没有子进程，没有 IPC",
    "An always-on tray icon — the macOS menu bar (a tinted template glyph) or the Windows notification area (the colored <code>icon.ico</code>). A single click (left or right) opens a dropdown with a <strong>live status snapshot</strong> queried straight from SQLite at click time — server port, active sessions, working agents, events today — followed by <strong>Open Dashboard</strong>, <strong>Open in Browser</strong>, <strong>Restart Server</strong>, <strong>Show Logs</strong>, <strong>Open at Login</strong> (toggle), and <strong>Quit</strong>. The snapshot rows are clickable — they open the dashboard. The menu is rebuilt on each open so every value is current.":
      "一个常驻的托盘图标 — macOS 菜单栏中（一个着色的模板图形）或 Windows 通知区域中（彩色的 <code>icon.ico</code>）。单击（左键或右键）即可打开一个下拉菜单，其中包含点击时直接从 SQLite 查询的<strong>实时状态快照</strong> — 服务器端口、活动会话、工作中的代理、今日事件 — 随后是 <strong>Open Dashboard</strong>、<strong>Open in Browser</strong>、<strong>Restart Server</strong>、<strong>Show Logs</strong>、<strong>Open at Login</strong>（切换）和 <strong>Quit</strong>。快照行可点击 — 点击会打开仪表盘。菜单在每次打开时重建，因此每个值都是最新的。",
    "A standard native application menu — <code>About</code>, <code>Open at Login</code>, <code>File</code>, <code>Edit</code>, <code>View</code>, <code>Window</code>, <code>Help</code> — with <code>⌘R</code> / <code>Ctrl+R</code> wired to <em>View ▸ reload</em>. External links open in the system browser, never inside Electron. The <code>File ▸ Open Dashboard</code> item (<code>⌘1</code>) is macOS-only; on Windows/Linux the window-attached menu can't reopen a hidden window, so reopen from the tray's <strong>Open Dashboard</strong>.":
      "一个标准的原生应用菜单 — <code>About</code>、<code>Open at Login</code>、<code>File</code>、<code>Edit</code>、<code>View</code>、<code>Window</code>、<code>Help</code> — 其中 <code>⌘R</code> / <code>Ctrl+R</code> 绑定到 <em>View ▸ reload</em>。外部链接会在系统浏览器中打开，绝不会在 Electron 内部打开。<code>File ▸ Open Dashboard</code> 菜单项（<code>⌘1</code>）仅限 macOS；在 Windows/Linux 上，附属于窗口的菜单无法重新打开一个已隐藏的窗口，因此请从托盘的 <strong>Open Dashboard</strong> 重新打开。",
    "Flip <em>Open at Login</em> in the tray or app menu — both platforms go through Electron's first-party <code>app.*LoginItemSettings</code> API. On macOS it registers via the modern <code>SMAppService</code> API and appears under <strong>System Settings → General → Login Items</strong>; on Windows it writes a per-user <code>HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run</code> entry, visible in <strong>Task Manager → Startup</strong>. When the app is launched at login it starts tray-only, with no window jumping into view (on Windows the login launch is detected via a <code>--ccam-hidden</code> argument).":
      "在托盘或应用菜单中切换 <em>Open at Login</em> — 两个平台都通过 Electron 的第一方 <code>app.*LoginItemSettings</code> API 实现。在 macOS 上，它通过现代的 <code>SMAppService</code> API 注册，并出现在 <strong>System Settings → General → Login Items</strong> 下；在 Windows 上，它会写入一个按用户的 <code>HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run</code> 条目，可在 <strong>Task Manager → Startup</strong> 中看到。当应用在登录时启动时，它仅以托盘形式启动，不会有窗口跳出来（在 Windows 上，登录启动通过 <code>--ccam-hidden</code> 参数来检测）。",
    'Closing the window hides it — the embedded server keeps running, the tray icon stays, and the dock / taskbar icon stays too (a clickable "still alive" indicator). <strong>Quit</strong> (<code>⌘Q</code> / <code>Ctrl+Q</code>, app menu, or tray → Quit) pops a confirmation modal — press the Quit button or hit <code>⌘Q</code> / <code>Ctrl+Q</code> a second time to skip the prompt — and only then does the embedded server shut down, closing SQLite cleanly with a WAL checkpoint and removing this PID\'s entry from the discovery file.':
      "关闭窗口只会将其隐藏 — 嵌入式服务器继续运行，托盘图标保留，dock / 任务栏图标也保留（一个可点击的“仍在运行”指示器）。<strong>Quit</strong>（<code>⌘Q</code> / <code>Ctrl+Q</code>、应用菜单，或托盘 → Quit）会弹出一个确认对话框 — 按下 Quit 按钮或再次按 <code>⌘Q</code> / <code>Ctrl+Q</code> 可跳过该提示 — 只有这时嵌入式服务器才会关闭，通过 WAL 检查点干净地关闭 SQLite，并从发现文件中移除此 PID 的条目。",
    "Launch the desktop app and <code>npm run dev</code> at the same time and both stay real-time. Each server appends its <code>{port, pid, startedAt}</code> entry to <code>~/.claude/.agent-dashboard.json</code> on startup; the Claude Code hook handler reads that list and fan-outs every event to every live entry in parallel. Stale entries self-evict via a PID liveness check on read, so a crashed server can't misroute events to a dead port.":
      "同时启动桌面应用和 <code>npm run dev</code>，两者都能保持实时。每个服务器在启动时都会将其 <code>{port, pid, startedAt}</code> 条目追加到 <code>~/.claude/.agent-dashboard.json</code>；Claude Code 的 hook 处理程序读取该列表，并将每个事件并行扇出到每个存活的条目。陈旧的条目会在读取时通过 PID 存活检查自行清除，因此崩溃的服务器无法将事件误路由到已失效的端口。",
    "Double-launching the app just focuses the existing window — no second server, no port collision, on every platform. The lock is acquired via <code>requestSingleInstanceLock()</code> before any server boots.":
      "重复启动应用只会聚焦现有窗口 — 在每个平台上都不会出现第二个服务器、不会发生端口冲突。该锁在任何服务器启动之前通过 <code>requestSingleInstanceLock()</code> 获取。",
    "On its first owned-server boot the app auto-installs the Claude Code hooks into <code>~/.claude/settings.json</code> and starts the background services (update scheduler, config watcher, orphaned-run reconciliation) — so an install-only user (DMG or EXE) gets events flowing without ever running <code>npm run install-hooks</code> from a checkout.":
      "在首次启动自有服务器时，应用会自动将 Claude Code hooks 安装到 <code>~/.claude/settings.json</code> 中，并启动后台服务（更新调度器、配置监视器、孤立运行的对账） — 因此仅通过安装包使用的用户（DMG 或 EXE）无需从检出的代码中运行 <code>npm run install-hooks</code> 即可让事件流动起来。",
    "Two packaging realities — a read-only application bundle / install directory and (on macOS) the minimal <code>PATH</code> a Finder-launched app inherits — are handled automatically so installs survive updates and the <strong>Run Claude</strong> feature works out of the box on both macOS and Windows.":
      "两个打包方面的现实情况 — 只读的应用程序包 / 安装目录，以及（在 macOS 上）Finder 启动的应用所继承的最小化 <code>PATH</code> — 都会被自动处理，因此安装能够在更新中保留下来，并且 <strong>Run Claude</strong> 功能在 macOS 和 Windows 上都能开箱即用。",
    "<strong>Your data survives reinstalls and updates</strong>":
      "<strong>你的数据在重装和更新后依然保留</strong>",
    "The SQLite database and VAPID keys live in a per-user app-data directory <em>outside</em> the application bundle / install dir — <code>~/Library/Application Support/Claude Code Monitor/data/</code> on macOS, <code>%APPDATA%\\Claude Code Monitor\\data\\</code> on Windows. <code>server-host.ts</code> points <code>DASHBOARD_DATA_DIR</code> at that per-user directory on boot. Because a packaged, code-signed, or app-translocated bundle is read-only, older builds that stored the database inside the bundle broke History Import; with the data directory now in app-data, your imported history and events persist across app reinstalls and updates (the Windows NSIS uninstaller keeps this data by default). After upgrading from a pre-fix build, re-run <strong>Import History → Rescan</strong> once to bridge the one-time gap.":
      "SQLite 数据库和 VAPID 密钥存放在应用程序包 / 安装目录<em>之外</em>的一个按用户的应用数据目录中 — 在 macOS 上为 <code>~/Library/Application Support/Claude Code Monitor/data/</code>，在 Windows 上为 <code>%APPDATA%\\Claude Code Monitor\\data\\</code>。<code>server-host.ts</code> 在启动时将 <code>DASHBOARD_DATA_DIR</code> 指向该按用户的目录。由于打包的、经过代码签名的或被应用转移（app-translocated）的应用包是只读的，将数据库存放在包内的旧版本构建会破坏 History Import；现在数据目录位于应用数据中，你导入的历史记录和事件会在应用重装和更新后持续保留（Windows NSIS 卸载程序默认会保留这些数据）。从修复前的构建升级后，请重新运行一次 <strong>Import History → Rescan</strong> 以弥补这一次性的间隔。",
    "<strong>The <code>claude</code> CLI is found automatically</strong>":
      "<strong>会自动找到 <code>claude</code> CLI</strong>",
    "A Finder- or Dock-launched macOS app inherits only launchd's minimal <code>PATH</code>, not your login shell's. At startup <code>shell-path.ts</code> recovers the user's login-shell <code>PATH</code> so the <strong>Run Claude</strong> feature can locate and spawn the <code>claude</code> CLI. (On Windows the process already inherits the user <code>PATH</code>, so no recovery step is needed.) If it still cannot be found, make sure <code>claude</code> is a real executable on your <code>PATH</code> — a shell alias or function cannot be spawned — and check the <code>user PATH resolved</code> line in the desktop log (<code>~/Library/Logs/Claude Code Monitor/desktop.log</code> on macOS, <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> on Windows).":
      "由 Finder 或 Dock 启动的 macOS 应用只会继承 launchd 的最小化 <code>PATH</code>，而不是你登录 shell 的 PATH。在启动时，<code>shell-path.ts</code> 会恢复用户登录 shell 的 <code>PATH</code>，以便 <strong>Run Claude</strong> 功能能够定位并启动 <code>claude</code> CLI。（在 Windows 上，进程已经继承了用户的 <code>PATH</code>，因此无需恢复步骤。）如果仍然找不到，请确保 <code>claude</code> 是你 <code>PATH</code> 上的一个真正的可执行文件 — shell 别名或函数无法被启动 — 并检查桌面日志中的 <code>user PATH resolved</code> 行（在 macOS 上为 <code>~/Library/Logs/Claude Code Monitor/desktop.log</code>，在 Windows 上为 <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code>）。",
    "On launch the Electron main process picks a free port. If a healthy dashboard server already answers <code>/api/health</code> on port <code>4820</code> (for example, you ran <code>npm start</code> in a terminal), the app <strong>adopts</strong> that server instead of starting a second one — no double-binding, no SQLite contention. An adopted server is not owned by the app, so quitting leaves it running.":
      "在启动时，Electron 主进程会选择一个空闲端口。如果已经有一个健康的仪表盘服务器在端口 <code>4820</code> 上响应 <code>/api/health</code>（例如，你在终端中运行了 <code>npm start</code>），应用会<strong>采用</strong>该服务器，而不是启动第二个 — 不会重复绑定，也不会发生 SQLite 争用。被采用的服务器不归应用所有，因此退出应用后它仍会继续运行。",
    Step: "步骤",
    "Port choice": "端口选择",
    Adopt: "采用",
    "A healthy server already on <code>4820</code> is adopted as-is":
      "已经在 <code>4820</code> 上运行的健康服务器会被原样采用",
    Preferred: "首选",
    "<code>4820</code> when free": "空闲时使用 <code>4820</code>",
    Fallback: "回退",
    "The first free port in <code>4821</code>–<code>4829</code>":
      "<code>4821</code>–<code>4829</code> 中的第一个空闲端口",
    "Last resort": "最后手段",
    "A random high port when all of the above are taken":
      "当上述端口都被占用时，使用一个随机的高位端口",
    "Three ways to obtain the desktop app — the latest GitHub Release (best for most users), a per-commit CI artifact (fresher than the latest release), or a local build.":
      "获取桌面应用有三种方式 — 最新的 GitHub Release（适合大多数用户）、按提交生成的 CI 构件（比最新发布版本更新），或本地构建。",
    'Open <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg> Releases → latest </a> and download the asset for your platform. The macOS and Windows Desktop CI jobs auto-publish a new <code>vX.Y.Z</code> release every time the version in <code>package.json</code> is bumped on <code>master</code>, so this link always points at the current build. Releases are public — no GitHub sign-in required.':
      '打开 <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg> Releases → latest </a> 并下载适合你平台的资源。每当 <code>master</code> 上 <code>package.json</code> 中的版本号被提升时，macOS 和 Windows 的桌面 CI 作业都会自动发布一个新的 <code>vX.Y.Z</code> release，因此此链接始终指向当前构建。Release 是公开的 — 无需登录 GitHub。',
    Platform: "平台",
    Asset: "资源",
    "macOS (Apple Silicon)": "macOS（Apple Silicon）",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-arm64.dmg</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-arm64.dmg</code>",
    "Drag the <code>.app</code> into <code>/Applications</code>":
      "将 <code>.app</code> 拖入 <code>/Applications</code>",
    "macOS (Intel)": "macOS（Intel）",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64.dmg</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64.dmg</code>",
    "Windows (installer)": "Windows（安装程序）",
    "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>":
      "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>",
    "NSIS installer — per-user, no admin elevation": "NSIS 安装程序——按用户安装，无需管理员提权",
    "Windows (portable)": "Windows（便携版）",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>",
    "Run without installing": "无需安装即可运行",
    'Want a build straight off the tip of <code>master</code>, ahead of the next tagged release? Every green run of the <code>🍎 macOS Desktop (DMG)</code> job on <code>macos-latest</code> uploads both per-arch DMGs as the <code>ClaudeCodeMonitor-dmg</code> workflow artifact, and the <code>🪟 Windows Desktop (EXE)</code> job on <code>windows-latest</code> uploads the installer + portable EXEs as the <code>ClaudeCodeMonitor-win</code> artifact. Open the <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg> latest passing run </a>, scroll to its Artifacts section, and download <code>ClaudeCodeMonitor-dmg</code> or <code>ClaudeCodeMonitor-win</code>. (GitHub sign-in required; 14-day retention.)':
      '想要直接从 <code>master</code> 最新提交处构建、抢先于下一个打标签的发布版本？<code>macos-latest</code> 上 <code>🍎 macOS Desktop (DMG)</code> 任务的每次成功运行都会将两个各架构 DMG 作为 <code>ClaudeCodeMonitor-dmg</code> 工作流制品上传，<code>windows-latest</code> 上 <code>🪟 Windows Desktop (EXE)</code> 任务则将安装程序 + 便携版 EXE 作为 <code>ClaudeCodeMonitor-win</code> 制品上传。打开 <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg> 最近一次通过的运行 </a>，滚动到其 Artifacts（制品）区，然后下载 <code>ClaudeCodeMonitor-dmg</code> 或 <code>ClaudeCodeMonitor-win</code>。（需登录 GitHub；保留 14 天。）',
    "From the project root, after <code>git clone</code>:":
      "在项目根目录下，执行 <code>git clone</code> 之后：",
    "Use the arch-specific build on your own machine": "在你自己的机器上使用特定架构的构建",
    "<code>desktop:dmg</code> is slower because it builds the full app tree <em>twice</em> (once per architecture) and ad-hoc-signs each — emitting two separate per-arch DMGs (<code>…-arm64.dmg</code> + <code>…-x64.dmg</code>). There is no <code>@electron/universal</code> merge; the release ships the two per-arch DMGs. For running on a single Mac, use <code>desktop:dmg:arm64</code> (Apple Silicon) or <code>desktop:dmg:x64</code> (Intel) — one architecture, finishing in roughly a minute. CI runs <code>desktop:dmg</code> and uploads both DMGs as <code>ClaudeCodeMonitor-dmg</code>, so you rarely need to build them yourself.":
      "<code>desktop:dmg</code> 较慢，因为它会<em>两次</em>构建完整的应用树（每个架构一次）并分别进行临时（ad-hoc）签名——生成两个独立的各架构 DMG（<code>…-arm64.dmg</code> + <code>…-x64.dmg</code>）。没有 <code>@electron/universal</code> 合并；发布版本会附带这两个各架构 DMG。若只在一台 Mac 上运行，请使用 <code>desktop:dmg:arm64</code>（Apple Silicon）或 <code>desktop:dmg:x64</code>（Intel）——单一架构，大约一分钟即可完成。CI 会运行 <code>desktop:dmg</code> 并将两个 DMG 都作为 <code>ClaudeCodeMonitor-dmg</code> 上传，因此你很少需要自己构建它们。",
    "Double-click the downloaded <code>.dmg</code> to mount it":
      "双击下载的 <code>.dmg</code> 以挂载它",
    "Drag <code>Claude Code Monitor.app</code> into your <code>Applications</code> folder":
      "将 <code>Claude Code Monitor.app</code> 拖入你的 <code>Applications</code> 文件夹",
    "Run <code>xattr -cr</code> on the app to get past Gatekeeper (see below)":
      "对该应用运行 <code>xattr -cr</code> 以绕过 Gatekeeper（见下文）",
    "Open the app — the tray icon appears and the dashboard window loads":
      "打开应用——托盘图标出现，仪表盘窗口随之加载",
    "Gatekeeper warning on first launch": "首次启动时的 Gatekeeper 警告",
    'The DMG is ad-hoc signed by default — that is all the project can offer without a paid Apple Developer ID. macOS warns the first time you open it (<em>"Apple could not verify…"</em>). Strip the quarantine attribute to get past it:':
      "DMG 默认采用临时签名——在没有付费 Apple Developer ID 的情况下，这已是该项目所能提供的全部。macOS 会在你首次打开时发出警告（<em>“Apple could not verify…”</em>）。去除隔离属性即可绕过它：",
    "Alternatively, open <strong>System Settings → Privacy &amp; Security</strong>, find the blocked app, and click <em>Open Anyway</em>. Code signing and Apple notarization are opt-in for the maintainer — when configured, this warning goes away for everyone.":
      "或者，打开 <strong>System Settings → Privacy &amp; Security</strong>，找到被阻止的应用，然后点击 <em>Open Anyway</em>。代码签名和 Apple 公证对维护者而言是可选项——一旦配置完成，这条警告对所有人都会消失。",
    "Run <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> — a per-user NSIS install (no admin), or run the <code>*-portable.exe</code> to skip installing":
      "运行 <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>——按用户的 NSIS 安装（无需管理员），或运行 <code>*-portable.exe</code> 以跳过安装",
    "The EXE is unsigned by default, so SmartScreen may warn — click <em>More info → Run anyway</em>":
      "EXE 默认未签名，因此 SmartScreen 可能会发出警告——点击 <em>More info → Run anyway</em>",
    "Open from the Start menu / desktop shortcut — the notification-area (tray) icon appears and the dashboard window loads":
      "从开始菜单/桌面快捷方式打开——通知区域（托盘）图标出现，仪表盘窗口随之加载",
    '<span class="caption-icon">1️⃣</span> <span>NSIS installer, step 1 — <strong>Choose Installation Options</strong>: pick per-user setup and optional shortcuts.</span>':
      '<span class="caption-icon">1️⃣</span> <span>NSIS 安装程序，第 1 步——<strong>Choose Installation Options</strong>：选择按用户安装和可选的快捷方式。</span>',
    '<span class="caption-icon">2️⃣</span> <span>NSIS installer, step 2 — <strong>Choose Install Location</strong>: defaults to <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code>, or point it anywhere.</span>':
      '<span class="caption-icon">2️⃣</span> <span>NSIS 安装程序，第 2 步——<strong>Choose Install Location</strong>：默认为 <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code>，也可指向任意位置。</span>',
    '<span class="caption-icon">3️⃣</span> <span>NSIS installer, step 3 — <strong>Completing Setup</strong>: click <em>Finish</em> to launch the app and drop the tray icon in the notification area.</span>':
      '<span class="caption-icon">3️⃣</span> <span>NSIS 安装程序，第 3 步——<strong>Completing Setup</strong>：点击 <em>Finish</em> 启动应用并将托盘图标放入通知区域。</span>',
    "SmartScreen warning on first launch": "首次启动时的 SmartScreen 警告",
    'The installer and portable EXE are <strong>unsigned</strong> by default — that is all the project can offer without a paid code-signing certificate. Windows <strong>SmartScreen</strong> may show <em>"Windows protected your PC"</em> the first time you run it; click <strong>More info → Run anyway</strong>. The installer lays the app down <strong>per-user</strong> under <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code> (and lets you choose the install directory) and sets an <code>AppUserModelId</code> (<code>com.hoangsonww.ccam.desktop</code>) so native toast notifications are attributed correctly and the window groups under one taskbar entry.':
      "安装程序和便携版 EXE 默认<strong>未签名</strong>——在没有付费代码签名证书的情况下，这已是该项目所能提供的全部。Windows <strong>SmartScreen</strong> 可能会在你首次运行时显示 <em>“Windows protected your PC”</em>；点击 <strong>More info → Run anyway</strong>。安装程序会将应用<strong>按用户</strong>放置在 <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code> 下（并允许你选择安装目录），并设置一个 <code>AppUserModelId</code>（<code>com.hoangsonww.ccam.desktop</code>），以便原生的 toast 通知能被正确归属，且窗口会归入同一个任务栏条目下。",
    "Bundle size": "包大小",
    "The DMG is roughly 80&nbsp;MB, about 250&nbsp;MB installed on disk — the standard Electron tax; the Windows installer is comparable. The app runs natively on <strong>macOS and Windows</strong>; Linux is tracked as a follow-up. Logs live at <code>~/Library/Logs/Claude Code Monitor/desktop.log</code> on macOS or <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> on Windows (reach them from the tray menu → <em>Show Logs</em>).":
      "DMG 大约 80&nbsp;MB，安装到磁盘上约 250&nbsp;MB——这是标准的 Electron 代价；Windows 安装程序也相当。该应用在 <strong>macOS 和 Windows</strong> 上原生运行；Linux 作为后续项被跟踪。日志在 macOS 上位于 <code>~/Library/Logs/Claude Code Monitor/desktop.log</code>，在 Windows 上位于 <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code>（可从托盘菜单 → <em>Show Logs</em> 找到它们）。",
    '📖 User-facing guide: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a> · architecture &amp; contributor reference: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/desktop/README.md"><code>desktop/README.md</code></a>':
      '📖 面向用户的指南：<a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a> · 架构与贡献者参考：<a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/desktop/README.md"><code>desktop/README.md</code></a>',
    '<span class="caption-icon">⚙️</span> Settings — model pricing editor, hook installation toggle, JSON data export, session cleanup, browser notification preferences, and system info panel with DB stats':
      '<span class="caption-icon">⚙️</span> Settings——模型定价编辑器、hook 安装开关、JSON 数据导出、会话清理、浏览器通知偏好，以及带数据库统计的系统信息面板',
    "The <code>/settings</code> route provides a comprehensive management interface with six sections:":
      "<code>/settings</code> 路由提供了一个含六个部分的综合管理界面：",
    "Editable table of per-model pricing rules. Each Claude model variant has its own explicit pattern (e.g., <code>claude-opus-4-6%</code>). Rates cover input, output, cache read, and cache write tokens. Each rule's editor also has a collapsible <strong>Introductory rates</strong> block — a <code>YYYY-MM-DD</code> promo cutoff plus per-category intro prices (input / output / cache-read / cache-write 5m &amp; 1h); an empty date means no promo, and any future model-launch promo needs no code change. Reset to defaults or add custom models. The section header carries an info popover (the <code>i</code> icon) that explains how rule lookup works (first matching pattern wins), the SQL-style <code>%</code> wildcard syntax with concrete examples (<code>claude-opus-4-7%</code>, <code>claude-%-haiku</code>, exact ids), and reminds the user that prices must be updated manually when Anthropic publishes new rates — already-stored sessions keep the price applied at ingest time. The CLAUDE_HOME panel and Import History flow are fully i18n-driven across en/vi/zh.":
      "可编辑的按模型定价规则表。每个 Claude 模型变体都有自己明确的模式（例如 <code>claude-opus-4-6%</code>）。费率涵盖输入、输出、缓存读取和缓存写入 token。每条规则的编辑器还带有一个可折叠的 <strong>Introductory rates</strong>（引导期费率）区块——一个 <code>YYYY-MM-DD</code> 的促销截止日期，外加按类别设置的引导期价格（输入 / 输出 / 缓存读取 / 缓存写入 5m &amp; 1h）；日期留空即表示没有促销，而任何未来的模型发布促销都无需改动代码。可重置为默认值或添加自定义模型。该区块标题带有一个信息弹出框（<code>i</code> 图标），用于说明规则查找的工作方式（首个匹配的模式胜出）、SQL 风格的 <code>%</code> 通配符语法及具体示例（<code>claude-opus-4-7%</code>、<code>claude-%-haiku</code>、精确 id），并提醒用户在 Anthropic 公布新费率时必须手动更新价格——已存储的会话会保留入库时所应用的价格。CLAUDE_HOME 面板和 Import History 流程在 en/vi/zh 之间完全由 i18n 驱动。",
    "Shows per-hook installation status (SessionStart, PreToolUse, PostToolUse, Stop, SubagentStop, Notification, SessionEnd). One-click reinstall if hooks are missing or outdated. Validates paths and permissions automatically.":
      "显示每个 hook 的安装状态（SessionStart、PreToolUse、PostToolUse、Stop、SubagentStop、Notification、SessionEnd）。若 hook 缺失或过时，可一键重新安装。自动验证路径和权限。",
    "View database row counts and size. Session cleanup: abandon stale active sessions after N hours, purge old completed sessions after N days. Danger zone: clear all data with confirmation dialog to prevent accidental loss.":
      "查看数据库行数和大小。会话清理：在 N 小时后放弃陈旧的活动会话，在 N 天后清除旧的已完成会话。危险区：通过确认对话框清除所有数据，以防意外丢失。",
    "Download all sessions, agents, events, token usage, and pricing rules as a single JSON file for backup or analysis. Includes full event history, model metadata, and cost breakdowns in one portable archive.":
      "将所有会话、智能体、事件、token 用量和定价规则下载为单个 JSON 文件，以供备份或分析。在一个可移植的归档中包含完整的事件历史、模型元数据和成本明细。",
    "Dedicated Health tab on the Dashboard with a composite health score (weighted from success rate, cache hit rate, error rate, and heap usage), storage engine donut chart, tool invocation frequency bars, subagent effectiveness, model token distribution, and compaction impact — all with cursor-following tooltips and 5-second auto-refresh.":
      "仪表盘上专门的 Health 选项卡，带有综合健康评分（由成功率、缓存命中率、错误率和堆内存使用加权得出）、存储引擎环形图、工具调用频率条形图、子智能体有效性、模型 token 分布以及压缩影响——全部配有跟随光标的提示框和 5 秒自动刷新。",
    "Configure native browser notifications with per-event toggles for session starts, completions, errors, and subagent spawns. Automatic permission management with test-send button and graceful fallback when denied.":
      "配置原生浏览器通知，针对会话开始、完成、错误和子智能体生成提供按事件的开关。自动权限管理，带测试发送按钮，并在被拒绝时优雅降级。",
    "Per-model pricing — no catch-all grouping": "按模型定价——没有一刀切的分组",
    "Each Claude model variant (e.g., Opus 4.6 vs Opus 4.1) has its own explicit pricing pattern because different model versions have different rates. The cost engine uses specificity sorting — longer patterns match before shorter ones.":
      "每个 Claude 模型变体（例如 Opus 4.6 与 Opus 4.1）都有自己明确的定价模式，因为不同的模型版本有不同的费率。成本引擎采用特异性排序——更长的模式优先于更短的模式进行匹配。",
    "Turns the dashboard from passive viewing into active monitoring. A rules-based alerting engine evaluates the live event stream <strong>server-side</strong>, and fired alerts fan out to outbound <strong>webhook channels</strong>. Everything lives in one place — <strong>Settings → Alerts</strong> — behind a segmented control with three tabs: <strong>Rules</strong> (what triggers an alert), <strong>Channels</strong> (where alerts are delivered), and <strong>Activity</strong> (the live fired-alert feed with acknowledge / acknowledge-all).":
      "将仪表盘从被动查看转变为主动监控。一个基于规则的告警引擎在<strong>服务器端</strong>评估实时事件流，触发的告警会扇出到出站的 <strong>webhook 通道</strong>。一切都集中于一处——<strong>Settings → Alerts</strong>——位于一个含三个选项卡的分段控件之后：<strong>Rules</strong>（什么会触发告警）、<strong>Channels</strong>（告警送往何处）和 <strong>Activity</strong>（实时的已触发告警信息流，带确认/全部确认）。",
    'Four condition types: <strong>event pattern</strong> (match <code>event_type</code> / <code>tool_name</code> / a summary substring, optionally requiring ≥ N matches within a rolling window — e.g. "5 errors in 2 minutes"), <strong>inactivity</strong> (an active session goes quiet for N minutes), <strong>status duration</strong> (an agent is stuck in <code>working</code> / <code>waiting</code> for N minutes), and <strong>token threshold</strong> (a session\'s cumulative tokens cross a limit). Each rule has a configurable <strong>cooldown</strong> that dedups repeat alerts per (rule, session, agent).':
      "四种条件类型：<strong>事件模式</strong>（匹配 <code>event_type</code> / <code>tool_name</code> / 摘要子串，可选地要求在滚动窗口内匹配 ≥ N 次——例如“2 分钟内 5 个错误”）、<strong>不活动</strong>（活动会话静默 N 分钟）、<strong>状态时长</strong>（智能体卡在 <code>working</code> / <code>waiting</code> 状态达 N 分钟）以及<strong>token 阈值</strong>（会话的累计 token 越过某个上限）。每条规则都有可配置的<strong>冷却时间</strong>，按（规则、会话、智能体）对重复告警去重。",
    "Event-driven rules (<code>event_pattern</code>, <code>token_threshold</code>) run on every hook ingest — <em>after</em> the transaction commits and the response is sent, fully try/catch-guarded, so alerting can never slow or fail hook delivery. Time-based rules (<code>inactivity</code>, <code>status_duration</code>) run on an unref'd 60-second sweep. Enabled rules are cached in memory and invalidated on every edit. Fired alerts persist to <code>alert_events</code> and broadcast an <code>alert_triggered</code> WebSocket message.":
      "事件驱动的规则（<code>event_pattern</code>、<code>token_threshold</code>）在每次 hook 入库时运行——在事务提交并发送响应<em>之后</em>，且完全由 try/catch 保护，因此告警绝不会拖慢或导致 hook 投递失败。基于时间的规则（<code>inactivity</code>、<code>status_duration</code>）在一个 unref 的 60 秒扫描中运行。已启用的规则缓存在内存中，并在每次编辑时失效。触发的告警会持久化到 <code>alert_events</code>，并广播一条 <code>alert_triggered</code> WebSocket 消息。",
    "Slack, Discord, Microsoft Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, and Pipedream — plus a generic JSON endpoint. A declarative <strong>provider registry</strong> describes each one's payload formatter, URL resolution, auth headers, and credential fields, so adding a provider is a single server-side entry that surfaces in the UI with no front-end change.":
      "Slack、Discord、Microsoft Teams、Google Chat、Mattermost、Rocket.Chat、Telegram、PagerDuty、Opsgenie、Splunk On-Call、Zapier、Make、n8n 和 Pipedream——外加一个通用的 JSON 端点。一个声明式的<strong>提供方注册表</strong>描述了每个提供方的载荷格式化器、URL 解析、认证头和凭据字段，因此添加一个提供方只是一个服务器端条目，便会在 UI 中呈现，无需任何前端改动。",
    'Each delivery POSTs with an <code>AbortController</code> timeout and bounded retry/backoff (retries transport errors, 429, and 5xx — never other 4xx), then records the attempt-chain in <code>webhook_deliveries</code>. A provider can also veto a 2xx whose body signals failure (Splunk On-Call returns 200 with <code>result:"failure"</code>). Delivery is <strong>detached and fail-safe</strong> — it never throws into, slows, or blocks the alert path.':
      '每次投递都带有 <code>AbortController</code> 超时和有界的重试/退避（对传输错误、429 和 5xx 重试——绝不对其他 4xx 重试），随后将尝试链记录到 <code>webhook_deliveries</code>。提供方也可以否决一个其响应体表示失败的 2xx（Splunk On-Call 返回 200 但带 <code>result:"failure"</code>）。投递是<strong>分离且故障安全</strong>的——它绝不会向告警路径抛出异常、拖慢或阻塞它。',
    "Target URLs are masked (host + last 4 chars), and secrets / credential fields (routing keys, API keys, bot tokens) plus custom-header values are redacted in every API response — the full URL and secrets are stored server-side and never leave it. Generic endpoints support optional <strong>HMAC-SHA256</strong> body signing (<code>X-Webhook-Signature</code> + <code>X-Webhook-Timestamp</code>) so receivers can verify authenticity.":
      "目标 URL 会被掩码（主机 + 后 4 个字符），且密钥/凭据字段（路由键、API 密钥、bot token）以及自定义头的值在每个 API 响应中都会被脱敏——完整的 URL 和密钥存储在服务器端，绝不离开它。通用端点支持可选的 <strong>HMAC-SHA256</strong> 请求体签名（<code>X-Webhook-Signature</code> + <code>X-Webhook-Timestamp</code>），以便接收方可以验证真实性。",
    'Every alert-rule field has a help tooltip — the event-type, tool-name, and summary-contains fields include example chips of real hook events and built-in tool names. Each webhook provider ships a collapsible step-by-step setup guide linking to the official docs. A one-click <strong>"Send test"</strong> probe fires a synthetic alert and reports the delivery result inline, and targets can be scoped to specific rules. Fully localized (en / zh / vi / ko).':
      "每个告警规则字段都有帮助提示——event-type、tool-name 和 summary-contains 字段包含真实 hook 事件和内置工具名称的示例标签。每个 webhook 提供方都附带一份可折叠的分步设置指南，并链接到官方文档。一键 <strong>“Send test”</strong> 探测会触发一个合成告警并就地报告投递结果，且目标可限定到特定规则。完全本地化（en / zh / vi / ko）。",
    "Provider(s)": "提供方",
    "Payload format": "载荷格式",
    "URL / credentials": "URL / 凭据",
    "Block Kit (header + section + context)": "Block Kit（header + section + context）",
    "Rich embed": "富嵌入（rich embed）",
    "Adaptive Card in a Workflows <code>message</code> envelope":
      "包装在 Workflows <code>message</code> 信封中的 Adaptive Card",
    "Power Automate Workflows URL": "Power Automate Workflows URL",
    "Text message (basic markdown)": "文本消息（基础 markdown）",
    "Space webhook URL": "Space webhook URL",
    "Slack-style legacy attachments": "Slack 风格的旧版附件",
    "Bot API <code>sendMessage</code> (HTML)": "Bot API <code>sendMessage</code>（HTML）",
    "Bot token + chat ID (URL derived)": "Bot token + chat ID（URL 由此推导）",
    "Events API v2 trigger (with <code>dedup_key</code>)":
      "Events API v2 触发（带 <code>dedup_key</code>）",
    "Routing key (URL prefilled)": "路由键（URL 预填）",
    "Alert API": "Alert API",
    "API key (GenieKey header) + region": "API 密钥（GenieKey 头）+ 区域",
    "VictorOps REST": "VictorOps REST",
    "REST endpoint URL (key embedded)": "REST 端点 URL（密钥内嵌）",
    "Stable <code>{ event, alert }</code> JSON envelope":
      "稳定的 <code>{ event, alert }</code> JSON 信封",
    "Endpoint URL (+ optional HMAC &amp; headers)": "端点 URL（+ 可选的 HMAC 与头）",
    "Additive &amp; non-blocking by design": "设计上为增量且非阻塞",
    "Two new tables — <code>webhook_targets</code> (config; survives Clear Data like alert rules) and <code>webhook_deliveries</code> (audit log) — with no changes to existing tables, response shapes, or WebSocket message types. Webhook dispatch is fire-and-forget off the alert path, so a slow or failing endpoint can never slow or break alert firing or hook ingestion.":
      "两个新表——<code>webhook_targets</code>（配置；像告警规则一样在 Clear Data 中保留）和 <code>webhook_deliveries</code>（审计日志）——对现有表、响应结构或 WebSocket 消息类型没有任何改动。Webhook 分发在告警路径之外采用即发即弃方式，因此一个缓慢或失败的端点绝不会拖慢或破坏告警触发或 hook 入库。",
    "Provider setup steps can drift": "提供方的设置步骤可能会变动",
    "Microsoft retired classic Office 365 connectors in 2025, so Teams uses an Adaptive Card delivered via Power Automate <strong>Workflows</strong>. More broadly, provider setup UIs change often — the in-app guides say so and link to each provider's official docs. Always confirm against the source.":
      "Microsoft 在 2025 年停用了经典的 Office 365 连接器，因此 Teams 使用通过 Power Automate <strong>Workflows</strong> 投递的 Adaptive Card。更广泛地说，提供方的设置界面经常变化——应用内的指南也这样说明，并链接到各提供方的官方文档。请始终对照源头进行确认。",
    '<span class="caption-icon">⬆</span> Update Notifier — version comparison modal with one-click copy of the update command. No automatic self-restart; you stay in control of when upgrades happen':
      '<span class="caption-icon">⬆</span> Update Notifier — 版本对比弹窗，可一键复制更新命令。没有自动自我重启；何时升级始终由你掌控',
    "A detection-only subsystem that tells the user when the dashboard's git checkout is behind the canonical default branch. <strong>Branch- and fork-aware:</strong> if an <code>upstream</code> remote is configured (the standard convention for forks), it takes priority over <code>origin</code>; the chosen remote's <code>master</code> / <code>main</code> / <code>HEAD</code> is the comparison ref. The printed command adapts to the user's situation — <code>git pull --ff-only</code> only when their branch actually tracks the canonical ref, otherwise <code>git fetch</code> (with a fast-forward merge in the fork case). The server <strong>never</strong> pulls or restarts itself — the user runs the command in a terminal — so the mechanism cannot break dev sessions, pm2/systemd/launchd/Docker supervision, or leave orphaned processes.":
      "一个仅做检测的子系统，当仪表盘的 git 检出落后于规范默认分支时通知用户。<strong>感知分支与 fork：</strong>如果配置了 <code>upstream</code> 远程（fork 的标准约定），它会优先于 <code>origin</code>；所选远程的 <code>master</code> / <code>main</code> / <code>HEAD</code> 即为对比基准。打印出的命令会根据用户的实际情况自适应——仅当其分支确实跟踪规范基准时才使用 <code>git pull --ff-only</code>，否则使用 <code>git fetch</code>（在 fork 情形下附带一次快进合并）。服务器<strong>绝不</strong>会自行拉取或重启——由用户在终端中运行该命令——因此该机制不会破坏开发会话、pm2/systemd/launchd/Docker 监管，也不会留下孤儿进程。",
    "The same status is available from any terminal: <code>ccam update-check</code> asks the server for the branch- and fork-aware comparison, prints the behind-by count and the copy-paste update command, and triggers the <code>update_status</code> WebSocket broadcast so any open dashboard tab refreshes at the same time.":
      "同样的状态也可以在任意终端中获取：<code>ccam update-check</code> 向服务器请求感知分支与 fork 的对比结果，打印落后的提交数和可复制粘贴的更新命令，并触发 <code>update_status</code> WebSocket 广播，让所有打开的仪表盘标签页同时刷新。",
    "A shell-less <code>git fetch</code> with a 120-second timeout, followed by a <code>rev-list</code> against the tracked upstream. Each call runs from <code>server/lib/update-check.js</code> and returns a structured payload — never throws — so a flaky remote can&apos;t stall the dashboard.":
      "一次无 shell 的 <code>git fetch</code>，带 120 秒超时，随后对所跟踪的上游执行 <code>rev-list</code>。每次调用都由 <code>server/lib/update-check.js</code> 发起并返回结构化的负载——绝不抛出异常——因此不稳定的远程无法卡住仪表盘。",
    "<code>update-scheduler.js</code> polls every five minutes with <code>.unref()</code> timers so it never blocks shutdown, de-duplicates with a fingerprint over the status payload, and announces up-to-date → behind transitions in a framed stdout block. Disable entirely with <code>DASHBOARD_UPDATE_CHECK=0</code>.":
      "<code>update-scheduler.js</code> 使用 <code>.unref()</code> 定时器每五分钟轮询一次，因此绝不会阻塞关闭流程，并通过状态负载的指纹去重，在带边框的 stdout 块中宣告从最新 → 落后的转变。使用 <code>DASHBOARD_UPDATE_CHECK=0</code> 可完全禁用。",
    "Each status payload carries a <code>manual_command</code> shaped for the user's actual situation: <code>git pull --ff-only</code> on a tracked canonical branch, <code>git fetch &amp;&amp; git merge --ff-only</code> for forks where local tracks the wrong remote, and a plain <code>git fetch</code> on a feature branch where pulling would update the wrong branch. Install / build steps are appended only when the working tree is actually being rewritten.":
      "每个状态负载都带有一个针对用户实际情况量身定制的 <code>manual_command</code>：在跟踪规范分支时为 <code>git pull --ff-only</code>，在本地跟踪了错误远程的 fork 情形下为 <code>git fetch &amp;&amp; git merge --ff-only</code>，而在拉取会更新错误分支的特性分支上则为纯粹的 <code>git fetch</code>。只有当工作树确实会被改写时，才会追加安装/构建步骤。",
    "A modal opens automatically when upstream is ahead; ESC or a backdrop click dismisses it. A persistent sidebar button stays in the footer — emerald when behind, amber when the last check errored — so users can always trigger a fresh check on demand.":
      "当上游领先时会自动打开一个弹窗；按 ESC 或点击背景即可关闭。一个常驻的侧边栏按钮留在页脚——落后时为翠绿色，上次检查出错时为琥珀色——因此用户随时都能按需触发一次新的检查。",
    "Non-git installs, no remotes configured, offline fetches, and unresolvable upstream refs all return tagged payloads instead of throwing. The sidebar badge turns amber on fetch errors and the modal stays suppressed until a successful check arrives — no spinners, no stuck state.":
      "非 git 安装、未配置远程、离线抓取以及无法解析的上游基准都会返回带标记的负载，而不是抛出异常。侧边栏徽标在抓取出错时变为琥珀色，弹窗会一直被抑制，直到收到一次成功的检查——没有加载转圈，也没有卡住的状态。",
    "Dismissal is keyed by the upstream SHA in <code>localStorage</code>, so closing the modal silences it only for <em>that</em> commit — a newer upstream commit re-opens it automatically. Clicking the sidebar button is an explicit intent signal and clears the stored dismissal before firing a fresh check.":
      "忽略状态以上游 SHA 为键存储在 <code>localStorage</code> 中，因此关闭弹窗只会针对<em>那个</em>提交将其静音——更新的上游提交会自动重新打开它。点击侧边栏按钮是一种明确的意图信号，会在发起新检查之前清除已存储的忽略状态。",
    "Read-only check — runs <code>git fetch</code>, compares, returns the payload.":
      "只读检查——运行 <code>git fetch</code>，进行对比，返回负载。",
    "Same check, and broadcasts <code>update_status</code> over WebSocket so every connected client re-syncs at once.":
      "相同的检查，并通过 WebSocket 广播 <code>update_status</code>，使每个已连接的客户端同时重新同步。",
    "<strong>Detection-only by design</strong>": "<strong>设计上仅做检测</strong>",
    "There is no <code>POST /api/updates/apply</code> and no in-process restart helper. A process cannot reliably replace itself without an external supervisor, and <code>npm run dev</code>, <code>npm start</code>, pm2, systemd, launchd, and Docker each need different restart logic. Detection-only keeps the mechanism portable across every supervisor and OS, and leaves the dashboard's lifecycle owned by whatever started it. The user runs the printed command in their own shell.":
      "不存在 <code>POST /api/updates/apply</code>，也没有进程内重启助手。进程在没有外部监管者的情况下无法可靠地替换自身，而 <code>npm run dev</code>、<code>npm start</code>、pm2、systemd、launchd 和 Docker 各自需要不同的重启逻辑。仅做检测让该机制在每种监管者和操作系统间都保持可移植，并把仪表盘的生命周期交还给启动它的那个程序。用户在自己的 shell 中运行打印出的命令。",
    '<span class="caption-icon">◈</span> Connection Status — sidebar-launched details modal with WebSocket endpoint, connection uptime, 60-second throughput sparkline, top event-type breakdown, and recent activity list':
      '<span class="caption-icon">◈</span> Connection Status — 从侧边栏启动的详情弹窗，包含 WebSocket 端点、连接已运行时长、60 秒吞吐量迷你折线图、热门事件类型分布以及近期活动列表',
    'The <strong>Live</strong> / <strong>Disconnected</strong> pill in the sidebar footer opens a small details panel about the dashboard\'s WebSocket transport. It surfaces the active <code>ws://</code> endpoint, how long the current socket has been up, total events received, the top event types as a horizontal bar chart, a 60-second throughput sparkline, and the most recent 8 events as an activity list. Cumulative stats (totals, type breakdown, recent list) persist across reloads via <code>localStorage</code> under <code>sidebar-connection-stats</code>; the rolling sparkline and "connected since" timer are intentionally ephemeral since they only make sense relative to "now". A <strong>Reset</strong> button clears everything on demand.':
      "侧边栏页脚中的 <strong>Live</strong> / <strong>Disconnected</strong> 胶囊按钮会打开一个关于仪表盘 WebSocket 传输的小型详情面板。它展示当前活跃的 <code>ws://</code> 端点、当前套接字已运行多久、接收到的事件总数、以水平条形图呈现的热门事件类型、一条 60 秒吞吐量迷你折线图，以及作为活动列表的最近 8 个事件。累计统计（总数、类型分布、近期列表）通过 <code>localStorage</code> 以 <code>sidebar-connection-stats</code> 为键在重新加载后持久保留；滚动的迷你折线图和“连接以来”计时器有意设计为短暂的，因为它们只有相对于“当前”才有意义。一个 <strong>Reset</strong> 按钮可按需清除一切。",
    "Implementation note: per-event state lives in <code>useRef</code> buffers on the sidebar so the WS firehose never re-renders the navigation tree — the modal does its own one-second tick to sample the refs while open. Writes are throttled (single-flight timer, 2 s window) and flushed on <code>pagehide</code> / <code>visibilitychange</code> so the latest events aren't lost to the throttle window. The modal itself is portalled to <code>document.body</code> so the sidebar's stacking context can't trap it.":
      "实现说明：逐事件状态保存在侧边栏的 <code>useRef</code> 缓冲区中，因此 WS 的高速事件流绝不会重新渲染导航树——弹窗打开时会以每秒一次的节拍自行采样这些引用。写入被节流（单飞定时器，2 秒窗口）并在 <code>pagehide</code> / <code>visibilitychange</code> 时刷新，因此最新事件不会因节流窗口而丢失。弹窗本身被传送到 <code>document.body</code>，因此侧边栏的层叠上下文无法困住它。",
    "The entire UI ships in <strong>four languages — English, 简体中文, Tiếng Việt, and 한국어</strong> — built on <code>i18next</code> + <code>react-i18next</code> with <code>i18next-browser-languagedetector</code>. Coverage is end-to-end: every page, chart tooltip, Settings flow, Workflow narrative, Config Explorer tab, Run page, and the Alerts rule-help tooltips + webhook setup guides are translated. Switch languages from the sidebar (EN / 中文 / VI / 한국어) — the choice persists in <code>localStorage</code>.":
      "整个 UI 提供<strong>四种语言——English、简体中文、Tiếng Việt 和 한국어</strong>——基于 <code>i18next</code> + <code>react-i18next</code> 并配合 <code>i18next-browser-languagedetector</code> 构建。覆盖是端到端的：每个页面、图表工具提示、Settings 流程、Workflow 叙述、Config Explorer 标签页、Run 页面，以及 Alerts 规则帮助工具提示 + webhook 设置指南都已翻译。从侧边栏切换语言（EN / 中文 / VI / 한국어）——选择会持久保存在 <code>localStorage</code> 中。",
    'Translations are split into per-area JSON namespaces (<code>common</code>, <code>nav</code>, <code>dashboard</code>, <code>sessions</code>, <code>analytics</code>, <code>workflows</code>, <code>settings</code>, <code>kanban</code>, <code>run</code>, <code>ccConfig</code>, <code>alerts</code>, <code>errors</code>, <code>updates</code>) under <code>client/src/i18n/locales/&lt;lng&gt;/</code>. Components load only the namespaces they need via <code>useTranslation("…")</code>.':
      '翻译按区域拆分为各个 JSON 命名空间（<code>common</code>、<code>nav</code>、<code>dashboard</code>、<code>sessions</code>、<code>analytics</code>、<code>workflows</code>、<code>settings</code>、<code>kanban</code>、<code>run</code>、<code>ccConfig</code>、<code>alerts</code>、<code>errors</code>、<code>updates</code>），位于 <code>client/src/i18n/locales/&lt;lng&gt;/</code> 之下。组件仅通过 <code>useTranslation("…")</code> 加载它们所需的命名空间。',
    "Language is detected from <code>localStorage</code> (<code>i18nextLng</code>) then the browser's <code>navigator</code> setting, and the choice is cached back to <code>localStorage</code>. <code>fallbackLng</code> is English and <code>nonExplicitSupportedLngs</code> resolves regional tags (e.g. <code>vi-VN</code> → <code>vi</code>), so any unmapped key falls back gracefully rather than rendering a raw key.":
      "语言首先从 <code>localStorage</code>（<code>i18nextLng</code>）检测，然后从浏览器的 <code>navigator</code> 设置检测，并将选择缓存回 <code>localStorage</code>。<code>fallbackLng</code> 为英语，<code>nonExplicitSupportedLngs</code> 会解析区域标签（例如 <code>vi-VN</code> → <code>vi</code>），因此任何未映射的键都会优雅地回退，而不是渲染出原始键。",
    "Numbers, costs, dates, and relative times format against the active locale via a shared <code>getCurrentLocale()</code> helper, and plurals use i18next's <code>_one</code> / <code>_other</code> suffixes. Interpolated values (<code>{{count}}</code>, <code>{{provider}}</code>, …) keep sentences natural across languages.":
      "数字、费用、日期和相对时间通过共享的 <code>getCurrentLocale()</code> 助手按当前区域设置进行格式化，而复数则使用 i18next 的 <code>_one</code> / <code>_other</code> 后缀。插值的值（<code>{{count}}</code>、<code>{{provider}}</code>、…）让句子在各种语言中都保持自然。",
    "Domain terms that are proper nouns or code stay untranslated in every locale — <em>Agent</em>, <em>Subagent</em>, hook event names (<code>PostToolUse</code>), tool names (<code>Bash</code>), and webhook provider names (Slack, PagerDuty). Only the surrounding prose is localized, so instructions stay accurate.":
      "属于专有名词或代码的领域术语在每种区域设置下都保持不翻译——<em>Agent</em>、<em>Subagent</em>、hook 事件名（<code>PostToolUse</code>）、工具名（<code>Bash</code>）以及 webhook 提供商名称（Slack、PagerDuty）。只有周围的散文文本会被本地化，因此说明保持准确。",
    "<strong>Adding a language</strong>": "<strong>添加一种语言</strong>",
    "Copy <code>client/src/i18n/locales/en/</code> to a new locale folder, translate the JSON values (leaving keys and technical terms intact), then register the bundle and add the tag to <code>supportedLngs</code> in <code>client/src/i18n/index.ts</code>. Missing keys fall back to English automatically, so even a partial translation ships cleanly.":
      "将 <code>client/src/i18n/locales/en/</code> 复制到一个新的区域设置文件夹，翻译其中的 JSON 值（保持键和技术术语不变），然后注册该捆绑包并在 <code>client/src/i18n/index.ts</code> 中将该标签添加到 <code>supportedLngs</code>。缺失的键会自动回退到英语，因此即使是部分翻译也能干净地发布。",
    "<strong>Tabby</strong> is a cute SVG cat companion pinned to the <strong>edges of every page</strong> of the dashboard. It is always present and turns the live session stream into glanceable, ambient feedback — calm when idle, alert when something needs attention, and celebratory when a run finishes. Tabby is built entirely on the existing <code>eventBus</code> WebSocket stream: <strong>no new backend, no API key, and no new dependencies</strong>. The component lives in <code>client/src/components/Tabby/</code> and can be toggled on or off in Settings page.":
      "<strong>Tabby</strong> 是一只可爱的 SVG 猫咪伙伴，固定在仪表盘<strong>每个页面的边缘</strong>。它始终在场，把实时会话流转化为可一眼看懂的环境式反馈——空闲时安静，有事需要关注时警觉，运行完成时欢庆。Tabby 完全构建在现有的 <code>eventBus</code> WebSocket 流之上：<strong>没有新的后端、没有 API key，也没有新的依赖</strong>。该组件位于 <code>client/src/components/Tabby/</code>，可在 Settings 页面中开启或关闭。",
    '<span class="caption-icon">📥</span> Tabby Companion — a cute SVG cat in the edges of every page, reacting in real time to the live session stream with eight distinct moods and animations, auto-surfacing speech bubbles for notable events, and serving as the gateway to a status panel and Ask box':
      '<span class="caption-icon">📥</span> Tabby Companion — 位于每个页面边缘的一只可爱 SVG 猫咪，以八种不同的心情和动画实时响应实时会话流，为值得注意的事件自动弹出对话气泡，并充当通往状态面板和 Ask 框的入口',
    "Tabby derives one of eight moods from the live session WebSocket stream, each with its own animation. The eyes track your cursor, and the active mood drives a distinct motion cue.":
      "Tabby 从实时会话 WebSocket 流中推导出八种心情之一，每种都有自己的动画。眼睛会追踪你的光标，而当前心情会驱动一个独特的动作提示。",
    "Notable events — session started or finished, errors, and run completed — automatically surface a speech bubble. Bubbles are <strong>throttled and coalesced</strong> so bursts of events never spam you, and they can be muted on demand. Everything reflects in real time over the existing <code>eventBus</code> WebSocket channel, with no polling and no extra services.":
      "值得注意的事件——会话开始或结束、错误以及运行完成——会自动弹出一个对话气泡。气泡经过<strong>节流与合并</strong>，因此密集的事件绝不会刷屏，并且可按需静音。一切都通过现有的 <code>eventBus</code> WebSocket 通道实时反映，无需轮询，也无需额外的服务。",
    "Click the cat — or press <code>⌘B</code> / <code>Ctrl+B</code> — to open Tabby's panel (<code>Esc</code> closes it). The panel groups a live status line, quick actions, and an Ask box.":
      "点击猫咪——或按 <code>⌘B</code> / <code>Ctrl+B</code>——即可打开 Tabby 的面板（<code>Esc</code> 将其关闭）。该面板汇集了一行实时状态、快捷操作和一个 Ask 框。",
    "<strong>Live status line:</strong> <em>N live · M errored · connection state</em>, updated from cached data.":
      "<strong>实时状态行：</strong><em>N live · M errored · connection state</em>，由缓存数据更新。",
    "<strong>Quick actions:</strong> jump to Run Claude, Activity, Sessions, or errored sessions; mute bubbles; clear alerts.":
      "<strong>快捷操作：</strong>跳转到 Run Claude、Activity、Sessions 或出错的会话；静音气泡；清除告警。",
    "<strong>Ask box:</strong> answers simple status questions locally from cached data (&ldquo;what's running&rdquo;, &ldquo;any errors&rdquo;, &ldquo;status&rdquo;).":
      "<strong>Ask 框：</strong>从缓存数据本地回答简单的状态问题（&ldquo;what's running&rdquo;、&ldquo;any errors&rdquo;、&ldquo;status&rdquo;）。",
    "The Ask box answers status questions instantly and offline from cached data. For anything beyond a simple status question, Tabby hands off to the existing <strong>Run Claude</strong> page (<code>/run?prompt=...</code>) to spawn a real Claude Code session — so there is never a separate model call, key, or service to manage.":
      "Ask 框从缓存数据即时且离线地回答状态问题。对于超出简单状态问题的任何内容，Tabby 会移交给现有的 <strong>Run Claude</strong> 页面（<code>/run?prompt=...</code>），以启动一个真实的 Claude Code 会话——因此从来不需要管理单独的模型调用、密钥或服务。",
    "Fully keyboard operable: <code>⌘B</code> / <code>Ctrl+B</code> to open, <code>Esc</code> to close.":
      "完全可用键盘操作：<code>⌘B</code> / <code>Ctrl+B</code> 打开，<code>Esc</code> 关闭。",
    "Status and bubbles announce via <code>aria-live</code> for screen readers.":
      "状态和气泡通过 <code>aria-live</code> 向屏幕阅读器播报。",
    "Respects <code>prefers-reduced-motion</code> to calm animations.":
      "遵从 <code>prefers-reduced-motion</code> 以减弱动画。",
    "Degrades gracefully to a calm, dimmed disconnected state when offline.":
      "离线时会优雅降级为一个安静、变暗的断连状态。",
    Endpoint: "端点",
    Mood: "心情",
    "When it appears": "何时出现",
    Animation: "动画",
    Idle: "Idle",
    "Nothing notable happening": "没有任何值得注意的事情发生",
    "Gentle tail flick": "轻轻甩尾",
    Watching: "Watching",
    "Sessions active, observing the stream": "会话活跃，正在观察事件流",
    "Ear perk, cursor-tracking eyes": "竖起耳朵，眼睛追踪光标",
    Happy: "Happy",
    "A run completed successfully": "一次运行成功完成",
    Sparkle: "闪光",
    Worried: "Worried",
    "Something looks off": "有些地方看起来不对劲",
    "Head bob": "点头",
    Stuck: "Stuck",
    "A session appears blocked": "某个会话似乎被阻塞",
    "Shake + alert <code>!</code>": "晃动 + 警示 <code>!</code>",
    Thinking: "Thinking",
    "Work in progress": "工作进行中",
    Sleeping: "Sleeping",
    "Quiet for a while": "已安静一段时间",
    Zzz: "Zzz",
    Disconnected: "Disconnected",
    "WebSocket offline": "WebSocket 离线",
    "Calm, dimmed state": "安静、变暗的状态",
    "Development vs production deployment topology": "开发与生产部署拓扑",
    Aspect: "方面",
    Development: "开发",
    Production: "生产",
    Processes: "进程",
    "2 (Express + Vite)": "2 个（Express + Vite）",
    "1 (Express only)": "1 个（仅 Express）",
    "Client URL": "客户端 URL",
    "API proxy": "API 代理",
    "Vite proxies <code>/api</code> + <code>/ws</code> to :4820":
      "Vite 将 <code>/api</code> + <code>/ws</code> 代理到 :4820",
    "Same origin, no proxy": "同源，无代理",
    "File watching": "文件监听",
    "<code>node --watch</code> + Vite HMR": "<code>node --watch</code> + Vite HMR",
    None: "无",
    "Source maps": "源映射",
    Inline: "内联",
    "External files": "外部文件",
    "<strong>A third way to run: the Desktop App (macOS &amp; Windows)</strong>":
      "<strong>第三种运行方式：桌面应用（macOS &amp; Windows）</strong>",
    'Beyond development and standalone production, the dashboard also ships as a native desktop app — a macOS <code>.app</code> and a Windows <code>.exe</code> — that embeds the same production server in-process, no terminal required. See the <a href="#desktop-app">Desktop App (macOS &amp; Windows)</a> section for download, build, and install instructions.':
      '除了开发模式和独立的生产模式之外，仪表盘还以原生桌面应用的形式发布——一个 macOS 的 <code>.app</code> 和一个 Windows 的 <code>.exe</code>——它在进程内嵌入了相同的生产服务器，无需终端。有关下载、构建和安装说明，请参阅<a href="#desktop-app">桌面应用（macOS &amp; Windows）</a>章节。',
    "The production image is OCI-compatible and works with both Docker and Podman. The server listens on <code>4820</code>, reads legacy Claude history from a read-only mount, and persists SQLite data under <code>/app/data</code>.":
      "生产镜像兼容 OCI，可同时配合 Docker 和 Podman 使用。服务器监听 <code>4820</code>，从只读挂载点读取旧版 Claude 历史记录，并将 SQLite 数据持久化到 <code>/app/data</code> 下。",
    "Container image build and runtime mounts": "容器镜像构建与运行时挂载",
    Mount: "挂载",
    "Read historical Claude session files for import without modifying them":
      "读取历史 Claude 会话文件以供导入，且不会对其进行修改",
    "Persist the SQLite database across rebuilds and container restarts":
      "在重新构建和容器重启之间持久化 SQLite 数据库",
    "<strong>Hooks still run on the host</strong>": "<strong>Hooks 仍在主机上运行</strong>",
    "Claude Code fires hooks from the host machine, not from inside the container. After the container is healthy on <code>http://localhost:4820</code>, run <code>npm run install-hooks</code> on the host so hook events post back to the containerized server.":
      "Claude Code 从主机上触发 hooks，而不是从容器内部触发。在容器于 <code>http://localhost:4820</code> 上运行正常后，请在主机上运行 <code>npm run install-hooks</code>，以便 hook 事件回传到容器化的服务器。",
    "A multi-stage <code>Dockerfile</code> and <code>docker-compose.yml</code> are included. Both <strong>Docker</strong> and <strong>Podman</strong> are fully supported — the image is OCI-compliant.":
      "项目包含一个多阶段的 <code>Dockerfile</code> 和 <code>docker-compose.yml</code>。<strong>Docker</strong> 和 <strong>Podman</strong> 均获得完全支持——该镜像符合 OCI 规范。",
    "Read-only access to legacy session history for automatic import on startup":
      "以只读方式访问旧版会话历史记录，以便在启动时自动导入",
    "Persists the SQLite database across container restarts": "在容器重启之间持久化 SQLite 数据库",
    "The Dockerfile uses three stages to minimize the final image size:":
      "Dockerfile 使用三个阶段来最小化最终镜像的体积：",
    Stage: "阶段",
    "Installs production <code>node_modules</code> on digest-pinned <code>node:24.19.0-alpine3.24</code>. <code>better-sqlite3</code> is optional — if prebuilds are unavailable, the server falls back to built-in <code>node:sqlite</code>":
      "在固定摘要的 <code>node:24.19.0-alpine3.24</code> 上安装生产环境的 <code>node_modules</code>。<code>better-sqlite3</code> 是可选的——如果预构建不可用，服务器会回退到内置的 <code>node:sqlite</code>",
    "Runs <code>npm ci</code> + <code>vite build</code> to produce optimized static assets":
      "运行 <code>npm ci</code> + <code>vite build</code> 以生成经过优化的静态资源",
    "Clean digest-pinned <code>node:24.19.0-alpine3.24</code> with only <code>node_modules</code>, server code, and <code>client/dist</code>":
      "干净的固定摘要 <code>node:24.19.0-alpine3.24</code>，仅包含 <code>node_modules</code>、服务器代码和 <code>client/dist</code>",
    "<strong>Hook note</strong>": "<strong>Hook 说明</strong>",
    "Claude Code hooks run on the host, not inside the container. The containerized server receives hook events via HTTP on <code>localhost:4820</code>. Run <code>npm run install-hooks</code> on the host after starting the container.":
      "Claude Code 的 hooks 在主机上运行，而不是在容器内部运行。容器化的服务器通过 HTTP 在 <code>localhost:4820</code> 上接收 hook 事件。启动容器后，请在主机上运行 <code>npm run install-hooks</code>。",
    Metric: "指标",
    "Server startup": "服务器启动",
    "SQLite opens instantly; schema migration is idempotent": "SQLite 立即打开；架构迁移是幂等的",
    "Hook latency": "Hook 延迟",
    "Transaction + broadcast, no async I/O beyond SQLite": "事务 + 广播，除 SQLite 外无异步 I/O",
    "Client JS bundle": "客户端 JS 包",
    "WebSocket latency": "WebSocket 延迟",
    "Local loopback, JSON serialization only": "本地回环，仅 JSON 序列化",
    "SQLite write throughput": "SQLite 写入吞吐量",
    "WAL mode on SSD; far exceeds any hook event rate": "SSD 上的 WAL 模式；远超任何 hook 事件速率",
    "Max events before slowdown": "性能下降前的最大事件数",
    "Pagination prevents full-table scans": "分页可避免全表扫描",
    "Server memory": "服务器内存",
    "SQLite in-process, no ORM overhead": "SQLite 进程内运行，无 ORM 开销",
    "Client memory": "客户端内存",
    "React + Tailwind, minimal runtime deps": "React + Tailwind，运行时依赖极少",
    "Input validation": "输入校验",
    "Required fields checked before DB operations; CHECK constraints on status enums":
      "在数据库操作前检查必填字段；对状态枚举使用 CHECK 约束",
    "Hook safety": "Hook 安全",
    "Hook handler always exits 0; 5s max lifetime; uses <code>127.0.0.1</code> not external hosts":
      "Hook 处理器始终以 0 退出；最长存活 5s；使用 <code>127.0.0.1</code> 而非外部主机",
    CORS: "CORS",
    "Restricted to loopback origins, so cross-origin pages can't read responses; no-Origin clients like curl still work":
      "仅限于回环来源，因此跨源页面无法读取响应；像 curl 这样的无 Origin 客户端仍然可用",
    Authentication: "身份验证",
    "Off by default since the loopback bind is the trust boundary; set <code>DASHBOARD_TOKEN</code> to require a bearer token on every <code>/api/*</code> request and the WebSocket when exposing on a LAN.":
      "默认关闭，因为回环绑定即是信任边界；在局域网中暴露时，可设置 <code>DASHBOARD_TOKEN</code>，以要求每个 <code>/api/*</code> 请求和 WebSocket 都提供 bearer 令牌。",
    Secrets: "密钥",
    "No API keys, tokens, or credentials stored or transmitted anywhere":
      "不在任何地方存储或传输 API 密钥、令牌或凭据",
    "Dependency surface": "依赖面",
    "5 runtime server deps, 6 runtime client deps (includes D3.js for Workflows) — minimal attack surface":
      "5 个运行时服务端依赖，6 个运行时客户端依赖（包含用于 Workflows 的 D3.js）——攻击面极小",
    "Hooks only apply to sessions started <em>after</em> installation. Restart Claude Code after starting the dashboard.":
      "Hook 仅对安装<em>之后</em>启动的会话生效。启动仪表板后请重启 Claude Code。",
    "On some systems the shell environment when Claude Code fires hooks may not include the full PATH. Test with <code>node --version</code>. If not found, use the absolute path to <code>node</code> in the hook command.":
      "在某些系统上，Claude Code 触发 Hook 时的 shell 环境可能不包含完整的 PATH。可用 <code>node --version</code> 测试。如果找不到，请在 Hook 命令中使用 <code>node</code> 的绝对路径。",
    Problem: "问题",
    Solution: "解决方案",
    "<code>better-sqlite3</code> errors during install":
      "安装期间出现 <code>better-sqlite3</code> 错误",
    "This is non-fatal — <code>better-sqlite3</code> is an optional dependency. On Node 22+ the server automatically falls back to built-in <code>node:sqlite</code>. On older Node versions, install Python 3 + C++ build tools, then run <code>npm rebuild better-sqlite3</code>. For the desktop app, the <code>desktop:install</code> preflight prints copy-pasteable per-OS setup guidance (incl. a no-toolchain alternative) when the native build fails.":
      "这不是致命错误——<code>better-sqlite3</code> 是可选依赖。在 Node 22+ 上，服务器会自动回退到内置的 <code>node:sqlite</code>。在较旧的 Node 版本上，请安装 Python 3 + C++ 构建工具，然后运行 <code>npm rebuild better-sqlite3</code>。对于桌面应用，当原生构建失败时，<code>desktop:install</code> 预检会打印可复制粘贴的各操作系统设置指引（包括无需工具链的替代方案）。",
    'Dashboard shows "Disconnected"': "仪表板显示“已断开连接”",
    "Server is not running. Start it with <code>npm run dev</code>. Client auto-reconnects every 2s.":
      "服务器未运行。使用 <code>npm run dev</code> 启动。客户端每 2s 自动重连一次。",
    "Events Today shows 0": "Events Today 显示 0",
    "Ensure you are on the latest version (timezone bug was fixed). Restart the server.":
      "请确保你使用的是最新版本（时区错误已修复）。然后重启服务器。",
    "Port 4820 already in use": "端口 4820 已被占用",
    "Run <code>DASHBOARD_PORT=4821 npm run dev</code>, update Vite proxy in <code>client/vite.config.ts</code>, and re-run <code>npm run install-hooks</code>.":
      "运行 <code>DASHBOARD_PORT=4821 npm run dev</code>，更新 <code>client/vite.config.ts</code> 中的 Vite 代理，然后重新运行 <code>npm run install-hooks</code>。",
    "Stale seed data shown": "显示了过期的种子数据",
    "Run <code>npm run clear-data</code> to wipe all rows, then restart.":
      "运行 <code>npm run clear-data</code> 清除所有行，然后重启。",
    "Hooks show validation error about matcher": "Hook 显示关于 matcher 的校验错误",
    'Ensure you\'re on the latest version — the hook format was updated to use <code>matcher: "*"</code> string (not object).':
      '请确保你使用的是最新版本——Hook 格式已更新为使用 <code>matcher: "*"</code> 字符串（而非对象）。',
    '"SQLite backend not available" on startup': "启动时出现“SQLite backend not available”",
    "Neither <code>better-sqlite3</code> nor <code>node:sqlite</code> could load. Upgrade to Node.js 22+ (recommended), or install Python 3 + C++ build tools and run <code>npm rebuild better-sqlite3</code>.":
      "<code>better-sqlite3</code> 和 <code>node:sqlite</code> 都无法加载。请升级到 Node.js 22+（推荐），或安装 Python 3 + C++ 构建工具并运行 <code>npm rebuild better-sqlite3</code>。",
    "Docker container runs but no sessions appear": "Docker 容器在运行但没有出现会话",
    "Hooks run on the host, not inside the container. Run <code>npm run install-hooks</code> on the host after the container starts. Verify hooks in <code>~/.claude/settings.json</code> point to <code>localhost:4820</code>.":
      "Hook 在宿主机上运行，而不是在容器内。容器启动后，请在宿主机上运行 <code>npm run install-hooks</code>。确认 <code>~/.claude/settings.json</code> 中的 Hook 指向 <code>localhost:4820</code>。",
    Technology: "技术",
    "Why This Over Alternatives": "为何选择它而非替代方案",
    "Zero-config, embedded, no server process. WAL mode gives concurrent reads. Synchronous API is simpler than async for this use case. <code>better-sqlite3</code> is preferred when prebuilds are available; falls back to Node.js built-in <code>node:sqlite</code> on Node 22+ when the native module cannot be compiled.":
      "零配置、嵌入式、无服务器进程。WAL 模式提供并发读取。对于此用例，同步 API 比异步更简单。当有预构建可用时优先使用 <code>better-sqlite3</code>；当原生模块无法编译时，在 Node 22+ 上回退到 Node.js 内置的 <code>node:sqlite</code>。",
    "Battle-tested, minimal, well-understood. Fastify would be overkill; raw <code>http</code> module would require too much boilerplate for routing.":
      "久经考验、精简、易于理解。Fastify 会显得过度；原始的 <code>http</code> 模块在路由方面需要过多的样板代码。",
    "Fastest, most lightweight WebSocket library for Node. No Socket.IO overhead needed — we only push typed JSON messages one-way.":
      "Node 上最快、最轻量的 WebSocket 库。无需 Socket.IO 的额外开销——我们只单向推送带类型的 JSON 消息。",
    "Current supported client runtime with strong TypeScript support. CCAM remains a client-rendered SPA and does not enable Server Components or RSC.":
      "当前受支持的客户端运行时，对 TypeScript 支持强。CCAM 仍是客户端渲染的 SPA，不启用 Server Components 或 RSC。",
    "Fast builds, native ESM, excellent dev experience. Proxy config handles the dev server split cleanly with no ejection.":
      "构建快速、原生 ESM、出色的开发体验。代理配置可干净地处理开发服务器的拆分，无需弹出（eject）。",
    "Utility-first approach keeps styles colocated with markup. No CSS module boilerplate. Custom dark theme config for the dark UI.":
      "实用优先的方式让样式与标记就近放置。没有 CSS module 样板代码。为深色 UI 提供自定义深色主题配置。",
    "Standard declarative routing for React SPAs. Layout routes with <code>&lt;Outlet&gt;</code> give clean shell composition without enabling framework or RSC modes.":
      "React SPA 的标准声明式路由。使用 <code>&lt;Outlet&gt;</code> 的布局路由可实现干净的外壳组合，同时不启用 framework 或 RSC 模式。",
    "Tree-shakeable icon library — only imports what's used (~20 icons). No heavy icon font.":
      "可摇树优化的图标库——只导入用到的部分（约 20 个图标）。没有沉重的图标字体。",
    "Catches null/undefined bugs at compile time. <code>noUncheckedIndexedAccess</code> prevents array bounds issues in analytics aggregations.":
      "在编译时捕获 null/undefined 错误。<code>noUncheckedIndexedAccess</code> 可防止分析聚合中的数组越界问题。",
    "Industry-standard data visualization library. Powers the Workflows page's 11 interactive sections — DAG layouts, Sankey diagrams, force-directed graphs, bubble charts, and swim-lane timelines. No wrapper libraries needed; direct SVG rendering keeps bundle impact minimal.":
      "行业标准的数据可视化库。为 Workflows 页面的 11 个交互式部分提供支撑——DAG 布局、桑基图、力导向图、气泡图和泳道时间线。无需任何封装库；直接进行 SVG 渲染，使打包体积的影响降到最低。",
    "Available on virtually all systems. Handles ANSI and JSON natively with stdlib only. No install step required.":
      "几乎在所有系统上都可用。仅用标准库即可原生处理 ANSI 和 JSON。无需任何安装步骤。",
    "Local-first monitoring for Claude Code sessions, agents, and tool events. Built for real-time visibility with zero external dependencies.":
      "面向 Claude Code 会话、代理和工具事件的本地优先监控。为实现实时可见性而构建，零外部依赖。",
    Install: "安装",
    Setup: "设置",
    "About the Creator": "关于创作者",
    '<span class="caption-icon">⭐</span> <span> Enjoying the project? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer">Give it a star on GitHub</a> and help more builders discover it. </span>':
      '<span class="caption-icon">⭐</span> <span> 喜欢这个项目吗？<a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer">在 GitHub 上给它一个 star</a>，帮助更多开发者发现它。 </span>',
    'Clears the waiting flag and promotes the main agent to <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>. The only reliable signal that text-only assistant turns have started — they emit no <code>PreToolUse</code> before <code>Stop</code>.':
      '清除等待标志，并将主智能体提升为 <span class="status-chip chip-working"><span class="chip-dot"></span>工作中</span>。这是纯文本助手回合已经开始的唯一可靠信号——它们在 <code>Stop</code> 之前不会发出 <code>PreToolUse</code>。',
    'Clears the waiting flag, sets agent → <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>, <code>current_tool</code> set. If tool is <code>Agent</code>, subagent record created.':
      '清除等待标志，将智能体设为 <span class="status-chip chip-working"><span class="chip-dot"></span>工作中</span>，并设置 <code>current_tool</code>。如果工具是 <code>Agent</code>，则创建子智能体记录。',
    'Clears the waiting flag (covers permission-prompt approvals mid-tool). <code>current_tool</code> cleared. Agent stays <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>.':
      '清除等待标志（涵盖工具执行中途的权限提示批准）。<code>current_tool</code> 被清除。智能体保持 <span class="status-chip chip-working"><span class="chip-dot"></span>工作中</span>。',
    'Non-error: main agent → <code>waiting</code> — UI shows <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> until the next user input. <code>stop_reason=error</code>: marks the agent and session <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>. Background subagents keep running.':
      '非错误情况：主智能体 → <code>waiting</code>——UI 显示 <span class="status-chip chip-waiting"><span class="chip-dot"></span>等待中</span>，直到下一次用户输入。<code>stop_reason=error</code>：将该智能体和会话标记为 <span class="status-chip chip-error"><span class="chip-dot"></span>错误</span>。后台子智能体继续运行。',
    'Matched subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session\'s <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent\'s own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks.':
      '匹配到的子智能体 → <span class="status-chip chip-completed"><span class="chip-dot"></span>已完成</span>。它<strong>故意</strong>不清除等待标志——一个后台子智能体完成并不能告诉我们任何关于人类的信息。它还会启动一次即发即弃的 JSONL 扫描（<code>scanAndImportSubagents</code>），遍历会话的 <code>subagents/agent-*.jsonl</code> 文件，按 <code>tool_use_id</code> 将 <code>tool_use</code> ↔ <code>tool_result</code> 块配对，并在每个子智能体自己的 <code>agent_id</code> 下为每个工具发出 <code>PreToolUse</code> + <code>PostToolUse</code> 事件——从而呈现出子智能体在内部进行、却从不触发任何钩子的工具调用。',
    'Creates a compaction subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Detected via <code>isCompactSummary</code> entries in the transcript. Token baselines preserve pre-compaction totals. Periodic scanner (cadence ~¼ of <code>DASHBOARD_STALE_MINUTES</code>) catches compactions when no hooks fire.':
      '创建一个压缩子智能体 → <span class="status-chip chip-completed"><span class="chip-dot"></span>已完成</span>。通过转录中的 <code>isCompactSummary</code> 条目进行检测。Token 基线会保留压缩前的总量。当没有钩子触发时，周期性扫描器（频率约为 <code>DASHBOARD_STALE_MINUTES</code> 的 ¼）会捕捉到这些压缩。',
    'Drops the waiting flag. If the session is already in <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>, the error state is preserved; otherwise marks all agents and the session as <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Evicts the session\'s transcript from the shared cache.':
      '丢弃等待标志。如果会话已处于 <span class="status-chip chip-error"><span class="chip-dot"></span>错误</span> 状态，则保留该错误状态；否则将所有智能体和会话标记为 <span class="status-chip chip-completed"><span class="chip-dot"></span>已完成</span>。并将该会话的转录从共享缓存中逐出。',
    "SQLite connection, WAL/FK pragmas, schema migrations (<code>CREATE TABLE IF NOT EXISTS</code>), all prepared statements as a reusable <code>stmts</code> object. Tries <code>better-sqlite3</code> first, falls back to built-in <code>node:sqlite</code> via <code>compat-sqlite.js</code>":
      "SQLite 连接、WAL/FK pragma、schema 迁移（<code>CREATE TABLE IF NOT EXISTS</code>），以及作为可复用 <code>stmts</code> 对象的所有预处理语句。先尝试 <code>better-sqlite3</code>，再通过 <code>compat-sqlite.js</code> 回退到内置的 <code>node:sqlite</code>",
    "Each page pulls initial data from REST then subscribes to eventBus for live updates":
      "每个页面先从 REST 拉取初始数据，然后订阅 eventBus 以获取实时更新",
    "Entity Relationship Diagram — SQLite schema": "实体关系图——SQLite schema",
    "Working Dir": "工作目录",
    "Git Branch": "Git 分支",
    "Context Bar": "上下文栏",
    "Token Counts": "Token 计数",
    "Session Cost": "会话成本",
    "Statusline rendering pipeline — invoked on each Claude Code update":
      "状态栏渲染管线——在每次 Claude Code 更新时被调用",
    "Aggregates data from multiple API endpoints to display high-signal metrics directly in the sidebar:":
      "聚合来自多个 API 端点的数据，直接在侧边栏中显示高价值指标：",
    "Zero-Config Setup": "零配置安装",
    "One-line mental model": "一句话心智模型",
    "Your data survives reinstalls and updates": "你的数据在重装和更新后依然保留",
    "The <code>claude</code> CLI is found automatically": "<code>claude</code> CLI 会被自动找到",
    'Open <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> Releases → latest </a> and download the asset for your platform. The macOS and Windows Desktop CI jobs auto-publish a new <code>vX.Y.Z</code> release every time the version in <code>package.json</code> is bumped on <code>master</code>, so this link always points at the current build. Releases are public — no GitHub sign-in required.':
      '打开 <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> Releases → latest </a> 并下载适用于你平台的资源包。每当 <code>master</code> 上 <code>package.json</code> 中的版本号被提升时，macOS 与 Windows Desktop 的 CI 任务都会自动发布一个新的 <code>vX.Y.Z</code> 版本，因此该链接始终指向当前构建。这些发布是公开的——无需登录 GitHub。',
    'Want a build straight off the tip of <code>master</code>, ahead of the next tagged release? Every green run of the <code>🍎 macOS Desktop (DMG)</code> job on <code>macos-latest</code> uploads both per-arch DMGs as the <code>ClaudeCodeMonitor-dmg</code> workflow artifact, and the <code>🪟 Windows Desktop (EXE)</code> job on <code>windows-latest</code> uploads the installer + portable EXEs as the <code>ClaudeCodeMonitor-win</code> artifact. Open the <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><polyline points="12 7 12 12 15 14"></polyline></svg> latest passing run </a>, scroll to its Artifacts section, and download <code>ClaudeCodeMonitor-dmg</code> or <code>ClaudeCodeMonitor-win</code>. (GitHub sign-in required; 14-day retention.)':
      '想要直接从 <code>master</code> 最新提交构建、领先于下一个打标签的发布版本？<code>macos-latest</code> 上 <code>🍎 macOS Desktop (DMG)</code> 任务的每一次绿色运行都会将两个各架构 DMG 作为 <code>ClaudeCodeMonitor-dmg</code> 工作流工件上传，而 <code>windows-latest</code> 上的 <code>🪟 Windows Desktop (EXE)</code> 任务会将安装程序 + 便携式 EXE 作为 <code>ClaudeCodeMonitor-win</code> 工件上传。打开 <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><polyline points="12 7 12 12 15 14"></polyline></svg> latest passing run </a>，滚动到其 Artifacts 部分，下载 <code>ClaudeCodeMonitor-dmg</code> 或 <code>ClaudeCodeMonitor-win</code>。（需要登录 GitHub；保留 14 天。）',
    "Incoming Webhook URL": "传入 Webhook URL",
    "Webhook URL": "Webhook URL",
    "Detection pipeline from scheduler to UI": "从调度器到 UI 的检测管线",
    "A shell-less <code>git fetch</code> with a 120-second timeout, followed by a <code>rev-list</code> against the tracked upstream. Each call runs from <code>server/lib/update-check.js</code> and returns a structured payload — never throws — so a flaky remote can't stall the dashboard.":
      "一次无 shell 的 <code>git fetch</code>（120 秒超时），随后对所跟踪的上游执行一次 <code>rev-list</code>。每次调用都从 <code>server/lib/update-check.js</code> 运行，并返回结构化的载荷——绝不抛出异常——因此不稳定的远端无法拖垮仪表盘。",
    "Detection-only by design": "按设计仅做检测",
    "Adding a language": "添加一种语言",
    "<strong>Ask box:</strong> answers simple status questions locally from cached data (“what's running”, “any errors”, “status”).":
      "<strong>询问框：</strong>基于缓存数据在本地回答简单的状态问题（“正在运行什么”、“有没有错误”、“状态”）。",
    "A third way to run: the Desktop App (macOS &amp; Windows)":
      "第三种运行方式：桌面应用（macOS &amp; Windows）",
    "Hooks still run on the host": "钩子仍然在宿主机上运行",
    "Hook note": "钩子说明",
    "SQL injection": "SQL 注入",
    "All queries use prepared statements with parameterized values — no string interpolation":
      "所有查询都使用带参数化值的预处理语句——不进行字符串插值",
    "Request size": "请求大小",
    "Express JSON body parser limited to 1MB": "Express JSON 请求体解析器限制为 1MB",
    "Dashboard — stats, active agents, recent events":
      "Dashboard — 统计数据、活跃的智能体、近期事件",
    "KanbanBoard — agent status columns": "KanbanBoard — 智能体状态列",
    "Sessions — searchable, filterable table": "Sessions — 可搜索、可筛选的表格",
    "SessionDetail — agents + full event timeline": "SessionDetail — 智能体及完整事件时间线",
    "ActivityFeed — real-time streaming event log": "ActivityFeed — 实时流式事件日志",
    "Analytics — token usage, heatmap (day-of-week aligned), tool charts, donut charts":
      "Analytics — 令牌用量、热力图（按星期对齐）、工具图表、环形图",
    "Workflows — D3.js visualizations, cross-filtering, status filter, session drill-in":
      "Workflows — D3.js 可视化、交叉筛选、状态筛选、会话下钻",
    "Settings — model pricing, hook status, data export, session cleanup":
      "Settings — 模型定价、钩子状态、数据导出、会话清理",
    'Returns <code>{ "status": "ok", "timestamp": "..." }</code>':
      '返回 <code>{ "status": "ok", "timestamp": "..." }</code>',
    "List sessions with agent counts and per-session cost. Params: <code>status</code>, <code>q</code> (case-insensitive search across <code>id</code>/<code>name</code>/<code>cwd</code>), <code>limit</code> (default 50, max 10000), <code>offset</code>. Response includes <code>total</code> for paginators.":
      "列出会话，附带智能体数量及每个会话的成本。参数：<code>status</code>、<code>q</code>（在 <code>id</code>/<code>name</code>/<code>cwd</code> 上进行不区分大小写的搜索）、<code>limit</code>（默认 50，最大 10000）、<code>offset</code>。响应中包含 <code>total</code> 以供分页器使用。",
    "Session detail with agents and events": "会话详情，附带智能体和事件",
    "Create session (idempotent on <code>id</code>)": "创建会话（基于 <code>id</code> 幂等）",
    "Update session status / metadata": "更新会话状态 / 元数据",
    "List agents — params: <code>status</code>, <code>session_id</code>, <code>limit</code>, <code>offset</code>":
      "列出智能体 — 参数：<code>status</code>、<code>session_id</code>、<code>limit</code>、<code>offset</code>",
    "Single agent detail": "单个智能体详情",
    "Create agent": "创建智能体",
    "Update agent status / task / current_tool": "更新智能体的状态 / 任务 / current_tool",
    "List events newest-first — params: <code>session_id</code>, <code>limit</code>, <code>offset</code>":
      "按最新优先列出事件 — 参数：<code>session_id</code>、<code>limit</code>、<code>offset</code>",
    "Aggregate counts + status distributions + WS connections": "聚合计数 + 状态分布 + WS 连接数",
    "Token totals, tool usage, daily trends, agent types, event types, averages":
      "令牌总量、工具用量、每日趋势、智能体类型、事件类型、平均值",
    "Receive and process a Claude Code hook event (called by hook-handler.js)":
      "接收并处理 Claude Code 钩子事件（由 hook-handler.js 调用）",
    "List all model pricing rules": "列出所有模型定价规则",
    "Create or update a pricing rule": "创建或更新一条定价规则",
    "Delete a pricing rule": "删除一条定价规则",
    "Total cost across all sessions": "所有会话的总成本",
    "Cost breakdown for a specific session": "特定会话的成本明细",
    "System info, DB stats, hook installation status": "系统信息、DB 统计、钩子安装状态",
    "Delete all sessions, agents, events, token usage": "删除所有会话、智能体、事件、令牌用量",
    "Reinstall Claude Code hooks": "重新安装 Claude Code 钩子",
    "Reset Claude, Codex, or both pricing tables to defaults":
      "将 Claude、Codex 或两者的定价表重置为默认值",
    "Export all data as JSON download": "将所有数据导出为 JSON 下载",
    "Abandon stale sessions (by hours), purge old data (by days)":
      "放弃陈旧会话（按小时），清除旧数据（按天）",
    "OS-aware paths, archive command, supported extensions, step-by-step instructions; includes live stats for the default <code>~/.claude/projects</code> folder":
      "感知 OS 的路径、归档命令、支持的扩展名、分步说明；包含默认 <code>~/.claude/projects</code> 文件夹的实时统计",
    "Re-scan the default <code>~/.claude/projects</code> directory; safe to re-run (idempotent via session-ID dedup)":
      "重新扫描默认的 <code>~/.claude/projects</code> 目录；可安全重复运行（通过会话 ID 去重实现幂等）",
    "Scan any absolute directory (body <code>{ path }</code>); tilde (<code>~</code>) is expanded; walks subdirectories recursively and imports every <code>.jsonl</code> found":
      "扫描任意绝对路径目录（请求体 <code>{ path }</code>）；波浪号（<code>~</code>）会被展开；递归遍历子目录并导入找到的每个 <code>.jsonl</code>",
    "Multipart upload of <code>.jsonl</code>, <code>.meta.json</code>, <code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>, <code>.gz</code>. Per-request staging dir, path-traversal and extraction-size guards. Returns 413 <code>EXTRACTION_LIMIT_EXCEEDED</code> on suspected bomb archives":
      "分块上传 <code>.jsonl</code>、<code>.meta.json</code>、<code>.zip</code>、<code>.tar</code>、<code>.tar.gz</code>、<code>.tgz</code>、<code>.gz</code>。为每个请求设置暂存目录，并提供路径穿越和解压大小防护。对疑似炸弹归档返回 413 <code>EXTRACTION_LIMIT_EXCEEDED</code>",
    "Aggregate workflow data — orchestration graphs, tool flows, effectiveness, patterns, model delegation, error propagation, concurrency, complexity, compaction impact. Accepts <code>?status=active|completed</code> query param to filter by workflow status":
      "聚合工作流数据 — 编排图、工具流、有效性、模式、模型委派、错误传播、并发、复杂度、压缩影响。接受 <code>?status=active|completed</code> 查询参数以按工作流状态筛选",
    "Per-session drill-in — agent tree, tool timeline, event details":
      "按会话下钻 — 智能体树、工具时间线、事件详情",
    "Fired-alert feed, newest first (<code>?unacked=true</code>, <code>limit</code>, <code>offset</code>; carries <code>total</code> and <code>unacked</code> counts)":
      "已触发告警的信息流，最新优先（<code>?unacked=true</code>、<code>limit</code>、<code>offset</code>；携带 <code>total</code> 和 <code>unacked</code> 计数）",
    "Acknowledge one alert": "确认一条告警",
    "Acknowledge every unacked alert": "确认每一条未确认的告警",
    "List alert rules": "列出告警规则",
    "Create a rule (<code>event_pattern</code> | <code>inactivity</code> | <code>status_duration</code> | <code>token_threshold</code>)":
      "创建一条规则（<code>event_pattern</code> | <code>inactivity</code> | <code>status_duration</code> | <code>token_threshold</code>）",
    "Update name / config / enabled / cooldown": "更新名称 / 配置 / 启用状态 / 冷却时间",
    "Delete a rule and its fired-alert history": "删除一条规则及其已触发告警的历史记录",
    "Supported providers + their config fields (drives the UI form)":
      "支持的提供方及其配置字段（驱动 UI 表单）",
    "List webhook targets (URLs masked, secrets redacted)":
      "列出 webhook 目标（URL 已掩码、密钥已脱敏）",
    "Create a target — 14 first-class providers (Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream) + a generic JSON endpoint":
      "创建一个目标 — 14 个一等支持的提供方（Slack、Discord、Teams、Google Chat、Mattermost、Rocket.Chat、Telegram、PagerDuty、Opsgenie、Splunk On-Call、Zapier、Make、n8n、Pipedream）以及一个通用 JSON 端点",
    "Update name / url / enabled / secret / headers / config / rule scope (<code>type</code> is immutable)":
      "更新名称 / url / 启用状态 / 密钥 / 请求头 / 配置 / 规则范围（<code>type</code> 不可变）",
    "Delete a target and its delivery log": "删除一个目标及其投递日志",
    "Send a synthetic test alert and report the result": "发送一条合成的测试告警并报告结果",
    "Recent delivery log for a target": "某个目标的近期投递日志",
    Documentation: "文档",
    Architecture: "架构",
    "Relevant Links": "相关链接",
    "GitHub Repo": "GitHub 仓库",
    '<span class="caption-icon">🔔</span> <span><strong>Settings · Alerts</strong> — rules-based alerting engine and outbound webhooks in one place: alert rules (event pattern / inactivity / stuck agent / token threshold) with per-rule cooldown, a live fired-alert feed, and 14 first-class webhook providers plus a generic JSON endpoint with optional HMAC signing</span>':
      '<span class="caption-icon">🔔</span> <span><strong>设置 · 告警</strong> — 集于一处的基于规则的告警引擎与出站 Webhook：告警规则（事件模式 / 不活动 / agent 卡住 / token 阈值）支持按规则冷却、实时的已触发告警流，以及 14 个一等公民 Webhook 提供方加一个支持可选 HMAC 签名的通用 JSON 端点</span>',
    '<span class="caption-icon">📗</span> <span><strong>API Docs · ReDoc</strong> — a self-hosted, read-optimized rendering of the full OpenAPI 3.0 spec at <code>/api/redoc</code>, served entirely offline with no CDN. Complements the interactive Swagger UI at <code>/api/docs</code>; every backend route is documented with parameters, schemas, and examples</span>':
      '<span class="caption-icon">📗</span> <span><strong>API 文档 · ReDoc</strong> — 在 <code>/api/redoc</code> 上自托管、便于阅读的完整 OpenAPI 3.0 规范渲染，完全离线、无 CDN。与 <code>/api/docs</code> 的交互式 Swagger UI 互补；每个后端路由都附带参数、模式与示例文档</span>',
    '<span class="caption-icon">📘</span> <span><strong>API Docs · Swagger UI</strong> — interactive OpenAPI 3.0 playground at <code>/api/docs</code>: collapsible endpoint groups, request/response schemas, auth headers, and try-it-out request execution against the live local server</span>':
      '<span class="caption-icon">📘</span> <span><strong>API 文档 · Swagger UI</strong> — 在 <code>/api/docs</code> 的交互式 OpenAPI 3.0 演练场：可折叠的端点分组、请求/响应模式、鉴权头，以及针对本地运行服务器的 try-it-out 请求执行</span>',
    '<span class="caption-icon">📗</span> <span>ReDoc at <code>/api/redoc</code> — a self-hosted, read-optimized three-panel rendering of the same OpenAPI spec: deep-linkable sections, search, and full schema/example detail. Works entirely offline (no CDN)</span>':
      '<span class="caption-icon">📗</span> <span>位于 <code>/api/redoc</code> 的 ReDoc — 同一 OpenAPI 规范的自托管、便于阅读的三栏式渲染：可深链的章节、搜索，以及完整的模式/示例细节。完全离线运行（无 CDN）</span>',
    '<span class="caption-icon">🔔</span> Settings · Alerts — the rules-based alerting engine, a live fired-alert feed, and outbound webhook channels (14 first-class providers + a generic JSON endpoint) managed together in one place':
      '<span class="caption-icon">🔔</span> 设置 · 告警 — 基于规则的告警引擎、实时的已触发告警流，以及出站 Webhook 通道（14 个一等公民提供方 + 一个通用 JSON 端点）集中管理于一处',
    'Surfaces "dynamic workflows" — the fleets of sub-agents spawned by the <code>Workflow</code> tool and self-paced <code>/loop</code> runs. These emit no hooks, so they are reconstructed from the on-disk run journal written when a workflow finishes (<code>workflows/wf_&lt;runId&gt;.json</code>) plus the inner <code>subagents/agent-*.jsonl</code> transcripts. Each run shows its phases and a per-agent token / tool-call / duration breakdown; a running workflow is detected from its launch script before the journal exists. Runs appear in a panel on the Workflows page and as a linked subsection on each session.':
      "呈现「动态工作流」——由 <code>Workflow</code> 工具和自定节奏的 <code>/loop</code> 运行派生的 sub-agent 群组。它们不触发任何 hook，因此依据工作流完成时写入的磁盘运行日志（<code>workflows/wf_&lt;runId&gt;.json</code>）以及内部的 <code>subagents/agent-*.jsonl</code> 转录重建。每次运行展示其阶段以及按 Agent 的 token / 工具调用 / 时长分解；运行中的工作流会在日志存在之前依据其启动脚本被检测到。运行会显示在工作流页面的面板中，并作为每个会话的关联子区块。",
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs</strong> — "dynamic workflows" spawned by the Workflow tool, reconstructed from on-disk run journals: status, agent count, tokens, and tool calls, expandable into a per-agent breakdown (phase, state, tokens, tools, duration) with humanized result previews</span>':
      '<span class="caption-icon">🧬</span> <span><strong>工作流运行</strong> — 由 Workflow 工具派生的「动态工作流」，依据磁盘上的运行日志重建：状态、Agent 数量、token 与工具调用，可展开为按 Agent 的明细（阶段、状态、token、工具、时长），并附经过人性化处理的结果预览</span>',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · in a session</strong> — the same fleets linked to their launching session, so a session\'s dynamic-workflow sub-agents and their folded-in token cost are visible inline</span>':
      '<span class="caption-icon">🧬</span> <span><strong>工作流运行 · 会话内</strong> — 同样的群组关联到其启动会话，因此会话的动态工作流子 Agent 及其已计入的 token 成本可在会话内直接查看</span>',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · expanded</strong> — a run opened up: clickable color-coded phase filters, the per-agent metrics table, and a full list of clickable result items that expand to each agent\'s complete prompt and result</span>':
      '<span class="caption-icon">🧬</span> <span><strong>工作流运行 · 展开</strong> — 展开的一次运行：可点击的彩色阶段筛选、按 Agent 的指标表，以及完整的可点击结果项列表，点击即可展开每个 Agent 的完整提示词与结果</span>',
    'Live counters in Prometheus text-exposition format (v0.0.4): sessions and agents by status, total events, cumulative tokens by kind, connected WebSocket clients, configured remote sources, process uptime and RSS, and build version — so CCAM can be scraped into Prometheus / Grafana. Read-only, and — being under <code>/api</code> — behind the DNS-rebinding Host-header guard and the optional <code>DASHBOARD_TOKEN</code>: a non-loopback scraper (e.g. Prometheus in Docker via <code>host.docker.internal</code>) must be allowlisted with <code>DASHBOARD_ALLOWED_HOSTS</code> or the scrape returns <code>403 EBADHOST</code>, and must send the token when one is set. A ready-to-run Prometheus + Grafana stack with a pre-provisioned "CCAM — Overview" dashboard lives in <code>monitoring/</code> (<code>cd monitoring &amp;&amp; docker compose up -d</code>; Grafana on port 3000).':
      'Prometheus 文本展示格式（v0.0.4）的实时计数器：按状态划分的 sessions 与 agents、事件总数、按种类累计的 token、已连接的 WebSocket 客户端、已配置的远程数据源、进程运行时长与 RSS，以及构建版本——以便将 CCAM 抓取到 Prometheus / Grafana。只读，且——由于位于 <code>/api</code> 之下——处于 DNS 重绑定 Host 头防护和可选的 <code>DASHBOARD_TOKEN</code> 之后：非回环抓取器（例如 Docker 中通过 <code>host.docker.internal</code> 访问的 Prometheus）必须用 <code>DASHBOARD_ALLOWED_HOSTS</code> 加入白名单，否则抓取会返回 <code>403 EBADHOST</code>，并且在设置了令牌时必须发送该令牌。一个开箱即用、预置了 "CCAM — Overview" 仪表盘的 Prometheus + Grafana 组合位于 <code>monitoring/</code>（<code>cd monitoring &amp;&amp; docker compose up -d</code>；Grafana 在 3000 端口）。',
    "Comma-separated extra <code>Host</code>-header names allowed past the DNS-rebinding guard — needed for a non-loopback client or scraper, such as Prometheus in Docker reaching <code>/api/metrics</code> via <code>host.docker.internal</code>; an unlisted host is rejected with <code>403 EBADHOST</code>":
      "逗号分隔的额外 <code>Host</code> 头名称，允许其通过 DNS 重绑定防护——非回环客户端或抓取器需要它，例如 Docker 中通过 <code>host.docker.internal</code> 访问 <code>/api/metrics</code> 的 Prometheus；未列入的主机会被拒绝并返回 <code>403 EBADHOST</code>",
    "The entire UI ships in <strong>five languages — English, 简体中文, Tiếng Việt, 한국어, and Español</strong> — built on <code>i18next</code> + <code>react-i18next</code> with <code>i18next-browser-languagedetector</code>. Coverage is end-to-end: every page, chart tooltip, Settings flow, Workflow narrative, Config Explorer tab, Run page, and the Alerts rule-help tooltips + webhook setup guides are translated. Switch languages from the sidebar's custom language dropdown — the choice persists in <code>localStorage</code>.":
      "整个 UI 提供<strong>五种语言——English、简体中文、Tiếng Việt、한국어 和 Español</strong>，基于 <code>i18next</code> + <code>react-i18next</code> 与 <code>i18next-browser-languagedetector</code> 构建。覆盖范围端到端：每个页面、图表工具提示、Settings 流程、Workflow 叙述、Config Explorer 标签页、Run 页面，以及 Alerts 规则帮助工具提示和 webhook 设置指南均已翻译。通过侧边栏的自定义语言下拉菜单切换语言，选择会保存在 <code>localStorage</code> 中。",
    'Every alert-rule field has a help tooltip — the event-type, tool-name, and summary-contains fields include example chips of real hook events and built-in tool names. Each webhook provider ships a collapsible step-by-step setup guide linking to the official docs. A one-click <strong>"Send test"</strong> probe fires a synthetic alert and reports the delivery result inline, and targets can be scoped to specific rules. Fully localized (en / zh / vi / ko / es).':
      "每个告警规则字段都有帮助提示——event-type、tool-name 和 summary-contains 字段包含真实 hook 事件和内置工具名称的示例标签。每个 webhook 提供方都附带一份可折叠的分步设置指南，并链接到官方文档。一键 <strong>“Send test”</strong> 探测会触发一个合成告警并就地报告投递结果，且目标可限定到特定规则。完全本地化（en / zh / vi / ko / es）。",
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. A searchable checkbox project picker supports selecting multiple directories; long paths remain available in full, while compact trigger text keeps the filter bar readable. Sorting uses the same custom dropdown pattern. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, and estimated cost. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "<strong>服务端分页</strong>的表格，列出每一个已记录的会话。每一页只获取对应的数据片段，因此无论会话数量多少，成本计算都保持有界。针对 <code>id</code>、<code>name</code> 和 <code>cwd</code> 的不区分大小写搜索在服务端运行，并带有 300 ms 防抖；状态过滤器可与搜索组合以精确缩小范围。可搜索的复选框项目选择器支持选择多个目录；完整长路径仍可查看，紧凑的触发器文字让筛选栏保持易读。排序使用相同的自定义下拉模式。每一行显示会话的真实名称（从 transcript 实时同步：<code>/rename</code> 或 <code>claude -n</code> 标题，否则使用自动标题，再否则使用首条用户 prompt，并回退到短 ID）、状态徽章、agent 数量、时长、模型和预估成本。点击任意行即可深入查看完整的会话详情视图，包含对话记录和 agent 层级。",
  },
  vi: {
    "<span class=\"caption-icon\">⌘</span> <span><strong>Command Palette</strong> — <code>Cmd/Ctrl+K</code> from anywhere opens the dashboard's only launcher. One query resolves nine groups: recent commands, the nine pages, live server-side session search, the current page's own actions, every known project directory, page sub-views and list filters, all 13 Settings sections, all 12 Agent Config tabs, and preference, data-scope and language actions. Matching is subsequence-based with the matched characters highlighted</span>":
      '<span class="caption-icon">⌘</span> <span><strong>Bảng lệnh</strong> — nhấn <code>Cmd/Ctrl+K</code> ở bất kỳ đâu để mở trình khởi chạy duy nhất của dashboard. Một truy vấn giải quyết chín nhóm: lệnh gần đây, chín trang, tìm kiếm phiên phía máy chủ theo thời gian thực, các hành động của chính trang hiện tại, mọi thư mục dự án đã biết, chế độ xem con và bộ lọc danh sách, cả 13 mục Cài đặt, cả 12 tab Cấu hình Agent, cùng các hành động về tùy chọn, phạm vi dữ liệu và ngôn ngữ. Khớp theo chuỗi con và tô sáng ký tự trùng</span>',
    "Command Palette": "Bảng lệnh",
    "Press <strong>Cmd/Ctrl+K</strong> anywhere to open a launcher over the whole dashboard. One query resolves nine groups: recently run commands; the current page's own actions; every known project directory; the nine sidebar routes matched on their translated labels, so it works in every locale; live session search backed by <code>/api/sessions?q=</code>, debounced and scoped to the active machine and provider rather than a stale client-side index of thousands of sessions; every page sub-view and list filter; all thirteen Settings sections; all twelve Agent Config tabs; and actions for preferences, data scope, language, and page operations. Ranking is subsequence matching with the matched characters highlighted, so <code>mcp</code> finds MCP servers and <code>kbrd</code> finds Kanban Board, and a leading <code>&gt;</code>, <code>@</code>, or <code>#</code> narrows to actions, pages, or sessions. Arrows, Home/End, PageUp/PageDown and Tab move; Enter opens; Escape closes; a failed session query degrades quietly instead of blocking the palette. Destructive operations are deliberately absent — the palette navigates to them, never performs them.":
      "Nhấn <strong>Cmd/Ctrl+K</strong> ở bất kỳ đâu để mở trình khởi chạy cho toàn bộ dashboard. Một truy vấn phân giải chín nhóm: các lệnh vừa chạy; các hành động của chính trang hiện tại; mọi thư mục dự án đã biết; chín tuyến trong sidebar khớp theo nhãn đã dịch, nên hoạt động ở mọi ngôn ngữ; tìm kiếm phiên trực tiếp dựa trên <code>/api/sessions?q=</code>, có debounce và giới hạn theo máy cùng nhà cung cấp đang chọn thay vì một chỉ mục cũ của hàng nghìn phiên phía client; mọi chế độ xem con và bộ lọc danh sách của trang; cả mười ba mục trong Cài đặt; cả mười hai tab Cấu hình Agent; và các hành động cho tùy chọn, phạm vi dữ liệu, ngôn ngữ và thao tác trang. Xếp hạng dùng khớp chuỗi con có tô sáng ký tự trùng, nên <code>mcp</code> tìm ra MCP servers và <code>kbrd</code> tìm ra Kanban Board; ký tự <code>&gt;</code>, <code>@</code> hoặc <code>#</code> ở đầu thu hẹp về hành động, trang hoặc phiên. Mũi tên, Home/End, PageUp/PageDown và Tab để di chuyển; Enter mở; Escape đóng; truy vấn phiên thất bại sẽ giảm cấp êm ái thay vì chặn bảng lệnh. Các thao tác phá hủy được cố ý loại bỏ — bảng lệnh chỉ điều hướng tới chúng, không thực thi.",
    "Serve-stale window (ms) for per-transcript task-progress summaries behind <code>include_task_progress</code> list requests and the session-detail <code>todo_snapshot</code>. A transcript being actively appended to rarely hits the size+mtime cache key, so without this floor every list reload fully re-parses each multi-MB live transcript. <code>0</code> restores immediate re-parse on every change.":
      "C\u1eeda s\u1ed5 ph\u1ee5c v\u1ee5 d\u1eef li\u1ec7u c\u0169 (ms) cho t\u00f3m t\u1eaft ti\u1ebfn \u0111\u1ed9 t\u00e1c v\u1ee5 theo t\u1eebng transcript, ph\u00eda sau c\u00e1c y\u00eau c\u1ea7u danh s\u00e1ch <code>include_task_progress</code> v\u00e0 <code>todo_snapshot</code> c\u1ee7a trang chi ti\u1ebft phi\u00ean. Transcript \u0111ang \u0111\u01b0\u1ee3c ghi n\u1ed1i th\u00eam li\u00ean t\u1ee5c hi\u1ebfm khi tr\u00fang kh\u00f3a cache size+mtime, n\u00ean n\u1ebfu thi\u1ebfu ng\u01b0\u1ee1ng n\u00e0y, m\u1ed7i l\u1ea7n t\u1ea3i l\u1ea1i danh s\u00e1ch s\u1ebd ph\u00e2n t\u00edch l\u1ea1i to\u00e0n b\u1ed9 transcript nhi\u1ec1u MB. \u0110\u1eb7t <code>0</code> \u0111\u1ec3 kh\u00f4i ph\u1ee5c ph\u00e2n t\u00edch l\u1ea1i ngay khi c\u00f3 thay \u0111\u1ed5i.",
    "Closes an idle MCP HTTP or SSE session after this long with no request, reclaiming its server; <code>0</code> disables reaping":
      "Đóng một phiên MCP HTTP hoặc SSE nhàn rỗi sau khoảng thời gian này mà không có yêu cầu nào, thu hồi Server của nó; <code>0</code> tắt việc thu hồi.",
    "Every transport reclaims its own resources. A stdio server self-terminates once its host process is gone — stdin closing, or the process being re-parented away from the parent it started under — so a host that dies without signalling leaves no orphan behind. An HTTP session is released by <code>DELETE /mcp</code>, by an SSE disconnect, or by an idle sweep governed by <code>MCP_HTTP_SESSION_TIMEOUT_MS</code>, because terminating a session is optional in the protocol and a vanished client would otherwise hold its server forever.":
      "Mỗi phương thức truyền tải tự thu hồi tài nguyên của mình. Server stdio tự kết thúc khi tiến trình chủ của nó biến mất — stdin đóng lại, hoặc tiến trình được chuyển sang một tiến trình cha khác với tiến trình cha lúc khởi động — nên một host sập mà không gửi tín hiệu sẽ không để lại tiến trình mồ côi. Phiên HTTP được giải phóng bằng <code>DELETE /mcp</code>, bằng việc ngắt kết nối SSE, hoặc bằng lượt quét nhàn rỗi do <code>MCP_HTTP_SESSION_TIMEOUT_MS</code> điều khiển, vì việc kết thúc phiên là tùy chọn trong giao thức và một client đã biến mất sẽ giữ Server của nó mãi mãi.",
    "One-time automatic repair of token totals inflated before usage was reconciled per <code>message.id</code>. <code>replaceTokenUsage</code> is a high-water mark, so the parser fix alone can never lower a historical total. Marker-gated, deferred off the boot path, skipped while another dashboard shares the data directory, and it snapshots the old rows to <code>token_usage_pre_repair</code> first. <code>0</code> skips it \u2014 repair manually with <code>npm run repair-tokens</code>.":
      "T\u1ef1 \u0111\u1ed9ng s\u1eeda m\u1ed9t l\u1ea7n c\u00e1c t\u1ed5ng Token b\u1ecb th\u1ed5i ph\u1ed3ng tr\u01b0\u1edbc khi usage \u0111\u01b0\u1ee3c \u0111\u1ed1i so\u00e1t theo <code>message.id</code>. <code>replaceTokenUsage</code> l\u00e0 m\u1ed1c cao nh\u1ea5t n\u00ean ri\u00eang b\u1ea3n v\u00e1 parser kh\u00f4ng bao gi\u1edd h\u1ea1 \u0111\u01b0\u1ee3c t\u1ed5ng c\u0169. B\u01b0\u1edbc n\u00e0y c\u00f3 marker, \u0111\u01b0\u1ee3c ho\u00e3n kh\u1ecfi \u0111\u01b0\u1eddng kh\u1edfi \u0111\u1ed9ng, b\u1ecf qua khi c\u00f3 dashboard kh\u00e1c d\u00f9ng chung th\u01b0 m\u1ee5c d\u1eef li\u1ec7u, v\u00e0 sao l\u01b0u c\u00e1c h\u00e0ng c\u0169 sang <code>token_usage_pre_repair</code> tr\u01b0\u1edbc. \u0110\u1eb7t <code>0</code> \u0111\u1ec3 b\u1ecf qua \u2014 h\u00e3y t\u1ef1 ch\u1ea1y <code>npm run repair-tokens</code>.",
    "<strong>Monitor</strong> combines overview stats, active agent cards, collapsible subagent trees, recent activity, and the same task-progress previews used by Sessions. <strong>Health</strong> summarizes system score, storage, cache, errors, tool use, subagent effectiveness, model tokens, and compactions. Both tabs refresh every 5 seconds through WebSocket push, while the activity feed adapts its item count to the available viewport height.":
      "<strong>Monitor</strong> kết hợp thống kê tổng quan, thẻ Agent đang hoạt động, cây subagent có thể thu gọn, hoạt động gần đây và cùng bản xem trước tiến độ tác vụ như Sessions. <strong>Health</strong> tóm tắt điểm hệ thống, lưu trữ, bộ nhớ đệm, lỗi, mức dùng công cụ, hiệu quả subagent, Token mô hình và compaction. Cả hai tab làm mới mỗi 5 giây qua WebSocket, còn số mục của luồng hoạt động tự thích ứng với chiều cao viewport.",
    "Switch between <strong>Agent</strong> and <strong>Session</strong> swim lanes for working, waiting, completed, error, and abandoned states. Yellow Waiting cards identify work paused for user input, a permission gate, or the next prompt; hover badges and column headers for the exact reason and lifecycle rules. Cards show model, cumulative cost, and current tool, while WebSocket updates keep lane counts synchronized with the live event store.":
      "Chuyển giữa các làn <strong>Agent</strong> và <strong>Session</strong> cho trạng thái đang làm, chờ, hoàn tất, lỗi và bỏ dở. Thẻ Waiting màu vàng xác định công việc tạm dừng để chờ người dùng, cổng quyền hoặc Prompt tiếp theo; di chuột lên huy hiệu và tiêu đề cột để xem lý do cùng quy tắc vòng đời. Thẻ hiển thị mô hình, chi phí tích lũy và công cụ hiện tại, còn WebSocket giữ số lượng làn đồng bộ với kho sự kiện trực tiếp.",
    "Server-side rules detect event patterns, inactivity, stuck agents, and token thresholds with per-session cooldown dedup. Fired alerts appear in a live feed and can reach 14 built-in providers or a generic JSON endpoint with optional HMAC-SHA256 signing and custom headers. Delivery stays fail-safe through bounded timeouts, retries, secret redaction, test probes, and per-target logs. Configure rules and channels together in <strong>Settings → Alerts</strong>.":
      "Quy tắc phía server phát hiện mẫu sự kiện, trạng thái không hoạt động, Agent bị kẹt và ngưỡng Token với chống trùng theo thời gian chờ của từng phiên. Cảnh báo xuất hiện trong luồng trực tiếp và có thể gửi tới 14 Provider tích hợp hoặc endpoint JSON chung với chữ ký HMAC-SHA256 và Header tùy chỉnh. Timeout giới hạn, thử lại, che Secret, kiểm tra thử và nhật ký theo đích giúp giao hàng an toàn khi lỗi. Cấu hình quy tắc và kênh cùng nhau trong <strong>Settings → Alerts</strong>.",
    "Electron packages CCAM as a macOS <code>.dmg</code> and Windows installer or portable <code>.exe</code>. The app embeds the Express server in-process and renders the React client in a native window, with tray controls, application menus, login startup, and a single-instance lock. Closing the window leaves the local server running, and first boot installs Claude Code hooks automatically so events flow without a repository checkout.":
      "Electron đóng gói CCAM thành <code>.dmg</code> cho macOS và bộ cài hoặc <code>.exe</code> di động cho Windows. Ứng dụng nhúng server Express trong cùng tiến trình và hiển thị client React trong cửa sổ gốc, kèm điều khiển khay, menu ứng dụng, khởi động khi đăng nhập và khóa một phiên bản. Đóng cửa sổ vẫn để server cục bộ chạy, còn lần khởi động đầu tự cài hook Claude Code để sự kiện hoạt động mà không cần checkout kho mã.",
    "Compare model tokens, tool frequency, agent types, session outcomes, and total input, output, and cache spend. A 52-week activity heatmap and 30-day cost and session sparklines reveal longer trends, while WebSocket refresh and a live indicator keep current values clear. Responsive charts adapt to mobile screens; long legends paginate with localized controls without changing compact legends.":
      "So sánh Token theo mô hình, tần suất công cụ, loại Agent, kết quả phiên và tổng chi phí đầu vào, đầu ra, bộ nhớ đệm. Bản đồ nhiệt 52 tuần cùng biểu đồ nhỏ 30 ngày về chi phí và phiên cho thấy xu hướng dài hạn, trong khi làm mới WebSocket và chỉ báo trực tiếp giữ số liệu hiện tại rõ ràng. Biểu đồ thích ứng với màn hình di động; chú giải dài được phân trang bằng điều khiển bản địa hóa mà không đổi chú giải ngắn.",
    "Measure subagent effectiveness, workflow patterns, model delegation, error propagation, concurrency, and complexity in one view. Expand detected patterns for their full step chain, statistics, and loop-aware narrative; info popovers explain each headline metric, and the status filter applies across the page. Data-driven legends paginate when needed so every label remains reachable without making chart cards excessively tall.":
      "Đo hiệu quả subagent, mẫu workflow, phân công mô hình, lan truyền lỗi, đồng thời và độ phức tạp trong một màn hình. Mở rộng mẫu đã phát hiện để xem toàn bộ chuỗi bước, thống kê và phần mô tả nhận biết vòng lặp; popover thông tin giải thích từng chỉ số chính, còn bộ lọc trạng thái áp dụng cho cả trang. Chú giải dựa trên dữ liệu sẽ phân trang khi cần để mọi nhãn đều truy cập được mà không làm thẻ biểu đồ quá cao.",
    "Start Claude Code or Codex from a streaming browser chat, using multi-turn, headless, or resumable sessions. Re-attachment reconciles memory with the saved transcript, while slash-command completion, file references, token context, and effort controls bring core TUI workflows to the dashboard. Codex uses a native interactive <code>app-server</code> thread with provider approval, sandbox, and signed-in model discovery; same-origin checks protect every spawn.":
      "Khởi chạy Claude Code hoặc Codex từ cuộc trò chuyện trình duyệt dạng streaming với phiên nhiều lượt, headless hoặc có thể tiếp tục. Kết nối lại đối chiếu bộ nhớ với transcript đã lưu, còn hoàn thành lệnh slash, tham chiếu tệp, ngữ cảnh Token và điều khiển effort đưa quy trình TUI cốt lõi lên Dashboard. Codex dùng luồng <code>app-server</code> tương tác gốc với phê duyệt Provider, Sandbox và khám phá mô hình đã đăng nhập; kiểm tra cùng nguồn bảo vệ mọi lần khởi chạy.",
    "Open any session for overview counters, top-tool and subagent breakdowns, a collapsible agent tree, and a chronological event timeline. When task state exists, an owner-aware panel adds a segmented donut, active task, completion bar, and paginated rows. The conversation view renders Markdown, highlighted code, tool blocks, slash-command output, renames, and correctly attributed queued messages. Waiting sessions explain why and for how long; export the record as JSON or share its permalink.":
      "Mở bất kỳ phiên nào để xem các bộ đếm tổng quan, phân bổ công cụ hàng đầu và subagent, cây Agent có thể thu gọn cùng dòng thời gian sự kiện theo thứ tự. Khi có trạng thái tác vụ, bảng theo chủ sở hữu hiển thị biểu đồ vòng phân đoạn, tác vụ đang chạy, thanh hoàn thành và các hàng phân trang. Chế độ hội thoại hiển thị Markdown, mã được tô sáng, khối công cụ, đầu ra lệnh slash, đổi tên và tin nhắn xếp hàng được gán đúng nguồn. Phiên đang chờ nêu rõ lý do và thời gian; bạn cũng có thể xuất JSON hoặc chia sẻ liên kết cố định.",
    "Background sync keeps new Claude project folders and session files visible after the one-time startup import. An immediate sweep, debounced filesystem watcher, and optional 30-second safety poll share one mtime cache and coalesce overlapping work; set <code>DASHBOARD_SESSION_SYNC_MS</code> to tune or disable only the poll. Each sweep parses only changed transcripts, skips unchanged imports, and broadcasts session and main-agent updates so the UI refreshes live across macOS, Windows, and Linux.":
      "Đồng bộ nền giúp các thư mục dự án và tệp phiên Claude mới tiếp tục xuất hiện sau lần nhập khởi động duy nhất. Một lượt quét tức thì, trình theo dõi hệ thống tệp có debounce và lượt thăm dò an toàn 30 giây tùy chọn dùng chung một bộ nhớ đệm mtime và gộp công việc trùng lặp; đặt <code>DASHBOARD_SESSION_SYNC_MS</code> để điều chỉnh hoặc chỉ tắt lượt thăm dò. Mỗi lượt chỉ phân tích transcript đã đổi, bỏ qua bản nhập không đổi và phát cập nhật phiên cùng Agent chính để UI làm mới trực tiếp trên macOS, Windows và Linux.",
    "Configure per-model input, output, cache, and Codex reasoning rates in Settings, then see consistent totals across Sessions, Session Detail, Analytics, Kanban, and agent cards. Compaction-aware accounting preserves usage across context compression, imports reuse the same pricing table, and rate edits recalculate stored sessions. Subagent cards show only their own transcript cost while main agents retain session totals. GPT pricing distinguishes standard, long-context, and Fast modes, leaving unpublished tiers explicitly unpriced.":
      "Cấu hình giá đầu vào, đầu ra, bộ nhớ đệm và suy luận Codex theo từng mô hình trong Settings, rồi xem tổng chi phí nhất quán trên Sessions, Session Detail, Analytics, Kanban và thẻ Agent. Hạch toán có nhận biết compaction giữ nguyên mức sử dụng qua nén ngữ cảnh, dữ liệu nhập dùng cùng bảng giá và thay đổi giá sẽ tính lại các phiên đã lưu. Thẻ subagent chỉ hiển thị chi phí transcript của chính nó, còn Agent chính giữ tổng của phiên. Giá GPT phân biệt chế độ chuẩn, ngữ cảnh dài và Fast; các bậc chưa công bố vẫn được ghi rõ là chưa định giá.",
    "Explore Claude Code configuration in 12 tabs covering skills, subagents, commands, styles, plugins, MCP, hooks, settings, memory, keybindings, and statusline scripts. The page resolves <code>/config</code> values across scopes, searches project memory, links <code>MEMORY.md</code> entries to their source facts, and shows plugin authorship. Low-risk text, memory, and keybinding files support create, edit, and delete with timestamped backups; plugins, MCP, hooks, and live settings remain read-only with copyable CLI guidance.":
      "Khám phá cấu hình Claude Code qua 12 tab gồm skill, subagent, lệnh, kiểu đầu ra, plugin, MCP, hook, cài đặt, bộ nhớ, phím tắt và script statusline. Trang này phân giải giá trị <code>/config</code> giữa các phạm vi, tìm kiếm bộ nhớ dự án, liên kết mục <code>MEMORY.md</code> tới dữ kiện nguồn và hiển thị tác giả plugin. Các tệp văn bản, bộ nhớ và phím tắt ít rủi ro hỗ trợ tạo, sửa và xóa với bản sao lưu có dấu thời gian; plugin, MCP, hook và cài đặt trực tiếp vẫn chỉ đọc kèm hướng dẫn CLI có thể sao chép.",
    "Browse every recorded session in a <strong>server-paginated</strong> table with fast search, project and status filters, sorting, cost, model, duration, and agent counts. The first page can show a temporary local Codex startup row until its durable session arrives. Rows with task state add a compact progress donut and owner-aware preview; stale trackers disappear when newer work no longer uses them or a turn ends unfinished, while completed progress remains available. Open any durable row for its conversation, events, and agent hierarchy.":
      "Duyệt mọi phiên đã ghi trong bảng <strong>phân trang phía máy chủ</strong> với tìm kiếm nhanh, bộ lọc dự án và trạng thái, sắp xếp, chi phí, mô hình, thời lượng và số Agent. Trang đầu có thể hiển thị tạm một hàng khởi động Codex cục bộ cho đến khi phiên bền vững xuất hiện. Các hàng có trạng thái tác vụ thêm vòng tiến độ gọn và bản xem trước theo chủ sở hữu; tracker cũ biến mất khi công việc mới không còn dùng nó hoặc lượt kết thúc chưa hoàn tất, còn tiến độ đã hoàn thành vẫn được giữ lại. Mở bất kỳ hàng bền vững nào để xem hội thoại, sự kiện và phân cấp Agent.",
    "Reads Claude and Codex transcripts incrementally, keeping long-running sessions fast while powering conversation history, live titles, prompt previews, attachments, and turn statistics. Renames and recent human prompts flow to session and agent cards in real time, older conversation pages load on demand, and duplicate Codex records collapse into one readable turn. Stable transcript identities also prevent replayed events from inflating totals, while provider-aware process matching keeps historical sessions from appearing active.":
      "Đọc tăng dần transcript Claude và Codex, giữ các phiên chạy lâu luôn nhanh đồng thời cung cấp lịch sử hội thoại, tiêu đề trực tiếp, xem trước prompt, tệp đính kèm và thống kê lượt. Tên đổi và các prompt người dùng gần đây xuất hiện trên thẻ phiên và Agent theo thời gian thực, các trang hội thoại cũ được tải theo nhu cầu, và bản ghi Codex trùng lặp được gộp thành một lượt dễ đọc. Định danh transcript ổn định cũng ngăn sự kiện phát lại làm tăng sai tổng số, còn đối sánh tiến trình theo provider giữ các phiên lịch sử không bị hiển thị là active.",
    "Stable transcript identities keep Claude <code>TurnDuration</code> events replay-safe and repair legacy duplicate totals. On supported local hosts, exact open-rollout probing also prevents historical Codex sessions that share a project directory from appearing as active agents. Node launcher/native-child pairs are collapsed first, so each interactive TUI produces one card.":
      "Định danh transcript ổn định giúp sự kiện Claude <code>TurnDuration</code> an toàn khi phát lại và sửa các tổng trùng lặp từ phiên bản cũ. Trên máy cục bộ được hỗ trợ, việc dò đúng rollout đang mở cũng ngăn phiên Codex lịch sử dùng chung thư mục dự án xuất hiện như agent active. Cặp tiến trình khởi chạy Node và tiến trình con native được gộp trước, nên mỗi TUI tương tác chỉ tạo một thẻ.",
    '<span class="caption-icon">📋</span> <span><strong>Task Progress · Overview</strong> — Dashboard agent cards and Sessions rows reuse the same compact completion donut beside status; hover or focus opens an owner-aware preview of current work and task states</span>':
      '<span class="caption-icon">📋</span> <span><strong>Tiến độ công việc · Tổng quan</strong> — thẻ Agent trên Dashboard và hàng trong Sessions dùng cùng donut hoàn thành nhỏ gọn cạnh trạng thái; hover hoặc focus mở bản xem trước theo chủ sở hữu về công việc hiện tại và trạng thái tác vụ</span>',
    '<span class="caption-icon">✅</span> <span><strong>Task Progress · Session Detail</strong> — the full owner-aware tracker combines a segmented completion donut, active task, completion bar, owner breakdown, and a task list paginated at 10 rows per page</span>':
      '<span class="caption-icon">✅</span> <span><strong>Tiến độ công việc · Chi tiết phiên</strong> — trình theo dõi đầy đủ theo chủ sở hữu kết hợp donut hoàn thành phân đoạn, tác vụ đang chạy, thanh hoàn thành, phân tích chủ sở hữu và danh sách phân trang 10 hàng mỗi trang</span>',
    "Two tabs: <strong>Monitor</strong> shows overview stats, active agent cards with collapsible subagent hierarchy, and a recent activity feed whose item count fills available viewport height. When an active session exposes task state, its card shows the same completion donut as the Sessions table immediately before the status badge; hover or focus opens the detailed task preview. <strong>Health</strong> renders a composite system health score ring, storage engine donut chart, cache/error/success gauges, tool invocation bars, subagent effectiveness ratios, model token distribution, and compaction stats. Both tabs auto-refresh every 5 seconds via WebSocket push so the view is always current without manual reload.":
      "Hai tab: <strong>Monitor</strong> hiển thị số liệu tổng quan, thẻ Agent đang hoạt động với phân cấp subagent có thể thu gọn và nguồn cấp hoạt động gần đây có số mục lấp đầy chiều cao khung nhìn khả dụng. Khi một phiên đang hoạt động cung cấp trạng thái công việc, thẻ của phiên hiển thị donut hoàn thành giống bảng Sessions ngay trước huy hiệu trạng thái; di chuột hoặc focus sẽ mở bản xem trước công việc chi tiết. <strong>Health</strong> hiển thị vòng điểm sức khỏe hệ thống tổng hợp, donut công cụ lưu trữ, đồng hồ cache/lỗi/thành công, thanh gọi công cụ, tỷ lệ hiệu quả của subagent, phân bố Token theo mô hình và thống kê nén. Cả hai tab tự động làm mới mỗi 5 giây qua WebSocket để luôn cập nhật mà không cần tải lại thủ công.",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. When observable task state exists, an owner-aware progress panel appears directly below the overview with a segmented donut, active task, completion bar, owner breakdown, and dense task rows paginated at 10 tasks per page. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "Đi sâu vào từng phiên với cây phân cấp Agent có thể thu gọn và dòng thời gian sự kiện đầy đủ theo thứ tự thời gian, hiển thị tên và tóm tắt của mọi lần gọi công cụ. Bảng tổng quan phía trên hiển thị các ô đếm cho sự kiện, lượt gọi công cụ, subagent, lần nén, lỗi và thời lượng. Thanh sử dụng công cụ hàng đầu và phân tích loại subagent giúp đọc nhanh phân bố. Khi có trạng thái công việc quan sát được, một bảng tiến độ có thông tin chủ sở hữu xuất hiện ngay dưới tổng quan với donut phân đoạn, tác vụ đang chạy, thanh hoàn thành, phân tích chủ sở hữu và các hàng tác vụ gọn được phân trang 10 tác vụ mỗi trang. Trình xem hội thoại hiển thị Markdown có tô sáng cú pháp, khối theo từng công cụ, chip lệnh slash cùng đầu ra TUI đã ghi và dấu đổi tên phiên nội tuyến. Tin nhắn gõ giữa lượt, được xếp hàng khi Claude còn làm việc, xuất hiện đúng nơi Claude thực sự nhận được, còn thông báo harness được gán cho System. Có thể xuất toàn bộ phiên thành JSON hoặc chia sẻ permalink để đánh giá bất đồng bộ. Khi phiên đang chờ bạn, banner vàng dưới tiêu đề nêu lý do và thời gian chờ.",
    '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — live overview of active sessions and agents. Cards with task state show a progress donut beside status with the same detailed preview as Sessions. Stats tiles, collapsible subagent hierarchy cards, and a recent activity feed auto-refresh via WebSocket</span>':
      '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — tổng quan trực tiếp về các phiên và Agent đang hoạt động. Thẻ có trạng thái công việc hiển thị donut tiến độ cạnh trạng thái với bản xem trước chi tiết giống Sessions. Các ô thống kê, thẻ phân cấp subagent có thể thu gọn và nguồn cấp hoạt động gần đây tự động làm mới qua WebSocket</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles, an owner-aware task-progress panel with 10 tasks per page when available, top-tool usage, subagent breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>Chi tiết phiên · Agent</strong> — các ô tổng quan, bảng tiến độ công việc theo chủ sở hữu với 10 tác vụ mỗi trang khi có dữ liệu, top công cụ, phân tích subagent và cây Agent cha-con có thể thu gọn</span>',
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. The first page also shows the same local, in-memory Codex startup row used by Dashboard and Kanban. It appears immediately but stays non-navigable until a durable session ID replaces it, without changing durable totals or pagination. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, estimated cost, and — when the provider exposed task state — a compact completion donut beside the status badge. Hovering or focusing the donut opens a viewport-clamped preview with current work, owner labels, source, and up to five tasks. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "Bảng <strong>phân trang phía máy chủ</strong> của mọi phiên đã ghi nhận — mỗi trang chỉ tải phần dữ liệu của mình nên việc tính chi phí luôn được giới hạn dù có bao nhiêu phiên. Trang đầu cũng hiển thị hàng khởi động Codex cục bộ chỉ tồn tại trong bộ nhớ giống Dashboard và Kanban. Hàng này xuất hiện ngay nhưng không thể điều hướng cho đến khi ID phiên bền vững thay thế, và không làm thay đổi tổng bền vững hay phân trang. Tìm kiếm không phân biệt hoa thường trên <code>id</code>, <code>name</code> và <code>cwd</code> chạy ở máy chủ với debounce 300 ms; bộ lọc trạng thái kết hợp với tìm kiếm để thu hẹp chính xác. Mỗi hàng hiển thị tên thật của phiên (đồng bộ trực tiếp từ transcript — tiêu đề <code>/rename</code> hoặc <code>claude -n</code>, nếu không thì tiêu đề tự động, tiếp theo là prompt đầu tiên của người dùng, cuối cùng lùi về ID rút gọn), huy hiệu trạng thái, số Agent, thời lượng, mô hình, chi phí ước tính và — khi provider phát trạng thái công việc — một donut hoàn thành nhỏ cạnh huy hiệu trạng thái. Di chuột hoặc focus donut sẽ mở bản xem trước được kẹp trong viewport với công việc hiện tại, nhãn chủ sở hữu, nguồn và tối đa năm tác vụ. Nhấp hàng phiên bền vững để mở chi tiết đầy đủ với transcript hội thoại và cây Agent.",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. When observable task state exists, an owner-aware progress panel appears directly below the overview with a segmented donut, active task, completion bar, owner breakdown, and dense task rows. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "Đi sâu vào từng phiên với cây phân cấp Agent có thể thu gọn và dòng thời gian sự kiện đầy đủ theo thứ tự thời gian, hiển thị tên và tóm tắt của mọi lần gọi công cụ. Bảng tổng quan phía trên hiển thị các ô đếm cho sự kiện, lượt gọi công cụ, subagent, lần nén, lỗi và thời lượng. Thanh sử dụng công cụ hàng đầu và phân tích loại subagent giúp đọc nhanh phân bố. Khi có trạng thái công việc quan sát được, một bảng tiến độ có thông tin chủ sở hữu xuất hiện ngay dưới tổng quan với donut phân đoạn, tác vụ đang chạy, thanh hoàn thành, phân tích chủ sở hữu và các hàng tác vụ gọn. Trình xem hội thoại hiển thị markdown có tô sáng cú pháp, khối theo từng công cụ, chip lệnh slash cùng đầu ra TUI đã ghi và dấu đổi tên phiên nội tuyến. Tin nhắn gõ giữa lượt (xếp hàng khi Claude còn làm việc) xuất hiện đúng nơi Claude thực sự nhận được, còn thông báo harness được gán cho System. Có thể xuất toàn bộ phiên thành JSON hoặc chia sẻ permalink để đánh giá bất đồng bộ. Khi phiên đang chờ bạn, banner vàng dưới tiêu đề nêu lý do và thời gian chờ.",
    '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — searchable, filterable, server-paginated table. Rows with task state add a progress donut beside status; hover or focus opens a detailed task preview. Click any row to drill into session detail</span>':
      '<span class="caption-icon">📂</span> <span><strong>Phiên</strong> — bảng có tìm kiếm, bộ lọc và phân trang phía máy chủ. Hàng có trạng thái công việc thêm donut tiến độ cạnh trạng thái; hover hoặc focus mở bản xem trước chi tiết. Nhấp hàng để mở chi tiết phiên</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles, an owner-aware task-progress panel when available, top-tool usage, subagent breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>Chi tiết phiên · Agent</strong> — các ô tổng quan, bảng tiến độ công việc theo chủ sở hữu khi có dữ liệu, top công cụ, phân tích subagent và cây Agent cha-con có thể thu gọn</span>',
    "A fresh interactive Codex process first appears as an in-memory Waiting card, including while Codex is still at its trust screen or first prompt and has no stable session ID. The card is local-only, non-navigable, excluded from durable APIs unless <code>include_transient=true</code>, and removed on process exit. A <code>SessionStart</code> hook, native live-thread row, or rollout then creates the durable session and main-agent rows without copying the temporary card into history. If the user selects an existing thread in Codex's Resume picker, that exact Codex PID opens the resumed rollout or writer lock immediately. CCAM uses that signal to switch from the temporary card to the durable resumed session before the first new message.":
      "Một tiến trình Codex tương tác mới trước tiên xuất hiện dưới dạng thẻ Đang chờ chỉ tồn tại trong bộ nhớ, kể cả khi Codex vẫn ở màn hình xác nhận tin cậy hoặc prompt đầu tiên và chưa có session ID ổn định. Thẻ này chỉ tồn tại cục bộ, không thể điều hướng, không xuất hiện trong API bền vững trừ khi dùng <code>include_transient=true</code>, và bị xóa khi tiến trình thoát. Sau đó Hook <code>SessionStart</code>, hàng live-thread gốc hoặc rollout sẽ tạo các hàng phiên và agent chính bền vững mà không sao chép thẻ tạm thời vào lịch sử. Nếu người dùng chọn một luồng hiện có trong bộ chọn Resume của Codex, chính PID Codex đó sẽ mở rollout hoặc writer lock được tiếp tục ngay lập tức. CCAM dùng tín hiệu này để chuyển từ thẻ tạm thời sang phiên bền vững được tiếp tục trước tin nhắn mới đầu tiên.",
    "<strong>React 19</strong>": "<strong>React 19</strong>",
    "<strong>Vite 7</strong>": "<strong>Vite 7</strong>",
    "<strong>React Router 8</strong>": "<strong>React Router 8</strong>",
    '<span class="caption-icon">📊</span> <span><strong>Observability · Grafana</strong> — default home dashboard <em>CCAM — Overview</em> (four boards auto-provisioned): live fleet snapshot, database totals, breakdown charts, and rates from <code>/api/metrics</code> scrapes. The npm-only local helper uses <code>admin</code> / <code>admin</code>; container stacks read the admin password from <code>deployments/secrets/grafana-admin-password</code>.</span>':
      '<span class="caption-icon">📊</span> <span><strong>Khả năng quan sát · Grafana</strong> — dashboard mặc định <em>CCAM — Overview</em> với bốn bảng được cấp sẵn: ảnh chụp đội hình trực tiếp, tổng số cơ sở dữ liệu, biểu đồ phân rã và tốc độ từ các lần scrape <code>/api/metrics</code>. Trình hỗ trợ cục bộ chỉ dùng npm sử dụng <code>admin</code> / <code>admin</code>; stack container đọc mật khẩu quản trị từ <code>deployments/secrets/grafana-admin-password</code>.</span>',
    '<span class="caption-icon">📊</span> <span><strong>Grafana · CCAM — Overview</strong> — default home dashboard after login on port 3000: fleet snapshot, database totals, breakdown charts, and rates from live <code>/api/metrics</code> scrapes — no sample or synthetic data. The npm-only local helper uses <code>admin</code> / <code>admin</code>; container stacks use <code>deployments/secrets/grafana-admin-password</code>.</span>':
      '<span class="caption-icon">📊</span> <span><strong>Grafana · CCAM — Overview</strong> — dashboard mặc định sau khi đăng nhập trên cổng 3000: ảnh chụp đội hình, tổng số cơ sở dữ liệu, biểu đồ phân rã và tốc độ từ scrape <code>/api/metrics</code> trực tiếp — không có dữ liệu mẫu hay tổng hợp. Trình hỗ trợ cục bộ chỉ dùng npm sử dụng <code>admin</code> / <code>admin</code>; stack container dùng <code>deployments/secrets/grafana-admin-password</code>.</span>',
    "<strong>CCAM — Overview</strong> (default home), <strong>CCAM — Sessions &amp; Agents</strong>, <strong>CCAM — Tokens &amp; Events</strong>, and <strong>CCAM — Platform</strong> — all provisioned from <code>monitoring/grafana/dashboards/</code> with PromQL against your live scrape. The npm-only local helper uses <code>admin</code> / <code>admin</code> via <code>monitoring/grafana.defaults.env</code>. Container stacks read the password from <code>deployments/secrets/grafana-admin-password</code>.":
      "<strong>CCAM — Overview</strong> (trang mặc định), <strong>CCAM — Sessions &amp; Agents</strong>, <strong>CCAM — Tokens &amp; Events</strong> và <strong>CCAM — Platform</strong> đều được cấp từ <code>monitoring/grafana/dashboards/</code> với PromQL trên dữ liệu scrape trực tiếp. Trình hỗ trợ cục bộ chỉ dùng npm sử dụng <code>admin</code> / <code>admin</code> qua <code>monitoring/grafana.defaults.env</code>. Stack container đọc mật khẩu từ <code>deployments/secrets/grafana-admin-password</code>.",
    "The OCI runtime is non-root, uses Tini as PID 1, includes Git, OpenSSH, and SQLite, and becomes read-only except for persistent data/config volumes and tmpfs. Docker Compose and Podman Compose use the same file.":
      "OCI runtime chạy non-root, dùng Tini làm PID 1, có Git, OpenSSH và SQLite, đồng thời chỉ cho phép ghi vào volume dữ liệu/cấu hình bền vững và tmpfs. Docker Compose và Podman Compose dùng cùng một tệp.",
    "Dashboard, MCP, Nginx, Prometheus, and Grafana bind host loopback by default. Nginx proxies the UI, authenticated API, and WebSocket, while hooks, metrics, and MCP remain blocked at the edge unless explicitly enabled.":
      "Dashboard, MCP, Nginx, Prometheus và Grafana mặc định chỉ bind host loopback. Nginx proxy UI, API có xác thực và WebSocket, còn hook, metric và MCP bị chặn ở edge trừ khi được bật rõ ràng.",
    "Run exactly one dashboard container per data volume. Install hooks on the host. Remote hook destinations must use HTTPS plus <code>CCAM_HOOK_TOKEN</code>.":
      "Chỉ chạy đúng một dashboard container trên mỗi data volume. Cài hook trên host. Đích hook từ xa phải dùng HTTPS cùng <code>CCAM_HOOK_TOKEN</code>.",
    "The dashboard image runs as UID/GID 1000 with a read-only root filesystem, dropped capabilities, <code>no-new-privileges</code>, health checks, and persistent <code>/app/data</code> plus <code>/app/config</code> volumes. Claude and Codex homes mount read-only at <code>/home/node/.claude</code> and <code>/home/node/.codex</code>.":
      "Image dashboard chạy bằng UID/GID 1000 với root filesystem chỉ-đọc, bỏ mọi capability, bật <code>no-new-privileges</code>, health check và volume bền vững <code>/app/data</code> cùng <code>/app/config</code>. Claude và Codex home được mount chỉ-đọc tại <code>/home/node/.claude</code> và <code>/home/node/.codex</code>.",
    'The optional <code>agent-runtime</code> image target adds pinned Claude Code and Codex CLIs for container-native Run Agent workflows. Cloud deployments use one Recreate-managed dashboard replica on a retained ReadWriteOnce PVC through Helm, Kustomize, or Terraform-to-existing-Kubernetes. See <a href="../DEPLOYMENT.md">DEPLOYMENT.md</a>.':
      'Image target <code>agent-runtime</code> tùy chọn thêm Claude Code và Codex CLI được ghim phiên bản cho workflow Run Agent chạy native trong container. Triển khai cloud dùng một replica dashboard do Recreate quản lý trên PVC ReadWriteOnce được giữ lại qua Helm, Kustomize hoặc Terraform cho Kubernetes có sẵn. Xem <a href="../DEPLOYMENT.md">DEPLOYMENT.md</a>.',
    "Protects MCP <code>/mcp</code>, <code>/sse</code>, and <code>/messages</code>; <code>/health</code> remains available to probes":
      "Bảo vệ MCP <code>/mcp</code>, <code>/sse</code> và <code>/messages</code>; <code>/health</code> vẫn mở cho probe",
    "File-backed dashboard REST and WebSocket token for Docker or Kubernetes secrets":
      "Token REST và WebSocket của dashboard đọc từ tệp cho Secret Docker hoặc Kubernetes",
    "Independent credential for authenticated remote <code>/api/hooks/*</code> ingestion":
      "Credential độc lập cho remote <code>/api/hooks/*</code> ingestion có xác thực",
    "Writable dotenv path used when Settings persists Claude or Codex home overrides":
      "Đường dẫn dotenv có quyền ghi khi Settings lưu override Claude hoặc Codex home",
    "Optional remote hook destination; non-loopback URLs require HTTPS and a hook token":
      "Đích hook từ xa tùy chọn; URL không phải loopback yêu cầu HTTPS và hook token",
    "Rootless Docker and Podman stacks cover the dashboard, authenticated MCP, Nginx, Prometheus, and Grafana. Read-only root filesystems, file-backed secrets, health checks, WebSocket proxying, private metrics, backup tooling, and one-writer SQLite safeguards ship together.":
      "Stack Docker và Podman rootless bao gồm dashboard, MCP có xác thực, Nginx, Prometheus và Grafana. Root filesystem chỉ-đọc, Secret từ tệp, health check, WebSocket proxy, metric riêng tư, công cụ backup và bảo vệ SQLite một writer được cung cấp cùng nhau.",
    "hooks, metrics, MCP blocked by default": "hook, metric và MCP bị chặn mặc định",
    "private bearer scrape": "scrape Bearer riêng tư",
    "password file": "tệp mật khẩu",
    "One SQLite writer": "Một SQLite writer",
    "Base URL used by the local MCP server when calling dashboard APIs. Direct loopback HTTP is allowed with a bearer token; tokenized container-host aliases require HTTPS.":
      "URL cơ sở mà máy chủ MCP cục bộ dùng để gọi API dashboard. HTTP loopback trực tiếp được phép mang bearer token; alias host container có token phải dùng HTTPS.",
    "Bearer token for a protected dashboard. Falls back to <code>DASHBOARD_API_TOKEN</code>.":
      "Bearer token cho dashboard được bảo vệ. Nếu không đặt, dùng <code>DASHBOARD_API_TOKEN</code> làm fallback.",
    "Restore one export bundle up to 25 MiB (multipart <code>file</code> or JSON <code>{ path }</code>). The restore is idempotent and non-destructive, so it can consolidate machines.":
      "Khôi phục một gói xuất tối đa 25 MiB (multipart <code>file</code> hoặc JSON <code>{ path }</code>). Việc khôi phục có tính idempotent và không phá hủy, nên có thể hợp nhất dữ liệu từ nhiều máy.",
    "All dashboard requests reject redirects. Direct loopback HTTP may carry the dashboard bearer token, while tokenized container-host aliases require HTTPS. History uploads are capped at 50 MiB per file and 100 MiB per call, binary responses at 10 MiB, and backup restore at 25 MiB. Only GET requests retry automatically.":
      "Mọi yêu cầu tới dashboard đều từ chối redirect. HTTP loopback trực tiếp có thể mang bearer token của dashboard, còn alias host container có token phải dùng HTTPS. Upload lịch sử giới hạn 50 MiB mỗi tệp và 100 MiB mỗi lần gọi, phản hồi nhị phân 10 MiB, khôi phục backup 25 MiB. Chỉ yêu cầu GET được tự động thử lại.",
    "Configuration file reads canonicalize the target and allowed roots, so symlinks cannot escape the trusted Claude directories.":
      "Khi đọc tệp cấu hình, hệ thống chuẩn hóa đích và các root được phép, nên symlink không thể thoát khỏi thư mục Claude tin cậy.",
    "Codex preview targets are canonicalized before containment checks. Edits reject symlinked path components and refuse content containing <code>[redacted]</code>.":
      "Đích preview Codex được chuẩn hóa trước khi kiểm tra phạm vi. Thao tác sửa từ chối thành phần đường dẫn symlink và nội dung chứa <code>[redacted]</code>.",
    "Redirects are rejected rather than forwarding credentials or signed headers to another URL.":
      "Redirect bị từ chối thay vì chuyển tiếp thông tin xác thực hoặc header đã ký sang URL khác.",
    "Hosted providers require HTTPS. Generic and n8n targets may use HTTP for local or self-hosted receivers.":
      "Nhà cung cấp được lưu trữ yêu cầu HTTPS. Đích Generic và n8n có thể dùng HTTP cho bên nhận cục bộ hoặc tự lưu trữ.",
    "Stakeholder-ready executive, cost, reliability, and workflow reports with explicit scope and evidence":
      "Báo cáo điều hành, chi phí, độ tin cậy và workflow sẵn sàng cho stakeholder, với phạm vi và bằng chứng rõ ràng",
    "Long Analytics legends use compact pages with localized controls. Legends that fit on one page stay unchanged.":
      "Chú giải Analytics dài dùng các trang gọn với điều khiển đã bản địa hóa. Chú giải vừa một trang giữ nguyên.",
    "Data-driven Workflows legends also paginate when labels would make a chart card too tall, while keeping every label reachable.":
      "Chú giải Workflows theo dữ liệu cũng được phân trang khi nhãn làm thẻ biểu đồ quá cao, đồng thời mọi nhãn vẫn truy cập được.",
    "First-run readiness follows the selected scope: Claude Code requires Claude hooks, Codex requires Codex hooks, and Both requires both. Ready selections open the dashboard immediately; otherwise only missing selected providers appear in setup.":
      "Kiểm tra sẵn sàng lần đầu bám theo phạm vi đã chọn: Claude Code cần hook Claude, Codex cần hook Codex, còn Cả hai cần đủ hai bộ. Lựa chọn đã sẵn sàng mở dashboard ngay; nếu chưa, phần thiết lập chỉ hiển thị những nguồn đã chọn còn thiếu.",
    "Local MCP sidecar with three transport modes and 97 typed tools across 16 domain modules. It covers the complete supported app surface, including scoped data, transcripts and images, Claude/GPT pricing, workflows, alerts, webhooks, imports and restore, Claude/Codex config, Run Agent, remote sources, push, and maintenance. Every transport shares one validated catalog with bearer-token support and tiered mutation/destructive gates.":
      "MCP sidecar cục bộ hỗ trợ ba chế độ transport và 97 công cụ được định kiểu trong 16 mô-đun miền. Nó bao phủ toàn bộ bề mặt hành động được ứng dụng hỗ trợ, gồm dữ liệu có scope, transcript và ảnh, định giá Claude/GPT, workflow, alert, webhook, import và restore, cấu hình Claude/Codex, Run Agent, remote source, push và bảo trì. Mọi transport dùng chung một catalog đã xác thực, hỗ trợ Bearer Token và các cổng mutation/destructive theo cấp.",
    "One shared marketplace ships 14 plugins for Claude Code and Codex, with 66 packaged skills, 18 Claude subagents, 34 Claude commands, 3 CLI helpers, and 2 MCP-enabled plugins. Every skill includes canonical frontmatter and OpenAI metadata. The skills.sh CLI discovers 76 repository skills from the same public source. Install with <code>claude plugin marketplace add</code>, <code>codex plugin marketplace add</code>, or <code>npx skills add</code>.":
      "Một Marketplace dùng chung cung cấp 14 plugin cho Claude Code và Codex, cùng 66 skill đóng gói, 18 Claude subagent, 34 lệnh Claude, 3 tiện ích CLI và 2 plugin có MCP. Mỗi skill đều có Frontmatter chuẩn và metadata OpenAI. CLI skills.sh phát hiện 76 skill trong repo từ cùng một nguồn công khai. Cài bằng <code>claude plugin marketplace add</code>, <code>codex plugin marketplace add</code> hoặc <code>npx skills add</code>.",
    "The Agent Monitor ships one shared marketplace containing 14 plugins for Claude Code and Codex, 66 packaged plugin skills, 18 Claude subagents, 34 Claude commands, 3 CLI helpers, 3 hook configurations, and 2 MCP-enabled plugins. The skills.sh CLI discovers 76 total repository skills. Product-specific manifests stay separate while skill instructions and validation remain shared.":
      "Agent Monitor cung cấp một Marketplace dùng chung gồm 14 plugin cho Claude Code và Codex, 66 plugin skill đóng gói, 18 Claude subagent, 34 lệnh Claude, 3 tiện ích CLI, 3 cấu hình hook và 2 plugin có MCP. CLI skills.sh phát hiện tổng cộng 76 skill trong repo. Manifest riêng theo sản phẩm vẫn tách biệt, còn hướng dẫn và xác thực skill được dùng chung.",
    "Each plugin carries product-specific Claude Code and Codex manifests. The catalogs live at <code>.claude-plugin/marketplace.json</code> and <code>.agents/plugins/marketplace.json</code>. Each plugin directory contains:":
      "Mỗi plugin mang Manifest riêng cho Claude Code và Codex. Các Catalog nằm tại <code>.claude-plugin/marketplace.json</code> và <code>.agents/plugins/marketplace.json</code>. Mỗi thư mục plugin chứa:",
    "Safe monitored Claude Code/Codex launch, follow-up, stop, resume, and run history":
      "Khởi chạy, theo dõi, dừng, tiếp tục và xem lịch sử chạy Claude Code/Codex một cách an toàn.",
    "Alert rules, webhook providers, push notifications, and SSH remote collection":
      "Quy tắc cảnh báo, nhà cung cấp webhook, thông báo push và thu thập từ xa qua SSH.",
    "Claude/Codex config, hooks, provider imports, backup restore, updates, and MCP operations":
      "Cấu hình Claude/Codex, hook, import theo provider, khôi phục bản sao lưu, cập nhật và vận hành MCP.",
    "Remote sessions never emit live hooks to this dashboard, so a healthy Claude Code or Codex provider mirror remains exempt from the local process-liveness reap and watchdog transcript scan. Every successful provider sync still reconciles live status from the matching mirrored transcript: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime); within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) keeps the session <code>active</code>, otherwise it is reconciled to <code>completed</code>. If that provider's SSH sync fails, is unavailable, or is stranded in <code>syncing</code> longer than <code>DASHBOARD_STALE_MINUTES</code> — and an active session is itself older than that same window — the ordinary stale-session sweep marks it <code>abandoned</code> and completes its agents. A later fresh mirror reactivates a session that is still writing, so unavailable providers never leave permanent Waiting cards.":
      "Phiên từ xa không bao giờ gửi hook trực tiếp đến bảng điều khiển này, vì vậy mirror provider Claude Code hoặc Codex khỏe mạnh vẫn được loại khỏi quá trình thu hồi tiến trình cục bộ và quét transcript của watchdog. Mỗi lần đồng bộ provider thành công vẫn điều hòa trạng thái live từ transcript mirror tương ứng: hoạt động được đánh giá từ <b>dấu thời gian sự kiện mới nhất trong JSONL</b> (dự phòng mtime bản sao); nằm trong <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (mặc định 10 phút) giữ phiên ở <code>active</code>, nếu không nó được điều hòa thành <code>completed</code>. Nếu đồng bộ SSH của provider đó thất bại, không khả dụng, hoặc bị kẹt ở <code>syncing</code> lâu hơn <code>DASHBOARD_STALE_MINUTES</code> — và bản thân phiên active cũng cũ hơn cùng cửa sổ — lượt quét stale-session thông thường sẽ đánh dấu nó là <code>abandoned</code> và hoàn tất agent. Một mirror mới sau đó sẽ kích hoạt lại phiên vẫn đang ghi, vì vậy provider không khả dụng không bao giờ để lại thẻ Waiting vĩnh viễn.",
    '<span class="caption-icon">🛰️</span> <span><strong>Settings · Remote Data Sources</strong> — pull Claude Code and Codex activity from other machines over SSH: optionally set independent Remote Claude home and Remote Codex home paths, test each provider, sync manually or on a background poller, and switch the global data scope between local, all sources, or a specific machine with per-session source badges</span>':
      '<span class="caption-icon">🛰️</span> <span><strong>Cài đặt · Nguồn dữ liệu từ xa</strong> — kéo hoạt động Claude Code và Codex từ các máy khác qua SSH: tùy chọn cấu hình riêng đường dẫn Home Claude từ xa và Home Codex từ xa, kiểm tra từng provider, đồng bộ thủ công hoặc theo bộ hẹn giờ nền, và chuyển phạm vi dữ liệu toàn cục giữa cục bộ, tất cả nguồn, hoặc một máy cụ thể, với huy hiệu nguồn cho từng phiên</span>',
    '<span class="caption-icon">🧰</span> <span><strong>Codex Config Explorer</strong> — the Codex workspace brings together <code>config.toml</code>, account models, profiles, MCP servers, projects, skills, hooks, rules, plugins, and instructions. Edit supported user-managed files with timestamped backups; <code>config.toml</code> is edit-only.</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Trình khám phá cấu hình Codex</strong> — không gian làm việc Codex tập hợp <code>config.toml</code>, model tài khoản, profile, máy chủ MCP, dự án, skill, hook, rule, plugin và tệp hướng dẫn. Chỉnh sửa các tệp do người dùng quản lý được hỗ trợ với bản sao lưu có dấu thời gian; <code>config.toml</code> chỉ có thể chỉnh sửa.</span>',
    "Claude Code and Codex cards show a compact two-row history of their latest distinct human prompts beneath the provider-native title, so a short friendly name or terse follow-up never hides the active task. Claude refreshes this context from its local transcript cache during live hooks, imports, and watchdog sweeps; Codex refreshes it from rollout records and falls back to persisted <code>user_message</code> events for older imports. The transcript renders persisted PNG/JPEG/GIF/WebP attachments for Claude Code and Codex when available, while duplicate Codex response/event copies collapse into one human turn.":
      "Các thẻ Claude Code và Codex hiển thị lịch sử gọn hai dòng của các lời nhắc người dùng khác nhau gần nhất bên dưới tiêu đề riêng của từng nhà cung cấp, vì vậy tên ngắn gọn hoặc câu theo dõi ngắn không che khuất tác vụ đang hoạt động. Claude làm mới ngữ cảnh này từ bộ nhớ đệm transcript cục bộ trong hook trực tiếp, quá trình nhập và các lượt watchdog; Codex làm mới từ bản ghi rollout và dự phòng bằng các sự kiện <code>user_message</code> đã lưu cho các lần nhập cũ. Transcript hiển thị tệp đính kèm PNG/JPEG/GIF/WebP đã lưu của cả Claude Code và Codex khi có sẵn, đồng thời gộp bản sao response/event trùng lặp của Codex thành một lượt người dùng.",
    "<strong>Run Agent</strong> now begins by choosing Claude Code or Codex. Codex starts a native interactive <code>app-server</code> thread, offers provider-native approval and sandbox controls, and discovers models directly from the signed-in CLI so new releases appear without a dashboard update.":
      "<strong>Chạy Agent</strong> giờ bắt đầu bằng việc chọn Claude Code hoặc Codex. Codex khởi tạo thread <code>app-server</code> tương tác native, có approval và sandbox riêng, đồng thời khám phá model trực tiếp từ CLI đã đăng nhập để model mới xuất hiện mà không cần cập nhật dashboard.",
    "The same page also has a <strong>Codex</strong> switch. Its workspace shows defaults, the model cache, profiles, MCP servers, projects, skills, rules, hooks, installed plugins, and instruction files from <code>CODEX_HOME</code>. Plugin cards use <code>codex plugin list</code> rather than exposing cache folders. Secret-like TOML and JSON values are redacted in previews; an explicit local editor supports <code>config.toml</code>, <code>hooks.json</code>, user rules, skills, and instruction files with atomic saves and timestamped backups. It warns that syntax is not validated, and <code>codex_config_changed</code> refreshes the view when those files change.":
      "Cùng trang còn có công tắc <strong>Codex</strong>. Không gian làm việc hiển thị mặc định, cache model, profile, máy chủ MCP, dự án, skill, rule, hook, plugin đã cài và tệp hướng dẫn từ <code>CODEX_HOME</code>. Thẻ plugin dùng <code>codex plugin list</code> thay vì hiển thị thư mục cache. Giá trị TOML và JSON giống bí mật được che trong bản xem trước; trình soạn thảo cục bộ rõ ràng hỗ trợ <code>config.toml</code>, <code>hooks.json</code>, rule, skill và tệp hướng dẫn của người dùng với lưu nguyên tử và bản sao lưu có dấu thời gian. Nó cảnh báo rằng cú pháp không được kiểm tra, và <code>codex_config_changed</code> làm mới giao diện khi các tệp thay đổi.",
    "The same page also has a <strong>Codex</strong> switch. Its workspace shows defaults, the full local account model catalog, profiles, MCP servers, projects, skills, rules, hooks, installed plugins, and instruction files from <code>CODEX_HOME</code>. The model catalog is read apart from the small redacted-preview limit, so long account instructions cannot make the Models tab report zero. Create a standard named <code>&lt;name&gt;.config.toml</code> profile in place; Codex overlays it only for <code>codex --profile &lt;name&gt;</code>, and its card copies that exact command in one click. Plugin cards use <code>codex plugin list</code> rather than exposing cache folders. Secret-like TOML and JSON values are redacted in previews; an explicit local editor supports <code>config.toml</code>, profile overlays, <code>hooks.json</code>, user rules, skills, and instruction files with atomic saves and timestamped backups. User-maintained profiles, hooks, rules, skills, and instructions also have confirmed backup-backed <strong>View source / Copy path / Edit / Delete</strong> actions; a skill deletion preserves its whole directory, while <code>config.toml</code> stays edit-only. It warns that syntax is not validated, and <code>codex_config_changed</code> refreshes the view when those files change.":
      "Cùng trang còn có công tắc <strong>Codex</strong>. Không gian làm việc hiển thị mặc định, toàn bộ danh mục model tài khoản cục bộ, profile, máy chủ MCP, dự án, skill, rule, hook, plugin đã cài và tệp hướng dẫn từ <code>CODEX_HOME</code>. Danh mục model được đọc tách khỏi giới hạn bản xem trước đã che thông tin, nên chỉ dẫn tài khoản dài không thể khiến tab Models báo sai là không có model. Bạn có thể tạo trực tiếp profile chuẩn có tên <code>&lt;name&gt;.config.toml</code>; Codex chỉ chồng lớp nó khi dùng <code>codex --profile &lt;name&gt;</code>, và thẻ của nó sao chép chính xác lệnh đó bằng một cú nhấp. Thẻ plugin dùng <code>codex plugin list</code> thay vì hiển thị thư mục cache. Giá trị TOML và JSON giống bí mật được che trong bản xem trước; trình soạn thảo cục bộ rõ ràng hỗ trợ <code>config.toml</code>, các lớp profile, <code>hooks.json</code>, rule, skill và tệp hướng dẫn của người dùng với lưu nguyên tử và bản sao lưu có dấu thời gian. Profile, hook, rule, skill và chỉ dẫn do người dùng quản lý còn có thao tác <strong>Xem mã nguồn / Sao chép đường dẫn / Sửa / Xóa</strong> được xác nhận và sao lưu; xóa skill giữ lại toàn bộ thư mục của nó, còn <code>config.toml</code> luôn chỉ có thể sửa. Nó cảnh báo rằng cú pháp không được kiểm tra, và <code>codex_config_changed</code> làm mới giao diện khi các tệp thay đổi.",
    '<span class="caption-icon">🧰</span> <span><strong>Agent Config</strong> — switch between the editable Claude Code explorer and a live Codex workspace for defaults, models, profiles, MCP, projects, skills, rules, hooks, plugins, and instructions. Previews redact secret-like values; user-maintained files receive backup-backed edits.</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Cấu hình Agent</strong> — chuyển giữa trình khám phá Claude Code có thể chỉnh sửa và không gian làm việc Codex trực tiếp cho mặc định, model, profile, MCP, dự án, skill, rule, hook, plugin và hướng dẫn. Bản xem trước che giá trị nhạy cảm; tệp do người dùng quản lý được chỉnh sửa kèm sao lưu.</span>',
    '<span class="caption-icon">▶️</span> <span><strong>Run Agent</strong> — choose Claude Code or Codex each time you open the launcher. Claude supports Conversation or Headless mode; Codex starts a native interactive thread with approval and sandbox controls.</span>':
      '<span class="caption-icon">▶️</span> <span><strong>Chạy Agent</strong> — chọn Claude Code hoặc Codex mỗi khi mở trình khởi chạy. Claude hỗ trợ Hội thoại hoặc Một lần; Codex khởi chạy thread tương tác native với approval và sandbox.</span>',
    '<span class="caption-icon">💬</span> <span><strong>Run Agent · live stream</strong> — Claude stream-json and Codex app-server events render together as chat, including reasoning, commands, file changes, and tool activity. Dashboard Runs lets you re-attach later.</span>':
      '<span class="caption-icon">💬</span> <span><strong>Chạy Agent · luồng trực tiếp</strong> — stream-json của Claude và sự kiện app-server của Codex cùng hiển thị như chat, gồm reasoning, command, thay đổi tệp và hoạt động tool. Dashboard Runs cho phép gắn lại sau.</span>',
    "Codex also reads its native session index, so <code>/rename</code> titles reach session and agent cards in real time. Its conversation reader includes human turns plus <code>exec</code> custom-tool calls and outputs, and loads older pages when you reach the top of the transcript.":
      "Codex cũng đọc chỉ mục phiên gốc, vì vậy tiêu đề <code>/rename</code> xuất hiện trên thẻ phiên và agent theo thời gian thực. Trình đọc hội thoại gồm lượt của người dùng cùng lời gọi và đầu ra custom tool <code>exec</code>, đồng thời tải các trang cũ hơn khi bạn chạm đầu transcript.",
    "Optional Codex home. Settings saves this dashboard-only override, re-arms live rollout watching, and immediately scans the new <code>sessions/</code> tree.":
      "Thư mục Codex tùy chọn. Cài đặt lưu ghi đè riêng cho dashboard này, kích hoạt lại việc theo dõi rollout trực tiếp và quét ngay cây <code>sessions/</code> mới.",
    "<strong>Dashboard Data</strong> lets you switch the entire app between Claude Code, Codex, or both. Sessions, agents, events, token totals, workflows, analytics, and costs refetch immediately. The same page saves separate Claude Code and Codex homes; saving the Codex home re-arms live rollout watching and performs a fresh scan.":
      "<strong>Dữ liệu dashboard</strong> cho phép bạn chuyển toàn bộ ứng dụng giữa Claude Code, Codex hoặc cả hai. Sessions, agents, events, tổng token, workflows, analytics và chi phí được lấy lại ngay lập tức. Cùng trang đó lưu riêng thư mục Claude Code và Codex; lưu thư mục Codex sẽ kích hoạt lại việc theo dõi rollout trực tiếp và quét mới.",
    "The manual installer is interactive: Claude Code starts selected; use arrow keys, <kbd>Space</kbd>, then <kbd>Enter</kbd> to choose Claude Code, Codex (beta), or both. If a selected dashboard hook is already present, it warns before replacing only that entry and preserves unrelated hooks. The same guided installer is available from <strong>Settings → Hook Configuration</strong>. On the first dashboard visit, choose a data source, then use the provider-locked live-monitoring setup gate to install the selected hooks in place and review its output. You can also explicitly confirm hooks are already installed before continuing.":
      "Trình cài đặt thủ công có tính tương tác: Claude Code được chọn sẵn; dùng phím mũi tên, <kbd>Space</kbd>, rồi <kbd>Enter</kbd> để chọn Claude Code, Codex (beta), hoặc cả hai. Nếu hook dashboard đã có cho lựa chọn đó, trình cài đặt sẽ cảnh báo trước khi chỉ thay thế mục đó và vẫn giữ các hook không liên quan. Cùng trình cài đặt có hướng dẫn này cũng có trong <strong>Settings → Hook Configuration</strong>. Khi vào dashboard lần đầu, hãy chọn nguồn dữ liệu, sau đó dùng cổng thiết lập giám sát trực tiếp được khóa theo nguồn để cài hook đã chọn ngay tại đó và xem kết quả. Bạn cũng có thể xác nhận rõ rằng hook đã được cài trước khi tiếp tục.",
    "Codex rollouts are accounted separately with fresh, cached, cache-write, output, and reasoning-output deltas. The GPT rate card distinguishes standard short context (≤272K input tokens), long context, and Fast mode; unavailable published tiers remain explicitly unpriced until you set a rate.":
      "Các rollout Codex được tính riêng bằng các delta đầu vào mới, đầu vào đã lưu đệm, ghi cache, đầu ra và đầu ra suy luận. Bảng giá GPT phân biệt ngữ cảnh ngắn chuẩn (≤272K token đầu vào), ngữ cảnh dài và chế độ Fast; các tier chưa có giá công bố vẫn được ghi rõ là chưa định giá cho đến khi bạn đặt giá.",
    "Codex rollout records are the source of truth for live cards: <code>user_message</code> and <code>task_started</code> mark the main agent working; <code>task_complete</code> keeps the session active while showing Waiting; and <code>turn_aborted</code> shows interrupted Waiting. Newest rollouts are processed first, and an unreadable historical file is retried without delaying the rest.":
      "Bản ghi rollout Codex là nguồn sự thật cho các thẻ trực tiếp: <code>user_message</code> và <code>task_started</code> đánh dấu agent chính đang làm việc; <code>task_complete</code> giữ phiên active nhưng hiển thị Đang chờ; và <code>turn_aborted</code> hiển thị Đang chờ bị gián đoạn. Rollout mới nhất được xử lý trước, còn tệp lịch sử không đọc được sẽ được thử lại mà không làm chậm phần còn lại.",
    "Codex <code>response_item</code> tool calls are indexed exactly once through a separate rollout cursor, so provider-scoped Workflows shows recorded tool flows, session drill-ins, token/model totals, and <code>context_compacted</code> counts without replaying lifecycle or token accounting. Dynamic Workflows stays hidden in a Codex-only scope because its on-disk journals are a Claude Code feature.":
      "Các lời gọi tool <code>response_item</code> của Codex được lập chỉ mục đúng một lần bằng cursor rollout riêng, vì vậy Workflows theo phạm vi provider hiển thị luồng tool đã ghi, drill-in phiên, tổng token/model và số lần <code>context_compacted</code> mà không chạy lại vòng đời hay tính token. Dynamic Workflows vẫn ẩn trong phạm vi chỉ Codex vì journal trên đĩa của nó là tính năng Claude Code.",
    "Optional Codex home. Defaults to <code>CODEX_HOME</code> or <code>~/.codex</code>.":
      "Thư mục Codex tùy chọn. Mặc định là <code>CODEX_HOME</code> hoặc <code>~/.codex</code>.",
    "Safety-net interval (ms) for incremental Codex rollout scanning. Default <code>4000</code>; <code>0</code> disables polling but keeps the watcher.":
      "Khoảng an toàn (ms) để quét rollout Codex tăng dần. Mặc định <code>4000</code>; <code>0</code> tắt polling nhưng vẫn giữ watcher.",
    "How long a hook-only Codex session may leave a reported-finished turn unanswered before its <code>SessionEnd</code> is presumed lost. A run started with <code>codex exec --ephemeral</code> writes no rollout, so it has neither a transcript mtime nor an open file for the liveness probes — the fallback that matters most on Windows, where both probes are unavailable. Only a session already parked by a real <code>Stop</code> hook qualifies, because <code>SessionEnd</code> follows <code>Stop</code> within a few hundred milliseconds. Silence never qualifies: a rollout-less run emits no hooks at all while a tool runs, so an idle timer would complete a live CI build. A session still running self-heals on its next hook.":
      "Thời gian một phiên Codex chỉ-từ-hook được phép để một lượt đã báo kết thúc mà không có phản hồi, trước khi <code>SessionEnd</code> của nó bị coi là đã mất. Lần chạy khởi động bằng <code>codex exec --ephemeral</code> không ghi rollout, nên không có mtime transcript lẫn tệp đang mở cho các liveness probe — đây là phương án dự phòng quan trọng nhất trên Windows, nơi cả hai probe đều không khả dụng. Chỉ phiên đã được một hook <code>Stop</code> thật sự đưa vào trạng thái chờ mới đủ điều kiện, vì <code>SessionEnd</code> đến sau <code>Stop</code> trong vòng vài trăm mili giây. Sự im lặng không bao giờ đủ điều kiện: lần chạy không có rollout hoàn toàn không phát hook nào khi một công cụ đang chạy, nên bộ đếm nhàn rỗi sẽ kết thúc nhầm một bản build CI đang chạy. Phiên vẫn đang chạy sẽ tự phục hồi ở hook kế tiếp.",
    "CCAM exposes a read-only <strong>Prometheus text-exposition</strong> endpoint at <code>GET /api/metrics</code> — sessions and agents by status, cumulative events and tokens, connected WebSocket clients, configured remote sources, process uptime/RSS, and build version — so the same dashboard you use for Claude Code monitoring can be scraped into your own observability stack. A turnkey stack lives in <code>monitoring/</code>: npm-managed binaries (no Homebrew/apt) or optional Docker Compose, with Grafana datasource and four dashboards auto-provisioned on first start.":
      "CCAM cung cấp endpoint <strong>Prometheus text-exposition</strong> chỉ-đọc tại <code>GET /api/metrics</code> — sessions/agents theo trạng thái, sự kiện và token tích lũy, client WebSocket đang kết nối, nguồn từ xa đã cấu hình, uptime/RSS tiến trình và phiên bản build — để dashboard bạn dùng giám sát Claude Code có thể được scrape vào observability stack riêng. Stack sẵn dùng trong <code>monitoring/</code>: binary quản lý bằng npm (không cần Homebrew/apt) hoặc Docker Compose tùy chọn, datasource Grafana và bốn dashboard tự cấp hình khi khởi động lần đầu.",
    "<strong>Quick start (npm):</strong> run the dashboard on loopback, then <code>npm run monitoring:install</code> once and <code>npm run monitoring:up</code>. Open Grafana at <code>http://localhost:3000</code> and Prometheus at <code>http://localhost:9090</code>. The pre-built CCAM console is at <code>http://localhost:9090/consoles/index.html</code> (also reachable from the Prometheus UI under <strong>Consoles</strong>). For an all-Docker path use <code>npm run monitoring:docker:up</code> or <code>npm run docker:full:up</code> for dashboard + Prometheus + Grafana together. Verify with <code>npm run monitoring:verify</code> — the <code>ccam</code> scrape target should read <strong>UP</strong>.":
      "<strong>Khởi động nhanh (npm):</strong> chạy dashboard trên loopback, rồi <code>npm run monitoring:install</code> một lần và <code>npm run monitoring:up</code>. Grafana: <code>http://localhost:3000</code>, Prometheus: <code>http://localhost:9090</code>. Console CCAM dựng sẵn: <code>http://localhost:9090/consoles/index.html</code> (cũng có trong Prometheus UI mục <strong>Consoles</strong>). Đường Docker đầy đủ: <code>npm run monitoring:docker:up</code> hoặc <code>npm run docker:full:up</code>. Xác minh bằng <code>npm run monitoring:verify</code> — target scrape <code>ccam</code> phải <strong>UP</strong>.",
    "Four Grafana boards": "Bốn bảng Grafana",
    "Prometheus console": "Console Prometheus",
    "Prometheus 3.x dropped the legacy console template libraries, so CCAM ships a <strong>static HTML console</strong> at <code>monitoring/prometheus/consoles/index.html</code> that fetches <code>/api/v1/query</code> directly — live cards, session tables, and Graph links without Go-template errors on <code>/consoles/index.html</code>.":
      "Prometheus 3.x bỏ thư viện template console cũ, nên CCAM cung cấp <strong>console HTML tĩnh</strong> tại <code>monitoring/prometheus/consoles/index.html</code> gọi <code>/api/v1/query</code> trực tiếp — thẻ trực tiếp, bảng phiên và liên kết Graph, không lỗi Go-template trên <code>/consoles/index.html</code>.",
    "Scrape &amp; auth guard": "Bảo vệ scrape &amp; xác thực",
    "Loopback scrapes (<code>127.0.0.1:4820</code>) work out of the box. When Prometheus runs in Docker and reaches the host via <code>host.docker.internal</code>, add that host to <code>DASHBOARD_ALLOWED_HOSTS</code> or the scrape returns <code>403 EBADHOST</code>. If <code>DASHBOARD_TOKEN</code> is set, scrapers must send the same bearer token.":
      "Scrape loopback (<code>127.0.0.1:4820</code>) hoạt động ngay. Khi Prometheus chạy trong Docker và truy cập host qua <code>host.docker.internal</code>, thêm host đó vào <code>DASHBOARD_ALLOWED_HOSTS</code> nếu không scrape trả <code>403 EBADHOST</code>. Nếu đặt <code>DASHBOARD_TOKEN</code>, scraper phải gửi cùng bearer token.",
    "Full reference": "Tham chiếu đầy đủ",
    'Commands, Docker vs npm paths, dashboard UIDs, starter PromQL, and troubleshooting (Grafana login reset, target DOWN) are documented in <a href="../monitoring/README.md"><code>monitoring/README.md</code></a> and the <a href="#api-reference">API reference</a> entry for <code>GET /api/metrics</code>.':
      'Lệnh, đường Docker vs npm, UID dashboard, PromQL khởi đầu và xử lý sự cố (đặt lại đăng nhập Grafana, target DOWN) có trong <a href="../monitoring/README.md"><code>monitoring/README.md</code></a> và mục <a href="#api-reference">API reference</a> cho <code>GET /api/metrics</code>.',
    "Pull Claude Code history from other machines over SSH. Each remote's <code>~/.claude/projects</code> tree is mirrored via <code>scp</code> into a sandboxed staging directory, fed through the same importer as local history, and every session is tagged with its source. A background poller keeps remote data near-real-time, a global data-scope selector (Local only / All / a specific source) narrows the whole app, and sessions from a remote show a source badge. Auth defers entirely to the host's SSH stack — no passwords or secrets are stored. Managed in <b>Settings → Remote Data Sources</b>.":
      "Kéo lịch sử Claude Code từ các máy khác qua SSH. Cây thư mục <code>~/.claude/projects</code> của mỗi máy từ xa được mirror qua <code>scp</code> vào một thư mục dàn dựng biệt lập, đưa qua cùng bộ nhập như lịch sử cục bộ, và mỗi phiên được gắn thẻ nguồn của nó. Một bộ thăm dò nền giữ dữ liệu từ xa gần thời gian thực, một bộ chọn phạm vi dữ liệu toàn cục (Chỉ cục bộ / Tất cả / một nguồn cụ thể) thu hẹp toàn bộ ứng dụng, và các phiên từ máy từ xa hiển thị huy hiệu nguồn. Xác thực hoàn toàn dựa vào ngăn xếp SSH của máy chủ — không lưu trữ mật khẩu hay bí mật nào. Quản lý trong <b>Settings → Remote Data Sources</b>.",
    "Remote Data Sources": "Nguồn dữ liệu từ xa",
    "One remote source can independently import <b>Claude Code</b>, <b>Codex</b>, or both. Codex mirrors <code>~/.codex/sessions</code> and its lightweight <code>session_index.jsonl</code> title index; the connection test and sync result show a separate state for each provider, so a missing provider never hides fresh data from the other.":
      "Một nguồn từ xa có thể nhập độc lập <b>Claude Code</b>, <b>Codex</b> hoặc cả hai. Codex mirror <code>~/.codex/sessions</code> cùng chỉ mục tiêu đề nhẹ <code>session_index.jsonl</code>; kiểm tra kết nối và kết quả đồng bộ hiển thị trạng thái riêng cho từng provider, nên provider thiếu không bao giờ che dữ liệu mới của provider còn lại.",
    "List, add, and test SSH data sources; sync one source (or all when no id is given); and remove a source, optionally purging the sessions it imported":
      "Liệt kê, thêm và kiểm tra các nguồn dữ liệu SSH; đồng bộ một nguồn (hoặc tất cả khi không cung cấp id); và gỡ bỏ một nguồn, tùy chọn xóa các phiên mà nó đã nhập",
    "Beyond local history, the dashboard can pull Claude Code activity from <b>other machines over SSH</b> and fold it into the same views as your local sessions. Each remote's <code>~/.claude/projects</code> tree is mirrored via <code>scp</code> (or <code>wsl.exe</code> + <code>tar</code> when Claude runs in WSL on a Windows SSH host) into a sandboxed staging directory, fed through the exact same importer used for local history, and every imported session is tagged with the source it came from. A background poller (<code>DASHBOARD_REMOTE_SYNC_MS</code>, default <code>15000</code> ms) keeps remote data near-real-time — adding or re-enabling a source also triggers an immediate pull — and successful syncs broadcast <code>remote_data.updated</code> plus per-session <code>session_created</code> / <code>session_updated</code> frames. A global <b>data scope</b> selector lets you narrow the entire app to local sessions only, all sources, or a specific machine. Sources are managed in <b>Settings → Remote Data Sources</b>.":
      "Ngoài lịch sử cục bộ, bảng điều khiển có thể kéo hoạt động Claude Code từ <b>các máy khác qua SSH</b> và gộp vào cùng các khung nhìn với các phiên cục bộ của bạn. Cây thư mục <code>~/.claude/projects</code> của mỗi máy từ xa được mirror qua <code>scp</code> (hoặc <code>wsl.exe</code> + <code>tar</code> khi Claude chạy trong WSL trên máy chủ SSH Windows) vào một thư mục dàn dựng biệt lập, đưa qua đúng bộ nhập được dùng cho lịch sử cục bộ, và mỗi phiên được nhập đều được gắn thẻ nguồn xuất xứ. Một bộ thăm dò nền (<code>DASHBOARD_REMOTE_SYNC_MS</code>, mặc định <code>15000</code> ms) giữ dữ liệu từ xa gần thời gian thực — thêm hoặc bật lại nguồn cũng kích hoạt kéo ngay — và đồng bộ thành công phát <code>remote_data.updated</code> cùng các khung <code>session_created</code> / <code>session_updated</code> theo từng phiên. Bộ chọn <b>phạm vi dữ liệu</b> toàn cục cho phép bạn thu hẹp toàn bộ ứng dụng chỉ còn các phiên cục bộ, tất cả nguồn, hoặc một máy cụ thể. Các nguồn được quản lý trong <b>Settings → Remote Data Sources</b>.",
    "Each remote is mirrored over SSH, imported through the local pipeline, and tagged by source — the poller (default 15 s) keeps it near-real-time":
      "Mỗi máy từ xa được mirror qua SSH, nhập qua đường ống cục bộ và gắn thẻ theo nguồn — bộ thăm dò (mặc định 15 giây) giữ nó gần thời gian thực",
    "Add a source with an SSH destination — either a <code>user@host</code> pair or an alias defined in your <code>~/.ssh/config</code> — plus an optional port, identity file, and remote home directory. Use <b>Test connection</b> to verify reachability before saving, then <b>Sync now</b> to pull immediately, or let the background poller do it on its own schedule. Removing a source can optionally purge the sessions it imported.":
      "Thêm một nguồn với đích SSH — hoặc một cặp <code>user@host</code> hoặc một bí danh được định nghĩa trong <code>~/.ssh/config</code> của bạn — cùng với cổng, tệp danh tính và thư mục home từ xa tùy chọn. Dùng <b>Test connection</b> để xác minh khả năng kết nối trước khi lưu, rồi <b>Sync now</b> để kéo ngay lập tức, hoặc để bộ thăm dò nền tự thực hiện theo lịch của nó. Việc gỡ bỏ một nguồn có thể tùy chọn xóa các phiên mà nó đã nhập.",
    "A global data-scope selector narrows the whole app: choose <b>Local only</b>, <b>All</b> sources, or one specific machine. The scope flows into every data view through a <code>sources</code> query parameter on the sessions, events, agents, stats, and analytics endpoints, and sessions imported from a remote carry a <b>source badge</b> so their origin is always visible.":
      "Bộ chọn phạm vi dữ liệu toàn cục thu hẹp toàn bộ ứng dụng: chọn <b>Local only</b>, <b>All</b> nguồn, hoặc một máy cụ thể. Phạm vi này chảy vào mọi khung nhìn dữ liệu thông qua tham số truy vấn <code>sources</code> trên các điểm cuối sessions, events, agents, stats và analytics, và các phiên được nhập từ máy từ xa mang một <b>source badge</b> để nguồn gốc của chúng luôn hiển thị.",
    "Authentication defers entirely to the host's own SSH stack — <code>~/.ssh/config</code>, <code>ssh-agent</code>, on-disk keys, and <code>known_hosts</code>. The dashboard stores <b>no passwords or secrets</b>; it only records the SSH destination and connection options you supply. <code>StrictHostKeyChecking</code> is left at its default, so the remote host must already be present in <code>known_hosts</code> — an unknown host fails the connection rather than being trusted blindly.":
      "Xác thực hoàn toàn dựa vào ngăn xếp SSH của chính máy chủ — <code>~/.ssh/config</code>, <code>ssh-agent</code>, khóa trên đĩa, và <code>known_hosts</code>. Bảng điều khiển <b>không lưu trữ mật khẩu hay bí mật nào</b>; nó chỉ ghi lại đích SSH và các tùy chọn kết nối bạn cung cấp. <code>StrictHostKeyChecking</code> được giữ ở giá trị mặc định, vì vậy máy chủ từ xa phải đã có mặt trong <code>known_hosts</code> — một máy chủ không xác định sẽ khiến kết nối thất bại thay vì được tin tưởng một cách mù quáng.",
    "Interval for the background remote poller (add/re-enable also syncs immediately); <code>0</code> disables the poller while leaving manual sync available":
      "Khoảng thời gian cho bộ thăm dò từ xa chạy nền (thêm/bật lại nguồn cũng đồng bộ ngay); <code>0</code> tắt bộ thăm dò trong khi vẫn giữ đồng bộ thủ công",
    "Ceiling on a single scp-and-import run before it is aborted":
      "Giới hạn trên cho một lần chạy scp-và-nhập trước khi bị hủy",
    "Timeout for the <b>Test connection</b> probe":
      "Thời gian chờ cho phép thăm dò <b>Test connection</b>",
    "Freshness window (10 min) for a Remote Data Source session's live status — judged from the newest JSONL event timestamp in the mirror (mtime fallback); remote sessions receive no live hooks, so this replaces the local liveness and stale sweeps (which skip them)":
      "Cửa sổ mới (10 phút) cho trạng thái trực tiếp của một phiên Remote Data Source — đánh giá từ dấu thời gian sự kiện JSONL mới nhất trong bản sao (dự phòng mtime); các phiên từ xa không nhận hook trực tiếp, nên điều này thay thế các quét liveness và stale cục bộ (vốn bỏ qua chúng)",
    "Freshness window (10 min) for a Remote Data Source session's live status — judged from the newest JSONL event timestamp in the mirror (mtime fallback); healthy sources stay out of local liveness/stale checks, while errored or stale-syncing sources fall back to the global abandon sweep":
      "Cửa sổ mới (10 phút) cho trạng thái trực tiếp của một phiên Remote Data Source — đánh giá từ dấu thời gian sự kiện JSONL mới nhất trong bản sao (dự phòng mtime); nguồn khỏe mạnh không tham gia kiểm tra liveness/stale cục bộ, còn nguồn lỗi hoặc syncing quá lâu sẽ quay về quét bỏ rơi toàn cục",
    "Because a remote session is pulled over SSH, it never emits live hooks to this dashboard, so remote sessions (<code>source</code> ≠ <code>local</code>) are excluded from every local liveness and stale sweep — the process liveness reap, the watchdog transcript scan, the startup cleanup, and the periodic abandon sweep all skip them. Instead, a remote session's live status is reconciled from the mirrored transcript on every sync: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime). If the last event is within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) the session stays <code>active</code>; once the mirror stops advancing for longer than that window, the session is reconciled to <code>completed</code>. This keeps a still-running remote session from being wrongly shown as Completed.":
      "Vì một phiên từ xa được kéo qua SSH, nó không bao giờ phát hook trực tiếp đến bảng điều khiển này, nên các phiên từ xa (<code>source</code> ≠ <code>local</code>) bị loại khỏi mọi quét liveness và stale cục bộ — quá trình thu hồi liveness của tiến trình, quét transcript của watchdog, dọn dẹp khi khởi động, và quét bỏ rơi định kỳ đều bỏ qua chúng. Thay vào đó, trạng thái trực tiếp của một phiên từ xa được điều hòa từ transcript đã sao chép ở mỗi lần đồng bộ: hoạt động được đánh giá từ <b>dấu thời gian sự kiện mới nhất trong JSONL</b> (dự phòng mtime bản sao). Nếu sự kiện cuối nằm trong <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (mặc định 10 phút) thì phiên vẫn ở trạng thái <code>active</code>; một khi bản sao ngừng tiến triển lâu hơn cửa sổ đó, phiên được điều hòa thành <code>completed</code>. Điều này giúp một phiên từ xa vẫn đang chạy không bị hiển thị sai là Completed.",
    "Remote sessions never emit live hooks to this dashboard, so a healthy source remains exempt from the local process-liveness reap and watchdog transcript scan. Every successful sync still reconciles live status from the mirrored transcript: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime); within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) keeps the session <code>active</code>, otherwise it is reconciled to <code>completed</code>. If an SSH sync fails — or a source is stranded in <code>syncing</code> longer than <code>DASHBOARD_STALE_MINUTES</code> — and an active session is itself older than that same window, the ordinary stale-session sweep marks it <code>abandoned</code> and completes its agents. A later fresh mirror reactivates a session that is still writing, so unavailable remotes never leave permanent Waiting cards.":
      "Phiên từ xa không bao giờ gửi hook trực tiếp đến bảng điều khiển này, vì vậy một nguồn khỏe mạnh vẫn được loại khỏi quá trình thu hồi tiến trình cục bộ và quét transcript của watchdog. Mỗi lần đồng bộ thành công vẫn điều hòa trạng thái trực tiếp từ transcript đã sao chép: hoạt động được đánh giá từ <b>dấu thời gian sự kiện mới nhất trong JSONL</b> (dự phòng mtime bản sao); nằm trong <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (mặc định 10 phút) giữ phiên ở <code>active</code>, nếu không nó được điều hòa thành <code>completed</code>. Nếu đồng bộ SSH thất bại — hoặc một nguồn bị kẹt ở <code>syncing</code> lâu hơn <code>DASHBOARD_STALE_MINUTES</code> — và bản thân phiên active cũng cũ hơn cùng cửa sổ đó, lần quét phiên stale thông thường sẽ đánh dấu nó là <code>abandoned</code> và hoàn tất các agent. Một bản sao mới sau đó sẽ kích hoạt lại phiên vẫn đang ghi, do đó nguồn từ xa không khả dụng không bao giờ để lại thẻ Đang chờ vĩnh viễn.",
    "List configured sources with their status":
      "Liệt kê các nguồn đã cấu hình cùng trạng thái của chúng",
    "Add a source — SSH destination plus optional port, identity file, and remote home":
      "Thêm một nguồn — đích SSH cùng cổng, tệp danh tính và home từ xa tùy chọn",
    "Edit an existing source": "Chỉnh sửa một nguồn hiện có",
    "Remove a source; optionally purge the sessions it imported":
      "Gỡ bỏ một nguồn; tùy chọn xóa các phiên mà nó đã nhập",
    "Test SSH reachability without importing anything":
      "Kiểm tra khả năng kết nối SSH mà không nhập gì cả",
    "Trigger an immediate scp-and-import for that source":
      "Kích hoạt một lần scp-và-nhập ngay lập tức cho nguồn đó",
    "The <code>sources</code> query parameter is also accepted on <code>/api/sessions</code>, <code>/api/events</code>, <code>/api/agents</code>, <code>/api/stats</code>, and <code>/api/analytics</code> to scope any view to <code>local</code>, <code>all</code>, or a comma-separated list of source IDs.":
      "Tham số truy vấn <code>sources</code> cũng được chấp nhận trên <code>/api/sessions</code>, <code>/api/events</code>, <code>/api/agents</code>, <code>/api/stats</code>, và <code>/api/analytics</code> để giới hạn bất kỳ khung nhìn nào thành <code>local</code>, <code>all</code>, hoặc một danh sách ID nguồn phân tách bằng dấu phẩy.",
    "Sync progress and connection state broadcast on <code>/ws</code> as <code>remote_source.status</code> messages, so the Settings panel and the source badges update live as a remote connects, syncs, or fails.":
      "Tiến trình đồng bộ và trạng thái kết nối được phát trên <code>/ws</code> dưới dạng thông điệp <code>remote_source.status</code>, vì vậy bảng Settings và các huy hiệu nguồn cập nhật trực tiếp khi một máy từ xa kết nối, đồng bộ hoặc thất bại.",
    "The same operations are available from the terminal via <code>ccam remote-sources</code>, with subcommands <code>list</code>, <code>add</code>, <code>test &lt;id&gt;</code>, <code>sync [id]</code> (all sources when no id is given), and <code>rm &lt;id&gt; [--purge]</code> to remove a source and optionally drop the sessions it imported.":
      "Các thao tác tương tự cũng có sẵn từ terminal qua <code>ccam remote-sources</code>, với các lệnh con <code>list</code>, <code>add</code>, <code>test &lt;id&gt;</code>, <code>sync [id]</code> (tất cả nguồn khi không cung cấp id), và <code>rm &lt;id&gt; [--purge]</code> để gỡ bỏ một nguồn và tùy chọn xóa các phiên mà nó đã nhập.",
    "Local-only by default": "Mặc định chỉ truy cập cục bộ",
    "The image bakes <code>DASHBOARD_HOST=0.0.0.0</code> (the server binds loopback by default, but a container's loopback is a separate namespace the published port cannot reach) and <code>DASHBOARD_DATA_DIR=/app/data</code> (the read-only <code>~/.claude</code> mount cannot hold the SQLite data dir). The trust boundary is the host port publish: the examples use <code>-p 127.0.0.1:4820:4820</code>, so the dashboard is local-only. To expose it on a LAN, publish on <code>0.0.0.0</code> (<code>-p 4820:4820</code>) and set <code>DASHBOARD_TOKEN</code>.":
      "Image đã tích hợp sẵn <code>DASHBOARD_HOST=0.0.0.0</code> (server mặc định bind loopback, nhưng loopback của container là một namespace riêng mà cổng được publish không thể truy cập) và <code>DASHBOARD_DATA_DIR=/app/data</code> (mount chỉ-đọc <code>~/.claude</code> không thể chứa thư mục dữ liệu SQLite). Ranh giới tin cậy là việc publish cổng trên host: các ví dụ dùng <code>-p 127.0.0.1:4820:4820</code>, nên bảng điều khiển chỉ truy cập được cục bộ. Để expose ra mạng LAN, hãy publish trên <code>0.0.0.0</code> (<code>-p 4820:4820</code>) và đặt <code>DASHBOARD_TOKEN</code>.",
    "The CLI also manages the server itself: <code>ccam status</code> shows an up/down indicator and <code>ccam start</code> boots a detached production server. When the server is down, read-only commands fall back to reading <code>data/dashboard.db</code> directly under an explicit offline banner, while live or server-computed commands refuse with the specific reason.":
      "CLI c\u0169ng qu\u1ea3n l\u00fd ch\u00ednh server: <code>ccam status</code> hi\u1ec3n th\u1ecb ch\u1ec9 b\u00e1o \u0111ang ch\u1ea1y/d\u1eebng v\u00e0 <code>ccam start</code> kh\u1edfi \u0111\u1ed9ng server production t\u00e1ch r\u1eddi ch\u1ea1y n\u1ec1n. Khi server kh\u00f4ng ch\u1ea1y, c\u00e1c l\u1ec7nh ch\u1ec9-\u0111\u1ecdc t\u1ef1 chuy\u1ec3n sang \u0111\u1ecdc tr\u1ef1c ti\u1ebfp <code>data/dashboard.db</code> d\u01b0\u1edbi banner ngo\u1ea1i tuy\u1ebfn r\u00f5 r\u00e0ng, c\u00f2n c\u00e1c l\u1ec7nh tr\u1ef1c ti\u1ebfp ho\u1eb7c c\u1ea7n t\u00ednh to\u00e1n ph\u00eda server s\u1ebd t\u1eeb ch\u1ed1i k\u00e8m l\u00fd do c\u1ee5 th\u1ec3.",
    "The full dashboard feature surface is also available from any terminal through the dependency-free <code>ccam</code> CLI (<code>bin/ccam.js</code>). It is linked automatically by <code>npm run setup</code> (via a fail-soft <code>npm link</code>), discovers the running server through the same <code>~/.claude/.agent-dashboard.json</code> registry the hook handler uses (override with <code>CLAUDE_DASHBOARD_PORT</code> / <code>DASHBOARD_PORT</code>), and renders a full terminal UI — box-drawn tables, status icons, inline bar charts, and agent trees — that degrades to plain text when piped (<code>--no-color</code> / <code>NO_COLOR</code> / <code>FORCE_COLOR</code> respected), so it is equally at home in an interactive shell, <code>grep</code> pipelines, or CI.":
      "Toàn bộ bề mặt tính năng của dashboard cũng dùng được từ bất kỳ terminal nào thông qua CLI <code>ccam</code> không phụ thuộc thư viện ngoài (<code>bin/ccam.js</code>). Nó được liên kết tự động bởi <code>npm run setup</code> (qua <code>npm link</code> kiểu fail-soft), tìm server đang chạy qua chính sổ đăng ký <code>~/.claude/.agent-dashboard.json</code> mà hook handler sử dụng (ghi đè bằng <code>CLAUDE_DASHBOARD_PORT</code> / <code>DASHBOARD_PORT</code>), và hiển thị giao diện terminal đầy đủ — bảng kẻ khung, biểu tượng trạng thái, biểu đồ thanh inline và cây agent — tự hạ xuống văn bản thuần khi bị pipe (tôn trọng <code>--no-color</code> / <code>NO_COLOR</code> / <code>FORCE_COLOR</code>) — nên nó phù hợp như nhau với shell tương tác, pipeline <code>grep</code> hay CI.",
    "For a live monitoring session, <code>ccam repl</code> (aliases <code>shell</code> / <code>i</code>) opens an interactive shell where you type commands without the <code>ccam</code> prefix — with a CCAM welcome banner, tab-completion, persisted arrow-key history, a live server-status prompt (<code>●</code> up / <code>○</code> offline), a grouped <code>help</code> / <code>help &lt;cmd&gt;</code> menu, and a <code>watch [secs] &lt;cmd&gt;</code> built-in that auto-refreshes any command. Each entered line runs as an isolated child process, so an offline refusal or a blocking <code>tail</code> never takes the shell down.":
      "Cho một phiên theo dõi liên tục, <code>ccam repl</code> (bí danh <code>shell</code> / <code>i</code>) mở một shell tương tác nơi bạn gõ lệnh mà không cần tiền tố <code>ccam</code> — kèm biểu ngữ chào CCAM, tự hoàn thành bằng Tab, lịch sử phím mũi tên được lưu lại, một dấu nhắc trạng thái server trực tiếp (<code>●</code> bật / <code>○</code> tắt), menu <code>help</code> / <code>help &lt;cmd&gt;</code> phân nhóm, và một built-in <code>watch [giây] &lt;lệnh&gt;</code> tự làm mới bất kỳ lệnh nào. Mỗi dòng nhập vào chạy như một tiến trình con biệt lập, nên việc từ chối khi offline hay một <code>tail</code> đang chặn không bao giờ làm sập shell.",
    Group: "Nhóm",
    Commands: "Lệnh",
    "What you get": "Bạn nhận được gì",
    Monitoring: "Giám sát",
    "Reachability, totals and status distributions, the Kanban board rendered as text columns, and a live event feed that polls for new rows every 2 seconds":
      "Kiểm tra khả năng kết nối, tổng số và phân bố trạng thái, bảng Kanban hiển thị dạng cột văn bản, cùng luồng sự kiện trực tiếp poll dòng mới mỗi 2 giây",
    "Up/down indicator, a background server start that waits for health, and the interactive <code>ccam repl</code> shell — tab-completion, persisted history, a live server-status prompt, and a <code>watch</code> auto-refresh":
      "Chỉ báo bật/tắt, khởi động server chạy nền và chờ đến khi khỏe mạnh, và shell tương tác <code>ccam repl</code> — tự hoàn thành bằng Tab, lịch sử được lưu lại, dấu nhắc trạng thái server trực tiếp, và tự làm mới bằng <code>watch</code>",
    Data: "Dữ liệu",
    "Filterable tables plus a per-session deep dive with an indented agent tree, per-session cost, and the most recent events":
      "Các bảng có bộ lọc, cộng thêm góc nhìn sâu theo phiên: cây agent thụt lề, chi phí từng phiên và các sự kiện gần nhất",
    Insights: "Phân tích",
    "Token totals and top tools, workflow-intelligence stats with detected patterns, dynamic Workflow-tool runs, and the per-model cost breakdown, which can be scoped to a single session with <code>--session</code>, surfaces server-tool surcharges (web search / code execution), warns about models with usage but no matching pricing rule (priced at $0, excluded from the total), and points to <code>ccam pricing set</code>":
      "Tổng token và các công cụ dùng nhiều nhất, thống kê workflow-intelligence với các mẫu được phát hiện, các lần chạy Workflow động, và phân tích chi phí theo model — có thể giới hạn vào một phiên duy nhất bằng <code>--session</code>, hiển thị phụ phí công cụ máy chủ (tìm kiếm web / thực thi mã), cảnh báo khi một model có mức sử dụng nhưng không có quy tắc định giá khớp (tính giá $0, bị loại khỏi tổng) và chỉ tới <code>ccam pricing set</code>",
    "Alerts &amp; Webhooks": "Cảnh báo &amp; Webhook",
    "The fired-alert feed with acknowledge / acknowledge-all, the rule list, and synthetic test deliveries against any webhook target":
      "Luồng cảnh báo đã kích hoạt (xác nhận từng cái / tất cả), danh sách quy tắc, và gửi thử tổng hợp tới bất kỳ đích webhook nào",
    Pricing: "Giá",
    "Full pricing-rule CRUD with per-mtok rates — including 1-hour cache-write, fast-tier, and time-limited intro rates — straight from the terminal, with Fast In/Out and Intro In/Out columns in the list view":
      "CRUD đầy đủ cho quy tắc định giá theo mỗi triệu token — bao gồm ghi cache 1 giờ, mức fast và giá intro có thời hạn — ngay trong terminal, với các cột Fast In/Out và Intro In/Out trong chế độ danh sách",
    Import: "Nhập",
    "The same idempotent ingestion pipeline the Settings page uses, reporting imported / backfilled / skipped / error counts":
      "Cùng một đường ống nhập liệu bất biến mà trang Settings sử dụng, báo cáo số lượng imported / backfilled / skipped / error",
    Administration: "Quản trị",
    "Connectivity and hook diagnosis, raw system info, full JSON export, session cleanup, hook reinstallation, an update check that prints how far behind the canonical remote you are plus the copy-paste update command, and a guarded wipe that refuses to run without <code>--yes</code>":
      "Chẩn đoán kết nối và hook, thông tin hệ thống thô, xuất JSON đầy đủ, dọn dẹp phiên, cài lại hook, một lệnh kiểm tra cập nhật in ra số commit bạn đang tụt so với remote chuẩn kèm lệnh cập nhật sao chép-dán, và thao tác xóa được bảo vệ — từ chối chạy nếu thiếu <code>--yes</code>",
    "Safe by default": "An toàn theo mặc định",
    "Read commands only issue GETs; mutating commands map one-to-one to explicit dashboard actions; and the single destructive command, <code>clear-data</code>, always refuses to run without an explicit <code>--yes</code> flag. Exit codes are script-friendly: 0 on success, 1 on any failure. See <code>docs/CLI.md</code> for the complete reference.":
      "Các lệnh đọc chỉ gửi GET; các lệnh thay đổi tương ứng một-một với thao tác rõ ràng trên dashboard; và lệnh phá hủy duy nhất, <code>clear-data</code>, luôn từ chối chạy nếu không có cờ <code>--yes</code> tường minh. Mã thoát thân thiện với script: 0 khi thành công, 1 cho mọi thất bại. Xem <code>docs/CLI.md</code> để có tài liệu đầy đủ.",
    "The whole dashboard as terminal commands — <code>ccam stats</code>, <code>kanban</code>, <code>tail</code>, <code>session &lt;id&gt;</code>, <code>analytics</code>, <code>alerts</code>, <code>pricing</code>, <code>import</code>, <code>doctor</code>, <code>export</code> and more. Zero dependencies, linked by <code>npm run setup</code>, auto-discovers the running server, renders a full terminal UI (box-drawn tables, status icons, inline bar charts, agent trees), and pipes cleanly (colors off when not a TTY). The one destructive command is gated behind <code>--yes</code>.":
      "Toàn bộ dashboard dưới dạng lệnh terminal — <code>ccam stats</code>, <code>kanban</code>, <code>tail</code>, <code>session &lt;id&gt;</code>, <code>analytics</code>, <code>alerts</code>, <code>pricing</code>, <code>import</code>, <code>doctor</code>, <code>export</code> và hơn nữa. Không phụ thuộc thư viện, được liên kết bởi <code>npm run setup</code>, tự phát hiện server đang chạy, hiển thị giao diện terminal đầy đủ (bảng kẻ khung, biểu tượng trạng thái, biểu đồ thanh inline, cây agent), và pipe sạch sẽ (tắt màu khi không phải TTY). Lệnh phá hủy duy nhất được chặn sau <code>--yes</code>.",
    '<span class="caption-icon">📡</span> Live dashboard — real-time agent cards, stats, and activity feed':
      '<span class="caption-icon">📡</span> Bảng điều khiển trực tiếp — thẻ agent, số liệu thống kê và luồng hoạt động theo thời gian thực',
    "Claude Code Agent Monitor integrates with Claude Code through its native hook system. When Claude Code performs any action — tool use, session start, subagent orchestration, session end — it fires a hook that calls a small Node.js script bundled with this project. That script forwards the event over HTTP to the dashboard server, which stores it in SQLite and broadcasts it to the browser over WebSocket.":
      "Claude Code Agent Monitor tích hợp với Claude Code thông qua hệ thống hook gốc của nó. Khi Claude Code thực hiện bất kỳ hành động nào — sử dụng công cụ, bắt đầu phiên, điều phối subagent, kết thúc phiên — nó sẽ kích hoạt một hook gọi đến một script Node.js nhỏ đi kèm với dự án này. Script đó chuyển tiếp sự kiện qua HTTP đến máy chủ bảng điều khiển, nơi lưu sự kiện vào SQLite và phát quảng bá nó đến trình duyệt qua WebSocket.",
    "End-to-end data pipeline from Claude Code to the browser":
      "Đường ống dữ liệu đầu-cuối từ Claude Code đến trình duyệt",
    "Local-first by design": "Ưu tiên cục bộ theo thiết kế",
    "The server binds <code>127.0.0.1</code> (loopback) by default, so it is not network-reachable and everything runs on your machine. No data leaves your system. No API keys. No external services. Exposing it more widely is opt-in via <code>DASHBOARD_HOST</code> and should be paired with <code>DASHBOARD_TOKEN</code>.":
      "Máy chủ mặc định liên kết với <code>127.0.0.1</code> (loopback), nên nó không thể truy cập qua mạng và mọi thứ đều chạy trên máy của bạn. Không có dữ liệu nào rời khỏi hệ thống của bạn. Không cần API key. Không có dịch vụ bên ngoài. Việc để lộ rộng hơn là tùy chọn bật qua <code>DASHBOARD_HOST</code> và nên đi kèm với <code>DASHBOARD_TOKEN</code>.",
    "Every feature is driven by real hook events — nothing is hardcoded or simulated in production mode.":
      "Mọi tính năng đều được điều khiển bởi các sự kiện hook thực — không có gì bị hardcode hoặc mô phỏng ở chế độ production.",
    "Two tabs: <strong>Monitor</strong> shows overview stats, active agent cards with collapsible subagent hierarchy, and a recent activity feed whose item count fills available viewport height. <strong>Health</strong> renders a composite system health score ring, storage engine donut chart, cache/error/success gauges, tool invocation bars, subagent effectiveness ratios, model token distribution, and compaction stats. Both tabs auto-refresh every 5 seconds via WebSocket push so the view is always current without manual reload.":
      "Hai tab: <strong>Monitor</strong> hiển thị số liệu tổng quan, các thẻ agent đang hoạt động với cây phân cấp subagent có thể thu gọn, và một luồng hoạt động gần đây có số lượng mục lấp đầy chiều cao viewport khả dụng. <strong>Health</strong> hiển thị một vòng điểm sức khỏe hệ thống tổng hợp, biểu đồ vành khuyên về storage engine, các đồng hồ đo cache/lỗi/thành công, các thanh lời gọi công cụ, tỷ lệ hiệu quả của subagent, phân bố token theo mô hình, và số liệu thống kê nén. Cả hai tab tự động làm mới mỗi 5 giây qua WebSocket push nên chế độ xem luôn cập nhật mà không cần tải lại thủ công.",
    "Toggle between <strong>Agents</strong> (Working / Waiting / Completed / Error) and <strong>Sessions</strong> (Active / Waiting / Completed / Error / Abandoned) swim lanes. A yellow <strong>Waiting</strong> column flags items sitting on the user — fresh prompt, between turns, or permission gate. Hover any column header for lifecycle tooltips explaining each state transition. Cards surface model name, cumulative cost, and the current tool being called. Counts update in real time via WebSocket so the board is always in sync with the live event store. Hover a Waiting badge to see why — needs input, turn done, at prompt, or interrupted.":
      "Chuyển đổi giữa các làn <strong>Agents</strong> (Working / Waiting / Completed / Error) và <strong>Sessions</strong> (Active / Waiting / Completed / Error / Abandoned). Một cột <strong>Waiting</strong> màu vàng đánh dấu các mục đang chờ người dùng — lời nhắc mới, giữa các lượt, hoặc cổng cấp quyền. Di chuột qua bất kỳ tiêu đề cột nào để xem các chú giải về vòng đời giải thích từng lần chuyển trạng thái. Các thẻ hiển thị tên mô hình, chi phí tích lũy, và công cụ đang được gọi hiện tại. Số đếm cập nhật theo thời gian thực qua WebSocket nên bảng luôn đồng bộ với kho sự kiện trực tiếp. Di chuột lên huy hiệu Waiting để xem lý do — cần phản hồi, xong lượt, chờ prompt hoặc bị ngắt.",
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, and estimated cost. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "Bảng <strong>phân trang phía máy chủ</strong> của mọi phiên đã ghi — mỗi trang chỉ lấy phần dữ liệu của nó nên việc tính chi phí luôn bị giới hạn bất kể có bao nhiêu phiên tồn tại. Tìm kiếm không phân biệt hoa thường trên <code>id</code>, <code>name</code> và <code>cwd</code> chạy ở phía máy chủ với độ trễ chống dội 300 ms; bộ lọc trạng thái kết hợp với tìm kiếm để thu hẹp chính xác. Mỗi hàng hiển thị tên thật của phiên (đồng bộ trực tiếp từ bản ghi — tiêu đề <code>/rename</code> hoặc <code>claude -n</code>, nếu không thì dùng tiêu đề tự động, nếu vẫn không có thì prompt đầu tiên của người dùng, với dự phòng là ID ngắn), huy hiệu trạng thái, số lượng agent, thời lượng, mô hình và chi phí ước tính. Nhấp vào bất kỳ hàng nào để đi sâu vào chế độ xem chi tiết đầy đủ của phiên với bản ghi hội thoại và phân cấp agent.",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "Phân tích sâu theo từng phiên với một cây phân cấp agent có thể thu gọn và một dòng thời gian sự kiện đầy đủ theo trình tự thời gian, hiển thị tên và bản tóm tắt của mỗi lần gọi công cụ. Một bảng tổng quan ở trên cùng hiển thị các bộ đếm dạng ô cho sự kiện, lời gọi công cụ, subagent, lần nén, lỗi và thời lượng. Các thanh sử dụng công cụ hàng đầu và bảng phân tích theo loại subagent cho phép đọc nhanh phân bố. Trình xem hội thoại hiển thị markdown với tô sáng cú pháp, các khối có kiểu dáng riêng theo từng công cụ, pill lệnh gạch chéo kèm output TUI đã ghi lại, và chỉ báo đổi tên phiên nội tuyến. Tin nhắn gõ giữa lượt (xếp hàng khi Claude còn đang làm việc) hiển thị đúng vị trí Claude thực sự nhận được, và thông báo từ harness được gán cho System. Xuất toàn bộ phiên dưới dạng JSON hoặc chia sẻ liên kết cố định để xem xét bất đồng bộ. Khi phiên đang chờ bạn, một banner màu vàng dưới tiêu đề sẽ nêu rõ lý do và thời gian đã chờ.",
    "A rules-based alerting engine evaluates the live event stream server-side: <strong>event pattern</strong> (match event type / tool / summary text, optionally N matches within a time window), <strong>inactivity</strong>, <strong>stuck agent</strong>, and <strong>token threshold</strong> — each with per-(rule, session, agent) cooldown dedup. Fired alerts surface in a live feed and fan out to <strong>14 first-class webhook providers</strong> — Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream — plus any generic JSON endpoint (with optional HMAC-SHA256 signing and custom headers). Delivery is detached and fail-safe with a request timeout, bounded retry/backoff, secret redaction, a one-click test probe, and a per-target delivery log. Rules and channels are managed together in <strong>Settings → Alerts</strong>.":
      "Một engine cảnh báo dựa trên quy tắc đánh giá luồng sự kiện trực tiếp ở phía máy chủ: <strong>mẫu sự kiện</strong> (khớp loại sự kiện / công cụ / văn bản tóm tắt, tùy chọn N lần khớp trong một khoảng thời gian), <strong>không hoạt động</strong>, <strong>agent bị kẹt</strong>, và <strong>ngưỡng token</strong> — mỗi loại đều có khử trùng lặp thời gian chờ theo từng (quy tắc, phiên, agent). Các cảnh báo được kích hoạt xuất hiện trong một luồng trực tiếp và lan tỏa đến <strong>14 nhà cung cấp webhook hạng nhất</strong> — Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream — cùng với bất kỳ endpoint JSON chung nào (với tùy chọn ký HMAC-SHA256 và header tùy chỉnh). Việc gửi là tách rời và an toàn khi lỗi với thời gian chờ yêu cầu, thử lại/backoff có giới hạn, che giấu bí mật, một đầu dò kiểm thử bằng một cú nhấp, và nhật ký gửi theo từng đích. Quy tắc và kênh được quản lý cùng nhau trong <strong>Settings → Alerts</strong>.",
    "A native desktop app — a macOS <code>.app</code> (shipped as a <code>.dmg</code>) and a Windows <code>.exe</code> (NSIS installer plus a no-install portable build) — built with Electron 35. It <strong>embeds the Express server in-process</strong> — <code>require()</code>-ing <code>server/index.js</code> directly, with no child process and no IPC — and renders the built React client in a <code>BrowserWindow</code>. Adds a menu-bar / notification-area (tray) icon, a native application menu, auto-start at login (macOS Login Items via <code>SMAppService</code>; Windows per-user <code>HKCU\\…\\Run</code>), and a single-instance lock. Closing the window hides it while the server keeps running, and the app auto-installs Claude Code hooks on first boot so an install-only user gets events flowing without a checkout.":
      "Một ứng dụng desktop gốc — một <code>.app</code> trên macOS (phát hành dưới dạng <code>.dmg</code>) và một <code>.exe</code> trên Windows (trình cài đặt NSIS cộng với một bản portable không cần cài đặt) — được xây dựng bằng Electron 35. Nó <strong>nhúng máy chủ Express trong cùng tiến trình</strong> — <code>require()</code> trực tiếp <code>server/index.js</code>, không có tiến trình con và không có IPC — và hiển thị client React đã build trong một <code>BrowserWindow</code>. Bổ sung một biểu tượng thanh menu / khu vực thông báo (khay), một menu ứng dụng gốc, tự khởi động khi đăng nhập (macOS dùng Login Items qua <code>SMAppService</code>; Windows dùng <code>HKCU\\…\\Run</code> theo từng người dùng), và một khóa đơn-thực-thể. Đóng cửa sổ sẽ ẩn nó trong khi máy chủ vẫn chạy, và ứng dụng tự động cài đặt các hook của Claude Code ngay ở lần khởi động đầu tiên nên người dùng chỉ cài đặt cũng có sự kiện chảy về mà không cần checkout mã nguồn.",
    "Real-time streaming event log showing tool calls, agent state changes, errors, and compaction events as they arrive. Pause/resume with automatic buffering, paginated history for scrollback, and auto-scrolling to the latest entry. Click any row to expand its full hook payload inline. A dedicated <strong>Session →</strong> button navigates directly to session detail without collapsing the expanded state. Every entry carries the same status badge the Dashboard and Session Detail show for that event — one shared mapping that understands both Claude and Codex event types — and rows are grouped by session for quick scanning of concurrent work.":
      "Nhật ký sự kiện phát trực tiếp theo thời gian thực hiển thị các lời gọi công cụ, thay đổi trạng thái agent, lỗi và sự kiện nén ngay khi chúng đến. Tạm dừng/tiếp tục với bộ đệm tự động, lịch sử phân trang để cuộn lại, và tự động cuộn đến mục mới nhất. Nhấp vào bất kỳ hàng nào để mở rộng toàn bộ payload hook của nó ngay tại chỗ. Một nút <strong>Session →</strong> chuyên dụng điều hướng trực tiếp đến chi tiết phiên mà không thu gọn trạng thái đã mở rộng. Mọi mục đều mang đúng huy hiệu trạng thái mà Dashboard và chi tiết phiên hiển thị cho sự kiện đó — một ánh xạ dùng chung hiểu cả loại sự kiện Claude lẫn Codex — và được nhóm theo phiên để quét nhanh các công việc đồng thời.",
    "Token usage breakdown by model with stacked bar charts, tool frequency rankings, agent type distribution donuts, and session outcome pie charts. A 52-week activity heatmap aligned by day-of-week shows density with hover tooltips. 30-day sparkline trends track cost and session volume at a glance. The cost summary panel totals input, output, and cache spend across all models. A live/offline indicator and auto-refresh via WebSocket keep everything current. All charts are responsive and adapt to mobile viewports.":
      "Phân tích mức sử dụng token theo mô hình với biểu đồ thanh xếp chồng, bảng xếp hạng tần suất công cụ, biểu đồ vành khuyên phân bố loại agent, và biểu đồ tròn kết quả phiên. Một bản đồ nhiệt hoạt động 52 tuần căn theo ngày trong tuần hiển thị mật độ với chú giải khi di chuột. Các đường xu hướng sparkline 30 ngày theo dõi chi phí và khối lượng phiên trong nháy mắt. Bảng tóm tắt chi phí cộng tổng chi phí đầu vào, đầu ra và cache trên tất cả các mô hình. Một chỉ báo trực tuyến/ngoại tuyến và tự động làm mới qua WebSocket giữ mọi thứ luôn cập nhật. Tất cả biểu đồ đều đáp ứng và thích ứng với viewport di động.",
    "Every UI update is pushed over a persistent WebSocket with sub-5 ms dispatch latency — zero polling anywhere. If the connection drops, automatic 2-second reconnect kicks in while a ping/pong heartbeat detects stale connections early. A sidebar indicator turns green/red so you always know whether you're live. The WebSocket carries typed JSON envelopes for new events, session updates, agent transitions, compaction results, and import progress — all parsed into the same eventBus the REST layer uses.":
      "Mọi cập nhật UI đều được đẩy qua một WebSocket bền vững với độ trễ điều phối dưới 5 ms — không hề có polling ở bất kỳ đâu. Nếu kết nối rớt, cơ chế tự động kết nối lại sau 2 giây sẽ kích hoạt trong khi nhịp tim ping/pong phát hiện sớm các kết nối cũ. Một chỉ báo ở thanh bên chuyển xanh/đỏ để bạn luôn biết mình có đang trực tuyến hay không. WebSocket mang các bao JSON có kiểu cho sự kiện mới, cập nhật phiên, chuyển trạng thái agent, kết quả nén và tiến độ nhập — tất cả đều được phân tích vào cùng một eventBus mà tầng REST sử dụng.",
    "Standalone CLI statusline for Claude Code that prints model name, user, working directory, git branch, and a color-coded context-window bar (green → yellow → red). Token counts show input (green ↑), output (cyan ↓), and cache (dim) separately. Session cost in USD shifts color by configurable thresholds. ANSI-colored output updates on every turn. Python-based with a thin shell wrapper — drop it into your prompt or tmux status line. Works with any terminal emulator that supports 256-color ANSI.":
      "Thanh trạng thái CLI độc lập cho Claude Code, in ra tên model, người dùng, thư mục làm việc, git branch và một thanh cửa sổ ngữ cảnh được mã màu (green → yellow → red). Số lượng token hiển thị riêng phần đầu vào (green ↑), đầu ra (cyan ↓) và cache (dim). Chi phí phiên tính bằng USD đổi màu theo các ngưỡng có thể cấu hình. Đầu ra tô màu ANSI cập nhật theo từng lượt. Dựa trên Python với một lớp bọc shell mỏng — chỉ cần thả nó vào prompt hoặc thanh trạng thái tmux của bạn. Hoạt động với bất kỳ trình giả lập terminal nào hỗ trợ ANSI 256 màu.",
    "Import existing Claude Code or Codex sessions from three sources — rescan the selected default <code>~/.claude/projects</code> or <code>~/.codex/sessions</code> folder, scan any absolute path on disk, or drag-drop <code>.jsonl</code>, <code>.zip</code>, <code>.tar.gz</code>, and <code>.gz</code> archives through <b>Settings → Import History</b>. All paths use each provider's live ingestion pipeline, so imported tokens, per-model cost, and tool data match real-time capture exactly. Codex uploads are snapshotted before temporary files are cleaned up, and re-imports are idempotent.":
      "Nhập các phiên Claude Code hoặc Codex hiện có từ ba nguồn — quét lại thư mục mặc định đã chọn <code>~/.claude/projects</code> hoặc <code>~/.codex/sessions</code>, quét bất kỳ đường dẫn tuyệt đối nào trên đĩa, hoặc kéo-thả các tệp lưu trữ <code>.jsonl</code>, <code>.zip</code>, <code>.tar.gz</code> và <code>.gz</code> qua <b>Settings → Import History</b>. Mọi đường dẫn đều dùng pipeline ingest trực tiếp của nhà cung cấp tương ứng, nên token, chi phí theo model và dữ liệu tool đã nhập khớp chính xác với dữ liệu thời gian thực. Tệp Codex tải lên được snapshot trước khi tệp tạm bị dọn dẹp và việc nhập lại vẫn bất biến.",
    "The startup auto-import of <code>~/.claude/projects</code> is one-time and marker-gated, so a project folder created after first launch — whose sessions never flow through hooks (for example with host-only hooks disabled) — would stay invisible until a manual rescan. A background sync closes that gap with three triggers sharing one mtime cache and a single coalesced sweep: an immediate sweep at startup, a debounced <code>fs.watch</code> (recursive on macOS and Windows; root plus immediate child folders on Linux to avoid the userland recursive-watcher hazard) that fires the moment a new session file or project folder appears, and a periodic safety-net poll tunable via <code>DASHBOARD_SESSION_SYNC_MS</code> (default <code>30000</code> ms; <code>0</code> disables the poll while leaving the watcher running). Each sweep re-parses only files whose mtime advanced and broadcasts <code>session_created</code> / <code>session_updated</code> (plus the main agent) so the UI refreshes live, while an already-imported unchanged session is skipped without re-parsing.":
      "Việc tự động nhập <code>~/.claude/projects</code> lúc khởi động chỉ chạy một lần và được kiểm soát bằng marker, nên một thư mục dự án được tạo sau lần khởi chạy đầu tiên — mà các phiên của nó không bao giờ chảy qua hook (ví dụ khi đã tắt hook chỉ-dành-cho-máy-chủ) — sẽ không hiển thị cho tới khi quét lại thủ công. Một tiến trình đồng bộ nền lấp khoảng trống đó bằng ba trình kích hoạt cùng dùng chung một bộ nhớ đệm mtime và một lượt quét gộp duy nhất: một lượt quét tức thì lúc khởi động, một <code>fs.watch</code> có khử dội (đệ quy trên macOS và Windows; theo dõi thư mục gốc cộng từng thư mục con trực tiếp trên Linux để tránh hiểm họa của trình theo dõi đệ quy ở tầng người dùng) kích hoạt ngay khoảnh khắc một tệp phiên hoặc thư mục dự án mới xuất hiện, và một lượt thăm dò an toàn định kỳ có thể điều chỉnh qua <code>DASHBOARD_SESSION_SYNC_MS</code> (mặc định <code>30000</code> ms; <code>0</code> tắt thăm dò nhưng vẫn giữ trình theo dõi chạy). Mỗi lượt quét chỉ phân tích lại những tệp có mtime tiến lên và phát đi <code>session_created</code> / <code>session_updated</code> (cùng với agent chính) để UI làm mới trực tiếp, trong khi một phiên đã nhập và không thay đổi thì được bỏ qua mà không phân tích lại.",
    "Incremental JSONL reader shared across the hook handler, compaction scanner, conversation viewer, and import pipeline. Byte-offset tracking skips already-parsed content; cache hits short-circuit disk I/O so even sessions with tens of thousands of turns stay fast. It also extracts the live session title (<code>custom-title</code> / <code>ai-title</code>) so renames surface in real time, plus the first user prompt as a fallback descriptor for placeholder-named sessions and agents.":
      "Trình đọc JSONL tăng dần được chia sẻ giữa trình xử lý hook, trình quét nén, trình xem hội thoại và đường ống nhập. Việc theo dõi byte-offset bỏ qua nội dung đã phân tích; các lần trúng cache rút ngắn I/O đĩa nên ngay cả những phiên có hàng chục nghìn lượt vẫn chạy nhanh. Nó cũng trích xuất tiêu đề phiên trực tiếp (<code>custom-title</code> / <code>ai-title</code>) để việc đổi tên hiển thị theo thời gian thực, cùng prompt đầu tiên của người dùng làm mô tả dự phòng cho các phiên và agent còn mang tên placeholder.",
    "LRU eviction of cold session buffers plus a tail-cap on per-entry growable arrays (turn durations, API errors, compaction entries). A session that runs for days cannot grow a single cache entry without bound, and each entry stores its parsed result only once — no shadow copy.":
      "Loại bỏ theo LRU các bộ đệm phiên nguội cùng với một giới hạn đuôi trên các mảng có thể tăng trưởng theo từng mục (thời lượng lượt, lỗi API, mục nén). Một phiên chạy nhiều ngày cũng không thể làm một mục cache đơn lẻ tăng không giới hạn, và mỗi mục chỉ lưu kết quả đã phân tích của nó một lần — không có bản sao bóng.",
    "The periodic compaction sweep reads each active session's transcript path directly from <code>sessions.transcript_path</code> (a partial index covers exactly those rows), so the work is O(active sessions) instead of a <code>json_extract</code> scan over the whole events table.":
      "Lượt quét nén định kỳ đọc đường dẫn transcript của mỗi phiên đang hoạt động trực tiếp từ <code>sessions.transcript_path</code> (một chỉ mục bộ phận phủ đúng các hàng đó), nên khối lượng công việc là O(số phiên đang hoạt động) thay vì một lần quét <code>json_extract</code> trên toàn bộ bảng sự kiện.",
    "Collapsible parent–child agent tree rendered on both Dashboard and Session Detail. Agents with subagents display expand/collapse chevrons; leaf agents show a dot indicator. The tree auto-expands when any child transitions to active and correctly tracks backgrounded subagents without premature completion. Depth is unlimited — deeply nested chains render as indented rows with connecting lines. Each node shows model, current tool, status badge, and cumulative token cost for tracing spend down the spawn chain.":
      "Cây agent cha–con có thể thu gọn, được hiển thị trên cả Dashboard lẫn Session Detail. Các agent có subagent hiển thị mũi tên mở rộng/thu gọn; agent lá hiển thị một dấu chấm. Cây tự động mở rộng khi bất kỳ con nào chuyển sang hoạt động và theo dõi chính xác các subagent chạy nền mà không đánh dấu hoàn tất quá sớm. Độ sâu không giới hạn — các chuỗi lồng sâu được hiển thị thành những hàng thụt vào với đường nối. Mỗi nút hiển thị model, công cụ hiện tại, huy hiệu trạng thái và chi phí token tích lũy để truy vết mức chi xuống theo chuỗi sinh.",
    "Per-model cost estimation with configurable pricing rules — set input, output, and cache-read rates per model variant through the Settings UI. View total and per-session breakdowns on Sessions, Session Detail, and Analytics. Compaction- aware token accounting preserves baselines across context compressions so no usage is silently dropped. Cost chips appear on Kanban cards, session rows, and the sidebar summary. Subagent cards show each subagent's own cost, computed from that subagent's transcript token usage priced at current rates, so a subagent card no longer misleadingly reads as if it cost the whole session; main-agent cards still show the session total. Pricing changes retroactively recalculate all stored sessions, and imports apply the same rate table.":
      "Ước tính chi phí theo từng model với các quy tắc định giá có thể cấu hình — đặt mức phí cho đầu vào, đầu ra và đọc cache cho mỗi biến thể model qua Settings UI. Xem tổng chi phí và phân tích theo từng phiên trên Sessions, Session Detail và Analytics. Việc tính token có nhận biết nén giữ lại các mốc cơ sở qua các lần nén ngữ cảnh nên không có lượng dùng nào bị âm thầm bỏ qua. Các nhãn chi phí xuất hiện trên thẻ Kanban, hàng phiên và phần tóm tắt ở thanh bên. Thẻ subagent hiển thị chi phí của riêng từng subagent, được tính từ lượng dùng token trong transcript của subagent đó theo mức giá hiện hành, nên thẻ subagent không còn hiểu nhầm như thể nó tốn chi phí của cả phiên; thẻ agent chính vẫn hiển thị tổng của phiên. Thay đổi định giá sẽ tính lại hồi tố tất cả các phiên đã lưu, và việc nhập cũng áp dụng cùng bảng giá đó.",
    "Model pricing editor with per-token rate configuration for every Claude variant. Hook installation status with one-click reinstall and per-hook health checks. Full JSON data export covering sessions, agents, events, tokens, and pricing rules. Session cleanup controls to abandon stale sessions or purge old data by age. Browser notification preferences with per-event toggles. A system information panel shows database row counts, file sizes, server uptime, and WebSocket connection status at a glance.":
      "Trình chỉnh sửa định giá model với cấu hình mức phí theo từng token cho mọi biến thể Claude. Trạng thái cài đặt hook với việc cài lại bằng một cú nhấp và kiểm tra sức khỏe theo từng hook. Xuất dữ liệu JSON đầy đủ bao gồm phiên, agent, sự kiện, token và quy tắc định giá. Các điều khiển dọn dẹp phiên để bỏ các phiên cũ hoặc xóa dữ liệu cũ theo độ tuổi. Tùy chọn thông báo trình duyệt với công tắc theo từng sự kiện. Một bảng thông tin hệ thống hiển thị nhanh số hàng cơ sở dữ liệu, kích thước tệp, thời gian hoạt động của máy chủ và trạng thái kết nối WebSocket.",
    "Local MCP sidecar with three transport modes — stdio for Claude Code native integration, HTTP+SSE for remote clients, and an interactive REPL for ad-hoc terminal queries. Exposes 25 typed tools across 6 domains: sessions, agents, events, analytics, settings, and system health. Every mutation is gated behind a tiered policy so nothing dangerous fires without opt-in. Retry-aware API access handles transient failures. Runs as a standalone Node process with no Docker or cloud dependency.":
      "MCP sidecar cục bộ với ba chế độ truyền tải — stdio để tích hợp gốc với Claude Code, HTTP+SSE cho các client từ xa, và một REPL tương tác cho các truy vấn terminal tạm thời. Phơi bày 25 công cụ có kiểu trên 6 lĩnh vực: phiên, agent, sự kiện, phân tích, cài đặt và sức khỏe hệ thống. Mọi thao tác thay đổi đều được kiểm soát qua một chính sách phân tầng nên không có hành động nguy hiểm nào kích hoạt mà không có sự đồng ý trước. Truy cập API có nhận biết thử lại xử lý các lỗi nhất thời. Chạy như một tiến trình Node độc lập, không phụ thuộc Docker hay đám mây.",
    "Instruction, skills, rules, and custom-agent layers for both Claude Code and Codex. Path-scoped rules target backend, frontend, MCP, and docs directories with context-appropriate guidelines. Reusable skills cover onboarding, feature shipping, live-issue debugging, release-readiness, and MCP operations. Specialized subagents for backend, frontend, and MCP code review run in parallel with focused tooling. Everything lives in <code>.claude/</code> and is version-controlled alongside the codebase.":
      "Các lớp chỉ dẫn, kỹ năng, quy tắc và agent tùy chỉnh cho cả Claude Code lẫn Codex. Các quy tắc giới hạn theo đường dẫn nhắm vào các thư mục backend, frontend, MCP và docs với những hướng dẫn phù hợp ngữ cảnh. Các kỹ năng tái sử dụng bao gồm khâu nhập môn, phát hành tính năng, gỡ lỗi sự cố trực tiếp, kiểm tra sẵn sàng phát hành và vận hành MCP. Các subagent chuyên biệt cho việc review mã backend, frontend và MCP chạy song song với bộ công cụ tập trung. Mọi thứ nằm trong <code>.claude/</code> và được quản lý phiên bản cùng với mã nguồn.",
    "D3.js-powered visualizations: an agent orchestration DAG showing spawn patterns across sessions, a tool-execution Sankey diagram mapping tool-to- tool transitions, and a directed pipeline graph with frequency labels. Every chart title carries an info icon that opens a popover explaining what it shows and how to read it. Hovering nodes, edges, and bars surfaces tooltips with share-of-source percentages, success-rate buckets, and timing patterns. All labels are translated to English, Vietnamese, and Chinese.":
      "Các trực quan hóa do D3.js cung cấp: một DAG điều phối agent thể hiện các mẫu sinh qua các phiên, một biểu đồ Sankey thực thi công cụ ánh xạ các chuyển tiếp công cụ-sang-công cụ, và một đồ thị pipeline có hướng với nhãn tần suất. Mỗi tiêu đề biểu đồ đều có một biểu tượng thông tin, khi nhấp sẽ mở một popover giải thích nó thể hiện điều gì và cách đọc. Việc rê chuột lên các nút, cạnh và thanh sẽ hiện ra tooltip với tỷ lệ phần trăm theo nguồn, các nhóm tỷ lệ thành công và các mẫu thời gian. Tất cả nhãn đều được dịch sang tiếng Anh, tiếng Việt và tiếng Trung.",
    "Subagent effectiveness scorecards with success-rate rings and day-of-week sparklines. Auto-detected workflow patterns expand on click into a detail panel with the full step chain, stats grid, and a narrative with loop detection. Model delegation flow, error propagation bars with API error cards, concurrency swim-lanes, and complexity bubble charts round out the view. Six headline stat cards each include an info popover explaining the metric and its current value. Status filter applies globally.":
      "Bảng điểm hiệu quả của subagent với các vòng tỷ lệ thành công và biểu đồ tia theo ngày trong tuần. Các mẫu quy trình được tự động phát hiện sẽ mở rộng khi nhấp thành một bảng chi tiết với toàn bộ chuỗi bước, lưới thống kê và một phần tường thuật có phát hiện vòng lặp. Luồng ủy quyền model, các thanh lan truyền lỗi kèm thẻ lỗi API, các làn bơi đồng thời và các biểu đồ bong bóng độ phức tạp hoàn thiện khung nhìn. Sáu thẻ thống kê nổi bật, mỗi thẻ kèm một popover thông tin giải thích chỉ số và giá trị hiện tại của nó. Bộ lọc trạng thái áp dụng trên toàn cục.",
    "Searchable session selector with pagination to explore any session's agent tree, tool-call timeline, and event sequence. The detail page opens with a live-updating overview — tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and subagent breakdown give quick reads. The conversation viewer renders markdown with syntax highlighting. Cross-filter from DAG nodes, run compaction analysis, or export as JSON — all with real-time WebSocket auto-refresh.":
      "Bộ chọn phiên có thể tìm kiếm kèm phân trang để khám phá cây agent, dòng thời gian gọi công cụ và chuỗi sự kiện của bất kỳ phiên nào. Trang chi tiết mở ra với một phần tổng quan cập nhật trực tiếp — các bộ đếm dạng ô cho sự kiện, lần gọi công cụ, subagent, lần nén, lỗi và thời lượng. Các thanh sử dụng công cụ hàng đầu và phần phân tích subagent giúp xem nhanh. Trình xem hội thoại hiển thị markdown kèm tô sáng cú pháp. Lọc chéo từ các nút DAG, chạy phân tích nén, hoặc xuất ra JSON — tất cả đều có tự động làm mới WebSocket theo thời gian thực.",
    "Persistent browser notifications via Web Push (VAPID) for real-time alerts even when the tab is not focused or the browser is backgrounded. Includes macOS audio support so notifications are audible alongside system sounds. Per-event toggles let you choose which events fire — session starts, completions, errors, compactions, or agent spawns. Server-side subscription management ensures one push per event per browser. Works on Chrome, Edge, Firefox, and Safari 17+ with graceful degradation elsewhere.":
      "Thông báo trình duyệt bền vững qua Web Push (VAPID) cho các cảnh báo thời gian thực ngay cả khi tab không được lấy nét hoặc trình duyệt đang chạy nền. Bao gồm hỗ trợ âm thanh trên macOS để thông báo phát ra cùng với âm thanh hệ thống. Các công tắc theo từng sự kiện cho phép bạn chọn sự kiện nào sẽ kích hoạt — phiên bắt đầu, hoàn tất, lỗi, lần nén hoặc sinh agent. Việc quản lý đăng ký phía máy chủ đảm bảo mỗi sự kiện chỉ đẩy một lần cho mỗi trình duyệt. Hoạt động trên Chrome, Edge, Firefox và Safari 17+ với cơ chế suy giảm nhẹ nhàng ở những nơi khác.",
    "Ready-to-use Dockerfile and docker-compose.yml for one-command deployment. Supports both Docker and Podman with persistent volume mounts for the SQLite database and hook data. Configurable port mapping via environment variables and a health-check endpoint the container runtime can poll. Multi-stage build keeps the image lean — only production deps and the compiled bundle ship. Run <code>docker compose up -d --build</code> and the dashboard is live with zero additional setup or configuration required.":
      "Dockerfile và docker-compose.yml sẵn dùng để triển khai bằng một lệnh. Hỗ trợ cả Docker lẫn Podman với các volume mount bền vững cho cơ sở dữ liệu SQLite và dữ liệu hook. Ánh xạ cổng có thể cấu hình qua biến môi trường và một endpoint kiểm tra sức khỏe mà runtime container có thể thăm dò. Quá trình build nhiều giai đoạn giữ cho image gọn nhẹ — chỉ các phụ thuộc production và gói đã biên dịch được đưa vào. Chạy <code>docker compose up -d --build</code> và dashboard sẽ hoạt động ngay mà không cần bất kỳ thiết lập hay cấu hình bổ sung nào.",
    "Official Claude Code plugin marketplace shipping 10 plugins with 53 skills, 14 agents, 30 slash commands, 3 CLI tools, 3 hook configs, and 1 MCP server. Deep analytics with compaction-aware baselines, productivity automation, developer diagnostics, AI-powered workflow intelligence, and dashboard MCP integration. Five newer plugins go further: <code>ccam-cost-guard</code> (budget guardrails, spend forecasts, and cost-threshold alerts), <code>ccam-sessions</code> (session forensics — search, timeline, and transcript replay), <code>ccam-workflows</code> (multi-agent orchestration and fleet intelligence), <code>ccam-quality</code> (reliability and SLO checks), and <code>ccam-config</code> (Claude Code config and memory governance). Install with <code>claude plugin install</code> — no restart needed. Each listing shows author, license, homepage, and per-skill contribution breakdown. The Config Explorer's Plugins tab surfaces installed plugins with live status.":
      "Chợ plugin chính thức của Claude Code phát hành 10 plugin với 53 kỹ năng, 14 agent, 30 lệnh gạch chéo, 3 công cụ CLI, 3 cấu hình hook và 1 máy chủ MCP. Phân tích chuyên sâu với các mốc cơ sở có nhận biết nén, tự động hóa năng suất, chẩn đoán cho lập trình viên, trí tuệ quy trình được hỗ trợ bởi AI, và tích hợp MCP của dashboard. Năm plugin mới hơn còn đi xa hơn nữa: <code>ccam-cost-guard</code> (lan can ngân sách, dự báo chi tiêu và cảnh báo ngưỡng chi phí), <code>ccam-sessions</code> (pháp y phiên — tìm kiếm, dòng thời gian và phát lại transcript), <code>ccam-workflows</code> (điều phối đa agent và trí tuệ đội nhóm), <code>ccam-quality</code> (kiểm tra độ tin cậy và SLO), và <code>ccam-config</code> (quản trị cấu hình và bộ nhớ của Claude Code). Cài đặt bằng <code>claude plugin install</code> — không cần khởi động lại. Mỗi mục liệt kê hiển thị tác giả, giấy phép, trang chủ và phần phân tích đóng góp theo từng kỹ năng. Tab Plugins của Config Explorer hiển thị các plugin đã cài kèm trạng thái trực tiếp.",
    "Spawn <code>claude</code> subprocesses straight from the dashboard with a chat-style streaming UI — multi-turn <b>Conversation</b> or single-shot <b>Headless</b> mode. One-click <b>Resume</b> on any past conversation spawns <code>claude --resume</code> seeded with the prior transcript. Re- attach reconciles in-memory logs with the on-disk JSONL so navigating away never loses history. Slash-command autocomplete, file references, live token/context-window meter, and a thinking-effort dial bring TUI parity to the browser. Same-origin guard blocks drive-by spawns.":
      "Sinh các tiến trình con <code>claude</code> ngay từ dashboard với một UI luồng theo kiểu trò chuyện — chế độ <b>Conversation</b> nhiều lượt hoặc <b>Headless</b> một lần. Nhấp một lần <b>Resume</b> trên bất kỳ cuộc trò chuyện nào trong quá khứ sẽ sinh <code>claude --resume</code> được gieo mầm bằng transcript trước đó. Việc kết nối lại đối chiếu nhật ký trong bộ nhớ với JSONL trên đĩa nên rời khỏi trang không bao giờ làm mất lịch sử. Tự động hoàn thành lệnh gạch chéo, tham chiếu tệp, đồng hồ đo token/cửa sổ ngữ cảnh trực tiếp và một núm điều chỉnh mức độ suy nghĩ mang lại sự tương đương với TUI ngay trên trình duyệt. Cơ chế bảo vệ same-origin chặn các lần sinh lén lút.",
    "A 12-tab inspector at <code>/cc-config</code> for everything Claude Code knows about: skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts. The Settings tab leads with a Current configuration summary of the options <code>/config</code> controls — model, verbose, theme, output style, auto-compact, notifications — resolved across scopes. The Memory tab surfaces both the user and project <code>CLAUDE.md</code> files and the per-project file-based memory store — every auto-memory <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+), grouped by project, searchable, and editable. Create, edit, and delete the low-risk text-file surfaces with mandatory timestamped backups before every write. Plugins, MCP, hooks, and live settings stay read-only with explainer banners and copy-able CLI commands. Per-plugin contribution breakdowns show author and license.":
      "Một trình kiểm tra gồm 12 tab tại <code>/cc-config</code> cho mọi thứ mà Claude Code biết: kỹ năng, subagent, lệnh gạch chéo, kiểu đầu ra, plugin, chợ, máy chủ MCP, hook, cài đặt, bộ nhớ, gán phím và các script thanh trạng thái. Tab Cài đặt mở đầu bằng một bản tóm tắt Cấu hình hiện tại của các tùy chọn mà <code>/config</code> kiểm soát — model, verbose, theme, kiểu đầu ra, tự động nén, thông báo — được phân giải trên các phạm vi. Tab Bộ nhớ hiển thị cả tệp <code>CLAUDE.md</code> của người dùng lẫn của dự án, cùng kho bộ nhớ dựa trên tệp theo từng dự án — mỗi tệp auto-memory <code>*.md</code> nằm dưới <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (một chỉ mục <code>MEMORY.md</code> cộng với một tệp cho mỗi sự kiện được ghi nhớ, thường hơn 100 tệp), được nhóm theo dự án, có thể tìm kiếm và chỉnh sửa. Tạo, chỉnh sửa và xóa các bề mặt tệp văn bản rủi ro thấp với việc sao lưu bắt buộc có dấu thời gian trước mỗi lần ghi. Plugin, MCP, hook và cài đặt trực tiếp vẫn ở chế độ chỉ đọc kèm các biểu ngữ giải thích và lệnh CLI có thể sao chép. Phần phân tích đóng góp theo từng plugin hiển thị tác giả và giấy phép.",
    "Mobile-first layouts with stacking grids, horizontally scrollable tables, and a collapsible sidebar that auto-hides below 1400 px. All pages adapt from phone to ultrawide with consistent navigation and readable typography. Kanban columns stack vertically on narrow screens, analytics charts reflow to single-column, and the activity feed stays fully swipeable. Touch targets meet 44 px minimum. Dark theme renders consistently across iOS Safari, Chrome, and Firefox with no flash of unstyled content.":
      "Bố cục ưu tiên di động với các lưới xếp chồng, bảng có thể cuộn ngang và một thanh bên có thể thu gọn tự động ẩn khi dưới 1400 px. Mọi trang đều thích ứng từ điện thoại đến màn hình siêu rộng với điều hướng nhất quán và kiểu chữ dễ đọc. Các cột Kanban xếp chồng theo chiều dọc trên màn hình hẹp, các biểu đồ phân tích sắp xếp lại thành một cột, và dòng hoạt động vẫn có thể vuốt hoàn toàn. Các vùng chạm đạt tối thiểu 44 px. Giao diện tối hiển thị nhất quán trên iOS Safari, Chrome và Firefox mà không có hiện tượng nhấp nháy nội dung chưa định kiểu.",
    "Visualize parallel agent execution with a Gantt-style timeline showing overlapping subagent lifetimes, tool-call concurrency windows, and wait gaps. Color-coded bars distinguish working, waiting, and errored states so bottlenecks are immediately visible. Hover any bar for exact timestamps and duration. Zoom and pan across long-running sessions with hundreds of agents. The timeline shares the Workflows status filter so you can isolate active, completed, or errored sessions without leaving the view.":
      "Trực quan hóa việc thực thi agent song song bằng một dòng thời gian kiểu Gantt thể hiện các vòng đời subagent chồng lấn, các cửa sổ đồng thời của lần gọi công cụ và các khoảng chờ. Các thanh được mã màu phân biệt trạng thái đang làm việc, đang chờ và bị lỗi nên các nút thắt cổ chai hiện ra ngay lập tức. Rê chuột lên bất kỳ thanh nào để xem dấu thời gian và thời lượng chính xác. Phóng to và kéo qua các phiên chạy dài với hàng trăm agent. Dòng thời gian này dùng chung bộ lọc trạng thái của Workflows nên bạn có thể tách riêng các phiên đang hoạt động, đã hoàn tất hoặc bị lỗi mà không cần rời khỏi khung nhìn.",
    "Professional VS Code extension with a real-time Activity Bar sidebar showing active sessions, agent counts, and recent events without leaving your editor. A status bar pulse monitor surfaces connection health and the latest event type at a glance. Deep navigation links open any session or analytics view directly in your browser. An embedded webview renders the full dashboard inside a VS Code tab with WebSocket push, theme sync, and responsive layout. Install from the marketplace or build from source.":
      "Tiện ích mở rộng VS Code chuyên nghiệp với một thanh bên Activity Bar theo thời gian thực hiển thị các phiên đang hoạt động, số lượng agent và các sự kiện gần đây mà không cần rời khỏi trình soạn thảo. Một bộ giám sát nhịp ở thanh trạng thái hiện ra sức khỏe kết nối và loại sự kiện mới nhất chỉ trong nháy mắt. Các liên kết điều hướng chuyên sâu mở bất kỳ phiên hoặc khung nhìn phân tích nào trực tiếp trong trình duyệt của bạn. Một webview nhúng hiển thị toàn bộ dashboard ngay trong một tab VS Code với WebSocket push, đồng bộ giao diện và bố cục đáp ứng. Cài đặt từ chợ hoặc build từ mã nguồn.",
    "Trace how errors cascade across agents and tool calls with a directed graph showing failure origins, retry paths, and recovery points. Each node displays the agent or tool that errored, the error message, and whether a retry succeeded or propagated upstream. Pinpoint root causes in deeply nested subagent chains. Horizontal bar charts rank the most error-prone tools and models. API error cards group failures by HTTP status and endpoint. Filter by session, time range, or error severity to narrow the view.":
      "Truy vết cách các lỗi lan truyền dây chuyền qua các agent và lần gọi công cụ bằng một đồ thị có hướng thể hiện nguồn gốc lỗi, đường thử lại và điểm phục hồi. Mỗi nút hiển thị agent hoặc công cụ bị lỗi, thông báo lỗi và việc thử lại đã thành công hay lan truyền lên thượng nguồn. Xác định chính xác nguyên nhân gốc trong các chuỗi subagent lồng sâu. Các biểu đồ thanh ngang xếp hạng những công cụ và model dễ lỗi nhất. Các thẻ lỗi API nhóm các thất bại theo trạng thái HTTP và endpoint. Lọc theo phiên, khoảng thời gian hoặc mức độ nghiêm trọng của lỗi để thu hẹp khung nhìn.",
    'Three independent PWAs — dashboard, landing page, and wiki — each with its own Web App Manifest and Service Worker. Install to your home screen or dock for a standalone, chrome-less experience. SVG icons with <code>sizes="any"</code> and iOS standalone meta tags included.':
      'Ba PWA độc lập — dashboard, trang đích và wiki — mỗi cái có Web App Manifest và Service Worker riêng. Cài đặt vào màn hình chính hoặc dock của bạn để có trải nghiệm độc lập, không khung trình duyệt. Bao gồm các biểu tượng SVG với <code>sizes="any"</code> và các thẻ meta standalone của iOS.',
    "The dashboard SW serves Vite's hashed <code>/assets/*</code> bundles cache-first (URLs are immutable per build) and treats everything else as network-first with cache fallback. Explicit <code>Cache-Control</code> headers on the production Express static middleware reinforce the policy, so a rebuild replaces the in-browser code without a hard refresh.":
      "SW của dashboard phục vụ các gói <code>/assets/*</code> đã băm của Vite theo kiểu cache-first (các URL là bất biến theo mỗi lần build) và xử lý mọi thứ khác theo kiểu network-first với cache làm phương án dự phòng. Các header <code>Cache-Control</code> tường minh trên middleware tĩnh Express ở môi trường production củng cố chính sách này, nên một lần build lại sẽ thay thế mã trong trình duyệt mà không cần làm mới cứng.",
    "A <code>controllerchange</code> listener in <code>client/src/main.tsx</code> reloads the page exactly once when a new SW takes over an already-controlled page. First installs do not reload, so the very first visit is never interrupted.":
      "Một trình lắng nghe <code>controllerchange</code> trong <code>client/src/main.tsx</code> tải lại trang đúng một lần khi một SW mới tiếp quản một trang đã được kiểm soát. Các lần cài đặt đầu tiên không tải lại, nên lần truy cập đầu tiên không bao giờ bị gián đoạn.",
    '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — live overview of active sessions and agents. Stats tiles, collapsible subagent hierarchy cards, and a recent activity feed. Auto-refreshes every 5 s via WebSocket</span>':
      '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — tổng quan trực tiếp về các phiên và agent đang hoạt động. Các ô thống kê, thẻ phân cấp subagent có thể thu gọn, và một nguồn cấp hoạt động gần đây. Tự động làm mới mỗi 5 s qua WebSocket</span>',
    '<span class="caption-icon">🩺</span> <span><strong>Dashboard · Health</strong> — composite health score ring, storage engine donut, cache/error/success gauges, tool invocation bars, subagent effectiveness, and model token distribution</span>':
      '<span class="caption-icon">🩺</span> <span><strong>Dashboard · Health</strong> — vòng điểm sức khỏe tổng hợp, biểu đồ vành khuyên về storage engine, các đồng hồ đo cache/lỗi/thành công, biểu đồ cột số lần gọi công cụ, hiệu quả của subagent, và phân bố token theo mô hình</span>',
    '<span class="caption-icon">📋</span> <span><strong>Kanban Board (agents)</strong> — agents swim-laned by status: Working, Waiting, Completed, Error. Cards show model, cost, and current tool call. Yellow column flags agents waiting on user input</span>':
      '<span class="caption-icon">📋</span> <span><strong>Kanban Board (agent)</strong> — các agent được xếp theo làn dựa trên trạng thái: Working, Waiting, Completed, Error. Thẻ hiển thị mô hình, chi phí và lời gọi công cụ hiện tại. Cột màu vàng đánh dấu các agent đang chờ người dùng nhập liệu</span>',
    '<span class="caption-icon">🗂️</span> <span><strong>Kanban Board (sessions)</strong> — sessions swim-laned across 5 columns: Active, Waiting, Completed, Error, Abandoned. Each card shows agent count, duration, model, and cumulative cost</span>':
      '<span class="caption-icon">🗂️</span> <span><strong>Kanban Board (session)</strong> — các session được xếp theo làn trên 5 cột: Active, Waiting, Completed, Error, Abandoned. Mỗi thẻ hiển thị số lượng agent, thời lượng, mô hình và chi phí tích lũy</span>',
    '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — searchable, filterable, server-paginated table. Each row shows status badge, agent count, duration, model, and cost. Click any row to drill into session detail</span>':
      '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — bảng có thể tìm kiếm, lọc và phân trang phía máy chủ. Mỗi hàng hiển thị huy hiệu trạng thái, số lượng agent, thời lượng, mô hình và chi phí. Nhấp vào bất kỳ hàng nào để xem chi tiết phiên</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles (events, tool calls, subagents, errors, duration), top-tool usage bars, subagent type breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — các ô tổng quan (sự kiện, lời gọi công cụ, subagent, lỗi, thời lượng), biểu đồ cột công cụ dùng nhiều nhất, phân tích theo loại subagent, và một cây phân cấp agent cha–con có thể thu gọn</span>',
    '<span class="caption-icon">💬</span> <span><strong>Session Detail · Conversation</strong> — full transcript viewer with markdown rendering, syntax-highlighted code blocks, per-tool sections, and collapsible thinking blocks</span>':
      '<span class="caption-icon">💬</span> <span><strong>Session Detail · Conversation</strong> — trình xem bản ghi đầy đủ với kết xuất markdown, khối mã được tô sáng cú pháp, các phần theo từng công cụ, và các khối suy nghĩ có thể thu gọn</span>',
    '<span class="caption-icon">🔬</span> <span><strong>Session Detail · Timeline</strong> — chronological event timeline with multi-dimension filters, color-coded entries by type, expandable hook payloads, and direct links to the owning session and agent</span>':
      '<span class="caption-icon">🔬</span> <span><strong>Session Detail · Timeline</strong> — dòng thời gian sự kiện theo trình tự thời gian với bộ lọc đa chiều, các mục được mã hóa màu theo loại, payload hook có thể mở rộng, và liên kết trực tiếp đến phiên và agent sở hữu</span>',
    '<span class="caption-icon">📰</span> <span><strong>Activity Feed</strong> — real-time streaming event log with pause/resume buffering, multi-dimension filters, expandable hook payloads, color-coded entries, and per-row session navigation buttons</span>':
      '<span class="caption-icon">📰</span> <span><strong>Activity Feed</strong> — nhật ký sự kiện truyền trực tiếp theo thời gian thực với bộ đệm tạm dừng/tiếp tục, bộ lọc đa chiều, payload hook có thể mở rộng, các mục được mã hóa màu, và nút điều hướng phiên trên từng hàng</span>',
    '<span class="caption-icon">📊</span> <span><strong>Analytics</strong> — token usage by model, tool frequency bars, 52-week activity heatmap, 30-day sparkline trends, session outcome donuts, and cost summary with WebSocket auto-refresh</span>':
      '<span class="caption-icon">📊</span> <span><strong>Analytics</strong> — mức dùng token theo mô hình, biểu đồ cột tần suất công cụ, bản đồ nhiệt hoạt động 52 tuần, xu hướng sparkline 30 ngày, biểu đồ vành khuyên về kết quả phiên, và bản tóm tắt chi phí với tự động làm mới qua WebSocket</span>',
    '<span class="caption-icon">🔀</span> <span><strong>Workflows</strong> — D3.js agent orchestration DAG, tool-execution Sankey diagram, directed pipeline graph, effectiveness scorecards, concurrency swim-lanes, and complexity bubble charts</span>':
      '<span class="caption-icon">🔀</span> <span><strong>Workflows</strong> — DAG điều phối agent bằng D3.js, sơ đồ Sankey về thực thi công cụ, đồ thị pipeline có hướng, bảng điểm hiệu quả, làn bơi đồng thời, và biểu đồ bong bóng độ phức tạp</span>',
    '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — 12-tab inspector for skills, subagents, slash commands, plugins, MCP servers, hooks, settings, memory, keybindings, and statusline. Safe edits with backups</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — trình kiểm tra gồm 12 tab cho skill, subagent, lệnh slash, plugin, máy chủ MCP, hook, cài đặt, bộ nhớ, phím tắt và thanh trạng thái. Chỉnh sửa an toàn kèm sao lưu</span>',
    '<span class="caption-icon">▶️</span> <span><strong>Run Claude</strong> — spawn or resume Claude subprocesses from the browser. Pick Conversation or Headless mode, set cwd, model, permission level, and thinking effort. Same-origin guard included</span>':
      '<span class="caption-icon">▶️</span> <span><strong>Run Claude</strong> — khởi chạy hoặc tiếp tục các tiến trình con Claude từ trình duyệt. Chọn chế độ Conversation hoặc Headless, đặt cwd, mô hình, mức quyền và cường độ suy nghĩ. Có kèm bảo vệ same-origin</span>',
    '<span class="caption-icon">💬</span> <span><strong>Run Claude · live stream</strong> — character-by-character streaming output. Tool uses, tool results, and thinking blocks are collapsible. Active runs switcher juggles multiple sessions</span>':
      '<span class="caption-icon">💬</span> <span><strong>Run Claude · live stream</strong> — kết xuất truyền trực tiếp theo từng ký tự. Lời gọi công cụ, kết quả công cụ và khối suy nghĩ đều có thể thu gọn. Bộ chuyển đổi các lần chạy đang hoạt động giúp quản lý nhiều phiên cùng lúc</span>',
    '<span class="caption-icon">⚙️</span> <span><strong>Settings</strong> — model pricing editor with per-token rates, hook installation status, JSON data export, session cleanup controls, browser notification toggles, and system info panel with DB stats</span>':
      '<span class="caption-icon">⚙️</span> <span><strong>Settings</strong> — trình chỉnh sửa giá mô hình với mức phí theo từng token, trạng thái cài đặt hook, xuất dữ liệu JSON, các điều khiển dọn dẹp phiên, công tắc thông báo trình duyệt, và bảng thông tin hệ thống kèm thống kê DB</span>',
    "This chart tracks how interest in Claude Code Agent Monitor has grown over time. The curve keeps climbing as more developers discover the project, share it, and use it in real workflows. Each new star is a small vote of confidence from the community.":
      "Biểu đồ này theo dõi mức độ quan tâm đến Claude Code Agent Monitor đã tăng trưởng như thế nào theo thời gian. Đường cong tiếp tục đi lên khi ngày càng nhiều nhà phát triển khám phá dự án, chia sẻ nó và dùng nó trong các quy trình làm việc thực tế. Mỗi star mới là một lá phiếu tin tưởng nhỏ từ cộng đồng.",
    '<span class="caption-icon">⭐</span> <span> Enjoying the project? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer" >Give it a star on GitHub</a > and help more builders discover it. </span>':
      '<span class="caption-icon">⭐</span> <span> Bạn thích dự án này? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer" >Hãy gắn sao cho nó trên GitHub</a > và giúp nhiều nhà phát triển khác khám phá nó. </span>',
    "Hook Type": "Loại Hook",
    Trigger: "Điều kiện kích hoạt",
    "Dashboard Action": "Hành động trên Dashboard",
    "Claude Code session begins": "Phiên Claude Code bắt đầu",
    "Creates session and main agent. Stamps <code>awaiting_input_since</code> (with <code>awaiting_reason</code> = <code>session_start</code>) so the row lands in <strong>Waiting</strong> from the start (the CLI is at a prompt) — except a <code>compact</code>-source SessionStart (mid-turn auto-compaction), which leaves the flag untouched so a working session stays <strong>Active</strong>. Reactivates resumed sessions. Abandons orphaned sessions with no activity for <code>DASHBOARD_STALE_MINUTES</code> (default 180).":
      "Tạo phiên và agent chính. Đóng dấu thời gian <code>awaiting_input_since</code> (với <code>awaiting_reason</code> = <code>session_start</code>) để hàng đó nằm trong <strong>Waiting</strong> ngay từ đầu (CLI đang ở dấu nhắc) — trừ SessionStart nguồn <code>compact</code> (nén giữa lượt), vốn giữ nguyên cờ để phiên đang chạy vẫn <strong>Active</strong>. Kích hoạt lại các phiên được tiếp tục. Bỏ rơi các phiên mồ côi không có hoạt động trong <code>DASHBOARD_STALE_MINUTES</code> (mặc định 180).",
    "User hits enter on a prompt": "Người dùng nhấn enter tại dấu nhắc",
    'Clears the waiting flag and promotes the main agent to <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >. The only reliable signal that text-only assistant turns have started — they emit no <code>PreToolUse</code> before <code>Stop</code>.':
      'Xóa cờ chờ và nâng agent chính lên <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >. Đây là tín hiệu đáng tin cậy duy nhất cho biết các lượt trợ lý chỉ-văn-bản đã bắt đầu — chúng không phát ra <code>PreToolUse</code> trước <code>Stop</code>.',
    "Agent begins using a tool": "Agent bắt đầu sử dụng một công cụ",
    'Clears the waiting flag, sets agent → <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >, <code>current_tool</code> set. If tool is <code>Agent</code>, subagent record created.':
      'Xóa cờ chờ, đặt agent → <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >, đặt <code>current_tool</code>. Nếu công cụ là <code>Agent</code>, tạo bản ghi subagent.',
    "Tool execution completes": "Việc thực thi công cụ hoàn tất",
    'Clears the waiting flag (covers permission-prompt approvals mid-tool). <code>current_tool</code> cleared. Agent stays <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >.':
      'Xóa cờ chờ (bao quát các phê duyệt nhắc-quyền giữa chừng khi dùng công cụ). Xóa <code>current_tool</code>. Agent vẫn ở <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >.',
    "Claude finishes a turn": "Claude hoàn thành một lượt",
    'Non-error: main agent → <code>waiting</code> — UI shows <span class="status-chip chip-waiting" ><span class="chip-dot"></span>Waiting</span > until the next user input. <code>stop_reason=error</code>: marks the agent and session <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >. Background subagents keep running.':
      'Không lỗi: agent chính → <code>waiting</code> — UI hiển thị <span class="status-chip chip-waiting" ><span class="chip-dot"></span>Waiting</span > cho đến lần nhập liệu tiếp theo của người dùng. <code>stop_reason=error</code>: đánh dấu agent và phiên là <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >. Các subagent chạy nền vẫn tiếp tục chạy.',
    "Background agent finished": "Agent chạy nền đã hoàn thành",
    "Matched subagent → <span class=\"status-chip chip-completed\" ><span class=\"chip-dot\"></span>Completed</span >. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session's <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent's own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks. The same scan also rebuilds the nested-subagent hierarchy — it repoints each subagent's <code>parent_agent_id</code> to the true spawner recovered from the transcript's Task tool result (<code>toolUseResult.agentId</code>), so subagents that spawn their own subagents nest correctly instead of flattening under main.":
      'Subagent khớp → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Cố ý <strong>không</strong> xóa cờ chờ — việc một subagent chạy nền hoàn thành không cho ta biết gì về con người. Đồng thời khởi động một lượt quét JSONL kiểu phát-rồi-quên (<code>scanAndImportSubagents</code>) duyệt qua các tệp <code>subagents/agent-*.jsonl</code> của phiên, ghép cặp các khối <code>tool_use</code> ↔ <code>tool_result</code> theo <code>tool_use_id</code>, và phát ra các sự kiện <code>PreToolUse</code> + <code>PostToolUse</code> theo từng công cụ dưới <code>agent_id</code> riêng của mỗi subagent — làm lộ ra các lời gọi công cụ mà subagent thực hiện nội bộ và vốn không bao giờ kích hoạt bất kỳ hook nào. Cùng lượt quét đó còn dựng lại phân cấp subagent lồng nhau — nó trỏ lại <code>parent_agent_id</code> của mỗi subagent về đúng agent đã spawn, khôi phục từ kết quả tool Task trong transcript (<code>toolUseResult.agentId</code>), nên subagent tự spawn subagent con sẽ lồng đúng chỗ thay vì dồn phẳng dưới main.',
    "Agent sends notification": "Agent gửi thông báo",
    'Event logged to activity feed. Permission/input-prompt patterns (e.g. "needs your permission", "waiting for your input") set the agent to <code>waiting</code> and stamp <code>awaiting_input_since</code> (with <code>awaiting_reason</code> = <code>notification</code>). Compaction-related notifications tagged as <code>Compaction</code> events. Triggers a browser notification if enabled.':
      'Sự kiện được ghi vào nguồn cấp hoạt động. Các mẫu nhắc quyền/nhắc nhập liệu (ví dụ "needs your permission", "waiting for your input") đặt agent thành <code>waiting</code> và đóng dấu thời gian <code>awaiting_input_since</code> (với <code>awaiting_reason</code> = <code>notification</code>). Các thông báo liên quan đến nén được gắn thẻ là sự kiện <code>Compaction</code>. Kích hoạt thông báo trình duyệt nếu được bật.',
    "<code>/compact</code> detected in JSONL": "Phát hiện <code>/compact</code> trong JSONL",
    'Creates a compaction subagent → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Detected via <code>isCompactSummary</code> entries in the transcript. Token baselines preserve pre-compaction totals. Periodic scanner (cadence ~¼ of <code>DASHBOARD_STALE_MINUTES</code>) catches compactions when no hooks fire.':
      'Tạo một subagent nén → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Được phát hiện qua các mục <code>isCompactSummary</code> trong bản ghi. Các đường cơ sở token giữ lại tổng số trước khi nén. Bộ quét định kỳ (nhịp khoảng ¼ của <code>DASHBOARD_STALE_MINUTES</code>) bắt được các lần nén khi không có hook nào kích hoạt.',
    "API error detected in transcript": "Phát hiện lỗi API trong bản ghi",
    "Extracted from JSONL during history import, real-time transcript scanning, or the error detection watchdog. Captures quota limits, rate limits, auth failures, and other API errors. <strong>Immediately marks sessions and agents as error</strong> — previously recorded as events without changing status.":
      "Được trích xuất từ JSONL trong quá trình nhập lịch sử, quét bản ghi theo thời gian thực, hoặc bộ canh gác phát hiện lỗi. Bắt được các giới hạn hạn ngạch, giới hạn tốc độ, lỗi xác thực và các lỗi API khác. <strong>Lập tức đánh dấu phiên và agent là lỗi</strong> — trước đây chỉ được ghi nhận như sự kiện mà không thay đổi trạng thái.",
    "Turn cancelled by the user (<code>Esc</code>)": "Lượt bị người dùng hủy (<code>Esc</code>)",
    "Synthesized by the watchdog because pressing <code>Esc</code> fires no hook. Recovered either from the transcript <code>[Request interrupted by user]</code> marker (flagged as <code>pendingInterrupt</code>) or, when <code>Esc</code> preceded any output and left no marker, from the idle-working timeout (<code>DASHBOARD_WORKING_IDLE_SECONDS</code>, default 120). Moves the session to <strong>Waiting</strong> — the same state a normal <code>Stop</code> produces.":
      "Được tổng hợp bởi watchdog vì việc nhấn <code>Esc</code> không kích hoạt hook nào. Khôi phục hoặc từ dấu hiệu <code>[Request interrupted by user]</code> trong bản ghi (được gắn cờ là <code>pendingInterrupt</code>), hoặc khi <code>Esc</code> diễn ra trước bất kỳ đầu ra nào và không để lại dấu hiệu, từ thời gian chờ làm-việc-rảnh-rỗi (<code>DASHBOARD_WORKING_IDLE_SECONDS</code>, mặc định 120). Chuyển phiên sang <strong>Waiting</strong> — cùng trạng thái mà một <code>Stop</code> bình thường tạo ra.",
    "Per-turn timing recorded": "Ghi lại thời gian theo từng lượt",
    "Extracted from JSONL turn boundaries. Records the duration of each assistant turn for latency analysis.":
      "Được trích xuất từ các ranh giới lượt trong JSONL. Ghi lại thời lượng của mỗi lượt trợ lý để phân tích độ trễ.",
    "Claude Code CLI process exits": "Tiến trình Claude Code CLI thoát",
    'Drops the waiting flag. If the session is already in <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >, the error state is preserved; otherwise marks all agents and the session as <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Evicts the session\'s transcript from the shared cache.':
      'Bỏ cờ chờ. Nếu phiên đã ở trạng thái <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >, trạng thái lỗi được giữ nguyên; nếu không, đánh dấu tất cả agent và phiên là <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Loại bỏ bản ghi của phiên khỏi bộ nhớ đệm dùng chung.',
    "Clone the repository to your machine": "Sao chép kho lưu trữ về máy của bạn",
    "Run <code>npm run setup</code> to install all dependencies":
      "Chạy <code>npm run setup</code> để cài đặt tất cả phụ thuộc",
    "Run <code>npm run dev</code> — server + client launch automatically":
      "Chạy <code>npm run dev</code> — máy chủ + máy khách tự động khởi chạy",
    "Start a new Claude Code session — events appear in real-time":
      "Khởi động một phiên Claude Code mới — các sự kiện xuất hiện theo thời gian thực",
    "A multi-stage <code>Dockerfile</code> and <code>docker-compose.yml</code> are included. You can run the monitor with either Docker or Podman and keep the SQLite database in a named volume.":
      "Đã bao gồm một <code>Dockerfile</code> nhiều giai đoạn và <code>docker-compose.yml</code>. Bạn có thể chạy trình giám sát bằng Docker hoặc Podman và giữ cơ sở dữ liệu SQLite trong một named volume.",
    "Hooks auto-install in local mode": "Hooks tự động cài đặt ở chế độ cục bộ",
    "When you run the server directly on the host with <code>npm run dev</code> or <code>npm start</code>, it automatically writes Claude Code hook entries to <code>~/.claude/settings.json</code>. If you run the dashboard in Docker or Podman, install hooks from the host with <code>npm run install-hooks</code> after the container is up, then restart Claude Code. The installer refuses to run inside a container (issue #193) so it never writes a container-internal handler path into a bind-mounted host <code>~/.claude</code>; override with <code>CCAM_ALLOW_CONTAINER_HOOKS=1</code> only if Claude Code itself runs in the container.":
      "Khi bạn chạy máy chủ trực tiếp trên host bằng <code>npm run dev</code> hoặc <code>npm start</code>, nó tự động ghi các mục hook của Claude Code vào <code>~/.claude/settings.json</code>. Nếu bạn chạy bảng điều khiển trong Docker hoặc Podman, hãy cài đặt hooks từ host bằng <code>npm run install-hooks</code> sau khi container đã chạy, rồi khởi động lại Claude Code. Trình cài đặt từ chối chạy bên trong một container (issue #193) nên không bao giờ ghi đường dẫn handler nội bộ của container vào <code>~/.claude</code> của máy chủ được bind-mount; chỉ ghi đè bằng <code>CCAM_ALLOW_CONTAINER_HOOKS=1</code> nếu chính Claude Code chạy trong container.",
    "This repository also ships a local MCP server under <code>mcp/</code> and extension scaffolding for both Claude Code and Codex. These are optional for the dashboard UI, but recommended for complete local-agent workflows. The MCP server supports stdio (for host integration), HTTP+SSE (for remote clients), and an interactive REPL (for operator debugging).":
      "Kho lưu trữ này cũng đi kèm một máy chủ MCP cục bộ tại <code>mcp/</code> và bộ khung mở rộng cho cả Claude Code và Codex. Những thành phần này là tùy chọn đối với giao diện bảng điều khiển, nhưng được khuyến nghị cho các quy trình local-agent đầy đủ. Máy chủ MCP hỗ trợ stdio (để tích hợp với host), HTTP+SSE (cho các client từ xa) và một REPL tương tác (để vận hành viên gỡ lỗi).",
    "After starting a Claude Code session, you should see:":
      "Sau khi bắt đầu một phiên Claude Code, bạn sẽ thấy:",
    Page: "Trang",
    Expected: "Kết quả mong đợi",
    Sessions: "Phiên (Sessions)",
    'Your session listed with status <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> (a fresh CLI is sitting at the prompt) — flips to <span class="status-chip chip-active"><span class="chip-dot"></span>Active</span> the moment Claude starts a turn':
      'Phiên của bạn được liệt kê với trạng thái <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> (một CLI mới đang chờ ở dấu nhắc) — chuyển sang <span class="status-chip chip-active"><span class="chip-dot"></span>Active</span> ngay khi Claude bắt đầu một lượt',
    "Kanban Board": "Bảng Kanban",
    "A <em>Main Agent</em> card in the <strong>Waiting</strong> column until you type your first message; flips to <em>Working</em> on <code>UserPromptSubmit</code> / <code>PreToolUse</code> and back to <em>Waiting</em> after each <code>Stop</code>":
      "Một thẻ <em>Main Agent</em> nằm ở cột <strong>Waiting</strong> cho đến khi bạn nhập tin nhắn đầu tiên; chuyển sang <em>Working</em> khi có <code>UserPromptSubmit</code> / <code>PreToolUse</code> và trở lại <em>Waiting</em> sau mỗi <code>Stop</code>",
    "Activity Feed": "Luồng hoạt động",
    'Events streaming in; click any row to expand payload, use "Session →" to drill into session details':
      "Các sự kiện liên tục truyền vào; nhấp vào bất kỳ hàng nào để mở rộng payload, dùng “Session →” để đi sâu vào chi tiết phiên",
    Dashboard: "Bảng điều khiển",
    "Stats updating in real-time": "Số liệu thống kê cập nhật theo thời gian thực",
    "Start server before Claude Code": "Khởi động máy chủ trước Claude Code",
    "Hooks only fire to a running server. If Claude Code was already running when you started the dashboard, restart the Claude Code session.":
      "Hooks chỉ kích hoạt tới một máy chủ đang chạy. Nếu Claude Code đã chạy khi bạn khởi động bảng điều khiển, hãy khởi động lại phiên Claude Code.",
    Variable: "Biến",
    Default: "Mặc định",
    Description: "Mô tả",
    "Port the Express server listens on": "Cổng mà máy chủ Express lắng nghe",
    "Port used by the hook handler to reach the server (for custom port setups)":
      "Cổng mà trình xử lý hook dùng để kết nối đến máy chủ (dành cho cấu hình cổng tùy chỉnh)",
    "Base URL used by the local MCP server when calling dashboard APIs":
      "URL gốc mà máy chủ MCP cục bộ dùng khi gọi các API của bảng điều khiển",
    "MCP transport mode: <code>stdio</code>, <code>http</code>, <code>repl</code>":
      "Chế độ truyền tải MCP: <code>stdio</code>, <code>http</code>, <code>repl</code>",
    "Port for the MCP HTTP+SSE server (only when <code>MCP_TRANSPORT=http</code>)":
      "Cổng cho máy chủ MCP HTTP+SSE (chỉ khi <code>MCP_TRANSPORT=http</code>)",
    "Bind address for the MCP HTTP server": "Địa chỉ bind cho máy chủ MCP HTTP",
    "Path to the SQLite database file": "Đường dẫn tới tệp cơ sở dữ liệu SQLite",
    "Set to <code>0</code> to disable the dead-session liveness reap — the watchdog completes active local sessions only when their matching <code>claude</code> or <code>codex</code> process is gone; auto-disabled on Windows and in containers. Sessions forwarded from another machine (household hooks) report a non-POSIX <code>cwd</code> and are auto-skipped, so a mixed local + forwarded deployment no longer needs this off.":
      "Đặt <code>0</code> để tắt cơ chế thu dọn phiên đã chết — watchdog chỉ hoàn tất các phiên cục bộ đang hoạt động khi tiến trình <code>claude</code> hoặc <code>codex</code> tương ứng đã kết thúc; tự động tắt trên Windows và trong container. Các phiên được chuyển tiếp từ máy khác (household hooks) báo cáo <code>cwd</code> không theo chuẩn POSIX nên được tự động bỏ qua, vì vậy triển khai hỗn hợp cục bộ + chuyển tiếp không còn cần tắt tùy chọn này.",
    "Idle gate for watchdog-tick liveness reaps — the transcript must not have been written for at least this long (last hook write is the fallback clock); startup passes skip the gate":
      "Ngưỡng nhàn rỗi cho thu dọn ở nhịp watchdog — transcript phải không được ghi trong ít nhất khoảng này (lấy lần ghi hook cuối làm đồng hồ dự phòng); các lượt thu dọn lúc khởi động bỏ qua ngưỡng này",
    "Idle-working timeout the watchdog uses to recover an <code>Esc</code> cancel that left no transcript marker":
      "Thời gian chờ làm-việc-rảnh-rỗi mà watchdog dùng để khôi phục một lần hủy <code>Esc</code> không để lại dấu hiệu trong bản ghi",
    "Poll interval for the background sync of <code>~/.claude/projects</code>; <code>0</code> disables the poll but keeps the filesystem watcher":
      "Khoảng thời gian thăm dò cho việc đồng bộ nền của <code>~/.claude/projects</code>; <code>0</code> tắt thăm dò nhưng vẫn giữ trình theo dõi hệ thống tệp",
    "Set to <code>production</code> to serve built client from <code>client/dist/</code>":
      "Đặt thành <code>production</code> để phục vụ client đã build từ <code>client/dist/</code>",
    "The server writes the following to <code>~/.claude/settings.json</code> on every startup:":
      "Máy chủ ghi nội dung sau vào <code>~/.claude/settings.json</code> mỗi khi khởi động:",
    "Existing hooks are preserved. The installer only adds or updates entries containing <code>hook-handler.js</code>.":
      "Các hooks hiện có được giữ nguyên. Trình cài đặt chỉ thêm hoặc cập nhật các mục có chứa <code>hook-handler.js</code>.",
    Script: "Script",
    Command: "Lệnh",
    "Install all dependencies (server + client)": "Cài đặt tất cả phụ thuộc (máy chủ + client)",
    "Start server + client in development mode with hot reload":
      "Khởi động máy chủ + client ở chế độ phát triển với hot reload",
    "Start only the Express server with <code>--watch</code>":
      "Chỉ khởi động máy chủ Express với <code>--watch</code>",
    "Start only the Vite dev server": "Chỉ khởi động máy chủ phát triển Vite",
    "TypeScript check + Vite production build to <code>client/dist/</code>":
      "Kiểm tra TypeScript + build production bằng Vite vào <code>client/dist/</code>",
    "Start Express in production mode serving built client":
      "Khởi động Express ở chế độ production để phục vụ client đã build",
    "Manually write Claude Code hooks to <code>~/.claude/settings.json</code>":
      "Ghi thủ công các hook của Claude Code vào <code>~/.claude/settings.json</code>",
    "Insert demo sessions, agents, and events (8 sessions / 23 agents / 106 events)":
      "Chèn các phiên, agent và sự kiện mẫu (8 phiên / 23 agent / 106 sự kiện)",
    "Refresh token totals for imported sessions from their transcripts. Never lowers an existing total":
      "Làm mới tổng Token cho các phiên đã nhập từ transcript của chúng. Không bao giờ hạ thấp tổng hiện có",
    "Re-derive non-workflow token totals for Claude sessions whose transcripts are still on disk and reset the compaction baselines, preserving workflow and Codex rows. One-time repair for databases inflated by the old per-record usage sum; stop the dashboard before running it":
      "T\u00ednh l\u1ea1i t\u1ed5ng Token kh\u00f4ng thu\u1ed9c workflow cho c\u00e1c phi\u00ean Claude v\u1eabn c\u00f2n transcript tr\u00ean \u0111\u0129a v\u00e0 \u0111\u01b0a c\u00e1c baseline n\u00e9n ng\u1eef c\u1ea3nh v\u1ec1 0, \u0111\u1ed3ng th\u1eddi gi\u1eef nguy\u00ean c\u00e1c h\u00e0ng workflow v\u00e0 Codex. \u0110\u00e2y l\u00e0 b\u1ea3n s\u1eeda m\u1ed9t l\u1ea7n cho c\u01a1 s\u1edf d\u1eef li\u1ec7u b\u1ecb th\u1ed5i ph\u1ed3ng do c\u00e1ch c\u1ed9ng usage theo t\u1eebng b\u1ea3n ghi tr\u01b0\u1edbc \u0111\u00e2y; h\u00e3y d\u1eebng Dashboard tr\u01b0\u1edbc khi ch\u1ea1y",
    "Import historical Claude Code sessions from <code>~/.claude</code> with deep JSONL extraction (API errors, turn durations, thinking blocks, subagent data)":
      "Nhập các phiên Claude Code trong lịch sử từ <code>~/.claude</code> với trích xuất JSONL sâu (lỗi API, thời lượng lượt, khối suy nghĩ, dữ liệu subagent)",
    "Delete all data from the database (keeps schema)":
      "Xóa toàn bộ dữ liệu khỏi cơ sở dữ liệu (giữ lại schema)",
    "Run all server and client tests": "Chạy tất cả các bài kiểm thử của máy chủ và client",
    "Server integration tests only (Node built-in test runner)":
      "Chỉ kiểm thử tích hợp máy chủ (trình chạy kiểm thử tích hợp sẵn của Node)",
    "Client unit tests only (Vitest + Testing Library)":
      "Chỉ kiểm thử đơn vị của client (Vitest + Testing Library)",
    "Install dependencies for the local MCP package under <code>mcp/</code>":
      "Cài đặt phụ thuộc cho gói MCP cục bộ tại <code>mcp/</code>",
    "Type-check MCP source without emitting build output":
      "Kiểm tra kiểu của mã nguồn MCP mà không tạo ra kết quả build",
    "Compile MCP server into <code>mcp/build/</code>":
      "Biên dịch máy chủ MCP vào <code>mcp/build/</code>",
    "Start MCP server (stdio transport — for MCP hosts)":
      "Khởi động máy chủ MCP (truyền tải stdio — dành cho các host MCP)",
    "Start MCP HTTP+SSE server on port 8819 (Streamable HTTP + legacy SSE)":
      "Khởi động máy chủ MCP HTTP+SSE trên cổng 8819 (Streamable HTTP + SSE kiểu cũ)",
    "Start interactive MCP REPL with tab completion and colored output":
      "Khởi động MCP REPL tương tác với tính năng hoàn thành bằng tab và đầu ra có màu",
    "Run MCP server in dev mode with <code>tsx</code> (stdio)":
      "Chạy máy chủ MCP ở chế độ dev với <code>tsx</code> (stdio)",
    "Run MCP HTTP server in dev mode with <code>tsx</code>":
      "Chạy máy chủ MCP HTTP ở chế độ dev với <code>tsx</code>",
    "Run MCP REPL in dev mode with <code>tsx</code>":
      "Chạy MCP REPL ở chế độ dev với <code>tsx</code>",
    "Build MCP container image with Docker (<code>agent-dashboard-mcp:local</code>)":
      "Build image container MCP bằng Docker (<code>agent-dashboard-mcp:local</code>)",
    "Build MCP container image with Podman (<code>localhost/agent-dashboard-mcp:local</code>)":
      "Build image container MCP bằng Podman (<code>localhost/agent-dashboard-mcp:local</code>)",
    "Run MCP server unit tests": "Chạy các bài kiểm thử đơn vị của máy chủ MCP",
    "Install Electron + electron-builder under <code>desktop/</code>; rebuilds <code>better-sqlite3</code> for Electron's ABI. Preflights the native <code>better-sqlite3</code> build; prints actionable setup help (incl. a no-toolchain alternative) on failure":
      "Cài đặt Electron + electron-builder tại <code>desktop/</code>; build lại <code>better-sqlite3</code> cho ABI của Electron. Tiền kiểm tra quá trình build <code>better-sqlite3</code> gốc; in ra hướng dẫn thiết lập khả thi (bao gồm một giải pháp thay thế không cần toolchain) khi thất bại",
    "Prebuild guard + <code>tsc</code> compile of the Electron main process into <code>desktop/out/</code>":
      "Kiểm tra trước khi build + biên dịch tiến trình chính Electron bằng <code>tsc</code> vào <code>desktop/out/</code>",
    "Build, then launch the desktop app against <code>desktop/out/main.js</code>":
      "Build, sau đó khởi chạy ứng dụng desktop dựa trên <code>desktop/out/main.js</code>",
    "Desktop smoke test — spawn Electron and probe <code>/api/health</code>":
      "Kiểm thử smoke cho desktop — khởi chạy Electron và thăm dò <code>/api/health</code>",
    "Build <strong>both</strong> per-arch DMGs (arm64 + x64). Correct for release — slower (packages each arch)":
      "Dựng <strong>cả hai</strong> tệp DMG cho từng kiến trúc (arm64 + x64). Đúng cho việc phát hành — chậm hơn (đóng gói riêng từng kiến trúc)",
    "Build an Apple-Silicon-only DMG — fast (~1 min), recommended for a single machine":
      "Build một DMG chỉ dành cho Apple Silicon — nhanh (~1 phút), khuyến nghị cho một máy duy nhất",
    "Build an Intel-only DMG — fast (macOS host)":
      "Build một DMG chỉ dành cho Intel — nhanh (host macOS)",
    "Build one merged universal DMG (arm64 + x86_64 in a single file). Optional, slowest — not what the release ships":
      "Tạo một DMG universal hợp nhất (arm64 + x86_64 trong một tệp). Tùy chọn, chậm nhất — không phải thứ bản phát hành đi kèm",
    "Build the Windows NSIS installer <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> (Windows host)":
      "Build trình cài đặt Windows NSIS <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> (host Windows)",
    "Build the no-install portable <code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code> (Windows host)":
      "Build phiên bản portable không cần cài <code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code> (host Windows)",
    "Regenerate <code>desktop/assets/icon.ico</code> from <code>icon.png</code> (PowerShell + .NET; Windows host)":
      "Tạo lại <code>desktop/assets/icon.ico</code> từ <code>icon.png</code> (PowerShell + .NET; host Windows)",
    "Format all files with Prettier": "Định dạng tất cả các tệp bằng Prettier",
    "Check formatting without writing": "Kiểm tra định dạng mà không ghi",
    "Core dashboard telemetry is composed of three processes (Claude hook source, dashboard server, browser UI). When the local MCP sidecar is enabled, it integrates with the same dashboard API via stdio, HTTP+SSE, or interactive REPL transport.":
      "Phép đo từ xa cốt lõi của dashboard gồm ba tiến trình (nguồn hook Claude, máy chủ dashboard, giao diện trình duyệt). Khi sidecar MCP cục bộ được bật, nó tích hợp với cùng một dashboard API qua stdio, HTTP+SSE, hoặc phương thức truyền tải REPL tương tác.",
    "Full system architecture — Claude Code process → Hook Layer → Server → Browser":
      "Kiến trúc hệ thống đầy đủ — tiến trình Claude Code → Lớp Hook → Máy chủ → Trình duyệt",
    "Agent status transitions driven by hook events. <code>waiting</code> is a real persisted status — agents start as <code>waiting</code> and return to it after each turn. Error recovery requires active user retry (<code>UserPromptSubmit</code> or <code>PreToolUse</code>). A background watchdog detects API errors in transcripts every 15 s. The same watchdog also recovers <code>Esc</code>-cancelled turns — via either the transcript <code>[Request interrupted by user]</code> marker or the idle-working timeout when <code>Esc</code> preceded any output — and moves the session to Waiting.":
      "Các chuyển trạng thái của agent được điều khiển bởi các sự kiện hook. <code>waiting</code> là một trạng thái được lưu trữ thực sự — các agent bắt đầu ở trạng thái <code>waiting</code> và quay lại trạng thái đó sau mỗi lượt. Việc khôi phục lỗi đòi hỏi người dùng chủ động thử lại (<code>UserPromptSubmit</code> hoặc <code>PreToolUse</code>). Một watchdog chạy nền phát hiện lỗi API trong bản ghi mỗi 15 s. Cùng watchdog đó cũng khôi phục các lượt bị hủy bằng <code>Esc</code> — thông qua dấu hiệu <code>[Request interrupted by user]</code> trong bản ghi hoặc thời gian chờ làm-việc-rảnh-rỗi khi <code>Esc</code> diễn ra trước bất kỳ đầu ra nào — và chuyển phiên sang Waiting.",
    "Session status lifecycle. <code>waiting</code> is a UI overlay — persisted as <code>active</code> with <code>awaiting_input_since</code> set and <code>awaiting_reason</code> recording why (<code>session_start</code>, <code>stop</code>, <code>notification</code>, or <code>interrupted</code>). <code>SessionEnd</code> preserves error state. Error recovery requires <code>UserPromptSubmit</code> or <code>PreToolUse</code>. The watchdog also recovers <code>Esc</code>-cancelled turns (marker or idle-timeout path) and moves the session to Waiting. The dashboard surfaces the reason on every Waiting badge (hover tooltip, plus an inline chip on the Sessions table and session detail header) and in a waiting-for-input banner on the session detail page.":
      "Vòng đời trạng thái của phiên. <code>waiting</code> là một lớp phủ giao diện — được lưu trữ dưới dạng <code>active</code> với <code>awaiting_input_since</code> đã được đặt và <code>awaiting_reason</code> ghi lại lý do (<code>session_start</code>, <code>stop</code>, <code>notification</code> hoặc <code>interrupted</code>). <code>SessionEnd</code> giữ nguyên trạng thái lỗi. Việc khôi phục lỗi đòi hỏi <code>UserPromptSubmit</code> hoặc <code>PreToolUse</code>. Watchdog cũng khôi phục các lượt bị hủy bằng <code>Esc</code> (đường dấu hiệu hoặc đường chờ rảnh-rỗi) và chuyển phiên sang Waiting. Bảng điều khiển hiển thị lý do này trên mọi huy hiệu Waiting (tooltip khi di chuột, kèm chip nội dòng trên bảng Sessions và tiêu đề trang chi tiết phiên) và trong banner chờ phản hồi trên trang chi tiết phiên.",
    "Complete event ingestion from hook fire to browser re-render":
      "Quy trình thu nhận sự kiện hoàn chỉnh từ khi hook kích hoạt đến khi trình duyệt render lại",
    "Initial load + WebSocket subscription lifecycle": "Tải ban đầu + vòng đời đăng ký WebSocket",
    "Server module dependency graph": "Đồ thị phụ thuộc giữa các module của máy chủ",
    Module: "Mô-đun",
    Responsibility: "Trách nhiệm",
    "Express app setup, middleware (CORS, JSON 1MB limit), route mounting, static serving in production, HTTP server, auto-hook installation on startup":
      "Thiết lập ứng dụng Express, middleware (CORS, giới hạn JSON 1MB), gắn kết route, phục vụ tệp tĩnh trong môi trường production, máy chủ HTTP, tự động cài đặt hook khi khởi động",
    "SQLite connection, WAL/FK pragmas, schema migrations (<code >CREATE TABLE IF NOT EXISTS</code >), all prepared statements as a reusable <code>stmts</code> object. Tries <code>better-sqlite3</code> first, falls back to built-in <code>node:sqlite</code> via <code>compat-sqlite.js</code>":
      "Kết nối SQLite, các pragma WAL/FK, di trú schema (<code >CREATE TABLE IF NOT EXISTS</code >), tất cả các câu lệnh đã chuẩn bị dưới dạng một đối tượng <code>stmts</code> có thể tái sử dụng. Thử <code>better-sqlite3</code> trước, quay về <code>node:sqlite</code> tích hợp sẵn qua <code>compat-sqlite.js</code>",
    "Compatibility wrapper giving Node.js built-in <code>node:sqlite</code> (<code>DatabaseSync</code>) the same API as <code>better-sqlite3</code> — pragma, transaction, prepare. Used as automatic fallback on Node 22+":
      "Lớp bao tương thích giúp <code>node:sqlite</code> tích hợp sẵn của Node.js (<code>DatabaseSync</code>) có cùng API với <code>better-sqlite3</code> — pragma, transaction, prepare. Được dùng làm phương án dự phòng tự động trên Node 22+",
    "WebSocket server on <code>/ws</code> path, 30s ping/pong heartbeat, typed <code>broadcast(type, data)</code> function":
      "Máy chủ WebSocket trên đường dẫn <code>/ws</code>, nhịp tim ping/pong 30s, hàm <code>broadcast(type, data)</code> có kiểu",
    "Core event processing inside SQLite transactions. Auto-creates sessions/agents. Switch-case dispatch by hook type. Extracts token usage from Stop events.":
      "Xử lý sự kiện cốt lõi bên trong các giao dịch SQLite. Tự động tạo phiên/agent. Phân phối theo switch-case dựa trên loại hook. Trích xuất mức sử dụng token từ các sự kiện Stop.",
    "CRUD with pagination. GET includes agent count via LEFT JOIN. POST is idempotent on session ID.":
      "CRUD có phân trang. GET bao gồm số lượng agent qua LEFT JOIN. POST có tính bất biến trên session ID.",
    "CRUD with status/session_id filtering. PATCH broadcasts <code>agent_updated</code>.":
      "CRUD có lọc theo status/session_id. PATCH phát đi <code>agent_updated</code>.",
    "Read-only event listing with session_id filter and pagination.":
      "Danh sách sự kiện chỉ đọc với bộ lọc session_id và phân trang.",
    "Single aggregate query — total/active counts, status distributions, WS connection count.":
      "Một truy vấn tổng hợp duy nhất — số lượng tổng/đang hoạt động, phân bố trạng thái, số kết nối WS.",
    "Extended analytics — token totals, tool usage counts, daily event/session trends, agent type distribution, event type breakdown, average events per session.":
      "Phân tích mở rộng — tổng token, số lần sử dụng công cụ, xu hướng sự kiện/phiên theo ngày, phân bố loại agent, phân tích loại sự kiện, số sự kiện trung bình mỗi phiên.",
    "Model pricing CRUD (list, upsert, delete). Per-session and global cost calculation with pattern-based model matching and specificity sorting.":
      "CRUD định giá mô hình (list, upsert, delete). Tính chi phí theo phiên và toàn cục với việc so khớp mô hình dựa trên mẫu và sắp xếp theo độ cụ thể.",
    "System info (DB size, row counts, hook status, server uptime). Data export as JSON. Session cleanup (abandon stale active sessions, purge old completed sessions). Clear all data. Reset pricing to defaults. Reinstall hooks.":
      "Thông tin hệ thống (kích thước DB, số hàng, trạng thái hook, thời gian hoạt động của máy chủ). Xuất dữ liệu dưới dạng JSON. Dọn dẹp phiên (bỏ các phiên đang hoạt động đã cũ, xóa các phiên đã hoàn tất cũ). Xóa toàn bộ dữ liệu. Đặt lại định giá về mặc định. Cài đặt lại các hook.",
    "Aggregate workflow visualization data (agent orchestration, tool transitions, collaboration networks, workflow patterns, model delegation, error propagation, concurrency, session complexity, compaction impact). Accepts <code>?status=active|completed</code> filter. Per-session drill-in with agent tree, tool timeline, and events.":
      "Dữ liệu trực quan hóa luồng công việc tổng hợp (điều phối agent, chuyển tiếp công cụ, mạng lưới cộng tác, các mẫu luồng công việc, ủy quyền mô hình, lan truyền lỗi, tính đồng thời, độ phức tạp của phiên, tác động của việc nén). Chấp nhận bộ lọc <code>?status=active|completed</code>. Đi sâu theo từng phiên với cây agent, dòng thời gian công cụ và các sự kiện.",
    "React component tree": "Cây thành phần React",
    Purpose: "Mục đích",
    "<code>lib/api.ts</code>": "<code>lib/api.ts</code>",
    "Typed fetch wrapper — one method per REST endpoint. All return typed promises.":
      "Trình bao bọc fetch có kiểu — mỗi điểm cuối REST một phương thức. Tất cả đều trả về promise có kiểu.",
    "<code>lib/types.ts</code>": "<code>lib/types.ts</code>",
    "TypeScript interfaces: <code>Session</code>, <code>Agent</code>, <code>DashboardEvent</code>, <code>Stats</code>, <code>Analytics</code>, <code>WSMessage</code>, plus all workflow-related types (<code>WorkflowData</code>, <code>SessionDrillIn</code>, etc). Status config maps.":
      "Các interface TypeScript: <code>Session</code>, <code>Agent</code>, <code>DashboardEvent</code>, <code>Stats</code>, <code>Analytics</code>, <code>WSMessage</code>, cùng tất cả các kiểu liên quan đến quy trình làm việc (<code>WorkflowData</code>, <code>SessionDrillIn</code>, v.v.). Cùng các bản đồ cấu hình trạng thái.",
    "<code>lib/eventBus.ts</code>": "<code>lib/eventBus.ts</code>",
    "Set-based pub/sub. <code>subscribe(fn)</code> returns an unsubscribe function for clean useEffect teardown.":
      "Pub/sub dựa trên Set. <code>subscribe(fn)</code> trả về một hàm hủy đăng ký để dọn dẹp useEffect gọn gàng.",
    "<code>lib/format.ts</code>": "<code>lib/format.ts</code>",
    "Date/time formatting helpers — relative time, duration, ISO display.":
      "Các hàm trợ giúp định dạng ngày/giờ — thời gian tương đối, thời lượng, hiển thị ISO.",
    "<code>hooks/useWebSocket.ts</code>": "<code>hooks/useWebSocket.ts</code>",
    "Auto-reconnecting WebSocket React hook. 2-second reconnect interval. Publishes messages to eventBus.":
      "React hook WebSocket tự động kết nối lại. Khoảng thời gian kết nối lại 2 giây. Phát các tin nhắn tới eventBus.",
    "The dashboard is a Progressive Web App with its own <code>manifest.json</code> and Service Worker (<code>client/public/sw.js</code>). The landing page and wiki are also independent PWAs with separate manifests and service workers.":
      "Bảng điều khiển là một Progressive Web App với <code>manifest.json</code> và Service Worker riêng (<code>client/public/sw.js</code>). Trang đích và wiki cũng là các PWA độc lập với các manifest và service worker riêng biệt.",
    Surface: "Bề mặt",
    Manifest: "Manifest",
    "Service Worker": "Service Worker",
    Strategy: "Chiến lược",
    "<code>client/public/manifest.json</code>": "<code>client/public/manifest.json</code>",
    "<code>client/public/sw.js</code>": "<code>client/public/sw.js</code>",
    "Precaches app shell. Cache-first for static assets (JS/CSS bundles). Network-first for navigation with offline fallback. Skips <code>/api/*</code>, <code>/ws</code>, and Vite HMR. Preserves push notification handlers.":
      "Tiền lưu vỏ ứng dụng. Ưu tiên cache cho tài nguyên tĩnh (gói JS/CSS). Ưu tiên mạng cho điều hướng với phương án dự phòng ngoại tuyến. Bỏ qua <code>/api/*</code>, <code>/ws</code> và Vite HMR. Bảo toàn các trình xử lý thông báo đẩy.",
    "Landing page": "Trang đích",
    "<code>manifest.json</code> (root)": "<code>manifest.json</code> (gốc)",
    "<code>sw.js</code> (root)": "<code>sw.js</code> (gốc)",
    "Precaches HTML shell, favicon, OG image. Lazy-caches screenshot PNGs on first view. Network-first HTML, cache-first assets.":
      "Tiền lưu vỏ HTML, favicon, ảnh OG. Lưu cache lười các ảnh PNG chụp màn hình khi xem lần đầu. HTML ưu tiên mạng, tài nguyên ưu tiên cache.",
    Wiki: "Wiki",
    "<code>wiki/manifest.json</code>": "<code>wiki/manifest.json</code>",
    "<code>wiki/sw.js</code>": "<code>wiki/sw.js</code>",
    "Precaches <code>index.html</code>, <code>style.css</code>, <code>script.js</code>. Fully offline after one visit.":
      "Tiền lưu <code>index.html</code>, <code>style.css</code>, <code>script.js</code>. Hoàn toàn ngoại tuyến sau một lần truy cập.",
    'All three SWs call <code>skipWaiting()</code> on install and delete stale caches on activate (keyed by version strings like <code>dashboard-v1</code>). Manifests use SVG icons (<code>favicon.svg</code>) with <code>sizes="any"</code>. iOS standalone mode is enabled via <code>apple-mobile-web-app-capable</code> meta tags.':
      'Cả ba SW đều gọi <code>skipWaiting()</code> khi cài đặt và xóa các cache cũ khi kích hoạt (được khóa bằng các chuỗi phiên bản như <code>dashboard-v1</code>). Các manifest sử dụng biểu tượng SVG (<code>favicon.svg</code>) với <code>sizes="any"</code>. Chế độ độc lập trên iOS được bật thông qua các thẻ meta <code>apple-mobile-web-app-capable</code>.',
    "The client deliberately avoids Redux / Zustand / Context. Each page owns its data and lifecycle. WebSocket events trigger a reload or append — no complex state merging.":
      "Phía client cố ý tránh dùng Redux / Zustand / Context. Mỗi trang sở hữu dữ liệu và vòng đời của riêng nó. Các sự kiện WebSocket kích hoạt việc tải lại hoặc nối thêm — không có việc hợp nhất trạng thái phức tạp.",
    "No global store — by design": "Không có kho lưu trữ toàn cục — đây là chủ ý thiết kế",
    "There is no cross-page shared state. Each page fetches and owns exactly the data it displays. This simplifies debugging and avoids stale-closure hazards that are common with global stores in long-running WebSocket apps.":
      "Không có trạng thái chia sẻ giữa các trang. Mỗi trang lấy và sở hữu chính xác dữ liệu mà nó hiển thị. Điều này đơn giản hóa việc gỡ lỗi và tránh được các nguy cơ stale-closure thường gặp với các kho lưu trữ toàn cục trong những ứng dụng WebSocket chạy lâu dài.",
    Index: "Chỉ mục",
    Table: "Bảng",
    "Column(s)": "Cột",
    "Fast agent lookup by session": "Tra cứu nhanh agent theo phiên",
    "Kanban column queries": "Truy vấn cột Kanban",
    "Session detail event list": "Danh sách sự kiện chi tiết phiên",
    "Filter events by type": "Lọc sự kiện theo loại",
    "Activity feed ordering": "Sắp xếp luồng hoạt động",
    "Status filter on sessions page": "Bộ lọc trạng thái trên trang phiên",
    "Default sort order": "Thứ tự sắp xếp mặc định",
    Pragma: "Pragma",
    Value: "Giá trị",
    Rationale: "Lý do",
    "Concurrent reads during writes. Far better for read-heavy dashboards.":
      "Đọc đồng thời trong khi ghi. Tốt hơn nhiều cho các bảng điều khiển nặng về đọc.",
    "Referential integrity — prevents orphaned agents/events.":
      "Toàn vẹn tham chiếu — ngăn chặn các agent/sự kiện mồ côi.",
    "Wait up to 5s for write lock instead of failing immediately under load.":
      "Chờ tối đa 5s để có khóa ghi thay vì lỗi ngay lập tức khi chịu tải.",
    'All endpoints return JSON. Errors follow <code>{ "error": { "code", "message" } }</code>. The OpenAPI 3.0 spec comprehensively documents every backend route - parameters, request/response schemas, field descriptions, and examples. It is served at <code>/api/openapi.json</code> (with a committed <code>openapi.yaml</code> mirror), rendered as interactive Swagger UI at <code>/api/docs</code>, and as a clean, read-optimized ReDoc reference at <code>/api/redoc</code>. ReDoc is self-hosted, so it works fully offline.':
      'Tất cả các điểm cuối đều trả về JSON. Lỗi tuân theo định dạng <code>{ "error": { "code", "message" } }</code>. Đặc tả OpenAPI 3.0 ghi lại toàn diện mọi tuyến backend - tham số, lược đồ yêu cầu/phản hồi, mô tả từng trường và ví dụ. Nó được phục vụ tại <code>/api/openapi.json</code> (kèm bản sao <code>openapi.yaml</code> đã commit), hiển thị dưới dạng Swagger UI tương tác tại <code>/api/docs</code>, và dưới dạng tài liệu tham khảo ReDoc gọn gàng, tối ưu cho việc đọc tại <code>/api/redoc</code>. ReDoc được tự lưu trữ cục bộ nên hoạt động hoàn toàn ngoại tuyến.',
    '<span class="caption-icon">📘</span> <span>Swagger UI at <code>/api/docs</code> — auto-generated interactive playground for every REST endpoint. Try-it-out forms, request/response schema, auth headers, and curl snippets</span>':
      '<span class="caption-icon">📘</span> <span>Swagger UI tại <code>/api/docs</code> — sân chơi tương tác được tạo tự động cho mọi điểm cuối REST. Biểu mẫu dùng thử, lược đồ yêu cầu/phản hồi, tiêu đề xác thực và đoạn mã curl</span>',
    Property: "Thuộc tính",
    Path: "Đường dẫn",
    Protocol: "Giao thức",
    "Standard WebSocket (RFC 6455)": "WebSocket tiêu chuẩn (RFC 6455)",
    Heartbeat: "Nhịp tim",
    "Server pings every 30s — clients that don't pong are terminated":
      "Máy chủ ping mỗi 30s — các client không phản hồi pong sẽ bị chấm dứt",
    Reconnect: "Kết nối lại",
    "Client retries every 2 seconds on disconnect": "Client thử lại mỗi 2 giây khi mất kết nối",
    "Client WebSocket auto-reconnect state machine":
      "Máy trạng thái tự động kết nối lại WebSocket của client",
    "<code>scripts/hook-handler.js</code> is a minimal, fail-safe forwarder. It always exits 0 so it can never block Claude Code regardless of server state.":
      "<code>scripts/hook-handler.js</code> là một bộ chuyển tiếp tối giản, an toàn khi lỗi. Nó luôn thoát với mã 0 nên không bao giờ có thể chặn Claude Code bất kể trạng thái máy chủ.",
    "hook-handler.js flow — always exits 0, never blocks Claude Code":
      "Luồng hook-handler.js — luôn thoát với mã 0, không bao giờ chặn Claude Code",
    "Hook installation is idempotent — safe to run multiple times":
      "Việc cài đặt hook là bất biến (idempotent) — an toàn khi chạy nhiều lần",
    '<span class="caption-icon">📥</span> <span>Settings → Import History — rescan default paths, set a custom directory, or drag-and-drop <code>.gz</code> archives. Progress bar and result card show counts for every run</span>':
      '<span class="caption-icon">📥</span> <span>Settings → Import History — quét lại các đường dẫn mặc định, đặt thư mục tùy chỉnh, hoặc kéo-thả các tệp lưu trữ <code>.gz</code>. Thanh tiến trình và thẻ kết quả hiển thị số lượng cho mỗi lần chạy</span>',
    "The dashboard ships with a first-class <b>history importer</b> that backfills sessions, agents, events, tokens, and costs from Claude Code JSONL transcripts. Live hook ingestion and manual import share the exact same parser — <code>parseSessionFile</code> + <code>importSession</code> in <code>scripts/import-history.js</code> — which is the architectural contract that guarantees imported token totals and cost values are identical to those captured in real time. Re-imports are idempotent: session IDs are the dedup key and compaction <code>baseline_*</code> columns preserve pre-compaction token totals.":
      "Bảng điều khiển đi kèm một <b>trình nhập lịch sử</b> hạng nhất, giúp bổ sung dữ liệu phiên, tác tử, sự kiện, token và chi phí từ các bản ghi JSONL của Claude Code. Việc thu nhận qua hook trực tiếp và nhập thủ công dùng chung chính xác cùng một bộ phân tích — <code>parseSessionFile</code> + <code>importSession</code> trong <code>scripts/import-history.js</code> — đây là giao kèo kiến trúc bảo đảm tổng số token và giá trị chi phí được nhập vào giống hệt với những gì được ghi nhận theo thời gian thực. Việc nhập lại là idempotent: ID phiên là khóa khử trùng lặp và các cột nén <code>baseline_*</code> giữ lại tổng số token trước khi nén.",
    "All three modes funnel into the same parser and DB transaction — imported numbers match live capture bit-for-bit":
      "Cả ba chế độ đều dồn vào cùng một bộ phân tích và giao dịch CSDL — các con số được nhập khớp với bản ghi trực tiếp theo từng bit",
    "Upload path: multipart → safe extract → walk → parse → import — every temp dir reclaimed in <code>finally</code>":
      "Đường dẫn tải lên: multipart → giải nén an toàn → duyệt → phân tích → nhập — mọi thư mục tạm đều được thu hồi trong <code>finally</code>",
    "The <code>baseline_*</code> columns make cost monotonic under re-imports — compacted sessions retain pre-compaction usage for billing":
      "Các cột <code>baseline_*</code> giúp chi phí đơn điệu khi nhập lại — các phiên đã nén vẫn giữ lại mức sử dụng trước khi nén để tính phí",
    Layout: "Bố cục",
    Example: "Ví dụ",
    Handling: "Cách xử lý",
    "Default Claude Code": "Claude Code mặc định",
    "Session transcript — extracts tokens, compactions, tool uses, turn durations":
      "Bản ghi phiên — trích xuất token, các lần nén, lượt dùng công cụ, thời lượng từng lượt",
    "Default subagent": "Tác tử con mặc định",
    "Paired with parent on discovery via <code>findSessionSubagents</code>":
      "Được ghép với tác tử cha khi phát hiện thông qua <code>findSessionSubagents</code>",
    "Alternative subagent": "Tác tử con thay thế",
    "Paired with parent on discovery (second layout probed automatically)":
      "Được ghép với tác tử cha khi phát hiện (bố cục thứ hai được dò tự động)",
    "Orphan subagent": "Tác tử con mồ côi",
    "No parent JSONL in source, but <code>sid</code> exists in DB":
      "Không có JSONL cha trong nguồn, nhưng <code>sid</code> tồn tại trong CSDL",
    "<code>importFromDirectory</code> probes both layouts; attaches if the parent is found":
      "<code>importFromDirectory</code> dò cả hai bố cục; gắn kết nếu tìm thấy tác tử cha",
    "Flat JSONL drop": "Thả JSONL phẳng",
    "Recognized as a loose session transcript": "Được nhận diện như một bản ghi phiên rời rạc",
    Archives: "Tệp lưu trữ",
    "Extracted into a per-request temp dir, then walked by the same importer":
      "Được giải nén vào một thư mục tạm cho mỗi yêu cầu, rồi được cùng trình nhập duyệt qua",
    "Single-file gzip": "gzip tệp đơn",
    "Gunzipped in streaming mode with running byte-counter size cap":
      "Được giải nén gzip ở chế độ luồng với giới hạn kích thước bằng bộ đếm byte đang chạy",
    Threat: "Mối đe dọa",
    Mitigation: "Biện pháp giảm thiểu",
    "Path traversal via archive entries": "Đi xuyên đường dẫn qua các mục trong tệp lưu trữ",
    "<code>archive.safeJoin</code> resolves under the extraction root; any <code>..</code> or absolute path returns <code>null</code>":
      "<code>archive.safeJoin</code> phân giải bên dưới gốc giải nén; bất kỳ <code>..</code> hoặc đường dẫn tuyệt đối nào đều trả về <code>null</code>",
    "Zip / tar / gzip bombs": "Bom Zip / tar / gzip",
    "<code>MAX_EXTRACT_BYTES</code> (default 4 GB) enforced by running byte counter; aborts with <code>ExtractionLimitError</code> → HTTP 413":
      "<code>MAX_EXTRACT_BYTES</code> (mặc định 4 GB) được thực thi bằng bộ đếm byte đang chạy; hủy bỏ với <code>ExtractionLimitError</code> → HTTP 413",
    "Per-file upload size abuse": "Lạm dụng kích thước tải lên cho từng tệp",
    "multer <code>limits.fileSize = MAX_UPLOAD_BYTES</code> (default 1 GB)":
      "multer <code>limits.fileSize = MAX_UPLOAD_BYTES</code> (mặc định 1 GB)",
    "Too many files per request": "Quá nhiều tệp trong mỗi yêu cầu",
    "multer <code>limits.files = MAX_UPLOAD_FILES</code> (default 2000)":
      "multer <code>limits.files = MAX_UPLOAD_FILES</code> (mặc định 2000)",
    "Unsupported file types": "Loại tệp không được hỗ trợ",
    "<code>fileFilter</code> drops them early and reports them in <code>rejected_files[]</code>":
      "<code>fileFilter</code> loại bỏ chúng sớm và báo cáo chúng trong <code>rejected_files[]</code>",
    "Concurrent upload temp-dir collisions": "Xung đột thư mục tạm khi tải lên đồng thời",
    "Per-request temp dir on <code>req._ccamUploadDir</code>; created in multer <code>destination</code>, reclaimed in <code>finally</code>":
      "Thư mục tạm cho mỗi yêu cầu trên <code>req._ccamUploadDir</code>; được tạo trong multer <code>destination</code>, được thu hồi trong <code>finally</code>",
    "Arbitrary absolute path on <code>scan-path</code>":
      "Đường dẫn tuyệt đối tùy ý trên <code>scan-path</code>",
    "Validated: must be absolute (after <code>~</code> expansion), exist, and be a directory":
      "Được kiểm tra: phải là tuyệt đối (sau khi mở rộng <code>~</code>), tồn tại và là một thư mục",
    "Relative / traversal paths on <code>scan-path</code>":
      "Đường dẫn tương đối / đi xuyên trên <code>scan-path</code>",
    "Rejected with <code>INVALID_INPUT</code>": "Bị từ chối với <code>INVALID_INPUT</code>",
    "Maximum size per uploaded file on <code>/api/import/upload</code>":
      "Kích thước tối đa cho mỗi tệp tải lên trên <code>/api/import/upload</code>",
    "Maximum files per upload request": "Số tệp tối đa cho mỗi yêu cầu tải lên",
    "Ceiling on total uncompressed bytes from any single archive (zip-bomb defense)":
      "Trần cho tổng số byte chưa nén từ bất kỳ tệp lưu trữ đơn lẻ nào (phòng vệ bom zip)",
    "Every import emits <code>import.progress</code> messages on <code>/ws</code>. Messages are throttled to at most one every ~150 ms to avoid flooding the channel on multi-thousand-session imports; the terminal <code>complete</code> and <code>error</code> frames are never throttled.":
      "Mỗi lần nhập đều phát ra các thông điệp <code>import.progress</code> trên <code>/ws</code>. Các thông điệp được điều tiết tối đa một thông điệp mỗi ~150 ms để tránh làm ngập kênh khi nhập hàng nghìn phiên; các khung kết thúc <code>complete</code> và <code>error</code> không bao giờ bị điều tiết.",
    "Phases: <code>start</code> → <code>scan</code> → <code>extract</code> (upload only) → <code>parse</code> → <code>complete</code>, with <code>error</code> / <code>extract_error</code> replacing <code>complete</code> on failure.":
      "Các giai đoạn: <code>start</code> → <code>scan</code> → <code>extract</code> (chỉ khi tải lên) → <code>parse</code> → <code>complete</code>, với <code>error</code> / <code>extract_error</code> thay thế <code>complete</code> khi thất bại.",
    "In addition to dashboard telemetry, this project includes a production-grade local MCP server and complete extension scaffolding for both Claude Code and Codex. This gives agents a richer local tool surface while keeping all execution local-first. The MCP server supports three transport modes: stdio for host integration, HTTP+SSE for remote clients, and an interactive REPL for operator debugging.":
      "Ngoài dữ liệu đo từ xa của bảng điều khiển, dự án này còn bao gồm một máy chủ MCP cục bộ cấp sản xuất và bộ khung mở rộng hoàn chỉnh cho cả Claude Code và Codex. Điều này mang lại cho các tác tử một bề mặt công cụ cục bộ phong phú hơn trong khi vẫn giữ mọi thực thi theo nguyên tắc cục bộ trước. Máy chủ MCP hỗ trợ ba chế độ truyền tải: stdio để tích hợp với máy chủ chính, HTTP+SSE cho các máy khách từ xa, và một REPL tương tác để người vận hành gỡ lỗi.",
    '<span class="caption-icon">🔧</span> MCP Server REPL — interactive tool invocation terminal with colored JSON output, argument prompts, error formatting, and session-aware context for rapid testing':
      '<span class="caption-icon">🔧</span> REPL máy chủ MCP — thiết bị đầu cuối gọi công cụ tương tác với đầu ra JSON có màu, lời nhắc tham số, định dạng lỗi và ngữ cảnh nhận biết phiên để kiểm thử nhanh',
    "Local extension architecture: host instructions + skills + multi-transport MCP sidecar":
      "Kiến trúc mở rộng cục bộ: chỉ dẫn cho máy chủ chính + kỹ năng + sidecar MCP đa truyền tải",
    "The <code>mcp/</code> package exposes dashboard-oriented tools for AI agents across three transport modes. Mutation and destructive operations are policy-gated by environment variables and disabled by default. HTTP mode serves both Streamable HTTP (protocol 2025-11-25) and legacy SSE (protocol 2024-11-05). REPL mode provides tab-completed interactive tool invocation with colored output and JSON syntax highlighting.":
      "Gói <code>mcp/</code> cung cấp các công cụ hướng tới bảng điều khiển cho các tác tử AI qua ba chế độ truyền tải. Các thao tác thay đổi và phá hủy được kiểm soát theo chính sách bằng các biến môi trường và bị tắt theo mặc định. Chế độ HTTP phục vụ cả Streamable HTTP (giao thức 2025-11-25) và SSE cũ (giao thức 2024-11-05). Chế độ REPL cung cấp việc gọi công cụ tương tác có hoàn tất bằng Tab với đầu ra có màu và làm nổi bật cú pháp JSON.",
    Component: "Thành phần",
    Location: "Vị trí",
    Notes: "Ghi chú",
    "MCP source": "Mã nguồn MCP",
    "TypeScript server, tools, policy guards, transport layer, CLI UI":
      "Máy chủ TypeScript, các công cụ, bộ bảo vệ chính sách, lớp truyền tải, CLI UI",
    "MCP build output": "Đầu ra build của MCP",
    "Compiled JavaScript runtime for all transport modes":
      "Runtime JavaScript đã biên dịch cho tất cả các chế độ truyền tải",
    "MCP docs": "Tài liệu MCP",
    "Tool catalog, architecture diagrams, host integration examples, REPL guide":
      "Danh mục công cụ, sơ đồ kiến trúc, ví dụ tích hợp máy chủ chính, hướng dẫn REPL",
    "Transport layer": "Lớp truyền tải",
    "HTTP+SSE server, interactive REPL, tool handler collector":
      "Máy chủ HTTP+SSE, REPL tương tác, bộ thu thập trình xử lý công cụ",
    "CLI UI": "CLI UI",
    "ANSI banner, colors, formatter with tables, boxes, JSON highlighting":
      "Biểu ngữ ANSI, màu sắc, bộ định dạng với bảng, hộp, làm nổi bật JSON",
    "Runtime commands": "Lệnh runtime",
    "Start MCP in stdio, HTTP+SSE, or REPL mode (production or dev)":
      "Khởi động MCP ở chế độ stdio, HTTP+SSE hoặc REPL (sản xuất hoặc phát triển)",
    Target: "Mục tiêu",
    Files: "Tệp",
    "Claude Code": "Claude Code",
    "Persistent project instructions + path-scoped coding rules":
      "Chỉ dẫn dự án bền vững + quy tắc lập trình theo phạm vi đường dẫn",
    "Claude Code Skills": "Kỹ năng Claude Code",
    "Reusable workflows (onboarding, shipping, MCP ops, live debugging)":
      "Các quy trình có thể tái sử dụng (làm quen, phát hành, vận hành MCP, gỡ lỗi trực tiếp)",
    "Claude Code Subagents": "Tác tử con Claude Code",
    "Specialized reviewers for backend, frontend, and MCP code paths":
      "Các trình duyệt xét chuyên biệt cho các đường mã backend, frontend và MCP",
    "Codex Base Instructions": "Chỉ dẫn cơ sở Codex",
    "Project-wide guidance + execution policy defaults":
      "Hướng dẫn toàn dự án + các mặc định chính sách thực thi",
    "Codex Skills": "Kỹ năng Codex",
    "Task-specific skills aligned to this repository":
      "Các kỹ năng đặc thù theo tác vụ được căn chỉnh với kho lưu trữ này",
    "Codex Agents": "Tác tử Codex",
    "Reusable custom-agent templates for implementation and review":
      "Các mẫu tác tử tùy chỉnh có thể tái sử dụng cho việc triển khai và rà soát",
    Role: "Vai trò",
    "Receives Claude hook payloads over stdin and forwards them to dashboard API":
      "Nhận payload hook của Claude qua stdin và chuyển tiếp chúng đến API bảng điều khiển",
    "Writes/updates hook registration in <code>~/.claude/settings.json</code>":
      "Ghi/cập nhật đăng ký hook trong <code>~/.claude/settings.json</code>",
    "Batch history importer used by server startup auto-import, the <code>/api/import/*</code> routes, and the <code>import-history</code> CLI. Exposes <code>importAllSessions()</code> for the default projects dir and the generalized <code>importFromDirectory(dbModule, rootDir, {onProgress})</code> which walks any directory recursively, classifies session vs subagent JSONLs (probes both <code>&lt;proj&gt;/&lt;sid&gt;/subagents/*</code> and <code>&lt;proj&gt;/subagents/&lt;sid&gt;/*</code> layouts), and funnels everything through the shared <code>parseSessionFile</code> + <code>importSession</code> pipeline — identical to live ingest. <b>Re-import is fully incremental</b>: a per-event-type high-water mark (<code>MAX(created_at) GROUP BY event_type</code> for the session) drives <code>ts &gt; cutoff[type]</code> dedup for Stop / PostToolUse / TurnDuration / ToolError, so long-running sessions whose transcripts grow across multiple days keep receiving new events on every re-run. <code>sessions.ended_at</code> is rolled forward when the JSONL has progressed past the stored value, and message-count metadata is refreshed on every pass. Session-ID dedup and <code>baseline_*</code> preservation keep token totals stable. Extracts tokens, API errors, turn durations, thinking blocks, usage extras, and per-subagent breakdowns":
      "Trình nhập lịch sử hàng loạt được dùng bởi tính năng tự động nhập khi khởi động máy chủ, các route <code>/api/import/*</code> và CLI <code>import-history</code>. Cung cấp <code>importAllSessions()</code> cho thư mục dự án mặc định và hàm tổng quát <code>importFromDirectory(dbModule, rootDir, {onProgress})</code> duyệt đệ quy bất kỳ thư mục nào, phân loại JSONL phiên với JSONL tác tử con (dò cả hai bố cục <code>&lt;proj&gt;/&lt;sid&gt;/subagents/*</code> và <code>&lt;proj&gt;/subagents/&lt;sid&gt;/*</code>), và đưa mọi thứ qua pipeline dùng chung <code>parseSessionFile</code> + <code>importSession</code> — giống hệt với việc thu nạp trực tiếp. <b>Việc nhập lại hoàn toàn tăng dần</b>: một mốc nước cao theo từng loại sự kiện (<code>MAX(created_at) GROUP BY event_type</code> cho phiên) điều khiển việc khử trùng lặp <code>ts &gt; cutoff[type]</code> cho Stop / PostToolUse / TurnDuration / ToolError, nên các phiên chạy dài có bản ghi tăng trưởng qua nhiều ngày vẫn tiếp tục nhận sự kiện mới ở mỗi lần chạy lại. <code>sessions.ended_at</code> được dời về phía trước khi JSONL đã tiến quá giá trị đã lưu, và siêu dữ liệu đếm tin nhắn được làm mới ở mỗi lượt. Việc khử trùng lặp theo ID phiên và giữ lại <code>baseline_*</code> giữ cho tổng số token ổn định. Trích xuất token, lỗi API, thời lượng lượt, khối suy nghĩ, các phần phụ về sử dụng và phân tích chi tiết theo từng tác tử con",
    "Express router for Import History. Four endpoints: <code>GET /api/import/guide</code> (OS-aware instructions + default-dir stats), <code>POST /api/import/rescan</code> (default <code>~/.claude/projects</code>), <code>POST /api/import/scan-path</code> (arbitrary absolute dir with <code>~</code> expansion), <code>POST /api/import/upload</code> (multer multipart). Each request uses a per-request temp dir reclaimed in <code>finally</code>. Progress broadcast as throttled <code>import.progress</code> WebSocket messages. Limits tunable via <code>CCAM_IMPORT_MAX_BYTES</code>, <code>CCAM_IMPORT_MAX_FILES</code>, <code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>":
      "Router Express cho Nhập Lịch sử. Bốn endpoint: <code>GET /api/import/guide</code> (hướng dẫn nhận biết hệ điều hành + thống kê thư mục mặc định), <code>POST /api/import/rescan</code> (mặc định <code>~/.claude/projects</code>), <code>POST /api/import/scan-path</code> (thư mục tuyệt đối tùy ý có mở rộng <code>~</code>), <code>POST /api/import/upload</code> (multer multipart). Mỗi yêu cầu dùng một thư mục tạm riêng theo yêu cầu được thu hồi trong <code>finally</code>. Tiến độ được phát đi dưới dạng các tin nhắn WebSocket <code>import.progress</code> bị tiết lưu. Các giới hạn có thể điều chỉnh qua <code>CCAM_IMPORT_MAX_BYTES</code>, <code>CCAM_IMPORT_MAX_FILES</code>, <code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>",
    "Safe archive extraction: <code>.zip</code> via <code>adm-zip</code>, <code>.tar</code>/<code>.tar.gz</code>/<code>.tgz</code> via <code>tar</code>, plain <code>.gz</code> streaming via <code>zlib</code>. Every entry validated through <code>safeJoin</code> which rejects absolute paths and <code>..</code> traversal before any bytes are written. Enforces a hard <code>MAX_EXTRACT_BYTES</code> cap (default 4 GB) with <code>ExtractionLimitError</code> surfaced as HTTP 413 — defense against zip/tar/gzip bombs":
      "Giải nén kho lưu trữ an toàn: <code>.zip</code> qua <code>adm-zip</code>, <code>.tar</code>/<code>.tar.gz</code>/<code>.tgz</code> qua <code>tar</code>, <code>.gz</code> thuần qua luồng <code>zlib</code>. Mọi mục đều được xác thực qua <code>safeJoin</code>, hàm này từ chối các đường dẫn tuyệt đối và việc duyệt <code>..</code> trước khi ghi bất kỳ byte nào. Áp đặt một mức trần cứng <code>MAX_EXTRACT_BYTES</code> (mặc định 4 GB) với <code>ExtractionLimitError</code> được phơi bày dưới dạng HTTP 413 — phòng vệ chống lại các bom zip/tar/gzip",
    "Loads deterministic demo data for testing and demos":
      "Nạp dữ liệu demo có tính tất định cho việc kiểm thử và trình diễn",
    "Removes persisted rows while preserving schema":
      "Loại bỏ các hàng đã lưu giữ trong khi vẫn bảo toàn lược đồ",
    "The Agent Monitor ships with an official Claude Code plugin marketplace containing ten production-ready plugins (53 skills, 14 agents, 30 slash commands, 3 CLI tools, 3 hook configs, and 1 MCP server). These extend Claude Code with skills, agents, hooks, CLI tools, and MCP integration — all grounded in the real data model (token tracking with compaction baselines, cost calculation via pattern-matched pricing rules, workflow intelligence with 11 datasets per session, and session metadata including thinking blocks, turn counts, and inference geography).":
      "Agent Monitor đi kèm một chợ plugin Claude Code chính thức chứa mười plugin sẵn sàng cho sản xuất (53 kỹ năng, 14 agent, 30 lệnh gạch chéo, 3 công cụ CLI, 3 cấu hình hook và 1 máy chủ MCP). Chúng mở rộng Claude Code bằng các kỹ năng, tác tử, hook, công cụ CLI và tích hợp MCP — tất cả đều dựa trên mô hình dữ liệu thực tế (theo dõi token với các đường cơ sở nén, tính toán chi phí qua các quy tắc định giá khớp mẫu, trí tuệ quy trình với 11 tập dữ liệu mỗi phiên, và siêu dữ liệu phiên bao gồm khối suy nghĩ, số lượt và vị trí địa lý suy luận).",
    Plugin: "Plugin",
    Skills: "Kỹ năng",
    Agent: "Agent",
    "CLI Tools": "Công cụ CLI",
    Focus: "Trọng tâm",
    "Token usage (4 types + baselines), cost via pricing engine, daily trends, productivity scoring":
      "Sử dụng token (4 loại + đường cơ sở), chi phí qua công cụ định giá, xu hướng hằng ngày, chấm điểm năng suất",
    "Standup reports, sprint tracking, workflow optimization via 11 workflow intelligence datasets":
      "Báo cáo standup, theo dõi sprint, tối ưu quy trình qua 11 tập dữ liệu trí tuệ quy trình",
    "Session debugging, hook diagnostics, data export (JSON/CSV), system health":
      "Gỡ lỗi phiên, chẩn đoán hook, xuất dữ liệu (JSON/CSV), tình trạng hệ thống",
    "Pattern detection via tool flow transitions, anomaly alerting, optimization, session comparison":
      "Phát hiện mẫu qua các chuyển tiếp luồng công cụ, cảnh báo bất thường, tối ưu hóa, so sánh phiên",
    "Budget guardrails: set budgets, forecast week/month-end spend, cost-threshold alerts, model-routing savings (fail-safe Stop hook)":
      "Lan can ngân sách: đặt ngân sách, dự báo chi tiêu cuối tuần/cuối tháng, cảnh báo ngưỡng chi phí, tiết kiệm nhờ định tuyến mô hình (Stop hook an toàn khi lỗi)",
    "Session forensics: search, timeline, transcript replay, per-cwd rollup, cleanup":
      "Pháp y phiên: tìm kiếm, dòng thời gian, phát lại transcript, tổng hợp theo cwd, dọn dẹp",
    "Multi-agent orchestration &amp; fleet intelligence: DAG map, delegation audit, concurrency, error propagation, fleet runs (11-dataset workflow intelligence API)":
      "Điều phối đa agent &amp; trí tuệ đội nhóm: bản đồ DAG, kiểm toán ủy quyền, đồng thời, lan truyền lỗi, lần chạy đội nhóm (API trí tuệ quy trình 11 tập dữ liệu)",
    "Reliability &amp; SLOs: error scan, API-error report, hook-failure audit, SLO check, regression alert":
      "Độ tin cậy &amp; SLO: quét lỗi, báo cáo lỗi API, kiểm toán lỗi hook, kiểm tra SLO, cảnh báo hồi quy",
    "Claude Code config &amp; memory governance: config audit, memory review, skill/MCP/hook inventory (via the Config Explorer API)":
      "Quản trị cấu hình &amp; bộ nhớ của Claude Code: kiểm toán cấu hình, rà soát bộ nhớ, kiểm kê kỹ năng/MCP/hook (qua Config Explorer API)",
    "Dashboard connector with MCP integration and one-line metric summaries":
      "Bộ kết nối bảng điều khiển với tích hợp MCP và tóm tắt chỉ số một dòng",
    "Each plugin follows the official Claude Code plugin specification. The marketplace manifest at <code>.claude-plugin/marketplace.json</code> catalogs all ten plugins. Each plugin directory contains:":
      "Mỗi plugin tuân theo đặc tả plugin chính thức của Claude Code. Bản kê chợ tại <code>.claude-plugin/marketplace.json</code> liệt kê tất cả mười plugin. Mỗi thư mục plugin chứa:",
    "All plugins query the Agent Monitor API at <code>http://localhost:4820</code>. Key capabilities they leverage:":
      "Tất cả plugin truy vấn API Agent Monitor tại <code>http://localhost:4820</code>. Các khả năng chính mà chúng tận dụng:",
    Capability: "Khả năng",
    Details: "Chi tiết",
    "Token tracking": "Theo dõi token",
    "4 types (input, output, cache_read, cache_write) + 4 compaction baselines per model per session":
      "4 loại (input, output, cache_read, cache_write) + 4 đường cơ sở nén mỗi mô hình mỗi phiên",
    "Cost calculation": "Tính toán chi phí",
    "<code>(tokens / 1M) × rate_per_mtok</code> for each type; longest pattern match wins":
      "<code>(tokens / 1M) × rate_per_mtok</code> cho mỗi loại; khớp mẫu dài nhất thắng",
    "Session metadata": "Siêu dữ liệu phiên",
    "Event types": "Loại sự kiện",
    "Workflow intelligence": "Trí tuệ quy trình",
    "11 datasets: stats, orchestration (DAG), toolFlow, effectiveness, patterns, modelDelegation, errorPropagation, concurrency, complexity, compaction, cooccurrence":
      "11 tập dữ liệu: stats, orchestration (DAG), toolFlow, effectiveness, patterns, modelDelegation, errorPropagation, concurrency, complexity, compaction, cooccurrence",
    "Agent hierarchy": "Phân cấp tác tử",
    "Recursive parent/child tree with subagent_type, depth tracking via recursive CTE":
      "Cây cha/con đệ quy với subagent_type, theo dõi độ sâu qua CTE đệ quy",
    '📖 Full documentation: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/docs/PLUGINS.md"><code>docs/plugins.md</code></a>':
      '📖 Tài liệu đầy đủ: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/docs/PLUGINS.md"><code>docs/plugins.md</code></a>',
    '<span class="caption-icon">🖥️</span> Statusline — always-visible bar showing context window usage, token counts, active model, git branch, and session ID. Configurable segments with theme support':
      '<span class="caption-icon">🖥️</span> Statusline — thanh luôn hiển thị cho thấy mức sử dụng cửa sổ ngữ cảnh, số lượng token, mô hình đang hoạt động, nhánh git và ID phiên. Các phân đoạn có thể cấu hình với hỗ trợ chủ đề',
    "The <code>statusline/</code> directory contains a standalone CLI statusline for Claude Code — completely independent of the web dashboard. It renders a color-coded bar at the bottom of the Claude Code terminal showing context window usage, per-direction token counts, session cost in USD, and git branch.":
      "Thư mục <code>statusline/</code> chứa một statusline CLI độc lập cho Claude Code — hoàn toàn tách biệt với bảng điều khiển web. Nó hiển thị một thanh được mã hóa màu ở cuối terminal Claude Code cho thấy mức sử dụng cửa sổ ngữ cảnh, số lượng token theo từng chiều, chi phí phiên tính bằng USD, và nhánh git.",
    Segment: "Phân đoạn",
    Source: "Nguồn",
    "Color Logic": "Logic màu",
    "Always cyan": "Luôn màu lục lam",
    "Always green": "Luôn màu lục",
    "Always yellow, <code>~</code> prefix for home":
      "Luôn màu vàng, tiền tố <code>~</code> cho thư mục home",
    "Always magenta, hidden outside git repos": "Luôn màu đỏ tươi, ẩn bên ngoài các kho git",
    "Green &lt; 50%, Yellow 50–79%, Red ≥ 80%": "Lục &lt; 50%, Vàng 50–79%, Đỏ ≥ 80%",
    "Green <code>↑</code> input, cyan <code>↓</code> output, dim <code>c</code> cache reads":
      "Lục <code>↑</code> đầu vào, lục lam <code>↓</code> đầu ra, mờ <code>c</code> cho các lần đọc cache",
    "Green &lt; $5, Yellow $5–$20, Red ≥ $20 (shown on API and subscription plans)":
      "Lục &lt; $5, Vàng $5–$20, Đỏ ≥ $20 (hiển thị trên các gói API và đăng ký)",
    "Add this to <code>~/.claude/settings.json</code>:":
      "Thêm nội dung này vào <code>~/.claude/settings.json</code>:",
    "No dependencies required": "Không cần phụ thuộc nào",
    "The statusline uses only Python 3.6+ stdlib (<code>sys</code>, <code>json</code>, <code>os</code>, <code>subprocess</code>). It fails silently on empty input or JSON errors and never blocks Claude Code.":
      "Statusline chỉ dùng thư viện chuẩn Python 3.6+ (<code>sys</code>, <code>json</code>, <code>os</code>, <code>subprocess</code>). Nó thất bại một cách im lặng khi đầu vào rỗng hoặc lỗi JSON và không bao giờ chặn Claude Code.",
    '<span class="caption-icon">🔌</span> Sidebar — live health, analytics, and deep navigation links':
      '<span class="caption-icon">🔌</span> Thanh bên — tình trạng trực tiếp, phân tích và các liên kết điều hướng sâu',
    "The <b>Claude Code Agent Monitor</b> is a premium, high-fidelity extension designed to minimize context switching for AI engineers. It brings the full power of the dashboard directly into VS Code, allowing you to monitor complex subagent orchestration without ever leaving your active code file.":
      "<b>Claude Code Agent Monitor</b> là một tiện ích mở rộng cao cấp, độ trung thực cao được thiết kế để giảm thiểu việc chuyển đổi ngữ cảnh cho các kỹ sư AI. Nó đưa toàn bộ sức mạnh của bảng điều khiển trực tiếp vào VS Code, cho phép bạn giám sát việc điều phối subagent phức tạp mà không cần rời khỏi tập tin mã đang hoạt động.",
    "A dedicated Activity Bar view that performs background polling every 5 seconds. Includes a real-time <b>Agent Health</b> monitor tracking all 5 states (Working, Connected, Idle, Completed, Error) with native VS Code theme-aware icons and colors.":
      "Một chế độ xem Activity Bar chuyên dụng thực hiện thăm dò nền mỗi 5 giây. Bao gồm một bộ giám sát <b>Agent Health</b> thời gian thực theo dõi cả 5 trạng thái (Working, Connected, Idle, Completed, Error) với biểu tượng và màu sắc gốc nhận biết theo chủ đề của VS Code.",
    "Aggregates data from multiple API endpoints to display high-signal metrics directly in the sidebar: <ul style=\"margin-top: 8px; color: var(--text-muted); font-size: 13px; list-style-type: '→ '; padding-left: 15px;\"> <li><b>Token Consumption</b>: Scaled tracking from 1k to 1.0B+ tokens.</li> <li><b>Live Cost Estimates</b>: Automatic USD cost calculation based on model pricing rules.</li> <li><b>Event Frequency</b>: Total events, daily sessions, and subagent spawning rates.</li> </ul>":
      "Tổng hợp dữ liệu từ nhiều điểm cuối API để hiển thị các chỉ số có tín hiệu cao trực tiếp trong thanh bên: <ul style=\"margin-top: 8px; color: var(--text-muted); font-size: 13px; list-style-type: '→ '; padding-left: 15px;\"> <li><b>Token Consumption</b>: Theo dõi theo tỷ lệ từ 1k đến hơn 1.0B tokens.</li> <li><b>Live Cost Estimates</b>: Tự động tính chi phí bằng USD dựa trên các quy tắc định giá của mô hình.</li> <li><b>Event Frequency</b>: Tổng số sự kiện, các phiên hằng ngày và tốc độ sinh ra subagent.</li> </ul>",
    "<b>Token Consumption</b>: Scaled tracking from 1k to 1.0B+ tokens.":
      "<b>Token Consumption</b>: Theo dõi theo tỷ lệ từ 1k đến hơn 1.0B tokens.",
    "<b>Live Cost Estimates</b>: Automatic USD cost calculation based on model pricing rules.":
      "<b>Live Cost Estimates</b>: Tự động tính chi phí bằng USD dựa trên các quy tắc định giá của mô hình.",
    "<b>Event Frequency</b>: Total events, daily sessions, and subagent spawning rates.":
      "<b>Event Frequency</b>: Tổng số sự kiện, các phiên hằng ngày và tốc độ sinh ra subagent.",
    "Renders the full React application within a native webview tab. Supports <b>Deep Linking</b>: one-click jump from the sidebar directly to specific views like the <i>Kanban Board</i>, <i>Analytics Hub</i>, or your <i>Last 10 Sessions</i>.":
      "Kết xuất toàn bộ ứng dụng React trong một tab webview gốc. Hỗ trợ <b>Deep Linking</b>: nhảy một cú nhấp từ thanh bên trực tiếp đến các chế độ xem cụ thể như <i>Kanban Board</i>, <i>Analytics Hub</i>, hoặc <i>Last 10 Sessions</i> của bạn.",
    "Seamlessly scans ports <code>5173</code> (Vite Dev) and <code>4820</code> (Production) on localhost. Automatically toggles between <b>Online</b> and <b>Offline</b> modes in the sidebar as you start or stop your local server.":
      "Quét liền mạch các cổng <code>5173</code> (Vite Dev) và <code>4820</code> (Production) trên localhost. Tự động chuyển đổi giữa chế độ <b>Online</b> và <b>Offline</b> trong thanh bên khi bạn khởi động hoặc dừng máy chủ cục bộ của mình.",
    "<strong>Zero-Config Setup</strong>": "<strong>Thiết lập không cần cấu hình</strong>",
    "The extension is designed to be plug-and-play. Once your server is running, the extension automatically discovers the API and begins streaming telemetry — no manual URL configuration required.":
      "Tiện ích mở rộng được thiết kế để cắm-và-chạy. Khi máy chủ của bạn đang chạy, tiện ích sẽ tự động phát hiện API và bắt đầu truyền phát dữ liệu đo từ xa — không cần cấu hình URL thủ công.",
    '📖 Full developer guide: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/vscode-extension/README.md"><code>vscode-extension/README.md</code></a>':
      '📖 Hướng dẫn đầy đủ cho nhà phát triển: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/vscode-extension/README.md"><code>vscode-extension/README.md</code></a>',
    "The dashboard ships as an optional <strong>native desktop application</strong> — a <code>desktop/</code> workspace that wraps the existing server and client into a macOS <code>.app</code> (distributed as a <code>.dmg</code>) and a Windows <code>.exe</code> (an NSIS installer plus a no-install portable build) you install once and forget. <code>desktop/</code> is a sibling workspace to <code>client/</code>, <code>server/</code>, <code>mcp/</code>, and <code>vscode-extension/</code>, built with <strong>Electron 35</strong>. It <strong>embeds the Express server in-process</strong> — it <code>require()</code>s <code>server/index.js</code> directly in the same Node runtime as the Electron main process (no child process, no IPC) — and renders the already-built React client in a <code>BrowserWindow</code>. Everything you see in the browser at <code>localhost:4820</code> lives inside this window, with native OS lifecycle on top.":
      "Bảng điều khiển còn được phát hành dưới dạng một <strong>ứng dụng máy tính để bàn gốc</strong> tùy chọn — một không gian làm việc <code>desktop/</code> đóng gói máy chủ và máy khách hiện có thành một <code>.app</code> của macOS (phân phối dưới dạng <code>.dmg</code>) và một <code>.exe</code> của Windows (một trình cài đặt NSIS cộng với một bản dựng di động không cần cài đặt) mà bạn chỉ cần cài đặt một lần rồi quên đi. <code>desktop/</code> là một không gian làm việc ngang hàng với <code>client/</code>, <code>server/</code>, <code>mcp/</code> và <code>vscode-extension/</code>, được xây dựng bằng <strong>Electron 35</strong>. Nó <strong>nhúng máy chủ Express trong tiến trình</strong> — nó <code>require()</code> trực tiếp <code>server/index.js</code> trong cùng một runtime Node với tiến trình chính của Electron (không có tiến trình con, không có IPC) — và kết xuất máy khách React đã được xây dựng sẵn trong một <code>BrowserWindow</code>. Mọi thứ bạn thấy trong trình duyệt tại <code>localhost:4820</code> đều nằm bên trong cửa sổ này, với vòng đời gốc của hệ điều hành ở bên trên.",
    '<span class="caption-icon">🍎🪟</span> <span>The full dashboard, natively on macOS <strong>and</strong> Windows — same React client, same Express server, real <code>BrowserWindow</code>. Menu-bar / notification-area (tray) icon included. Shipped as a macOS DMG and a Windows EXE (macOS shown) — see <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a>.</span>':
      '<span class="caption-icon">🍎🪟</span> <span>Bảng điều khiển đầy đủ, chạy gốc trên macOS <strong>và</strong> Windows — cùng một máy khách React, cùng một máy chủ Express, một <code>BrowserWindow</code> thực sự. Bao gồm biểu tượng thanh menu / khu vực thông báo (khay). Được phát hành dưới dạng DMG cho macOS và EXE cho Windows (hình minh họa là macOS) — xem <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a>.</span>',
    '<span class="caption-icon">🪟</span> <span>The same dashboard as a native <strong>Windows</strong> app — real <code>BrowserWindow</code> with the native Windows window menu, live Activity Feed, and the Tabby companion. A notification-area (system tray) icon sits beside the clock for quick access.</span>':
      '<span class="caption-icon">🪟</span> <span>Cùng một bảng điều khiển dưới dạng một ứng dụng <strong>Windows</strong> gốc — một <code>BrowserWindow</code> thực sự với menu cửa sổ gốc của Windows, Activity Feed trực tiếp và bạn đồng hành Tabby. Một biểu tượng khu vực thông báo (khay hệ thống) nằm cạnh đồng hồ để truy cập nhanh.</span>',
    "<strong>One-line mental model</strong>": "<strong>Mô hình tư duy một dòng</strong>",
    "<em>Electron is a window onto the same code.</em> The desktop app does not reimplement the dashboard — it hosts the exact server and client the standalone deployment runs. The only change outside <code>desktop/</code> is a behavior-preserving refactor of <code>server/index.js</code>: its post-listen bootstrap was extracted into an exported <code>startBackgroundServices()</code> so the embedded server runs exactly what <code>node server/index.js</code> runs.":
      "<em>Electron là một cửa sổ nhìn vào cùng một bộ mã.</em> Ứng dụng máy tính để bàn không triển khai lại bảng điều khiển — nó lưu trữ chính xác máy chủ và máy khách mà bản triển khai độc lập chạy. Thay đổi duy nhất bên ngoài <code>desktop/</code> là một lần tái cấu trúc giữ nguyên hành vi của <code>server/index.js</code>: phần khởi động sau khi lắng nghe của nó được tách ra thành một hàm <code>startBackgroundServices()</code> được xuất ra, để máy chủ nhúng chạy đúng những gì <code>node server/index.js</code> chạy.",
    "The Electron main process hosts the embedded server <em>and</em> manages the window, tray, and menus. The renderer is just Chromium loading <code>http://127.0.0.1:&lt;port&gt;</code> — the same origin a normal browser would use.":
      "Tiến trình chính của Electron lưu trữ máy chủ nhúng <em>và</em> quản lý cửa sổ, khay và các menu. Bộ kết xuất chỉ đơn giản là Chromium đang tải <code>http://127.0.0.1:&lt;port&gt;</code> — cùng một origin mà một trình duyệt thông thường sẽ sử dụng.",
    "The desktop app embeds the Express server in-process — no child process, no IPC":
      "Ứng dụng máy tính để bàn nhúng máy chủ Express trong tiến trình — không có tiến trình con, không có IPC",
    "An always-on tray icon — the macOS menu bar (a tinted template glyph) or the Windows notification area (the colored <code>icon.ico</code>). A single click (left or right) opens a dropdown with a <strong>live status snapshot</strong> queried straight from SQLite at click time — server port, active sessions, working agents, events today — followed by <strong>Open Dashboard</strong>, <strong>Open in Browser</strong>, <strong>Restart Server</strong>, <strong>Show Logs</strong>, <strong>Open at Login</strong> (toggle), and <strong>Quit</strong>. The snapshot rows are clickable — they open the dashboard. The menu is rebuilt on each open so every value is current.":
      "Một biểu tượng khay luôn bật — thanh menu macOS (một biểu tượng mẫu được tô màu) hoặc khu vực thông báo Windows (<code>icon.ico</code> có màu). Một cú nhấp đơn (trái hoặc phải) mở ra một danh sách thả xuống với một <strong>ảnh chụp trạng thái trực tiếp</strong> được truy vấn thẳng từ SQLite tại thời điểm nhấp — cổng máy chủ, các phiên đang hoạt động, các agent đang làm việc, các sự kiện hôm nay — tiếp theo là <strong>Open Dashboard</strong>, <strong>Open in Browser</strong>, <strong>Restart Server</strong>, <strong>Show Logs</strong>, <strong>Open at Login</strong> (chuyển đổi) và <strong>Quit</strong>. Các hàng ảnh chụp có thể nhấp được — chúng mở bảng điều khiển. Menu được dựng lại mỗi lần mở để mọi giá trị đều là hiện thời.",
    "A standard native application menu — <code>About</code>, <code>Open at Login</code>, <code>File</code>, <code>Edit</code>, <code>View</code>, <code>Window</code>, <code>Help</code> — with <code>⌘R</code> / <code>Ctrl+R</code> wired to <em>View ▸ reload</em>. External links open in the system browser, never inside Electron. The <code>File ▸ Open Dashboard</code> item (<code>⌘1</code>) is macOS-only; on Windows/Linux the window-attached menu can't reopen a hidden window, so reopen from the tray's <strong>Open Dashboard</strong>.":
      "Một menu ứng dụng gốc tiêu chuẩn — <code>About</code>, <code>Open at Login</code>, <code>File</code>, <code>Edit</code>, <code>View</code>, <code>Window</code>, <code>Help</code> — với <code>⌘R</code> / <code>Ctrl+R</code> được nối với <em>View ▸ reload</em>. Các liên kết bên ngoài mở trong trình duyệt hệ thống, không bao giờ bên trong Electron. Mục <code>File ▸ Open Dashboard</code> (<code>⌘1</code>) chỉ có trên macOS; trên Windows/Linux, menu gắn với cửa sổ không thể mở lại một cửa sổ đã ẩn, vì vậy hãy mở lại từ <strong>Open Dashboard</strong> của khay.",
    "Flip <em>Open at Login</em> in the tray or app menu — both platforms go through Electron's first-party <code>app.*LoginItemSettings</code> API. On macOS it registers via the modern <code>SMAppService</code> API and appears under <strong>System Settings → General → Login Items</strong>; on Windows it writes a per-user <code>HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run</code> entry, visible in <strong>Task Manager → Startup</strong>. When the app is launched at login it starts tray-only, with no window jumping into view (on Windows the login launch is detected via a <code>--ccam-hidden</code> argument).":
      "Bật <em>Open at Login</em> trong khay hoặc menu ứng dụng — cả hai nền tảng đều đi qua API <code>app.*LoginItemSettings</code> bên thứ nhất của Electron. Trên macOS, nó đăng ký thông qua API <code>SMAppService</code> hiện đại và xuất hiện trong <strong>System Settings → General → Login Items</strong>; trên Windows, nó ghi một mục <code>HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run</code> theo từng người dùng, hiển thị trong <strong>Task Manager → Startup</strong>. Khi ứng dụng được khởi chạy lúc đăng nhập, nó khởi động chỉ ở dạng khay, không có cửa sổ nào bật lên (trên Windows, lần khởi chạy khi đăng nhập được phát hiện qua một đối số <code>--ccam-hidden</code>).",
    'Closing the window hides it — the embedded server keeps running, the tray icon stays, and the dock / taskbar icon stays too (a clickable "still alive" indicator). <strong>Quit</strong> (<code>⌘Q</code> / <code>Ctrl+Q</code>, app menu, or tray → Quit) pops a confirmation modal — press the Quit button or hit <code>⌘Q</code> / <code>Ctrl+Q</code> a second time to skip the prompt — and only then does the embedded server shut down, closing SQLite cleanly with a WAL checkpoint and removing this PID\'s entry from the discovery file.':
      'Đóng cửa sổ chỉ ẩn nó đi — máy chủ nhúng vẫn tiếp tục chạy, biểu tượng khay vẫn ở đó, và biểu tượng dock / thanh tác vụ cũng vẫn còn (một chỉ báo "vẫn còn sống" có thể nhấp). <strong>Quit</strong> (<code>⌘Q</code> / <code>Ctrl+Q</code>, menu ứng dụng, hoặc khay → Quit) bật lên một hộp thoại xác nhận — nhấn nút Quit hoặc nhấn <code>⌘Q</code> / <code>Ctrl+Q</code> lần thứ hai để bỏ qua lời nhắc — và chỉ khi đó máy chủ nhúng mới tắt, đóng SQLite một cách sạch sẽ bằng một WAL checkpoint và xóa mục của PID này khỏi tập tin phát hiện.',
    "Launch the desktop app and <code>npm run dev</code> at the same time and both stay real-time. Each server appends its <code>{port, pid, startedAt}</code> entry to <code>~/.claude/.agent-dashboard.json</code> on startup; the Claude Code hook handler reads that list and fan-outs every event to every live entry in parallel. Stale entries self-evict via a PID liveness check on read, so a crashed server can't misroute events to a dead port.":
      "Khởi chạy ứng dụng máy tính để bàn và <code>npm run dev</code> cùng một lúc và cả hai đều giữ thời gian thực. Mỗi máy chủ thêm mục <code>{port, pid, startedAt}</code> của nó vào <code>~/.claude/.agent-dashboard.json</code> khi khởi động; trình xử lý hook của Claude Code đọc danh sách đó và phân phối song song mọi sự kiện đến mọi mục còn sống. Các mục cũ tự loại bỏ thông qua một kiểm tra tính sống của PID khi đọc, vì vậy một máy chủ bị sập không thể định tuyến sai các sự kiện đến một cổng đã chết.",
    "Double-launching the app just focuses the existing window — no second server, no port collision, on every platform. The lock is acquired via <code>requestSingleInstanceLock()</code> before any server boots.":
      "Khởi chạy ứng dụng hai lần chỉ tập trung vào cửa sổ hiện có — không có máy chủ thứ hai, không có xung đột cổng, trên mọi nền tảng. Khóa được giành lấy thông qua <code>requestSingleInstanceLock()</code> trước khi bất kỳ máy chủ nào khởi động.",
    "On its first owned-server boot the app auto-installs the Claude Code hooks into <code>~/.claude/settings.json</code> and starts the background services (update scheduler, config watcher, orphaned-run reconciliation) — so an install-only user (DMG or EXE) gets events flowing without ever running <code>npm run install-hooks</code> from a checkout.":
      "Trong lần khởi động máy chủ do nó sở hữu đầu tiên, ứng dụng tự động cài đặt các hook của Claude Code vào <code>~/.claude/settings.json</code> và khởi động các dịch vụ nền (trình lập lịch cập nhật, trình theo dõi cấu hình, đối soát các lần chạy mồ côi) — vì vậy một người dùng chỉ cài đặt (DMG hoặc EXE) có được luồng sự kiện mà không bao giờ phải chạy <code>npm run install-hooks</code> từ một bản checkout.",
    "Two packaging realities — a read-only application bundle / install directory and (on macOS) the minimal <code>PATH</code> a Finder-launched app inherits — are handled automatically so installs survive updates and the <strong>Run Claude</strong> feature works out of the box on both macOS and Windows.":
      "Hai thực tế về đóng gói — một bộ ứng dụng / thư mục cài đặt chỉ đọc và (trên macOS) <code>PATH</code> tối thiểu mà một ứng dụng được khởi chạy từ Finder thừa hưởng — được xử lý tự động để các bản cài đặt tồn tại qua các bản cập nhật và tính năng <strong>Run Claude</strong> hoạt động ngay lập tức trên cả macOS và Windows.",
    "<strong>Your data survives reinstalls and updates</strong>":
      "<strong>Dữ liệu của bạn tồn tại qua các lần cài đặt lại và cập nhật</strong>",
    "The SQLite database and VAPID keys live in a per-user app-data directory <em>outside</em> the application bundle / install dir — <code>~/Library/Application Support/Claude Code Monitor/data/</code> on macOS, <code>%APPDATA%\\Claude Code Monitor\\data\\</code> on Windows. <code>server-host.ts</code> points <code>DASHBOARD_DATA_DIR</code> at that per-user directory on boot. Because a packaged, code-signed, or app-translocated bundle is read-only, older builds that stored the database inside the bundle broke History Import; with the data directory now in app-data, your imported history and events persist across app reinstalls and updates (the Windows NSIS uninstaller keeps this data by default). After upgrading from a pre-fix build, re-run <strong>Import History → Rescan</strong> once to bridge the one-time gap.":
      "Cơ sở dữ liệu SQLite và các khóa VAPID nằm trong một thư mục dữ liệu ứng dụng theo từng người dùng <em>bên ngoài</em> bộ ứng dụng / thư mục cài đặt — <code>~/Library/Application Support/Claude Code Monitor/data/</code> trên macOS, <code>%APPDATA%\\Claude Code Monitor\\data\\</code> trên Windows. <code>server-host.ts</code> trỏ <code>DASHBOARD_DATA_DIR</code> tới thư mục theo từng người dùng đó khi khởi động. Vì một bộ ứng dụng đã được đóng gói, ký mã, hoặc bị di dời (app-translocated) là chỉ đọc, các bản dựng cũ lưu cơ sở dữ liệu bên trong bộ ứng dụng đã làm hỏng History Import; với thư mục dữ liệu giờ nằm trong app-data, lịch sử và các sự kiện bạn đã nhập vẫn tồn tại qua các lần cài đặt lại và cập nhật ứng dụng (trình gỡ cài đặt NSIS của Windows mặc định giữ lại dữ liệu này). Sau khi nâng cấp từ một bản dựng trước khi sửa lỗi, hãy chạy lại <strong>Import History → Rescan</strong> một lần để khắc phục khoảng trống một lần đó.",
    "<strong>The <code>claude</code> CLI is found automatically</strong>":
      "<strong><code>claude</code> CLI được tìm thấy tự động</strong>",
    "A Finder- or Dock-launched macOS app inherits only launchd's minimal <code>PATH</code>, not your login shell's. At startup <code>shell-path.ts</code> recovers the user's login-shell <code>PATH</code> so the <strong>Run Claude</strong> feature can locate and spawn the <code>claude</code> CLI. (On Windows the process already inherits the user <code>PATH</code>, so no recovery step is needed.) If it still cannot be found, make sure <code>claude</code> is a real executable on your <code>PATH</code> — a shell alias or function cannot be spawned — and check the <code>user PATH resolved</code> line in the desktop log (<code>~/Library/Logs/Claude Code Monitor/desktop.log</code> on macOS, <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> on Windows).":
      "Một ứng dụng macOS được khởi chạy từ Finder hoặc Dock chỉ thừa hưởng <code>PATH</code> tối thiểu của launchd, chứ không phải của login shell của bạn. Khi khởi động, <code>shell-path.ts</code> khôi phục <code>PATH</code> của login shell người dùng để tính năng <strong>Run Claude</strong> có thể định vị và khởi chạy <code>claude</code> CLI. (Trên Windows, tiến trình đã thừa hưởng <code>PATH</code> của người dùng, nên không cần bước khôi phục nào.) Nếu vẫn không tìm thấy, hãy đảm bảo <code>claude</code> là một tập tin thực thi thực sự trên <code>PATH</code> của bạn — một bí danh hoặc hàm của shell không thể được khởi chạy — và kiểm tra dòng <code>user PATH resolved</code> trong nhật ký máy tính để bàn (<code>~/Library/Logs/Claude Code Monitor/desktop.log</code> trên macOS, <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> trên Windows).",
    "On launch the Electron main process picks a free port. If a healthy dashboard server already answers <code>/api/health</code> on port <code>4820</code> (for example, you ran <code>npm start</code> in a terminal), the app <strong>adopts</strong> that server instead of starting a second one — no double-binding, no SQLite contention. An adopted server is not owned by the app, so quitting leaves it running.":
      "Khi khởi chạy, tiến trình chính của Electron chọn một cổng trống. Nếu đã có một máy chủ bảng điều khiển khỏe mạnh trả lời <code>/api/health</code> trên cổng <code>4820</code> (ví dụ, bạn đã chạy <code>npm start</code> trong một terminal), ứng dụng <strong>tiếp nhận</strong> máy chủ đó thay vì khởi động một máy chủ thứ hai — không ràng buộc trùng, không tranh chấp SQLite. Một máy chủ được tiếp nhận không thuộc sở hữu của ứng dụng, vì vậy việc thoát sẽ để nó tiếp tục chạy.",
    Step: "Bước",
    "Port choice": "Lựa chọn cổng",
    Adopt: "Tiếp nhận",
    "A healthy server already on <code>4820</code> is adopted as-is":
      "Một máy chủ khỏe mạnh đã có sẵn trên <code>4820</code> được tiếp nhận nguyên trạng",
    Preferred: "Ưu tiên",
    "<code>4820</code> when free": "<code>4820</code> khi trống",
    Fallback: "Dự phòng",
    "The first free port in <code>4821</code>–<code>4829</code>":
      "Cổng trống đầu tiên trong khoảng <code>4821</code>–<code>4829</code>",
    "Last resort": "Phương án cuối cùng",
    "A random high port when all of the above are taken":
      "Một cổng cao ngẫu nhiên khi tất cả các cổng trên đều bị chiếm",
    "Three ways to obtain the desktop app — the latest GitHub Release (best for most users), a per-commit CI artifact (fresher than the latest release), or a local build.":
      "Có ba cách để lấy ứng dụng máy tính để bàn — GitHub Release mới nhất (tốt nhất cho hầu hết người dùng), một CI artifact theo từng commit (mới hơn bản phát hành mới nhất), hoặc một bản dựng cục bộ.",
    'Open <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg> Releases → latest </a> and download the asset for your platform. The macOS and Windows Desktop CI jobs auto-publish a new <code>vX.Y.Z</code> release every time the version in <code>package.json</code> is bumped on <code>master</code>, so this link always points at the current build. Releases are public — no GitHub sign-in required.':
      'Mở <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg> Releases → latest </a> và tải xuống tài nguyên cho nền tảng của bạn. Các tác vụ CI Desktop của macOS và Windows tự động phát hành một bản <code>vX.Y.Z</code> mới mỗi khi phiên bản trong <code>package.json</code> được nâng trên <code>master</code>, vì vậy liên kết này luôn trỏ tới bản dựng hiện tại. Các bản phát hành là công khai — không cần đăng nhập GitHub.',
    Platform: "Nền tảng",
    Asset: "Tài nguyên",
    "macOS (Apple Silicon)": "macOS (Apple Silicon)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-arm64.dmg</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-arm64.dmg</code>",
    "Drag the <code>.app</code> into <code>/Applications</code>":
      "Kéo <code>.app</code> vào <code>/Applications</code>",
    "macOS (Intel)": "macOS (Intel)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64.dmg</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64.dmg</code>",
    "Windows (installer)": "Windows (trình cài đặt)",
    "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>":
      "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>",
    "NSIS installer — per-user, no admin elevation":
      "Trình cài đặt NSIS — theo từng người dùng, không cần nâng quyền quản trị",
    "Windows (portable)": "Windows (bản di động)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>",
    "Run without installing": "Chạy mà không cần cài đặt",
    'Want a build straight off the tip of <code>master</code>, ahead of the next tagged release? Every green run of the <code>🍎 macOS Desktop (DMG)</code> job on <code>macos-latest</code> uploads both per-arch DMGs as the <code>ClaudeCodeMonitor-dmg</code> workflow artifact, and the <code>🪟 Windows Desktop (EXE)</code> job on <code>windows-latest</code> uploads the installer + portable EXEs as the <code>ClaudeCodeMonitor-win</code> artifact. Open the <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg> latest passing run </a>, scroll to its Artifacts section, and download <code>ClaudeCodeMonitor-dmg</code> or <code>ClaudeCodeMonitor-win</code>. (GitHub sign-in required; 14-day retention.)':
      'Muốn một bản dựng ngay từ đỉnh của <code>master</code>, trước cả bản phát hành được gắn thẻ kế tiếp? Mỗi lần chạy thành công của job <code>🍎 macOS Desktop (DMG)</code> trên <code>macos-latest</code> đều tải lên cả hai tệp DMG cho từng kiến trúc dưới dạng artifact quy trình <code>ClaudeCodeMonitor-dmg</code>, còn job <code>🪟 Windows Desktop (EXE)</code> trên <code>windows-latest</code> tải lên trình cài đặt + các EXE di động dưới dạng artifact <code>ClaudeCodeMonitor-win</code>. Mở <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg> lần chạy gần nhất đã vượt qua </a>, cuộn tới mục Artifacts của nó, rồi tải xuống <code>ClaudeCodeMonitor-dmg</code> hoặc <code>ClaudeCodeMonitor-win</code>. (Cần đăng nhập GitHub; lưu giữ 14 ngày.)',
    "From the project root, after <code>git clone</code>:":
      "Từ thư mục gốc của dự án, sau khi <code>git clone</code>:",
    "Use the arch-specific build on your own machine":
      "Hãy dùng bản dựng theo kiến trúc cụ thể trên máy của bạn",
    "<code>desktop:dmg</code> is slower because it builds the full app tree <em>twice</em> (once per architecture) and ad-hoc-signs each — emitting two separate per-arch DMGs (<code>…-arm64.dmg</code> + <code>…-x64.dmg</code>). There is no <code>@electron/universal</code> merge; the release ships the two per-arch DMGs. For running on a single Mac, use <code>desktop:dmg:arm64</code> (Apple Silicon) or <code>desktop:dmg:x64</code> (Intel) — one architecture, finishing in roughly a minute. CI runs <code>desktop:dmg</code> and uploads both DMGs as <code>ClaudeCodeMonitor-dmg</code>, so you rarely need to build them yourself.":
      "<code>desktop:dmg</code> chậm hơn vì nó dựng toàn bộ cây ứng dụng <em>hai lần</em> (mỗi kiến trúc một lần) và ký tạm thời (ad-hoc) cho từng bản — tạo ra hai tệp DMG riêng cho từng kiến trúc (<code>…-arm64.dmg</code> + <code>…-x64.dmg</code>). Không có bước hợp nhất <code>@electron/universal</code>; bản phát hành đi kèm hai tệp DMG cho từng kiến trúc này. Để chạy trên một máy Mac duy nhất, hãy dùng <code>desktop:dmg:arm64</code> (Apple Silicon) hoặc <code>desktop:dmg:x64</code> (Intel) — một kiến trúc, hoàn tất trong khoảng một phút. CI chạy <code>desktop:dmg</code> và tải lên cả hai tệp DMG dưới dạng <code>ClaudeCodeMonitor-dmg</code>, nên bạn hiếm khi cần tự dựng chúng.",
    "Double-click the downloaded <code>.dmg</code> to mount it":
      "Nhấp đúp vào tệp <code>.dmg</code> đã tải để gắn kết (mount) nó",
    "Drag <code>Claude Code Monitor.app</code> into your <code>Applications</code> folder":
      "Kéo <code>Claude Code Monitor.app</code> vào thư mục <code>Applications</code> của bạn",
    "Run <code>xattr -cr</code> on the app to get past Gatekeeper (see below)":
      "Chạy <code>xattr -cr</code> trên ứng dụng để vượt qua Gatekeeper (xem bên dưới)",
    "Open the app — the tray icon appears and the dashboard window loads":
      "Mở ứng dụng — biểu tượng khay xuất hiện và cửa sổ bảng điều khiển được nạp",
    "Gatekeeper warning on first launch": "Cảnh báo Gatekeeper ở lần khởi chạy đầu tiên",
    'The DMG is ad-hoc signed by default — that is all the project can offer without a paid Apple Developer ID. macOS warns the first time you open it (<em>"Apple could not verify…"</em>). Strip the quarantine attribute to get past it:':
      "DMG mặc định được ký tạm thời (ad-hoc) — đó là tất cả những gì dự án có thể cung cấp khi không có Apple Developer ID trả phí. macOS sẽ cảnh báo lần đầu tiên bạn mở nó (<em>“Apple could not verify…”</em>). Hãy gỡ thuộc tính cách ly (quarantine) để vượt qua nó:",
    "Alternatively, open <strong>System Settings → Privacy &amp; Security</strong>, find the blocked app, and click <em>Open Anyway</em>. Code signing and Apple notarization are opt-in for the maintainer — when configured, this warning goes away for everyone.":
      "Hoặc, mở <strong>System Settings → Privacy &amp; Security</strong>, tìm ứng dụng bị chặn, rồi nhấp <em>Open Anyway</em>. Việc ký mã và công chứng (notarization) của Apple là tùy chọn đối với người bảo trì — khi được cấu hình, cảnh báo này sẽ biến mất với tất cả mọi người.",
    "Run <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> — a per-user NSIS install (no admin), or run the <code>*-portable.exe</code> to skip installing":
      "Chạy <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> — một bản cài NSIS theo từng người dùng (không cần quản trị), hoặc chạy <code>*-portable.exe</code> để bỏ qua việc cài đặt",
    "The EXE is unsigned by default, so SmartScreen may warn — click <em>More info → Run anyway</em>":
      "EXE mặc định chưa được ký, nên SmartScreen có thể cảnh báo — nhấp <em>More info → Run anyway</em>",
    "Open from the Start menu / desktop shortcut — the notification-area (tray) icon appears and the dashboard window loads":
      "Mở từ menu Start / lối tắt trên desktop — biểu tượng vùng thông báo (khay) xuất hiện và cửa sổ bảng điều khiển được nạp",
    '<span class="caption-icon">1️⃣</span> <span>NSIS installer, step 1 — <strong>Choose Installation Options</strong>: pick per-user setup and optional shortcuts.</span>':
      '<span class="caption-icon">1️⃣</span> <span>Trình cài đặt NSIS, bước 1 — <strong>Choose Installation Options</strong>: chọn cài đặt theo từng người dùng và các lối tắt tùy chọn.</span>',
    '<span class="caption-icon">2️⃣</span> <span>NSIS installer, step 2 — <strong>Choose Install Location</strong>: defaults to <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code>, or point it anywhere.</span>':
      '<span class="caption-icon">2️⃣</span> <span>Trình cài đặt NSIS, bước 2 — <strong>Choose Install Location</strong>: mặc định là <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code>, hoặc trỏ tới bất kỳ đâu.</span>',
    '<span class="caption-icon">3️⃣</span> <span>NSIS installer, step 3 — <strong>Completing Setup</strong>: click <em>Finish</em> to launch the app and drop the tray icon in the notification area.</span>':
      '<span class="caption-icon">3️⃣</span> <span>Trình cài đặt NSIS, bước 3 — <strong>Completing Setup</strong>: nhấp <em>Finish</em> để khởi chạy ứng dụng và đặt biểu tượng khay vào vùng thông báo.</span>',
    "SmartScreen warning on first launch": "Cảnh báo SmartScreen ở lần khởi chạy đầu tiên",
    'The installer and portable EXE are <strong>unsigned</strong> by default — that is all the project can offer without a paid code-signing certificate. Windows <strong>SmartScreen</strong> may show <em>"Windows protected your PC"</em> the first time you run it; click <strong>More info → Run anyway</strong>. The installer lays the app down <strong>per-user</strong> under <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code> (and lets you choose the install directory) and sets an <code>AppUserModelId</code> (<code>com.hoangsonww.ccam.desktop</code>) so native toast notifications are attributed correctly and the window groups under one taskbar entry.':
      "Trình cài đặt và EXE di động mặc định <strong>chưa được ký</strong> — đó là tất cả những gì dự án có thể cung cấp khi không có chứng chỉ ký mã trả phí. Windows <strong>SmartScreen</strong> có thể hiển thị <em>“Windows protected your PC”</em> lần đầu tiên bạn chạy nó; nhấp <strong>More info → Run anyway</strong>. Trình cài đặt đặt ứng dụng <strong>theo từng người dùng</strong> dưới <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code> (và cho phép bạn chọn thư mục cài đặt) và thiết lập một <code>AppUserModelId</code> (<code>com.hoangsonww.ccam.desktop</code>) để các thông báo toast gốc được quy đúng nguồn và cửa sổ được nhóm dưới một mục trên thanh tác vụ.",
    "Bundle size": "Kích thước gói",
    "The DMG is roughly 80&nbsp;MB, about 250&nbsp;MB installed on disk — the standard Electron tax; the Windows installer is comparable. The app runs natively on <strong>macOS and Windows</strong>; Linux is tracked as a follow-up. Logs live at <code>~/Library/Logs/Claude Code Monitor/desktop.log</code> on macOS or <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> on Windows (reach them from the tray menu → <em>Show Logs</em>).":
      "DMG khoảng 80&nbsp;MB, khoảng 250&nbsp;MB khi đã cài trên đĩa — cái giá tiêu chuẩn của Electron; trình cài đặt Windows cũng tương đương. Ứng dụng chạy gốc trên <strong>macOS và Windows</strong>; Linux được theo dõi như một hạng mục tiếp theo. Nhật ký nằm ở <code>~/Library/Logs/Claude Code Monitor/desktop.log</code> trên macOS hoặc <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> trên Windows (truy cập chúng từ menu khay → <em>Show Logs</em>).",
    '📖 User-facing guide: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a> · architecture &amp; contributor reference: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/desktop/README.md"><code>desktop/README.md</code></a>':
      '📖 Hướng dẫn dành cho người dùng: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a> · tài liệu tham khảo kiến trúc &amp; người đóng góp: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/desktop/README.md"><code>desktop/README.md</code></a>',
    '<span class="caption-icon">⚙️</span> Settings — model pricing editor, hook installation toggle, JSON data export, session cleanup, browser notification preferences, and system info panel with DB stats':
      '<span class="caption-icon">⚙️</span> Settings — trình chỉnh sửa giá theo mô hình, công tắc cài đặt hook, xuất dữ liệu JSON, dọn dẹp phiên, tùy chọn thông báo của trình duyệt, và bảng thông tin hệ thống kèm thống kê DB',
    "The <code>/settings</code> route provides a comprehensive management interface with six sections:":
      "Tuyến <code>/settings</code> cung cấp một giao diện quản lý toàn diện gồm sáu phần:",
    "Editable table of per-model pricing rules. Each Claude model variant has its own explicit pattern (e.g., <code>claude-opus-4-6%</code>). Rates cover input, output, cache read, and cache write tokens. Each rule's editor also has a collapsible <strong>Introductory rates</strong> block — a <code>YYYY-MM-DD</code> promo cutoff plus per-category intro prices (input / output / cache-read / cache-write 5m &amp; 1h); an empty date means no promo, and any future model-launch promo needs no code change. Reset to defaults or add custom models. The section header carries an info popover (the <code>i</code> icon) that explains how rule lookup works (first matching pattern wins), the SQL-style <code>%</code> wildcard syntax with concrete examples (<code>claude-opus-4-7%</code>, <code>claude-%-haiku</code>, exact ids), and reminds the user that prices must be updated manually when Anthropic publishes new rates — already-stored sessions keep the price applied at ingest time. The CLAUDE_HOME panel and Import History flow are fully i18n-driven across en/vi/zh.":
      "Bảng có thể chỉnh sửa các quy tắc giá theo từng mô hình. Mỗi biến thể mô hình Claude có mẫu (pattern) riêng rõ ràng (ví dụ <code>claude-opus-4-6%</code>). Mức giá bao gồm token đầu vào, đầu ra, đọc cache và ghi cache. Trình chỉnh sửa của mỗi quy tắc còn có một khối <strong>Introductory rates</strong> (mức giá giới thiệu) có thể thu gọn — một mốc kết thúc khuyến mãi dạng <code>YYYY-MM-DD</code> cùng các mức giá giới thiệu theo từng danh mục (đầu vào / đầu ra / đọc cache / ghi cache 5m &amp; 1h); để trống ngày nghĩa là không có khuyến mãi, và bất kỳ khuyến mãi ra mắt mô hình nào trong tương lai đều không cần thay đổi mã. Đặt lại về mặc định hoặc thêm mô hình tùy chỉnh. Tiêu đề phần này có một popover thông tin (biểu tượng <code>i</code>) giải thích cách tra cứu quy tắc hoạt động (mẫu khớp đầu tiên thắng), cú pháp ký tự đại diện <code>%</code> kiểu SQL kèm ví dụ cụ thể (<code>claude-opus-4-7%</code>, <code>claude-%-haiku</code>, id chính xác), và nhắc người dùng rằng giá phải được cập nhật thủ công khi Anthropic công bố mức giá mới — các phiên đã lưu vẫn giữ mức giá được áp dụng tại thời điểm nhập. Bảng CLAUDE_HOME và luồng Import History được điều khiển hoàn toàn bằng i18n trên en/vi/zh.",
    "Shows per-hook installation status (SessionStart, PreToolUse, PostToolUse, Stop, SubagentStop, Notification, SessionEnd). One-click reinstall if hooks are missing or outdated. Validates paths and permissions automatically.":
      "Hiển thị trạng thái cài đặt theo từng hook (SessionStart, PreToolUse, PostToolUse, Stop, SubagentStop, Notification, SessionEnd). Cài đặt lại bằng một cú nhấp nếu hook bị thiếu hoặc lỗi thời. Tự động kiểm tra đường dẫn và quyền.",
    "View database row counts and size. Session cleanup: abandon stale active sessions after N hours, purge old completed sessions after N days. Danger zone: clear all data with confirmation dialog to prevent accidental loss.":
      "Xem số dòng và kích thước cơ sở dữ liệu. Dọn dẹp phiên: bỏ các phiên đang hoạt động bị cũ sau N giờ, xóa các phiên đã hoàn tất cũ sau N ngày. Vùng nguy hiểm: xóa toàn bộ dữ liệu kèm hộp thoại xác nhận để tránh mất mát ngoài ý muốn.",
    "Download all sessions, agents, events, token usage, and pricing rules as a single JSON file for backup or analysis. Includes full event history, model metadata, and cost breakdowns in one portable archive.":
      "Tải xuống tất cả phiên, tác nhân, sự kiện, lượng token sử dụng và quy tắc giá dưới dạng một tệp JSON duy nhất để sao lưu hoặc phân tích. Bao gồm toàn bộ lịch sử sự kiện, siêu dữ liệu mô hình và bảng phân tích chi phí trong một kho lưu trữ di động.",
    "Dedicated Health tab on the Dashboard with a composite health score (weighted from success rate, cache hit rate, error rate, and heap usage), storage engine donut chart, tool invocation frequency bars, subagent effectiveness, model token distribution, and compaction impact — all with cursor-following tooltips and 5-second auto-refresh.":
      "Tab Health chuyên dụng trên Dashboard với điểm sức khỏe tổng hợp (được tính trọng số từ tỷ lệ thành công, tỷ lệ trúng cache, tỷ lệ lỗi và mức dùng heap), biểu đồ vành khuyên về công cụ lưu trữ, các thanh tần suất gọi công cụ, hiệu quả của tác nhân con, phân bố token theo mô hình và tác động của việc nén — tất cả đều có chú giải đi theo con trỏ và tự động làm mới mỗi 5 giây.",
    "Configure native browser notifications with per-event toggles for session starts, completions, errors, and subagent spawns. Automatic permission management with test-send button and graceful fallback when denied.":
      "Cấu hình thông báo gốc của trình duyệt với các công tắc theo từng sự kiện cho việc bắt đầu phiên, hoàn tất, lỗi và sinh tác nhân con. Quản lý quyền tự động kèm nút gửi thử và phương án dự phòng nhẹ nhàng khi bị từ chối.",
    "Per-model pricing — no catch-all grouping": "Giá theo từng mô hình — không gộp chung một kiểu",
    "Each Claude model variant (e.g., Opus 4.6 vs Opus 4.1) has its own explicit pricing pattern because different model versions have different rates. The cost engine uses specificity sorting — longer patterns match before shorter ones.":
      "Mỗi biến thể mô hình Claude (ví dụ Opus 4.6 so với Opus 4.1) có mẫu giá riêng rõ ràng vì các phiên bản mô hình khác nhau có mức giá khác nhau. Bộ máy tính chi phí dùng cách sắp xếp theo độ cụ thể — các mẫu dài hơn được khớp trước các mẫu ngắn hơn.",
    "Turns the dashboard from passive viewing into active monitoring. A rules-based alerting engine evaluates the live event stream <strong>server-side</strong>, and fired alerts fan out to outbound <strong>webhook channels</strong>. Everything lives in one place — <strong>Settings → Alerts</strong> — behind a segmented control with three tabs: <strong>Rules</strong> (what triggers an alert), <strong>Channels</strong> (where alerts are delivered), and <strong>Activity</strong> (the live fired-alert feed with acknowledge / acknowledge-all).":
      "Biến bảng điều khiển từ việc xem thụ động thành giám sát chủ động. Một bộ máy cảnh báo dựa trên quy tắc đánh giá luồng sự kiện trực tiếp ở <strong>phía máy chủ</strong>, và các cảnh báo được kích hoạt sẽ tỏa ra các <strong>kênh webhook</strong> đầu ra. Mọi thứ nằm ở một nơi — <strong>Settings → Alerts</strong> — phía sau một điều khiển phân đoạn với ba tab: <strong>Rules</strong> (điều gì kích hoạt một cảnh báo), <strong>Channels</strong> (cảnh báo được gửi đến đâu) và <strong>Activity</strong> (luồng cảnh báo đã kích hoạt trực tiếp kèm xác nhận / xác nhận tất cả).",
    'Four condition types: <strong>event pattern</strong> (match <code>event_type</code> / <code>tool_name</code> / a summary substring, optionally requiring ≥ N matches within a rolling window — e.g. "5 errors in 2 minutes"), <strong>inactivity</strong> (an active session goes quiet for N minutes), <strong>status duration</strong> (an agent is stuck in <code>working</code> / <code>waiting</code> for N minutes), and <strong>token threshold</strong> (a session\'s cumulative tokens cross a limit). Each rule has a configurable <strong>cooldown</strong> that dedups repeat alerts per (rule, session, agent).':
      "Bốn loại điều kiện: <strong>mẫu sự kiện</strong> (khớp <code>event_type</code> / <code>tool_name</code> / một chuỗi con trong tóm tắt, tùy chọn yêu cầu ≥ N lần khớp trong một cửa sổ trượt — ví dụ “5 lỗi trong 2 phút”), <strong>không hoạt động</strong> (một phiên đang hoạt động im lặng trong N phút), <strong>thời lượng trạng thái</strong> (một tác nhân kẹt ở trạng thái <code>working</code> / <code>waiting</code> trong N phút), và <strong>ngưỡng token</strong> (token tích lũy của một phiên vượt qua một giới hạn). Mỗi quy tắc có một <strong>thời gian chờ (cooldown)</strong> có thể cấu hình, dùng để loại trùng các cảnh báo lặp theo từng (quy tắc, phiên, tác nhân).",
    "Event-driven rules (<code>event_pattern</code>, <code>token_threshold</code>) run on every hook ingest — <em>after</em> the transaction commits and the response is sent, fully try/catch-guarded, so alerting can never slow or fail hook delivery. Time-based rules (<code>inactivity</code>, <code>status_duration</code>) run on an unref'd 60-second sweep. Enabled rules are cached in memory and invalidated on every edit. Fired alerts persist to <code>alert_events</code> and broadcast an <code>alert_triggered</code> WebSocket message.":
      "Các quy tắc theo sự kiện (<code>event_pattern</code>, <code>token_threshold</code>) chạy mỗi khi có hook nhập vào — <em>sau khi</em> giao dịch được commit và phản hồi đã được gửi, được bảo vệ hoàn toàn bằng try/catch, nên việc cảnh báo không bao giờ có thể làm chậm hoặc làm hỏng việc gửi hook. Các quy tắc theo thời gian (<code>inactivity</code>, <code>status_duration</code>) chạy trên một lượt quét 60 giây được unref. Các quy tắc đã bật được lưu cache trong bộ nhớ và bị vô hiệu hóa sau mỗi lần chỉnh sửa. Các cảnh báo đã kích hoạt được lưu vào <code>alert_events</code> và phát đi một thông điệp WebSocket <code>alert_triggered</code>.",
    "Slack, Discord, Microsoft Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, and Pipedream — plus a generic JSON endpoint. A declarative <strong>provider registry</strong> describes each one's payload formatter, URL resolution, auth headers, and credential fields, so adding a provider is a single server-side entry that surfaces in the UI with no front-end change.":
      "Slack, Discord, Microsoft Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n và Pipedream — cùng với một điểm cuối JSON tổng quát. Một <strong>sổ đăng ký nhà cung cấp</strong> mang tính khai báo mô tả bộ định dạng payload, cách phân giải URL, các tiêu đề xác thực và các trường thông tin xác thực của mỗi nhà cung cấp, nên việc thêm một nhà cung cấp chỉ là một mục duy nhất ở phía máy chủ và sẽ xuất hiện trong giao diện mà không cần thay đổi front-end.",
    'Each delivery POSTs with an <code>AbortController</code> timeout and bounded retry/backoff (retries transport errors, 429, and 5xx — never other 4xx), then records the attempt-chain in <code>webhook_deliveries</code>. A provider can also veto a 2xx whose body signals failure (Splunk On-Call returns 200 with <code>result:"failure"</code>). Delivery is <strong>detached and fail-safe</strong> — it never throws into, slows, or blocks the alert path.':
      'Mỗi lần gửi đều thực hiện POST với một thời gian chờ <code>AbortController</code> và cơ chế thử lại/lùi (backoff) có giới hạn (thử lại với lỗi truyền tải, 429 và 5xx — không bao giờ với các 4xx khác), sau đó ghi lại chuỗi các lần thử vào <code>webhook_deliveries</code>. Một nhà cung cấp cũng có thể phủ quyết một mã 2xx mà thân phản hồi báo hiệu thất bại (Splunk On-Call trả về 200 kèm <code>result:"failure"</code>). Việc gửi là <strong>tách rời và an toàn khi lỗi</strong> — nó không bao giờ ném lỗi vào, làm chậm hay chặn đường dẫn cảnh báo.',
    "Target URLs are masked (host + last 4 chars), and secrets / credential fields (routing keys, API keys, bot tokens) plus custom-header values are redacted in every API response — the full URL and secrets are stored server-side and never leave it. Generic endpoints support optional <strong>HMAC-SHA256</strong> body signing (<code>X-Webhook-Signature</code> + <code>X-Webhook-Timestamp</code>) so receivers can verify authenticity.":
      "Các URL đích được che (host + 4 ký tự cuối), và các trường bí mật / thông tin xác thực (khóa định tuyến, khóa API, token bot) cùng các giá trị tiêu đề tùy chỉnh đều được ẩn trong mọi phản hồi API — URL đầy đủ và các bí mật được lưu ở phía máy chủ và không bao giờ rời khỏi đó. Các điểm cuối tổng quát hỗ trợ tùy chọn ký thân bằng <strong>HMAC-SHA256</strong> (<code>X-Webhook-Signature</code> + <code>X-Webhook-Timestamp</code>) để bên nhận có thể xác minh tính xác thực.",
    'Every alert-rule field has a help tooltip — the event-type, tool-name, and summary-contains fields include example chips of real hook events and built-in tool names. Each webhook provider ships a collapsible step-by-step setup guide linking to the official docs. A one-click <strong>"Send test"</strong> probe fires a synthetic alert and reports the delivery result inline, and targets can be scoped to specific rules. Fully localized (en / zh / vi / ko).':
      "Mỗi trường của quy tắc cảnh báo đều có chú giải trợ giúp — các trường event-type, tool-name và summary-contains bao gồm các chip ví dụ về sự kiện hook thật và tên công cụ tích hợp sẵn. Mỗi nhà cung cấp webhook đi kèm một hướng dẫn thiết lập từng bước có thể thu gọn, liên kết tới tài liệu chính thức. Một thăm dò <strong>“Send test”</strong> bằng một cú nhấp sẽ kích hoạt một cảnh báo tổng hợp và báo cáo kết quả gửi ngay tại chỗ, và các đích có thể được giới hạn theo các quy tắc cụ thể. Được bản địa hóa hoàn toàn (en / zh / vi / ko).",
    "Provider(s)": "Nhà cung cấp",
    "Payload format": "Định dạng payload",
    "URL / credentials": "URL / thông tin xác thực",
    "Block Kit (header + section + context)": "Block Kit (header + section + context)",
    "Rich embed": "Embed phong phú (rich embed)",
    "Adaptive Card in a Workflows <code>message</code> envelope":
      "Adaptive Card trong một phong bì <code>message</code> của Workflows",
    "Power Automate Workflows URL": "Power Automate Workflows URL",
    "Text message (basic markdown)": "Tin nhắn văn bản (markdown cơ bản)",
    "Space webhook URL": "Space webhook URL",
    "Slack-style legacy attachments": "Tệp đính kèm kiểu cũ theo phong cách Slack",
    "Bot API <code>sendMessage</code> (HTML)": "Bot API <code>sendMessage</code> (HTML)",
    "Bot token + chat ID (URL derived)": "Bot token + chat ID (URL được suy ra)",
    "Events API v2 trigger (with <code>dedup_key</code>)":
      "Kích hoạt Events API v2 (kèm <code>dedup_key</code>)",
    "Routing key (URL prefilled)": "Khóa định tuyến (URL điền sẵn)",
    "Alert API": "Alert API",
    "API key (GenieKey header) + region": "Khóa API (tiêu đề GenieKey) + vùng",
    "VictorOps REST": "VictorOps REST",
    "REST endpoint URL (key embedded)": "URL điểm cuối REST (khóa được nhúng)",
    "Stable <code>{ event, alert }</code> JSON envelope":
      "Phong bì JSON <code>{ event, alert }</code> ổn định",
    "Endpoint URL (+ optional HMAC &amp; headers)": "URL điểm cuối (+ HMAC &amp; tiêu đề tùy chọn)",
    "Additive &amp; non-blocking by design": "Mang tính bổ sung &amp; không chặn theo thiết kế",
    "Two new tables — <code>webhook_targets</code> (config; survives Clear Data like alert rules) and <code>webhook_deliveries</code> (audit log) — with no changes to existing tables, response shapes, or WebSocket message types. Webhook dispatch is fire-and-forget off the alert path, so a slow or failing endpoint can never slow or break alert firing or hook ingestion.":
      "Hai bảng mới — <code>webhook_targets</code> (cấu hình; tồn tại qua Clear Data giống như các quy tắc cảnh báo) và <code>webhook_deliveries</code> (nhật ký kiểm toán) — mà không thay đổi các bảng hiện có, hình dạng phản hồi hay các loại thông điệp WebSocket. Việc điều phối webhook là kiểu phát-rồi-quên nằm ngoài đường dẫn cảnh báo, nên một điểm cuối chậm hoặc lỗi không bao giờ có thể làm chậm hoặc phá vỡ việc kích hoạt cảnh báo hay việc nhập hook.",
    "Provider setup steps can drift":
      "Các bước thiết lập của nhà cung cấp có thể thay đổi theo thời gian",
    "Microsoft retired classic Office 365 connectors in 2025, so Teams uses an Adaptive Card delivered via Power Automate <strong>Workflows</strong>. More broadly, provider setup UIs change often — the in-app guides say so and link to each provider's official docs. Always confirm against the source.":
      "Microsoft đã ngừng các connector Office 365 cổ điển vào năm 2025, nên Teams dùng một Adaptive Card được gửi qua Power Automate <strong>Workflows</strong>. Rộng hơn, giao diện thiết lập của các nhà cung cấp thường xuyên thay đổi — các hướng dẫn trong ứng dụng cũng nói vậy và liên kết tới tài liệu chính thức của từng nhà cung cấp. Luôn xác nhận lại với nguồn gốc.",
    '<span class="caption-icon">⬆</span> Update Notifier — version comparison modal with one-click copy of the update command. No automatic self-restart; you stay in control of when upgrades happen':
      '<span class="caption-icon">⬆</span> Update Notifier — hộp thoại so sánh phiên bản với khả năng sao chép lệnh cập nhật chỉ bằng một cú nhấp. Không tự khởi động lại tự động; bạn luôn kiểm soát thời điểm nâng cấp diễn ra',
    "A detection-only subsystem that tells the user when the dashboard's git checkout is behind the canonical default branch. <strong>Branch- and fork-aware:</strong> if an <code>upstream</code> remote is configured (the standard convention for forks), it takes priority over <code>origin</code>; the chosen remote's <code>master</code> / <code>main</code> / <code>HEAD</code> is the comparison ref. The printed command adapts to the user's situation — <code>git pull --ff-only</code> only when their branch actually tracks the canonical ref, otherwise <code>git fetch</code> (with a fast-forward merge in the fork case). The server <strong>never</strong> pulls or restarts itself — the user runs the command in a terminal — so the mechanism cannot break dev sessions, pm2/systemd/launchd/Docker supervision, or leave orphaned processes.":
      "Một hệ thống con chỉ phát hiện, báo cho người dùng biết khi bản checkout git của bảng điều khiển bị tụt lại sau nhánh mặc định chính thức. <strong>Nhận biết nhánh và fork:</strong> nếu một remote <code>upstream</code> được cấu hình (quy ước tiêu chuẩn cho các fork), nó được ưu tiên hơn <code>origin</code>; <code>master</code> / <code>main</code> / <code>HEAD</code> của remote được chọn chính là tham chiếu so sánh. Lệnh được in ra thích ứng theo tình huống của người dùng — <code>git pull --ff-only</code> chỉ khi nhánh của họ thực sự theo dõi tham chiếu chính thức, ngược lại là <code>git fetch</code> (kèm một lần hợp nhất tua nhanh trong trường hợp fork). Máy chủ <strong>không bao giờ</strong> tự kéo về hay tự khởi động lại — người dùng chạy lệnh trong một terminal — nên cơ chế này không thể phá vỡ các phiên dev, sự giám sát của pm2/systemd/launchd/Docker, hay để lại các tiến trình mồ côi.",
    "The same status is available from any terminal: <code>ccam update-check</code> asks the server for the branch- and fork-aware comparison, prints the behind-by count and the copy-paste update command, and triggers the <code>update_status</code> WebSocket broadcast so any open dashboard tab refreshes at the same time.":
      "Trạng thái này cũng có thể xem từ bất kỳ terminal nào: <code>ccam update-check</code> hỏi máy chủ kết quả so sánh có nhận biết nhánh và fork, in ra số commit bị tụt lại cùng lệnh cập nhật sao chép-dán, và kích hoạt broadcast WebSocket <code>update_status</code> để mọi tab dashboard đang mở cùng làm mới.",
    "A shell-less <code>git fetch</code> with a 120-second timeout, followed by a <code>rev-list</code> against the tracked upstream. Each call runs from <code>server/lib/update-check.js</code> and returns a structured payload — never throws — so a flaky remote can&apos;t stall the dashboard.":
      "Một lệnh <code>git fetch</code> không qua shell với thời gian chờ 120 giây, theo sau là một <code>rev-list</code> đối chiếu với upstream được theo dõi. Mỗi lệnh gọi chạy từ <code>server/lib/update-check.js</code> và trả về một payload có cấu trúc — không bao giờ ném ngoại lệ — nên một remote chập chờn không thể làm treo bảng điều khiển.",
    "<code>update-scheduler.js</code> polls every five minutes with <code>.unref()</code> timers so it never blocks shutdown, de-duplicates with a fingerprint over the status payload, and announces up-to-date → behind transitions in a framed stdout block. Disable entirely with <code>DASHBOARD_UPDATE_CHECK=0</code>.":
      "<code>update-scheduler.js</code> thăm dò mỗi năm phút bằng các bộ định thời <code>.unref()</code> nên không bao giờ chặn việc tắt, khử trùng lặp bằng một dấu vân tay trên payload trạng thái, và thông báo các chuyển đổi từ cập-nhật → tụt-lại trong một khối stdout có khung. Tắt hoàn toàn bằng <code>DASHBOARD_UPDATE_CHECK=0</code>.",
    "Each status payload carries a <code>manual_command</code> shaped for the user's actual situation: <code>git pull --ff-only</code> on a tracked canonical branch, <code>git fetch &amp;&amp; git merge --ff-only</code> for forks where local tracks the wrong remote, and a plain <code>git fetch</code> on a feature branch where pulling would update the wrong branch. Install / build steps are appended only when the working tree is actually being rewritten.":
      "Mỗi payload trạng thái mang theo một <code>manual_command</code> được định hình cho tình huống thực tế của người dùng: <code>git pull --ff-only</code> trên một nhánh chính thức được theo dõi, <code>git fetch &amp;&amp; git merge --ff-only</code> cho các fork mà bản cục bộ theo dõi sai remote, và một lệnh <code>git fetch</code> thuần túy trên một nhánh tính năng nơi việc kéo về sẽ cập nhật nhầm nhánh. Các bước cài đặt / build chỉ được thêm vào khi cây làm việc thực sự đang bị ghi đè lại.",
    "A modal opens automatically when upstream is ahead; ESC or a backdrop click dismisses it. A persistent sidebar button stays in the footer — emerald when behind, amber when the last check errored — so users can always trigger a fresh check on demand.":
      "Một hộp thoại tự động mở ra khi upstream đi trước; nhấn ESC hoặc nhấp vào nền sẽ đóng nó lại. Một nút thanh bên cố định nằm ở chân trang — màu lục bảo khi tụt lại, màu hổ phách khi lần kiểm tra trước bị lỗi — nên người dùng luôn có thể kích hoạt một lần kiểm tra mới theo nhu cầu.",
    "Non-git installs, no remotes configured, offline fetches, and unresolvable upstream refs all return tagged payloads instead of throwing. The sidebar badge turns amber on fetch errors and the modal stays suppressed until a successful check arrives — no spinners, no stuck state.":
      "Các bản cài đặt phi git, không cấu hình remote nào, các lần fetch ngoại tuyến, và các tham chiếu upstream không thể phân giải đều trả về payload có gắn thẻ thay vì ném ngoại lệ. Huy hiệu ở thanh bên chuyển sang màu hổ phách khi fetch bị lỗi và hộp thoại vẫn bị ẩn cho đến khi có một lần kiểm tra thành công — không có vòng xoay, không có trạng thái kẹt.",
    "Dismissal is keyed by the upstream SHA in <code>localStorage</code>, so closing the modal silences it only for <em>that</em> commit — a newer upstream commit re-opens it automatically. Clicking the sidebar button is an explicit intent signal and clears the stored dismissal before firing a fresh check.":
      "Việc bỏ qua được đánh khóa theo SHA upstream trong <code>localStorage</code>, nên đóng hộp thoại chỉ làm im lặng nó cho <em>commit đó</em> — một commit upstream mới hơn sẽ tự động mở lại nó. Nhấp vào nút thanh bên là một tín hiệu ý định rõ ràng và sẽ xóa trạng thái bỏ qua đã lưu trước khi kích hoạt một lần kiểm tra mới.",
    "Read-only check — runs <code>git fetch</code>, compares, returns the payload.":
      "Kiểm tra chỉ-đọc — chạy <code>git fetch</code>, so sánh, trả về payload.",
    "Same check, and broadcasts <code>update_status</code> over WebSocket so every connected client re-syncs at once.":
      "Cùng một lần kiểm tra, và phát sóng <code>update_status</code> qua WebSocket để mọi máy khách đang kết nối đồng bộ lại cùng lúc.",
    "<strong>Detection-only by design</strong>": "<strong>Chỉ phát hiện theo thiết kế</strong>",
    "There is no <code>POST /api/updates/apply</code> and no in-process restart helper. A process cannot reliably replace itself without an external supervisor, and <code>npm run dev</code>, <code>npm start</code>, pm2, systemd, launchd, and Docker each need different restart logic. Detection-only keeps the mechanism portable across every supervisor and OS, and leaves the dashboard's lifecycle owned by whatever started it. The user runs the printed command in their own shell.":
      "Không có <code>POST /api/updates/apply</code> và không có trợ thủ khởi động lại trong tiến trình. Một tiến trình không thể tự thay thế chính nó một cách đáng tin cậy nếu thiếu một trình giám sát bên ngoài, và <code>npm run dev</code>, <code>npm start</code>, pm2, systemd, launchd, cùng Docker mỗi cái cần một logic khởi động lại khác nhau. Việc chỉ phát hiện giữ cho cơ chế khả chuyển trên mọi trình giám sát và hệ điều hành, và để vòng đời của bảng điều khiển thuộc về thứ đã khởi chạy nó. Người dùng chạy lệnh được in ra trong shell của chính họ.",
    '<span class="caption-icon">◈</span> Connection Status — sidebar-launched details modal with WebSocket endpoint, connection uptime, 60-second throughput sparkline, top event-type breakdown, and recent activity list':
      '<span class="caption-icon">◈</span> Connection Status — hộp thoại chi tiết khởi chạy từ thanh bên với endpoint WebSocket, thời gian kết nối hoạt động, biểu đồ tia thông lượng 60 giây, phân tách các loại sự kiện hàng đầu, và danh sách hoạt động gần đây',
    'The <strong>Live</strong> / <strong>Disconnected</strong> pill in the sidebar footer opens a small details panel about the dashboard\'s WebSocket transport. It surfaces the active <code>ws://</code> endpoint, how long the current socket has been up, total events received, the top event types as a horizontal bar chart, a 60-second throughput sparkline, and the most recent 8 events as an activity list. Cumulative stats (totals, type breakdown, recent list) persist across reloads via <code>localStorage</code> under <code>sidebar-connection-stats</code>; the rolling sparkline and "connected since" timer are intentionally ephemeral since they only make sense relative to "now". A <strong>Reset</strong> button clears everything on demand.':
      'Viên thuốc <strong>Live</strong> / <strong>Disconnected</strong> ở chân thanh bên mở ra một bảng chi tiết nhỏ về tầng vận chuyển WebSocket của bảng điều khiển. Nó hiển thị endpoint <code>ws://</code> đang hoạt động, socket hiện tại đã hoạt động bao lâu, tổng số sự kiện đã nhận, các loại sự kiện hàng đầu dưới dạng biểu đồ thanh ngang, một biểu đồ tia thông lượng 60 giây, và 8 sự kiện gần nhất dưới dạng danh sách hoạt động. Các thống kê tích lũy (tổng số, phân tách theo loại, danh sách gần đây) được giữ qua các lần tải lại thông qua <code>localStorage</code> dưới khóa <code>sidebar-connection-stats</code>; biểu đồ tia cuộn và bộ đếm thời gian "kết nối từ" được cố ý làm tạm thời vì chúng chỉ có ý nghĩa so với "bây giờ". Một nút <strong>Reset</strong> xóa mọi thứ theo nhu cầu.',
    "Implementation note: per-event state lives in <code>useRef</code> buffers on the sidebar so the WS firehose never re-renders the navigation tree — the modal does its own one-second tick to sample the refs while open. Writes are throttled (single-flight timer, 2 s window) and flushed on <code>pagehide</code> / <code>visibilitychange</code> so the latest events aren't lost to the throttle window. The modal itself is portalled to <code>document.body</code> so the sidebar's stacking context can't trap it.":
      "Ghi chú triển khai: trạng thái theo từng sự kiện nằm trong các bộ đệm <code>useRef</code> trên thanh bên nên dòng sự kiện WS dồn dập không bao giờ render lại cây điều hướng — hộp thoại tự thực hiện một nhịp mỗi giây để lấy mẫu các ref khi đang mở. Việc ghi được điều tiết (bộ định thời chạy-đơn, cửa sổ 2 giây) và được xả ra khi <code>pagehide</code> / <code>visibilitychange</code> để các sự kiện mới nhất không bị mất vào cửa sổ điều tiết. Bản thân hộp thoại được portal sang <code>document.body</code> nên ngữ cảnh xếp lớp của thanh bên không thể giam giữ nó.",
    "The entire UI ships in <strong>four languages — English, 简体中文, Tiếng Việt, and 한국어</strong> — built on <code>i18next</code> + <code>react-i18next</code> with <code>i18next-browser-languagedetector</code>. Coverage is end-to-end: every page, chart tooltip, Settings flow, Workflow narrative, Config Explorer tab, Run page, and the Alerts rule-help tooltips + webhook setup guides are translated. Switch languages from the sidebar (EN / 中文 / VI / 한국어) — the choice persists in <code>localStorage</code>.":
      "Toàn bộ giao diện được phát hành bằng <strong>bốn ngôn ngữ — English, 简体中文, Tiếng Việt và 한국어</strong> — xây dựng trên <code>i18next</code> + <code>react-i18next</code> cùng <code>i18next-browser-languagedetector</code>. Phạm vi bao phủ là đầu-cuối: mọi trang, chú giải biểu đồ, luồng Settings, tường thuật Workflow, tab Config Explorer, trang Run, cùng các chú giải trợ giúp quy tắc Alerts + hướng dẫn thiết lập webhook đều được dịch. Chuyển ngôn ngữ từ thanh bên (EN / 中文 / VI / 한국어) — lựa chọn được giữ trong <code>localStorage</code>.",
    'Translations are split into per-area JSON namespaces (<code>common</code>, <code>nav</code>, <code>dashboard</code>, <code>sessions</code>, <code>analytics</code>, <code>workflows</code>, <code>settings</code>, <code>kanban</code>, <code>run</code>, <code>ccConfig</code>, <code>alerts</code>, <code>errors</code>, <code>updates</code>) under <code>client/src/i18n/locales/&lt;lng&gt;/</code>. Components load only the namespaces they need via <code>useTranslation("…")</code>.':
      'Các bản dịch được chia thành các không gian tên JSON theo từng khu vực (<code>common</code>, <code>nav</code>, <code>dashboard</code>, <code>sessions</code>, <code>analytics</code>, <code>workflows</code>, <code>settings</code>, <code>kanban</code>, <code>run</code>, <code>ccConfig</code>, <code>alerts</code>, <code>errors</code>, <code>updates</code>) dưới <code>client/src/i18n/locales/&lt;lng&gt;/</code>. Các thành phần chỉ tải các không gian tên mà chúng cần thông qua <code>useTranslation("…")</code>.',
    "Language is detected from <code>localStorage</code> (<code>i18nextLng</code>) then the browser's <code>navigator</code> setting, and the choice is cached back to <code>localStorage</code>. <code>fallbackLng</code> is English and <code>nonExplicitSupportedLngs</code> resolves regional tags (e.g. <code>vi-VN</code> → <code>vi</code>), so any unmapped key falls back gracefully rather than rendering a raw key.":
      "Ngôn ngữ được phát hiện từ <code>localStorage</code> (<code>i18nextLng</code>) rồi đến thiết lập <code>navigator</code> của trình duyệt, và lựa chọn được lưu vào lại <code>localStorage</code>. <code>fallbackLng</code> là tiếng Anh và <code>nonExplicitSupportedLngs</code> phân giải các thẻ vùng (ví dụ <code>vi-VN</code> → <code>vi</code>), nên bất kỳ khóa nào chưa được ánh xạ đều quay lui một cách uyển chuyển thay vì hiển thị một khóa thô.",
    "Numbers, costs, dates, and relative times format against the active locale via a shared <code>getCurrentLocale()</code> helper, and plurals use i18next's <code>_one</code> / <code>_other</code> suffixes. Interpolated values (<code>{{count}}</code>, <code>{{provider}}</code>, …) keep sentences natural across languages.":
      "Số, chi phí, ngày tháng, và thời gian tương đối được định dạng theo locale đang hoạt động thông qua một trợ thủ <code>getCurrentLocale()</code> dùng chung, và số nhiều dùng các hậu tố <code>_one</code> / <code>_other</code> của i18next. Các giá trị nội suy (<code>{{count}}</code>, <code>{{provider}}</code>, …) giữ cho câu văn tự nhiên qua các ngôn ngữ.",
    "Domain terms that are proper nouns or code stay untranslated in every locale — <em>Agent</em>, <em>Subagent</em>, hook event names (<code>PostToolUse</code>), tool names (<code>Bash</code>), and webhook provider names (Slack, PagerDuty). Only the surrounding prose is localized, so instructions stay accurate.":
      "Các thuật ngữ chuyên ngành là danh từ riêng hoặc mã sẽ không được dịch trong mọi locale — <em>Agent</em>, <em>Subagent</em>, tên sự kiện hook (<code>PostToolUse</code>), tên công cụ (<code>Bash</code>), và tên nhà cung cấp webhook (Slack, PagerDuty). Chỉ phần văn xuôi xung quanh được bản địa hóa, nên các hướng dẫn vẫn chính xác.",
    "<strong>Adding a language</strong>": "<strong>Thêm một ngôn ngữ</strong>",
    "Copy <code>client/src/i18n/locales/en/</code> to a new locale folder, translate the JSON values (leaving keys and technical terms intact), then register the bundle and add the tag to <code>supportedLngs</code> in <code>client/src/i18n/index.ts</code>. Missing keys fall back to English automatically, so even a partial translation ships cleanly.":
      "Sao chép <code>client/src/i18n/locales/en/</code> sang một thư mục locale mới, dịch các giá trị JSON (giữ nguyên các khóa và thuật ngữ kỹ thuật), rồi đăng ký bundle và thêm thẻ vào <code>supportedLngs</code> trong <code>client/src/i18n/index.ts</code>. Các khóa bị thiếu sẽ tự động quay lui về tiếng Anh, nên ngay cả một bản dịch một phần cũng phát hành gọn gàng.",
    "<strong>Tabby</strong> is a cute SVG cat companion pinned to the <strong>edges of every page</strong> of the dashboard. It is always present and turns the live session stream into glanceable, ambient feedback — calm when idle, alert when something needs attention, and celebratory when a run finishes. Tabby is built entirely on the existing <code>eventBus</code> WebSocket stream: <strong>no new backend, no API key, and no new dependencies</strong>. The component lives in <code>client/src/components/Tabby/</code> and can be toggled on or off in Settings page.":
      "<strong>Tabby</strong> là một chú mèo SVG dễ thương đồng hành, được ghim vào <strong>các rìa của mọi trang</strong> trong bảng điều khiển. Nó luôn hiện diện và biến luồng phiên trực tiếp thành phản hồi mang tính nền, dễ nhìn thoáng qua — bình thản khi rảnh rỗi, cảnh giác khi có điều gì cần chú ý, và ăn mừng khi một lần chạy hoàn tất. Tabby được xây dựng hoàn toàn trên luồng WebSocket <code>eventBus</code> hiện có: <strong>không có backend mới, không có API key, và không có phụ thuộc mới</strong>. Thành phần này nằm trong <code>client/src/components/Tabby/</code> và có thể bật hoặc tắt trong trang Settings.",
    '<span class="caption-icon">📥</span> Tabby Companion — a cute SVG cat in the edges of every page, reacting in real time to the live session stream with eight distinct moods and animations, auto-surfacing speech bubbles for notable events, and serving as the gateway to a status panel and Ask box':
      '<span class="caption-icon">📥</span> Tabby Companion — một chú mèo SVG dễ thương ở các rìa của mọi trang, phản ứng theo thời gian thực với luồng phiên trực tiếp bằng tám tâm trạng và hoạt ảnh khác biệt, tự động bật lên các bong bóng thoại cho các sự kiện đáng chú ý, và đóng vai trò cổng vào một bảng trạng thái và ô Ask',
    "Tabby derives one of eight moods from the live session WebSocket stream, each with its own animation. The eyes track your cursor, and the active mood drives a distinct motion cue.":
      "Tabby suy ra một trong tám tâm trạng từ luồng WebSocket phiên trực tiếp, mỗi cái có hoạt ảnh riêng. Đôi mắt dõi theo con trỏ của bạn, và tâm trạng đang hoạt động điều khiển một tín hiệu chuyển động riêng biệt.",
    "Notable events — session started or finished, errors, and run completed — automatically surface a speech bubble. Bubbles are <strong>throttled and coalesced</strong> so bursts of events never spam you, and they can be muted on demand. Everything reflects in real time over the existing <code>eventBus</code> WebSocket channel, with no polling and no extra services.":
      "Các sự kiện đáng chú ý — phiên bắt đầu hoặc kết thúc, lỗi, và lần chạy hoàn tất — tự động làm hiện lên một bong bóng thoại. Các bong bóng được <strong>điều tiết và gộp lại</strong> nên các đợt sự kiện dồn dập không bao giờ làm phiền bạn, và chúng có thể được tắt tiếng theo nhu cầu. Mọi thứ phản ánh theo thời gian thực qua kênh WebSocket <code>eventBus</code> hiện có, không thăm dò và không có dịch vụ phụ.",
    "Click the cat — or press <code>⌘B</code> / <code>Ctrl+B</code> — to open Tabby's panel (<code>Esc</code> closes it). The panel groups a live status line, quick actions, and an Ask box.":
      "Nhấp vào chú mèo — hoặc nhấn <code>⌘B</code> / <code>Ctrl+B</code> — để mở bảng của Tabby (<code>Esc</code> đóng nó lại). Bảng này nhóm một dòng trạng thái trực tiếp, các hành động nhanh, và một ô Ask.",
    "<strong>Live status line:</strong> <em>N live · M errored · connection state</em>, updated from cached data.":
      "<strong>Dòng trạng thái trực tiếp:</strong> <em>N live · M errored · connection state</em>, được cập nhật từ dữ liệu đã lưu vào bộ nhớ đệm.",
    "<strong>Quick actions:</strong> jump to Run Claude, Activity, Sessions, or errored sessions; mute bubbles; clear alerts.":
      "<strong>Hành động nhanh:</strong> nhảy tới Run Claude, Activity, Sessions, hoặc các phiên bị lỗi; tắt tiếng bong bóng; xóa cảnh báo.",
    "<strong>Ask box:</strong> answers simple status questions locally from cached data (&ldquo;what's running&rdquo;, &ldquo;any errors&rdquo;, &ldquo;status&rdquo;).":
      "<strong>Ô Ask:</strong> trả lời tại chỗ các câu hỏi trạng thái đơn giản từ dữ liệu đã lưu vào bộ nhớ đệm (&ldquo;what's running&rdquo;, &ldquo;any errors&rdquo;, &ldquo;status&rdquo;).",
    "The Ask box answers status questions instantly and offline from cached data. For anything beyond a simple status question, Tabby hands off to the existing <strong>Run Claude</strong> page (<code>/run?prompt=...</code>) to spawn a real Claude Code session — so there is never a separate model call, key, or service to manage.":
      "Ô Ask trả lời các câu hỏi trạng thái tức thì và ngoại tuyến từ dữ liệu đã lưu vào bộ nhớ đệm. Đối với bất cứ điều gì vượt ra ngoài một câu hỏi trạng thái đơn giản, Tabby chuyển giao cho trang <strong>Run Claude</strong> hiện có (<code>/run?prompt=...</code>) để khởi tạo một phiên Claude Code thực sự — nên không bao giờ có một lệnh gọi mô hình, khóa, hay dịch vụ riêng nào phải quản lý.",
    "Fully keyboard operable: <code>⌘B</code> / <code>Ctrl+B</code> to open, <code>Esc</code> to close.":
      "Hoàn toàn thao tác được bằng bàn phím: <code>⌘B</code> / <code>Ctrl+B</code> để mở, <code>Esc</code> để đóng.",
    "Status and bubbles announce via <code>aria-live</code> for screen readers.":
      "Trạng thái và bong bóng thông báo qua <code>aria-live</code> dành cho trình đọc màn hình.",
    "Respects <code>prefers-reduced-motion</code> to calm animations.":
      "Tôn trọng <code>prefers-reduced-motion</code> để làm dịu các hoạt ảnh.",
    "Degrades gracefully to a calm, dimmed disconnected state when offline.":
      "Suy giảm một cách uyển chuyển về một trạng thái mất kết nối bình thản, mờ đi khi ngoại tuyến.",
    Endpoint: "Endpoint",
    Mood: "Tâm trạng",
    "When it appears": "Khi nào nó xuất hiện",
    Animation: "Hoạt ảnh",
    Idle: "Idle",
    "Nothing notable happening": "Không có gì đáng chú ý đang xảy ra",
    "Gentle tail flick": "Phẩy đuôi nhẹ nhàng",
    Watching: "Watching",
    "Sessions active, observing the stream": "Có phiên đang hoạt động, đang quan sát luồng",
    "Ear perk, cursor-tracking eyes": "Vểnh tai, mắt dõi theo con trỏ",
    Happy: "Happy",
    "A run completed successfully": "Một lần chạy hoàn tất thành công",
    Sparkle: "Lấp lánh",
    Worried: "Worried",
    "Something looks off": "Có gì đó trông không ổn",
    "Head bob": "Gật đầu",
    Stuck: "Stuck",
    "A session appears blocked": "Một phiên có vẻ bị chặn",
    "Shake + alert <code>!</code>": "Rung lắc + cảnh báo <code>!</code>",
    Thinking: "Thinking",
    "Work in progress": "Công việc đang diễn ra",
    Sleeping: "Sleeping",
    "Quiet for a while": "Yên ắng trong một lúc",
    Zzz: "Zzz",
    Disconnected: "Disconnected",
    "WebSocket offline": "WebSocket ngoại tuyến",
    "Calm, dimmed state": "Trạng thái bình thản, mờ đi",
    "Development vs production deployment topology":
      "Cấu trúc triển khai phát triển so với sản xuất",
    Aspect: "Khía cạnh",
    Development: "Phát triển",
    Production: "Sản xuất",
    Processes: "Tiến trình",
    "2 (Express + Vite)": "2 (Express + Vite)",
    "1 (Express only)": "1 (chỉ Express)",
    "Client URL": "URL máy khách",
    "API proxy": "Proxy API",
    "Vite proxies <code>/api</code> + <code>/ws</code> to :4820":
      "Vite chuyển tiếp <code>/api</code> + <code>/ws</code> tới :4820",
    "Same origin, no proxy": "Cùng nguồn gốc, không có proxy",
    "File watching": "Theo dõi tệp",
    "<code>node --watch</code> + Vite HMR": "<code>node --watch</code> + Vite HMR",
    None: "Không có",
    "Source maps": "Source map",
    Inline: "Nội tuyến",
    "External files": "Tệp bên ngoài",
    "<strong>A third way to run: the Desktop App (macOS &amp; Windows)</strong>":
      "<strong>Một cách thứ ba để chạy: Ứng dụng máy tính để bàn (macOS &amp; Windows)</strong>",
    'Beyond development and standalone production, the dashboard also ships as a native desktop app — a macOS <code>.app</code> and a Windows <code>.exe</code> — that embeds the same production server in-process, no terminal required. See the <a href="#desktop-app">Desktop App (macOS &amp; Windows)</a> section for download, build, and install instructions.':
      'Ngoài chế độ phát triển và sản xuất độc lập, bảng điều khiển còn được phát hành dưới dạng ứng dụng máy tính để bàn gốc — một <code>.app</code> cho macOS và một <code>.exe</code> cho Windows — nhúng cùng một máy chủ sản xuất ngay trong tiến trình, không cần terminal. Xem phần <a href="#desktop-app">Ứng dụng máy tính để bàn (macOS &amp; Windows)</a> để biết hướng dẫn tải xuống, xây dựng và cài đặt.',
    "The production image is OCI-compatible and works with both Docker and Podman. The server listens on <code>4820</code>, reads legacy Claude history from a read-only mount, and persists SQLite data under <code>/app/data</code>.":
      "Image sản xuất tương thích OCI và hoạt động với cả Docker lẫn Podman. Máy chủ lắng nghe trên <code>4820</code>, đọc lịch sử Claude cũ từ một điểm gắn kết chỉ đọc, và lưu dữ liệu SQLite vào <code>/app/data</code>.",
    "Container image build and runtime mounts":
      "Quá trình xây dựng image container và các điểm gắn kết khi chạy",
    Mount: "Điểm gắn kết",
    "Read historical Claude session files for import without modifying them":
      "Đọc các tệp phiên Claude lịch sử để nhập vào mà không sửa đổi chúng",
    "Persist the SQLite database across rebuilds and container restarts":
      "Lưu giữ cơ sở dữ liệu SQLite qua các lần xây dựng lại và khởi động lại container",
    "<strong>Hooks still run on the host</strong>": "<strong>Hooks vẫn chạy trên máy chủ</strong>",
    "Claude Code fires hooks from the host machine, not from inside the container. After the container is healthy on <code>http://localhost:4820</code>, run <code>npm run install-hooks</code> on the host so hook events post back to the containerized server.":
      "Claude Code kích hoạt các hook từ máy chủ, không phải từ bên trong container. Sau khi container hoạt động bình thường trên <code>http://localhost:4820</code>, hãy chạy <code>npm run install-hooks</code> trên máy chủ để các sự kiện hook gửi trở lại máy chủ được container hóa.",
    "A multi-stage <code>Dockerfile</code> and <code>docker-compose.yml</code> are included. Both <strong>Docker</strong> and <strong>Podman</strong> are fully supported — the image is OCI-compliant.":
      "Dự án bao gồm một <code>Dockerfile</code> nhiều giai đoạn và <code>docker-compose.yml</code>. Cả <strong>Docker</strong> lẫn <strong>Podman</strong> đều được hỗ trợ đầy đủ — image tuân thủ OCI.",
    "Read-only access to legacy session history for automatic import on startup":
      "Quyền truy cập chỉ đọc vào lịch sử phiên cũ để tự động nhập khi khởi động",
    "Persists the SQLite database across container restarts":
      "Lưu giữ cơ sở dữ liệu SQLite qua các lần khởi động lại container",
    "The Dockerfile uses three stages to minimize the final image size:":
      "Dockerfile sử dụng ba giai đoạn để giảm thiểu kích thước image cuối cùng:",
    Stage: "Giai đoạn",
    "Installs production <code>node_modules</code> on digest-pinned <code>node:24.19.0-alpine3.24</code>. <code>better-sqlite3</code> is optional — if prebuilds are unavailable, the server falls back to built-in <code>node:sqlite</code>":
      "Cài đặt <code>node_modules</code> sản xuất trên <code>node:24.19.0-alpine3.24</code> được cố định digest. <code>better-sqlite3</code> là tùy chọn — nếu không có bản dựng sẵn, máy chủ sẽ quay về dùng <code>node:sqlite</code> tích hợp sẵn",
    "Runs <code>npm ci</code> + <code>vite build</code> to produce optimized static assets":
      "Chạy <code>npm ci</code> + <code>vite build</code> để tạo ra các tài nguyên tĩnh được tối ưu hóa",
    "Clean digest-pinned <code>node:24.19.0-alpine3.24</code> with only <code>node_modules</code>, server code, and <code>client/dist</code>":
      "Bản <code>node:24.19.0-alpine3.24</code> sạch, được cố định digest, chỉ gồm <code>node_modules</code>, mã máy chủ và <code>client/dist</code>",
    "<strong>Hook note</strong>": "<strong>Lưu ý về hook</strong>",
    "Claude Code hooks run on the host, not inside the container. The containerized server receives hook events via HTTP on <code>localhost:4820</code>. Run <code>npm run install-hooks</code> on the host after starting the container.":
      "Các hook của Claude Code chạy trên máy chủ, không phải bên trong container. Máy chủ được container hóa nhận các sự kiện hook qua HTTP trên <code>localhost:4820</code>. Hãy chạy <code>npm run install-hooks</code> trên máy chủ sau khi khởi động container.",
    Metric: "Chỉ số",
    "Server startup": "Khởi động máy chủ",
    "SQLite opens instantly; schema migration is idempotent":
      "SQLite mở ngay lập tức; việc di chuyển lược đồ là bất biến (idempotent)",
    "Hook latency": "Độ trễ hook",
    "Transaction + broadcast, no async I/O beyond SQLite":
      "Giao dịch + phát sóng, không có I/O bất đồng bộ ngoài SQLite",
    "Client JS bundle": "Gói JS máy khách",
    "WebSocket latency": "Độ trễ WebSocket",
    "Local loopback, JSON serialization only": "Vòng lặp cục bộ, chỉ tuần tự hóa JSON",
    "SQLite write throughput": "Thông lượng ghi SQLite",
    "WAL mode on SSD; far exceeds any hook event rate":
      "Chế độ WAL trên SSD; vượt xa mọi tốc độ sự kiện hook",
    "Max events before slowdown": "Số sự kiện tối đa trước khi chậm lại",
    "Pagination prevents full-table scans": "Phân trang ngăn việc quét toàn bộ bảng",
    "Server memory": "Bộ nhớ máy chủ",
    "SQLite in-process, no ORM overhead": "SQLite chạy trong tiến trình, không có chi phí ORM",
    "Client memory": "Bộ nhớ máy khách",
    "React + Tailwind, minimal runtime deps":
      "React + Tailwind, phụ thuộc thời gian chạy tối thiểu",
    "Input validation": "Kiểm tra dữ liệu đầu vào",
    "Required fields checked before DB operations; CHECK constraints on status enums":
      "Các trường bắt buộc được kiểm tra trước các thao tác DB; ràng buộc CHECK trên các enum trạng thái",
    "Hook safety": "An toàn của hook",
    "Hook handler always exits 0; 5s max lifetime; uses <code>127.0.0.1</code> not external hosts":
      "Trình xử lý hook luôn thoát với mã 0; vòng đời tối đa 5s; dùng <code>127.0.0.1</code> chứ không phải các host bên ngoài",
    CORS: "CORS",
    "Restricted to loopback origins, so cross-origin pages can't read responses; no-Origin clients like curl still work":
      "Giới hạn ở các nguồn loopback, nên các trang khác nguồn không thể đọc phản hồi; các client không có Origin như curl vẫn hoạt động",
    Authentication: "Xác thực",
    "Off by default since the loopback bind is the trust boundary; set <code>DASHBOARD_TOKEN</code> to require a bearer token on every <code>/api/*</code> request and the WebSocket when exposing on a LAN.":
      "Tắt theo mặc định vì việc liên kết loopback chính là ranh giới tin cậy; đặt <code>DASHBOARD_TOKEN</code> để yêu cầu một bearer token trên mọi yêu cầu <code>/api/*</code> và WebSocket khi để lộ trên mạng LAN.",
    Secrets: "Bí mật (secrets)",
    "No API keys, tokens, or credentials stored or transmitted anywhere":
      "Không lưu trữ hoặc truyền API key, token hay thông tin xác thực ở bất cứ đâu",
    "Dependency surface": "Bề mặt phụ thuộc",
    "5 runtime server deps, 6 runtime client deps (includes D3.js for Workflows) — minimal attack surface":
      "5 phụ thuộc runtime phía máy chủ, 6 phụ thuộc runtime phía client (bao gồm D3.js cho Workflows) — bề mặt tấn công tối thiểu",
    "Hooks only apply to sessions started <em>after</em> installation. Restart Claude Code after starting the dashboard.":
      "Hook chỉ áp dụng cho các phiên được khởi động <em>sau</em> khi cài đặt. Hãy khởi động lại Claude Code sau khi khởi động dashboard.",
    "On some systems the shell environment when Claude Code fires hooks may not include the full PATH. Test with <code>node --version</code>. If not found, use the absolute path to <code>node</code> in the hook command.":
      "Trên một số hệ thống, môi trường shell khi Claude Code kích hoạt hook có thể không bao gồm đầy đủ PATH. Hãy kiểm tra bằng <code>node --version</code>. Nếu không tìm thấy, hãy dùng đường dẫn tuyệt đối tới <code>node</code> trong lệnh hook.",
    Problem: "Vấn đề",
    Solution: "Giải pháp",
    "<code>better-sqlite3</code> errors during install":
      "Lỗi <code>better-sqlite3</code> trong quá trình cài đặt",
    "This is non-fatal — <code>better-sqlite3</code> is an optional dependency. On Node 22+ the server automatically falls back to built-in <code>node:sqlite</code>. On older Node versions, install Python 3 + C++ build tools, then run <code>npm rebuild better-sqlite3</code>. For the desktop app, the <code>desktop:install</code> preflight prints copy-pasteable per-OS setup guidance (incl. a no-toolchain alternative) when the native build fails.":
      "Đây không phải lỗi nghiêm trọng — <code>better-sqlite3</code> là một phụ thuộc tùy chọn. Trên Node 22+ máy chủ tự động chuyển sang dùng <code>node:sqlite</code> tích hợp sẵn. Trên các phiên bản Node cũ hơn, hãy cài Python 3 + công cụ build C++, rồi chạy <code>npm rebuild better-sqlite3</code>. Đối với ứng dụng desktop, bước kiểm tra trước <code>desktop:install</code> sẽ in ra hướng dẫn thiết lập theo từng hệ điều hành có thể sao chép-dán (kèm cả một phương án không cần toolchain) khi quá trình build native thất bại.",
    'Dashboard shows "Disconnected"': 'Dashboard hiển thị "Disconnected"',
    "Server is not running. Start it with <code>npm run dev</code>. Client auto-reconnects every 2s.":
      "Máy chủ chưa chạy. Hãy khởi động bằng <code>npm run dev</code>. Client tự động kết nối lại mỗi 2s.",
    "Events Today shows 0": "Events Today hiển thị 0",
    "Ensure you are on the latest version (timezone bug was fixed). Restart the server.":
      "Hãy đảm bảo bạn đang dùng phiên bản mới nhất (lỗi múi giờ đã được sửa). Sau đó khởi động lại máy chủ.",
    "Port 4820 already in use": "Cổng 4820 đã bị chiếm dụng",
    "Run <code>DASHBOARD_PORT=4821 npm run dev</code>, update Vite proxy in <code>client/vite.config.ts</code>, and re-run <code>npm run install-hooks</code>.":
      "Chạy <code>DASHBOARD_PORT=4821 npm run dev</code>, cập nhật proxy Vite trong <code>client/vite.config.ts</code>, rồi chạy lại <code>npm run install-hooks</code>.",
    "Stale seed data shown": "Hiển thị dữ liệu mẫu (seed) cũ",
    "Run <code>npm run clear-data</code> to wipe all rows, then restart.":
      "Chạy <code>npm run clear-data</code> để xóa toàn bộ các hàng, rồi khởi động lại.",
    "Hooks show validation error about matcher": "Hook hiển thị lỗi kiểm tra về matcher",
    'Ensure you\'re on the latest version — the hook format was updated to use <code>matcher: "*"</code> string (not object).':
      'Hãy đảm bảo bạn đang dùng phiên bản mới nhất — định dạng hook đã được cập nhật để dùng chuỗi <code>matcher: "*"</code> (không phải đối tượng).',
    '"SQLite backend not available" on startup': '"SQLite backend not available" khi khởi động',
    "Neither <code>better-sqlite3</code> nor <code>node:sqlite</code> could load. Upgrade to Node.js 22+ (recommended), or install Python 3 + C++ build tools and run <code>npm rebuild better-sqlite3</code>.":
      "Cả <code>better-sqlite3</code> lẫn <code>node:sqlite</code> đều không thể nạp được. Hãy nâng cấp lên Node.js 22+ (khuyến nghị), hoặc cài Python 3 + công cụ build C++ và chạy <code>npm rebuild better-sqlite3</code>.",
    "Docker container runs but no sessions appear":
      "Container Docker chạy nhưng không có phiên nào xuất hiện",
    "Hooks run on the host, not inside the container. Run <code>npm run install-hooks</code> on the host after the container starts. Verify hooks in <code>~/.claude/settings.json</code> point to <code>localhost:4820</code>.":
      "Hook chạy trên host, không phải bên trong container. Hãy chạy <code>npm run install-hooks</code> trên host sau khi container khởi động. Kiểm tra để chắc chắn hook trong <code>~/.claude/settings.json</code> trỏ tới <code>localhost:4820</code>.",
    Technology: "Công nghệ",
    "Why This Over Alternatives": "Vì sao chọn nó thay vì các lựa chọn khác",
    "Zero-config, embedded, no server process. WAL mode gives concurrent reads. Synchronous API is simpler than async for this use case. <code>better-sqlite3</code> is preferred when prebuilds are available; falls back to Node.js built-in <code>node:sqlite</code> on Node 22+ when the native module cannot be compiled.":
      "Không cần cấu hình, nhúng sẵn, không có tiến trình máy chủ. Chế độ WAL cho phép đọc đồng thời. API đồng bộ đơn giản hơn bất đồng bộ cho trường hợp sử dụng này. <code>better-sqlite3</code> được ưu tiên khi có sẵn bản dựng trước; chuyển sang dùng <code>node:sqlite</code> tích hợp sẵn của Node.js trên Node 22+ khi không thể biên dịch được mô-đun native.",
    "Battle-tested, minimal, well-understood. Fastify would be overkill; raw <code>http</code> module would require too much boilerplate for routing.":
      "Đã được kiểm chứng kỹ, tối giản, dễ hiểu. Fastify sẽ là quá mức cần thiết; mô-đun <code>http</code> thuần sẽ đòi hỏi quá nhiều mã soạn sẵn cho việc định tuyến.",
    "Fastest, most lightweight WebSocket library for Node. No Socket.IO overhead needed — we only push typed JSON messages one-way.":
      "Thư viện WebSocket nhanh nhất, nhẹ nhất cho Node. Không cần thêm chi phí của Socket.IO — chúng ta chỉ đẩy các thông điệp JSON có kiểu theo một chiều.",
    "Current supported client runtime with strong TypeScript support. CCAM remains a client-rendered SPA and does not enable Server Components or RSC.":
      "Runtime client hiện được hỗ trợ với TypeScript mạnh mẽ. CCAM vẫn là SPA render phía client và không bật Server Components hay RSC.",
    "Fast builds, native ESM, excellent dev experience. Proxy config handles the dev server split cleanly with no ejection.":
      "Build nhanh, ESM gốc, trải nghiệm phát triển tuyệt vời. Cấu hình proxy xử lý việc tách máy chủ phát triển một cách gọn gàng mà không cần eject.",
    "Utility-first approach keeps styles colocated with markup. No CSS module boilerplate. Custom dark theme config for the dark UI.":
      "Cách tiếp cận ưu tiên tiện ích (utility-first) giữ cho style nằm cạnh markup. Không có mã soạn sẵn của CSS module. Cấu hình chủ đề tối tùy chỉnh cho giao diện tối.",
    "Standard declarative routing for React SPAs. Layout routes with <code>&lt;Outlet&gt;</code> give clean shell composition without enabling framework or RSC modes.":
      "Định tuyến khai báo tiêu chuẩn cho SPA React. Các route bố cục với <code>&lt;Outlet&gt;</code> tạo lớp vỏ gọn gàng mà không bật chế độ framework hay RSC.",
    "Tree-shakeable icon library — only imports what's used (~20 icons). No heavy icon font.":
      "Thư viện biểu tượng có thể tree-shake — chỉ nhập những gì được dùng (~20 biểu tượng). Không có font biểu tượng nặng nề.",
    "Catches null/undefined bugs at compile time. <code>noUncheckedIndexedAccess</code> prevents array bounds issues in analytics aggregations.":
      "Bắt các lỗi null/undefined tại thời điểm biên dịch. <code>noUncheckedIndexedAccess</code> ngăn các vấn đề vượt giới hạn mảng trong các phép tổng hợp phân tích.",
    "Industry-standard data visualization library. Powers the Workflows page's 11 interactive sections — DAG layouts, Sankey diagrams, force-directed graphs, bubble charts, and swim-lane timelines. No wrapper libraries needed; direct SVG rendering keeps bundle impact minimal.":
      "Thư viện trực quan hóa dữ liệu tiêu chuẩn ngành. Cung cấp sức mạnh cho 11 phần tương tác của trang Workflows — bố cục DAG, biểu đồ Sankey, đồ thị định hướng theo lực, biểu đồ bong bóng và dòng thời gian theo làn (swim-lane). Không cần thư viện bao bọc nào; việc render SVG trực tiếp giữ cho tác động lên kích thước gói ở mức tối thiểu.",
    "Available on virtually all systems. Handles ANSI and JSON natively with stdlib only. No install step required.":
      "Có sẵn trên gần như mọi hệ thống. Xử lý ANSI và JSON một cách gốc chỉ với thư viện chuẩn. Không cần bước cài đặt nào.",
    "Local-first monitoring for Claude Code sessions, agents, and tool events. Built for real-time visibility with zero external dependencies.":
      "Giám sát ưu tiên cục bộ cho các phiên, agent và sự kiện công cụ của Claude Code. Được xây dựng để mang lại khả năng quan sát theo thời gian thực với không có phụ thuộc bên ngoài nào.",
    Install: "Cài đặt",
    Setup: "Thiết lập",
    "About the Creator": "Về người tạo ra",
    '<span class="caption-icon">⭐</span> <span> Enjoying the project? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer">Give it a star on GitHub</a> and help more builders discover it. </span>':
      '<span class="caption-icon">⭐</span> <span> Bạn thấy dự án hữu ích? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer">Hãy gắn sao cho nó trên GitHub</a> và giúp nhiều nhà phát triển khác biết đến nó. </span>',
    'Clears the waiting flag and promotes the main agent to <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>. The only reliable signal that text-only assistant turns have started — they emit no <code>PreToolUse</code> before <code>Stop</code>.':
      'Xóa cờ chờ và nâng agent chính lên trạng thái <span class="status-chip chip-working"><span class="chip-dot"></span>Đang làm việc</span>. Đây là tín hiệu đáng tin cậy duy nhất cho biết các lượt trợ lý chỉ có văn bản đã bắt đầu — chúng không phát ra <code>PreToolUse</code> trước <code>Stop</code>.',
    'Clears the waiting flag, sets agent → <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>, <code>current_tool</code> set. If tool is <code>Agent</code>, subagent record created.':
      'Xóa cờ chờ, đặt agent → <span class="status-chip chip-working"><span class="chip-dot"></span>Đang làm việc</span>, gán <code>current_tool</code>. Nếu công cụ là <code>Agent</code>, một bản ghi subagent được tạo.',
    'Clears the waiting flag (covers permission-prompt approvals mid-tool). <code>current_tool</code> cleared. Agent stays <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>.':
      'Xóa cờ chờ (bao gồm cả việc phê duyệt nhắc quyền giữa chừng khi đang dùng công cụ). <code>current_tool</code> được xóa. Agent vẫn ở trạng thái <span class="status-chip chip-working"><span class="chip-dot"></span>Đang làm việc</span>.',
    'Non-error: main agent → <code>waiting</code> — UI shows <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> until the next user input. <code>stop_reason=error</code>: marks the agent and session <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>. Background subagents keep running.':
      'Không phải lỗi: agent chính → <code>waiting</code> — UI hiển thị <span class="status-chip chip-waiting"><span class="chip-dot"></span>Đang chờ</span> cho đến lần nhập tiếp theo của người dùng. <code>stop_reason=error</code>: đánh dấu agent và phiên là <span class="status-chip chip-error"><span class="chip-dot"></span>Lỗi</span>. Các subagent chạy nền vẫn tiếp tục chạy.',
    'Matched subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session\'s <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent\'s own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks.':
      'Subagent được khớp → <span class="status-chip chip-completed"><span class="chip-dot"></span>Hoàn tất</span>. Nó cố ý <strong>không</strong> xóa cờ chờ — việc một subagent chạy nền hoàn tất không cho ta biết gì về con người. Nó cũng khởi động một lượt quét JSONL kiểu fire-and-forget (<code>scanAndImportSubagents</code>) duyệt qua các tệp <code>subagents/agent-*.jsonl</code> của phiên, ghép các khối <code>tool_use</code> ↔ <code>tool_result</code> theo <code>tool_use_id</code>, và phát ra các sự kiện <code>PreToolUse</code> + <code>PostToolUse</code> cho từng công cụ dưới <code>agent_id</code> riêng của mỗi subagent — qua đó làm lộ ra các lệnh gọi công cụ mà subagent thực hiện nội bộ và vốn không bao giờ kích hoạt bất kỳ hook nào.',
    'Creates a compaction subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Detected via <code>isCompactSummary</code> entries in the transcript. Token baselines preserve pre-compaction totals. Periodic scanner (cadence ~¼ of <code>DASHBOARD_STALE_MINUTES</code>) catches compactions when no hooks fire.':
      'Tạo một subagent nén → <span class="status-chip chip-completed"><span class="chip-dot"></span>Hoàn tất</span>. Được phát hiện qua các mục <code>isCompactSummary</code> trong bản ghi. Các mốc nền token giữ lại tổng số trước khi nén. Bộ quét định kỳ (nhịp khoảng ¼ của <code>DASHBOARD_STALE_MINUTES</code>) bắt được các lần nén khi không có hook nào kích hoạt.',
    'Drops the waiting flag. If the session is already in <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>, the error state is preserved; otherwise marks all agents and the session as <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Evicts the session\'s transcript from the shared cache.':
      'Bỏ cờ chờ. Nếu phiên đã ở trạng thái <span class="status-chip chip-error"><span class="chip-dot"></span>Lỗi</span>, trạng thái lỗi được giữ nguyên; nếu không, đánh dấu tất cả agent và phiên là <span class="status-chip chip-completed"><span class="chip-dot"></span>Hoàn tất</span>. Đẩy bản ghi của phiên ra khỏi bộ nhớ đệm dùng chung.',
    "SQLite connection, WAL/FK pragmas, schema migrations (<code>CREATE TABLE IF NOT EXISTS</code>), all prepared statements as a reusable <code>stmts</code> object. Tries <code>better-sqlite3</code> first, falls back to built-in <code>node:sqlite</code> via <code>compat-sqlite.js</code>":
      "Kết nối SQLite, các pragma WAL/FK, di trú schema (<code>CREATE TABLE IF NOT EXISTS</code>), tất cả câu lệnh đã chuẩn bị dưới dạng đối tượng <code>stmts</code> có thể tái sử dụng. Thử <code>better-sqlite3</code> trước, rồi quay về dùng <code>node:sqlite</code> tích hợp sẵn thông qua <code>compat-sqlite.js</code>",
    "Each page pulls initial data from REST then subscribes to eventBus for live updates":
      "Mỗi trang lấy dữ liệu ban đầu từ REST rồi đăng ký eventBus để nhận cập nhật trực tiếp",
    "Entity Relationship Diagram — SQLite schema": "Sơ đồ quan hệ thực thể — schema SQLite",
    "Working Dir": "Thư mục làm việc",
    "Git Branch": "Nhánh Git",
    "Context Bar": "Thanh ngữ cảnh",
    "Token Counts": "Số lượng token",
    "Session Cost": "Chi phí phiên",
    "Statusline rendering pipeline — invoked on each Claude Code update":
      "Quy trình kết xuất thanh trạng thái — được gọi mỗi lần Claude Code cập nhật",
    "Aggregates data from multiple API endpoints to display high-signal metrics directly in the sidebar:":
      "Tổng hợp dữ liệu từ nhiều điểm cuối API để hiển thị các chỉ số quan trọng ngay trên thanh bên:",
    "Zero-Config Setup": "Cài đặt không cần cấu hình",
    "One-line mental model": "Mô hình tư duy trong một dòng",
    "Your data survives reinstalls and updates":
      "Dữ liệu của bạn vẫn được giữ lại sau khi cài lại và cập nhật",
    "The <code>claude</code> CLI is found automatically":
      "CLI <code>claude</code> được tìm thấy tự động",
    'Open <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> Releases → latest </a> and download the asset for your platform. The macOS and Windows Desktop CI jobs auto-publish a new <code>vX.Y.Z</code> release every time the version in <code>package.json</code> is bumped on <code>master</code>, so this link always points at the current build. Releases are public — no GitHub sign-in required.':
      'Mở <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> Releases → latest </a> và tải về tệp phù hợp với nền tảng của bạn. Các job CI Desktop trên macOS và Windows tự động phát hành một bản <code>vX.Y.Z</code> mới mỗi khi phiên bản trong <code>package.json</code> được tăng trên <code>master</code>, nên liên kết này luôn trỏ tới bản dựng hiện tại. Các bản phát hành là công khai — không cần đăng nhập GitHub.',
    'Want a build straight off the tip of <code>master</code>, ahead of the next tagged release? Every green run of the <code>🍎 macOS Desktop (DMG)</code> job on <code>macos-latest</code> uploads both per-arch DMGs as the <code>ClaudeCodeMonitor-dmg</code> workflow artifact, and the <code>🪟 Windows Desktop (EXE)</code> job on <code>windows-latest</code> uploads the installer + portable EXEs as the <code>ClaudeCodeMonitor-win</code> artifact. Open the <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><polyline points="12 7 12 12 15 14"></polyline></svg> latest passing run </a>, scroll to its Artifacts section, and download <code>ClaudeCodeMonitor-dmg</code> or <code>ClaudeCodeMonitor-win</code>. (GitHub sign-in required; 14-day retention.)':
      'Muốn một bản dựng ngay từ đầu nhánh <code>master</code>, trước cả bản phát hành được gắn thẻ tiếp theo? Mỗi lần chạy thành công của job <code>🍎 macOS Desktop (DMG)</code> trên <code>macos-latest</code> đều tải lên cả hai tệp DMG cho từng kiến trúc dưới dạng workflow artifact <code>ClaudeCodeMonitor-dmg</code>, và job <code>🪟 Windows Desktop (EXE)</code> trên <code>windows-latest</code> tải lên trình cài đặt + các EXE di động dưới dạng artifact <code>ClaudeCodeMonitor-win</code>. Mở <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><polyline points="12 7 12 12 15 14"></polyline></svg> latest passing run </a>, cuộn đến phần Artifacts của nó, rồi tải về <code>ClaudeCodeMonitor-dmg</code> hoặc <code>ClaudeCodeMonitor-win</code>. (Cần đăng nhập GitHub; lưu giữ 14 ngày.)',
    "Incoming Webhook URL": "URL Webhook đến",
    "Webhook URL": "URL Webhook",
    "Detection pipeline from scheduler to UI": "Quy trình phát hiện từ bộ lập lịch đến UI",
    "A shell-less <code>git fetch</code> with a 120-second timeout, followed by a <code>rev-list</code> against the tracked upstream. Each call runs from <code>server/lib/update-check.js</code> and returns a structured payload — never throws — so a flaky remote can't stall the dashboard.":
      "Một lệnh <code>git fetch</code> không qua shell với thời gian chờ 120 giây, theo sau là một <code>rev-list</code> đối chiếu với upstream đang theo dõi. Mỗi lệnh gọi chạy từ <code>server/lib/update-check.js</code> và trả về một payload có cấu trúc — không bao giờ ném ngoại lệ — nên một remote chập chờn không thể làm treo dashboard.",
    "Detection-only by design": "Chỉ phát hiện theo thiết kế",
    "Adding a language": "Thêm một ngôn ngữ",
    "<strong>Ask box:</strong> answers simple status questions locally from cached data (“what's running”, “any errors”, “status”).":
      "<strong>Ô hỏi:</strong> trả lời cục bộ các câu hỏi trạng thái đơn giản từ dữ liệu đã lưu đệm (“đang chạy gì”, “có lỗi không”, “trạng thái”).",
    "A third way to run: the Desktop App (macOS &amp; Windows)":
      "Cách chạy thứ ba: Ứng dụng Desktop (macOS &amp; Windows)",
    "Hooks still run on the host": "Các hook vẫn chạy trên máy chủ",
    "Hook note": "Ghi chú về hook",
    "SQL injection": "Tấn công SQL injection",
    "All queries use prepared statements with parameterized values — no string interpolation":
      "Tất cả truy vấn đều dùng câu lệnh đã chuẩn bị với giá trị tham số hóa — không nối chuỗi",
    "Request size": "Kích thước yêu cầu",
    "Express JSON body parser limited to 1MB":
      "Bộ phân tích thân JSON của Express bị giới hạn ở 1MB",
    "Dashboard — stats, active agents, recent events":
      "Dashboard — số liệu thống kê, agent đang hoạt động, sự kiện gần đây",
    "KanbanBoard — agent status columns": "KanbanBoard — các cột trạng thái agent",
    "Sessions — searchable, filterable table": "Sessions — bảng có thể tìm kiếm và lọc",
    "SessionDetail — agents + full event timeline":
      "SessionDetail — các agent và dòng thời gian sự kiện đầy đủ",
    "ActivityFeed — real-time streaming event log":
      "ActivityFeed — nhật ký sự kiện truyền phát theo thời gian thực",
    "Analytics — token usage, heatmap (day-of-week aligned), tool charts, donut charts":
      "Analytics — mức dùng token, bản đồ nhiệt (căn theo ngày trong tuần), biểu đồ công cụ, biểu đồ vành khuyên",
    "Workflows — D3.js visualizations, cross-filtering, status filter, session drill-in":
      "Workflows — trực quan hóa D3.js, lọc chéo, lọc theo trạng thái, đi sâu vào phiên",
    "Settings — model pricing, hook status, data export, session cleanup":
      "Settings — định giá mô hình, trạng thái hook, xuất dữ liệu, dọn dẹp phiên",
    'Returns <code>{ "status": "ok", "timestamp": "..." }</code>':
      'Trả về <code>{ "status": "ok", "timestamp": "..." }</code>',
    "List sessions with agent counts and per-session cost. Params: <code>status</code>, <code>q</code> (case-insensitive search across <code>id</code>/<code>name</code>/<code>cwd</code>), <code>limit</code> (default 50, max 10000), <code>offset</code>. Response includes <code>total</code> for paginators.":
      "Liệt kê các phiên kèm số lượng agent và chi phí mỗi phiên. Tham số: <code>status</code>, <code>q</code> (tìm kiếm không phân biệt hoa thường trên <code>id</code>/<code>name</code>/<code>cwd</code>), <code>limit</code> (mặc định 50, tối đa 10000), <code>offset</code>. Phản hồi bao gồm <code>total</code> cho bộ phân trang.",
    "Session detail with agents and events": "Chi tiết phiên kèm các agent và sự kiện",
    "Create session (idempotent on <code>id</code>)": "Tạo phiên (idempotent theo <code>id</code>)",
    "Update session status / metadata": "Cập nhật trạng thái / metadata của phiên",
    "List agents — params: <code>status</code>, <code>session_id</code>, <code>limit</code>, <code>offset</code>":
      "Liệt kê các agent — tham số: <code>status</code>, <code>session_id</code>, <code>limit</code>, <code>offset</code>",
    "Single agent detail": "Chi tiết một agent",
    "Create agent": "Tạo agent",
    "Update agent status / task / current_tool":
      "Cập nhật trạng thái / tác vụ / current_tool của agent",
    "List events newest-first — params: <code>session_id</code>, <code>limit</code>, <code>offset</code>":
      "Liệt kê sự kiện mới nhất trước — tham số: <code>session_id</code>, <code>limit</code>, <code>offset</code>",
    "Aggregate counts + status distributions + WS connections":
      "Số liệu tổng hợp + phân bố trạng thái + kết nối WS",
    "Token totals, tool usage, daily trends, agent types, event types, averages":
      "Tổng số token, mức dùng công cụ, xu hướng theo ngày, loại agent, loại sự kiện, giá trị trung bình",
    "Receive and process a Claude Code hook event (called by hook-handler.js)":
      "Nhận và xử lý một sự kiện hook của Claude Code (được hook-handler.js gọi)",
    "List all model pricing rules": "Liệt kê tất cả quy tắc định giá mô hình",
    "Create or update a pricing rule": "Tạo hoặc cập nhật một quy tắc định giá",
    "Delete a pricing rule": "Xóa một quy tắc định giá",
    "Total cost across all sessions": "Tổng chi phí trên tất cả các phiên",
    "Cost breakdown for a specific session": "Phân tích chi phí cho một phiên cụ thể",
    "System info, DB stats, hook installation status":
      "Thông tin hệ thống, số liệu DB, trạng thái cài đặt hook",
    "Delete all sessions, agents, events, token usage":
      "Xóa tất cả phiên, agent, sự kiện, mức dùng token",
    "Reinstall Claude Code hooks": "Cài đặt lại các hook của Claude Code",
    "Reset Claude, Codex, or both pricing tables to defaults":
      "Đặt lại bảng giá Claude, Codex hoặc cả hai về mặc định",
    "Export all data as JSON download": "Xuất tất cả dữ liệu dưới dạng tải về JSON",
    "Abandon stale sessions (by hours), purge old data (by days)":
      "Bỏ các phiên cũ (theo giờ), xóa dữ liệu cũ (theo ngày)",
    "OS-aware paths, archive command, supported extensions, step-by-step instructions; includes live stats for the default <code>~/.claude/projects</code> folder":
      "Đường dẫn nhận biết OS, lệnh lưu trữ, các phần mở rộng được hỗ trợ, hướng dẫn từng bước; bao gồm số liệu trực tiếp cho thư mục mặc định <code>~/.claude/projects</code>",
    "Re-scan the default <code>~/.claude/projects</code> directory; safe to re-run (idempotent via session-ID dedup)":
      "Quét lại thư mục mặc định <code>~/.claude/projects</code>; an toàn để chạy lại (idempotent nhờ khử trùng lặp theo ID phiên)",
    "Scan any absolute directory (body <code>{ path }</code>); tilde (<code>~</code>) is expanded; walks subdirectories recursively and imports every <code>.jsonl</code> found":
      "Quét bất kỳ thư mục đường dẫn tuyệt đối nào (thân yêu cầu <code>{ path }</code>); dấu ngã (<code>~</code>) được mở rộng; duyệt đệ quy các thư mục con và nhập mọi <code>.jsonl</code> tìm thấy",
    "Multipart upload of <code>.jsonl</code>, <code>.meta.json</code>, <code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>, <code>.gz</code>. Per-request staging dir, path-traversal and extraction-size guards. Returns 413 <code>EXTRACTION_LIMIT_EXCEEDED</code> on suspected bomb archives":
      "Tải lên nhiều phần các tệp <code>.jsonl</code>, <code>.meta.json</code>, <code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>, <code>.gz</code>. Có thư mục tạm cho mỗi yêu cầu, cùng cơ chế bảo vệ chống path-traversal và giới hạn kích thước giải nén. Trả về 413 <code>EXTRACTION_LIMIT_EXCEEDED</code> đối với các kho lưu trữ nghi là bom",
    "Aggregate workflow data — orchestration graphs, tool flows, effectiveness, patterns, model delegation, error propagation, concurrency, complexity, compaction impact. Accepts <code>?status=active|completed</code> query param to filter by workflow status":
      "Tổng hợp dữ liệu quy trình làm việc — đồ thị điều phối, luồng công cụ, hiệu quả, mẫu hình, ủy thác mô hình, lan truyền lỗi, đồng thời, độ phức tạp, tác động của việc nén. Chấp nhận tham số truy vấn <code>?status=active|completed</code> để lọc theo trạng thái quy trình",
    "Per-session drill-in — agent tree, tool timeline, event details":
      "Đi sâu theo từng phiên — cây agent, dòng thời gian công cụ, chi tiết sự kiện",
    "Fired-alert feed, newest first (<code>?unacked=true</code>, <code>limit</code>, <code>offset</code>; carries <code>total</code> and <code>unacked</code> counts)":
      "Luồng cảnh báo đã kích hoạt, mới nhất trước (<code>?unacked=true</code>, <code>limit</code>, <code>offset</code>; kèm theo số đếm <code>total</code> và <code>unacked</code>)",
    "Acknowledge one alert": "Xác nhận một cảnh báo",
    "Acknowledge every unacked alert": "Xác nhận mọi cảnh báo chưa được xác nhận",
    "List alert rules": "Liệt kê các quy tắc cảnh báo",
    "Create a rule (<code>event_pattern</code> | <code>inactivity</code> | <code>status_duration</code> | <code>token_threshold</code>)":
      "Tạo một quy tắc (<code>event_pattern</code> | <code>inactivity</code> | <code>status_duration</code> | <code>token_threshold</code>)",
    "Update name / config / enabled / cooldown":
      "Cập nhật tên / cấu hình / trạng thái bật / thời gian chờ",
    "Delete a rule and its fired-alert history":
      "Xóa một quy tắc và lịch sử cảnh báo đã kích hoạt của nó",
    "Supported providers + their config fields (drives the UI form)":
      "Các nhà cung cấp được hỗ trợ cùng các trường cấu hình của chúng (điều khiển biểu mẫu UI)",
    "List webhook targets (URLs masked, secrets redacted)":
      "Liệt kê các đích webhook (URL được che, khóa bí mật được ẩn)",
    "Create a target — 14 first-class providers (Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream) + a generic JSON endpoint":
      "Tạo một đích — 14 nhà cung cấp được hỗ trợ hạng nhất (Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream) cùng một endpoint JSON tổng quát",
    "Update name / url / enabled / secret / headers / config / rule scope (<code>type</code> is immutable)":
      "Cập nhật tên / url / trạng thái bật / khóa bí mật / tiêu đề / cấu hình / phạm vi quy tắc (<code>type</code> là bất biến)",
    "Delete a target and its delivery log": "Xóa một đích và nhật ký gửi của nó",
    "Send a synthetic test alert and report the result":
      "Gửi một cảnh báo thử nghiệm tổng hợp và báo cáo kết quả",
    "Recent delivery log for a target": "Nhật ký gửi gần đây cho một đích",
    Documentation: "Tài liệu",
    Architecture: "Kiến trúc",
    "Relevant Links": "Liên kết liên quan",
    "GitHub Repo": "Kho GitHub",
    '<span class="caption-icon">🔔</span> <span><strong>Settings · Alerts</strong> — rules-based alerting engine and outbound webhooks in one place: alert rules (event pattern / inactivity / stuck agent / token threshold) with per-rule cooldown, a live fired-alert feed, and 14 first-class webhook providers plus a generic JSON endpoint with optional HMAC signing</span>':
      '<span class="caption-icon">🔔</span> <span><strong>Cài đặt · Cảnh báo</strong> — công cụ cảnh báo theo quy tắc và webhook gửi đi trong cùng một nơi: quy tắc cảnh báo (mẫu sự kiện / không hoạt động / agent bị treo / ngưỡng token) với cooldown theo từng quy tắc, nguồn cấp cảnh báo đã kích hoạt theo thời gian thực, và 14 nhà cung cấp webhook hạng nhất cùng một endpoint JSON tổng quát có tùy chọn ký HMAC</span>',
    '<span class="caption-icon">📗</span> <span><strong>API Docs · ReDoc</strong> — a self-hosted, read-optimized rendering of the full OpenAPI 3.0 spec at <code>/api/redoc</code>, served entirely offline with no CDN. Complements the interactive Swagger UI at <code>/api/docs</code>; every backend route is documented with parameters, schemas, and examples</span>':
      '<span class="caption-icon">📗</span> <span><strong>Tài liệu API · ReDoc</strong> — bản hiển thị toàn bộ đặc tả OpenAPI 3.0 tự lưu trữ, tối ưu cho việc đọc tại <code>/api/redoc</code>, phục vụ hoàn toàn ngoại tuyến, không CDN. Bổ sung cho Swagger UI tương tác tại <code>/api/docs</code>; mọi route backend đều được tài liệu hóa kèm tham số, schema và ví dụ</span>',
    '<span class="caption-icon">📘</span> <span><strong>API Docs · Swagger UI</strong> — interactive OpenAPI 3.0 playground at <code>/api/docs</code>: collapsible endpoint groups, request/response schemas, auth headers, and try-it-out request execution against the live local server</span>':
      '<span class="caption-icon">📘</span> <span><strong>Tài liệu API · Swagger UI</strong> — sân chơi OpenAPI 3.0 tương tác tại <code>/api/docs</code>: nhóm endpoint có thể thu gọn, schema yêu cầu/phản hồi, header xác thực, và thực thi yêu cầu try-it-out với máy chủ cục bộ đang chạy</span>',
    '<span class="caption-icon">📗</span> <span>ReDoc at <code>/api/redoc</code> — a self-hosted, read-optimized three-panel rendering of the same OpenAPI spec: deep-linkable sections, search, and full schema/example detail. Works entirely offline (no CDN)</span>':
      '<span class="caption-icon">📗</span> <span>ReDoc tại <code>/api/redoc</code> — bản hiển thị ba khung tự lưu trữ, tối ưu cho việc đọc của cùng đặc tả OpenAPI: các mục có thể liên kết sâu, tìm kiếm, và đầy đủ chi tiết schema/ví dụ. Hoạt động hoàn toàn ngoại tuyến (không CDN)</span>',
    '<span class="caption-icon">🔔</span> Settings · Alerts — the rules-based alerting engine, a live fired-alert feed, and outbound webhook channels (14 first-class providers + a generic JSON endpoint) managed together in one place':
      '<span class="caption-icon">🔔</span> Cài đặt · Cảnh báo — công cụ cảnh báo theo quy tắc, nguồn cấp cảnh báo đã kích hoạt theo thời gian thực, và các kênh webhook gửi đi (14 nhà cung cấp hạng nhất + một endpoint JSON tổng quát) được quản lý cùng một nơi',
    'Surfaces "dynamic workflows" — the fleets of sub-agents spawned by the <code>Workflow</code> tool and self-paced <code>/loop</code> runs. These emit no hooks, so they are reconstructed from the on-disk run journal written when a workflow finishes (<code>workflows/wf_&lt;runId&gt;.json</code>) plus the inner <code>subagents/agent-*.jsonl</code> transcripts. Each run shows its phases and a per-agent token / tool-call / duration breakdown; a running workflow is detected from its launch script before the journal exists. Runs appear in a panel on the Workflows page and as a linked subsection on each session.':
      'Hiển thị các "quy trình động" — những nhóm sub-agent do công cụ <code>Workflow</code> và các lần chạy <code>/loop</code> tự định nhịp tạo ra. Chúng không phát ra hook, nên được dựng lại từ nhật ký chạy trên đĩa được ghi khi quy trình kết thúc (<code>workflows/wf_&lt;runId&gt;.json</code>) cùng các bản ghi <code>subagents/agent-*.jsonl</code> bên trong. Mỗi lần chạy hiển thị các giai đoạn và bảng phân tích token / lệnh gọi công cụ / thời lượng theo từng agent; một quy trình đang chạy được phát hiện từ script khởi chạy trước khi nhật ký tồn tại. Các lần chạy xuất hiện trong một bảng trên trang Quy trình và dưới dạng mục liên kết trên mỗi phiên.',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs</strong> — "dynamic workflows" spawned by the Workflow tool, reconstructed from on-disk run journals: status, agent count, tokens, and tool calls, expandable into a per-agent breakdown (phase, state, tokens, tools, duration) with humanized result previews</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Lần chạy quy trình</strong> — các "quy trình động" do công cụ Workflow tạo ra, được dựng lại từ nhật ký chạy trên đĩa: trạng thái, số agent, token và lệnh gọi công cụ, mở rộng thành bảng phân tích theo từng agent (giai đoạn, trạng thái, token, công cụ, thời lượng) kèm bản xem trước kết quả đã được làm gọn</span>',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · in a session</strong> — the same fleets linked to their launching session, so a session\'s dynamic-workflow sub-agents and their folded-in token cost are visible inline</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Lần chạy quy trình · trong phiên</strong> — cùng các nhóm đó được liên kết tới phiên khởi chạy, nên các sub-agent của quy trình động và chi phí token đã được gộp vào của phiên đều hiển thị ngay trong phiên</span>',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · expanded</strong> — a run opened up: clickable color-coded phase filters, the per-agent metrics table, and a full list of clickable result items that expand to each agent\'s complete prompt and result</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Lần chạy quy trình · mở rộng</strong> — một lần chạy được mở ra: bộ lọc giai đoạn có màu và bấm được, bảng số liệu theo từng agent, và danh sách đầy đủ các mục kết quả bấm được để mở ra lời nhắc và kết quả đầy đủ của từng agent</span>',
    'Live counters in Prometheus text-exposition format (v0.0.4): sessions and agents by status, total events, cumulative tokens by kind, connected WebSocket clients, configured remote sources, process uptime and RSS, and build version — so CCAM can be scraped into Prometheus / Grafana. Read-only, and — being under <code>/api</code> — behind the DNS-rebinding Host-header guard and the optional <code>DASHBOARD_TOKEN</code>: a non-loopback scraper (e.g. Prometheus in Docker via <code>host.docker.internal</code>) must be allowlisted with <code>DASHBOARD_ALLOWED_HOSTS</code> or the scrape returns <code>403 EBADHOST</code>, and must send the token when one is set. A ready-to-run Prometheus + Grafana stack with a pre-provisioned "CCAM — Overview" dashboard lives in <code>monitoring/</code> (<code>cd monitoring &amp;&amp; docker compose up -d</code>; Grafana on port 3000).':
      'Các bộ đếm trực tiếp ở định dạng Prometheus text-exposition (v0.0.4): sessions và agents theo trạng thái, tổng số sự kiện, token tích lũy theo loại, số client WebSocket đang kết nối, các nguồn dữ liệu từ xa đã cấu hình, thời gian chạy tiến trình và RSS, cùng phiên bản build — để CCAM có thể được scrape vào Prometheus / Grafana. Chỉ-đọc, và — vì nằm dưới <code>/api</code> — nằm sau lớp bảo vệ Host-header chống DNS-rebinding và <code>DASHBOARD_TOKEN</code> tùy chọn: một scraper không phải loopback (ví dụ Prometheus trong Docker qua <code>host.docker.internal</code>) phải được đưa vào danh sách cho phép bằng <code>DASHBOARD_ALLOWED_HOSTS</code> nếu không lần scrape sẽ trả về <code>403 EBADHOST</code>, và phải gửi token khi có đặt token. Một bộ Prometheus + Grafana chạy-ngay với bảng điều khiển "CCAM — Overview" dựng sẵn nằm trong <code>monitoring/</code> (<code>cd monitoring &amp;&amp; docker compose up -d</code>; Grafana ở cổng 3000).',
    "Comma-separated extra <code>Host</code>-header names allowed past the DNS-rebinding guard — needed for a non-loopback client or scraper, such as Prometheus in Docker reaching <code>/api/metrics</code> via <code>host.docker.internal</code>; an unlisted host is rejected with <code>403 EBADHOST</code>":
      "Các tên <code>Host</code>-header bổ sung, phân tách bằng dấu phẩy, được cho phép vượt qua lớp bảo vệ chống DNS-rebinding — cần cho một client hoặc scraper không phải loopback, chẳng hạn Prometheus trong Docker truy cập <code>/api/metrics</code> qua <code>host.docker.internal</code>; một host không có trong danh sách sẽ bị từ chối với <code>403 EBADHOST</code>",
    "The entire UI ships in <strong>five languages — English, 简体中文, Tiếng Việt, 한국어, and Español</strong> — built on <code>i18next</code> + <code>react-i18next</code> with <code>i18next-browser-languagedetector</code>. Coverage is end-to-end: every page, chart tooltip, Settings flow, Workflow narrative, Config Explorer tab, Run page, and the Alerts rule-help tooltips + webhook setup guides are translated. Switch languages from the sidebar's custom language dropdown — the choice persists in <code>localStorage</code>.":
      "Toàn bộ giao diện có <strong>năm ngôn ngữ — English, 简体中文, Tiếng Việt, 한국어 và Español</strong> — được xây dựng trên <code>i18next</code> + <code>react-i18next</code> cùng <code>i18next-browser-languagedetector</code>. Phạm vi bao phủ là đầu-cuối: mọi trang, chú giải biểu đồ, luồng Settings, tường thuật Workflow, thẻ Config Explorer, trang Run, cùng chú giải trợ giúp quy tắc Alerts và hướng dẫn thiết lập webhook đều được dịch. Chọn ngôn ngữ từ menu thả xuống tùy chỉnh ở thanh bên; lựa chọn được lưu trong <code>localStorage</code>.",
    'Every alert-rule field has a help tooltip — the event-type, tool-name, and summary-contains fields include example chips of real hook events and built-in tool names. Each webhook provider ships a collapsible step-by-step setup guide linking to the official docs. A one-click <strong>"Send test"</strong> probe fires a synthetic alert and reports the delivery result inline, and targets can be scoped to specific rules. Fully localized (en / zh / vi / ko / es).':
      "Mỗi trường của quy tắc cảnh báo đều có chú giải trợ giúp — các trường event-type, tool-name và summary-contains bao gồm các chip ví dụ về sự kiện hook thật và tên công cụ tích hợp sẵn. Mỗi nhà cung cấp webhook đi kèm một hướng dẫn thiết lập từng bước có thể thu gọn, liên kết tới tài liệu chính thức. Một thăm dò <strong>“Send test”</strong> bằng một cú nhấp sẽ kích hoạt một cảnh báo tổng hợp và báo cáo kết quả gửi ngay tại chỗ, và các đích có thể được giới hạn theo các quy tắc cụ thể. Được bản địa hóa hoàn toàn (en / zh / vi / ko / es).",
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. A searchable checkbox project picker supports selecting multiple directories; long paths remain available in full, while compact trigger text keeps the filter bar readable. Sorting uses the same custom dropdown pattern. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, and estimated cost. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "Bảng <strong>phân trang phía máy chủ</strong> liệt kê mọi phiên đã ghi. Mỗi trang chỉ lấy phần dữ liệu của mình nên việc tính chi phí luôn có giới hạn, dù có bao nhiêu phiên. Tìm kiếm không phân biệt hoa thường trên <code>id</code>, <code>name</code> và <code>cwd</code> chạy ở phía máy chủ với độ trễ chống dội 300 ms; bộ lọc trạng thái kết hợp với tìm kiếm để thu hẹp chính xác. Bộ chọn dự án dạng hộp kiểm có thể tìm kiếm hỗ trợ chọn nhiều thư mục; đường dẫn dài vẫn có thể xem đầy đủ, còn văn bản gọn trên nút giữ thanh lọc dễ đọc. Sắp xếp dùng cùng mẫu menu tùy chỉnh. Mỗi hàng hiển thị tên thật của phiên (đồng bộ trực tiếp từ bản ghi: tiêu đề <code>/rename</code> hoặc <code>claude -n</code>, nếu không thì tiêu đề tự động, nếu vẫn không có thì prompt đầu tiên của người dùng, với dự phòng là ID ngắn), huy hiệu trạng thái, số lượng agent, thời lượng, mô hình và chi phí ước tính. Nhấp vào bất kỳ hàng nào để đi sâu vào chế độ xem chi tiết đầy đủ của phiên với bản ghi hội thoại và phân cấp agent.",
  },
  ko: {
    "<span class=\"caption-icon\">⌘</span> <span><strong>Command Palette</strong> — <code>Cmd/Ctrl+K</code> from anywhere opens the dashboard's only launcher. One query resolves nine groups: recent commands, the nine pages, live server-side session search, the current page's own actions, every known project directory, page sub-views and list filters, all 13 Settings sections, all 12 Agent Config tabs, and preference, data-scope and language actions. Matching is subsequence-based with the matched characters highlighted</span>":
      '<span class="caption-icon">⌘</span> <span><strong>명령 팔레트</strong> — 어디서든 <code>Cmd/Ctrl+K</code> 를 누르면 대시보드의 유일한 실행기가 열립니다. 하나의 질의로 아홉 개 그룹을 해결합니다: 최근 명령, 아홉 개 페이지, 실시간 서버 측 세션 검색, 현재 페이지 자체의 액션, 알려진 모든 프로젝트 디렉터리, 페이지 하위 보기와 목록 필터, 13개 설정 섹션 전부, 12개 Agent 설정 탭 전부, 그리고 환경설정·데이터 범위·언어 액션. 부분 수열 매칭으로 일치한 글자를 강조합니다</span>',
    "Command Palette": "명령 팔레트",
    "Press <strong>Cmd/Ctrl+K</strong> anywhere to open a launcher over the whole dashboard. One query resolves nine groups: recently run commands; the current page's own actions; every known project directory; the nine sidebar routes matched on their translated labels, so it works in every locale; live session search backed by <code>/api/sessions?q=</code>, debounced and scoped to the active machine and provider rather than a stale client-side index of thousands of sessions; every page sub-view and list filter; all thirteen Settings sections; all twelve Agent Config tabs; and actions for preferences, data scope, language, and page operations. Ranking is subsequence matching with the matched characters highlighted, so <code>mcp</code> finds MCP servers and <code>kbrd</code> finds Kanban Board, and a leading <code>&gt;</code>, <code>@</code>, or <code>#</code> narrows to actions, pages, or sessions. Arrows, Home/End, PageUp/PageDown and Tab move; Enter opens; Escape closes; a failed session query degrades quietly instead of blocking the palette. Destructive operations are deliberately absent — the palette navigates to them, never performs them.":
      "어디서든 <strong>Cmd/Ctrl+K</strong> 를 누르면 대시보드 전체를 대상으로 하는 실행기가 열립니다. 하나의 질의가 아홉 개 그룹을 해결합니다: 최근 실행한 명령; 현재 페이지 자체의 액션; 알려진 모든 프로젝트 디렉터리; 번역된 레이블로 매칭되는 아홉 개 사이드바 경로(그래서 모든 언어에서 동작); <code>/api/sessions?q=</code> 기반의 실시간 세션 검색(디바운스 적용, 현재 머신과 제공자 범위로 한정 — 수천 개 세션의 오래된 클라이언트 인덱스를 두지 않음); 모든 페이지 하위 보기와 목록 필터; 열세 개 설정 섹션 전부; 열두 개 Agent 설정 탭 전부; 그리고 환경설정·데이터 범위·언어·페이지 조작 액션. 순위는 부분 수열 매칭이며 일치한 글자를 강조하므로 <code>mcp</code> 로 MCP servers 를, <code>kbrd</code> 로 Kanban Board 를 찾을 수 있고, 맨 앞의 <code>&gt;</code>, <code>@</code>, <code>#</code> 는 액션·페이지·세션으로 좁힙니다. 방향키, Home/End, PageUp/PageDown, Tab 으로 이동하고 Enter 로 열며 Escape 로 닫습니다. 세션 질의가 실패해도 팔레트를 막지 않고 조용히 축소됩니다. 파괴적 작업은 의도적으로 제외했습니다 — 팔레트는 해당 화면으로 이동만 하고 직접 실행하지 않습니다.",
    "Serve-stale window (ms) for per-transcript task-progress summaries behind <code>include_task_progress</code> list requests and the session-detail <code>todo_snapshot</code>. A transcript being actively appended to rarely hits the size+mtime cache key, so without this floor every list reload fully re-parses each multi-MB live transcript. <code>0</code> restores immediate re-parse on every change.":
      "<code>include_task_progress</code> \ubaa9\ub85d \uc694\uccad\uacfc \uc138\uc158 \uc0c1\uc138\uc758 <code>todo_snapshot</code>\uc744 \ub4b7\ubc1b\uce68\ud558\ub294 \ud2b8\ub79c\uc2a4\ud06c\ub9bd\ud2b8\ubcc4 \uc791\uc5c5 \uc9c4\ud589 \uc694\uc57d\uc758 stale-\ud5c8\uc6a9 \ucc3d(ms)\uc785\ub2c8\ub2e4. \uacc4\uc18d append\ub418\ub294 \ud2b8\ub79c\uc2a4\ud06c\ub9bd\ud2b8\ub294 size+mtime \uce90\uc2dc \ud0a4\uc5d0 \uac70\uc758 \uc801\uc911\ud558\uc9c0 \ubabb\ud558\ubbc0\ub85c, \uc774 \ud558\ud55c\uc774 \uc5c6\uc73c\uba74 \ubaa9\ub85d\uc744 \uc0c8\ub85c\uace0\uce68\ud560 \ub54c\ub9c8\ub2e4 \uc218 MB\uc758 \ud65c\uc131 \ud2b8\ub79c\uc2a4\ud06c\ub9bd\ud2b8\ub97c \uc804\uccb4 \uc7ac\ud30c\uc2f1\ud569\ub2c8\ub2e4. <code>0</code>\uc73c\ub85c \uc124\uc815\ud558\uba74 \ubcc0\uacbd \uc2dc\ub9c8\ub2e4 \uc989\uc2dc \uc7ac\ud30c\uc2f1\ud569\ub2c8\ub2e4.",
    "Closes an idle MCP HTTP or SSE session after this long with no request, reclaiming its server; <code>0</code> disables reaping":
      "유휴 MCP HTTP 또는 SSE 세션을 이 시간 동안 요청이 없으면 닫고 해당 Server를 회수합니다. <code>0</code>은 회수를 비활성화합니다.",
    "Every transport reclaims its own resources. A stdio server self-terminates once its host process is gone — stdin closing, or the process being re-parented away from the parent it started under — so a host that dies without signalling leaves no orphan behind. An HTTP session is released by <code>DELETE /mcp</code>, by an SSE disconnect, or by an idle sweep governed by <code>MCP_HTTP_SESSION_TIMEOUT_MS</code>, because terminating a session is optional in the protocol and a vanished client would otherwise hold its server forever.":
      "각 전송 방식은 자체 리소스를 회수합니다. stdio Server는 호스트 프로세스가 사라지면 스스로 종료합니다 — stdin이 닫히거나, 프로세스가 시작할 때의 부모가 아닌 다른 부모로 재지정되는 경우입니다 — 따라서 신호 없이 종료된 호스트는 고아 프로세스를 남기지 않습니다. HTTP 세션은 <code>DELETE /mcp</code>, SSE 연결 해제, 또는 <code>MCP_HTTP_SESSION_TIMEOUT_MS</code>가 제어하는 유휴 정리로 해제됩니다. 프로토콜에서 세션 종료는 선택 사항이므로, 그렇지 않으면 사라진 클라이언트가 Server를 영원히 붙잡게 됩니다.",
    "One-time automatic repair of token totals inflated before usage was reconciled per <code>message.id</code>. <code>replaceTokenUsage</code> is a high-water mark, so the parser fix alone can never lower a historical total. Marker-gated, deferred off the boot path, skipped while another dashboard shares the data directory, and it snapshots the old rows to <code>token_usage_pre_repair</code> first. <code>0</code> skips it \u2014 repair manually with <code>npm run repair-tokens</code>.":
      "usage \uac00 <code>message.id</code> \ub2e8\uc704\ub85c \uc870\uc815\ub418\uae30 \uc804\uc5d0 \ubd80\ud480\ub824\uc9c4 \ud1a0\ud070 \ud569\uacc4\ub97c \uc77c\ud68c\uc131\uc73c\ub85c \uc790\ub3d9 \ubcf5\uad6c\ud569\ub2c8\ub2e4. <code>replaceTokenUsage</code> \ub294 high-water mark \uc774\ubbc0\ub85c \ud30c\uc11c \uc218\uc815\ub9cc\uc73c\ub85c\ub294 \uacfc\uac70 \ud569\uacc4\ub97c \ub0ae\ucd9c \uc218 \uc5c6\uc2b5\ub2c8\ub2e4. \ub9c8\ucee4 \uae30\ubc18\uc774\uba70 \ubd80\ud305 \uacbd\ub85c\uc5d0\uc11c \uc9c0\uc5f0\ub418\uace0, \ub2e4\ub978 \ub300\uc2dc\ubcf4\ub4dc\uac00 \uac19\uc740 \ub370\uc774\ud130 \ub514\ub809\ud130\ub9ac\ub97c \uacf5\uc720\ud558\uba74 \uac74\ub108\ub6f0\uba70, \uba3c\uc800 \uae30\uc874 \ud589\uc744 <code>token_usage_pre_repair</code> \ub85c \uc2a4\ub0c5\uc20f\ud569\ub2c8\ub2e4. <code>0</code> \uc774\uba74 \uac74\ub108\ub6f0\uace0 <code>npm run repair-tokens</code> \ub85c \uc9c1\uc811 \ubcf5\uad6c\ud569\ub2c8\ub2e4.",
    "<strong>Monitor</strong> combines overview stats, active agent cards, collapsible subagent trees, recent activity, and the same task-progress previews used by Sessions. <strong>Health</strong> summarizes system score, storage, cache, errors, tool use, subagent effectiveness, model tokens, and compactions. Both tabs refresh every 5 seconds through WebSocket push, while the activity feed adapts its item count to the available viewport height.":
      "<strong>Monitor</strong>는 개요 통계, 활성 에이전트 카드, 접을 수 있는 서브에이전트 트리, 최근 활동과 Sessions에서 사용하는 동일한 작업 진행 미리보기를 결합합니다. <strong>Health</strong>는 시스템 점수, 저장소, 캐시, 오류, 도구 사용, 서브에이전트 효율, 모델 Token과 압축을 요약합니다. 두 탭은 WebSocket 푸시로 5초마다 새로 고쳐지며 활동 피드는 사용 가능한 뷰포트 높이에 맞춰 항목 수를 조정합니다.",
    "Switch between <strong>Agent</strong> and <strong>Session</strong> swim lanes for working, waiting, completed, error, and abandoned states. Yellow Waiting cards identify work paused for user input, a permission gate, or the next prompt; hover badges and column headers for the exact reason and lifecycle rules. Cards show model, cumulative cost, and current tool, while WebSocket updates keep lane counts synchronized with the live event store.":
      "작업 중, 대기, 완료, 오류, 중단 상태를 보여주는 <strong>Agent</strong>와 <strong>Session</strong> 스윔레인을 전환합니다. 노란색 Waiting 카드는 사용자 입력, 권한 승인 또는 다음 Prompt를 기다리는 작업을 표시하며 배지와 열 제목에 마우스를 올리면 정확한 이유와 수명 주기 규칙을 볼 수 있습니다. 카드는 모델, 누적 비용과 현재 도구를 보여주고 WebSocket 업데이트가 레인 수를 실시간 이벤트 저장소와 동기화합니다.",
    "Server-side rules detect event patterns, inactivity, stuck agents, and token thresholds with per-session cooldown dedup. Fired alerts appear in a live feed and can reach 14 built-in providers or a generic JSON endpoint with optional HMAC-SHA256 signing and custom headers. Delivery stays fail-safe through bounded timeouts, retries, secret redaction, test probes, and per-target logs. Configure rules and channels together in <strong>Settings → Alerts</strong>.":
      "서버 측 규칙은 세션별 쿨다운 중복 제거와 함께 이벤트 패턴, 비활성, 멈춘 에이전트와 Token 임계값을 감지합니다. 발생한 알림은 실시간 피드에 표시되고 14개 내장 Provider 또는 선택적 HMAC-SHA256 서명과 사용자 Header를 지원하는 일반 JSON 엔드포인트로 전송됩니다. 제한된 타임아웃, 재시도, Secret 마스킹, 테스트 탐색과 대상별 로그로 전달이 안전하게 실패합니다. <strong>Settings → Alerts</strong>에서 규칙과 채널을 함께 구성합니다.",
    "Electron packages CCAM as a macOS <code>.dmg</code> and Windows installer or portable <code>.exe</code>. The app embeds the Express server in-process and renders the React client in a native window, with tray controls, application menus, login startup, and a single-instance lock. Closing the window leaves the local server running, and first boot installs Claude Code hooks automatically so events flow without a repository checkout.":
      "Electron은 CCAM을 macOS <code>.dmg</code>와 Windows 설치 프로그램 또는 휴대용 <code>.exe</code>로 패키징합니다. 앱은 Express 서버를 프로세스 안에 포함하고 React 클라이언트를 네이티브 창에 렌더링하며 트레이 제어, 애플리케이션 메뉴, 로그인 시작과 단일 인스턴스 잠금을 제공합니다. 창을 닫아도 로컬 서버는 계속 실행되고 첫 실행 시 Claude Code 훅을 자동 설치하므로 저장소를 체크아웃하지 않아도 이벤트가 수집됩니다.",
    "Compare model tokens, tool frequency, agent types, session outcomes, and total input, output, and cache spend. A 52-week activity heatmap and 30-day cost and session sparklines reveal longer trends, while WebSocket refresh and a live indicator keep current values clear. Responsive charts adapt to mobile screens; long legends paginate with localized controls without changing compact legends.":
      "모델 Token, 도구 빈도, 에이전트 유형, 세션 결과와 입력, 출력, 캐시 총비용을 비교합니다. 52주 활동 히트맵과 30일 비용 및 세션 스파크라인이 장기 추세를 보여주며 WebSocket 새로 고침과 실시간 표시기가 현재 값을 명확하게 유지합니다. 반응형 차트는 모바일 화면에 맞춰지고 긴 범례는 지역화된 제어로 페이지를 나누며 짧은 범례는 그대로 유지합니다.",
    "Measure subagent effectiveness, workflow patterns, model delegation, error propagation, concurrency, and complexity in one view. Expand detected patterns for their full step chain, statistics, and loop-aware narrative; info popovers explain each headline metric, and the status filter applies across the page. Data-driven legends paginate when needed so every label remains reachable without making chart cards excessively tall.":
      "한 화면에서 서브에이전트 효율, 워크플로 패턴, 모델 위임, 오류 전파, 동시성과 복잡도를 측정합니다. 감지된 패턴을 펼쳐 전체 단계 체인, 통계와 루프 인식 설명을 확인할 수 있고 정보 팝오버가 각 주요 지표를 설명하며 상태 필터는 페이지 전체에 적용됩니다. 데이터 기반 범례는 필요할 때 페이지를 나눠 차트 카드를 지나치게 높이지 않으면서 모든 라벨에 접근할 수 있게 합니다.",
    "Start Claude Code or Codex from a streaming browser chat, using multi-turn, headless, or resumable sessions. Re-attachment reconciles memory with the saved transcript, while slash-command completion, file references, token context, and effort controls bring core TUI workflows to the dashboard. Codex uses a native interactive <code>app-server</code> thread with provider approval, sandbox, and signed-in model discovery; same-origin checks protect every spawn.":
      "스트리밍 브라우저 채팅에서 Claude Code 또는 Codex를 시작하고 다중 턴, headless 또는 재개 가능한 세션을 사용할 수 있습니다. 다시 연결하면 메모리와 저장된 트랜스크립트가 조정되고 슬래시 명령 완성, 파일 참조, Token 컨텍스트와 effort 제어가 핵심 TUI 흐름을 Dashboard로 가져옵니다. Codex는 Provider 승인, Sandbox와 로그인 모델 탐색을 지원하는 네이티브 대화형 <code>app-server</code> 스레드를 사용하며 동일 출처 검사가 모든 실행을 보호합니다.",
    "Open any session for overview counters, top-tool and subagent breakdowns, a collapsible agent tree, and a chronological event timeline. When task state exists, an owner-aware panel adds a segmented donut, active task, completion bar, and paginated rows. The conversation view renders Markdown, highlighted code, tool blocks, slash-command output, renames, and correctly attributed queued messages. Waiting sessions explain why and for how long; export the record as JSON or share its permalink.":
      "세션을 열면 개요 카운터, 주요 도구 및 서브에이전트 분포, 접을 수 있는 에이전트 트리, 시간순 이벤트 타임라인을 볼 수 있습니다. 작업 상태가 있으면 소유자 인식 패널에 분할 도넛, 활성 작업, 완료 막대와 페이지가 나뉜 행이 표시됩니다. 대화 보기는 Markdown, 강조된 코드, 도구 블록, 슬래시 명령 출력, 이름 변경과 올바르게 분류된 대기 메시지를 렌더링합니다. 대기 중인 세션은 이유와 시간을 설명하며, 기록을 JSON으로 내보내거나 고정 링크를 공유할 수 있습니다.",
    "Background sync keeps new Claude project folders and session files visible after the one-time startup import. An immediate sweep, debounced filesystem watcher, and optional 30-second safety poll share one mtime cache and coalesce overlapping work; set <code>DASHBOARD_SESSION_SYNC_MS</code> to tune or disable only the poll. Each sweep parses only changed transcripts, skips unchanged imports, and broadcasts session and main-agent updates so the UI refreshes live across macOS, Windows, and Linux.":
      "백그라운드 동기화는 일회성 시작 가져오기 이후에도 새 Claude 프로젝트 폴더와 세션 파일을 표시합니다. 즉시 스윕, 디바운스된 파일 시스템 감시기, 선택적 30초 안전 폴링이 하나의 mtime 캐시를 공유하고 겹치는 작업을 합칩니다. <code>DASHBOARD_SESSION_SYNC_MS</code>로 폴링만 조정하거나 끌 수 있습니다. 각 스윕은 변경된 트랜스크립트만 파싱하고 변경 없는 가져오기를 건너뛰며 세션과 메인 에이전트 업데이트를 브로드캐스트해 macOS, Windows, Linux에서 UI를 실시간 갱신합니다.",
    "Configure per-model input, output, cache, and Codex reasoning rates in Settings, then see consistent totals across Sessions, Session Detail, Analytics, Kanban, and agent cards. Compaction-aware accounting preserves usage across context compression, imports reuse the same pricing table, and rate edits recalculate stored sessions. Subagent cards show only their own transcript cost while main agents retain session totals. GPT pricing distinguishes standard, long-context, and Fast modes, leaving unpublished tiers explicitly unpriced.":
      "Settings에서 모델별 입력, 출력, 캐시 및 Codex 추론 요금을 구성하고 Sessions, Session Detail, Analytics, Kanban과 에이전트 카드에서 일관된 합계를 확인할 수 있습니다. 압축 인식 계산은 컨텍스트 압축 후에도 사용량을 보존하고, 가져오기는 같은 요금표를 사용하며, 요금 변경 시 저장된 세션을 다시 계산합니다. 서브에이전트 카드는 자체 트랜스크립트 비용만 표시하고 메인 에이전트는 세션 합계를 유지합니다. GPT 요금은 표준, 긴 컨텍스트, Fast 모드를 구분하며 공개되지 않은 구간은 명시적으로 미책정 상태로 둡니다.",
    "Explore Claude Code configuration in 12 tabs covering skills, subagents, commands, styles, plugins, MCP, hooks, settings, memory, keybindings, and statusline scripts. The page resolves <code>/config</code> values across scopes, searches project memory, links <code>MEMORY.md</code> entries to their source facts, and shows plugin authorship. Low-risk text, memory, and keybinding files support create, edit, and delete with timestamped backups; plugins, MCP, hooks, and live settings remain read-only with copyable CLI guidance.":
      "스킬, 서브에이전트, 명령, 스타일, 플러그인, MCP, 훅, 설정, 메모리, 키 바인딩 및 상태 표시줄 스크립트를 다루는 12개 탭에서 Claude Code 구성을 살펴볼 수 있습니다. 페이지는 범위별 <code>/config</code> 값을 해석하고 프로젝트 메모리를 검색하며 <code>MEMORY.md</code> 항목을 원본 사실에 연결하고 플러그인 작성자를 표시합니다. 위험도가 낮은 텍스트, 메모리, 키 바인딩 파일은 타임스탬프 백업과 함께 생성, 편집, 삭제할 수 있으며 플러그인, MCP, 훅과 실시간 설정은 복사 가능한 CLI 안내와 함께 읽기 전용으로 유지됩니다.",
    "Browse every recorded session in a <strong>server-paginated</strong> table with fast search, project and status filters, sorting, cost, model, duration, and agent counts. The first page can show a temporary local Codex startup row until its durable session arrives. Rows with task state add a compact progress donut and owner-aware preview; stale trackers disappear when newer work no longer uses them or a turn ends unfinished, while completed progress remains available. Open any durable row for its conversation, events, and agent hierarchy.":
      "빠른 검색, 프로젝트 및 상태 필터, 정렬, 비용, 모델, 지속 시간, Agent 수를 제공하는 <strong>서버 페이지네이션</strong> 테이블에서 모든 기록된 세션을 탐색합니다. 첫 페이지는 영구 세션이 도착할 때까지 임시 로컬 Codex 시작 행을 표시할 수 있습니다. 작업 상태가 있는 행에는 컴팩트 진행률 도넛과 소유자별 미리보기가 추가됩니다. 새 작업이 더 이상 tracker를 사용하지 않거나 턴이 미완료로 끝나면 오래된 tracker가 사라지고, 완료된 진행률은 유지됩니다. 영구 행을 열어 대화, 이벤트, Agent 계층을 확인할 수 있습니다.",
    "Reads Claude and Codex transcripts incrementally, keeping long-running sessions fast while powering conversation history, live titles, prompt previews, attachments, and turn statistics. Renames and recent human prompts flow to session and agent cards in real time, older conversation pages load on demand, and duplicate Codex records collapse into one readable turn. Stable transcript identities also prevent replayed events from inflating totals, while provider-aware process matching keeps historical sessions from appearing active.":
      "Claude와 Codex 트랜스크립트를 증분 방식으로 읽어 장시간 실행되는 세션도 빠르게 유지하면서 대화 기록, 실시간 제목, 프롬프트 미리보기, 첨부 파일, 턴 통계를 제공합니다. 이름 변경과 최근 사용자 프롬프트는 세션 및 Agent 카드에 실시간으로 반영되고, 이전 대화 페이지는 필요할 때 로드되며, 중복 Codex 레코드는 읽기 쉬운 하나의 턴으로 합쳐집니다. 안정적인 트랜스크립트 ID는 재생된 이벤트가 합계를 부풀리는 것도 막고, provider 인식 프로세스 매칭은 과거 세션이 active로 표시되지 않도록 합니다.",
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. The first page also shows the same local, in-memory Codex startup row used by Dashboard and Kanban. It appears immediately but stays non-navigable until a durable session ID replaces it, without changing durable totals or pagination. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, estimated cost, and — when the provider exposed task state — a compact completion donut beside the status badge. Hovering or focusing the donut opens a viewport-clamped preview with current work, owner labels, source, and up to five tasks. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "기록된 모든 세션을 보여주는 <strong>서버 페이지네이션</strong> 테이블입니다. 각 페이지는 해당 구간만 가져오므로 세션 수가 아무리 많아도 비용 계산 범위가 제한됩니다. 첫 페이지는 Dashboard와 Kanban에 표시되는 것과 동일한 로컬 메모리 내 Codex 시작 행도 보여줍니다. 이 행은 즉시 나타나지만 영구 세션 ID가 대체하기 전에는 이동할 수 없고, 영구 총계나 페이지네이션을 변경하지 않습니다. <code>id</code>, <code>name</code>, <code>cwd</code>에 대한 대소문자 구분 없는 검색은 300 ms 디바운스와 함께 서버 측에서 실행되며 상태 필터와 조합할 수 있습니다. 각 행은 트랜스크립트에서 실시간으로 동기화되는 실제 이름(<code>/rename</code> 또는 <code>claude -n</code> 제목, 자동 제목, 첫 사용자 프롬프트, 짧은 ID 순으로 대체), 상태 배지, Agent 수, 지속 시간, 모델, 예상 비용을 표시하며, provider가 작업 상태를 제공하면 상태 배지 옆에 컴팩트 완료 도넛도 표시합니다. 도넛에 마우스를 올리거나 포커스하면 현재 작업, 소유자 레이블, 소스, 최대 다섯 개 작업을 담은 뷰포트 제한 미리보기가 열립니다. 영구 세션 행을 클릭하면 대화 트랜스크립트와 Agent 계층이 포함된 전체 세션 상세 화면으로 이동합니다.",
    "A fresh interactive Codex process first appears as an in-memory Waiting card, including while Codex is still at its trust screen or first prompt and has no stable session ID. The card is local-only, non-navigable, excluded from durable APIs unless <code>include_transient=true</code>, and removed on process exit. A <code>SessionStart</code> hook, native live-thread row, or rollout then creates the durable session and main-agent rows without copying the temporary card into history. If the user selects an existing thread in Codex's Resume picker, that exact Codex PID opens the resumed rollout or writer lock immediately. CCAM uses that signal to switch from the temporary card to the durable resumed session before the first new message.":
      "새 대화형 Codex 프로세스는 안정적인 세션 ID가 없고 신뢰 화면이나 첫 프롬프트에 머물러 있어도 먼저 메모리 내 Waiting 카드로 표시됩니다. 이 카드는 로컬 전용이며 이동할 수 없고, <code>include_transient=true</code>를 사용하지 않으면 영구 API에서 제외되며 프로세스 종료 시 제거됩니다. 이후 <code>SessionStart</code> Hook, 네이티브 live-thread 행 또는 rollout이 임시 카드를 기록에 복사하지 않고 영구 세션과 메인 Agent 행을 만듭니다. 사용자가 Codex의 Resume 선택기에서 기존 스레드를 고르면 해당 Codex PID가 재개된 rollout 또는 writer lock을 즉시 엽니다. CCAM은 이 신호를 사용해 첫 새 메시지 전에 임시 카드에서 영구 재개 세션으로 전환합니다.",
    '<span class="caption-icon">📋</span> <span><strong>Task Progress · Overview</strong> — Dashboard agent cards and Sessions rows reuse the same compact completion donut beside status; hover or focus opens an owner-aware preview of current work and task states</span>':
      '<span class="caption-icon">📋</span> <span><strong>작업 진행률 · 개요</strong> — Dashboard Agent 카드와 Sessions 행은 상태 옆에 동일한 컴팩트 완료 도넛을 사용하며, 마우스를 올리거나 포커스하면 현재 작업과 작업 상태를 소유자별로 보여주는 미리보기가 열립니다</span>',
    '<span class="caption-icon">✅</span> <span><strong>Task Progress · Session Detail</strong> — the full owner-aware tracker combines a segmented completion donut, active task, completion bar, owner breakdown, and a task list paginated at 10 rows per page</span>':
      '<span class="caption-icon">✅</span> <span><strong>작업 진행률 · 세션 상세</strong> — 전체 소유자 인식 추적기는 분할 완료 도넛, 활성 작업, 완료 막대, 소유자 분석, 페이지당 10개 행으로 나뉜 작업 목록을 제공합니다</span>',
    "Two tabs: <strong>Monitor</strong> shows overview stats, active agent cards with collapsible subagent hierarchy, and a recent activity feed whose item count fills available viewport height. When an active session exposes task state, its card shows the same completion donut as the Sessions table immediately before the status badge; hover or focus opens the detailed task preview. <strong>Health</strong> renders a composite system health score ring, storage engine donut chart, cache/error/success gauges, tool invocation bars, subagent effectiveness ratios, model token distribution, and compaction stats. Both tabs auto-refresh every 5 seconds via WebSocket push so the view is always current without manual reload.":
      "두 개의 탭으로 구성됩니다. <strong>Monitor</strong>는 개요 통계, 접을 수 있는 서브에이전트 계층이 포함된 활성 Agent 카드, 사용 가능한 뷰포트 높이에 맞춰 항목 수가 채워지는 최근 활동 피드를 표시합니다. 활성 세션에 작업 상태가 있으면 카드의 상태 배지 바로 앞에 Sessions 표와 동일한 완료 도넛이 표시되며, 마우스를 올리거나 포커스하면 상세 작업 미리보기가 열립니다. <strong>Health</strong>는 종합 시스템 상태 점수 링, 스토리지 엔진 도넛 차트, 캐시/오류/성공 게이지, 도구 호출 막대, 서브에이전트 효율 비율, 모델별 Token 분포, 컴팩션 통계를 렌더링합니다. 두 탭 모두 WebSocket 푸시를 통해 5초마다 자동 새로고침되므로 수동으로 다시 불러오지 않아도 최신 상태를 유지합니다.",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. When observable task state exists, an owner-aware progress panel appears directly below the overview with a segmented donut, active task, completion bar, owner breakdown, and dense task rows paginated at 10 tasks per page. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "접을 수 있는 Agent 계층 트리와 모든 도구 호출 이름 및 요약을 보여주는 전체 시간순 이벤트 타임라인을 통해 세션별로 깊이 있게 살펴볼 수 있습니다. 상단의 개요 패널은 이벤트, 도구 호출, 서브에이전트, 컴팩션, 오류, 지속 시간에 대한 타일 카운터를 표시합니다. 상위 도구 사용 막대그래프와 서브에이전트 유형 분석을 통해 분포를 빠르게 파악할 수 있습니다. 관찰 가능한 작업 상태가 있으면 개요 바로 아래에 소유자 인식 진행 패널이 표시되며, 분할 도넛, 활성 작업, 완료 막대, 소유자 분석, 페이지당 10개로 나뉜 밀도 높은 작업 행을 제공합니다. 대화 뷰어는 구문 강조가 적용된 Markdown, 도구별 스타일 블록, 캡처된 TUI 출력이 포함된 슬래시 명령 필, 인라인 세션 이름 변경 마커를 렌더링합니다. 턴 도중 입력되어 Claude가 작업하는 동안 대기한 메시지는 Claude가 실제로 받은 위치에 표시되고, 하네스 알림은 System으로 표시됩니다. 전체 세션을 JSON으로 내보내거나 비동기 검토를 위해 고유 링크를 공유할 수 있습니다. 세션이 사용자를 기다리는 동안 헤더 아래 노란색 배너에 이유와 대기 시간이 표시됩니다.",
    '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — live overview of active sessions and agents. Cards with task state show a progress donut beside status with the same detailed preview as Sessions. Stats tiles, collapsible subagent hierarchy cards, and a recent activity feed auto-refresh via WebSocket</span>':
      '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — 활성 세션 및 Agent의 실시간 개요입니다. 작업 상태가 있는 카드는 상태 옆에 진행 도넛과 Sessions와 동일한 상세 미리보기를 표시합니다. 통계 타일, 접을 수 있는 서브에이전트 계층 카드, 최근 활동 피드는 WebSocket을 통해 자동 새로고침됩니다</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles, an owner-aware task-progress panel with 10 tasks per page when available, top-tool usage, subagent breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — 개요 타일, 사용 가능한 경우 페이지당 10개 작업을 표시하는 소유자 인식 작업 진행 패널, 상위 도구 사용량, 서브에이전트 분석, 접을 수 있는 부모-자식 Agent 계층 트리</span>',
    "<strong>React 19</strong>": "<strong>React 19</strong>",
    "<strong>Vite 7</strong>": "<strong>Vite 7</strong>",
    "<strong>React Router 8</strong>": "<strong>React Router 8</strong>",
    '<span class="caption-icon">📊</span> <span><strong>Observability · Grafana</strong> — default home dashboard <em>CCAM — Overview</em> (four boards auto-provisioned): live fleet snapshot, database totals, breakdown charts, and rates from <code>/api/metrics</code> scrapes. The npm-only local helper uses <code>admin</code> / <code>admin</code>; container stacks read the admin password from <code>deployments/secrets/grafana-admin-password</code>.</span>':
      '<span class="caption-icon">📊</span> <span><strong>관측성 · Grafana</strong> — 네 개의 보드가 자동 프로비저닝되는 기본 홈 대시보드 <em>CCAM — Overview</em>: 실시간 플릿 스냅샷, 데이터베이스 합계, 분해 차트, <code>/api/metrics</code> 스크레이프 기반 비율을 제공합니다. npm 전용 로컬 도우미만 <code>admin</code> / <code>admin</code>을 사용하며, 컨테이너 스택은 <code>deployments/secrets/grafana-admin-password</code>에서 관리자 비밀번호를 읽습니다.</span>',
    '<span class="caption-icon">📊</span> <span><strong>Grafana · CCAM — Overview</strong> — default home dashboard after login on port 3000: fleet snapshot, database totals, breakdown charts, and rates from live <code>/api/metrics</code> scrapes — no sample or synthetic data. The npm-only local helper uses <code>admin</code> / <code>admin</code>; container stacks use <code>deployments/secrets/grafana-admin-password</code>.</span>':
      '<span class="caption-icon">📊</span> <span><strong>Grafana · CCAM — Overview</strong> — 포트 3000 로그인 후 기본 홈 대시보드로 플릿 스냅샷, 데이터베이스 합계, 분해 차트, 실시간 <code>/api/metrics</code> 스크레이프 비율을 보여주며 샘플이나 합성 데이터는 없습니다. npm 전용 로컬 도우미만 <code>admin</code> / <code>admin</code>을 사용하고 컨테이너 스택은 <code>deployments/secrets/grafana-admin-password</code>를 사용합니다.</span>',
    "<strong>CCAM — Overview</strong> (default home), <strong>CCAM — Sessions &amp; Agents</strong>, <strong>CCAM — Tokens &amp; Events</strong>, and <strong>CCAM — Platform</strong> — all provisioned from <code>monitoring/grafana/dashboards/</code> with PromQL against your live scrape. The npm-only local helper uses <code>admin</code> / <code>admin</code> via <code>monitoring/grafana.defaults.env</code>. Container stacks read the password from <code>deployments/secrets/grafana-admin-password</code>.":
      "<strong>CCAM — Overview</strong>(기본 홈), <strong>CCAM — Sessions &amp; Agents</strong>, <strong>CCAM — Tokens &amp; Events</strong>, <strong>CCAM — Platform</strong>은 모두 <code>monitoring/grafana/dashboards/</code>에서 프로비저닝되고 실시간 스크레이프에 PromQL을 사용합니다. npm 전용 로컬 도우미는 <code>monitoring/grafana.defaults.env</code>를 통해 <code>admin</code> / <code>admin</code>을 사용합니다. 컨테이너 스택은 <code>deployments/secrets/grafana-admin-password</code>에서 비밀번호를 읽습니다.",
    "Base URL used by the local MCP server when calling dashboard APIs. Direct loopback HTTP is allowed with a bearer token; tokenized container-host aliases require HTTPS.":
      "로컬 MCP 서버가 대시보드 API를 호출할 때 사용하는 기본 URL입니다. 직접 루프백 HTTP는 Bearer Token을 사용할 수 있지만 토큰이 있는 컨테이너 호스트 별칭은 HTTPS가 필요합니다.",
    "Bearer token for a protected dashboard. Falls back to <code>DASHBOARD_API_TOKEN</code>.":
      "보호된 대시보드용 Bearer Token입니다. 설정되지 않으면 <code>DASHBOARD_API_TOKEN</code>을 사용합니다.",
    "Restore one export bundle up to 25 MiB (multipart <code>file</code> or JSON <code>{ path }</code>). The restore is idempotent and non-destructive, so it can consolidate machines.":
      "최대 25 MiB의 내보내기 번들 하나를 복원합니다(multipart <code>file</code> 또는 JSON <code>{ path }</code>). 복원은 멱등적이고 비파괴적이므로 여러 머신의 데이터를 통합할 수 있습니다.",
    "All dashboard requests reject redirects. Direct loopback HTTP may carry the dashboard bearer token, while tokenized container-host aliases require HTTPS. History uploads are capped at 50 MiB per file and 100 MiB per call, binary responses at 10 MiB, and backup restore at 25 MiB. Only GET requests retry automatically.":
      "모든 대시보드 요청은 리디렉션을 거부합니다. 직접 루프백 HTTP는 대시보드 Bearer Token을 사용할 수 있지만 토큰이 있는 컨테이너 호스트 별칭은 HTTPS가 필요합니다. 기록 업로드는 파일당 50 MiB 및 호출당 총 100 MiB, 바이너리 응답은 10 MiB, 백업 복원은 25 MiB로 제한됩니다. GET 요청만 자동 재시도됩니다.",
    "Configuration file reads canonicalize the target and allowed roots, so symlinks cannot escape the trusted Claude directories.":
      "구성 파일 읽기는 대상과 허용된 루트를 정규화하므로 심볼릭 링크가 신뢰된 Claude 디렉터리 밖으로 벗어날 수 없습니다.",
    "Codex preview targets are canonicalized before containment checks. Edits reject symlinked path components and refuse content containing <code>[redacted]</code>.":
      "Codex 미리보기 대상은 포함 검사를 하기 전에 정규화됩니다. 편집은 심볼릭 링크 경로 구성 요소와 <code>[redacted]</code>가 포함된 내용을 거부합니다.",
    "Redirects are rejected rather than forwarding credentials or signed headers to another URL.":
      "자격 증명이나 서명된 헤더를 다른 URL로 전달하지 않고 리디렉션을 거부합니다.",
    "Hosted providers require HTTPS. Generic and n8n targets may use HTTP for local or self-hosted receivers.":
      "호스팅 제공자는 HTTPS가 필요합니다. Generic 및 n8n 대상은 로컬 또는 자체 호스팅 수신기에 HTTP를 사용할 수 있습니다.",
    "Stakeholder-ready executive, cost, reliability, and workflow reports with explicit scope and evidence":
      "명확한 범위와 근거를 갖춘 이해관계자용 경영진, 비용, 안정성, 워크플로 보고서",
    "Long Analytics legends use compact pages with localized controls. Legends that fit on one page stay unchanged.":
      "긴 Analytics 범례는 현지화된 컨트롤이 있는 작은 페이지를 사용합니다. 한 페이지에 맞는 범례는 그대로 유지됩니다.",
    "Data-driven Workflows legends also paginate when labels would make a chart card too tall, while keeping every label reachable.":
      "데이터 기반 Workflows 범례도 레이블 때문에 차트 카드가 너무 길어질 때 페이지로 나뉘며, 모든 레이블에 계속 접근할 수 있습니다.",
    "First-run readiness follows the selected scope: Claude Code requires Claude hooks, Codex requires Codex hooks, and Both requires both. Ready selections open the dashboard immediately; otherwise only missing selected providers appear in setup.":
      "첫 실행 준비 상태는 선택한 범위를 따릅니다. Claude Code는 Claude Hook, Codex는 Codex Hook, 둘 다를 선택하면 두 세트가 모두 필요합니다. 준비된 선택은 즉시 대시보드를 열고, 그렇지 않으면 누락된 선택 제공자만 설정에 표시됩니다.",
    "Local MCP sidecar with three transport modes and 97 typed tools across 16 domain modules. It covers the complete supported app surface, including scoped data, transcripts and images, Claude/GPT pricing, workflows, alerts, webhooks, imports and restore, Claude/Codex config, Run Agent, remote sources, push, and maintenance. Every transport shares one validated catalog with bearer-token support and tiered mutation/destructive gates.":
      "로컬 MCP sidecar는 세 가지 전송 모드와 16개 도메인 모듈의 97개 타입 지정 도구를 제공합니다. 스코프 데이터, 트랜스크립트와 이미지, Claude/GPT 가격, 워크플로, 알림, Webhook, 가져오기와 복원, Claude/Codex 구성, Run Agent, 원격 소스, 푸시 및 유지보수를 포함한 전체 지원 앱 표면을 다룹니다. 모든 전송은 Bearer Token과 계층형 변경/파괴 게이트가 적용된 하나의 검증된 카탈로그를 공유합니다.",
    "One shared marketplace ships 14 plugins for Claude Code and Codex, with 66 packaged skills, 18 Claude subagents, 34 Claude commands, 3 CLI helpers, and 2 MCP-enabled plugins. Every skill includes canonical frontmatter and OpenAI metadata. The skills.sh CLI discovers 76 repository skills from the same public source. Install with <code>claude plugin marketplace add</code>, <code>codex plugin marketplace add</code>, or <code>npx skills add</code>.":
      "하나의 공유 Marketplace가 Claude Code와 Codex용 14개 플러그인, 66개 패키지 스킬, 18개 Claude 서브에이전트, 34개 Claude 명령, 3개 CLI 도우미, 2개의 MCP 플러그인을 제공합니다. 모든 스킬은 표준 Frontmatter와 OpenAI 메타데이터를 포함합니다. skills.sh CLI는 동일한 공개 소스에서 76개의 저장소 스킬을 찾습니다. <code>claude plugin marketplace add</code>, <code>codex plugin marketplace add</code> 또는 <code>npx skills add</code>로 설치합니다.",
    "The Agent Monitor ships one shared marketplace containing 14 plugins for Claude Code and Codex, 66 packaged plugin skills, 18 Claude subagents, 34 Claude commands, 3 CLI helpers, 3 hook configurations, and 2 MCP-enabled plugins. The skills.sh CLI discovers 76 total repository skills. Product-specific manifests stay separate while skill instructions and validation remain shared.":
      "Agent Monitor는 Claude Code와 Codex용 14개 플러그인, 66개 패키지 플러그인 스킬, 18개 Claude 서브에이전트, 34개 Claude 명령, 3개 CLI 도우미, 3개 Hook 구성, 2개의 MCP 플러그인을 포함한 하나의 공유 Marketplace를 제공합니다. skills.sh CLI는 저장소 전체에서 76개 스킬을 찾습니다. 제품별 Manifest는 분리하고 스킬 지침과 검증은 공유합니다.",
    "Each plugin carries product-specific Claude Code and Codex manifests. The catalogs live at <code>.claude-plugin/marketplace.json</code> and <code>.agents/plugins/marketplace.json</code>. Each plugin directory contains:":
      "각 플러그인은 제품별 Claude Code 및 Codex Manifest를 포함합니다. Catalog는 <code>.claude-plugin/marketplace.json</code>과 <code>.agents/plugins/marketplace.json</code>에 있습니다. 각 플러그인 디렉터리에는 다음이 포함됩니다.",
    "Safe monitored Claude Code/Codex launch, follow-up, stop, resume, and run history":
      "안전한 Claude Code/Codex 실행, 후속 메시지, 중지, 재개 및 실행 기록.",
    "Alert rules, webhook providers, push notifications, and SSH remote collection":
      "알림 규칙, Webhook 제공자, 푸시 알림 및 SSH 원격 수집.",
    "Claude/Codex config, hooks, provider imports, backup restore, updates, and MCP operations":
      "Claude/Codex 구성, Hook, 제공자 가져오기, 백업 복원, 업데이트 및 MCP 운영.",
    "Remote sessions never emit live hooks to this dashboard, so a healthy Claude Code or Codex provider mirror remains exempt from the local process-liveness reap and watchdog transcript scan. Every successful provider sync still reconciles live status from the matching mirrored transcript: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime); within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) keeps the session <code>active</code>, otherwise it is reconciled to <code>completed</code>. If that provider's SSH sync fails, is unavailable, or is stranded in <code>syncing</code> longer than <code>DASHBOARD_STALE_MINUTES</code> — and an active session is itself older than that same window — the ordinary stale-session sweep marks it <code>abandoned</code> and completes its agents. A later fresh mirror reactivates a session that is still writing, so unavailable providers never leave permanent Waiting cards.":
      "원격 세션은 이 대시보드에 실시간 hook을 보내지 않으므로 정상 Claude Code 또는 Codex provider 미러는 로컬 프로세스 liveness 회수와 watchdog transcript 스캔에서 계속 제외됩니다. 성공한 각 provider 동기화는 해당 미러 transcript에서 실시간 상태를 조정합니다. 활동은 <b>JSONL 내부 최신 이벤트 타임스탬프</b> (미러 mtime 폴백)로 판단하며, <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (기본 10분) 안이면 세션을 <code>active</code>로 유지하고 그 밖에는 <code>completed</code>로 조정합니다. 해당 provider의 SSH 동기화가 실패하거나 사용할 수 없거나 <code>DASHBOARD_STALE_MINUTES</code>보다 오래 <code>syncing</code>에 멈춰 있고 active 세션 자체도 같은 창보다 오래되면, 일반 stale-session 스윕이 이를 <code>abandoned</code>로 표시하고 agent를 완료합니다. 나중에 새 미러가 들어오면 여전히 쓰고 있는 세션을 다시 활성화하므로 사용할 수 없는 provider가 영구적인 Waiting 카드를 남기지 않습니다.",
    '<span class="caption-icon">🛰️</span> <span><strong>Settings · Remote Data Sources</strong> — pull Claude Code and Codex activity from other machines over SSH: optionally set independent Remote Claude home and Remote Codex home paths, test each provider, sync manually or on a background poller, and switch the global data scope between local, all sources, or a specific machine with per-session source badges</span>':
      '<span class="caption-icon">🛰️</span> <span><strong>설정 · 원격 데이터 소스</strong> — SSH로 다른 머신의 Claude Code 및 Codex 활동을 가져옵니다: 선택적으로 별도 원격 Claude 홈과 원격 Codex 홈 경로를 설정하고 각 provider를 테스트한 뒤, 수동 또는 백그라운드 폴러로 동기화하며 전역 데이터 범위를 로컬 · 전체 소스 · 특정 머신 사이에서 전환하고 세션별 소스 배지를 표시합니다</span>',
    '<span class="caption-icon">🧰</span> <span><strong>Codex Config Explorer</strong> — the Codex workspace brings together <code>config.toml</code>, account models, profiles, MCP servers, projects, skills, hooks, rules, plugins, and instructions. Edit supported user-managed files with timestamped backups; <code>config.toml</code> is edit-only.</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Codex 구성 탐색기</strong> — Codex 작업 공간은 <code>config.toml</code>, 계정 모델, 프로필, MCP 서버, 프로젝트, 스킬, 훅, 규칙, 플러그인 및 지침을 한데 모읍니다. 지원되는 사용자 관리 파일은 타임스탬프 백업과 함께 편집할 수 있으며, <code>config.toml</code>은 편집 전용입니다.</span>',
    "Claude Code and Codex cards show a compact two-row history of their latest distinct human prompts beneath the provider-native title, so a short friendly name or terse follow-up never hides the active task. Claude refreshes this context from its local transcript cache during live hooks, imports, and watchdog sweeps; Codex refreshes it from rollout records and falls back to persisted <code>user_message</code> events for older imports. The transcript renders persisted PNG/JPEG/GIF/WebP attachments for Claude Code and Codex when available, while duplicate Codex response/event copies collapse into one human turn.":
      "Claude Code와 Codex 카드는 각 제공자의 네이티브 제목 아래에 최근의 서로 다른 사용자 프롬프트를 두 줄로 간결하게 표시하므로, 짧고 친숙한 이름이나 짧은 후속 요청이 진행 중인 작업을 가리지 않습니다. Claude는 실시간 Hook, 가져오기 및 watchdog 스윕 중 로컬 transcript 캐시에서 이 컨텍스트를 새로 고치고, Codex는 rollout 레코드에서 새로 고치며 오래된 가져오기는 저장된 <code>user_message</code> 이벤트로 대체합니다. Transcript는 가능한 경우 Claude Code와 Codex에 저장된 PNG/JPEG/GIF/WebP 첨부 파일을 렌더링하고, 중복된 Codex response/event 사본은 하나의 사용자 턴으로 합칩니다.",
    "<strong>Run Agent</strong> now begins by choosing Claude Code or Codex. Codex starts a native interactive <code>app-server</code> thread, offers provider-native approval and sandbox controls, and discovers models directly from the signed-in CLI so new releases appear without a dashboard update.":
      "<strong>에이전트 실행</strong>은 이제 Claude Code 또는 Codex 선택으로 시작합니다. Codex는 네이티브 대화형 <code>app-server</code> 스레드를 시작하고 제공자 고유의 승인 및 샌드박스 제어를 제공하며, 로그인한 CLI에서 모델을 직접 검색하므로 대시보드를 업데이트하지 않아도 새 모델이 나타납니다.",
    "The same page also has a <strong>Codex</strong> switch. Its workspace shows defaults, the model cache, profiles, MCP servers, projects, skills, rules, hooks, installed plugins, and instruction files from <code>CODEX_HOME</code>. Plugin cards use <code>codex plugin list</code> rather than exposing cache folders. Secret-like TOML and JSON values are redacted in previews; an explicit local editor supports <code>config.toml</code>, <code>hooks.json</code>, user rules, skills, and instruction files with atomic saves and timestamped backups. It warns that syntax is not validated, and <code>codex_config_changed</code> refreshes the view when those files change.":
      "같은 페이지에는 <strong>Codex</strong> 전환도 있습니다. 작업 공간은 <code>CODEX_HOME</code>의 기본값, 모델 캐시, 프로필, MCP 서버, 프로젝트, 스킬, 규칙, 훅, 설치된 플러그인, 지침 파일을 보여 줍니다. 플러그인 카드는 캐시 폴더를 노출하지 않고 <code>codex plugin list</code>를 사용합니다. 미리보기에서 비밀과 유사한 TOML 및 JSON 값은 마스킹되고, 명시적 로컬 편집기는 <code>config.toml</code>, <code>hooks.json</code>, 사용자 규칙, 스킬, 지침 파일을 원자적 저장과 타임스탬프 백업으로 지원합니다. 구문을 검증하지 않는다는 경고가 표시되며 파일이 바뀌면 <code>codex_config_changed</code>가 보기를 새로고침합니다.",
    "The same page also has a <strong>Codex</strong> switch. Its workspace shows defaults, the full local account model catalog, profiles, MCP servers, projects, skills, rules, hooks, installed plugins, and instruction files from <code>CODEX_HOME</code>. The model catalog is read apart from the small redacted-preview limit, so long account instructions cannot make the Models tab report zero. Create a standard named <code>&lt;name&gt;.config.toml</code> profile in place; Codex overlays it only for <code>codex --profile &lt;name&gt;</code>, and its card copies that exact command in one click. Plugin cards use <code>codex plugin list</code> rather than exposing cache folders. Secret-like TOML and JSON values are redacted in previews; an explicit local editor supports <code>config.toml</code>, profile overlays, <code>hooks.json</code>, user rules, skills, and instruction files with atomic saves and timestamped backups. User-maintained profiles, hooks, rules, skills, and instructions also have confirmed backup-backed <strong>View source / Copy path / Edit / Delete</strong> actions; a skill deletion preserves its whole directory, while <code>config.toml</code> stays edit-only. It warns that syntax is not validated, and <code>codex_config_changed</code> refreshes the view when those files change.":
      "같은 페이지에는 <strong>Codex</strong> 전환도 있습니다. 그 작업 공간은 <code>CODEX_HOME</code>의 기본값, 전체 로컬 계정 모델 카탈로그, 프로필, MCP 서버, 프로젝트, 스킬, 규칙, 훅, 설치된 플러그인 및 지침 파일을 표시합니다. 모델 카탈로그는 작은 비밀값 마스킹 미리보기 한도와 분리해 읽으므로 긴 계정 지침 때문에 Models 탭이 모델 수를 0으로 잘못 표시하지 않습니다. 표준 이름의 <code>&lt;name&gt;.config.toml</code> 프로필을 바로 만들 수 있으며 Codex는 <code>codex --profile &lt;name&gt;</code>을 사용할 때만 이를 오버레이하고, 해당 카드는 정확한 명령을 한 번에 복사합니다. 플러그인 카드는 캐시 폴더를 노출하지 않고 <code>codex plugin list</code>를 사용합니다. 미리보기에서 비밀처럼 보이는 TOML 및 JSON 값은 가려지며, 명시적 로컬 편집기는 <code>config.toml</code>, 프로필 오버레이, <code>hooks.json</code>, 사용자 규칙, 스킬 및 지침 파일을 원자적 저장 및 타임스탬프 백업과 함께 지원합니다. 사용자가 관리하는 프로필, 훅, 규칙, 스킬 및 지침에는 확인 절차와 백업이 포함된 <strong>소스 보기 / 경로 복사 / 편집 / 삭제</strong> 작업도 제공됩니다. 스킬을 삭제하면 전체 디렉터리를 보존하고, <code>config.toml</code>은 계속 편집 전용입니다. 구문은 검증되지 않는다고 경고하며 파일이 변경되면 <code>codex_config_changed</code>가 보기를 새로 고칩니다.",
    '<span class="caption-icon">🧰</span> <span><strong>Agent Config</strong> — switch between the editable Claude Code explorer and a live Codex workspace for defaults, models, profiles, MCP, projects, skills, rules, hooks, plugins, and instructions. Previews redact secret-like values; user-maintained files receive backup-backed edits.</span>':
      '<span class="caption-icon">🧰</span> <span><strong>에이전트 구성</strong> — 편집 가능한 Claude Code 탐색기와 기본값, 모델, 프로필, MCP, 프로젝트, 스킬, 규칙, 훅, 플러그인, 지침을 위한 실시간 Codex 작업 공간 사이를 전환합니다. 미리보기는 비밀과 유사한 값을 마스킹하며 사용자가 관리하는 파일은 백업과 함께 편집할 수 있습니다.</span>',
    '<span class="caption-icon">▶️</span> <span><strong>Run Agent</strong> — choose Claude Code or Codex each time you open the launcher. Claude supports Conversation or Headless mode; Codex starts a native interactive thread with approval and sandbox controls.</span>':
      '<span class="caption-icon">▶️</span> <span><strong>에이전트 실행</strong> — 실행기를 열 때마다 Claude Code 또는 Codex를 선택합니다. Claude는 대화 또는 원샷 모드를 지원하고, Codex는 승인 및 샌드박스 제어가 있는 네이티브 대화형 스레드를 시작합니다.</span>',
    '<span class="caption-icon">💬</span> <span><strong>Run Agent · live stream</strong> — Claude stream-json and Codex app-server events render together as chat, including reasoning, commands, file changes, and tool activity. Dashboard Runs lets you re-attach later.</span>':
      '<span class="caption-icon">💬</span> <span><strong>에이전트 실행 · 라이브 스트림</strong> — Claude stream-json과 Codex app-server 이벤트가 추론, 명령, 파일 변경, 도구 활동을 포함한 채팅으로 함께 표시됩니다. Dashboard Runs를 통해 나중에 다시 연결할 수 있습니다.</span>',
    "Codex also reads its native session index, so <code>/rename</code> titles reach session and agent cards in real time. Its conversation reader includes human turns plus <code>exec</code> custom-tool calls and outputs, and loads older pages when you reach the top of the transcript.":
      "Codex는 네이티브 세션 인덱스도 읽으므로 <code>/rename</code> 제목이 세션과 agent 카드에 실시간으로 반영됩니다. 대화 리더에는 사용자 턴과 <code>exec</code> custom-tool 호출 및 출력이 포함되며, transcript 상단에 도달하면 이전 페이지를 불러옵니다.",
    "Optional Codex home. Settings saves this dashboard-only override, re-arms live rollout watching, and immediately scans the new <code>sessions/</code> tree.":
      "선택적 Codex 홈입니다. 설정은 이 대시보드 전용 재정의를 저장하고 실시간 rollout 감시를 다시 시작하며 새 <code>sessions/</code> 트리를 즉시 스캔합니다.",
    "<strong>Dashboard Data</strong> lets you switch the entire app between Claude Code, Codex, or both. Sessions, agents, events, token totals, workflows, analytics, and costs refetch immediately. The same page saves separate Claude Code and Codex homes; saving the Codex home re-arms live rollout watching and performs a fresh scan.":
      "<strong>대시보드 데이터</strong>에서 앱 전체를 Claude Code, Codex 또는 둘 다로 전환할 수 있습니다. 세션, 에이전트, 이벤트, 토큰 합계, 워크플로, 분석 및 비용이 즉시 다시 조회됩니다. 같은 페이지에서 Claude Code와 Codex 홈을 별도로 저장하며, Codex 홈을 저장하면 실시간 rollout 감시를 다시 시작하고 새로 스캔합니다.",
    "The manual installer is interactive: Claude Code starts selected; use arrow keys, <kbd>Space</kbd>, then <kbd>Enter</kbd> to choose Claude Code, Codex (beta), or both. If a selected dashboard hook is already present, it warns before replacing only that entry and preserves unrelated hooks. The same guided installer is available from <strong>Settings → Hook Configuration</strong>. On the first dashboard visit, choose a data source, then use the provider-locked live-monitoring setup gate to install the selected hooks in place and review its output. You can also explicitly confirm hooks are already installed before continuing.":
      "수동 설치기는 대화형입니다. Claude Code가 기본 선택되어 있으며 방향키, <kbd>Space</kbd>, <kbd>Enter</kbd>로 Claude Code, Codex(베타), 또는 둘 모두를 선택합니다. 선택한 대시보드 Hook이 이미 있으면 해당 항목만 교체하기 전에 경고하고 관련 없는 Hook은 보존합니다. 같은 안내형 설치기는 <strong>Settings → Hook Configuration</strong>에서도 사용할 수 있습니다. 대시보드를 처음 열면 데이터 제공자를 선택한 뒤 제공자별 실시간 모니터링 설정 화면에서 선택한 Hook을 바로 설치하고 출력을 검토할 수 있습니다. 이미 설치했다는 사실을 명시적으로 확인한 후에도 계속 진행할 수 있습니다.",
    "Codex rollouts are accounted separately with fresh, cached, cache-write, output, and reasoning-output deltas. The GPT rate card distinguishes standard short context (≤272K input tokens), long context, and Fast mode; unavailable published tiers remain explicitly unpriced until you set a rate.":
      "Codex rollout은 새 입력, 캐시 입력, 캐시 쓰기, 출력 및 추론 출력 델타를 별도로 계산합니다. GPT 요금표는 표준 짧은 컨텍스트(입력 token ≤272K), 긴 컨텍스트, Fast 모드를 구분합니다. 공개 가격이 없는 티어는 요금을 설정할 때까지 명시적으로 가격 미지정 상태로 남습니다.",
    "Codex rollout records are the source of truth for live cards: <code>user_message</code> and <code>task_started</code> mark the main agent working; <code>task_complete</code> keeps the session active while showing Waiting; and <code>turn_aborted</code> shows interrupted Waiting. Newest rollouts are processed first, and an unreadable historical file is retried without delaying the rest.":
      "Codex rollout 레코드는 실시간 카드의 기준 정보입니다. <code>user_message</code>와 <code>task_started</code>는 메인 에이전트를 작업 중으로 표시하고, <code>task_complete</code>는 세션을 active로 유지한 채 대기 중으로 표시하며, <code>turn_aborted</code>는 중단된 대기 중으로 표시합니다. 최신 rollout을 먼저 처리하고 읽을 수 없는 과거 파일은 나머지를 지연시키지 않고 다시 시도합니다.",
    "Codex <code>response_item</code> tool calls are indexed exactly once through a separate rollout cursor, so provider-scoped Workflows shows recorded tool flows, session drill-ins, token/model totals, and <code>context_compacted</code> counts without replaying lifecycle or token accounting. Dynamic Workflows stays hidden in a Codex-only scope because its on-disk journals are a Claude Code feature.":
      "Codex <code>response_item</code> 도구 호출은 별도 rollout 커서로 정확히 한 번 인덱싱되므로 provider 범위 Workflows는 수명 주기나 token 집계를 재생하지 않고 기록된 도구 흐름, 세션 drill-in, token/model 합계와 <code>context_compacted</code> 횟수를 보여 줍니다. Dynamic Workflows는 디스크 journal이 Claude Code 기능이므로 Codex 전용 범위에서 숨겨집니다.",
    "Optional Codex home. Defaults to <code>CODEX_HOME</code> or <code>~/.codex</code>.":
      "선택적 Codex 홈입니다. 기본값은 <code>CODEX_HOME</code> 또는 <code>~/.codex</code>입니다.",
    "Safety-net interval (ms) for incremental Codex rollout scanning. Default <code>4000</code>; <code>0</code> disables polling but keeps the watcher.":
      "증분 Codex rollout 스캔을 위한 안전망 간격(ms)입니다. 기본값은 <code>4000</code>이며, <code>0</code>은 폴링만 끄고 watcher는 유지합니다.",
    "How long a hook-only Codex session may leave a reported-finished turn unanswered before its <code>SessionEnd</code> is presumed lost. A run started with <code>codex exec --ephemeral</code> writes no rollout, so it has neither a transcript mtime nor an open file for the liveness probes — the fallback that matters most on Windows, where both probes are unavailable. Only a session already parked by a real <code>Stop</code> hook qualifies, because <code>SessionEnd</code> follows <code>Stop</code> within a few hundred milliseconds. Silence never qualifies: a rollout-less run emits no hooks at all while a tool runs, so an idle timer would complete a live CI build. A session still running self-heals on its next hook.":
      "hook 전용 Codex 세션이 종료를 보고한 턴에 응답이 없을 때, <code>SessionEnd</code>가 유실되었다고 판단하기까지 기다리는 시간입니다. <code>codex exec --ephemeral</code>로 시작한 실행은 rollout을 쓰지 않으므로 transcript mtime도, liveness 프로브가 볼 열린 파일도 없습니다 — 두 프로브가 모두 불가능한 Windows에서 특히 중요한 대비책입니다. 실제 <code>Stop</code> hook으로 이미 대기 상태가 된 세션만 대상이며, <code>SessionEnd</code>는 <code>Stop</code> 이후 수백 밀리초 안에 도착하기 때문입니다. 침묵은 결코 조건이 되지 않습니다: rollout이 없는 실행은 도구가 실행되는 동안 hook을 전혀 보내지 않으므로, 유휴 타이머는 실행 중인 CI 빌드를 완료로 처리해 버립니다. 실제로 실행 중인 세션은 다음 hook에서 자동 복구됩니다.",
    "CCAM exposes a read-only <strong>Prometheus text-exposition</strong> endpoint at <code>GET /api/metrics</code> — sessions and agents by status, cumulative events and tokens, connected WebSocket clients, configured remote sources, process uptime/RSS, and build version — so the same dashboard you use for Claude Code monitoring can be scraped into your own observability stack. A turnkey stack lives in <code>monitoring/</code>: npm-managed binaries (no Homebrew/apt) or optional Docker Compose, with Grafana datasource and four dashboards auto-provisioned on first start.":
      "CCAM은 <code>GET /api/metrics</code>에서 읽기 전용 <strong>Prometheus 텍스트 노출</strong> 엔드포인트를 제공합니다 — 상태별 sessions/agents, 누적 이벤트·토큰, 연결된 WebSocket 클라이언트, 구성된 원격 소스, 프로세스 uptime/RSS, 빌드 버전 — Claude Code 모니터링에 쓰는 동일 대시보드를 자체 관측성 스택으로 스크레이핑할 수 있습니다. 턴키 스택은 <code>monitoring/</code>에 있으며 npm 관리 바이너리(Homebrew/apt 불필요) 또는 선택적 Docker Compose로 첫 시작 시 Grafana 데이터소스와 네 개 대시보드를 자동 프로비저닝합니다.",
    "<strong>Quick start (npm):</strong> run the dashboard on loopback, then <code>npm run monitoring:install</code> once and <code>npm run monitoring:up</code>. Open Grafana at <code>http://localhost:3000</code> and Prometheus at <code>http://localhost:9090</code>. The pre-built CCAM console is at <code>http://localhost:9090/consoles/index.html</code> (also reachable from the Prometheus UI under <strong>Consoles</strong>). For an all-Docker path use <code>npm run monitoring:docker:up</code> or <code>npm run docker:full:up</code> for dashboard + Prometheus + Grafana together. Verify with <code>npm run monitoring:verify</code> — the <code>ccam</code> scrape target should read <strong>UP</strong>.":
      "<strong>빠른 시작(npm):</strong> 루프백에서 대시보드를 실행한 뒤 <code>npm run monitoring:install</code> 한 번과 <code>npm run monitoring:up</code>. Grafana <code>http://localhost:3000</code>, Prometheus <code>http://localhost:9090</code>. 사전 구축 CCAM 콘솔: <code>http://localhost:9090/consoles/index.html</code>(Prometheus UI <strong>Consoles</strong>에서도 가능). 전체 Docker: <code>npm run monitoring:docker:up</code> 또는 <code>npm run docker:full:up</code>. <code>npm run monitoring:verify</code>로 확인 — <code>ccam</code> 스크레이프 대상이 <strong>UP</strong>이어야 합니다.",
    "Four Grafana boards": "Grafana 보드 네 개",
    "Prometheus console": "Prometheus 콘솔",
    "Prometheus 3.x dropped the legacy console template libraries, so CCAM ships a <strong>static HTML console</strong> at <code>monitoring/prometheus/consoles/index.html</code> that fetches <code>/api/v1/query</code> directly — live cards, session tables, and Graph links without Go-template errors on <code>/consoles/index.html</code>.":
      "Prometheus 3.x는 레거시 콘솔 템플릿 라이브러리를 제거했으므로 CCAM은 <code>monitoring/prometheus/consoles/index.html</code>에 <code>/api/v1/query</code>를 직접 호출하는 <strong>정적 HTML 콘솔</strong>을 제공합니다 — 실시간 카드, 세션 표, Graph 링크, <code>/consoles/index.html</code> Go 템플릿 오류 없음.",
    "Recording rules": "기록 규칙",
    "Scrape &amp; auth guard": "스크레이프 및 인증 가드",
    "Loopback scrapes (<code>127.0.0.1:4820</code>) work out of the box. When Prometheus runs in Docker and reaches the host via <code>host.docker.internal</code>, add that host to <code>DASHBOARD_ALLOWED_HOSTS</code> or the scrape returns <code>403 EBADHOST</code>. If <code>DASHBOARD_TOKEN</code> is set, scrapers must send the same bearer token.":
      "루프백 스크레이프(<code>127.0.0.1:4820</code>)는 즉시 동작합니다. Docker에서 Prometheus가 <code>host.docker.internal</code>로 호스트에 접근할 때 해당 호스트를 <code>DASHBOARD_ALLOWED_HOSTS</code>에 추가하지 않으면 <code>403 EBADHOST</code>가 반환됩니다. <code>DASHBOARD_TOKEN</code>이 설정되면 스크레이퍼가 동일한 bearer 토큰을 보내야 합니다.",
    "Full reference": "전체 참고",
    'Commands, Docker vs npm paths, dashboard UIDs, starter PromQL, and troubleshooting (Grafana login reset, target DOWN) are documented in <a href="../monitoring/README.md"><code>monitoring/README.md</code></a> and the <a href="#api-reference">API reference</a> entry for <code>GET /api/metrics</code>.':
      '명령, Docker vs npm 경로, 대시보드 UID, 시작 PromQL, 문제 해결(Grafana 로그인 재설정, target DOWN)은 <a href="../monitoring/README.md"><code>monitoring/README.md</code></a>와 <code>GET /api/metrics</code>에 대한 <a href="#api-reference">API reference</a> 항목에 문서화되어 있습니다.',
    "Pull Claude Code history from other machines over SSH. Each remote's <code>~/.claude/projects</code> tree is mirrored via <code>scp</code> into a sandboxed staging directory, fed through the same importer as local history, and every session is tagged with its source. A background poller keeps remote data near-real-time, a global data-scope selector (Local only / All / a specific source) narrows the whole app, and sessions from a remote show a source badge. Auth defers entirely to the host's SSH stack — no passwords or secrets are stored. Managed in <b>Settings → Remote Data Sources</b>.":
      "SSH를 통해 다른 머신에서 Claude Code 기록을 가져옵니다. 각 원격 호스트의 <code>~/.claude/projects</code> 트리는 <code>scp</code>로 샌드박스화된 스테이징 디렉터리에 미러링되고, 로컬 기록과 동일한 임포터를 거치며, 모든 세션에 출처가 태그됩니다. 백그라운드 폴러가 원격 데이터를 준실시간으로 유지하고, 전역 데이터 범위 선택기(로컬 전용 / 전체 / 특정 소스)가 앱 전체 범위를 좁히며, 원격에서 온 세션은 소스 배지를 표시합니다. 인증은 전적으로 호스트의 SSH 스택에 위임되며 — 비밀번호나 비밀은 저장되지 않습니다. <b>Settings → Remote Data Sources</b>에서 관리합니다.",
    "Remote Data Sources": "원격 데이터 소스",
    "One remote source can independently import <b>Claude Code</b>, <b>Codex</b>, or both. Codex mirrors <code>~/.codex/sessions</code> and its lightweight <code>session_index.jsonl</code> title index; the connection test and sync result show a separate state for each provider, so a missing provider never hides fresh data from the other.":
      "하나의 원격 소스는 <b>Claude Code</b>, <b>Codex</b> 또는 둘 다를 독립적으로 가져올 수 있습니다. Codex는 <code>~/.codex/sessions</code>와 가벼운 <code>session_index.jsonl</code> 제목 인덱스를 미러링합니다. 연결 테스트와 동기화 결과는 provider별 상태를 따로 보여 주므로 한 provider가 없어도 다른 provider의 새 데이터가 숨겨지지 않습니다.",
    "List, add, and test SSH data sources; sync one source (or all when no id is given); and remove a source, optionally purging the sessions it imported":
      "SSH 데이터 소스를 나열, 추가, 테스트하고, 하나의 소스를 동기화하며(ID를 지정하지 않으면 전체), 소스를 제거하면서 선택적으로 가져온 세션을 정리합니다",
    "Beyond local history, the dashboard can pull Claude Code activity from <b>other machines over SSH</b> and fold it into the same views as your local sessions. Each remote's <code>~/.claude/projects</code> tree is mirrored via <code>scp</code> (or <code>wsl.exe</code> + <code>tar</code> when Claude runs in WSL on a Windows SSH host) into a sandboxed staging directory, fed through the exact same importer used for local history, and every imported session is tagged with the source it came from. A background poller (<code>DASHBOARD_REMOTE_SYNC_MS</code>, default <code>15000</code> ms) keeps remote data near-real-time — adding or re-enabling a source also triggers an immediate pull — and successful syncs broadcast <code>remote_data.updated</code> plus per-session <code>session_created</code> / <code>session_updated</code> frames. A global <b>data scope</b> selector lets you narrow the entire app to local sessions only, all sources, or a specific machine. Sources are managed in <b>Settings → Remote Data Sources</b>.":
      "로컬 기록 외에도 대시보드는 <b>SSH를 통해 다른 머신</b>에서 Claude Code 활동을 가져와 로컬 세션과 동일한 화면에 통합할 수 있습니다. 각 원격 호스트의 <code>~/.claude/projects</code> 트리는 <code>scp</code>(Windows SSH에서 Claude가 WSL에서 실행되면 <code>wsl.exe</code> + <code>tar</code>)로 샌드박스 스테이징 디렉터리에 미러링되고, 로컬 기록에 사용되는 것과 정확히 동일한 임포터를 거치며, 가져온 모든 세션에는 출처가 태그됩니다. 백그라운드 폴러(<code>DASHBOARD_REMOTE_SYNC_MS</code>, 기본 <code>15000</code> ms)가 원격 데이터를 준실시간으로 유지합니다 — 소스 추가/재활성화 시 즉시 동기화 — 성공한 동기화는 <code>remote_data.updated</code>와 세션별 <code>session_created</code> / <code>session_updated</code> 프레임을 브로드캐스트합니다. 전역 <b>데이터 범위</b> 선택기로 앱 전체를 로컬 세션만, 전체 소스, 또는 특정 머신으로 좁힐 수 있습니다. 소스는 <b>Settings → Remote Data Sources</b>에서 관리합니다.",
    "Each remote is mirrored over SSH, imported through the local pipeline, and tagged by source — the poller (default 15 s) keeps it near-real-time":
      "각 원격은 SSH로 미러링되어 로컬 파이프라인을 통해 가져와지고 소스별로 태그됩니다 — 폴러(기본 15초)가 준실시간으로 유지합니다",
    "Add a source with an SSH destination — either a <code>user@host</code> pair or an alias defined in your <code>~/.ssh/config</code> — plus an optional port, identity file, and remote home directory. Use <b>Test connection</b> to verify reachability before saving, then <b>Sync now</b> to pull immediately, or let the background poller do it on its own schedule. Removing a source can optionally purge the sessions it imported.":
      "SSH 대상 — <code>user@host</code> 쌍 또는 <code>~/.ssh/config</code>에 정의된 별칭 — 과 선택적 포트, 신원 파일, 원격 홈 디렉터리를 지정하여 소스를 추가합니다. 저장하기 전에 <b>Test connection</b>으로 연결 가능 여부를 확인한 다음 <b>Sync now</b>로 즉시 가져오거나, 백그라운드 폴러가 자체 일정에 따라 처리하도록 둡니다. 소스를 제거할 때 선택적으로 가져온 세션을 정리할 수 있습니다.",
    "A global data-scope selector narrows the whole app: choose <b>Local only</b>, <b>All</b> sources, or one specific machine. The scope flows into every data view through a <code>sources</code> query parameter on the sessions, events, agents, stats, and analytics endpoints, and sessions imported from a remote carry a <b>source badge</b> so their origin is always visible.":
      "전역 데이터 범위 선택기는 앱 전체 범위를 좁힙니다: <b>Local only</b>, <b>All</b> 소스, 또는 특정 머신 하나를 선택합니다. 이 범위는 sessions, events, agents, stats, analytics 엔드포인트의 <code>sources</code> 쿼리 매개변수를 통해 모든 데이터 화면에 반영되며, 원격에서 가져온 세션에는 <b>source badge</b>가 표시되어 출처가 항상 보입니다.",
    "Authentication defers entirely to the host's own SSH stack — <code>~/.ssh/config</code>, <code>ssh-agent</code>, on-disk keys, and <code>known_hosts</code>. The dashboard stores <b>no passwords or secrets</b>; it only records the SSH destination and connection options you supply. <code>StrictHostKeyChecking</code> is left at its default, so the remote host must already be present in <code>known_hosts</code> — an unknown host fails the connection rather than being trusted blindly.":
      "인증은 전적으로 호스트 자체의 SSH 스택 — <code>~/.ssh/config</code>, <code>ssh-agent</code>, 디스크의 키, <code>known_hosts</code> — 에 위임됩니다. 대시보드는 <b>비밀번호나 비밀을 저장하지 않으며</b>, 사용자가 제공한 SSH 대상과 연결 옵션만 기록합니다. <code>StrictHostKeyChecking</code>은 기본값으로 유지되므로 원격 호스트가 이미 <code>known_hosts</code>에 있어야 하며 — 알 수 없는 호스트는 맹목적으로 신뢰되지 않고 연결이 실패합니다.",
    "Interval for the background remote poller (add/re-enable also syncs immediately); <code>0</code> disables the poller while leaving manual sync available":
      "백그라운드 원격 폴러의 간격(추가/재활성화 시 즉시 동기화); <code>0</code>은 수동 동기화는 유지한 채 폴러를 비활성화합니다",
    "Ceiling on a single scp-and-import run before it is aborted":
      "단일 scp 및 가져오기 실행이 중단되기 전의 상한",
    "Timeout for the <b>Test connection</b> probe": "<b>Test connection</b> 프로브의 타임아웃",
    "Freshness window (10 min) for a Remote Data Source session's live status — judged from the newest JSONL event timestamp in the mirror (mtime fallback); remote sessions receive no live hooks, so this replaces the local liveness and stale sweeps (which skip them)":
      "Remote Data Source 세션의 실시간 상태에 대한 신선도 창(10분) — 미러의 최신 JSONL 이벤트 타임스탬프로 판단(mtime 폴백); 원격 세션은 실시간 hook을 받지 않으므로 이는 로컬 liveness 및 stale 스윕(이들은 원격 세션을 건너뜀)을 대체합니다",
    "Freshness window (10 min) for a Remote Data Source session's live status — judged from the newest JSONL event timestamp in the mirror (mtime fallback); healthy sources stay out of local liveness/stale checks, while errored or stale-syncing sources fall back to the global abandon sweep":
      "Remote Data Source 세션의 실시간 상태에 대한 신선도 창(10분) — 미러의 최신 JSONL 이벤트 타임스탬프로 판단(mtime 폴백); 정상 소스는 로컬 liveness/stale 검사에서 제외되고 오류 또는 오래 멈춘 syncing 소스는 전역 abandon 스윕으로 폴백합니다",
    "Because a remote session is pulled over SSH, it never emits live hooks to this dashboard, so remote sessions (<code>source</code> ≠ <code>local</code>) are excluded from every local liveness and stale sweep — the process liveness reap, the watchdog transcript scan, the startup cleanup, and the periodic abandon sweep all skip them. Instead, a remote session's live status is reconciled from the mirrored transcript on every sync: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime). If the last event is within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) the session stays <code>active</code>; once the mirror stops advancing for longer than that window, the session is reconciled to <code>completed</code>. This keeps a still-running remote session from being wrongly shown as Completed.":
      "원격 세션은 SSH를 통해 가져오기 때문에 이 대시보드에 실시간 hook을 전혀 보내지 않으므로, 원격 세션(<code>source</code> ≠ <code>local</code>)은 모든 로컬 liveness 및 stale 스윕에서 제외됩니다 — 프로세스 liveness 회수, 워치독 transcript 스캔, 시작 시 정리, 주기적 abandon 스윕이 모두 이들을 건너뜁니다. 대신 원격 세션의 실시간 상태는 매 동기화마다 미러링된 transcript로부터 조정됩니다: 활동은 <b>JSONL 내부 최신 이벤트 타임스탬프</b>로 판단됩니다(미러 mtime 폴백). 마지막 이벤트가 <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code>(기본 10분) 이내이면 세션은 <code>active</code>로 유지되고, 미러가 그 창보다 오래 진행되지 않으면 세션은 <code>completed</code>로 조정됩니다. 이는 아직 실행 중인 원격 세션이 잘못 Completed로 표시되는 것을 방지합니다.",
    "Remote sessions never emit live hooks to this dashboard, so a healthy source remains exempt from the local process-liveness reap and watchdog transcript scan. Every successful sync still reconciles live status from the mirrored transcript: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime); within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) keeps the session <code>active</code>, otherwise it is reconciled to <code>completed</code>. If an SSH sync fails — or a source is stranded in <code>syncing</code> longer than <code>DASHBOARD_STALE_MINUTES</code> — and an active session is itself older than that same window, the ordinary stale-session sweep marks it <code>abandoned</code> and completes its agents. A later fresh mirror reactivates a session that is still writing, so unavailable remotes never leave permanent Waiting cards.":
      "원격 세션은 이 대시보드에 실시간 hook을 보내지 않으므로, 정상 소스는 로컬 프로세스 liveness 회수와 워치독 transcript 스캔에서 계속 제외됩니다. 모든 성공적인 동기화는 여전히 미러 transcript에서 실시간 상태를 조정합니다: 활동은 <b>JSONL 내부 최신 이벤트 타임스탬프</b> (미러 mtime 폴백)로 판단하며, <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (기본 10분) 안이면 세션을 <code>active</code>로 유지하고, 그 밖에는 <code>completed</code>로 조정합니다. SSH 동기화가 실패하거나 소스가 <code>DASHBOARD_STALE_MINUTES</code>보다 오래 <code>syncing</code>에 멈춰 있고 active 세션 자체도 같은 창보다 오래되면, 일반 stale-session 스윕이 이를 <code>abandoned</code>로 표시하고 agent를 완료합니다. 나중에 새 미러가 들어오면 여전히 쓰고 있는 세션을 다시 활성화하므로 사용할 수 없는 원격 소스가 영구적인 대기 중 카드를 남기지 않습니다.",
    "List configured sources with their status": "구성된 소스와 상태를 나열합니다",
    "Add a source — SSH destination plus optional port, identity file, and remote home":
      "소스 추가 — SSH 대상과 선택적 포트, 신원 파일, 원격 홈",
    "Edit an existing source": "기존 소스를 편집합니다",
    "Remove a source; optionally purge the sessions it imported":
      "소스를 제거합니다; 선택적으로 가져온 세션을 정리합니다",
    "Test SSH reachability without importing anything":
      "아무것도 가져오지 않고 SSH 연결 가능 여부를 테스트합니다",
    "Trigger an immediate scp-and-import for that source":
      "해당 소스에 대해 즉시 scp 및 가져오기를 트리거합니다",
    "The <code>sources</code> query parameter is also accepted on <code>/api/sessions</code>, <code>/api/events</code>, <code>/api/agents</code>, <code>/api/stats</code>, and <code>/api/analytics</code> to scope any view to <code>local</code>, <code>all</code>, or a comma-separated list of source IDs.":
      "<code>sources</code> 쿼리 매개변수는 <code>/api/sessions</code>, <code>/api/events</code>, <code>/api/agents</code>, <code>/api/stats</code>, <code>/api/analytics</code>에서도 허용되어, 임의의 화면을 <code>local</code>, <code>all</code>, 또는 쉼표로 구분된 소스 ID 목록으로 범위를 좁힐 수 있습니다.",
    "Sync progress and connection state broadcast on <code>/ws</code> as <code>remote_source.status</code> messages, so the Settings panel and the source badges update live as a remote connects, syncs, or fails.":
      "동기화 진행 상황과 연결 상태는 <code>/ws</code>에서 <code>remote_source.status</code> 메시지로 브로드캐스트되므로, 원격이 연결, 동기화 또는 실패할 때 Settings 패널과 소스 배지가 실시간으로 업데이트됩니다.",
    "The same operations are available from the terminal via <code>ccam remote-sources</code>, with subcommands <code>list</code>, <code>add</code>, <code>test &lt;id&gt;</code>, <code>sync [id]</code> (all sources when no id is given), and <code>rm &lt;id&gt; [--purge]</code> to remove a source and optionally drop the sessions it imported.":
      "동일한 작업을 <code>ccam remote-sources</code>를 통해 터미널에서도 사용할 수 있으며, 하위 명령으로 <code>list</code>, <code>add</code>, <code>test &lt;id&gt;</code>, <code>sync [id]</code>(id를 지정하지 않으면 전체 소스), 그리고 소스를 제거하고 선택적으로 가져온 세션을 삭제하는 <code>rm &lt;id&gt; [--purge]</code>가 있습니다.",
    "The CLI also manages the server itself: <code>ccam status</code> shows an up/down indicator and <code>ccam start</code> boots a detached production server. When the server is down, read-only commands fall back to reading <code>data/dashboard.db</code> directly under an explicit offline banner, while live or server-computed commands refuse with the specific reason.":
      "CLI\ub294 \uc11c\ubc84 \uc790\uccb4\ub3c4 \uad00\ub9ac\ud569\ub2c8\ub2e4. <code>ccam status</code>\ub294 \uc2e4\ud589/\uc911\uc9c0 \ud45c\uc2dc\uae30\ub97c \ubcf4\uc5ec\uc8fc\uace0 <code>ccam start</code>\ub294 \ubd84\ub9ac\ub41c \ud504\ub85c\ub355\uc158 \uc11c\ubc84\ub97c \ubc31\uadf8\ub77c\uc6b4\ub4dc\ub85c \ub744\uc6c1\ub2c8\ub2e4. \uc11c\ubc84\uac00 \uaebc\uc838 \uc788\uc73c\uba74 \uc77d\uae30 \uc804\uc6a9 \uba85\ub839\uc740 \uba85\uc2dc\uc801\uc778 \uc624\ud504\ub77c\uc778 \ubc30\ub108 \uc544\ub798\uc5d0\uc11c <code>data/dashboard.db</code>\ub97c \uc9c1\uc811 \uc77d\ub294 \ubc29\uc2dd\uc73c\ub85c \ub300\uccb4\ub418\uba70, \uc2e4\uc2dc\uac04\uc774\uac70\ub098 \uc11c\ubc84 \uacc4\uc0b0\uc774 \ud544\uc694\ud55c \uba85\ub839\uc740 \uad6c\uccb4\uc801\uc778 \uc774\uc720\uc640 \ud568\uaed8 \uac70\ubd80\ub429\ub2c8\ub2e4.",
    "The full dashboard feature surface is also available from any terminal through the dependency-free <code>ccam</code> CLI (<code>bin/ccam.js</code>). It is linked automatically by <code>npm run setup</code> (via a fail-soft <code>npm link</code>), discovers the running server through the same <code>~/.claude/.agent-dashboard.json</code> registry the hook handler uses (override with <code>CLAUDE_DASHBOARD_PORT</code> / <code>DASHBOARD_PORT</code>), and renders a full terminal UI — box-drawn tables, status icons, inline bar charts, and agent trees — that degrades to plain text when piped (<code>--no-color</code> / <code>NO_COLOR</code> / <code>FORCE_COLOR</code> respected), so it is equally at home in an interactive shell, <code>grep</code> pipelines, or CI.":
      "대시보드의 전체 기능은 의존성 없는 <code>ccam</code> CLI(<code>bin/ccam.js</code>)를 통해 어떤 터미널에서도 사용할 수 있습니다. <code>npm run setup</code>이 자동으로 링크하며(실패해도 셋업을 막지 않는 <code>npm link</code>), hook 핸들러와 동일한 <code>~/.claude/.agent-dashboard.json</code> 레지스트리로 실행 중인 서버를 찾고(<code>CLAUDE_DASHBOARD_PORT</code> / <code>DASHBOARD_PORT</code>로 재정의 가능), 완전한 터미널 UI(박스 테이블, 상태 아이콘, 인라인 막대 차트, 에이전트 트리)를 렌더링하며 파이프로 연결되면 일반 텍스트로 낮춥니다(<code>--no-color</code> / <code>NO_COLOR</code> / <code>FORCE_COLOR</code> 지원) — 대화형 셸, <code>grep</code> 파이프라인, CI 어디에서든 자연스럽게 동작합니다.",
    "For a live monitoring session, <code>ccam repl</code> (aliases <code>shell</code> / <code>i</code>) opens an interactive shell where you type commands without the <code>ccam</code> prefix — with a CCAM welcome banner, tab-completion, persisted arrow-key history, a live server-status prompt (<code>●</code> up / <code>○</code> offline), a grouped <code>help</code> / <code>help &lt;cmd&gt;</code> menu, and a <code>watch [secs] &lt;cmd&gt;</code> built-in that auto-refreshes any command. Each entered line runs as an isolated child process, so an offline refusal or a blocking <code>tail</code> never takes the shell down.":
      "지속적인 모니터링을 위해 <code>ccam repl</code>(별칭 <code>shell</code> / <code>i</code>)은 <code>ccam</code> 접두사 없이 명령어를 입력하는 대화형 셸을 엽니다 — CCAM 환영 배너, Tab 자동 완성, 저장되는 방향키 히스토리, 실시간 서버 상태 프롬프트(<code>●</code> 실행 중 / <code>○</code> 중지), 그룹화된 <code>help</code> / <code>help &lt;cmd&gt;</code> 메뉴, 그리고 아무 명령이나 자동 새로고침하는 <code>watch [초] &lt;명령&gt;</code> 내장 기능을 제공합니다. 입력된 각 줄은 격리된 자식 프로세스로 실행되므로 오프라인 거부나 블로킹되는 <code>tail</code>이 셸을 종료시키는 일은 없습니다.",
    Group: "그룹",
    Commands: "명령",
    "What you get": "제공 기능",
    Monitoring: "모니터링",
    "Reachability, totals and status distributions, the Kanban board rendered as text columns, and a live event feed that polls for new rows every 2 seconds":
      "연결 확인, 총계와 상태 분포, 텍스트 컬럼으로 렌더링되는 Kanban 보드, 그리고 2초마다 새 행을 폴링하는 실시간 이벤트 피드",
    "Up/down indicator, a background server start that waits for health, and the interactive <code>ccam repl</code> shell — tab-completion, persisted history, a live server-status prompt, and a <code>watch</code> auto-refresh":
      "실행 여부 표시기, 헬스 상태를 기다리는 백그라운드 서버 시작, 그리고 대화형 <code>ccam repl</code> 셸 — Tab 자동 완성, 저장되는 히스토리, 실시간 서버 상태 프롬프트, 그리고 <code>watch</code> 자동 새로고침",
    Data: "데이터",
    "Filterable tables plus a per-session deep dive with an indented agent tree, per-session cost, and the most recent events":
      "필터링 가능한 테이블과 세션별 심층 보기: 들여쓰기된 에이전트 트리, 세션별 비용, 최근 이벤트",
    Insights: "인사이트",
    "Token totals and top tools, workflow-intelligence stats with detected patterns, dynamic Workflow-tool runs, and the per-model cost breakdown, which can be scoped to a single session with <code>--session</code>, surfaces server-tool surcharges (web search / code execution), warns about models with usage but no matching pricing rule (priced at $0, excluded from the total), and points to <code>ccam pricing set</code>":
      "토큰 총량과 상위 도구, 감지된 패턴이 포함된 워크플로 인텔리전스 통계, 동적 Workflow 도구 실행, 모델별 비용 분석 — <code>--session</code>으로 단일 세션에 한정할 수 있고, 서버 도구 추가 요금(웹 검색 / 코드 실행)을 표시하며, 사용량은 있지만 일치하는 가격 규칙이 없는 모델($0으로 계산되어 총액에서 제외)을 경고하고 <code>ccam pricing set</code>을 안내합니다",
    "Alerts &amp; Webhooks": "알림 &amp; Webhook",
    "The fired-alert feed with acknowledge / acknowledge-all, the rule list, and synthetic test deliveries against any webhook target":
      "발생 알림 피드(개별/전체 확인), 규칙 목록, 그리고 임의의 webhook 대상에 대한 합성 테스트 전송",
    Pricing: "가격",
    "Full pricing-rule CRUD with per-mtok rates — including 1-hour cache-write, fast-tier, and time-limited intro rates — straight from the terminal, with Fast In/Out and Intro In/Out columns in the list view":
      "백만 토큰당 요율 기반의 가격 규칙 CRUD를 터미널에서 바로 수행 — 1시간 캐시 쓰기, fast 등급, 기간 한정 intro 요율까지 포함하며, 목록 보기에는 Fast In/Out과 Intro In/Out 열이 표시됩니다",
    Import: "가져오기",
    "The same idempotent ingestion pipeline the Settings page uses, reporting imported / backfilled / skipped / error counts":
      "Settings 페이지와 동일한 멱등 수집 파이프라인으로, imported / backfilled / skipped / error 수를 보고합니다",
    Administration: "관리",
    "Connectivity and hook diagnosis, raw system info, full JSON export, session cleanup, hook reinstallation, an update check that prints how far behind the canonical remote you are plus the copy-paste update command, and a guarded wipe that refuses to run without <code>--yes</code>":
      "연결 및 hook 진단, 원시 시스템 정보, 전체 JSON 내보내기, 세션 정리, hook 재설치, 표준 원격보다 얼마나 뒤처졌는지와 복사-붙여넣기 업데이트 명령을 출력하는 업데이트 확인, 그리고 <code>--yes</code> 없이는 실행을 거부하는 보호된 전체 삭제",
    "Safe by default": "기본적으로 안전",
    "Read commands only issue GETs; mutating commands map one-to-one to explicit dashboard actions; and the single destructive command, <code>clear-data</code>, always refuses to run without an explicit <code>--yes</code> flag. Exit codes are script-friendly: 0 on success, 1 on any failure. See <code>docs/CLI.md</code> for the complete reference.":
      "읽기 명령은 GET만 보냅니다. 변경 명령은 대시보드의 명시적 작업과 일대일로 대응하며, 유일한 파괴적 명령인 <code>clear-data</code>는 명시적 <code>--yes</code> 플래그 없이는 항상 실행을 거부합니다. 종료 코드는 스크립트 친화적입니다: 성공 시 0, 모든 실패 시 1. 전체 참조는 <code>docs/CLI.md</code>를 보세요.",
    "The whole dashboard as terminal commands — <code>ccam stats</code>, <code>kanban</code>, <code>tail</code>, <code>session &lt;id&gt;</code>, <code>analytics</code>, <code>alerts</code>, <code>pricing</code>, <code>import</code>, <code>doctor</code>, <code>export</code> and more. Zero dependencies, linked by <code>npm run setup</code>, auto-discovers the running server, renders a full terminal UI (box-drawn tables, status icons, inline bar charts, agent trees), and pipes cleanly (colors off when not a TTY). The one destructive command is gated behind <code>--yes</code>.":
      "대시보드 전체를 터미널 명령으로 — <code>ccam stats</code>, <code>kanban</code>, <code>tail</code>, <code>session &lt;id&gt;</code>, <code>analytics</code>, <code>alerts</code>, <code>pricing</code>, <code>import</code>, <code>doctor</code>, <code>export</code> 등. 의존성 없음, <code>npm run setup</code>이 링크, 실행 중인 서버 자동 발견, 완전한 터미널 UI 렌더링(박스 테이블, 상태 아이콘, 인라인 막대 차트, 에이전트 트리), 파이프 출력 깔끔(TTY가 아니면 색상 해제). 유일한 파괴적 명령은 <code>--yes</code>로 보호됩니다.",
    '<span class="caption-icon">📡</span> Live dashboard — real-time agent cards, stats, and activity feed':
      '<span class="caption-icon">📡</span> 라이브 대시보드 — 실시간 에이전트 카드, 통계, 활동 피드',
    "Claude Code Agent Monitor integrates with Claude Code through its native hook system. When Claude Code performs any action — tool use, session start, subagent orchestration, session end — it fires a hook that calls a small Node.js script bundled with this project. That script forwards the event over HTTP to the dashboard server, which stores it in SQLite and broadcasts it to the browser over WebSocket.":
      "Claude Code Agent Monitor는 Claude Code의 네이티브 훅(hook) 시스템을 통해 통합됩니다. Claude Code가 도구 사용, 세션 시작, 서브에이전트 오케스트레이션, 세션 종료 등 어떤 작업을 수행하면 훅이 발생하여 이 프로젝트에 포함된 작은 Node.js 스크립트를 호출합니다. 이 스크립트는 이벤트를 HTTP를 통해 대시보드 서버로 전달하며, 서버는 이를 SQLite에 저장하고 WebSocket을 통해 브라우저로 브로드캐스트합니다.",
    "End-to-end data pipeline from Claude Code to the browser":
      "Claude Code에서 브라우저까지의 엔드투엔드 데이터 파이프라인",
    "Local-first by design": "설계부터 로컬 우선(Local-first)",
    "The server binds <code>127.0.0.1</code> (loopback) by default, so it is not network-reachable and everything runs on your machine. No data leaves your system. No API keys. No external services. Exposing it more widely is opt-in via <code>DASHBOARD_HOST</code> and should be paired with <code>DASHBOARD_TOKEN</code>.":
      "서버는 기본적으로 <code>127.0.0.1</code>(루프백)에 바인딩되므로 네트워크에서 접근할 수 없으며 모든 것이 사용자의 컴퓨터에서 실행됩니다. 데이터가 시스템 밖으로 나가지 않습니다. API 키도 필요 없습니다. 외부 서비스도 없습니다. 더 넓게 노출하려면 <code>DASHBOARD_HOST</code>를 통해 선택적으로 설정해야 하며, 이때 <code>DASHBOARD_TOKEN</code>과 함께 사용해야 합니다.",
    "Every feature is driven by real hook events — nothing is hardcoded or simulated in production mode.":
      "모든 기능은 실제 훅 이벤트에 의해 구동되며, 프로덕션 모드에서는 하드코딩되거나 시뮬레이션되는 것이 없습니다.",
    "Two tabs: <strong>Monitor</strong> shows overview stats, active agent cards with collapsible subagent hierarchy, and a recent activity feed whose item count fills available viewport height. <strong>Health</strong> renders a composite system health score ring, storage engine donut chart, cache/error/success gauges, tool invocation bars, subagent effectiveness ratios, model token distribution, and compaction stats. Both tabs auto-refresh every 5 seconds via WebSocket push so the view is always current without manual reload.":
      "두 개의 탭으로 구성됩니다. <strong>Monitor</strong> 탭은 개요 통계, 접을 수 있는 서브에이전트 계층 구조가 포함된 활성 에이전트 카드, 그리고 뷰포트의 남은 높이를 채우는 개수만큼 표시되는 최근 활동 피드를 보여줍니다. <strong>Health</strong> 탭은 종합 시스템 상태 점수 링, 스토리지 엔진 도넛 차트, 캐시/오류/성공 게이지, 도구 호출 막대그래프, 서브에이전트 효율성 비율, 모델별 토큰 분포, 컴팩션 통계를 렌더링합니다. 두 탭 모두 WebSocket 푸시를 통해 5초마다 자동 새로고침되므로 수동으로 새로고침하지 않아도 항상 최신 상태를 유지합니다.",
    "Toggle between <strong>Agents</strong> (Working / Waiting / Completed / Error) and <strong>Sessions</strong> (Active / Waiting / Completed / Error / Abandoned) swim lanes. A yellow <strong>Waiting</strong> column flags items sitting on the user — fresh prompt, between turns, or permission gate. Hover any column header for lifecycle tooltips explaining each state transition. Cards surface model name, cumulative cost, and the current tool being called. Counts update in real time via WebSocket so the board is always in sync with the live event store. Hover a Waiting badge to see why — needs input, turn done, at prompt, or interrupted.":
      "<strong>Agents</strong>(작업 중 / 대기 중 / 완료 / 오류)와 <strong>Sessions</strong>(활성 / 대기 중 / 완료 / 오류 / 중단됨) 스윔레인 사이를 전환할 수 있습니다. 노란색 <strong>Waiting</strong> 열은 새 프롬프트, 턴 사이, 또는 권한 게이트 등 사용자 입력을 기다리는 항목을 표시합니다. 열 제목에 마우스를 올리면 각 상태 전환을 설명하는 라이프사이클 툴팁이 표시됩니다. 카드에는 모델 이름, 누적 비용, 현재 호출 중인 도구가 표시됩니다. 개수는 WebSocket을 통해 실시간으로 업데이트되므로 보드는 항상 실시간 이벤트 저장소와 동기화된 상태를 유지합니다. Di chuột lên huy hiệu Waiting để xem lý do — cần phản hồi, xong lượt, chờ prompt hoặc bị ngắt.",
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, and estimated cost. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "기록된 모든 세션을 보여주는 <strong>서버 페이지네이션</strong> 테이블입니다. 각 페이지는 해당 구간만 가져오므로 세션 수가 아무리 많아도 비용 계산 범위가 제한됩니다. <code>id</code>, <code>name</code>, <code>cwd</code>에 대한 대소문자 구분 없는 검색은 300ms 디바운스와 함께 서버 측에서 실행되며, 상태 필터와 검색을 조합해 정확하게 좁힐 수 있습니다. 각 행은 세션의 실제 이름(트랜스크립트에서 실시간으로 동기화됨 — <code>/rename</code> 또는 <code>claude -n</code> 제목, 없으면 자동 생성된 제목, 그마저도 없으면 첫 사용자 프롬프트, 최종적으로 짧은 ID로 대체), 상태 배지, 에이전트 수, 지속 시간, 모델, 예상 비용을 표시합니다. 행을 클릭하면 대화 트랜스크립트와 에이전트 계층 구조가 포함된 전체 세션 상세 화면으로 이동합니다.",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "접을 수 있는 에이전트 계층 트리와 모든 도구 호출 이름 및 요약을 보여주는 전체 시간순 이벤트 타임라인을 통해 세션별로 깊이 있게 살펴볼 수 있습니다. 상단의 개요 패널은 이벤트, 도구 호출, 서브에이전트, 컴팩션, 오류, 지속 시간에 대한 타일 카운터를 표시합니다. 상위 도구 사용 막대그래프와 서브에이전트 유형 분석을 통해 분포를 빠르게 파악할 수 있습니다. 대화 뷰어는 구문 강조가 적용된 마크다운, 도구별 스타일이 지정된 블록, 캡처된 TUI 출력이 포함된 슬래시 명령 필, 인라인 세션 이름 변경 마커를 렌더링합니다. 전체 세션을 JSON으로 내보내거나 비동기 검토를 위해 고유 링크(permalink)를 공유할 수 있습니다. Khi phiên đang chờ bạn, một banner màu vàng dưới tiêu đề sẽ nêu rõ lý do và thời gian đã chờ.",
    "A rules-based alerting engine evaluates the live event stream server-side: <strong>event pattern</strong> (match event type / tool / summary text, optionally N matches within a time window), <strong>inactivity</strong>, <strong>stuck agent</strong>, and <strong>token threshold</strong> — each with per-(rule, session, agent) cooldown dedup. Fired alerts surface in a live feed and fan out to <strong>14 first-class webhook providers</strong> — Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream — plus any generic JSON endpoint (with optional HMAC-SHA256 signing and custom headers). Delivery is detached and fail-safe with a request timeout, bounded retry/backoff, secret redaction, a one-click test probe, and a per-target delivery log. Rules and channels are managed together in <strong>Settings → Alerts</strong>.":
      "규칙 기반 알림 엔진이 서버 측에서 실시간 이벤트 스트림을 평가합니다: <strong>이벤트 패턴</strong>(이벤트 유형/도구/요약 텍스트 일치, 선택적으로 특정 시간 창 내 N회 일치), <strong>비활성</strong>, <strong>정체된 에이전트</strong>, <strong>토큰 임계값</strong> — 각각 (규칙, 세션, 에이전트) 단위의 쿨다운 중복 제거를 적용합니다. 발생한 알림은 실시간 피드에 표시되며 <strong>14개의 공식 웹훅 제공자</strong> — Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream — 및 일반 JSON 엔드포인트(선택적 HMAC-SHA256 서명 및 사용자 지정 헤더 지원)로 전달됩니다. 전송은 요청 타임아웃, 제한된 재시도/백오프, 비밀 정보 마스킹, 원클릭 테스트 프로브, 대상별 전송 로그를 갖춘 분리된 안전 실패(fail-safe) 방식으로 이루어집니다. 규칙과 채널은 <strong>Settings → Alerts</strong>에서 함께 관리됩니다.",
    "A native desktop app — a macOS <code>.app</code> (shipped as a <code>.dmg</code>) and a Windows <code>.exe</code> (NSIS installer plus a no-install portable build) — built with Electron 35. It <strong>embeds the Express server in-process</strong> — <code>require()</code>-ing <code>server/index.js</code> directly, with no child process and no IPC — and renders the built React client in a <code>BrowserWindow</code>. Adds a menu-bar / notification-area (tray) icon, a native application menu, auto-start at login (macOS Login Items via <code>SMAppService</code>; Windows per-user <code>HKCU\\…\\Run</code>), and a single-instance lock. Closing the window hides it while the server keeps running, and the app auto-installs Claude Code hooks on first boot so an install-only user gets events flowing without a checkout.":
      "Electron 35로 빌드된 네이티브 데스크톱 앱 — macOS용 <code>.app</code>(<code>.dmg</code>로 배포)과 Windows용 <code>.exe</code>(NSIS 설치 프로그램 및 설치 없이 실행 가능한 포터블 빌드)입니다. <strong>Express 서버를 프로세스 내에 임베드</strong>하여 — 자식 프로세스나 IPC 없이 <code>server/index.js</code>를 직접 <code>require()</code>하고 — 빌드된 React 클라이언트를 <code>BrowserWindow</code>에 렌더링합니다. 메뉴바/알림 영역(트레이) 아이콘, 네이티브 애플리케이션 메뉴, 로그인 시 자동 시작(macOS는 <code>SMAppService</code>를 통한 로그인 항목, Windows는 사용자별 <code>HKCU\\…\\Run</code>), 단일 인스턴스 잠금 기능을 추가로 제공합니다. 창을 닫으면 서버는 계속 실행된 채로 창만 숨겨지며, 앱은 첫 실행 시 Claude Code 훅을 자동으로 설치하므로 설치만 한 사용자도 체크아웃 없이 이벤트를 바로 받아볼 수 있습니다.",
    "Real-time streaming event log showing tool calls, agent state changes, errors, and compaction events as they arrive. Pause/resume with automatic buffering, paginated history for scrollback, and auto-scrolling to the latest entry. Click any row to expand its full hook payload inline. A dedicated <strong>Session →</strong> button navigates directly to session detail without collapsing the expanded state. Every entry carries the same status badge the Dashboard and Session Detail show for that event — one shared mapping that understands both Claude and Codex event types — and rows are grouped by session for quick scanning of concurrent work.":
      "도구 호출, 에이전트 상태 변경, 오류, 컴팩션 이벤트를 도착하는 즉시 보여주는 실시간 스트리밍 이벤트 로그입니다. 자동 버퍼링을 지원하는 일시정지/재개, 스크롤백을 위한 페이지네이션된 기록, 최신 항목으로의 자동 스크롤을 제공합니다. 행을 클릭하면 전체 훅 페이로드가 인라인으로 확장됩니다. 전용 <strong>Session →</strong> 버튼을 누르면 확장 상태를 접지 않고도 세션 상세 화면으로 바로 이동합니다. 모든 항목은 Dashboard와 세션 상세가 해당 이벤트에 표시하는 것과 동일한 상태 배지를 가지며 — Claude와 Codex 이벤트 유형을 모두 이해하는 하나의 공유 매핑입니다 — 세션별로 그룹화되어 동시에 진행 중인 작업을 빠르게 훑어볼 수 있습니다.",
    "Token usage breakdown by model with stacked bar charts, tool frequency rankings, agent type distribution donuts, and session outcome pie charts. A 52-week activity heatmap aligned by day-of-week shows density with hover tooltips. 30-day sparkline trends track cost and session volume at a glance. The cost summary panel totals input, output, and cache spend across all models. A live/offline indicator and auto-refresh via WebSocket keep everything current. All charts are responsive and adapt to mobile viewports.":
      "누적 막대그래프로 모델별 토큰 사용량을 분석하고, 도구 사용 빈도 순위, 에이전트 유형 분포 도넛 차트, 세션 결과 파이 차트를 제공합니다. 요일별로 정렬된 52주 활동 히트맵은 호버 툴팁과 함께 밀도를 보여줍니다. 30일 스파크라인 추세로 비용과 세션 수를 한눈에 확인할 수 있습니다. 비용 요약 패널은 모든 모델에 걸친 입력, 출력, 캐시 지출을 합산합니다. 실시간/오프라인 표시기와 WebSocket을 통한 자동 새로고침으로 항상 최신 상태를 유지합니다. 모든 차트는 반응형이며 모바일 뷰포트에 맞춰 조정됩니다.",
    "Every UI update is pushed over a persistent WebSocket with sub-5 ms dispatch latency — zero polling anywhere. If the connection drops, automatic 2-second reconnect kicks in while a ping/pong heartbeat detects stale connections early. A sidebar indicator turns green/red so you always know whether you're live. The WebSocket carries typed JSON envelopes for new events, session updates, agent transitions, compaction results, and import progress — all parsed into the same eventBus the REST layer uses.":
      "모든 UI 업데이트는 지속적인 WebSocket 연결을 통해 5ms 미만의 전달 지연으로 푸시되며, 어디에도 폴링이 없습니다. 연결이 끊기면 2초 후 자동으로 재연결이 시작되며, ping/pong 하트비트가 오래된 연결을 조기에 감지합니다. 사이드바 표시기가 초록색/빨간색으로 바뀌어 항상 연결 상태를 알 수 있습니다. WebSocket은 새 이벤트, 세션 업데이트, 에이전트 전환, 컴팩션 결과, 가져오기 진행 상황에 대한 타입이 지정된 JSON 봉투(envelope)를 전달하며, 이는 모두 REST 계층이 사용하는 것과 동일한 eventBus로 파싱됩니다.",
    "Standalone CLI statusline for Claude Code that prints model name, user, working directory, git branch, and a color-coded context-window bar (green → yellow → red). Token counts show input (green ↑), output (cyan ↓), and cache (dim) separately. Session cost in USD shifts color by configurable thresholds. ANSI-colored output updates on every turn. Python-based with a thin shell wrapper — drop it into your prompt or tmux status line. Works with any terminal emulator that supports 256-color ANSI.":
      "모델 이름, 사용자, 작업 디렉터리, git 브랜치, 색상으로 구분된 컨텍스트 윈도우 막대(초록 → 노랑 → 빨강)를 출력하는 Claude Code용 독립 실행형 CLI 상태줄입니다. 토큰 수는 입력(초록 ↑), 출력(시안 ↓), 캐시(흐림)로 구분하여 표시됩니다. USD 기준 세션 비용은 설정 가능한 임계값에 따라 색상이 바뀝니다. ANSI 색상 출력은 매 턴마다 갱신됩니다. Python 기반이며 얇은 셸 래퍼가 포함되어 있어 프롬프트나 tmux 상태줄에 바로 넣을 수 있습니다. 256색 ANSI를 지원하는 모든 터미널 에뮬레이터에서 작동합니다.",
    "Import existing Claude Code or Codex sessions from three sources — rescan the selected default <code>~/.claude/projects</code> or <code>~/.codex/sessions</code> folder, scan any absolute path on disk, or drag-drop <code>.jsonl</code>, <code>.zip</code>, <code>.tar.gz</code>, and <code>.gz</code> archives through <b>Settings → Import History</b>. All paths use each provider's live ingestion pipeline, so imported tokens, per-model cost, and tool data match real-time capture exactly. Codex uploads are snapshotted before temporary files are cleaned up, and re-imports are idempotent.":
      "세 가지 소스에서 기존 Claude Code 또는 Codex 세션을 가져올 수 있습니다. 선택한 기본 <code>~/.claude/projects</code> 또는 <code>~/.codex/sessions</code> 폴더를 재스캔하거나, 디스크의 절대 경로를 스캔하거나, <b>Settings → Import History</b>에서 <code>.jsonl</code>, <code>.zip</code>, <code>.tar.gz</code>, <code>.gz</code> 아카이브를 드래그 앤 드롭합니다. 모든 경로는 각 공급자의 실시간 수집 파이프라인을 사용하므로 가져온 토큰, 모델별 비용 및 도구 데이터가 실시간 캡처와 정확히 일치합니다. Codex 업로드는 임시 파일이 정리되기 전에 스냅샷되며 재가져오기는 멱등적입니다.",
    "The startup auto-import of <code>~/.claude/projects</code> is one-time and marker-gated, so a project folder created after first launch — whose sessions never flow through hooks (for example with host-only hooks disabled) — would stay invisible until a manual rescan. A background sync closes that gap with three triggers sharing one mtime cache and a single coalesced sweep: an immediate sweep at startup, a debounced <code>fs.watch</code> (recursive on macOS and Windows; root plus immediate child folders on Linux to avoid the userland recursive-watcher hazard) that fires the moment a new session file or project folder appears, and a periodic safety-net poll tunable via <code>DASHBOARD_SESSION_SYNC_MS</code> (default <code>30000</code> ms; <code>0</code> disables the poll while leaving the watcher running). Each sweep re-parses only files whose mtime advanced and broadcasts <code>session_created</code> / <code>session_updated</code> (plus the main agent) so the UI refreshes live, while an already-imported unchanged session is skipped without re-parsing.":
      "시작 시 수행되는 <code>~/.claude/projects</code> 자동 가져오기는 1회성이며 마커로 제어되므로, 첫 실행 이후 생성된 프로젝트 폴더(예: 호스트 전용 훅이 비활성화되어 세션이 훅을 통해 전달되지 않는 경우)는 수동으로 재스캔하기 전까지 보이지 않게 됩니다. 백그라운드 동기화는 하나의 mtime 캐시와 단일 통합 스윕을 공유하는 세 가지 트리거로 이 격차를 해소합니다: 시작 시 즉시 수행되는 스윕, 디바운스된 <code>fs.watch</code>(macOS와 Windows에서는 재귀적으로, Linux에서는 사용자 영역 재귀 워처의 위험을 피하기 위해 루트와 바로 아래 하위 폴더에 대해서만 적용)로 새 세션 파일이나 프로젝트 폴더가 나타나는 즉시 실행되며, <code>DASHBOARD_SESSION_SYNC_MS</code>(기본값 <code>30000</code>ms, <code>0</code>으로 설정하면 워처는 계속 실행하되 폴링만 비활성화)로 조정 가능한 주기적 안전망 폴링이 있습니다. 각 스윕은 mtime이 갱신된 파일만 다시 파싱하고 <code>session_created</code>/<code>session_updated</code>(및 메인 에이전트)를 브로드캐스트하여 UI가 실시간으로 갱신되며, 이미 가져와서 변경되지 않은 세션은 다시 파싱하지 않고 건너뜁니다.",
    "Incremental JSONL reader shared across the hook handler, compaction scanner, conversation viewer, and import pipeline. Byte-offset tracking skips already-parsed content; cache hits short-circuit disk I/O so even sessions with tens of thousands of turns stay fast. It also extracts the live session title (<code>custom-title</code> / <code>ai-title</code>) so renames surface in real time, plus the first user prompt as a fallback descriptor for placeholder-named sessions and agents.":
      "훅 핸들러, 컴팩션 스캐너, 대화 뷰어, 가져오기 파이프라인 전반에서 공유되는 점진적(incremental) JSONL 리더입니다. 바이트 오프셋 추적으로 이미 파싱된 콘텐츠를 건너뛰며, 캐시 히트 시 디스크 I/O를 생략하므로 수만 개의 턴이 있는 세션도 빠르게 처리됩니다. 또한 실시간 세션 제목(<code>custom-title</code> / <code>ai-title</code>)을 추출하여 이름 변경이 실시간으로 반영되며, 임시 이름으로 표시되는 세션과 에이전트를 위한 대체 설명으로 첫 사용자 프롬프트도 함께 추출합니다.",
    "LRU eviction of cold session buffers plus a tail-cap on per-entry growable arrays (turn durations, API errors, compaction entries). A session that runs for days cannot grow a single cache entry without bound, and each entry stores its parsed result only once — no shadow copy.":
      "비활성 세션 버퍼에 대한 LRU 축출과 항목별 가변 배열(턴 지속 시간, API 오류, 컴팩션 항목)에 대한 꼬리 제한(tail-cap)을 적용합니다. 며칠 동안 실행되는 세션이라도 단일 캐시 항목이 무한정 커질 수 없으며, 각 항목은 파싱된 결과를 단 한 번만 저장합니다 — 그림자 복사본은 없습니다.",
    "The periodic compaction sweep reads each active session's transcript path directly from <code>sessions.transcript_path</code> (a partial index covers exactly those rows), so the work is O(active sessions) instead of a <code>json_extract</code> scan over the whole events table.":
      "주기적인 컴팩션 스윕은 각 활성 세션의 트랜스크립트 경로를 <code>sessions.transcript_path</code>에서 직접 읽어오며(부분 인덱스가 정확히 해당 행들을 커버합니다), 이에 따라 작업량은 전체 events 테이블에 대한 <code>json_extract</code> 스캔이 아니라 O(활성 세션 수)가 됩니다.",
    "Collapsible parent–child agent tree rendered on both Dashboard and Session Detail. Agents with subagents display expand/collapse chevrons; leaf agents show a dot indicator. The tree auto-expands when any child transitions to active and correctly tracks backgrounded subagents without premature completion. Depth is unlimited — deeply nested chains render as indented rows with connecting lines. Each node shows model, current tool, status badge, and cumulative token cost for tracing spend down the spawn chain.":
      "Dashboard와 Session Detail 양쪽에서 렌더링되는 접을 수 있는 부모-자식 에이전트 트리입니다. 서브에이전트가 있는 에이전트는 펼치기/접기 화살표를 표시하며, 리프 에이전트는 점 표시기를 보여줍니다. 트리는 하위 항목이 활성 상태로 전환되면 자동으로 펼쳐지며, 백그라운드로 실행 중인 서브에이전트를 조기 완료 없이 정확하게 추적합니다. 깊이 제한은 없으며, 깊이 중첩된 체인은 연결선이 있는 들여쓰기된 행으로 렌더링됩니다. 각 노드는 모델, 현재 도구, 상태 배지, 그리고 생성 체인을 따라 지출을 추적할 수 있는 누적 토큰 비용을 표시합니다.",
    "Per-model cost estimation with configurable pricing rules — set input, output, and cache-read rates per model variant through the Settings UI. View total and per-session breakdowns on Sessions, Session Detail, and Analytics. Compaction- aware token accounting preserves baselines across context compressions so no usage is silently dropped. Cost chips appear on Kanban cards, session rows, and the sidebar summary. Subagent cards show each subagent's own cost, computed from that subagent's transcript token usage priced at current rates, so a subagent card no longer misleadingly reads as if it cost the whole session; main-agent cards still show the session total. Pricing changes retroactively recalculate all stored sessions, and imports apply the same rate table.":
      "설정 가능한 가격 책정 규칙에 따라 모델별 비용을 추정합니다 — Settings UI를 통해 모델 변형별로 입력, 출력, 캐시 읽기 요율을 설정할 수 있습니다. Sessions, Session Detail, Analytics에서 전체 및 세션별 분석을 확인할 수 있습니다. 컴팩션을 고려한 토큰 계산은 컨텍스트 압축 전반에 걸쳐 기준값을 보존하므로 사용량이 은연중에 누락되지 않습니다. 비용 칩은 칸반 카드, 세션 행, 사이드바 요약에 표시됩니다. 서브에이전트 카드는 현재 요율로 계산된 해당 서브에이전트 트랜스크립트의 토큰 사용량을 기반으로 각 서브에이전트 고유의 비용을 표시하므로, 서브에이전트 카드가 마치 전체 세션 비용인 것처럼 오해를 주지 않습니다. 메인 에이전트 카드는 여전히 세션 총합을 표시합니다. 가격 변경은 저장된 모든 세션에 소급 적용되어 재계산되며, 가져오기 역시 동일한 요율표를 적용합니다.",
    "Model pricing editor with per-token rate configuration for every Claude variant. Hook installation status with one-click reinstall and per-hook health checks. Full JSON data export covering sessions, agents, events, tokens, and pricing rules. Session cleanup controls to abandon stale sessions or purge old data by age. Browser notification preferences with per-event toggles. A system information panel shows database row counts, file sizes, server uptime, and WebSocket connection status at a glance.":
      "모든 Claude 변형에 대해 토큰당 요율을 설정할 수 있는 모델 가격 편집기입니다. 원클릭 재설치와 훅별 상태 점검이 가능한 훅 설치 상태를 제공합니다. 세션, 에이전트, 이벤트, 토큰, 가격 규칙을 아우르는 전체 JSON 데이터 내보내기가 가능합니다. 오래된 세션을 중단시키거나 기간별로 오래된 데이터를 삭제하는 세션 정리 기능도 있습니다. 이벤트별로 켜고 끌 수 있는 브라우저 알림 설정을 제공합니다. 시스템 정보 패널은 데이터베이스 행 수, 파일 크기, 서버 가동 시간, WebSocket 연결 상태를 한눈에 보여줍니다.",
    "Local MCP sidecar with three transport modes — stdio for Claude Code native integration, HTTP+SSE for remote clients, and an interactive REPL for ad-hoc terminal queries. Exposes 25 typed tools across 6 domains: sessions, agents, events, analytics, settings, and system health. Every mutation is gated behind a tiered policy so nothing dangerous fires without opt-in. Retry-aware API access handles transient failures. Runs as a standalone Node process with no Docker or cloud dependency.":
      "세 가지 전송 모드를 지원하는 로컬 MCP 사이드카입니다 — Claude Code 네이티브 통합을 위한 stdio, 원격 클라이언트를 위한 HTTP+SSE, 임시 터미널 쿼리를 위한 대화형 REPL. sessions, agents, events, analytics, settings, system health 등 6개 영역에 걸쳐 25개의 타입이 지정된 도구를 제공합니다. 모든 변경 작업은 단계별 정책에 의해 게이트되므로 명시적으로 허용하지 않으면 위험한 작업이 실행되지 않습니다. 재시도를 인식하는 API 접근으로 일시적인 장애를 처리합니다. Docker나 클라우드에 의존하지 않는 독립 실행형 Node 프로세스로 동작합니다.",
    "Instruction, skills, rules, and custom-agent layers for both Claude Code and Codex. Path-scoped rules target backend, frontend, MCP, and docs directories with context-appropriate guidelines. Reusable skills cover onboarding, feature shipping, live-issue debugging, release-readiness, and MCP operations. Specialized subagents for backend, frontend, and MCP code review run in parallel with focused tooling. Everything lives in <code>.claude/</code> and is version-controlled alongside the codebase.":
      "Claude Code와 Codex 양쪽을 위한 지침, 스킬, 규칙, 커스텀 에이전트 레이어입니다. 경로 범위 규칙은 backend, frontend, MCP, docs 디렉터리를 대상으로 상황에 맞는 가이드라인을 적용합니다. 재사용 가능한 스킬은 온보딩, 기능 배포, 실시간 이슈 디버깅, 릴리스 준비 상태 점검, MCP 운영을 다룹니다. backend, frontend, MCP 코드 리뷰를 위한 전문 서브에이전트가 특화된 도구와 함께 병렬로 실행됩니다. 모든 것이 <code>.claude/</code>에 위치하며 코드베이스와 함께 버전 관리됩니다.",
    "D3.js-powered visualizations: an agent orchestration DAG showing spawn patterns across sessions, a tool-execution Sankey diagram mapping tool-to- tool transitions, and a directed pipeline graph with frequency labels. Every chart title carries an info icon that opens a popover explaining what it shows and how to read it. Hovering nodes, edges, and bars surfaces tooltips with share-of-source percentages, success-rate buckets, and timing patterns. All labels are translated to English, Vietnamese, and Chinese.":
      "D3.js 기반 시각화입니다: 세션 전반의 생성 패턴을 보여주는 에이전트 오케스트레이션 DAG, 도구 간 전환을 매핑하는 도구 실행 생키(Sankey) 다이어그램, 빈도 레이블이 표시된 방향성 파이프라인 그래프를 제공합니다. 모든 차트 제목에는 무엇을 보여주는지와 읽는 방법을 설명하는 팝오버를 여는 정보 아이콘이 있습니다. 노드, 엣지, 막대에 마우스를 올리면 소스별 비율, 성공률 구간, 타이밍 패턴이 담긴 툴팁이 표시됩니다. 모든 레이블은 영어, 베트남어, 중국어로 번역되어 있습니다.",
    "Subagent effectiveness scorecards with success-rate rings and day-of-week sparklines. Auto-detected workflow patterns expand on click into a detail panel with the full step chain, stats grid, and a narrative with loop detection. Model delegation flow, error propagation bars with API error cards, concurrency swim-lanes, and complexity bubble charts round out the view. Six headline stat cards each include an info popover explaining the metric and its current value. Status filter applies globally.":
      "성공률 링과 요일별 스파크라인이 포함된 서브에이전트 효율성 스코어카드입니다. 자동 감지된 워크플로 패턴을 클릭하면 전체 단계 체인, 통계 그리드, 루프 감지 결과를 포함한 서술을 담은 상세 패널로 펼쳐집니다. 모델 위임 흐름, API 오류 카드가 포함된 오류 전파 막대그래프, 동시성 스윔레인, 복잡도 버블 차트가 화면을 완성합니다. 6개의 주요 통계 카드에는 각각 해당 지표와 현재 값을 설명하는 정보 팝오버가 포함되어 있습니다. 상태 필터는 전역적으로 적용됩니다.",
    "Searchable session selector with pagination to explore any session's agent tree, tool-call timeline, and event sequence. The detail page opens with a live-updating overview — tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and subagent breakdown give quick reads. The conversation viewer renders markdown with syntax highlighting. Cross-filter from DAG nodes, run compaction analysis, or export as JSON — all with real-time WebSocket auto-refresh.":
      "페이지네이션이 적용된 검색 가능한 세션 선택기로 모든 세션의 에이전트 트리, 도구 호출 타임라인, 이벤트 시퀀스를 살펴볼 수 있습니다. 상세 페이지는 실시간으로 업데이트되는 개요와 함께 열립니다 — 이벤트, 도구 호출, 서브에이전트, 컴팩션, 오류, 지속 시간에 대한 타일 카운터를 제공합니다. 상위 도구 사용 막대그래프와 서브에이전트 분석으로 빠르게 파악할 수 있습니다. 대화 뷰어는 구문 강조가 적용된 마크다운을 렌더링합니다. DAG 노드를 통한 교차 필터링, 컴팩션 분석 실행, JSON으로 내보내기 등을 모두 실시간 WebSocket 자동 새로고침과 함께 사용할 수 있습니다.",
    "Persistent browser notifications via Web Push (VAPID) for real-time alerts even when the tab is not focused or the browser is backgrounded. Includes macOS audio support so notifications are audible alongside system sounds. Per-event toggles let you choose which events fire — session starts, completions, errors, compactions, or agent spawns. Server-side subscription management ensures one push per event per browser. Works on Chrome, Edge, Firefox, and Safari 17+ with graceful degradation elsewhere.":
      "Web Push(VAPID)를 통한 지속적인 브라우저 알림으로, 탭이 포커스되어 있지 않거나 브라우저가 백그라운드에 있을 때도 실시간 알림을 받을 수 있습니다. macOS 오디오 지원이 포함되어 시스템 사운드와 함께 알림 소리를 들을 수 있습니다. 이벤트별 토글로 세션 시작, 완료, 오류, 컴팩션, 에이전트 생성 중 어떤 이벤트를 알릴지 선택할 수 있습니다. 서버 측 구독 관리로 브라우저당 이벤트당 하나의 푸시만 전송되도록 보장합니다. Chrome, Edge, Firefox, Safari 17 이상에서 동작하며, 그 외 환경에서는 점진적 성능 저하(graceful degradation) 방식으로 동작합니다.",
    "Ready-to-use Dockerfile and docker-compose.yml for one-command deployment. Supports both Docker and Podman with persistent volume mounts for the SQLite database and hook data. Configurable port mapping via environment variables and a health-check endpoint the container runtime can poll. Multi-stage build keeps the image lean — only production deps and the compiled bundle ship. Run <code>docker compose up -d --build</code> and the dashboard is live with zero additional setup or configuration required.":
      "한 번의 명령으로 배포할 수 있도록 바로 사용 가능한 Dockerfile과 docker-compose.yml을 제공합니다. Docker와 Podman을 모두 지원하며 SQLite 데이터베이스와 훅 데이터를 위한 영구 볼륨 마운트를 지원합니다. 환경 변수를 통해 포트 매핑을 설정할 수 있으며 컨테이너 런타임이 폴링할 수 있는 헬스 체크 엔드포인트를 제공합니다. 멀티스테이지 빌드로 이미지를 가볍게 유지합니다 — 프로덕션 의존성과 컴파일된 번들만 포함됩니다. <code>docker compose up -d --build</code>를 실행하면 추가 설정 없이 바로 대시보드가 실행됩니다.",
    "Official Claude Code plugin marketplace shipping 10 plugins with 53 skills, 14 agents, 30 slash commands, 3 CLI tools, 3 hook configs, and 1 MCP server. Deep analytics with compaction-aware baselines, productivity automation, developer diagnostics, AI-powered workflow intelligence, and dashboard MCP integration. Five newer plugins go further: <code>ccam-cost-guard</code> (budget guardrails, spend forecasts, and cost-threshold alerts), <code>ccam-sessions</code> (session forensics — search, timeline, and transcript replay), <code>ccam-workflows</code> (multi-agent orchestration and fleet intelligence), <code>ccam-quality</code> (reliability and SLO checks), and <code>ccam-config</code> (Claude Code config and memory governance). Install with <code>claude plugin install</code> — no restart needed. Each listing shows author, license, homepage, and per-skill contribution breakdown. The Config Explorer's Plugins tab surfaces installed plugins with live status.":
      "53개의 스킬, 14개의 에이전트, 30개의 슬래시 명령, 3개의 CLI 도구, 3개의 훅 설정, 1개의 MCP 서버를 포함한 10개의 플러그인을 제공하는 공식 Claude Code 플러그인 마켓플레이스입니다. 컴팩션을 고려한 기준값을 반영한 심층 분석, 생산성 자동화, 개발자 진단, AI 기반 워크플로 인텔리전스, 대시보드 MCP 통합을 제공합니다. 다섯 개의 최신 플러그인은 한 걸음 더 나아갑니다: <code>ccam-cost-guard</code>(예산 가드레일, 지출 예측, 비용 임계값 알림), <code>ccam-sessions</code>(세션 포렌식 — 검색, 타임라인, 트랜스크립트 재생), <code>ccam-workflows</code>(다중 에이전트 오케스트레이션과 플릿 인텔리전스), <code>ccam-quality</code>(신뢰성 및 SLO 점검), <code>ccam-config</code>(Claude Code 설정 및 메모리 거버넌스). <code>claude plugin install</code>로 설치할 수 있으며 재시작이 필요 없습니다. 각 항목에는 작성자, 라이선스, 홈페이지, 스킬별 기여도 분석이 표시됩니다. Config Explorer의 Plugins 탭은 설치된 플러그인을 실시간 상태와 함께 보여줍니다.",
    "Spawn <code>claude</code> subprocesses straight from the dashboard with a chat-style streaming UI — multi-turn <b>Conversation</b> or single-shot <b>Headless</b> mode. One-click <b>Resume</b> on any past conversation spawns <code>claude --resume</code> seeded with the prior transcript. Re- attach reconciles in-memory logs with the on-disk JSONL so navigating away never loses history. Slash-command autocomplete, file references, live token/context-window meter, and a thinking-effort dial bring TUI parity to the browser. Same-origin guard blocks drive-by spawns.":
      "대시보드에서 직접 채팅 스타일 스트리밍 UI로 <code>claude</code> 서브프로세스를 실행할 수 있습니다 — 여러 턴에 걸친 <b>Conversation</b> 모드 또는 단발성 <b>Headless</b> 모드입니다. 과거 대화에서 원클릭 <b>Resume</b>을 누르면 이전 트랜스크립트를 기반으로 <code>claude --resume</code>이 실행됩니다. 다시 연결(re-attach) 시 메모리 내 로그와 디스크상의 JSONL을 조정하므로 페이지를 벗어나도 기록이 손실되지 않습니다. 슬래시 명령 자동 완성, 파일 참조, 실시간 토큰/컨텍스트 윈도우 미터, 사고 노력(thinking-effort) 다이얼을 통해 브라우저에서도 TUI와 동등한 경험을 제공합니다. 동일 출처(same-origin) 가드가 무단 실행을 차단합니다.",
    "A 12-tab inspector at <code>/cc-config</code> for everything Claude Code knows about: skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts. The Settings tab leads with a Current configuration summary of the options <code>/config</code> controls — model, verbose, theme, output style, auto-compact, notifications — resolved across scopes. The Memory tab surfaces both the user and project <code>CLAUDE.md</code> files and the per-project file-based memory store — every auto-memory <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+), grouped by project, searchable, and editable. Create, edit, and delete the low-risk text-file surfaces with mandatory timestamped backups before every write. Plugins, MCP, hooks, and live settings stay read-only with explainer banners and copy-able CLI commands. Per-plugin contribution breakdowns show author and license.":
      "<code>/cc-config</code>에서 Claude Code가 알고 있는 모든 것을 보여주는 12개 탭짜리 검사 도구입니다: 스킬, 서브에이전트, 슬래시 명령, 출력 스타일, 플러그인, 마켓플레이스, MCP 서버, 훅, 설정, 메모리, 키바인딩, 상태줄 스크립트가 포함됩니다. Settings 탭은 <code>/config</code>가 제어하는 옵션 — 모델, verbose, 테마, 출력 스타일, 자동 컴팩트, 알림 — 을 범위별로 해석하여 보여주는 현재 설정 요약으로 시작합니다. Memory 탭은 사용자 및 프로젝트의 <code>CLAUDE.md</code> 파일과 프로젝트별 파일 기반 메모리 저장소 — <code>~/.claude/projects/&lt;slug&gt;/memory/</code> 아래의 모든 자동 메모리 <code>*.md</code>(<code>MEMORY.md</code> 인덱스와 기억된 사실마다 하나씩, 보통 100개 이상의 파일) — 를 프로젝트별로 그룹화하여 검색 및 편집할 수 있게 보여줍니다. 위험도가 낮은 텍스트 파일 영역은 생성, 편집, 삭제가 가능하며 모든 쓰기 작업 전에 타임스탬프가 찍힌 백업이 필수로 생성됩니다. 플러그인, MCP, 훅, 실시간 설정은 설명 배너와 복사 가능한 CLI 명령과 함께 읽기 전용으로 유지됩니다. 플러그인별 기여도 분석은 작성자와 라이선스를 보여줍니다.",
    "Mobile-first layouts with stacking grids, horizontally scrollable tables, and a collapsible sidebar that auto-hides below 1400 px. All pages adapt from phone to ultrawide with consistent navigation and readable typography. Kanban columns stack vertically on narrow screens, analytics charts reflow to single-column, and the activity feed stays fully swipeable. Touch targets meet 44 px minimum. Dark theme renders consistently across iOS Safari, Chrome, and Firefox with no flash of unstyled content.":
      "스태킹 그리드, 가로 스크롤이 가능한 테이블, 1400px 미만에서 자동으로 숨겨지는 접이식 사이드바를 갖춘 모바일 우선 레이아웃입니다. 모든 페이지는 스마트폰부터 울트라와이드 화면까지 일관된 내비게이션과 읽기 쉬운 타이포그래피로 적응합니다. 칸반 열은 좁은 화면에서 세로로 쌓이고, 분석 차트는 단일 열로 재배치되며, 활동 피드는 완전히 스와이프 가능한 상태를 유지합니다. 터치 대상은 최소 44px를 충족합니다. 다크 테마는 스타일이 적용되지 않은 콘텐츠가 잠깐 보이는 현상 없이 iOS Safari, Chrome, Firefox에서 일관되게 렌더링됩니다.",
    "Visualize parallel agent execution with a Gantt-style timeline showing overlapping subagent lifetimes, tool-call concurrency windows, and wait gaps. Color-coded bars distinguish working, waiting, and errored states so bottlenecks are immediately visible. Hover any bar for exact timestamps and duration. Zoom and pan across long-running sessions with hundreds of agents. The timeline shares the Workflows status filter so you can isolate active, completed, or errored sessions without leaving the view.":
      "겹치는 서브에이전트 수명, 도구 호출 동시성 구간, 대기 간격을 보여주는 간트 차트 스타일 타임라인으로 병렬 에이전트 실행을 시각화합니다. 색상으로 구분된 막대는 작업 중, 대기 중, 오류 상태를 구분하여 병목 지점을 즉시 확인할 수 있게 합니다. 막대에 마우스를 올리면 정확한 타임스탬프와 지속 시간을 확인할 수 있습니다. 수백 개의 에이전트가 포함된 장시간 실행 세션도 확대/축소 및 이동이 가능합니다. 타임라인은 Workflows 상태 필터를 공유하므로 화면을 벗어나지 않고도 활성, 완료, 오류 세션을 분리해서 볼 수 있습니다.",
    "Professional VS Code extension with a real-time Activity Bar sidebar showing active sessions, agent counts, and recent events without leaving your editor. A status bar pulse monitor surfaces connection health and the latest event type at a glance. Deep navigation links open any session or analytics view directly in your browser. An embedded webview renders the full dashboard inside a VS Code tab with WebSocket push, theme sync, and responsive layout. Install from the marketplace or build from source.":
      "에디터를 벗어나지 않고도 활성 세션, 에이전트 수, 최근 이벤트를 보여주는 실시간 액티비티 바 사이드바를 갖춘 전문가용 VS Code 확장입니다. 상태 표시줄 펄스 모니터는 연결 상태와 최신 이벤트 유형을 한눈에 보여줍니다. 딥 내비게이션 링크로 브라우저에서 바로 세션이나 분석 화면을 열 수 있습니다. 내장된 웹뷰는 WebSocket 푸시, 테마 동기화, 반응형 레이아웃과 함께 VS Code 탭 안에서 전체 대시보드를 렌더링합니다. 마켓플레이스에서 설치하거나 소스에서 직접 빌드할 수 있습니다.",
    "Trace how errors cascade across agents and tool calls with a directed graph showing failure origins, retry paths, and recovery points. Each node displays the agent or tool that errored, the error message, and whether a retry succeeded or propagated upstream. Pinpoint root causes in deeply nested subagent chains. Horizontal bar charts rank the most error-prone tools and models. API error cards group failures by HTTP status and endpoint. Filter by session, time range, or error severity to narrow the view.":
      "오류 발생 지점, 재시도 경로, 복구 지점을 보여주는 방향성 그래프로 오류가 에이전트와 도구 호출 전반에 어떻게 전파되는지 추적합니다. 각 노드는 오류가 발생한 에이전트 또는 도구, 오류 메시지, 재시도가 성공했는지 아니면 상위로 전파되었는지를 표시합니다. 깊이 중첩된 서브에이전트 체인에서도 근본 원인을 정확히 짚어낼 수 있습니다. 가로 막대그래프는 오류가 가장 자주 발생하는 도구와 모델의 순위를 보여줍니다. API 오류 카드는 HTTP 상태와 엔드포인트별로 실패를 그룹화합니다. 세션, 시간 범위, 오류 심각도로 필터링하여 범위를 좁힐 수 있습니다.",
    'Three independent PWAs — dashboard, landing page, and wiki — each with its own Web App Manifest and Service Worker. Install to your home screen or dock for a standalone, chrome-less experience. SVG icons with <code>sizes="any"</code> and iOS standalone meta tags included.':
      '각각 고유한 Web App Manifest와 Service Worker를 갖춘 세 개의 독립적인 PWA — 대시보드, 랜딩 페이지, 위키입니다. 홈 화면이나 독(dock)에 설치하면 브라우저 UI 없이 독립 실행형으로 사용할 수 있습니다. <code>sizes="any"</code> SVG 아이콘과 iOS 독립 실행형(standalone) 메타 태그가 포함되어 있습니다.',
    "The dashboard SW serves Vite's hashed <code>/assets/*</code> bundles cache-first (URLs are immutable per build) and treats everything else as network-first with cache fallback. Explicit <code>Cache-Control</code> headers on the production Express static middleware reinforce the policy, so a rebuild replaces the in-browser code without a hard refresh.":
      "대시보드 서비스 워커는 Vite가 해시를 붙인 <code>/assets/*</code> 번들을 캐시 우선(cache-first) 방식으로 제공하고(URL은 빌드마다 불변이므로), 그 외의 모든 것은 네트워크 우선(network-first)에 캐시 폴백을 적용합니다. 프로덕션 Express 정적 미들웨어의 명시적인 <code>Cache-Control</code> 헤더가 이 정책을 뒷받침하므로, 리빌드 시 강제 새로고침 없이도 브라우저 내 코드가 교체됩니다.",
    "A <code>controllerchange</code> listener in <code>client/src/main.tsx</code> reloads the page exactly once when a new SW takes over an already-controlled page. First installs do not reload, so the very first visit is never interrupted.":
      "<code>client/src/main.tsx</code>의 <code>controllerchange</code> 리스너는 이미 제어 중인 페이지를 새 서비스 워커가 인수할 때 정확히 한 번만 페이지를 새로고침합니다. 최초 설치 시에는 새로고침하지 않으므로 첫 방문이 중단되는 일은 없습니다.",
    '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — live overview of active sessions and agents. Stats tiles, collapsible subagent hierarchy cards, and a recent activity feed. Auto-refreshes every 5 s via WebSocket</span>':
      '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — 활성 세션 및 에이전트의 실시간 개요. 통계 타일, 접을 수 있는 서브에이전트 계층 카드, 최근 활동 피드. WebSocket을 통해 5초마다 자동 새로고침됩니다</span>',
    '<span class="caption-icon">🩺</span> <span><strong>Dashboard · Health</strong> — composite health score ring, storage engine donut, cache/error/success gauges, tool invocation bars, subagent effectiveness, and model token distribution</span>':
      '<span class="caption-icon">🩺</span> <span><strong>Dashboard · Health</strong> — 종합 상태 점수 링, 스토리지 엔진 도넛 차트, 캐시/오류/성공 게이지, 도구 호출 막대그래프, 서브에이전트 효율성, 모델별 토큰 분포</span>',
    '<span class="caption-icon">📋</span> <span><strong>Kanban Board (agents)</strong> — agents swim-laned by status: Working, Waiting, Completed, Error. Cards show model, cost, and current tool call. Yellow column flags agents waiting on user input</span>':
      '<span class="caption-icon">📋</span> <span><strong>Kanban Board (agents)</strong> — 상태별로 스윔레인 처리된 에이전트: 작업 중, 대기 중, 완료, 오류. 카드에는 모델, 비용, 현재 도구 호출이 표시됩니다. 노란색 열은 사용자 입력을 기다리는 에이전트를 표시합니다</span>',
    '<span class="caption-icon">🗂️</span> <span><strong>Kanban Board (sessions)</strong> — sessions swim-laned across 5 columns: Active, Waiting, Completed, Error, Abandoned. Each card shows agent count, duration, model, and cumulative cost</span>':
      '<span class="caption-icon">🗂️</span> <span><strong>Kanban Board (sessions)</strong> — 5개의 열로 스윔레인 처리된 세션: 활성, 대기 중, 완료, 오류, 중단됨. 각 카드는 에이전트 수, 지속 시간, 모델, 누적 비용을 표시합니다</span>',
    '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — searchable, filterable, server-paginated table. Each row shows status badge, agent count, duration, model, and cost. Click any row to drill into session detail</span>':
      '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — 검색 및 필터링이 가능한 서버 페이지네이션 테이블입니다. 각 행은 상태 배지, 에이전트 수, 지속 시간, 모델, 비용을 표시합니다. 행을 클릭하면 세션 상세 화면으로 이동합니다</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles (events, tool calls, subagents, errors, duration), top-tool usage bars, subagent type breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — 개요 타일(이벤트, 도구 호출, 서브에이전트, 오류, 지속 시간), 상위 도구 사용 막대그래프, 서브에이전트 유형 분석, 접을 수 있는 부모-자식 에이전트 계층 트리</span>',
    '<span class="caption-icon">💬</span> <span><strong>Session Detail · Conversation</strong> — full transcript viewer with markdown rendering, syntax-highlighted code blocks, per-tool sections, and collapsible thinking blocks</span>':
      '<span class="caption-icon">💬</span> <span><strong>Session Detail · Conversation</strong> — 마크다운 렌더링, 구문 강조된 코드 블록, 도구별 섹션, 접을 수 있는 사고(thinking) 블록을 갖춘 전체 트랜스크립트 뷰어</span>',
    '<span class="caption-icon">🔬</span> <span><strong>Session Detail · Timeline</strong> — chronological event timeline with multi-dimension filters, color-coded entries by type, expandable hook payloads, and direct links to the owning session and agent</span>':
      '<span class="caption-icon">🔬</span> <span><strong>Session Detail · Timeline</strong> — 다차원 필터, 유형별 색상 구분 항목, 확장 가능한 훅 페이로드, 해당 세션 및 에이전트로의 직접 링크를 갖춘 시간순 이벤트 타임라인</span>',
    '<span class="caption-icon">📰</span> <span><strong>Activity Feed</strong> — real-time streaming event log with pause/resume buffering, multi-dimension filters, expandable hook payloads, color-coded entries, and per-row session navigation buttons</span>':
      '<span class="caption-icon">📰</span> <span><strong>Activity Feed</strong> — 일시정지/재개 버퍼링, 다차원 필터, 확장 가능한 훅 페이로드, 색상 구분 항목, 행별 세션 이동 버튼을 갖춘 실시간 스트리밍 이벤트 로그</span>',
    '<span class="caption-icon">📊</span> <span><strong>Analytics</strong> — token usage by model, tool frequency bars, 52-week activity heatmap, 30-day sparkline trends, session outcome donuts, and cost summary with WebSocket auto-refresh</span>':
      '<span class="caption-icon">📊</span> <span><strong>Analytics</strong> — 모델별 토큰 사용량, 도구 사용 빈도 막대그래프, 52주 활동 히트맵, 30일 스파크라인 추세, 세션 결과 도넛 차트, WebSocket 자동 새로고침이 적용된 비용 요약</span>',
    '<span class="caption-icon">🔀</span> <span><strong>Workflows</strong> — D3.js agent orchestration DAG, tool-execution Sankey diagram, directed pipeline graph, effectiveness scorecards, concurrency swim-lanes, and complexity bubble charts</span>':
      '<span class="caption-icon">🔀</span> <span><strong>Workflows</strong> — D3.js 기반 에이전트 오케스트레이션 DAG, 도구 실행 생키 다이어그램, 방향성 파이프라인 그래프, 효율성 스코어카드, 동시성 스윔레인, 복잡도 버블 차트</span>',
    '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — 12-tab inspector for skills, subagents, slash commands, plugins, MCP servers, hooks, settings, memory, keybindings, and statusline. Safe edits with backups</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — 스킬, 서브에이전트, 슬래시 명령, 플러그인, MCP 서버, 훅, 설정, 메모리, 키바인딩, 상태줄을 위한 12개 탭짜리 검사 도구입니다. 백업과 함께 안전하게 편집할 수 있습니다</span>',
    '<span class="caption-icon">▶️</span> <span><strong>Run Claude</strong> — spawn or resume Claude subprocesses from the browser. Pick Conversation or Headless mode, set cwd, model, permission level, and thinking effort. Same-origin guard included</span>':
      '<span class="caption-icon">▶️</span> <span><strong>Run Claude</strong> — 브라우저에서 Claude 서브프로세스를 실행하거나 재개합니다. Conversation 또는 Headless 모드를 선택하고 cwd, 모델, 권한 수준, 사고 노력을 설정할 수 있습니다. 동일 출처 가드가 포함되어 있습니다</span>',
    '<span class="caption-icon">💬</span> <span><strong>Run Claude · live stream</strong> — character-by-character streaming output. Tool uses, tool results, and thinking blocks are collapsible. Active runs switcher juggles multiple sessions</span>':
      '<span class="caption-icon">💬</span> <span><strong>Run Claude · live stream</strong> — 한 글자씩 스트리밍되는 출력입니다. 도구 사용, 도구 결과, 사고 블록은 접을 수 있습니다. 실행 중인 세션 전환기로 여러 세션을 오갈 수 있습니다</span>',
    '<span class="caption-icon">⚙️</span> <span><strong>Settings</strong> — model pricing editor with per-token rates, hook installation status, JSON data export, session cleanup controls, browser notification toggles, and system info panel with DB stats</span>':
      '<span class="caption-icon">⚙️</span> <span><strong>Settings</strong> — 토큰당 요율을 설정하는 모델 가격 편집기, 훅 설치 상태, JSON 데이터 내보내기, 세션 정리 기능, 브라우저 알림 토글, DB 통계가 포함된 시스템 정보 패널</span>',
    "This chart tracks how interest in Claude Code Agent Monitor has grown over time. The curve keeps climbing as more developers discover the project, share it, and use it in real workflows. Each new star is a small vote of confidence from the community.":
      "이 차트는 Claude Code Agent Monitor에 대한 관심이 시간이 지나면서 어떻게 성장했는지를 보여줍니다. 더 많은 개발자가 이 프로젝트를 발견하고, 공유하고, 실제 워크플로에서 사용함에 따라 곡선은 계속 상승합니다. 새로운 스타 하나하나가 커뮤니티의 작은 신뢰 표시입니다.",
    '<span class="caption-icon">⭐</span> <span> Enjoying the project? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer" >Give it a star on GitHub</a > and help more builders discover it. </span>':
      '<span class="caption-icon">⭐</span> <span> 이 프로젝트가 마음에 드시나요? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer" >GitHub에서 스타를 눌러주세요</a > 더 많은 개발자가 이 프로젝트를 발견할 수 있도록 도와주세요. </span>',
    "Hook Type": "훅 유형",
    Trigger: "트리거",
    "Dashboard Action": "대시보드 동작",
    "Claude Code session begins": "Claude Code 세션 시작",
    "Creates session and main agent. Stamps <code>awaiting_input_since</code> (with <code>awaiting_reason</code> = <code>session_start</code>) so the row lands in <strong>Waiting</strong> from the start (the CLI is at a prompt) — except a <code>compact</code>-source SessionStart (mid-turn auto-compaction), which leaves the flag untouched so a working session stays <strong>Active</strong>. Reactivates resumed sessions. Abandons orphaned sessions with no activity for <code>DASHBOARD_STALE_MINUTES</code> (default 180).":
      "세션과 메인 에이전트를 생성합니다. CLI가 프롬프트 상태이므로 처음부터 <strong>Waiting</strong>에 위치하도록 <code>awaiting_input_since</code>를(<code>awaiting_reason</code> = <code>session_start</code>와 함께) 기록합니다. 단, <code>compact</code> 소스 SessionStart(턴 도중 자동 압축)는 플래그를 그대로 두어 작업 중인 세션이 <strong>Active</strong> 상태를 유지합니다. 재개된 세션을 다시 활성화합니다. <code>DASHBOARD_STALE_MINUTES</code>(기본값 180) 동안 활동이 없는 고아 세션은 중단 처리합니다.",
    "User hits enter on a prompt": "사용자가 프롬프트에서 엔터를 입력",
    'Clears the waiting flag and promotes the main agent to <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >. The only reliable signal that text-only assistant turns have started — they emit no <code>PreToolUse</code> before <code>Stop</code>.':
      '대기 플래그를 지우고 메인 에이전트를 <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >으로 전환합니다. 텍스트로만 이루어진 어시스턴트 턴이 시작되었음을 알 수 있는 유일하게 신뢰할 수 있는 신호입니다 — 이런 턴은 <code>Stop</code> 이전에 <code>PreToolUse</code>를 전혀 발생시키지 않습니다.',
    "Agent begins using a tool": "에이전트가 도구 사용을 시작",
    'Clears the waiting flag, sets agent → <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >, <code>current_tool</code> set. If tool is <code>Agent</code>, subagent record created.':
      '대기 플래그를 지우고 에이전트를 <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >으로 설정하며, <code>current_tool</code>을 지정합니다. 도구가 <code>Agent</code>인 경우 서브에이전트 레코드를 생성합니다.',
    "Tool execution completes": "도구 실행 완료",
    'Clears the waiting flag (covers permission-prompt approvals mid-tool). <code>current_tool</code> cleared. Agent stays <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >.':
      '대기 플래그를 지웁니다(도구 실행 중 권한 프롬프트 승인도 포함). <code>current_tool</code>이 지워집니다. 에이전트는 계속 <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span > 상태를 유지합니다.',
    "Claude finishes a turn": "Claude가 턴을 종료",
    'Non-error: main agent → <code>waiting</code> — UI shows <span class="status-chip chip-waiting" ><span class="chip-dot"></span>Waiting</span > until the next user input. <code>stop_reason=error</code>: marks the agent and session <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >. Background subagents keep running.':
      '오류가 아닌 경우: 메인 에이전트 → <code>waiting</code> — 다음 사용자 입력이 있을 때까지 UI에 <span class="status-chip chip-waiting" ><span class="chip-dot"></span>Waiting</span >이 표시됩니다. <code>stop_reason=error</code>인 경우: 에이전트와 세션을 <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >로 표시합니다. 백그라운드 서브에이전트는 계속 실행됩니다.',
    "Background agent finished": "백그라운드 에이전트 완료",
    "Matched subagent → <span class=\"status-chip chip-completed\" ><span class=\"chip-dot\"></span>Completed</span >. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session's <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent's own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks. The same scan also rebuilds the nested-subagent hierarchy — it repoints each subagent's <code>parent_agent_id</code> to the true spawner recovered from the transcript's Task tool result (<code>toolUseResult.agentId</code>), so subagents that spawn their own subagents nest correctly instead of flattening under main.":
      '일치하는 서브에이전트 → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. 대기 플래그는 의도적으로 지우지 <strong>않습니다</strong> — 백그라운드 서브에이전트가 완료되었다는 사실은 사용자에 대해 아무것도 알려주지 않기 때문입니다. 또한 세션의 <code>subagents/agent-*.jsonl</code> 파일을 순회하며 <code>tool_use_id</code>로 <code>tool_use</code>와 <code>tool_result</code> 블록을 짝짓고, 각 서브에이전트 고유의 <code>agent_id</code> 아래에 도구별 <code>PreToolUse</code> + <code>PostToolUse</code> 이벤트를 발생시키는 fire-and-forget JSONL 스캔(<code>scanAndImportSubagents</code>)도 시작됩니다 — 이를 통해 서브에이전트가 내부적으로 수행하지만 어떤 훅도 발생시키지 않는 도구 호출을 드러냅니다. 같은 스캔은 중첩된 서브에이전트 계층도 재구성합니다 — 트랜스크립트의 Task 도구 결과(<code>toolUseResult.agentId</code>)에서 복구한 실제 생성자로 각 서브에이전트의 <code>parent_agent_id</code>를 다시 지정하므로, 자체적으로 서브에이전트를 생성하는 서브에이전트도 메인 아래로 평탄화되지 않고 올바르게 중첩됩니다.',
    "Agent sends notification": "에이전트가 알림 전송",
    'Event logged to activity feed. Permission/input-prompt patterns (e.g. "needs your permission", "waiting for your input") set the agent to <code>waiting</code> and stamp <code>awaiting_input_since</code> (with <code>awaiting_reason</code> = <code>notification</code>). Compaction-related notifications tagged as <code>Compaction</code> events. Triggers a browser notification if enabled.':
      '이벤트가 활동 피드에 기록됩니다. 권한/입력 프롬프트 패턴(예: "needs your permission", "waiting for your input")은 에이전트를 <code>waiting</code>으로 설정하고 <code>awaiting_input_since</code>를(<code>awaiting_reason</code> = <code>notification</code>과 함께) 기록합니다. 컴팩션 관련 알림은 <code>Compaction</code> 이벤트로 태그됩니다. 활성화되어 있으면 브라우저 알림을 트리거합니다.',
    "<code>/compact</code> detected in JSONL": "JSONL에서 <code>/compact</code> 감지됨",
    'Creates a compaction subagent → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Detected via <code>isCompactSummary</code> entries in the transcript. Token baselines preserve pre-compaction totals. Periodic scanner (cadence ~¼ of <code>DASHBOARD_STALE_MINUTES</code>) catches compactions when no hooks fire.':
      '컴팩션 서브에이전트를 생성합니다 → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. 트랜스크립트의 <code>isCompactSummary</code> 항목을 통해 감지됩니다. 토큰 기준값은 컴팩션 이전 총합을 보존합니다. 주기적 스캐너(주기는 대략 <code>DASHBOARD_STALE_MINUTES</code>의 1/4)가 훅이 발생하지 않는 컴팩션도 포착합니다.',
    "API error detected in transcript": "트랜스크립트에서 API 오류 감지됨",
    "Extracted from JSONL during history import, real-time transcript scanning, or the error detection watchdog. Captures quota limits, rate limits, auth failures, and other API errors. <strong>Immediately marks sessions and agents as error</strong> — previously recorded as events without changing status.":
      "기록 가져오기, 실시간 트랜스크립트 스캔, 또는 오류 감지 워치독 중에 JSONL에서 추출됩니다. 할당량 한도, 속도 제한, 인증 실패 등 다양한 API 오류를 포착합니다. <strong>세션과 에이전트를 즉시 오류 상태로 표시합니다</strong> — 이전에는 상태를 변경하지 않고 이벤트로만 기록되었습니다.",
    "Turn cancelled by the user (<code>Esc</code>)": "사용자가 턴을 취소함(<code>Esc</code>)",
    "Synthesized by the watchdog because pressing <code>Esc</code> fires no hook. Recovered either from the transcript <code>[Request interrupted by user]</code> marker (flagged as <code>pendingInterrupt</code>) or, when <code>Esc</code> preceded any output and left no marker, from the idle-working timeout (<code>DASHBOARD_WORKING_IDLE_SECONDS</code>, default 120). Moves the session to <strong>Waiting</strong> — the same state a normal <code>Stop</code> produces.":
      "<code>Esc</code>를 누르면 훅이 전혀 발생하지 않으므로 워치독이 이 이벤트를 합성합니다. 트랜스크립트의 <code>[Request interrupted by user]</code> 마커(<code>pendingInterrupt</code>로 표시됨)로부터 복구되거나, 출력이 나오기 전에 <code>Esc</code>가 눌려 마커가 남지 않은 경우에는 유휴 작업 타임아웃(<code>DASHBOARD_WORKING_IDLE_SECONDS</code>, 기본값 120)을 통해 복구됩니다. 세션을 <strong>Waiting</strong> 상태로 전환합니다 — 일반적인 <code>Stop</code>이 발생시키는 것과 동일한 상태입니다.",
    "Per-turn timing recorded": "턴별 타이밍 기록됨",
    "Extracted from JSONL turn boundaries. Records the duration of each assistant turn for latency analysis.":
      "JSONL의 턴 경계에서 추출됩니다. 지연 시간 분석을 위해 각 어시스턴트 턴의 지속 시간을 기록합니다.",
    "Claude Code CLI process exits": "Claude Code CLI 프로세스 종료",
    'Drops the waiting flag. If the session is already in <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >, the error state is preserved; otherwise marks all agents and the session as <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Evicts the session\'s transcript from the shared cache.':
      '대기 플래그를 제거합니다. 세션이 이미 <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span > 상태라면 오류 상태를 그대로 유지하고, 그렇지 않으면 모든 에이전트와 세션을 <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >로 표시합니다. 공유 캐시에서 세션의 트랜스크립트를 축출합니다.',
    "Clone the repository to your machine": "저장소를 컴퓨터에 클론합니다",
    "Run <code>npm run setup</code> to install all dependencies":
      "<code>npm run setup</code>을 실행하여 모든 의존성을 설치합니다",
    "Run <code>npm run dev</code> — server + client launch automatically":
      "<code>npm run dev</code>를 실행합니다 — 서버와 클라이언트가 자동으로 실행됩니다",
    "Start a new Claude Code session — events appear in real-time":
      "새 Claude Code 세션을 시작합니다 — 이벤트가 실시간으로 표시됩니다",
    "A multi-stage <code>Dockerfile</code> and <code>docker-compose.yml</code> are included. You can run the monitor with either Docker or Podman and keep the SQLite database in a named volume.":
      "멀티스테이지 <code>Dockerfile</code>과 <code>docker-compose.yml</code>이 포함되어 있습니다. Docker 또는 Podman으로 모니터를 실행할 수 있으며 SQLite 데이터베이스는 명명된 볼륨에 보관할 수 있습니다.",
    "Hooks auto-install in local mode": "로컬 모드에서는 훅이 자동으로 설치됩니다",
    "When you run the server directly on the host with <code>npm run dev</code> or <code>npm start</code>, it automatically writes Claude Code hook entries to <code>~/.claude/settings.json</code>. If you run the dashboard in Docker or Podman, install hooks from the host with <code>npm run install-hooks</code> after the container is up, then restart Claude Code. The installer refuses to run inside a container (issue #193) so it never writes a container-internal handler path into a bind-mounted host <code>~/.claude</code>; override with <code>CCAM_ALLOW_CONTAINER_HOOKS=1</code> only if Claude Code itself runs in the container.":
      "<code>npm run dev</code> 또는 <code>npm start</code>로 서버를 호스트에서 직접 실행하면 Claude Code 훅 항목이 <code>~/.claude/settings.json</code>에 자동으로 기록됩니다. Docker나 Podman에서 대시보드를 실행하는 경우, 컨테이너가 실행된 후 호스트에서 <code>npm run install-hooks</code>로 훅을 설치한 다음 Claude Code를 재시작하세요. 설치 프로그램은 컨테이너 내부에서 실행되는 것을 거부하므로(이슈 #193) 바인드 마운트된 호스트의 <code>~/.claude</code>에 컨테이너 내부 핸들러 경로가 기록되는 일이 없습니다. Claude Code 자체가 컨테이너 안에서 실행되는 경우에만 <code>CCAM_ALLOW_CONTAINER_HOOKS=1</code>로 이 동작을 재정의하세요.",
    "This repository also ships a local MCP server under <code>mcp/</code> and extension scaffolding for both Claude Code and Codex. These are optional for the dashboard UI, but recommended for complete local-agent workflows. The MCP server supports stdio (for host integration), HTTP+SSE (for remote clients), and an interactive REPL (for operator debugging).":
      "이 저장소에는 <code>mcp/</code> 아래에 로컬 MCP 서버와 Claude Code 및 Codex를 위한 확장 스캐폴딩도 함께 제공됩니다. 이는 대시보드 UI에는 선택 사항이지만, 완전한 로컬 에이전트 워크플로를 위해서는 사용을 권장합니다. MCP 서버는 stdio(호스트 통합용), HTTP+SSE(원격 클라이언트용), 대화형 REPL(운영자 디버깅용)을 지원합니다.",
    "After starting a Claude Code session, you should see:":
      "Claude Code 세션을 시작하면 다음과 같은 모습을 확인할 수 있습니다.",
    Page: "페이지",
    Expected: "예상 결과",
    Sessions: "Sessions",
    'Your session listed with status <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> (a fresh CLI is sitting at the prompt) — flips to <span class="status-chip chip-active"><span class="chip-dot"></span>Active</span> the moment Claude starts a turn':
      '세션이 <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> 상태로 표시됩니다(새 CLI가 프롬프트 상태로 대기 중) — Claude가 턴을 시작하는 순간 <span class="status-chip chip-active"><span class="chip-dot"></span>Active</span>로 전환됩니다',
    "Kanban Board": "Kanban Board",
    "A <em>Main Agent</em> card in the <strong>Waiting</strong> column until you type your first message; flips to <em>Working</em> on <code>UserPromptSubmit</code> / <code>PreToolUse</code> and back to <em>Waiting</em> after each <code>Stop</code>":
      "첫 메시지를 입력하기 전까지는 <strong>Waiting</strong> 열에 <em>Main Agent</em> 카드가 표시되며, <code>UserPromptSubmit</code> / <code>PreToolUse</code> 시 <em>Working</em>으로 전환되었다가 매 <code>Stop</code> 이후 다시 <em>Waiting</em>으로 돌아갑니다",
    "Activity Feed": "Activity Feed",
    'Events streaming in; click any row to expand payload, use "Session →" to drill into session details':
      '이벤트가 실시간으로 스트리밍됩니다. 행을 클릭하면 페이로드가 확장되며, "Session →"를 사용해 세션 상세 정보로 이동할 수 있습니다',
    Dashboard: "Dashboard",
    "Stats updating in real-time": "통계가 실시간으로 업데이트됩니다",
    "Start server before Claude Code": "Claude Code보다 먼저 서버를 시작하세요",
    "Hooks only fire to a running server. If Claude Code was already running when you started the dashboard, restart the Claude Code session.":
      "훅은 실행 중인 서버로만 전송됩니다. 대시보드를 시작하기 전에 Claude Code가 이미 실행 중이었다면 Claude Code 세션을 재시작하세요.",
    Variable: "변수",
    Default: "기본값",
    Description: "설명",
    "Port the Express server listens on": "Express 서버가 수신 대기하는 포트",
    "Port used by the hook handler to reach the server (for custom port setups)":
      "훅 핸들러가 서버에 접근할 때 사용하는 포트(사용자 지정 포트 설정용)",
    "Base URL used by the local MCP server when calling dashboard APIs":
      "로컬 MCP 서버가 대시보드 API를 호출할 때 사용하는 기본 URL",
    "MCP transport mode: <code>stdio</code>, <code>http</code>, <code>repl</code>":
      "MCP 전송 모드: <code>stdio</code>, <code>http</code>, <code>repl</code>",
    "Port for the MCP HTTP+SSE server (only when <code>MCP_TRANSPORT=http</code>)":
      "MCP HTTP+SSE 서버용 포트(<code>MCP_TRANSPORT=http</code>인 경우에만 사용)",
    "Bind address for the MCP HTTP server": "MCP HTTP 서버의 바인드 주소",
    "Path to the SQLite database file": "SQLite 데이터베이스 파일 경로",
    "Set to <code>0</code> to disable the dead-session liveness reap — the watchdog completes active local sessions only when their matching <code>claude</code> or <code>codex</code> process is gone; auto-disabled on Windows and in containers. Sessions forwarded from another machine (household hooks) report a non-POSIX <code>cwd</code> and are auto-skipped, so a mixed local + forwarded deployment no longer needs this off.":
      "<code>0</code>으로 설정하면 죽은 세션에 대한 생존 확인 정리(reap) 기능이 비활성화됩니다 — 워치독은 일치하는 로컬 <code>claude</code> 또는 <code>codex</code> 프로세스가 종료된 경우에만 활성 세션을 완료 처리합니다. Windows와 컨테이너에서는 자동으로 비활성화됩니다. 다른 머신에서 전달된 세션(household hooks)은 POSIX가 아닌 <code>cwd</code>를 보고하므로 자동으로 건너뛰며, 따라서 로컬 + 전달 혼합 배포에서는 더 이상 이 기능을 끌 필요가 없습니다.",
    "Idle gate for watchdog-tick liveness reaps — the transcript must not have been written for at least this long (last hook write is the fallback clock); startup passes skip the gate":
      "워치독 틱의 생존 확인 정리를 위한 유휴 게이트 — 트랜스크립트가 최소 이 시간 동안 기록되지 않아야 합니다(마지막 훅 기록 시각이 대체 기준이 됩니다). 시작 시점의 패스는 이 게이트를 건너뜁니다",
    "Idle-working timeout the watchdog uses to recover an <code>Esc</code> cancel that left no transcript marker":
      "트랜스크립트에 마커를 남기지 않은 <code>Esc</code> 취소를 복구하기 위해 워치독이 사용하는 유휴 작업 타임아웃",
    "Poll interval for the background sync of <code>~/.claude/projects</code>; <code>0</code> disables the poll but keeps the filesystem watcher":
      "<code>~/.claude/projects</code> 백그라운드 동기화의 폴링 간격입니다. <code>0</code>으로 설정하면 폴링은 비활성화되지만 파일 시스템 워처는 계속 유지됩니다",
    "Set to <code>production</code> to serve built client from <code>client/dist/</code>":
      "<code>production</code>으로 설정하면 <code>client/dist/</code>에서 빌드된 클라이언트를 제공합니다",
    "The server writes the following to <code>~/.claude/settings.json</code> on every startup:":
      "서버는 시작할 때마다 다음 내용을 <code>~/.claude/settings.json</code>에 기록합니다.",
    "Existing hooks are preserved. The installer only adds or updates entries containing <code>hook-handler.js</code>.":
      "기존 훅은 그대로 유지됩니다. 설치 프로그램은 <code>hook-handler.js</code>가 포함된 항목만 추가하거나 업데이트합니다.",
    Script: "스크립트",
    Command: "명령어",
    "Install all dependencies (server + client)": "모든 의존성 설치(서버 + 클라이언트)",
    "Start server + client in development mode with hot reload":
      "핫 리로드가 적용된 개발 모드로 서버와 클라이언트를 시작",
    "Start only the Express server with <code>--watch</code>":
      "<code>--watch</code> 옵션으로 Express 서버만 시작",
    "Start only the Vite dev server": "Vite 개발 서버만 시작합니다",
    "TypeScript check + Vite production build to <code>client/dist/</code>":
      "TypeScript 검사 후 <code>client/dist/</code>로 Vite 프로덕션 빌드를 수행합니다",
    "Start Express in production mode serving built client":
      "빌드된 클라이언트를 제공하는 프로덕션 모드로 Express를 시작합니다",
    "Manually write Claude Code hooks to <code>~/.claude/settings.json</code>":
      "Claude Code hook을 <code>~/.claude/settings.json</code>에 수동으로 기록합니다",
    "Insert demo sessions, agents, and events (8 sessions / 23 agents / 106 events)":
      "데모 세션, 에이전트, 이벤트를 삽입합니다 (세션 8개 / 에이전트 23개 / 이벤트 106개)",
    "Refresh token totals for imported sessions from their transcripts. Never lowers an existing total":
      "가져온 세션의 토큰 합계를 트랜스크립트에서 다시 계산합니다. 기존 합계를 낮추지는 않습니다",
    "Re-derive non-workflow token totals for Claude sessions whose transcripts are still on disk and reset the compaction baselines, preserving workflow and Codex rows. One-time repair for databases inflated by the old per-record usage sum; stop the dashboard before running it":
      "\ud2b8\ub79c\uc2a4\ud06c\ub9bd\ud2b8\uac00 \uc544\uc9c1 \ub514\uc2a4\ud06c\uc5d0 \ub0a8\uc544 \uc788\ub294 Claude \uc138\uc158\uc758 workflow \uac00 \uc544\ub2cc \ud1a0\ud070 \ud569\uacc4\ub97c \ub2e4\uc2dc \ub3c4\ucd9c\ud558\uace0 \ucee8\ud14d\uc2a4\ud2b8 \uc555\ucd95 baseline \uc744 0\uc73c\ub85c \ub9cc\ub4e4\uba70, workflow \ubc0f Codex \ud589\uc740 \uadf8\ub300\ub85c \uc720\uc9c0\ud569\ub2c8\ub2e4. \uc608\uc804\uc758 \ub808\ucf54\ub4dc \ub2e8\uc704 usage \ud569\uc0b0\uc73c\ub85c \ubd80\ud480\ub824\uc9c4 \ub370\uc774\ud130\ubca0\uc774\uc2a4\ub97c \uc704\ud55c \uc77c\ud68c\uc131 \ubcf5\uad6c\uc774\uba70, \uc2e4\ud589 \uc804\uc5d0 Dashboard \ub97c \uc911\uc9c0\ud558\uc138\uc694",
    "Import historical Claude Code sessions from <code>~/.claude</code> with deep JSONL extraction (API errors, turn durations, thinking blocks, subagent data)":
      "<code>~/.claude</code>에서 과거 Claude Code 세션을 가져오며, JSONL을 심층 추출합니다 (API 오류, 턴 소요 시간, 생각 블록, Subagent 데이터)",
    "Delete all data from the database (keeps schema)":
      "데이터베이스의 모든 데이터를 삭제합니다 (스키마는 유지)",
    "Run all server and client tests": "서버 및 클라이언트 테스트를 모두 실행합니다",
    "Server integration tests only (Node built-in test runner)":
      "서버 통합 테스트만 실행합니다 (Node 내장 테스트 러너)",
    "Client unit tests only (Vitest + Testing Library)":
      "클라이언트 단위 테스트만 실행합니다 (Vitest + Testing Library)",
    "Install dependencies for the local MCP package under <code>mcp/</code>":
      "<code>mcp/</code> 하위의 로컬 MCP 패키지 의존성을 설치합니다",
    "Type-check MCP source without emitting build output":
      "빌드 결과물을 생성하지 않고 MCP 소스의 타입을 검사합니다",
    "Compile MCP server into <code>mcp/build/</code>":
      "MCP 서버를 <code>mcp/build/</code>로 컴파일합니다",
    "Start MCP server (stdio transport — for MCP hosts)":
      "MCP 서버를 시작합니다 (stdio 전송 방식 — MCP 호스트용)",
    "Start MCP HTTP+SSE server on port 8819 (Streamable HTTP + legacy SSE)":
      "포트 8819에서 MCP HTTP+SSE 서버를 시작합니다 (Streamable HTTP + 레거시 SSE)",
    "Start interactive MCP REPL with tab completion and colored output":
      "탭 자동완성과 컬러 출력을 지원하는 대화형 MCP REPL을 시작합니다",
    "Run MCP server in dev mode with <code>tsx</code> (stdio)":
      "<code>tsx</code>를 사용해 개발 모드로 MCP 서버를 실행합니다 (stdio)",
    "Run MCP HTTP server in dev mode with <code>tsx</code>":
      "<code>tsx</code>를 사용해 개발 모드로 MCP HTTP 서버를 실행합니다",
    "Run MCP REPL in dev mode with <code>tsx</code>":
      "<code>tsx</code>를 사용해 개발 모드로 MCP REPL을 실행합니다",
    "Build MCP container image with Docker (<code>agent-dashboard-mcp:local</code>)":
      "Docker로 MCP 컨테이너 이미지를 빌드합니다 (<code>agent-dashboard-mcp:local</code>)",
    "Build MCP container image with Podman (<code>localhost/agent-dashboard-mcp:local</code>)":
      "Podman으로 MCP 컨테이너 이미지를 빌드합니다 (<code>localhost/agent-dashboard-mcp:local</code>)",
    "Run MCP server unit tests": "MCP 서버 단위 테스트를 실행합니다",
    "Install Electron + electron-builder under <code>desktop/</code>; rebuilds <code>better-sqlite3</code> for Electron's ABI. Preflights the native <code>better-sqlite3</code> build; prints actionable setup help (incl. a no-toolchain alternative) on failure":
      "<code>desktop/</code> 하위에 Electron과 electron-builder를 설치하고, Electron의 ABI에 맞게 <code>better-sqlite3</code>를 다시 빌드합니다. 네이티브 <code>better-sqlite3</code> 빌드를 사전 점검하며, 실패 시 (툴체인이 없는 경우의 대안을 포함한) 실행 가능한 설정 도움말을 출력합니다",
    "Prebuild guard + <code>tsc</code> compile of the Electron main process into <code>desktop/out/</code>":
      "사전 빌드 가드 실행 후 Electron 메인 프로세스를 <code>tsc</code>로 컴파일하여 <code>desktop/out/</code>에 출력합니다",
    "Build, then launch the desktop app against <code>desktop/out/main.js</code>":
      "빌드한 뒤 <code>desktop/out/main.js</code>를 대상으로 데스크톱 앱을 실행합니다",
    "Desktop smoke test — spawn Electron and probe <code>/api/health</code>":
      "데스크톱 스모크 테스트 — Electron을 구동하고 <code>/api/health</code>를 점검합니다",
    "Build <strong>both</strong> per-arch DMGs (arm64 + x64). Correct for release — slower (packages each arch)":
      "<strong>두 개</strong>의 아키텍처별 DMG(arm64 + x64)를 모두 빌드합니다. 릴리스에 적합하며, 더 느립니다(각 아키텍처를 개별 패키징)",
    "Build an Apple-Silicon-only DMG — fast (~1 min), recommended for a single machine":
      "Apple Silicon 전용 DMG를 빌드합니다 — 빠르며(약 1분), 단일 머신에서 사용을 권장합니다",
    "Build an Intel-only DMG — fast (macOS host)":
      "Intel 전용 DMG를 빌드합니다 — 빠름 (macOS 호스트)",
    "Build one merged universal DMG (arm64 + x86_64 in a single file). Optional, slowest — not what the release ships":
      "하나의 병합된 유니버설 DMG(arm64 + x86_64, 단일 파일)를 빌드. 선택 사항, 가장 느림 — 릴리스에 포함되는 것은 아님",
    "Build the Windows NSIS installer <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> (Windows host)":
      "Windows NSIS 설치 프로그램 <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>를 빌드합니다 (Windows 호스트)",
    "Build the no-install portable <code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code> (Windows host)":
      "설치가 필요 없는 포터블 <code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>를 빌드합니다 (Windows 호스트)",
    "Regenerate <code>desktop/assets/icon.ico</code> from <code>icon.png</code> (PowerShell + .NET; Windows host)":
      "<code>icon.png</code>로부터 <code>desktop/assets/icon.ico</code>를 재생성합니다 (PowerShell + .NET; Windows 호스트)",
    "Format all files with Prettier": "Prettier로 모든 파일의 서식을 정리합니다",
    "Check formatting without writing": "파일을 수정하지 않고 서식만 검사합니다",
    "Core dashboard telemetry is composed of three processes (Claude hook source, dashboard server, browser UI). When the local MCP sidecar is enabled, it integrates with the same dashboard API via stdio, HTTP+SSE, or interactive REPL transport.":
      "핵심 대시보드 텔레메트리는 세 개의 프로세스(Claude hook 소스, 대시보드 서버, 브라우저 UI)로 구성됩니다. 로컬 MCP 사이드카가 활성화되면 stdio, HTTP+SSE, 또는 대화형 REPL 전송 방식을 통해 동일한 대시보드 API와 통합됩니다.",
    "Full system architecture — Claude Code process → Hook Layer → Server → Browser":
      "전체 시스템 아키텍처 — Claude Code 프로세스 → Hook 계층 → 서버 → 브라우저",
    "Agent status transitions driven by hook events. <code>waiting</code> is a real persisted status — agents start as <code>waiting</code> and return to it after each turn. Error recovery requires active user retry (<code>UserPromptSubmit</code> or <code>PreToolUse</code>). A background watchdog detects API errors in transcripts every 15 s. The same watchdog also recovers <code>Esc</code>-cancelled turns — via either the transcript <code>[Request interrupted by user]</code> marker or the idle-working timeout when <code>Esc</code> preceded any output — and moves the session to Waiting.":
      "hook 이벤트에 의해 구동되는 에이전트 상태 전이입니다. <code>waiting</code>은 실제로 영속화되는 상태로, 에이전트는 <code>waiting</code>으로 시작하며 매 턴이 끝날 때마다 이 상태로 돌아옵니다. 오류 복구에는 사용자의 능동적인 재시도(<code>UserPromptSubmit</code> 또는 <code>PreToolUse</code>)가 필요합니다. 백그라운드 워치독이 15초마다 transcript에서 API 오류를 감지합니다. 동일한 워치독은 transcript의 <code>[Request interrupted by user]</code> 마커, 또는 <code>Esc</code> 입력 후 아무 출력도 없을 때의 유휴 작업 타임아웃을 통해 <code>Esc</code>로 취소된 턴도 복구하며, 세션을 Waiting 상태로 전환합니다.",
    "Session status lifecycle. <code>waiting</code> is a UI overlay — persisted as <code>active</code> with <code>awaiting_input_since</code> set and <code>awaiting_reason</code> recording why (<code>session_start</code>, <code>stop</code>, <code>notification</code>, or <code>interrupted</code>). <code>SessionEnd</code> preserves error state. Error recovery requires <code>UserPromptSubmit</code> or <code>PreToolUse</code>. The watchdog also recovers <code>Esc</code>-cancelled turns (marker or idle-timeout path) and moves the session to Waiting. The dashboard surfaces the reason on every Waiting badge (hover tooltip, plus an inline chip on the Sessions table and session detail header) and in a waiting-for-input banner on the session detail page.":
      "세션 상태 생명주기입니다. <code>waiting</code>은 UI 오버레이로, 실제로는 <code>awaiting_input_since</code>가 설정되고 <code>awaiting_reason</code>이 이유(<code>session_start</code>, <code>stop</code>, <code>notification</code> 또는 <code>interrupted</code>)를 기록하는 <code>active</code> 상태로 영속화됩니다. <code>SessionEnd</code>는 오류 상태를 그대로 유지합니다. 오류 복구에는 <code>UserPromptSubmit</code> 또는 <code>PreToolUse</code>가 필요합니다. 워치독은 (마커 또는 유휴 타임아웃 경로를 통해) <code>Esc</code>로 취소된 턴도 복구하여 세션을 Waiting 상태로 전환합니다. Bảng điều khiển hiển thị lý do này trên mọi huy hiệu Waiting (tooltip khi di chuột, kèm chip nội dòng trên bảng Sessions và tiêu đề trang chi tiết phiên) và trong banner chờ phản hồi trên trang chi tiết phiên.",
    "Complete event ingestion from hook fire to browser re-render":
      "hook 발생부터 브라우저 리렌더링까지 이어지는 전체 이벤트 수집 과정",
    "Initial load + WebSocket subscription lifecycle": "초기 로드 + WebSocket 구독 생명주기",
    "Server module dependency graph": "서버 모듈 의존성 그래프",
    Module: "모듈",
    Responsibility: "역할",
    "Express app setup, middleware (CORS, JSON 1MB limit), route mounting, static serving in production, HTTP server, auto-hook installation on startup":
      "Express 앱 설정, 미들웨어(CORS, JSON 1MB 제한), 라우트 마운트, 프로덕션 환경의 정적 파일 제공, HTTP 서버, 시작 시 hook 자동 설치",
    "SQLite connection, WAL/FK pragmas, schema migrations (<code >CREATE TABLE IF NOT EXISTS</code >), all prepared statements as a reusable <code>stmts</code> object. Tries <code>better-sqlite3</code> first, falls back to built-in <code>node:sqlite</code> via <code>compat-sqlite.js</code>":
      "SQLite 연결, WAL/FK 프래그마, 스키마 마이그레이션(<code >CREATE TABLE IF NOT EXISTS</code >), 재사용 가능한 <code>stmts</code> 객체로 관리되는 모든 프리페어드 스테이트먼트. 먼저 <code>better-sqlite3</code>를 시도하고, 실패하면 <code>compat-sqlite.js</code>를 통해 내장된 <code>node:sqlite</code>로 폴백합니다",
    "Compatibility wrapper giving Node.js built-in <code>node:sqlite</code> (<code>DatabaseSync</code>) the same API as <code>better-sqlite3</code> — pragma, transaction, prepare. Used as automatic fallback on Node 22+":
      "Node.js 내장 <code>node:sqlite</code>(<code>DatabaseSync</code>)에 <code>better-sqlite3</code>와 동일한 API(pragma, transaction, prepare)를 제공하는 호환성 래퍼입니다. Node 22 이상에서 자동 폴백으로 사용됩니다",
    "WebSocket server on <code>/ws</code> path, 30s ping/pong heartbeat, typed <code>broadcast(type, data)</code> function":
      "<code>/ws</code> 경로의 WebSocket 서버, 30초 ping/pong 하트비트, 타입이 지정된 <code>broadcast(type, data)</code> 함수",
    "Core event processing inside SQLite transactions. Auto-creates sessions/agents. Switch-case dispatch by hook type. Extracts token usage from Stop events.":
      "SQLite 트랜잭션 내에서 수행되는 핵심 이벤트 처리입니다. 세션/에이전트를 자동으로 생성합니다. hook 타입별로 switch-case 방식으로 분기합니다. Stop 이벤트에서 토큰 사용량을 추출합니다.",
    "CRUD with pagination. GET includes agent count via LEFT JOIN. POST is idempotent on session ID.":
      "페이지네이션을 지원하는 CRUD입니다. GET은 LEFT JOIN을 통해 에이전트 수를 포함합니다. POST는 세션 ID 기준으로 멱등성을 가집니다.",
    "CRUD with status/session_id filtering. PATCH broadcasts <code>agent_updated</code>.":
      "status/session_id 필터링을 지원하는 CRUD입니다. PATCH는 <code>agent_updated</code>를 브로드캐스트합니다.",
    "Read-only event listing with session_id filter and pagination.":
      "session_id 필터와 페이지네이션을 지원하는 읽기 전용 이벤트 목록입니다.",
    "Single aggregate query — total/active counts, status distributions, WS connection count.":
      "단일 집계 쿼리 — 전체/활성 개수, 상태 분포, WS 연결 수.",
    "Extended analytics — token totals, tool usage counts, daily event/session trends, agent type distribution, event type breakdown, average events per session.":
      "확장 분석 — 토큰 총합, 도구 사용 횟수, 일별 이벤트/세션 추이, 에이전트 유형 분포, 이벤트 유형별 분류, 세션당 평균 이벤트 수.",
    "Model pricing CRUD (list, upsert, delete). Per-session and global cost calculation with pattern-based model matching and specificity sorting.":
      "모델 가격 CRUD(목록 조회, upsert, 삭제)입니다. 패턴 기반 모델 매칭과 구체성 정렬을 통해 세션별 및 전체 비용을 계산합니다.",
    "System info (DB size, row counts, hook status, server uptime). Data export as JSON. Session cleanup (abandon stale active sessions, purge old completed sessions). Clear all data. Reset pricing to defaults. Reinstall hooks.":
      "시스템 정보(DB 크기, 행 수, hook 상태, 서버 가동 시간)입니다. 데이터를 JSON으로 내보냅니다. 세션 정리(오래된 활성 세션을 중단 처리하고, 오래된 완료 세션을 삭제)를 수행합니다. 모든 데이터를 삭제합니다. 가격을 기본값으로 초기화합니다. hook을 재설치합니다.",
    "Aggregate workflow visualization data (agent orchestration, tool transitions, collaboration networks, workflow patterns, model delegation, error propagation, concurrency, session complexity, compaction impact). Accepts <code>?status=active|completed</code> filter. Per-session drill-in with agent tree, tool timeline, and events.":
      "워크플로 시각화 데이터를 집계합니다(에이전트 오케스트레이션, 도구 전이, 협업 네트워크, 워크플로 패턴, 모델 위임, 오류 전파, 동시성, 세션 복잡도, 컴팩션 영향). <code>?status=active|completed</code> 필터를 지원합니다. 에이전트 트리, 도구 타임라인, 이벤트를 포함한 세션별 상세 드릴인을 제공합니다.",
    "React component tree": "React 컴포넌트 트리",
    Purpose: "목적",
    "<code>lib/api.ts</code>": "<code>lib/api.ts</code>",
    "Typed fetch wrapper — one method per REST endpoint. All return typed promises.":
      "타입이 지정된 fetch 래퍼 — REST 엔드포인트마다 메서드가 하나씩 있습니다. 모두 타입이 지정된 Promise를 반환합니다.",
    "<code>lib/types.ts</code>": "<code>lib/types.ts</code>",
    "TypeScript interfaces: <code>Session</code>, <code>Agent</code>, <code>DashboardEvent</code>, <code>Stats</code>, <code>Analytics</code>, <code>WSMessage</code>, plus all workflow-related types (<code>WorkflowData</code>, <code>SessionDrillIn</code>, etc). Status config maps.":
      "TypeScript 인터페이스: <code>Session</code>, <code>Agent</code>, <code>DashboardEvent</code>, <code>Stats</code>, <code>Analytics</code>, <code>WSMessage</code>와 워크플로 관련 타입 전체(<code>WorkflowData</code>, <code>SessionDrillIn</code> 등). 상태 설정 맵.",
    "<code>lib/eventBus.ts</code>": "<code>lib/eventBus.ts</code>",
    "Set-based pub/sub. <code>subscribe(fn)</code> returns an unsubscribe function for clean useEffect teardown.":
      "Set 기반 pub/sub입니다. <code>subscribe(fn)</code>은 useEffect를 깔끔하게 정리할 수 있도록 구독 해제 함수를 반환합니다.",
    "<code>lib/format.ts</code>": "<code>lib/format.ts</code>",
    "Date/time formatting helpers — relative time, duration, ISO display.":
      "날짜/시간 포맷팅 헬퍼 — 상대 시간, 기간, ISO 표시.",
    "<code>hooks/useWebSocket.ts</code>": "<code>hooks/useWebSocket.ts</code>",
    "Auto-reconnecting WebSocket React hook. 2-second reconnect interval. Publishes messages to eventBus.":
      "자동 재연결되는 WebSocket React hook입니다. 재연결 간격은 2초입니다. 메시지를 eventBus에 게시합니다.",
    "The dashboard is a Progressive Web App with its own <code>manifest.json</code> and Service Worker (<code>client/public/sw.js</code>). The landing page and wiki are also independent PWAs with separate manifests and service workers.":
      "대시보드는 자체 <code>manifest.json</code>과 서비스 워커(<code>client/public/sw.js</code>)를 갖춘 Progressive Web App입니다. 랜딩 페이지와 위키 또한 각각 별도의 매니페스트와 서비스 워커를 가진 독립적인 PWA입니다.",
    Surface: "영역",
    Manifest: "매니페스트",
    "Service Worker": "서비스 워커",
    Strategy: "전략",
    "<code>client/public/manifest.json</code>": "<code>client/public/manifest.json</code>",
    "<code>client/public/sw.js</code>": "<code>client/public/sw.js</code>",
    "Precaches app shell. Cache-first for static assets (JS/CSS bundles). Network-first for navigation with offline fallback. Skips <code>/api/*</code>, <code>/ws</code>, and Vite HMR. Preserves push notification handlers.":
      "앱 셸을 사전 캐싱합니다. 정적 자산(JS/CSS 번들)은 캐시 우선 방식입니다. 내비게이션은 오프라인 폴백을 포함한 네트워크 우선 방식입니다. <code>/api/*</code>, <code>/ws</code>, Vite HMR은 제외됩니다. 푸시 알림 핸들러는 유지됩니다.",
    "Landing page": "랜딩 페이지",
    "<code>manifest.json</code> (root)": "<code>manifest.json</code> (root)",
    "<code>sw.js</code> (root)": "<code>sw.js</code> (root)",
    "Precaches HTML shell, favicon, OG image. Lazy-caches screenshot PNGs on first view. Network-first HTML, cache-first assets.":
      "HTML 셸, 파비콘, OG 이미지를 사전 캐싱합니다. 스크린샷 PNG는 처음 볼 때 지연 캐싱됩니다. HTML은 네트워크 우선, 자산은 캐시 우선 방식입니다.",
    Wiki: "위키",
    "<code>wiki/manifest.json</code>": "<code>wiki/manifest.json</code>",
    "<code>wiki/sw.js</code>": "<code>wiki/sw.js</code>",
    "Precaches <code>index.html</code>, <code>style.css</code>, <code>script.js</code>. Fully offline after one visit.":
      "<code>index.html</code>, <code>style.css</code>, <code>script.js</code>를 사전 캐싱합니다. 한 번 방문하면 완전히 오프라인으로 동작합니다.",
    'All three SWs call <code>skipWaiting()</code> on install and delete stale caches on activate (keyed by version strings like <code>dashboard-v1</code>). Manifests use SVG icons (<code>favicon.svg</code>) with <code>sizes="any"</code>. iOS standalone mode is enabled via <code>apple-mobile-web-app-capable</code> meta tags.':
      '세 서비스 워커 모두 설치 시 <code>skipWaiting()</code>을 호출하고, 활성화 시 (<code>dashboard-v1</code>과 같은 버전 문자열을 키로 하는) 오래된 캐시를 삭제합니다. 매니페스트는 <code>sizes="any"</code>를 지정한 SVG 아이콘(<code>favicon.svg</code>)을 사용합니다. iOS 독립 실행형 모드는 <code>apple-mobile-web-app-capable</code> 메타 태그를 통해 활성화됩니다.',
    "The client deliberately avoids Redux / Zustand / Context. Each page owns its data and lifecycle. WebSocket events trigger a reload or append — no complex state merging.":
      "클라이언트는 의도적으로 Redux / Zustand / Context를 사용하지 않습니다. 각 페이지가 자신의 데이터와 생명주기를 직접 소유합니다. WebSocket 이벤트는 재로드 또는 추가(append)를 트리거할 뿐, 복잡한 상태 병합은 하지 않습니다.",
    "No global store — by design": "전역 스토어 없음 — 의도된 설계",
    "There is no cross-page shared state. Each page fetches and owns exactly the data it displays. This simplifies debugging and avoids stale-closure hazards that are common with global stores in long-running WebSocket apps.":
      "페이지 간에 공유되는 상태는 없습니다. 각 페이지는 자신이 표시할 데이터만 정확히 가져와 소유합니다. 이는 디버깅을 단순화하고, 장시간 실행되는 WebSocket 앱에서 전역 스토어를 사용할 때 흔히 발생하는 stale-closure 문제를 방지합니다.",
    Index: "인덱스",
    Table: "테이블",
    "Column(s)": "컬럼",
    "Fast agent lookup by session": "세션 기준 빠른 에이전트 조회",
    "Kanban column queries": "칸반 컬럼 쿼리",
    "Session detail event list": "세션 상세 이벤트 목록",
    "Filter events by type": "유형별 이벤트 필터링",
    "Activity feed ordering": "활동 피드 정렬",
    "Status filter on sessions page": "세션 페이지의 상태 필터",
    "Default sort order": "기본 정렬 순서",
    Pragma: "Pragma",
    Value: "값",
    Rationale: "근거",
    "Concurrent reads during writes. Far better for read-heavy dashboards.":
      "쓰기 중에도 동시 읽기가 가능합니다. 읽기 위주의 대시보드에 훨씬 유리합니다.",
    "Referential integrity — prevents orphaned agents/events.":
      "참조 무결성 — 고아 에이전트/이벤트 발생을 방지합니다.",
    "Wait up to 5s for write lock instead of failing immediately under load.":
      "부하 상황에서 즉시 실패하는 대신 쓰기 락을 최대 5초까지 대기합니다.",
    'All endpoints return JSON. Errors follow <code>{ "error": { "code", "message" } }</code>. The OpenAPI 3.0 spec comprehensively documents every backend route - parameters, request/response schemas, field descriptions, and examples. It is served at <code>/api/openapi.json</code> (with a committed <code>openapi.yaml</code> mirror), rendered as interactive Swagger UI at <code>/api/docs</code>, and as a clean, read-optimized ReDoc reference at <code>/api/redoc</code>. ReDoc is self-hosted, so it works fully offline.':
      '모든 엔드포인트는 JSON을 반환합니다. 오류는 <code>{ "error": { "code", "message" } }</code> 형식을 따릅니다. OpenAPI 3.0 스펙은 모든 백엔드 라우트 - 파라미터, 요청/응답 스키마, 필드 설명, 예제 - 를 빠짐없이 문서화합니다. 이 스펙은 <code>/api/openapi.json</code>에서 제공되며(커밋된 <code>openapi.yaml</code> 미러 포함), <code>/api/docs</code>에서 대화형 Swagger UI로, <code>/api/redoc</code>에서 읽기에 최적화된 깔끔한 ReDoc 레퍼런스로 렌더링됩니다. ReDoc은 자체 호스팅되므로 완전히 오프라인에서도 동작합니다.',
    '<span class="caption-icon">📘</span> <span>Swagger UI at <code>/api/docs</code> — auto-generated interactive playground for every REST endpoint. Try-it-out forms, request/response schema, auth headers, and curl snippets</span>':
      '<span class="caption-icon">📘</span> <span><code>/api/docs</code>의 Swagger UI — 모든 REST 엔드포인트에 대해 자동 생성되는 대화형 플레이그라운드입니다. Try-it-out 폼, 요청/응답 스키마, 인증 헤더, curl 스니펫을 제공합니다</span>',
    Property: "속성",
    Path: "경로",
    Protocol: "프로토콜",
    "Standard WebSocket (RFC 6455)": "표준 WebSocket (RFC 6455)",
    Heartbeat: "하트비트",
    "Server pings every 30s — clients that don't pong are terminated":
      "서버는 30초마다 핑을 보내며, pong으로 응답하지 않는 클라이언트는 연결이 종료됩니다",
    Reconnect: "재연결",
    "Client retries every 2 seconds on disconnect":
      "클라이언트는 연결이 끊기면 2초마다 재시도합니다",
    "Client WebSocket auto-reconnect state machine": "클라이언트 WebSocket 자동 재연결 상태 머신",
    "<code>scripts/hook-handler.js</code> is a minimal, fail-safe forwarder. It always exits 0 so it can never block Claude Code regardless of server state.":
      "<code>scripts/hook-handler.js</code>는 최소한의 기능만 갖춘 fail-safe 포워더입니다. 서버 상태와 무관하게 Claude Code를 절대 차단하지 않도록 항상 0으로 종료합니다.",
    "hook-handler.js flow — always exits 0, never blocks Claude Code":
      "hook-handler.js 흐름 — 항상 0으로 종료되며 Claude Code를 절대 차단하지 않습니다",
    "Hook installation is idempotent — safe to run multiple times":
      "Hook 설치는 멱등적이므로 여러 번 실행해도 안전합니다",
    '<span class="caption-icon">📥</span> <span>Settings → Import History — rescan default paths, set a custom directory, or drag-and-drop <code>.gz</code> archives. Progress bar and result card show counts for every run</span>':
      '<span class="caption-icon">📥</span> <span>설정 → 가져오기 기록 — 기본 경로를 다시 스캔하거나, 사용자 지정 디렉터리를 설정하거나, <code>.gz</code> 아카이브를 드래그 앤 드롭할 수 있습니다. 진행률 표시줄과 결과 카드에 매 실행의 집계 수치가 표시됩니다</span>',
    "The dashboard ships with a first-class <b>history importer</b> that backfills sessions, agents, events, tokens, and costs from Claude Code JSONL transcripts. Live hook ingestion and manual import share the exact same parser — <code>parseSessionFile</code> + <code>importSession</code> in <code>scripts/import-history.js</code> — which is the architectural contract that guarantees imported token totals and cost values are identical to those captured in real time. Re-imports are idempotent: session IDs are the dedup key and compaction <code>baseline_*</code> columns preserve pre-compaction token totals.":
      "대시보드는 Claude Code JSONL transcript로부터 세션, 에이전트, 이벤트, 토큰, 비용을 소급 채워주는 완전한 기능의 <b>기록 가져오기(history importer)</b>를 기본 제공합니다. 실시간 hook 수집과 수동 가져오기는 정확히 동일한 파서 — <code>scripts/import-history.js</code>의 <code>parseSessionFile</code> + <code>importSession</code> — 를 공유하며, 이것이 가져온 토큰 총합과 비용 값이 실시간으로 수집된 값과 동일함을 보장하는 아키텍처적 계약입니다. 재가져오기는 멱등적입니다: 세션 ID가 중복 제거 키로 사용되며, 컴팩션 <code>baseline_*</code> 컬럼이 컴팩션 이전의 토큰 총합을 보존합니다.",
    "All three modes funnel into the same parser and DB transaction — imported numbers match live capture bit-for-bit":
      "세 가지 모드 모두 동일한 파서와 DB 트랜잭션으로 수렴합니다 — 가져온 수치는 실시간 캡처 값과 완전히 일치합니다",
    "Upload path: multipart → safe extract → walk → parse → import — every temp dir reclaimed in <code>finally</code>":
      "업로드 경로: multipart → 안전한 압축 해제 → 순회 → 파싱 → 가져오기 — 모든 임시 디렉터리는 <code>finally</code>에서 회수됩니다",
    "The <code>baseline_*</code> columns make cost monotonic under re-imports — compacted sessions retain pre-compaction usage for billing":
      "<code>baseline_*</code> 컬럼은 재가져오기 시에도 비용이 단조 증가하도록 보장합니다 — 컴팩션된 세션도 청구를 위해 컴팩션 이전 사용량을 유지합니다",
    Layout: "레이아웃",
    Example: "예시",
    Handling: "처리 방식",
    "Default Claude Code": "기본 Claude Code",
    "Session transcript — extracts tokens, compactions, tool uses, turn durations":
      "세션 transcript — 토큰, 컴팩션, 도구 사용, 턴 소요 시간을 추출합니다",
    "Default subagent": "기본 서브에이전트",
    "Paired with parent on discovery via <code>findSessionSubagents</code>":
      "검색 시 <code>findSessionSubagents</code>를 통해 부모와 페어링됨",
    "Alternative subagent": "대체 서브에이전트",
    "Paired with parent on discovery (second layout probed automatically)":
      "검색 시 부모와 페어링됨(두 번째 레이아웃이 자동으로 탐색됨)",
    "Orphan subagent": "고아 서브에이전트",
    "No parent JSONL in source, but <code>sid</code> exists in DB":
      "소스에 부모 JSONL이 없지만 DB에 <code>sid</code>가 존재함",
    "<code>importFromDirectory</code> probes both layouts; attaches if the parent is found":
      "<code>importFromDirectory</code>가 두 레이아웃을 모두 탐색하며, 부모를 찾으면 연결함",
    "Flat JSONL drop": "평면 JSONL 드롭",
    "Recognized as a loose session transcript": "느슨한 세션 트랜스크립트로 인식됨",
    Archives: "아카이브",
    "Extracted into a per-request temp dir, then walked by the same importer":
      "요청별 임시 디렉터리로 압축 해제된 후 동일한 임포터가 순회함",
    "Single-file gzip": "단일 파일 gzip",
    "Gunzipped in streaming mode with running byte-counter size cap":
      "실행 중인 바이트 카운터 크기 제한을 적용해 스트리밍 방식으로 압축 해제됨",
    Threat: "위협",
    Mitigation: "완화 조치",
    "Path traversal via archive entries": "아카이브 항목을 통한 경로 순회 공격",
    "<code>archive.safeJoin</code> resolves under the extraction root; any <code>..</code> or absolute path returns <code>null</code>":
      "<code>archive.safeJoin</code>은 추출 루트 하위에서 경로를 해석하며, <code>..</code>나 절대 경로는 <code>null</code>을 반환함",
    "Zip / tar / gzip bombs": "Zip / tar / gzip 폭탄",
    "<code>MAX_EXTRACT_BYTES</code> (default 4 GB) enforced by running byte counter; aborts with <code>ExtractionLimitError</code> → HTTP 413":
      "실행 중인 바이트 카운터가 <code>MAX_EXTRACT_BYTES</code>(기본값 4GB)를 강제하며, 초과 시 <code>ExtractionLimitError</code>로 중단됨 → HTTP 413",
    "Per-file upload size abuse": "파일별 업로드 크기 남용",
    "multer <code>limits.fileSize = MAX_UPLOAD_BYTES</code> (default 1 GB)":
      "multer <code>limits.fileSize = MAX_UPLOAD_BYTES</code>(기본값 1GB)",
    "Too many files per request": "요청당 파일 수 초과",
    "multer <code>limits.files = MAX_UPLOAD_FILES</code> (default 2000)":
      "multer <code>limits.files = MAX_UPLOAD_FILES</code>(기본값 2000)",
    "Unsupported file types": "지원되지 않는 파일 형식",
    "<code>fileFilter</code> drops them early and reports them in <code>rejected_files[]</code>":
      "<code>fileFilter</code>가 이를 조기에 제외하고 <code>rejected_files[]</code>에 보고함",
    "Concurrent upload temp-dir collisions": "동시 업로드 임시 디렉터리 충돌",
    "Per-request temp dir on <code>req._ccamUploadDir</code>; created in multer <code>destination</code>, reclaimed in <code>finally</code>":
      "<code>req._ccamUploadDir</code>에 요청별 임시 디렉터리가 사용됨. multer <code>destination</code>에서 생성되고 <code>finally</code>에서 회수됨",
    "Arbitrary absolute path on <code>scan-path</code>":
      "<code>scan-path</code>에 임의의 절대 경로 입력",
    "Validated: must be absolute (after <code>~</code> expansion), exist, and be a directory":
      "검증됨: <code>~</code> 확장 후 절대 경로여야 하며, 존재해야 하고, 디렉터리여야 함",
    "Relative / traversal paths on <code>scan-path</code>":
      "<code>scan-path</code>의 상대 경로 / 순회 경로",
    "Rejected with <code>INVALID_INPUT</code>": "<code>INVALID_INPUT</code>으로 거부됨",
    "Maximum size per uploaded file on <code>/api/import/upload</code>":
      "<code>/api/import/upload</code>에서 업로드 파일당 최대 크기",
    "Maximum files per upload request": "업로드 요청당 최대 파일 수",
    "Ceiling on total uncompressed bytes from any single archive (zip-bomb defense)":
      "단일 아카이브에서 압축 해제된 총 바이트 수의 상한(zip 폭탄 방어)",
    "Every import emits <code>import.progress</code> messages on <code>/ws</code>. Messages are throttled to at most one every ~150 ms to avoid flooding the channel on multi-thousand-session imports; the terminal <code>complete</code> and <code>error</code> frames are never throttled.":
      "모든 임포트는 <code>/ws</code>에서 <code>import.progress</code> 메시지를 전송합니다. 수천 개 세션을 임포트할 때 채널이 넘치지 않도록 메시지는 최대 약 150ms에 한 번으로 제한되며, 최종 <code>complete</code> 및 <code>error</code> 프레임은 절대 제한되지 않습니다.",
    "Phases: <code>start</code> → <code>scan</code> → <code>extract</code> (upload only) → <code>parse</code> → <code>complete</code>, with <code>error</code> / <code>extract_error</code> replacing <code>complete</code> on failure.":
      "단계: <code>start</code> → <code>scan</code> → <code>extract</code>(업로드 전용) → <code>parse</code> → <code>complete</code>이며, 실패 시 <code>complete</code> 대신 <code>error</code> / <code>extract_error</code>가 대체됩니다.",
    "In addition to dashboard telemetry, this project includes a production-grade local MCP server and complete extension scaffolding for both Claude Code and Codex. This gives agents a richer local tool surface while keeping all execution local-first. The MCP server supports three transport modes: stdio for host integration, HTTP+SSE for remote clients, and an interactive REPL for operator debugging.":
      "대시보드 텔레메트리 외에도 이 프로젝트는 프로덕션 등급의 로컬 MCP 서버와 Claude Code 및 Codex 모두를 위한 완전한 확장 스캐폴딩을 포함합니다. 이를 통해 모든 실행을 로컬 우선으로 유지하면서도 에이전트에게 더 풍부한 로컬 도구 인터페이스를 제공합니다. MCP 서버는 호스트 통합을 위한 stdio, 원격 클라이언트를 위한 HTTP+SSE, 운영자 디버깅을 위한 대화형 REPL이라는 세 가지 전송 모드를 지원합니다.",
    '<span class="caption-icon">🔧</span> MCP Server REPL — interactive tool invocation terminal with colored JSON output, argument prompts, error formatting, and session-aware context for rapid testing':
      '<span class="caption-icon">🔧</span> MCP 서버 REPL — 색상이 적용된 JSON 출력, 인자 프롬프트, 오류 포맷팅, 빠른 테스트를 위한 세션 인식 컨텍스트를 갖춘 대화형 도구 호출 터미널',
    "Local extension architecture: host instructions + skills + multi-transport MCP sidecar":
      "로컬 확장 아키텍처: 호스트 지침 + 스킬 + 다중 전송 MCP 사이드카",
    "The <code>mcp/</code> package exposes dashboard-oriented tools for AI agents across three transport modes. Mutation and destructive operations are policy-gated by environment variables and disabled by default. HTTP mode serves both Streamable HTTP (protocol 2025-11-25) and legacy SSE (protocol 2024-11-05). REPL mode provides tab-completed interactive tool invocation with colored output and JSON syntax highlighting.":
      "<code>mcp/</code> 패키지는 세 가지 전송 모드에서 AI 에이전트를 위한 대시보드 지향 도구를 제공합니다. 변경 및 파괴적 작업은 환경 변수로 정책 제어되며 기본적으로 비활성화되어 있습니다. HTTP 모드는 Streamable HTTP(프로토콜 2025-11-25)와 레거시 SSE(프로토콜 2024-11-05)를 모두 지원합니다. REPL 모드는 탭 완성이 가능한 대화형 도구 호출을 색상 출력 및 JSON 구문 강조와 함께 제공합니다.",
    Component: "구성 요소",
    Location: "위치",
    Notes: "비고",
    "MCP source": "MCP 소스",
    "TypeScript server, tools, policy guards, transport layer, CLI UI":
      "TypeScript 서버, 도구, 정책 가드, 전송 계층, CLI UI",
    "MCP build output": "MCP 빌드 출력",
    "Compiled JavaScript runtime for all transport modes":
      "모든 전송 모드를 위한 컴파일된 JavaScript 런타임",
    "MCP docs": "MCP 문서",
    "Tool catalog, architecture diagrams, host integration examples, REPL guide":
      "도구 카탈로그, 아키텍처 다이어그램, 호스트 통합 예제, REPL 가이드",
    "Transport layer": "전송 계층",
    "HTTP+SSE server, interactive REPL, tool handler collector":
      "HTTP+SSE 서버, 대화형 REPL, 도구 핸들러 수집기",
    "CLI UI": "CLI UI",
    "ANSI banner, colors, formatter with tables, boxes, JSON highlighting":
      "ANSI 배너, 색상, 표·박스·JSON 강조 표시를 지원하는 포맷터",
    "Runtime commands": "런타임 명령",
    "Start MCP in stdio, HTTP+SSE, or REPL mode (production or dev)":
      "stdio, HTTP+SSE 또는 REPL 모드(프로덕션 또는 개발)로 MCP 시작",
    Target: "대상",
    Files: "파일",
    "Claude Code": "Claude Code",
    "Persistent project instructions + path-scoped coding rules":
      "영구 프로젝트 지침 + 경로 범위 코딩 규칙",
    "Claude Code Skills": "Claude Code 스킬",
    "Reusable workflows (onboarding, shipping, MCP ops, live debugging)":
      "재사용 가능한 워크플로(온보딩, 배포, MCP 운영, 라이브 디버깅)",
    "Claude Code Subagents": "Claude Code 서브에이전트",
    "Specialized reviewers for backend, frontend, and MCP code paths":
      "백엔드, 프런트엔드, MCP 코드 경로를 위한 전문 리뷰어",
    "Codex Base Instructions": "Codex 기본 지침",
    "Project-wide guidance + execution policy defaults":
      "프로젝트 전반의 가이드 + 실행 정책 기본값",
    "Codex Skills": "Codex 스킬",
    "Task-specific skills aligned to this repository": "이 저장소에 맞춘 작업별 스킬",
    "Codex Agents": "Codex 에이전트",
    "Reusable custom-agent templates for implementation and review":
      "구현 및 리뷰를 위한 재사용 가능한 커스텀 에이전트 템플릿",
    Role: "역할",
    "Receives Claude hook payloads over stdin and forwards them to dashboard API":
      "stdin을 통해 Claude 훅 페이로드를 수신하여 대시보드 API로 전달함",
    "Writes/updates hook registration in <code>~/.claude/settings.json</code>":
      "<code>~/.claude/settings.json</code>에 훅 등록을 작성/업데이트함",
    "Batch history importer used by server startup auto-import, the <code>/api/import/*</code> routes, and the <code>import-history</code> CLI. Exposes <code>importAllSessions()</code> for the default projects dir and the generalized <code>importFromDirectory(dbModule, rootDir, {onProgress})</code> which walks any directory recursively, classifies session vs subagent JSONLs (probes both <code>&lt;proj&gt;/&lt;sid&gt;/subagents/*</code> and <code>&lt;proj&gt;/subagents/&lt;sid&gt;/*</code> layouts), and funnels everything through the shared <code>parseSessionFile</code> + <code>importSession</code> pipeline — identical to live ingest. <b>Re-import is fully incremental</b>: a per-event-type high-water mark (<code>MAX(created_at) GROUP BY event_type</code> for the session) drives <code>ts &gt; cutoff[type]</code> dedup for Stop / PostToolUse / TurnDuration / ToolError, so long-running sessions whose transcripts grow across multiple days keep receiving new events on every re-run. <code>sessions.ended_at</code> is rolled forward when the JSONL has progressed past the stored value, and message-count metadata is refreshed on every pass. Session-ID dedup and <code>baseline_*</code> preservation keep token totals stable. Extracts tokens, API errors, turn durations, thinking blocks, usage extras, and per-subagent breakdowns":
      "서버 시작 시 자동 임포트, <code>/api/import/*</code> 라우트, <code>import-history</code> CLI에서 사용되는 배치 히스토리 임포터입니다. 기본 프로젝트 디렉터리를 위한 <code>importAllSessions()</code>와, 임의의 디렉터리를 재귀적으로 순회하며 세션과 서브에이전트 JSONL을 분류하고(<code>&lt;proj&gt;/&lt;sid&gt;/subagents/*</code> 및 <code>&lt;proj&gt;/subagents/&lt;sid&gt;/*</code> 두 레이아웃을 모두 탐색), 모든 것을 공유되는 <code>parseSessionFile</code> + <code>importSession</code> 파이프라인으로 전달하는 일반화된 <code>importFromDirectory(dbModule, rootDir, {onProgress})</code>를 제공하며 — 이는 실시간 수집과 동일합니다. <b>재임포트는 완전히 증분 방식입니다</b>: 이벤트 유형별 하이워터마크(세션에 대한 <code>MAX(created_at) GROUP BY event_type</code>)가 Stop / PostToolUse / TurnDuration / ToolError에 대한 <code>ts &gt; cutoff[type]</code> 중복 제거를 주도하므로, 여러 날에 걸쳐 트랜스크립트가 늘어나는 장시간 실행 세션도 재실행할 때마다 새 이벤트를 계속 수신합니다. JSONL이 저장된 값보다 진행된 경우 <code>sessions.ended_at</code>이 앞으로 갱신되며, 메시지 수 메타데이터는 매 실행마다 새로 고쳐집니다. 세션 ID 중복 제거와 <code>baseline_*</code> 보존은 토큰 합계를 안정적으로 유지합니다. 토큰, API 오류, 턴 지속 시간, 사고(thinking) 블록, 사용량 추가 정보, 서브에이전트별 세부 내역을 추출합니다.",
    "Express router for Import History. Four endpoints: <code>GET /api/import/guide</code> (OS-aware instructions + default-dir stats), <code>POST /api/import/rescan</code> (default <code>~/.claude/projects</code>), <code>POST /api/import/scan-path</code> (arbitrary absolute dir with <code>~</code> expansion), <code>POST /api/import/upload</code> (multer multipart). Each request uses a per-request temp dir reclaimed in <code>finally</code>. Progress broadcast as throttled <code>import.progress</code> WebSocket messages. Limits tunable via <code>CCAM_IMPORT_MAX_BYTES</code>, <code>CCAM_IMPORT_MAX_FILES</code>, <code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>":
      "임포트 히스토리를 위한 Express 라우터입니다. 네 가지 엔드포인트: <code>GET /api/import/guide</code>(OS 인식 지침 + 기본 디렉터리 통계), <code>POST /api/import/rescan</code>(기본값 <code>~/.claude/projects</code>), <code>POST /api/import/scan-path</code>(<code>~</code> 확장을 지원하는 임의의 절대 디렉터리), <code>POST /api/import/upload</code>(multer 멀티파트). 각 요청은 <code>finally</code>에서 회수되는 요청별 임시 디렉터리를 사용합니다. 진행 상황은 제한된 <code>import.progress</code> WebSocket 메시지로 브로드캐스트됩니다. 제한값은 <code>CCAM_IMPORT_MAX_BYTES</code>, <code>CCAM_IMPORT_MAX_FILES</code>, <code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>를 통해 조정할 수 있습니다.",
    "Safe archive extraction: <code>.zip</code> via <code>adm-zip</code>, <code>.tar</code>/<code>.tar.gz</code>/<code>.tgz</code> via <code>tar</code>, plain <code>.gz</code> streaming via <code>zlib</code>. Every entry validated through <code>safeJoin</code> which rejects absolute paths and <code>..</code> traversal before any bytes are written. Enforces a hard <code>MAX_EXTRACT_BYTES</code> cap (default 4 GB) with <code>ExtractionLimitError</code> surfaced as HTTP 413 — defense against zip/tar/gzip bombs":
      "안전한 아카이브 추출: <code>adm-zip</code>을 통한 <code>.zip</code>, <code>tar</code>를 통한 <code>.tar</code>/<code>.tar.gz</code>/<code>.tgz</code>, <code>zlib</code>을 통한 일반 <code>.gz</code> 스트리밍. 모든 항목은 바이트가 기록되기 전에 절대 경로와 <code>..</code> 순회를 거부하는 <code>safeJoin</code>을 통해 검증됩니다. 엄격한 <code>MAX_EXTRACT_BYTES</code> 상한(기본값 4GB)을 강제하며, 초과 시 <code>ExtractionLimitError</code>가 HTTP 413으로 표면화됩니다 — zip/tar/gzip 폭탄에 대한 방어책",
    "Loads deterministic demo data for testing and demos":
      "테스트 및 데모를 위한 결정론적 데모 데이터를 로드함",
    "Removes persisted rows while preserving schema": "스키마를 유지하면서 저장된 행을 제거함",
    "The Agent Monitor ships with an official Claude Code plugin marketplace containing ten production-ready plugins (53 skills, 14 agents, 30 slash commands, 3 CLI tools, 3 hook configs, and 1 MCP server). These extend Claude Code with skills, agents, hooks, CLI tools, and MCP integration — all grounded in the real data model (token tracking with compaction baselines, cost calculation via pattern-matched pricing rules, workflow intelligence with 11 datasets per session, and session metadata including thinking blocks, turn counts, and inference geography).":
      "Agent Monitor는 프로덕션 준비가 완료된 열 개의 플러그인(53개 스킬, 14개 에이전트, 30개 슬래시 명령, 3개 CLI 도구, 3개 훅 설정, 1개 MCP 서버)을 포함하는 공식 Claude Code 플러그인 마켓플레이스와 함께 제공됩니다. 이들은 스킬, 에이전트, 훅, CLI 도구, MCP 통합으로 Claude Code를 확장하며 — 모두 실제 데이터 모델(컴팩션 베이스라인을 포함한 토큰 추적, 패턴 매칭 가격 규칙을 통한 비용 계산, 세션당 11개 데이터셋을 갖춘 워크플로 인텔리전스, 사고 블록·턴 수·추론 지역을 포함한 세션 메타데이터)에 기반을 두고 있습니다.",
    Plugin: "플러그인",
    Skills: "스킬",
    Agent: "에이전트",
    "CLI Tools": "CLI 도구",
    Focus: "초점",
    "Token usage (4 types + baselines), cost via pricing engine, daily trends, productivity scoring":
      "토큰 사용량(4가지 유형 + 베이스라인), 가격 산정 엔진을 통한 비용 계산, 일별 트렌드, 생산성 점수",
    "Standup reports, sprint tracking, workflow optimization via 11 workflow intelligence datasets":
      "스탠드업 리포트, 스프린트 추적, 11개 워크플로 인텔리전스 데이터셋을 통한 워크플로 최적화",
    "Session debugging, hook diagnostics, data export (JSON/CSV), system health":
      "세션 디버깅, 훅 진단, 데이터 내보내기(JSON/CSV), 시스템 상태",
    "Pattern detection via tool flow transitions, anomaly alerting, optimization, session comparison":
      "도구 흐름 전이를 통한 패턴 탐지, 이상 징후 알림, 최적화, 세션 비교",
    "Budget guardrails: set budgets, forecast week/month-end spend, cost-threshold alerts, model-routing savings (fail-safe Stop hook)":
      "예산 가드레일: 예산 설정, 주간/월말 지출 예측, 비용 임계값 알림, 모델 라우팅을 통한 절감(장애 안전 Stop 훅)",
    "Session forensics: search, timeline, transcript replay, per-cwd rollup, cleanup":
      "세션 포렌식: 검색, 타임라인, 트랜스크립트 재생, cwd별 집계, 정리",
    "Multi-agent orchestration &amp; fleet intelligence: DAG map, delegation audit, concurrency, error propagation, fleet runs (11-dataset workflow intelligence API)":
      "멀티에이전트 오케스트레이션 &amp; 플릿 인텔리전스: DAG 맵, 위임 감사, 동시성, 오류 전파, 플릿 실행(11개 데이터셋 워크플로 인텔리전스 API)",
    "Reliability &amp; SLOs: error scan, API-error report, hook-failure audit, SLO check, regression alert":
      "안정성 &amp; SLO: 오류 스캔, API 오류 리포트, 훅 실패 감사, SLO 점검, 회귀 알림",
    "Claude Code config &amp; memory governance: config audit, memory review, skill/MCP/hook inventory (via the Config Explorer API)":
      "Claude Code 설정 &amp; 메모리 거버넌스: 설정 감사, 메모리 검토, 스킬/MCP/훅 인벤토리(Config Explorer API를 통해)",
    "Dashboard connector with MCP integration and one-line metric summaries":
      "MCP 통합과 한 줄 지표 요약을 갖춘 대시보드 커넥터",
    "Each plugin follows the official Claude Code plugin specification. The marketplace manifest at <code>.claude-plugin/marketplace.json</code> catalogs all ten plugins. Each plugin directory contains:":
      "각 플러그인은 공식 Claude Code 플러그인 사양을 따릅니다. <code>.claude-plugin/marketplace.json</code>의 마켓플레이스 매니페스트가 열 개 플러그인을 모두 카탈로그화합니다. 각 플러그인 디렉터리는 다음을 포함합니다.",
    "All plugins query the Agent Monitor API at <code>http://localhost:4820</code>. Key capabilities they leverage:":
      "모든 플러그인은 <code>http://localhost:4820</code>의 Agent Monitor API를 조회합니다. 이들이 활용하는 주요 기능은 다음과 같습니다.",
    Capability: "기능",
    Details: "세부 사항",
    "Token tracking": "토큰 추적",
    "4 types (input, output, cache_read, cache_write) + 4 compaction baselines per model per session":
      "4가지 유형(input, output, cache_read, cache_write) + 세션당 모델별 4개의 컴팩션 베이스라인",
    "Cost calculation": "비용 계산",
    "<code>(tokens / 1M) × rate_per_mtok</code> for each type; longest pattern match wins":
      "각 유형에 대해 <code>(tokens / 1M) × rate_per_mtok</code>; 가장 긴 패턴 매치가 우선함",
    "Session metadata": "세션 메타데이터",
    "Event types": "이벤트 유형",
    "Workflow intelligence": "워크플로 인텔리전스",
    "11 datasets: stats, orchestration (DAG), toolFlow, effectiveness, patterns, modelDelegation, errorPropagation, concurrency, complexity, compaction, cooccurrence":
      "11개 데이터셋: stats, orchestration(DAG), toolFlow, effectiveness, patterns, modelDelegation, errorPropagation, concurrency, complexity, compaction, cooccurrence",
    "Agent hierarchy": "에이전트 계층 구조",
    "Recursive parent/child tree with subagent_type, depth tracking via recursive CTE":
      "재귀 CTE를 통해 subagent_type과 깊이를 추적하는 재귀적 부모/자식 트리",
    '📖 Full documentation: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/docs/PLUGINS.md"><code>docs/plugins.md</code></a>':
      '📖 전체 문서: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/docs/PLUGINS.md"><code>docs/plugins.md</code></a>',
    '<span class="caption-icon">🖥️</span> Statusline — always-visible bar showing context window usage, token counts, active model, git branch, and session ID. Configurable segments with theme support':
      '<span class="caption-icon">🖥️</span> Statusline — 컨텍스트 윈도우 사용량, 토큰 수, 활성 모델, git 브랜치, 세션 ID를 표시하는 항상 표시되는 바. 테마를 지원하는 구성 가능한 세그먼트',
    "The <code>statusline/</code> directory contains a standalone CLI statusline for Claude Code — completely independent of the web dashboard. It renders a color-coded bar at the bottom of the Claude Code terminal showing context window usage, per-direction token counts, session cost in USD, and git branch.":
      "<code>statusline/</code> 디렉터리에는 Claude Code용 독립형 CLI statusline이 들어 있으며 — 웹 대시보드와는 완전히 별개입니다. 이는 컨텍스트 윈도우 사용량, 방향별 토큰 수, USD 기준 세션 비용, git 브랜치를 보여주는 색상 코드 바를 Claude Code 터미널 하단에 렌더링합니다.",
    Segment: "세그먼트",
    Source: "소스",
    "Color Logic": "색상 로직",
    "Always cyan": "항상 청록색(cyan)",
    "Always green": "항상 초록색",
    "Always yellow, <code>~</code> prefix for home":
      "항상 노란색, 홈 디렉터리는 <code>~</code> 접두사",
    "Always magenta, hidden outside git repos": "항상 마젠타색, git 저장소 밖에서는 숨김",
    "Green &lt; 50%, Yellow 50–79%, Red ≥ 80%": "초록 &lt; 50%, 노랑 50–79%, 빨강 ≥ 80%",
    "Green <code>↑</code> input, cyan <code>↓</code> output, dim <code>c</code> cache reads":
      "초록 <code>↑</code> 입력, 청록 <code>↓</code> 출력, 어두운 <code>c</code> 캐시 읽기",
    "Green &lt; $5, Yellow $5–$20, Red ≥ $20 (shown on API and subscription plans)":
      "초록 &lt; $5, 노랑 $5–$20, 빨강 ≥ $20 (API 및 구독 플랜에 표시)",
    "Add this to <code>~/.claude/settings.json</code>:":
      "다음을 <code>~/.claude/settings.json</code>에 추가하십시오.",
    "No dependencies required": "필요한 의존성 없음",
    "The statusline uses only Python 3.6+ stdlib (<code>sys</code>, <code>json</code>, <code>os</code>, <code>subprocess</code>). It fails silently on empty input or JSON errors and never blocks Claude Code.":
      "이 statusline은 Python 3.6+ 표준 라이브러리(<code>sys</code>, <code>json</code>, <code>os</code>, <code>subprocess</code>)만 사용합니다. 입력이 비어 있거나 JSON 오류가 발생해도 조용히 실패하며 Claude Code를 절대 차단하지 않습니다.",
    '<span class="caption-icon">🔌</span> Sidebar — live health, analytics, and deep navigation links':
      '<span class="caption-icon">🔌</span> 사이드바 — 실시간 상태, 분석, 심층 탐색 링크',
    "The <b>Claude Code Agent Monitor</b> is a premium, high-fidelity extension designed to minimize context switching for AI engineers. It brings the full power of the dashboard directly into VS Code, allowing you to monitor complex subagent orchestration without ever leaving your active code file.":
      "<b>Claude Code Agent Monitor</b>는 AI 엔지니어의 컨텍스트 전환을 최소화하도록 설계된 프리미엄 고정밀 확장 프로그램입니다. 대시보드의 모든 기능을 VS Code로 직접 가져와, 현재 작업 중인 코드 파일을 벗어나지 않고도 복잡한 서브에이전트 오케스트레이션을 모니터링할 수 있게 해줍니다.",
    "A dedicated Activity Bar view that performs background polling every 5 seconds. Includes a real-time <b>Agent Health</b> monitor tracking all 5 states (Working, Connected, Idle, Completed, Error) with native VS Code theme-aware icons and colors.":
      "5초마다 백그라운드 폴링을 수행하는 전용 Activity Bar 뷰입니다. VS Code 네이티브 테마를 인식하는 아이콘과 색상으로 5가지 상태(Working, Connected, Idle, Completed, Error)를 모두 추적하는 실시간 <b>Agent Health</b> 모니터를 포함합니다.",
    "Aggregates data from multiple API endpoints to display high-signal metrics directly in the sidebar: <ul style=\"margin-top: 8px; color: var(--text-muted); font-size: 13px; list-style-type: '→ '; padding-left: 15px;\"> <li><b>Token Consumption</b>: Scaled tracking from 1k to 1.0B+ tokens.</li> <li><b>Live Cost Estimates</b>: Automatic USD cost calculation based on model pricing rules.</li> <li><b>Event Frequency</b>: Total events, daily sessions, and subagent spawning rates.</li> </ul>":
      "여러 API 엔드포인트의 데이터를 취합하여 신호값이 높은 지표를 사이드바에 직접 표시합니다: <ul style=\"margin-top: 8px; color: var(--text-muted); font-size: 13px; list-style-type: '→ '; padding-left: 15px;\"> <li><b>토큰 소비량</b>: 1천 개부터 10억 개 이상의 토큰까지 확장 추적.</li> <li><b>실시간 비용 추정</b>: 모델 가격 규칙에 기반한 자동 USD 비용 계산.</li> <li><b>이벤트 빈도</b>: 총 이벤트 수, 일별 세션 수, 서브에이전트 생성 비율.</li> </ul>",
    "<b>Token Consumption</b>: Scaled tracking from 1k to 1.0B+ tokens.":
      "<b>토큰 소비량</b>: 1천 개부터 10억 개 이상의 토큰까지 확장 추적.",
    "<b>Live Cost Estimates</b>: Automatic USD cost calculation based on model pricing rules.":
      "<b>실시간 비용 추정</b>: 모델 가격 규칙을 기반으로 한 자동 USD 비용 계산입니다.",
    "<b>Event Frequency</b>: Total events, daily sessions, and subagent spawning rates.":
      "<b>이벤트 빈도</b>: 총 이벤트 수, 일별 세션 수, 서브에이전트 생성 비율입니다.",
    "Renders the full React application within a native webview tab. Supports <b>Deep Linking</b>: one-click jump from the sidebar directly to specific views like the <i>Kanban Board</i>, <i>Analytics Hub</i>, or your <i>Last 10 Sessions</i>.":
      "전체 React 애플리케이션을 네이티브 웹뷰 탭 안에 렌더링합니다. <b>딥 링크</b>를 지원하여 사이드바에서 <i>Kanban Board</i>, <i>Analytics Hub</i>, <i>Last 10 Sessions</i>와 같은 특정 화면으로 한 번의 클릭으로 바로 이동할 수 있습니다.",
    "Seamlessly scans ports <code>5173</code> (Vite Dev) and <code>4820</code> (Production) on localhost. Automatically toggles between <b>Online</b> and <b>Offline</b> modes in the sidebar as you start or stop your local server.":
      "localhost의 <code>5173</code>(Vite Dev) 포트와 <code>4820</code>(Production) 포트를 매끄럽게 스캔합니다. 로컬 서버를 시작하거나 중지하면 사이드바에서 <b>Online</b> 모드와 <b>Offline</b> 모드가 자동으로 전환됩니다.",
    "<strong>Zero-Config Setup</strong>": "<strong>제로 설정 구성</strong>",
    "The extension is designed to be plug-and-play. Once your server is running, the extension automatically discovers the API and begins streaming telemetry — no manual URL configuration required.":
      "이 확장 프로그램은 플러그 앤 플레이 방식으로 설계되었습니다. 서버가 실행되면 확장 프로그램이 API를 자동으로 감지하고 텔레메트리 스트리밍을 시작하므로, URL을 수동으로 설정할 필요가 없습니다.",
    '📖 Full developer guide: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/vscode-extension/README.md"><code>vscode-extension/README.md</code></a>':
      '📖 전체 개발자 가이드: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/vscode-extension/README.md"><code>vscode-extension/README.md</code></a>',
    "The dashboard ships as an optional <strong>native desktop application</strong> — a <code>desktop/</code> workspace that wraps the existing server and client into a macOS <code>.app</code> (distributed as a <code>.dmg</code>) and a Windows <code>.exe</code> (an NSIS installer plus a no-install portable build) you install once and forget. <code>desktop/</code> is a sibling workspace to <code>client/</code>, <code>server/</code>, <code>mcp/</code>, and <code>vscode-extension/</code>, built with <strong>Electron 35</strong>. It <strong>embeds the Express server in-process</strong> — it <code>require()</code>s <code>server/index.js</code> directly in the same Node runtime as the Electron main process (no child process, no IPC) — and renders the already-built React client in a <code>BrowserWindow</code>. Everything you see in the browser at <code>localhost:4820</code> lives inside this window, with native OS lifecycle on top.":
      "대시보드는 선택적 <strong>네이티브 데스크톱 애플리케이션</strong>으로도 제공됩니다 — 기존 서버와 클라이언트를 macOS <code>.app</code>(<code>.dmg</code>로 배포)과 Windows <code>.exe</code>(NSIS 설치 프로그램과 설치가 필요 없는 포터블 빌드)로 감싸는 <code>desktop/</code> 워크스페이스이며, 한 번 설치하면 신경 쓸 필요가 없습니다. <code>desktop/</code>은 <code>client/</code>, <code>server/</code>, <code>mcp/</code>, <code>vscode-extension/</code>과 나란히 위치한 워크스페이스이며, <strong>Electron 35</strong>로 빌드되었습니다. 이 앱은 <strong>Express 서버를 프로세스 내부에 내장</strong>합니다 — Electron 메인 프로세스와 동일한 Node 런타임에서 <code>server/index.js</code>를 직접 <code>require()</code>하며(자식 프로세스도, IPC도 없음), 이미 빌드된 React 클라이언트를 <code>BrowserWindow</code>에 렌더링합니다. 브라우저의 <code>localhost:4820</code>에서 보이는 모든 것이 이 창 안에 그대로 존재하며, 그 위에 네이티브 OS 라이프사이클이 얹혀 있습니다.",
    '<span class="caption-icon">🍎🪟</span> <span>The full dashboard, natively on macOS <strong>and</strong> Windows — same React client, same Express server, real <code>BrowserWindow</code>. Menu-bar / notification-area (tray) icon included. Shipped as a macOS DMG and a Windows EXE (macOS shown) — see <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a>.</span>':
      '<span class="caption-icon">🍎🪟</span> <span>macOS<strong>와</strong> Windows 양쪽에서 네이티브로 동작하는 전체 대시보드입니다 — 동일한 React 클라이언트, 동일한 Express 서버, 실제 <code>BrowserWindow</code>를 사용합니다. 메뉴 바 / 알림 영역(트레이) 아이콘이 포함되어 있습니다. macOS DMG와 Windows EXE로 제공됩니다(화면은 macOS 기준) — <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a>를 참고하세요.</span>',
    '<span class="caption-icon">🪟</span> <span>The same dashboard as a native <strong>Windows</strong> app — real <code>BrowserWindow</code> with the native Windows window menu, live Activity Feed, and the Tabby companion. A notification-area (system tray) icon sits beside the clock for quick access.</span>':
      '<span class="caption-icon">🪟</span> <span>동일한 대시보드를 네이티브 <strong>Windows</strong> 앱으로 구현한 모습입니다 — 네이티브 Windows 창 메뉴, 실시간 Activity Feed, Tabby 컴패니언을 갖춘 실제 <code>BrowserWindow</code>를 사용합니다. 시계 옆의 알림 영역(시스템 트레이) 아이콘으로 빠르게 접근할 수 있습니다.</span>',
    "<strong>One-line mental model</strong>": "<strong>한 줄로 요약한 개념 모델</strong>",
    "<em>Electron is a window onto the same code.</em> The desktop app does not reimplement the dashboard — it hosts the exact server and client the standalone deployment runs. The only change outside <code>desktop/</code> is a behavior-preserving refactor of <code>server/index.js</code>: its post-listen bootstrap was extracted into an exported <code>startBackgroundServices()</code> so the embedded server runs exactly what <code>node server/index.js</code> runs.":
      "<em>Electron은 동일한 코드를 비추는 창일 뿐입니다.</em> 데스크톱 앱은 대시보드를 다시 구현하지 않고, 독립 실행형 배포와 완전히 동일한 서버와 클라이언트를 그대로 호스팅합니다. <code>desktop/</code> 외부에서 이루어진 유일한 변경은 <code>server/index.js</code>에 대한 동작 보존 리팩터링뿐입니다 — listen 이후의 부트스트랩 로직을 <code>startBackgroundServices()</code>라는 이름으로 export하여, 내장 서버가 <code>node server/index.js</code>를 실행했을 때와 정확히 동일하게 동작하도록 했습니다.",
    "The Electron main process hosts the embedded server <em>and</em> manages the window, tray, and menus. The renderer is just Chromium loading <code>http://127.0.0.1:&lt;port&gt;</code> — the same origin a normal browser would use.":
      "Electron 메인 프로세스는 내장 서버를 호스팅하는 <em>동시에</em> 창, 트레이, 메뉴를 관리합니다. 렌더러는 <code>http://127.0.0.1:&lt;port&gt;</code>를 로드하는 단순한 Chromium일 뿐이며, 이는 일반 브라우저가 사용하는 것과 동일한 origin입니다.",
    "The desktop app embeds the Express server in-process — no child process, no IPC":
      "데스크톱 앱은 Express 서버를 프로세스 내부에 내장합니다 — 자식 프로세스도, IPC도 없습니다",
    "An always-on tray icon — the macOS menu bar (a tinted template glyph) or the Windows notification area (the colored <code>icon.ico</code>). A single click (left or right) opens a dropdown with a <strong>live status snapshot</strong> queried straight from SQLite at click time — server port, active sessions, working agents, events today — followed by <strong>Open Dashboard</strong>, <strong>Open in Browser</strong>, <strong>Restart Server</strong>, <strong>Show Logs</strong>, <strong>Open at Login</strong> (toggle), and <strong>Quit</strong>. The snapshot rows are clickable — they open the dashboard. The menu is rebuilt on each open so every value is current.":
      "항상 표시되는 트레이 아이콘입니다 — macOS 메뉴 바(색조가 적용된 템플릿 글리프)이거나 Windows 알림 영역(컬러 <code>icon.ico</code>)입니다. 한 번의 클릭(좌클릭 또는 우클릭)으로 드롭다운이 열리며, 클릭 시점에 SQLite에서 직접 조회한 <strong>실시간 상태 스냅샷</strong> — 서버 포트, 활성 세션, 작업 중인 에이전트, 오늘의 이벤트 수 — 이 표시되고, 이어서 <strong>Open Dashboard</strong>, <strong>Open in Browser</strong>, <strong>Restart Server</strong>, <strong>Show Logs</strong>, <strong>Open at Login</strong>(토글), <strong>Quit</strong>이 나타납니다. 스냅샷 항목은 클릭 가능하며 클릭하면 대시보드가 열립니다. 메뉴는 열 때마다 다시 생성되므로 모든 값이 항상 최신 상태입니다.",
    "A standard native application menu — <code>About</code>, <code>Open at Login</code>, <code>File</code>, <code>Edit</code>, <code>View</code>, <code>Window</code>, <code>Help</code> — with <code>⌘R</code> / <code>Ctrl+R</code> wired to <em>View ▸ reload</em>. External links open in the system browser, never inside Electron. The <code>File ▸ Open Dashboard</code> item (<code>⌘1</code>) is macOS-only; on Windows/Linux the window-attached menu can't reopen a hidden window, so reopen from the tray's <strong>Open Dashboard</strong>.":
      "표준 네이티브 애플리케이션 메뉴입니다 — <code>About</code>, <code>Open at Login</code>, <code>File</code>, <code>Edit</code>, <code>View</code>, <code>Window</code>, <code>Help</code>로 구성되며, <code>⌘R</code> / <code>Ctrl+R</code>은 <em>View ▸ reload</em>에 연결되어 있습니다. 외부 링크는 항상 시스템 브라우저에서 열리며, Electron 내부에서는 열리지 않습니다. <code>File ▸ Open Dashboard</code> 항목(<code>⌘1</code>)은 macOS 전용입니다. Windows/Linux에서는 창에 연결된 메뉴로 숨겨진 창을 다시 열 수 없으므로, 트레이의 <strong>Open Dashboard</strong>를 통해 다시 열어야 합니다.",
    "Flip <em>Open at Login</em> in the tray or app menu — both platforms go through Electron's first-party <code>app.*LoginItemSettings</code> API. On macOS it registers via the modern <code>SMAppService</code> API and appears under <strong>System Settings → General → Login Items</strong>; on Windows it writes a per-user <code>HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run</code> entry, visible in <strong>Task Manager → Startup</strong>. When the app is launched at login it starts tray-only, with no window jumping into view (on Windows the login launch is detected via a <code>--ccam-hidden</code> argument).":
      "트레이나 앱 메뉴에서 <em>Open at Login</em>을 전환하면 됩니다 — 두 플랫폼 모두 Electron의 자체 <code>app.*LoginItemSettings</code> API를 사용합니다. macOS에서는 최신 <code>SMAppService</code> API를 통해 등록되며 <strong>시스템 설정 → 일반 → 로그인 항목</strong>에 표시됩니다. Windows에서는 사용자별 <code>HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run</code> 항목을 기록하며, <strong>작업 관리자 → 시작 프로그램</strong>에서 확인할 수 있습니다. 로그인 시 앱이 실행되면 트레이 전용으로만 시작되고 창이 갑자기 나타나지 않습니다(Windows에서는 <code>--ccam-hidden</code> 인자를 통해 로그인 실행 여부를 감지합니다).",
    'Closing the window hides it — the embedded server keeps running, the tray icon stays, and the dock / taskbar icon stays too (a clickable "still alive" indicator). <strong>Quit</strong> (<code>⌘Q</code> / <code>Ctrl+Q</code>, app menu, or tray → Quit) pops a confirmation modal — press the Quit button or hit <code>⌘Q</code> / <code>Ctrl+Q</code> a second time to skip the prompt — and only then does the embedded server shut down, closing SQLite cleanly with a WAL checkpoint and removing this PID\'s entry from the discovery file.':
      '창을 닫아도 창이 숨겨질 뿐입니다 — 내장 서버는 계속 실행되고, 트레이 아이콘도 남아 있으며, 독 / 작업 표시줄 아이콘도 그대로 유지됩니다(클릭 가능한 "여전히 살아있음" 표시입니다). <strong>Quit</strong>(<code>⌘Q</code> / <code>Ctrl+Q</code>, 앱 메뉴, 또는 트레이 → Quit)을 실행하면 확인 모달이 나타나며 — Quit 버튼을 누르거나 <code>⌘Q</code> / <code>Ctrl+Q</code>를 한 번 더 눌러 확인창을 건너뛸 수 있습니다 — 이 확인이 이루어진 후에야 내장 서버가 종료되며, WAL 체크포인트로 SQLite를 안전하게 닫고 discovery 파일에서 해당 PID 항목을 제거합니다.',
    "Launch the desktop app and <code>npm run dev</code> at the same time and both stay real-time. Each server appends its <code>{port, pid, startedAt}</code> entry to <code>~/.claude/.agent-dashboard.json</code> on startup; the Claude Code hook handler reads that list and fan-outs every event to every live entry in parallel. Stale entries self-evict via a PID liveness check on read, so a crashed server can't misroute events to a dead port.":
      "데스크톱 앱과 <code>npm run dev</code>를 동시에 실행해도 둘 다 실시간으로 동작합니다. 각 서버는 시작 시 자신의 <code>{port, pid, startedAt}</code> 항목을 <code>~/.claude/.agent-dashboard.json</code>에 추가하며, Claude Code 훅 핸들러는 이 목록을 읽어 모든 이벤트를 살아있는 모든 항목에 병렬로 팬아웃합니다. 오래된 항목은 읽을 때 수행되는 PID 생존 확인을 통해 스스로 제거되므로, 크래시된 서버가 죽은 포트로 이벤트를 잘못 라우팅하는 일이 없습니다.",
    "Double-launching the app just focuses the existing window — no second server, no port collision, on every platform. The lock is acquired via <code>requestSingleInstanceLock()</code> before any server boots.":
      "앱을 두 번 실행해도 기존 창에 포커스만 맞춰질 뿐입니다 — 모든 플랫폼에서 두 번째 서버가 뜨지 않으며 포트 충돌도 발생하지 않습니다. 이 잠금은 어떤 서버도 부팅되기 전에 <code>requestSingleInstanceLock()</code>을 통해 획득됩니다.",
    "On its first owned-server boot the app auto-installs the Claude Code hooks into <code>~/.claude/settings.json</code> and starts the background services (update scheduler, config watcher, orphaned-run reconciliation) — so an install-only user (DMG or EXE) gets events flowing without ever running <code>npm run install-hooks</code> from a checkout.":
      "앱이 소유한 서버가 처음 부팅될 때, Claude Code 훅을 <code>~/.claude/settings.json</code>에 자동으로 설치하고 백그라운드 서비스(업데이트 스케줄러, 설정 워처, 고아 실행 정합화)를 시작합니다 — 그 결과 설치만 한 사용자(DMG 또는 EXE)도 체크아웃에서 <code>npm run install-hooks</code>를 실행하지 않고도 이벤트가 흐르기 시작합니다.",
    "Two packaging realities — a read-only application bundle / install directory and (on macOS) the minimal <code>PATH</code> a Finder-launched app inherits — are handled automatically so installs survive updates and the <strong>Run Claude</strong> feature works out of the box on both macOS and Windows.":
      "패키징에서 발생하는 두 가지 현실적인 문제 — 읽기 전용 애플리케이션 번들 / 설치 디렉터리, 그리고 (macOS의 경우) Finder로 실행한 앱이 상속받는 최소한의 <code>PATH</code> — 는 자동으로 처리되므로, 업데이트를 거쳐도 설치가 유지되고 <strong>Run Claude</strong> 기능이 macOS와 Windows 모두에서 별도 설정 없이 바로 동작합니다.",
    "<strong>Your data survives reinstalls and updates</strong>":
      "<strong>재설치와 업데이트에도 데이터가 유지됩니다</strong>",
    "The SQLite database and VAPID keys live in a per-user app-data directory <em>outside</em> the application bundle / install dir — <code>~/Library/Application Support/Claude Code Monitor/data/</code> on macOS, <code>%APPDATA%\\Claude Code Monitor\\data\\</code> on Windows. <code>server-host.ts</code> points <code>DASHBOARD_DATA_DIR</code> at that per-user directory on boot. Because a packaged, code-signed, or app-translocated bundle is read-only, older builds that stored the database inside the bundle broke History Import; with the data directory now in app-data, your imported history and events persist across app reinstalls and updates (the Windows NSIS uninstaller keeps this data by default). After upgrading from a pre-fix build, re-run <strong>Import History → Rescan</strong> once to bridge the one-time gap.":
      "SQLite 데이터베이스와 VAPID 키는 애플리케이션 번들 / 설치 디렉터리 <em>바깥</em>의 사용자별 앱 데이터 디렉터리에 저장됩니다 — macOS에서는 <code>~/Library/Application Support/Claude Code Monitor/data/</code>, Windows에서는 <code>%APPDATA%\\Claude Code Monitor\\data\\</code>입니다. <code>server-host.ts</code>는 부팅 시 <code>DASHBOARD_DATA_DIR</code>을 이 사용자별 디렉터리로 지정합니다. 패키징되거나 코드 서명되거나 앱 트랜스로케이션된 번들은 읽기 전용이기 때문에, 데이터베이스를 번들 내부에 저장했던 이전 빌드에서는 History Import가 제대로 동작하지 않았습니다. 이제 데이터 디렉터리가 앱 데이터 영역에 있으므로, 가져온 히스토리와 이벤트는 앱을 재설치하거나 업데이트해도 유지됩니다(Windows NSIS 제거 프로그램은 기본적으로 이 데이터를 보존합니다). 수정 이전 빌드에서 업그레이드한 경우, 한 번의 격차를 메우기 위해 <strong>Import History → Rescan</strong>을 한 번 다시 실행하세요.",
    "<strong>The <code>claude</code> CLI is found automatically</strong>":
      "<strong><code>claude</code> CLI가 자동으로 탐지됩니다</strong>",
    "A Finder- or Dock-launched macOS app inherits only launchd's minimal <code>PATH</code>, not your login shell's. At startup <code>shell-path.ts</code> recovers the user's login-shell <code>PATH</code> so the <strong>Run Claude</strong> feature can locate and spawn the <code>claude</code> CLI. (On Windows the process already inherits the user <code>PATH</code>, so no recovery step is needed.) If it still cannot be found, make sure <code>claude</code> is a real executable on your <code>PATH</code> — a shell alias or function cannot be spawned — and check the <code>user PATH resolved</code> line in the desktop log (<code>~/Library/Logs/Claude Code Monitor/desktop.log</code> on macOS, <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> on Windows).":
      "Finder나 Dock에서 실행한 macOS 앱은 로그인 셸의 PATH가 아니라 launchd의 최소한의 <code>PATH</code>만 상속받습니다. 시작 시 <code>shell-path.ts</code>가 사용자 로그인 셸의 <code>PATH</code>를 복원하여 <strong>Run Claude</strong> 기능이 <code>claude</code> CLI를 찾아 실행할 수 있도록 합니다. (Windows에서는 프로세스가 이미 사용자 <code>PATH</code>를 상속받으므로 별도의 복원 단계가 필요하지 않습니다.) 그래도 찾을 수 없다면, <code>claude</code>가 <code>PATH</code>상의 실제 실행 파일인지 확인하세요 — 셸 별칭(alias)이나 함수는 실행할 수 없습니다 — 그리고 데스크톱 로그(macOS의 <code>~/Library/Logs/Claude Code Monitor/desktop.log</code>, Windows의 <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code>)에서 <code>user PATH resolved</code> 줄을 확인하세요.",
    "On launch the Electron main process picks a free port. If a healthy dashboard server already answers <code>/api/health</code> on port <code>4820</code> (for example, you ran <code>npm start</code> in a terminal), the app <strong>adopts</strong> that server instead of starting a second one — no double-binding, no SQLite contention. An adopted server is not owned by the app, so quitting leaves it running.":
      "실행 시 Electron 메인 프로세스는 사용 가능한 포트를 선택합니다. 만약 정상적으로 동작하는 대시보드 서버가 이미 <code>4820</code> 포트에서 <code>/api/health</code>에 응답하고 있다면(예: 터미널에서 <code>npm start</code>를 실행한 경우), 앱은 두 번째 서버를 시작하는 대신 해당 서버를 <strong>채택(adopt)</strong>합니다 — 이중 바인딩도, SQLite 경합도 발생하지 않습니다. 채택된 서버는 앱이 소유한 것이 아니므로, 앱을 종료해도 계속 실행됩니다.",
    Step: "단계",
    "Port choice": "포트 선택",
    Adopt: "채택",
    "A healthy server already on <code>4820</code> is adopted as-is":
      "이미 <code>4820</code>에서 정상적으로 동작 중인 서버를 그대로 채택합니다",
    Preferred: "우선",
    "<code>4820</code> when free": "비어 있으면 <code>4820</code>",
    Fallback: "대체",
    "The first free port in <code>4821</code>–<code>4829</code>":
      "<code>4821</code>–<code>4829</code> 범위에서 처음으로 비어 있는 포트",
    "Last resort": "최후의 수단",
    "A random high port when all of the above are taken":
      "위 항목이 모두 사용 중일 때 임의의 높은 포트",
    "Three ways to obtain the desktop app — the latest GitHub Release (best for most users), a per-commit CI artifact (fresher than the latest release), or a local build.":
      "데스크톱 앱을 구하는 방법은 세 가지입니다 — 최신 GitHub Release(대부분의 사용자에게 최선), 커밋 단위 CI 아티팩트(최신 릴리스보다 더 신선함), 또는 로컬 빌드입니다.",
    'Open <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg> Releases → latest </a> and download the asset for your platform. The macOS and Windows Desktop CI jobs auto-publish a new <code>vX.Y.Z</code> release every time the version in <code>package.json</code> is bumped on <code>master</code>, so this link always points at the current build. Releases are public — no GitHub sign-in required.':
      '<a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg> 릴리스 → 최신 </a>을 열어 사용 중인 플랫폼에 맞는 애셋을 다운로드하세요. macOS와 Windows Desktop CI 작업은 <code>package.json</code>의 버전이 <code>master</code>에서 올라갈 때마다 새로운 <code>vX.Y.Z</code> 릴리스를 자동으로 게시하므로, 이 링크는 항상 현재 빌드를 가리킵니다. 릴리스는 공개되어 있으며 GitHub 로그인이 필요하지 않습니다.',
    Platform: "플랫폼",
    Asset: "애셋",
    "macOS (Apple Silicon)": "macOS (Apple Silicon)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-arm64.dmg</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-arm64.dmg</code>",
    "Drag the <code>.app</code> into <code>/Applications</code>":
      "<code>.app</code>을 <code>/Applications</code>로 드래그하세요",
    "macOS (Intel)": "macOS (Intel)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64.dmg</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64.dmg</code>",
    "Windows (installer)": "Windows (설치 프로그램)",
    "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>":
      "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>",
    "NSIS installer — per-user, no admin elevation":
      "NSIS 설치 프로그램 — 사용자별 설치, 관리자 권한 상승 불필요",
    "Windows (portable)": "Windows (포터블)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>",
    "Run without installing": "설치 없이 실행",
    'Want a build straight off the tip of <code>master</code>, ahead of the next tagged release? Every green run of the <code>🍎 macOS Desktop (DMG)</code> job on <code>macos-latest</code> uploads both per-arch DMGs as the <code>ClaudeCodeMonitor-dmg</code> workflow artifact, and the <code>🪟 Windows Desktop (EXE)</code> job on <code>windows-latest</code> uploads the installer + portable EXEs as the <code>ClaudeCodeMonitor-win</code> artifact. Open the <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg> latest passing run </a>, scroll to its Artifacts section, and download <code>ClaudeCodeMonitor-dmg</code> or <code>ClaudeCodeMonitor-win</code>. (GitHub sign-in required; 14-day retention.)':
      '다음 태그된 릴리스보다 앞선, <code>master</code> 최신 커밋을 그대로 반영한 빌드를 원하시나요? <code>macos-latest</code>에서 실행되는 <code>🍎 macOS Desktop (DMG)</code> 작업이 성공할 때마다 두 개의 아키텍처별 DMG를 <code>ClaudeCodeMonitor-dmg</code> 워크플로 아티팩트로 업로드하며, <code>windows-latest</code>에서 실행되는 <code>🪟 Windows Desktop (EXE)</code> 작업은 설치 프로그램과 포터블 EXE를 <code>ClaudeCodeMonitor-win</code> 아티팩트로 업로드합니다. <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg> 최신 성공 실행 </a>을 열어 Artifacts 섹션까지 스크롤한 다음 <code>ClaudeCodeMonitor-dmg</code> 또는 <code>ClaudeCodeMonitor-win</code>을 다운로드하세요. (GitHub 로그인 필요, 14일간 보관됩니다.)',
    "From the project root, after <code>git clone</code>:":
      "프로젝트 루트에서 <code>git clone</code> 이후:",
    "Use the arch-specific build on your own machine":
      "본인 컴퓨터에서는 아키텍처별 빌드를 사용하세요",
    "<code>desktop:dmg</code> is slower because it builds the full app tree <em>twice</em> (once per architecture) and ad-hoc-signs each — emitting two separate per-arch DMGs (<code>…-arm64.dmg</code> + <code>…-x64.dmg</code>). There is no <code>@electron/universal</code> merge; the release ships the two per-arch DMGs. For running on a single Mac, use <code>desktop:dmg:arm64</code> (Apple Silicon) or <code>desktop:dmg:x64</code> (Intel) — one architecture, finishing in roughly a minute. CI runs <code>desktop:dmg</code> and uploads both DMGs as <code>ClaudeCodeMonitor-dmg</code>, so you rarely need to build them yourself.":
      "<code>desktop:dmg</code>가 더 느린 이유는 전체 앱 트리를 <em>두 번</em>(아키텍처별로 한 번씩) 빌드하고 각각 애드혹(ad-hoc) 서명하기 때문입니다 — 각 아키텍처용 DMG 두 개(<code>…-arm64.dmg</code> + <code>…-x64.dmg</code>)를 생성합니다. <code>@electron/universal</code> 병합은 없으며, 릴리스에는 이 두 개의 아키텍처별 DMG가 포함됩니다. 단일 Mac에서 실행하려면 <code>desktop:dmg:arm64</code>(Apple Silicon) 또는 <code>desktop:dmg:x64</code>(Intel)를 사용하세요 — 아키텍처가 하나뿐이라 약 1분 만에 끝납니다. CI가 <code>desktop:dmg</code>를 실행해 두 DMG를 모두 <code>ClaudeCodeMonitor-dmg</code>로 업로드하므로, 직접 빌드해야 하는 경우는 거의 없습니다.",
    "Double-click the downloaded <code>.dmg</code> to mount it":
      "다운로드한 <code>.dmg</code>를 더블클릭하여 마운트하세요",
    "Drag <code>Claude Code Monitor.app</code> into your <code>Applications</code> folder":
      "<code>Claude Code Monitor.app</code>을 <code>Applications</code> 폴더로 드래그하세요",
    "Run <code>xattr -cr</code> on the app to get past Gatekeeper (see below)":
      "Gatekeeper를 통과하려면 앱에 <code>xattr -cr</code>을 실행하세요(아래 참고)",
    "Open the app — the tray icon appears and the dashboard window loads":
      "앱을 여세요 — 트레이 아이콘이 나타나고 대시보드 창이 로드됩니다",
    "Gatekeeper warning on first launch": "최초 실행 시 Gatekeeper 경고",
    'The DMG is ad-hoc signed by default — that is all the project can offer without a paid Apple Developer ID. macOS warns the first time you open it (<em>"Apple could not verify…"</em>). Strip the quarantine attribute to get past it:':
      'DMG는 기본적으로 애드혹 서명되어 있습니다 — 유료 Apple Developer ID 없이 프로젝트가 제공할 수 있는 것은 여기까지입니다. macOS는 처음 열 때 경고를 표시합니다(<em>"Apple에서 확인할 수 없습니다…"</em>). 이를 통과하려면 격리(quarantine) 속성을 제거하세요:',
    "Alternatively, open <strong>System Settings → Privacy &amp; Security</strong>, find the blocked app, and click <em>Open Anyway</em>. Code signing and Apple notarization are opt-in for the maintainer — when configured, this warning goes away for everyone.":
      "또는 <strong>시스템 설정 → 개인정보 보호 및 보안</strong>을 열어 차단된 앱을 찾은 다음 <em>확인 없이 열기</em>를 클릭하세요. 코드 서명과 Apple 공증은 유지관리자가 선택적으로 설정하는 항목입니다 — 설정이 완료되면 모든 사용자에게서 이 경고가 사라집니다.",
    "Run <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> — a per-user NSIS install (no admin), or run the <code>*-portable.exe</code> to skip installing":
      "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>를 실행하세요 — 사용자별 NSIS 설치(관리자 권한 불필요)이며, 설치를 건너뛰려면 <code>*-portable.exe</code>를 실행하세요",
    "The EXE is unsigned by default, so SmartScreen may warn — click <em>More info → Run anyway</em>":
      "EXE는 기본적으로 서명되지 않았으므로 SmartScreen이 경고를 표시할 수 있습니다 — <em>추가 정보 → 실행</em>을 클릭하세요",
    "Open from the Start menu / desktop shortcut — the notification-area (tray) icon appears and the dashboard window loads":
      "시작 메뉴 / 바탕화면 바로 가기에서 여세요 — 알림 영역(트레이) 아이콘이 나타나고 대시보드 창이 로드됩니다",
    '<span class="caption-icon">1️⃣</span> <span>NSIS installer, step 1 — <strong>Choose Installation Options</strong>: pick per-user setup and optional shortcuts.</span>':
      '<span class="caption-icon">1️⃣</span> <span>NSIS 설치 프로그램, 1단계 — <strong>설치 옵션 선택</strong>: 사용자별 설치와 선택적 바로 가기를 지정합니다.</span>',
    '<span class="caption-icon">2️⃣</span> <span>NSIS installer, step 2 — <strong>Choose Install Location</strong>: defaults to <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code>, or point it anywhere.</span>':
      '<span class="caption-icon">2️⃣</span> <span>NSIS 설치 프로그램, 2단계 — <strong>설치 위치 선택</strong>: 기본값은 <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code>이며, 원하는 위치로 변경할 수도 있습니다.</span>',
    '<span class="caption-icon">3️⃣</span> <span>NSIS installer, step 3 — <strong>Completing Setup</strong>: click <em>Finish</em> to launch the app and drop the tray icon in the notification area.</span>':
      '<span class="caption-icon">3️⃣</span> <span>NSIS 설치 프로그램, 3단계 — <strong>설치 완료</strong>: <em>마침</em>을 클릭하면 앱이 실행되고 알림 영역에 트레이 아이콘이 표시됩니다.</span>',
    "SmartScreen warning on first launch": "최초 실행 시 SmartScreen 경고",
    'The installer and portable EXE are <strong>unsigned</strong> by default — that is all the project can offer without a paid code-signing certificate. Windows <strong>SmartScreen</strong> may show <em>"Windows protected your PC"</em> the first time you run it; click <strong>More info → Run anyway</strong>. The installer lays the app down <strong>per-user</strong> under <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code> (and lets you choose the install directory) and sets an <code>AppUserModelId</code> (<code>com.hoangsonww.ccam.desktop</code>) so native toast notifications are attributed correctly and the window groups under one taskbar entry.':
      '설치 프로그램과 포터블 EXE는 기본적으로 <strong>서명되지 않았습니다</strong> — 유료 코드 서명 인증서 없이 프로젝트가 제공할 수 있는 것은 여기까지입니다. Windows <strong>SmartScreen</strong>이 처음 실행할 때 <em>"Windows에서 PC를 보호했습니다"</em>라는 메시지를 표시할 수 있습니다. 이때 <strong>추가 정보 → 실행</strong>을 클릭하세요. 설치 프로그램은 앱을 <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code> 아래에 <strong>사용자별</strong>로 배치하며(설치 디렉터리를 직접 선택할 수도 있습니다), <code>AppUserModelId</code>(<code>com.hoangsonww.ccam.desktop</code>)를 설정하여 네이티브 토스트 알림이 올바르게 귀속되고 창이 하나의 작업 표시줄 항목으로 그룹화되도록 합니다.',
    "Bundle size": "번들 크기",
    "The DMG is roughly 80&nbsp;MB, about 250&nbsp;MB installed on disk — the standard Electron tax; the Windows installer is comparable. The app runs natively on <strong>macOS and Windows</strong>; Linux is tracked as a follow-up. Logs live at <code>~/Library/Logs/Claude Code Monitor/desktop.log</code> on macOS or <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> on Windows (reach them from the tray menu → <em>Show Logs</em>).":
      "DMG 크기는 약 80&nbsp;MB이며, 디스크에 설치되면 약 250&nbsp;MB가 됩니다 — Electron을 사용할 때 흔히 발생하는 용량 부담이며, Windows 설치 프로그램도 비슷한 수준입니다. 앱은 <strong>macOS와 Windows</strong>에서 네이티브로 실행되며, Linux 지원은 후속 작업으로 추적 중입니다. 로그는 macOS의 경우 <code>~/Library/Logs/Claude Code Monitor/desktop.log</code>, Windows의 경우 <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code>에 저장됩니다(트레이 메뉴 → <em>Show Logs</em>에서 바로 접근할 수 있습니다).",
    '📖 User-facing guide: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a> · architecture &amp; contributor reference: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/desktop/README.md"><code>desktop/README.md</code></a>':
      '📖 사용자용 가이드: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a> · 아키텍처 및 기여자 참고 자료: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/desktop/README.md"><code>desktop/README.md</code></a>',
    '<span class="caption-icon">⚙️</span> Settings — model pricing editor, hook installation toggle, JSON data export, session cleanup, browser notification preferences, and system info panel with DB stats':
      '<span class="caption-icon">⚙️</span> Settings — 모델 가격 편집기, 훅 설치 토글, JSON 데이터 내보내기, 세션 정리, 브라우저 알림 환경설정, DB 통계가 포함된 시스템 정보 패널',
    "The <code>/settings</code> route provides a comprehensive management interface with six sections:":
      "<code>/settings</code> 경로는 여섯 개의 섹션으로 구성된 종합 관리 인터페이스를 제공합니다.",
    "Editable table of per-model pricing rules. Each Claude model variant has its own explicit pattern (e.g., <code>claude-opus-4-6%</code>). Rates cover input, output, cache read, and cache write tokens. Each rule's editor also has a collapsible <strong>Introductory rates</strong> block — a <code>YYYY-MM-DD</code> promo cutoff plus per-category intro prices (input / output / cache-read / cache-write 5m &amp; 1h); an empty date means no promo, and any future model-launch promo needs no code change. Reset to defaults or add custom models. The section header carries an info popover (the <code>i</code> icon) that explains how rule lookup works (first matching pattern wins), the SQL-style <code>%</code> wildcard syntax with concrete examples (<code>claude-opus-4-7%</code>, <code>claude-%-haiku</code>, exact ids), and reminds the user that prices must be updated manually when Anthropic publishes new rates — already-stored sessions keep the price applied at ingest time. The CLAUDE_HOME panel and Import History flow are fully i18n-driven across en/vi/zh.":
      "모델별 가격 규칙을 편집할 수 있는 표입니다. 각 Claude 모델 변형은 고유한 명시적 패턴을 가집니다(예: <code>claude-opus-4-6%</code>). 요율은 입력, 출력, 캐시 읽기, 캐시 쓰기 토큰을 포함합니다. 각 규칙의 편집기에는 접을 수 있는 <strong>도입 요율(Introductory rates)</strong> 블록도 있습니다 — <code>YYYY-MM-DD</code> 형식의 프로모션 종료일과 카테고리별 도입 가격(입력 / 출력 / 캐시 읽기 / 캐시 쓰기 5분 &amp; 1시간)으로 구성되며, 날짜가 비어 있으면 프로모션이 없다는 뜻이고, 향후 새 모델 출시 프로모션에도 코드 변경이 필요하지 않습니다. 기본값으로 재설정하거나 사용자 지정 모델을 추가할 수 있습니다. 섹션 헤더에는 정보 팝오버(<code>i</code> 아이콘)가 있어 규칙 조회 방식(처음으로 일치하는 패턴이 우선함), SQL 스타일 <code>%</code> 와일드카드 구문과 구체적인 예시(<code>claude-opus-4-7%</code>, <code>claude-%-haiku</code>, 정확한 id), 그리고 Anthropic이 새 요율을 공개하면 가격을 수동으로 업데이트해야 한다는 점(이미 저장된 세션은 수집 시점에 적용된 가격을 그대로 유지함)을 설명합니다. CLAUDE_HOME 패널과 Import History 흐름은 en/vi/zh 전반에 걸쳐 완전히 i18n으로 처리됩니다.",
    "Shows per-hook installation status (SessionStart, PreToolUse, PostToolUse, Stop, SubagentStop, Notification, SessionEnd). One-click reinstall if hooks are missing or outdated. Validates paths and permissions automatically.":
      "훅별 설치 상태(SessionStart, PreToolUse, PostToolUse, Stop, SubagentStop, Notification, SessionEnd)를 보여줍니다. 훅이 누락되었거나 오래된 경우 클릭 한 번으로 재설치할 수 있습니다. 경로와 권한을 자동으로 검증합니다.",
    "View database row counts and size. Session cleanup: abandon stale active sessions after N hours, purge old completed sessions after N days. Danger zone: clear all data with confirmation dialog to prevent accidental loss.":
      "데이터베이스의 행 수와 크기를 확인할 수 있습니다. 세션 정리: N시간이 지난 오래된 활성 세션을 종료 처리하고, N일이 지난 완료된 세션을 삭제합니다. 위험 구역: 실수로 인한 데이터 손실을 방지하기 위해 확인 대화상자와 함께 모든 데이터를 삭제할 수 있습니다.",
    "Download all sessions, agents, events, token usage, and pricing rules as a single JSON file for backup or analysis. Includes full event history, model metadata, and cost breakdowns in one portable archive.":
      "모든 세션, 에이전트, 이벤트, 토큰 사용량, 가격 규칙을 백업이나 분석을 위해 하나의 JSON 파일로 다운로드할 수 있습니다. 전체 이벤트 히스토리, 모델 메타데이터, 비용 내역을 하나의 이동 가능한 아카이브에 포함합니다.",
    "Dedicated Health tab on the Dashboard with a composite health score (weighted from success rate, cache hit rate, error rate, and heap usage), storage engine donut chart, tool invocation frequency bars, subagent effectiveness, model token distribution, and compaction impact — all with cursor-following tooltips and 5-second auto-refresh.":
      "대시보드에는 (성공률, 캐시 적중률, 오류율, 힙 사용량에 가중치를 부여한) 종합 상태 점수, 저장소 엔진 도넛 차트, 도구 호출 빈도 막대그래프, 서브에이전트 효과, 모델 토큰 분포, 압축(compaction) 영향을 보여 주는 전용 Health 탭이 있으며, 모두 커서를 따라다니는 툴팁과 5초 자동 새로고침을 지원합니다.",
    "Configure native browser notifications with per-event toggles for session starts, completions, errors, and subagent spawns. Automatic permission management with test-send button and graceful fallback when denied.":
      "세션 시작, 완료, 오류, 서브에이전트 생성에 대해 이벤트별로 토글할 수 있는 네이티브 브라우저 알림을 설정할 수 있습니다. 권한이 거부되었을 때도 우아하게 대체되며, 테스트 전송 버튼으로 권한을 자동 관리합니다.",
    "Per-model pricing — no catch-all grouping": "모델별 가격 책정 — 뭉뚱그린 그룹화 없음",
    "Each Claude model variant (e.g., Opus 4.6 vs Opus 4.1) has its own explicit pricing pattern because different model versions have different rates. The cost engine uses specificity sorting — longer patterns match before shorter ones.":
      "각 Claude 모델 변형(예: Opus 4.6과 Opus 4.1)은 모델 버전마다 요율이 다르기 때문에 각자 명시적인 가격 패턴을 가집니다. 비용 엔진은 구체성 기준 정렬을 사용하여, 더 긴 패턴이 더 짧은 패턴보다 먼저 일치합니다.",
    "Turns the dashboard from passive viewing into active monitoring. A rules-based alerting engine evaluates the live event stream <strong>server-side</strong>, and fired alerts fan out to outbound <strong>webhook channels</strong>. Everything lives in one place — <strong>Settings → Alerts</strong> — behind a segmented control with three tabs: <strong>Rules</strong> (what triggers an alert), <strong>Channels</strong> (where alerts are delivered), and <strong>Activity</strong> (the live fired-alert feed with acknowledge / acknowledge-all).":
      "대시보드를 수동적인 열람 도구에서 능동적인 모니터링 도구로 바꿔 줍니다. 규칙 기반 알림 엔진이 <strong>서버 측</strong>에서 실시간 이벤트 스트림을 평가하며, 발생한 알림은 외부 <strong>웹훅 채널</strong>로 팬아웃됩니다. 모든 기능은 <strong>Settings → Alerts</strong> 한 곳에 모여 있으며, 세 개의 탭으로 구성된 세그먼트 컨트롤 뒤에 배치됩니다: <strong>Rules</strong>(무엇이 알림을 발생시키는지), <strong>Channels</strong>(알림이 전달되는 곳), <strong>Activity</strong>(확인 / 모두 확인 기능이 있는 실시간 발생 알림 피드).",
    'Four condition types: <strong>event pattern</strong> (match <code>event_type</code> / <code>tool_name</code> / a summary substring, optionally requiring ≥ N matches within a rolling window — e.g. "5 errors in 2 minutes"), <strong>inactivity</strong> (an active session goes quiet for N minutes), <strong>status duration</strong> (an agent is stuck in <code>working</code> / <code>waiting</code> for N minutes), and <strong>token threshold</strong> (a session\'s cumulative tokens cross a limit). Each rule has a configurable <strong>cooldown</strong> that dedups repeat alerts per (rule, session, agent).':
      '네 가지 조건 유형이 있습니다: <strong>이벤트 패턴</strong>(<code>event_type</code> / <code>tool_name</code> / 요약 문자열 일부를 매칭하며, 필요 시 롤링 윈도우 내에서 N회 이상 일치하도록 설정 가능 — 예: "2분 내 오류 5회"), <strong>비활성</strong>(활성 세션이 N분 동안 조용해짐), <strong>상태 지속 시간</strong>(에이전트가 <code>working</code> / <code>waiting</code> 상태로 N분 동안 멈춰 있음), <strong>토큰 임계값</strong>(세션의 누적 토큰이 한도를 초과함)입니다. 각 규칙에는 (규칙, 세션, 에이전트) 조합별로 반복 알림을 중복 제거하는 설정 가능한 <strong>쿨다운</strong>이 있습니다.',
    "Event-driven rules (<code>event_pattern</code>, <code>token_threshold</code>) run on every hook ingest — <em>after</em> the transaction commits and the response is sent, fully try/catch-guarded, so alerting can never slow or fail hook delivery. Time-based rules (<code>inactivity</code>, <code>status_duration</code>) run on an unref'd 60-second sweep. Enabled rules are cached in memory and invalidated on every edit. Fired alerts persist to <code>alert_events</code> and broadcast an <code>alert_triggered</code> WebSocket message.":
      "이벤트 기반 규칙(<code>event_pattern</code>, <code>token_threshold</code>)은 모든 훅 수집 시마다 실행되며 — 트랜잭션이 커밋되고 응답이 전송된 <em>이후</em>에, 전체적으로 try/catch로 보호되어 실행되므로 알림 기능이 훅 전달을 절대 지연시키거나 실패하게 만들지 않습니다. 시간 기반 규칙(<code>inactivity</code>, <code>status_duration</code>)은 unref 처리된 60초 주기 스윕으로 실행됩니다. 활성화된 규칙은 메모리에 캐시되며 편집할 때마다 무효화됩니다. 발생한 알림은 <code>alert_events</code>에 영구 저장되고 <code>alert_triggered</code> WebSocket 메시지로 브로드캐스트됩니다.",
    "Slack, Discord, Microsoft Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, and Pipedream — plus a generic JSON endpoint. A declarative <strong>provider registry</strong> describes each one's payload formatter, URL resolution, auth headers, and credential fields, so adding a provider is a single server-side entry that surfaces in the UI with no front-end change.":
      "Slack, Discord, Microsoft Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream을 지원하며, 여기에 범용 JSON 엔드포인트도 추가되어 있습니다. 선언적 <strong>provider registry</strong>가 각 제공자의 페이로드 포맷터, URL 결정 방식, 인증 헤더, 자격 증명 필드를 기술하므로, 프로바이더를 추가하는 작업은 프런트엔드 변경 없이 UI에 자동으로 반영되는 서버 측 항목 하나만 추가하면 됩니다.",
    'Each delivery POSTs with an <code>AbortController</code> timeout and bounded retry/backoff (retries transport errors, 429, and 5xx — never other 4xx), then records the attempt-chain in <code>webhook_deliveries</code>. A provider can also veto a 2xx whose body signals failure (Splunk On-Call returns 200 with <code>result:"failure"</code>). Delivery is <strong>detached and fail-safe</strong> — it never throws into, slows, or blocks the alert path.':
      '각 전달은 <code>AbortController</code> 타임아웃과 제한된 재시도/백오프를 적용하여 POST 요청을 보내며(전송 오류, 429, 5xx는 재시도하지만 그 외 4xx는 재시도하지 않습니다), 시도 체인을 <code>webhook_deliveries</code>에 기록합니다. 프로바이더는 본문이 실패를 나타내는 2xx 응답도 거부(veto)할 수 있습니다(Splunk On-Call은 <code>result:"failure"</code>와 함께 200을 반환합니다). 전달은 <strong>분리되어 있고 페일세이프</strong>합니다 — 알림 경로에 예외를 던지거나 지연시키거나 차단하는 일이 결코 없습니다.',
    "Target URLs are masked (host + last 4 chars), and secrets / credential fields (routing keys, API keys, bot tokens) plus custom-header values are redacted in every API response — the full URL and secrets are stored server-side and never leave it. Generic endpoints support optional <strong>HMAC-SHA256</strong> body signing (<code>X-Webhook-Signature</code> + <code>X-Webhook-Timestamp</code>) so receivers can verify authenticity.":
      "대상 URL은 마스킹되어(호스트 + 마지막 4자) 표시되며, 시크릿 / 자격 증명 필드(라우팅 키, API 키, 봇 토큰)와 사용자 지정 헤더 값도 모든 API 응답에서 마스킹됩니다 — 전체 URL과 시크릿은 서버 측에만 저장되며 밖으로 나가지 않습니다. 범용 엔드포인트는 선택적으로 <strong>HMAC-SHA256</strong> 본문 서명(<code>X-Webhook-Signature</code> + <code>X-Webhook-Timestamp</code>)을 지원하여 수신자가 진위를 검증할 수 있습니다.",
    'Every alert-rule field has a help tooltip — the event-type, tool-name, and summary-contains fields include example chips of real hook events and built-in tool names. Each webhook provider ships a collapsible step-by-step setup guide linking to the official docs. A one-click <strong>"Send test"</strong> probe fires a synthetic alert and reports the delivery result inline, and targets can be scoped to specific rules. Fully localized (en / zh / vi / ko).':
      '모든 알림 규칙 필드에는 도움말 툴팁이 있습니다 — 이벤트 유형, 도구 이름, 요약 포함 필드에는 실제 훅 이벤트와 내장 도구 이름의 예시 칩이 함께 제공됩니다. 각 웹훅 프로바이더는 공식 문서로 연결되는 접을 수 있는 단계별 설정 가이드를 제공합니다. 클릭 한 번으로 실행하는 <strong>"테스트 전송"</strong> 기능은 가상의 알림을 발생시켜 전달 결과를 즉시 표시하며, 대상을 특정 규칙으로 범위를 제한할 수도 있습니다. en / zh / vi / ko로 완전히 현지화되어 있습니다.',
    "Provider(s)": "프로바이더",
    "Payload format": "페이로드 형식",
    "URL / credentials": "URL / 자격 증명",
    "Block Kit (header + section + context)": "Block Kit (header + section + context)",
    "Rich embed": "리치 임베드(Rich embed)",
    "Adaptive Card in a Workflows <code>message</code> envelope":
      "Workflows <code>message</code> 봉투에 담긴 Adaptive Card",
    "Power Automate Workflows URL": "Power Automate Workflows URL",
    "Text message (basic markdown)": "텍스트 메시지(기본 마크다운)",
    "Space webhook URL": "스페이스 웹훅 URL",
    "Slack-style legacy attachments": "Slack 스타일의 레거시 attachments",
    "Bot API <code>sendMessage</code> (HTML)": "Bot API <code>sendMessage</code> (HTML)",
    "Bot token + chat ID (URL derived)": "봇 토큰 + 채팅 ID (URL에서 파생)",
    "Events API v2 trigger (with <code>dedup_key</code>)":
      "Events API v2 트리거(<code>dedup_key</code> 포함)",
    "Routing key (URL prefilled)": "라우팅 키(URL 사전 입력됨)",
    "Alert API": "Alert API",
    "API key (GenieKey header) + region": "API 키(GenieKey 헤더) + 리전",
    "VictorOps REST": "VictorOps REST",
    "REST endpoint URL (key embedded)": "REST 엔드포인트 URL(키 내장)",
    "Stable <code>{ event, alert }</code> JSON envelope":
      "안정적인 <code>{ event, alert }</code> JSON 봉투",
    "Endpoint URL (+ optional HMAC &amp; headers)": "엔드포인트 URL(+ 선택적 HMAC 및 헤더)",
    "Additive &amp; non-blocking by design": "설계상 추가적이며 논블로킹입니다",
    "Two new tables — <code>webhook_targets</code> (config; survives Clear Data like alert rules) and <code>webhook_deliveries</code> (audit log) — with no changes to existing tables, response shapes, or WebSocket message types. Webhook dispatch is fire-and-forget off the alert path, so a slow or failing endpoint can never slow or break alert firing or hook ingestion.":
      "새로 추가된 두 개의 테이블 — <code>webhook_targets</code>(설정 정보로, 알림 규칙과 마찬가지로 Clear Data 이후에도 유지됨)와 <code>webhook_deliveries</code>(감사 로그) — 외에 기존 테이블, 응답 형식, WebSocket 메시지 유형에는 어떠한 변경도 없습니다. 웹훅 전달은 알림 경로에서 벗어난 fire-and-forget 방식이므로, 느리거나 실패하는 엔드포인트가 알림 발생이나 훅 수집을 지연시키거나 망가뜨리는 일은 결코 없습니다.",
    "Provider setup steps can drift": "프로바이더 설정 절차는 변경될 수 있습니다",
    "Microsoft retired classic Office 365 connectors in 2025, so Teams uses an Adaptive Card delivered via Power Automate <strong>Workflows</strong>. More broadly, provider setup UIs change often — the in-app guides say so and link to each provider's official docs. Always confirm against the source.":
      "Microsoft가 2025년에 기존 Office 365 커넥터를 폐지했기 때문에, Teams는 Power Automate <strong>Workflows</strong>를 통해 전달되는 Adaptive Card를 사용합니다. 더 넓게 보면, 프로바이더의 설정 UI는 자주 바뀝니다 — 앱 내 가이드에도 이 점이 명시되어 있으며 각 프로바이더의 공식 문서로 연결됩니다. 항상 원본 문서를 통해 확인하시기 바랍니다.",
    '<span class="caption-icon">⬆</span> Update Notifier — version comparison modal with one-click copy of the update command. No automatic self-restart; you stay in control of when upgrades happen':
      '<span class="caption-icon">⬆</span> Update Notifier — 업데이트 명령을 클릭 한 번으로 복사할 수 있는 버전 비교 모달입니다. 자동 재시작은 없으며, 업그레이드 시점은 항상 사용자가 직접 결정합니다',
    "A detection-only subsystem that tells the user when the dashboard's git checkout is behind the canonical default branch. <strong>Branch- and fork-aware:</strong> if an <code>upstream</code> remote is configured (the standard convention for forks), it takes priority over <code>origin</code>; the chosen remote's <code>master</code> / <code>main</code> / <code>HEAD</code> is the comparison ref. The printed command adapts to the user's situation — <code>git pull --ff-only</code> only when their branch actually tracks the canonical ref, otherwise <code>git fetch</code> (with a fast-forward merge in the fork case). The server <strong>never</strong> pulls or restarts itself — the user runs the command in a terminal — so the mechanism cannot break dev sessions, pm2/systemd/launchd/Docker supervision, or leave orphaned processes.":
      "대시보드의 git 체크아웃이 표준 기본 브랜치보다 뒤처져 있을 때 사용자에게 알려 주기만 하는 탐지 전용 서브시스템입니다. <strong>브랜치 및 포크를 인식합니다:</strong> <code>upstream</code> 원격 저장소가 설정되어 있으면(포크에서의 표준 관례) <code>origin</code>보다 우선시되며, 선택된 원격 저장소의 <code>master</code> / <code>main</code> / <code>HEAD</code>가 비교 기준(ref)이 됩니다. 표시되는 명령은 사용자의 상황에 맞춰 달라집니다 — 브랜치가 실제로 표준 ref를 추적하는 경우에만 <code>git pull --ff-only</code>를 사용하고, 그렇지 않으면 <code>git fetch</code>를 사용합니다(포크의 경우 fast-forward 병합 포함). 서버는 <strong>절대</strong> 스스로 pull하거나 재시작하지 않으며 — 사용자가 터미널에서 직접 명령을 실행합니다 — 따라서 이 메커니즘이 개발 세션이나 pm2/systemd/launchd/Docker 관리 체계를 깨뜨리거나 고아 프로세스를 남길 수 없습니다.",
    "The same status is available from any terminal: <code>ccam update-check</code> asks the server for the branch- and fork-aware comparison, prints the behind-by count and the copy-paste update command, and triggers the <code>update_status</code> WebSocket broadcast so any open dashboard tab refreshes at the same time.":
      "같은 상태를 어느 터미널에서나 확인할 수 있습니다: <code>ccam update-check</code>는 서버에 브랜치·포크를 인식하는 비교를 요청해 뒤처진 커밋 수와 복사-붙여넣기 업데이트 명령을 출력하고, <code>update_status</code> WebSocket 브로드캐스트를 트리거해 열려 있는 모든 대시보드 탭이 동시에 새로 고쳐지게 합니다.",
    "A shell-less <code>git fetch</code> with a 120-second timeout, followed by a <code>rev-list</code> against the tracked upstream. Each call runs from <code>server/lib/update-check.js</code> and returns a structured payload — never throws — so a flaky remote can&apos;t stall the dashboard.":
      "셸을 거치지 않는 <code>git fetch</code>를 120초 타임아웃으로 실행한 다음, 추적 중인 upstream을 대상으로 <code>rev-list</code>를 수행합니다. 각 호출은 <code>server/lib/update-check.js</code>에서 실행되며 구조화된 페이로드를 반환할 뿐 절대 예외를 던지지 않으므로, 불안정한 원격 저장소가 대시보드를 멈추게 할 수 없습니다.",
    "<code>update-scheduler.js</code> polls every five minutes with <code>.unref()</code> timers so it never blocks shutdown, de-duplicates with a fingerprint over the status payload, and announces up-to-date → behind transitions in a framed stdout block. Disable entirely with <code>DASHBOARD_UPDATE_CHECK=0</code>.":
      "<code>update-scheduler.js</code>는 종료를 절대 막지 않는 <code>.unref()</code> 타이머로 5분마다 폴링하며, 상태 페이로드에 대한 지문(fingerprint)으로 중복을 제거하고, 최신 → 뒤처짐으로의 전환을 테두리로 감싼 stdout 블록으로 알립니다. <code>DASHBOARD_UPDATE_CHECK=0</code>으로 완전히 비활성화할 수 있습니다.",
    "Each status payload carries a <code>manual_command</code> shaped for the user's actual situation: <code>git pull --ff-only</code> on a tracked canonical branch, <code>git fetch &amp;&amp; git merge --ff-only</code> for forks where local tracks the wrong remote, and a plain <code>git fetch</code> on a feature branch where pulling would update the wrong branch. Install / build steps are appended only when the working tree is actually being rewritten.":
      "각 상태 페이로드에는 사용자의 실제 상황에 맞춰진 <code>manual_command</code>가 포함됩니다: 추적 중인 표준 브랜치에서는 <code>git pull --ff-only</code>, 로컬이 잘못된 원격 저장소를 추적하고 있는 포크에서는 <code>git fetch &amp;&amp; git merge --ff-only</code>, pull이 잘못된 브랜치를 업데이트하게 되는 기능 브랜치에서는 단순한 <code>git fetch</code>가 사용됩니다. 설치 / 빌드 단계는 워킹 트리가 실제로 다시 작성될 때만 추가됩니다.",
    "A modal opens automatically when upstream is ahead; ESC or a backdrop click dismisses it. A persistent sidebar button stays in the footer — emerald when behind, amber when the last check errored — so users can always trigger a fresh check on demand.":
      "upstream이 앞서 있으면 모달이 자동으로 열리며, ESC 키나 배경 클릭으로 닫을 수 있습니다. 푸터에는 항상 표시되는 사이드바 버튼이 있어 — 뒤처져 있으면 에메랄드색, 마지막 확인에서 오류가 발생했으면 호박색으로 표시되며 — 사용자가 언제든 새로운 확인을 직접 실행할 수 있습니다.",
    "Non-git installs, no remotes configured, offline fetches, and unresolvable upstream refs all return tagged payloads instead of throwing. The sidebar badge turns amber on fetch errors and the modal stays suppressed until a successful check arrives — no spinners, no stuck state.":
      "git 설치가 아닌 경우, 원격 저장소가 설정되지 않은 경우, 오프라인 상태에서의 fetch, 해석할 수 없는 upstream ref는 모두 예외를 던지는 대신 태그가 지정된 페이로드를 반환합니다. 사이드바 배지는 fetch 오류 시 호박색으로 바뀌며, 성공적인 확인이 이루어지기 전까지 모달은 표시되지 않습니다 — 스피너도, 멈춘 상태도 없습니다.",
    "Dismissal is keyed by the upstream SHA in <code>localStorage</code>, so closing the modal silences it only for <em>that</em> commit — a newer upstream commit re-opens it automatically. Clicking the sidebar button is an explicit intent signal and clears the stored dismissal before firing a fresh check.":
      "닫기 상태는 <code>localStorage</code>에 upstream SHA를 키로 저장되므로, 모달을 닫아도 <em>해당</em> 커밋에 대해서만 잠잠해집니다 — 더 새로운 upstream 커밋이 생기면 자동으로 다시 열립니다. 사이드바 버튼을 클릭하는 것은 명시적인 의도 신호로 간주되어, 새로운 확인을 실행하기 전에 저장된 닫기 상태를 초기화합니다.",
    "Read-only check — runs <code>git fetch</code>, compares, returns the payload.":
      "읽기 전용 확인입니다 — <code>git fetch</code>를 실행하고 비교한 다음 페이로드를 반환합니다.",
    "Same check, and broadcasts <code>update_status</code> over WebSocket so every connected client re-syncs at once.":
      "동일한 확인을 수행하며, <code>update_status</code>를 WebSocket으로 브로드캐스트하여 연결된 모든 클라이언트가 즉시 다시 동기화되도록 합니다.",
    "<strong>Detection-only by design</strong>": "<strong>설계상 탐지 전용입니다</strong>",
    "There is no <code>POST /api/updates/apply</code> and no in-process restart helper. A process cannot reliably replace itself without an external supervisor, and <code>npm run dev</code>, <code>npm start</code>, pm2, systemd, launchd, and Docker each need different restart logic. Detection-only keeps the mechanism portable across every supervisor and OS, and leaves the dashboard's lifecycle owned by whatever started it. The user runs the printed command in their own shell.":
      "<code>POST /api/updates/apply</code>는 존재하지 않으며, 프로세스 내부의 재시작 도우미도 없습니다. 프로세스는 외부 감독자(supervisor) 없이는 스스로를 안정적으로 교체할 수 없으며, <code>npm run dev</code>, <code>npm start</code>, pm2, systemd, launchd, Docker는 각각 다른 재시작 로직을 필요로 합니다. 탐지 전용으로 설계함으로써 이 메커니즘은 모든 감독자와 OS에서 이식 가능하게 유지되며, 대시보드의 라이프사이클은 그것을 시작한 주체가 계속 소유하게 됩니다. 사용자는 표시된 명령을 자신의 셸에서 직접 실행합니다.",
    '<span class="caption-icon">◈</span> Connection Status — sidebar-launched details modal with WebSocket endpoint, connection uptime, 60-second throughput sparkline, top event-type breakdown, and recent activity list':
      '<span class="caption-icon">◈</span> Connection Status — WebSocket 엔드포인트, 연결 가동 시간, 60초 처리량 스파크라인, 상위 이벤트 유형 분석, 최근 활동 목록을 보여 주는 사이드바에서 실행되는 상세 정보 모달입니다',
    'The <strong>Live</strong> / <strong>Disconnected</strong> pill in the sidebar footer opens a small details panel about the dashboard\'s WebSocket transport. It surfaces the active <code>ws://</code> endpoint, how long the current socket has been up, total events received, the top event types as a horizontal bar chart, a 60-second throughput sparkline, and the most recent 8 events as an activity list. Cumulative stats (totals, type breakdown, recent list) persist across reloads via <code>localStorage</code> under <code>sidebar-connection-stats</code>; the rolling sparkline and "connected since" timer are intentionally ephemeral since they only make sense relative to "now". A <strong>Reset</strong> button clears everything on demand.':
      '사이드바 하단의 <strong>Live</strong> / <strong>Disconnected</strong> 알약형 버튼을 클릭하면 대시보드의 WebSocket 전송에 관한 작은 상세 패널이 열립니다. 이 패널에는 현재 활성화된 <code>ws://</code> 엔드포인트, 현재 소켓의 연결 유지 시간, 수신된 총 이벤트 수, 상위 이벤트 유형을 나타내는 가로 막대 차트, 60초 처리량 스파크라인, 그리고 가장 최근 8개 이벤트를 담은 활동 목록이 표시됩니다. 누적 통계(총계, 유형별 분류, 최근 목록)는 <code>localStorage</code>의 <code>sidebar-connection-stats</code> 키 아래에 저장되어 새로고침 후에도 유지되지만, 롤링 스파크라인과 "연결 이후" 타이머는 "현재 시점"을 기준으로만 의미가 있으므로 의도적으로 일시적인 상태로 유지됩니다. <strong>Reset</strong> 버튼을 누르면 필요할 때 언제든 모든 내용을 지울 수 있습니다.',
    "Implementation note: per-event state lives in <code>useRef</code> buffers on the sidebar so the WS firehose never re-renders the navigation tree — the modal does its own one-second tick to sample the refs while open. Writes are throttled (single-flight timer, 2 s window) and flushed on <code>pagehide</code> / <code>visibilitychange</code> so the latest events aren't lost to the throttle window. The modal itself is portalled to <code>document.body</code> so the sidebar's stacking context can't trap it.":
      "구현 참고 사항: 이벤트별 상태는 사이드바의 <code>useRef</code> 버퍼에 보관되므로 WS 이벤트 폭주가 내비게이션 트리를 다시 렌더링하는 일은 없습니다 — 모달이 열려 있는 동안에는 모달이 직접 1초 간격으로 해당 ref 값을 샘플링합니다. 쓰기 작업은 스로틀링되며(단일 실행 타이머, 2초 창), <code>pagehide</code> / <code>visibilitychange</code> 시점에 플러시되어 최신 이벤트가 스로틀링 구간 때문에 손실되지 않습니다. 모달 자체는 <code>document.body</code>로 포털링되어 있어 사이드바의 스태킹 컨텍스트에 갇히지 않습니다.",
    "The entire UI ships in <strong>four languages — English, 简体中文, Tiếng Việt, and 한국어</strong> — built on <code>i18next</code> + <code>react-i18next</code> with <code>i18next-browser-languagedetector</code>. Coverage is end-to-end: every page, chart tooltip, Settings flow, Workflow narrative, Config Explorer tab, Run page, and the Alerts rule-help tooltips + webhook setup guides are translated. Switch languages from the sidebar (EN / 中文 / VI / 한국어) — the choice persists in <code>localStorage</code>.":
      "전체 UI는 <strong>네 가지 언어 — English, 简体中文, Tiếng Việt, 한국어</strong> — 로 제공되며, <code>i18next</code> + <code>react-i18next</code>를 기반으로 <code>i18next-browser-languagedetector</code>와 함께 구축되었습니다. 번역 범위는 전 영역에 걸쳐 있습니다. 모든 페이지, 차트 툴팁, Settings 흐름, Workflow 내러티브, Config Explorer 탭, Run 페이지, 그리고 Alerts 규칙 도움말 툴팁과 webhook 설정 가이드까지 모두 번역되어 있습니다. 사이드바에서 언어를 전환할 수 있으며(EN / 中文 / VI / 한국어), 선택한 언어는 <code>localStorage</code>에 저장됩니다.",
    'Translations are split into per-area JSON namespaces (<code>common</code>, <code>nav</code>, <code>dashboard</code>, <code>sessions</code>, <code>analytics</code>, <code>workflows</code>, <code>settings</code>, <code>kanban</code>, <code>run</code>, <code>ccConfig</code>, <code>alerts</code>, <code>errors</code>, <code>updates</code>) under <code>client/src/i18n/locales/&lt;lng&gt;/</code>. Components load only the namespaces they need via <code>useTranslation("…")</code>.':
      '번역은 영역별 JSON 네임스페이스(<code>common</code>, <code>nav</code>, <code>dashboard</code>, <code>sessions</code>, <code>analytics</code>, <code>workflows</code>, <code>settings</code>, <code>kanban</code>, <code>run</code>, <code>ccConfig</code>, <code>alerts</code>, <code>errors</code>, <code>updates</code>)로 나뉘어 <code>client/src/i18n/locales/&lt;lng&gt;/</code> 아래에 위치합니다. 각 컴포넌트는 <code>useTranslation("…")</code>를 통해 필요한 네임스페이스만 불러옵니다.',
    "Language is detected from <code>localStorage</code> (<code>i18nextLng</code>) then the browser's <code>navigator</code> setting, and the choice is cached back to <code>localStorage</code>. <code>fallbackLng</code> is English and <code>nonExplicitSupportedLngs</code> resolves regional tags (e.g. <code>vi-VN</code> → <code>vi</code>), so any unmapped key falls back gracefully rather than rendering a raw key.":
      "언어는 먼저 <code>localStorage</code>(<code>i18nextLng</code>)에서 감지된 후 브라우저의 <code>navigator</code> 설정에서 감지되며, 선택한 값은 다시 <code>localStorage</code>에 캐시됩니다. <code>fallbackLng</code>는 영어이고, <code>nonExplicitSupportedLngs</code>는 지역 태그를 해석합니다(예: <code>vi-VN</code> → <code>vi</code>). 따라서 매핑되지 않은 키가 있어도 원시 키를 그대로 렌더링하지 않고 우아하게 폴백됩니다.",
    "Numbers, costs, dates, and relative times format against the active locale via a shared <code>getCurrentLocale()</code> helper, and plurals use i18next's <code>_one</code> / <code>_other</code> suffixes. Interpolated values (<code>{{count}}</code>, <code>{{provider}}</code>, …) keep sentences natural across languages.":
      "숫자, 비용, 날짜, 상대 시간은 공유된 <code>getCurrentLocale()</code> 헬퍼를 통해 현재 로케일에 맞춰 형식화되며, 복수형은 i18next의 <code>_one</code> / <code>_other</code> 접미사를 사용합니다. 보간된 값(<code>{{count}}</code>, <code>{{provider}}</code> 등)은 여러 언어에서도 문장이 자연스럽게 유지되도록 합니다.",
    "Domain terms that are proper nouns or code stay untranslated in every locale — <em>Agent</em>, <em>Subagent</em>, hook event names (<code>PostToolUse</code>), tool names (<code>Bash</code>), and webhook provider names (Slack, PagerDuty). Only the surrounding prose is localized, so instructions stay accurate.":
      "고유명사나 코드에 해당하는 도메인 용어는 모든 로케일에서 번역되지 않고 그대로 유지됩니다 — <em>Agent</em>, <em>Subagent</em>, hook 이벤트 이름(<code>PostToolUse</code>), 도구 이름(<code>Bash</code>), webhook 제공자 이름(Slack, PagerDuty) 등입니다. 주변의 설명 문구만 현지화되므로 안내 내용은 정확하게 유지됩니다.",
    "<strong>Adding a language</strong>": "<strong>언어 추가하기</strong>",
    "Copy <code>client/src/i18n/locales/en/</code> to a new locale folder, translate the JSON values (leaving keys and technical terms intact), then register the bundle and add the tag to <code>supportedLngs</code> in <code>client/src/i18n/index.ts</code>. Missing keys fall back to English automatically, so even a partial translation ships cleanly.":
      "<code>client/src/i18n/locales/en/</code>을 새 로케일 폴더로 복사하고, JSON 값을 번역합니다(키와 기술 용어는 그대로 유지). 그런 다음 번들을 등록하고 <code>client/src/i18n/index.ts</code>의 <code>supportedLngs</code>에 해당 태그를 추가합니다. 누락된 키는 자동으로 영어로 대체되므로, 부분적으로만 번역되어 있어도 문제없이 배포할 수 있습니다.",
    "<strong>Tabby</strong> is a cute SVG cat companion pinned to the <strong>edges of every page</strong> of the dashboard. It is always present and turns the live session stream into glanceable, ambient feedback — calm when idle, alert when something needs attention, and celebratory when a run finishes. Tabby is built entirely on the existing <code>eventBus</code> WebSocket stream: <strong>no new backend, no API key, and no new dependencies</strong>. The component lives in <code>client/src/components/Tabby/</code> and can be toggled on or off in Settings page.":
      "<strong>Tabby</strong>는 대시보드의 <strong>모든 페이지 가장자리</strong>에 고정되어 있는 귀여운 SVG 고양이 동반자입니다. Tabby는 항상 존재하며, 실시간 세션 스트림을 한눈에 파악할 수 있는 은은한 피드백으로 바꿔줍니다 — 유휴 상태일 때는 차분하고, 주의가 필요할 때는 경계하며, 실행이 완료되면 축하합니다. Tabby는 기존의 <code>eventBus</code> WebSocket 스트림 위에 완전히 구축되어 있습니다. <strong>새로운 백엔드도, API 키도, 새로운 의존성도 필요하지 않습니다.</strong> 이 컴포넌트는 <code>client/src/components/Tabby/</code>에 있으며 Settings 페이지에서 켜거나 끌 수 있습니다.",
    '<span class="caption-icon">📥</span> Tabby Companion — a cute SVG cat in the edges of every page, reacting in real time to the live session stream with eight distinct moods and animations, auto-surfacing speech bubbles for notable events, and serving as the gateway to a status panel and Ask box':
      '<span class="caption-icon">📥</span> Tabby Companion — 모든 페이지 가장자리에 있는 귀여운 SVG 고양이로, 여덟 가지 고유한 기분과 애니메이션으로 실시간 세션 스트림에 실시간으로 반응하고, 주목할 만한 이벤트에 대해 말풍선을 자동으로 표시하며, 상태 패널과 Ask 박스로 이어지는 관문 역할을 합니다',
    "Tabby derives one of eight moods from the live session WebSocket stream, each with its own animation. The eyes track your cursor, and the active mood drives a distinct motion cue.":
      "Tabby는 실시간 세션 WebSocket 스트림으로부터 여덟 가지 기분 중 하나를 도출하며, 각 기분마다 고유한 애니메이션을 가지고 있습니다. 눈은 커서를 따라 움직이며, 현재 활성화된 기분에 따라 고유한 동작 신호가 나타납니다.",
    "Notable events — session started or finished, errors, and run completed — automatically surface a speech bubble. Bubbles are <strong>throttled and coalesced</strong> so bursts of events never spam you, and they can be muted on demand. Everything reflects in real time over the existing <code>eventBus</code> WebSocket channel, with no polling and no extra services.":
      "세션 시작 또는 종료, 오류, 실행 완료와 같은 주목할 만한 이벤트가 발생하면 자동으로 말풍선이 표시됩니다. 말풍선은 <strong>스로틀링되고 병합되어</strong> 이벤트가 몰리더라도 스팸처럼 쏟아지지 않으며, 필요에 따라 음소거할 수도 있습니다. 모든 동작은 기존의 <code>eventBus</code> WebSocket 채널을 통해 실시간으로 반영되며, 폴링이나 추가 서비스는 전혀 필요하지 않습니다.",
    "Click the cat — or press <code>⌘B</code> / <code>Ctrl+B</code> — to open Tabby's panel (<code>Esc</code> closes it). The panel groups a live status line, quick actions, and an Ask box.":
      "고양이를 클릭하거나 <code>⌘B</code> / <code>Ctrl+B</code>를 눌러 Tabby의 패널을 엽니다(<code>Esc</code>로 닫습니다). 이 패널에는 실시간 상태 표시줄, 빠른 작업, Ask 박스가 함께 모여 있습니다.",
    "<strong>Live status line:</strong> <em>N live · M errored · connection state</em>, updated from cached data.":
      "<strong>실시간 상태 표시줄:</strong> <em>N live · M errored · connection state</em>로, 캐시된 데이터를 기반으로 갱신됩니다.",
    "<strong>Quick actions:</strong> jump to Run Claude, Activity, Sessions, or errored sessions; mute bubbles; clear alerts.":
      "<strong>빠른 작업:</strong> Run Claude, Activity, Sessions 또는 오류가 발생한 세션으로 바로 이동하기, 말풍선 음소거하기, 알림 지우기.",
    "<strong>Ask box:</strong> answers simple status questions locally from cached data (&ldquo;what's running&rdquo;, &ldquo;any errors&rdquo;, &ldquo;status&rdquo;).":
      "<strong>Ask 박스:</strong> 캐시된 데이터를 바탕으로 간단한 상태 질문에 로컬에서 답변합니다(&ldquo;what's running&rdquo;, &ldquo;any errors&rdquo;, &ldquo;status&rdquo; 등).",
    "The Ask box answers status questions instantly and offline from cached data. For anything beyond a simple status question, Tabby hands off to the existing <strong>Run Claude</strong> page (<code>/run?prompt=...</code>) to spawn a real Claude Code session — so there is never a separate model call, key, or service to manage.":
      "Ask 박스는 캐시된 데이터를 바탕으로 즉시, 오프라인 상태에서도 상태 질문에 답변합니다. 단순한 상태 질문을 넘어서는 요청의 경우, Tabby는 기존의 <strong>Run Claude</strong> 페이지(<code>/run?prompt=...</code>)로 넘겨 실제 Claude Code 세션을 실행시킵니다 — 따라서 별도로 관리해야 할 모델 호출, 키, 서비스는 결코 존재하지 않습니다.",
    "Fully keyboard operable: <code>⌘B</code> / <code>Ctrl+B</code> to open, <code>Esc</code> to close.":
      "완전한 키보드 조작이 가능합니다: 열려면 <code>⌘B</code> / <code>Ctrl+B</code>, 닫으려면 <code>Esc</code>.",
    "Status and bubbles announce via <code>aria-live</code> for screen readers.":
      "상태와 말풍선은 스크린 리더를 위해 <code>aria-live</code>를 통해 안내됩니다.",
    "Respects <code>prefers-reduced-motion</code> to calm animations.":
      "애니메이션을 줄이기 위해 <code>prefers-reduced-motion</code>을 준수합니다.",
    "Degrades gracefully to a calm, dimmed disconnected state when offline.":
      "오프라인 상태일 때는 차분하고 어두워진 연결 끊김 상태로 우아하게 저하됩니다.",
    Endpoint: "엔드포인트",
    Mood: "기분",
    "When it appears": "나타나는 시점",
    Animation: "애니메이션",
    Idle: "Idle",
    "Nothing notable happening": "특별히 주목할 만한 일이 없음",
    "Gentle tail flick": "부드러운 꼬리 흔들기",
    Watching: "Watching",
    "Sessions active, observing the stream": "세션이 활성 상태이며 스트림을 관찰 중",
    "Ear perk, cursor-tracking eyes": "귀를 쫑긋 세우고, 커서를 따라가는 눈",
    Happy: "Happy",
    "A run completed successfully": "실행이 성공적으로 완료됨",
    Sparkle: "반짝임",
    Worried: "Worried",
    "Something looks off": "무언가 이상해 보임",
    "Head bob": "고개 끄덕임",
    Stuck: "Stuck",
    "A session appears blocked": "세션이 막힌 것으로 보임",
    "Shake + alert <code>!</code>": "흔들림 + 경고 <code>!</code>",
    Thinking: "Thinking",
    "Work in progress": "작업 진행 중",
    Sleeping: "Sleeping",
    "Quiet for a while": "한동안 조용함",
    Zzz: "Zzz",
    Disconnected: "Disconnected",
    "WebSocket offline": "WebSocket 오프라인",
    "Calm, dimmed state": "차분하고 어두워진 상태",
    "Development vs production deployment topology": "개발 환경과 프로덕션 배포 구조 비교",
    Aspect: "항목",
    Development: "개발",
    Production: "프로덕션",
    Processes: "프로세스",
    "2 (Express + Vite)": "2개(Express + Vite)",
    "1 (Express only)": "1개(Express만)",
    "Client URL": "클라이언트 URL",
    "API proxy": "API 프록시",
    "Vite proxies <code>/api</code> + <code>/ws</code> to :4820":
      "Vite가 <code>/api</code> + <code>/ws</code>를 :4820으로 프록시",
    "Same origin, no proxy": "동일 출처, 프록시 없음",
    "File watching": "파일 감시",
    "<code>node --watch</code> + Vite HMR": "<code>node --watch</code> + Vite HMR",
    None: "없음",
    "Source maps": "소스 맵",
    Inline: "인라인",
    "External files": "외부 파일",
    "<strong>A third way to run: the Desktop App (macOS &amp; Windows)</strong>":
      "<strong>세 번째 실행 방법: 데스크톱 앱(macOS &amp; Windows)</strong>",
    'Beyond development and standalone production, the dashboard also ships as a native desktop app — a macOS <code>.app</code> and a Windows <code>.exe</code> — that embeds the same production server in-process, no terminal required. See the <a href="#desktop-app">Desktop App (macOS &amp; Windows)</a> section for download, build, and install instructions.':
      '개발 모드와 독립형 프로덕션 모드 외에도, 대시보드는 네이티브 데스크톱 앱 — macOS용 <code>.app</code>과 Windows용 <code>.exe</code> — 형태로도 제공되며, 동일한 프로덕션 서버를 프로세스 내에 내장하고 있어 터미널이 필요하지 않습니다. 다운로드, 빌드, 설치 방법은 <a href="#desktop-app">데스크톱 앱(macOS &amp; Windows)</a> 섹션을 참고하십시오.',
    "The production image is OCI-compatible and works with both Docker and Podman. The server listens on <code>4820</code>, reads legacy Claude history from a read-only mount, and persists SQLite data under <code>/app/data</code>.":
      "프로덕션 이미지는 OCI와 호환되며 Docker와 Podman 모두에서 동작합니다. 서버는 <code>4820</code> 포트에서 대기하고, 읽기 전용 마운트에서 레거시 Claude 기록을 읽으며, SQLite 데이터를 <code>/app/data</code> 아래에 영구 저장합니다.",
    "Container image build and runtime mounts": "컨테이너 이미지 빌드 및 런타임 마운트",
    Mount: "마운트",
    "Read historical Claude session files for import without modifying them":
      "과거 Claude 세션 파일을 수정하지 않고 가져오기 위해 읽음",
    "Persist the SQLite database across rebuilds and container restarts":
      "재빌드 및 컨테이너 재시작 전반에 걸쳐 SQLite 데이터베이스를 영구 저장",
    "<strong>Hooks still run on the host</strong>":
      "<strong>Hook은 여전히 호스트에서 실행됩니다</strong>",
    "Claude Code fires hooks from the host machine, not from inside the container. After the container is healthy on <code>http://localhost:4820</code>, run <code>npm run install-hooks</code> on the host so hook events post back to the containerized server.":
      "Claude Code는 컨테이너 내부가 아니라 호스트 머신에서 hook을 실행합니다. 컨테이너가 <code>http://localhost:4820</code>에서 정상적으로 동작하면, 호스트에서 <code>npm run install-hooks</code>를 실행하여 hook 이벤트가 컨테이너화된 서버로 다시 전달되도록 합니다.",
    "A multi-stage <code>Dockerfile</code> and <code>docker-compose.yml</code> are included. Both <strong>Docker</strong> and <strong>Podman</strong> are fully supported — the image is OCI-compliant.":
      "다단계 <code>Dockerfile</code>과 <code>docker-compose.yml</code>이 포함되어 있습니다. <strong>Docker</strong>와 <strong>Podman</strong> 모두 완전히 지원되며 — 이 이미지는 OCI를 준수합니다.",
    "Read-only access to legacy session history for automatic import on startup":
      "시작 시 자동 가져오기를 위한 레거시 세션 기록에 대한 읽기 전용 접근",
    "Persists the SQLite database across container restarts":
      "컨테이너 재시작 전반에 걸쳐 SQLite 데이터베이스를 영구 저장",
    "The Dockerfile uses three stages to minimize the final image size:":
      "Dockerfile은 최종 이미지 크기를 최소화하기 위해 세 단계를 사용합니다.",
    Stage: "단계",
    "Installs production <code>node_modules</code> on digest-pinned <code>node:24.19.0-alpine3.24</code>. <code>better-sqlite3</code> is optional — if prebuilds are unavailable, the server falls back to built-in <code>node:sqlite</code>":
      "digest가 고정된 <code>node:24.19.0-alpine3.24</code>에 프로덕션용 <code>node_modules</code>를 설치합니다. <code>better-sqlite3</code>는 선택 사항이며 사전 빌드가 없으면 서버가 내장 <code>node:sqlite</code>로 대체합니다",
    "Runs <code>npm ci</code> + <code>vite build</code> to produce optimized static assets":
      "<code>npm ci</code> + <code>vite build</code>를 실행하여 최적화된 정적 자산을 생성합니다",
    "Clean digest-pinned <code>node:24.19.0-alpine3.24</code> with only <code>node_modules</code>, server code, and <code>client/dist</code>":
      "<code>node_modules</code>, 서버 코드, <code>client/dist</code>만 포함한 깔끔한 digest 고정 <code>node:24.19.0-alpine3.24</code>",
    "<strong>Hook note</strong>": "<strong>Hook 참고 사항</strong>",
    "Claude Code hooks run on the host, not inside the container. The containerized server receives hook events via HTTP on <code>localhost:4820</code>. Run <code>npm run install-hooks</code> on the host after starting the container.":
      "Claude Code의 hook은 컨테이너 내부가 아니라 호스트에서 실행됩니다. 컨테이너화된 서버는 <code>localhost:4820</code>에서 HTTP를 통해 hook 이벤트를 수신합니다. 컨테이너를 시작한 후 호스트에서 <code>npm run install-hooks</code>를 실행하십시오.",
    Metric: "지표",
    "Server startup": "서버 시작",
    "SQLite opens instantly; schema migration is idempotent":
      "SQLite는 즉시 열리며, 스키마 마이그레이션은 멱등적입니다",
    "Hook latency": "Hook 지연 시간",
    "Transaction + broadcast, no async I/O beyond SQLite":
      "트랜잭션 + 브로드캐스트, SQLite 이외의 비동기 I/O 없음",
    "Client JS bundle": "클라이언트 JS 번들",
    "WebSocket latency": "WebSocket 지연 시간",
    "Local loopback, JSON serialization only": "로컬 루프백, JSON 직렬화만 수행",
    "SQLite write throughput": "SQLite 쓰기 처리량",
    "WAL mode on SSD; far exceeds any hook event rate":
      "SSD에서의 WAL 모드; 어떤 hook 이벤트 발생률도 훨씬 능가함",
    "Max events before slowdown": "성능 저하가 시작되는 최대 이벤트 수",
    "Pagination prevents full-table scans": "페이지네이션으로 전체 테이블 스캔을 방지",
    "Server memory": "서버 메모리",
    "SQLite in-process, no ORM overhead": "프로세스 내 SQLite, ORM 오버헤드 없음",
    "Client memory": "클라이언트 메모리",
    "React + Tailwind, minimal runtime deps": "React + Tailwind, 최소한의 런타임 의존성",
    "Input validation": "입력 검증",
    "Required fields checked before DB operations; CHECK constraints on status enums":
      "DB 작업 전에 필수 필드를 검사; 상태 열거형에 대한 CHECK 제약 조건 적용",
    "Hook safety": "Hook 안전성",
    "Hook handler always exits 0; 5s max lifetime; uses <code>127.0.0.1</code> not external hosts":
      "Hook 핸들러는 항상 종료 코드 0으로 종료됨; 최대 수명 5초; 외부 호스트가 아닌 <code>127.0.0.1</code> 사용",
    CORS: "CORS",
    "Restricted to loopback origins, so cross-origin pages can't read responses; no-Origin clients like curl still work":
      "루프백 출처로 제한되어 있어 교차 출처 페이지는 응답을 읽을 수 없습니다. curl처럼 Origin이 없는 클라이언트는 계속 동작합니다",
    Authentication: "인증",
    "Off by default since the loopback bind is the trust boundary; set <code>DASHBOARD_TOKEN</code> to require a bearer token on every <code>/api/*</code> request and the WebSocket when exposing on a LAN.":
      "루프백 바인딩 자체가 신뢰 경계이므로 기본적으로 비활성화되어 있습니다. LAN에 노출할 때는 <code>DASHBOARD_TOKEN</code>을 설정하여 모든 <code>/api/*</code> 요청과 WebSocket에 bearer 토큰을 요구하도록 하십시오.",
    Secrets: "비밀 정보",
    "No API keys, tokens, or credentials stored or transmitted anywhere":
      "API 키, 토큰, 자격 증명이 어디에도 저장되거나 전송되지 않음",
    "Dependency surface": "의존성 표면",
    "5 runtime server deps, 6 runtime client deps (includes D3.js for Workflows) — minimal attack surface":
      "런타임 서버 의존성 5개, 런타임 클라이언트 의존성 6개(Workflows용 D3.js 포함) — 최소한의 공격 표면",
    "Hooks only apply to sessions started <em>after</em> installation. Restart Claude Code after starting the dashboard.":
      "Hook은 설치 <em>이후</em>에 시작된 세션에만 적용됩니다. 대시보드를 시작한 뒤에는 Claude Code를 재시작하십시오.",
    "On some systems the shell environment when Claude Code fires hooks may not include the full PATH. Test with <code>node --version</code>. If not found, use the absolute path to <code>node</code> in the hook command.":
      "일부 시스템에서는 Claude Code가 hook을 실행할 때의 셸 환경에 전체 PATH가 포함되어 있지 않을 수 있습니다. <code>node --version</code>으로 테스트해 보십시오. 찾을 수 없다면 hook 명령에 <code>node</code>의 절대 경로를 사용하십시오.",
    Problem: "문제",
    Solution: "해결 방법",
    "<code>better-sqlite3</code> errors during install":
      "설치 중 <code>better-sqlite3</code> 오류 발생",
    "This is non-fatal — <code>better-sqlite3</code> is an optional dependency. On Node 22+ the server automatically falls back to built-in <code>node:sqlite</code>. On older Node versions, install Python 3 + C++ build tools, then run <code>npm rebuild better-sqlite3</code>. For the desktop app, the <code>desktop:install</code> preflight prints copy-pasteable per-OS setup guidance (incl. a no-toolchain alternative) when the native build fails.":
      "이는 치명적인 오류가 아닙니다 — <code>better-sqlite3</code>는 선택적 의존성입니다. Node 22 이상에서는 서버가 자동으로 내장된 <code>node:sqlite</code>로 대체됩니다. 이전 버전의 Node를 사용하는 경우 Python 3와 C++ 빌드 도구를 설치한 다음 <code>npm rebuild better-sqlite3</code>를 실행하십시오. 데스크톱 앱의 경우, 네이티브 빌드가 실패하면 <code>desktop:install</code> 사전 점검 과정에서 운영체제별로 그대로 복사해 붙여넣을 수 있는 설정 안내(툴체인이 필요 없는 대안 포함)를 출력합니다.",
    'Dashboard shows "Disconnected"': '대시보드에 "Disconnected"가 표시됨',
    "Server is not running. Start it with <code>npm run dev</code>. Client auto-reconnects every 2s.":
      "서버가 실행되고 있지 않습니다. <code>npm run dev</code>로 서버를 시작하십시오. 클라이언트는 2초마다 자동으로 재연결을 시도합니다.",
    "Events Today shows 0": "Events Today가 0으로 표시됨",
    "Ensure you are on the latest version (timezone bug was fixed). Restart the server.":
      "최신 버전을 사용하고 있는지 확인하십시오(시간대 버그가 수정되었습니다). 서버를 재시작하십시오.",
    "Port 4820 already in use": "포트 4820이 이미 사용 중",
    "Run <code>DASHBOARD_PORT=4821 npm run dev</code>, update Vite proxy in <code>client/vite.config.ts</code>, and re-run <code>npm run install-hooks</code>.":
      "<code>DASHBOARD_PORT=4821 npm run dev</code>를 실행하고, <code>client/vite.config.ts</code>의 Vite 프록시를 업데이트한 다음 <code>npm run install-hooks</code>를 다시 실행하십시오.",
    "Stale seed data shown": "오래된 시드 데이터가 표시됨",
    "Run <code>npm run clear-data</code> to wipe all rows, then restart.":
      "<code>npm run clear-data</code>를 실행하여 모든 행을 지운 다음 재시작하십시오.",
    "Hooks show validation error about matcher": "Hook에 matcher 관련 검증 오류가 표시됨",
    'Ensure you\'re on the latest version — the hook format was updated to use <code>matcher: "*"</code> string (not object).':
      '최신 버전을 사용하고 있는지 확인하세요 — 훅 형식이 <code>matcher: "*"</code> 문자열을 사용하도록 업데이트되었습니다(객체가 아님).',
    '"SQLite backend not available" on startup': '시작 시 "SQLite backend not available" 오류',
    "Neither <code>better-sqlite3</code> nor <code>node:sqlite</code> could load. Upgrade to Node.js 22+ (recommended), or install Python 3 + C++ build tools and run <code>npm rebuild better-sqlite3</code>.":
      "<code>better-sqlite3</code>와 <code>node:sqlite</code> 모두 로드할 수 없습니다. Node.js 22+로 업그레이드하거나(권장), Python 3와 C++ 빌드 도구를 설치한 후 <code>npm rebuild better-sqlite3</code>를 실행하세요.",
    "Docker container runs but no sessions appear":
      "Docker 컨테이너는 실행되지만 세션이 표시되지 않음",
    "Hooks run on the host, not inside the container. Run <code>npm run install-hooks</code> on the host after the container starts. Verify hooks in <code>~/.claude/settings.json</code> point to <code>localhost:4820</code>.":
      "훅은 컨테이너 내부가 아니라 호스트에서 실행됩니다. 컨테이너가 시작된 후 호스트에서 <code>npm run install-hooks</code>를 실행하세요. <code>~/.claude/settings.json</code>의 훅이 <code>localhost:4820</code>을 가리키는지 확인하세요.",
    Technology: "기술",
    "Why This Over Alternatives": "대안 대신 이것을 선택한 이유",
    "Zero-config, embedded, no server process. WAL mode gives concurrent reads. Synchronous API is simpler than async for this use case. <code>better-sqlite3</code> is preferred when prebuilds are available; falls back to Node.js built-in <code>node:sqlite</code> on Node 22+ when the native module cannot be compiled.":
      "설정이 필요 없고, 내장형이며, 별도의 서버 프로세스가 없습니다. WAL 모드는 동시 읽기를 지원합니다. 이 사용 사례에서는 동기 API가 비동기보다 더 단순합니다. 프리빌드가 제공되는 경우 <code>better-sqlite3</code>를 우선 사용하며, 네이티브 모듈을 컴파일할 수 없는 경우 Node 22+에 내장된 <code>node:sqlite</code>로 대체됩니다.",
    "Battle-tested, minimal, well-understood. Fastify would be overkill; raw <code>http</code> module would require too much boilerplate for routing.":
      "검증되었고, 최소한이며, 널리 이해되고 있습니다. Fastify는 과할 수 있고, 원시 <code>http</code> 모듈은 라우팅에 너무 많은 보일러플레이트 코드가 필요합니다.",
    "Fastest, most lightweight WebSocket library for Node. No Socket.IO overhead needed — we only push typed JSON messages one-way.":
      "Node에서 가장 빠르고 가벼운 WebSocket 라이브러리입니다. 타입이 지정된 JSON 메시지를 단방향으로 전송하기만 하므로 Socket.IO의 오버헤드가 필요하지 않습니다.",
    "Current supported client runtime with strong TypeScript support. CCAM remains a client-rendered SPA and does not enable Server Components or RSC.":
      "현재 지원되는 클라이언트 런타임이며 TypeScript 지원이 뛰어납니다. CCAM은 클라이언트 렌더링 SPA로 유지되며 Server Components나 RSC를 활성화하지 않습니다.",
    "Fast builds, native ESM, excellent dev experience. Proxy config handles the dev server split cleanly with no ejection.":
      "빌드 속도가 빠르고, 네이티브 ESM을 지원하며, 개발 경험이 뛰어납니다. 프록시 설정이 개발 서버 분리를 이젝트(eject) 없이 깔끔하게 처리합니다.",
    "Utility-first approach keeps styles colocated with markup. No CSS module boilerplate. Custom dark theme config for the dark UI.":
      "유틸리티 우선 접근 방식으로 스타일을 마크업과 함께 배치할 수 있습니다. CSS 모듈 보일러플레이트가 필요 없습니다. 다크 UI를 위한 커스텀 다크 테마 설정을 제공합니다.",
    "Standard declarative routing for React SPAs. Layout routes with <code>&lt;Outlet&gt;</code> give clean shell composition without enabling framework or RSC modes.":
      "React SPA를 위한 표준 선언형 라우팅입니다. <code>&lt;Outlet&gt;</code> 레이아웃 라우트는 framework 또는 RSC 모드를 활성화하지 않고 깔끔한 셸 구성을 제공합니다.",
    "Tree-shakeable icon library — only imports what's used (~20 icons). No heavy icon font.":
      "트리 셰이킹이 가능한 아이콘 라이브러리로, 사용되는 아이콘만 임포트합니다(약 20개). 무거운 아이콘 폰트가 필요 없습니다.",
    "Catches null/undefined bugs at compile time. <code>noUncheckedIndexedAccess</code> prevents array bounds issues in analytics aggregations.":
      "컴파일 타임에 null/undefined 버그를 잡아냅니다. <code>noUncheckedIndexedAccess</code>는 분석 집계에서 배열 범위 문제를 방지합니다.",
    "Industry-standard data visualization library. Powers the Workflows page's 11 interactive sections — DAG layouts, Sankey diagrams, force-directed graphs, bubble charts, and swim-lane timelines. No wrapper libraries needed; direct SVG rendering keeps bundle impact minimal.":
      "업계 표준 데이터 시각화 라이브러리입니다. Workflows 페이지의 11개 인터랙티브 섹션(DAG 레이아웃, Sankey 다이어그램, force-directed 그래프, 버블 차트, 스윔레인 타임라인)을 구동합니다. 래퍼 라이브러리가 필요 없으며, 직접 SVG 렌더링으로 번들 크기에 미치는 영향을 최소화합니다.",
    "Available on virtually all systems. Handles ANSI and JSON natively with stdlib only. No install step required.":
      "거의 모든 시스템에서 사용할 수 있습니다. stdlib만으로 ANSI와 JSON을 네이티브하게 처리합니다. 별도의 설치 단계가 필요하지 않습니다.",
    "Local-first monitoring for Claude Code sessions, agents, and tool events. Built for real-time visibility with zero external dependencies.":
      "Claude Code 세션, 에이전트, 도구 이벤트를 위한 로컬 우선 모니터링입니다. 외부 의존성 없이 실시간 가시성을 제공하도록 구축되었습니다.",
    Install: "설치",
    Setup: "설정",
    "About the Creator": "제작자 소개",
    '<span class="caption-icon">⭐</span> <span> Enjoying the project? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer">Give it a star on GitHub</a> and help more builders discover it. </span>':
      '<span class="caption-icon">⭐</span> <span> 이 프로젝트가 마음에 드시나요? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer">GitHub에서 별을 눌러주시면</a> 더 많은 개발자가 이 프로젝트를 발견하는 데 도움이 됩니다. </span>',
    'Clears the waiting flag and promotes the main agent to <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>. The only reliable signal that text-only assistant turns have started — they emit no <code>PreToolUse</code> before <code>Stop</code>.':
      '대기 플래그를 해제하고 메인 에이전트를 <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span> 상태로 전환합니다. 텍스트 전용 어시스턴트 턴이 시작되었음을 알리는 유일하게 신뢰할 수 있는 신호입니다 — 이러한 턴은 <code>Stop</code> 이전에 <code>PreToolUse</code>를 전혀 발생시키지 않습니다.',
    'Clears the waiting flag, sets agent → <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>, <code>current_tool</code> set. If tool is <code>Agent</code>, subagent record created.':
      '대기 플래그를 해제하고, 에이전트를 <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span> 상태로 설정하며, <code>current_tool</code>을 설정합니다. 도구가 <code>Agent</code>인 경우 서브에이전트 레코드가 생성됩니다.',
    'Clears the waiting flag (covers permission-prompt approvals mid-tool). <code>current_tool</code> cleared. Agent stays <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>.':
      '대기 플래그를 해제합니다(도구 실행 중 권한 프롬프트 승인 케이스 포함). <code>current_tool</code>이 지워집니다. 에이전트는 <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span> 상태를 유지합니다.',
    'Non-error: main agent → <code>waiting</code> — UI shows <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> until the next user input. <code>stop_reason=error</code>: marks the agent and session <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>. Background subagents keep running.':
      '오류가 아닌 경우: 메인 에이전트가 <code>waiting</code> 상태가 되며, UI는 다음 사용자 입력이 있을 때까지 <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span>을 표시합니다. <code>stop_reason=error</code>인 경우: 에이전트와 세션을 <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>로 표시합니다. 백그라운드 서브에이전트는 계속 실행됩니다.',
    'Matched subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session\'s <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent\'s own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks.':
      '일치하는 서브에이전트가 <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span> 상태가 됩니다. 대기 플래그는 의도적으로 <strong>해제하지 않습니다</strong> — 백그라운드 서브에이전트의 완료는 사용자(human)에 대해 아무것도 알려주지 않기 때문입니다. 또한 세션의 <code>subagents/agent-*.jsonl</code> 파일을 순회하며 <code>tool_use_id</code>로 <code>tool_use</code> ↔ <code>tool_result</code> 블록을 짝짓고, 각 서브에이전트 고유의 <code>agent_id</code> 아래에 도구별 <code>PreToolUse</code> + <code>PostToolUse</code> 이벤트를 발생시키는 fire-and-forget 방식의 JSONL 스캔(<code>scanAndImportSubagents</code>)도 시작합니다 — 서브에이전트가 내부적으로 수행하며 훅을 전혀 발생시키지 않는 도구 호출을 드러냅니다.',
    'Creates a compaction subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Detected via <code>isCompactSummary</code> entries in the transcript. Token baselines preserve pre-compaction totals. Periodic scanner (cadence ~¼ of <code>DASHBOARD_STALE_MINUTES</code>) catches compactions when no hooks fire.':
      '압축(compaction) 서브에이전트를 생성하여 <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span> 상태로 표시합니다. 트랜스크립트의 <code>isCompactSummary</code> 항목을 통해 감지됩니다. 토큰 기준값은 압축 이전의 합계를 보존합니다. 주기적 스캐너(주기는 <code>DASHBOARD_STALE_MINUTES</code>의 약 1/4)가 훅이 발생하지 않는 압축을 포착합니다.',
    'Drops the waiting flag. If the session is already in <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>, the error state is preserved; otherwise marks all agents and the session as <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Evicts the session\'s transcript from the shared cache.':
      '대기 플래그를 해제합니다. 세션이 이미 <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span> 상태라면 오류 상태를 유지하고, 그렇지 않으면 모든 에이전트와 세션을 <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>로 표시합니다. 공유 캐시에서 세션의 트랜스크립트를 제거합니다.',
    "SQLite connection, WAL/FK pragmas, schema migrations (<code>CREATE TABLE IF NOT EXISTS</code>), all prepared statements as a reusable <code>stmts</code> object. Tries <code>better-sqlite3</code> first, falls back to built-in <code>node:sqlite</code> via <code>compat-sqlite.js</code>":
      "SQLite 연결, WAL/FK 프래그마, 스키마 마이그레이션(<code>CREATE TABLE IF NOT EXISTS</code>), 재사용 가능한 <code>stmts</code> 객체로 관리되는 모든 프리페어드 스테이트먼트를 담당합니다. <code>better-sqlite3</code>를 먼저 시도하고, <code>compat-sqlite.js</code>를 통해 내장 <code>node:sqlite</code>로 대체됩니다.",
    "Each page pulls initial data from REST then subscribes to eventBus for live updates":
      "각 페이지는 REST에서 초기 데이터를 가져온 후 실시간 업데이트를 위해 eventBus를 구독합니다",
    "Entity Relationship Diagram — SQLite schema": "엔터티 관계 다이어그램 — SQLite 스키마",
    "Working Dir": "작업 디렉터리",
    "Git Branch": "Git 브랜치",
    "Context Bar": "컨텍스트 바",
    "Token Counts": "토큰 수",
    "Session Cost": "세션 비용",
    "Statusline rendering pipeline — invoked on each Claude Code update":
      "상태줄(Statusline) 렌더링 파이프라인 — Claude Code가 업데이트될 때마다 호출됨",
    "Aggregates data from multiple API endpoints to display high-signal metrics directly in the sidebar:":
      "여러 API 엔드포인트의 데이터를 집계하여 사이드바에 핵심 지표를 직접 표시합니다:",
    "Zero-Config Setup": "제로 설정(Zero-Config)",
    "One-line mental model": "한 줄로 정리하는 개념 모델",
    "Your data survives reinstalls and updates": "데이터는 재설치 및 업데이트 후에도 유지됩니다",
    "The <code>claude</code> CLI is found automatically":
      "<code>claude</code> CLI가 자동으로 감지됩니다",
    'Open <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> Releases → latest </a> and download the asset for your platform. The macOS and Windows Desktop CI jobs auto-publish a new <code>vX.Y.Z</code> release every time the version in <code>package.json</code> is bumped on <code>master</code>, so this link always points at the current build. Releases are public — no GitHub sign-in required.':
      '<a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> Releases → 최신 버전 </a> 링크를 열어 사용 중인 플랫폼에 맞는 에셋을 다운로드하세요. macOS 및 Windows Desktop CI 작업은 <code>master</code> 브랜치에서 <code>package.json</code>의 버전이 올라갈 때마다 새로운 <code>vX.Y.Z</code> 릴리스를 자동으로 게시하므로, 이 링크는 항상 최신 빌드를 가리킵니다. 릴리스는 공개되어 있으므로 GitHub 로그인이 필요하지 않습니다.',
    'Want a build straight off the tip of <code>master</code>, ahead of the next tagged release? Every green run of the <code>🍎 macOS Desktop (DMG)</code> job on <code>macos-latest</code> uploads both per-arch DMGs as the <code>ClaudeCodeMonitor-dmg</code> workflow artifact, and the <code>🪟 Windows Desktop (EXE)</code> job on <code>windows-latest</code> uploads the installer + portable EXEs as the <code>ClaudeCodeMonitor-win</code> artifact. Open the <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><polyline points="12 7 12 12 15 14"></polyline></svg> latest passing run </a>, scroll to its Artifacts section, and download <code>ClaudeCodeMonitor-dmg</code> or <code>ClaudeCodeMonitor-win</code>. (GitHub sign-in required; 14-day retention.)':
      '다음 태그 릴리스보다 앞선, <code>master</code>의 최신 커밋에서 곧바로 빌드한 버전을 원하시나요? <code>macos-latest</code>에서 실행되는 <code>🍎 macOS Desktop (DMG)</code> 작업이 성공할 때마다 두 개의 아키텍처별 DMG가 <code>ClaudeCodeMonitor-dmg</code> 워크플로 아티팩트로 업로드되고, <code>windows-latest</code>에서 실행되는 <code>🪟 Windows Desktop (EXE)</code> 작업은 인스톨러와 포터블 EXE를 <code>ClaudeCodeMonitor-win</code> 아티팩트로 업로드합니다. <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><polyline points="12 7 12 12 15 14"></polyline></svg> 최근 성공한 실행 </a> 페이지를 열어 Artifacts 섹션까지 스크롤한 다음 <code>ClaudeCodeMonitor-dmg</code> 또는 <code>ClaudeCodeMonitor-win</code>을 다운로드하세요. (GitHub 로그인 필요, 14일간 보관.)',
    "Incoming Webhook URL": "수신 웹훅 URL",
    "Webhook URL": "웹훅 URL",
    "Detection pipeline from scheduler to UI": "스케줄러에서 UI까지 이어지는 감지 파이프라인",
    "A shell-less <code>git fetch</code> with a 120-second timeout, followed by a <code>rev-list</code> against the tracked upstream. Each call runs from <code>server/lib/update-check.js</code> and returns a structured payload — never throws — so a flaky remote can't stall the dashboard.":
      "셸을 거치지 않는(shell-less) <code>git fetch</code>를 120초 타임아웃으로 실행한 뒤, 추적 중인 업스트림을 대상으로 <code>rev-list</code>를 수행합니다. 각 호출은 <code>server/lib/update-check.js</code>에서 실행되며 구조화된 페이로드를 반환합니다 — 절대 예외를 던지지 않으므로 원격 저장소가 불안정하더라도 대시보드가 멈추지 않습니다.",
    "Detection-only by design": "설계상 감지 전용",
    "Adding a language": "언어 추가하기",
    "<strong>Ask box:</strong> answers simple status questions locally from cached data (“what's running”, “any errors”, “status”).":
      "<strong>Ask 박스:</strong> 캐시된 데이터를 기반으로 로컬에서 간단한 상태 질문에 답합니다(“무엇이 실행 중인가요”, “오류가 있나요”, “상태”).",
    "A third way to run: the Desktop App (macOS &amp; Windows)":
      "실행하는 세 번째 방법: 데스크톱 앱(macOS 및 Windows)",
    "Hooks still run on the host": "훅은 여전히 호스트에서 실행됩니다",
    "Hook note": "훅 참고 사항",
    "SQL injection": "SQL 인젝션",
    "All queries use prepared statements with parameterized values — no string interpolation":
      "모든 쿼리는 매개변수화된 값을 사용하는 프리페어드 스테이트먼트를 사용합니다 — 문자열 보간을 사용하지 않습니다",
    "Request size": "요청 크기",
    "Express JSON body parser limited to 1MB": "Express JSON 본문 파서는 1MB로 제한됩니다",
    "Dashboard — stats, active agents, recent events":
      "Dashboard — 통계, 활성 에이전트, 최근 이벤트",
    "KanbanBoard — agent status columns": "KanbanBoard — 에이전트 상태별 열",
    "Sessions — searchable, filterable table": "Sessions — 검색 및 필터링이 가능한 테이블",
    "SessionDetail — agents + full event timeline":
      "SessionDetail — 에이전트 및 전체 이벤트 타임라인",
    "ActivityFeed — real-time streaming event log": "ActivityFeed — 실시간 스트리밍 이벤트 로그",
    "Analytics — token usage, heatmap (day-of-week aligned), tool charts, donut charts":
      "Analytics — 토큰 사용량, 히트맵(요일별 정렬), 도구 차트, 도넛 차트",
    "Workflows — D3.js visualizations, cross-filtering, status filter, session drill-in":
      "Workflows — D3.js 시각화, 교차 필터링, 상태 필터, 세션 드릴인",
    "Settings — model pricing, hook status, data export, session cleanup":
      "Settings — 모델 가격, 훅 상태, 데이터 내보내기, 세션 정리",
    'Returns <code>{ "status": "ok", "timestamp": "..." }</code>':
      '<code>{ "status": "ok", "timestamp": "..." }</code>를 반환합니다',
    "List sessions with agent counts and per-session cost. Params: <code>status</code>, <code>q</code> (case-insensitive search across <code>id</code>/<code>name</code>/<code>cwd</code>), <code>limit</code> (default 50, max 10000), <code>offset</code>. Response includes <code>total</code> for paginators.":
      "에이전트 수와 세션별 비용을 포함하여 세션 목록을 조회합니다. 매개변수: <code>status</code>, <code>q</code>(<code>id</code>/<code>name</code>/<code>cwd</code>에 대한 대소문자 구분 없는 검색), <code>limit</code>(기본값 50, 최대 10000), <code>offset</code>. 응답에는 페이지네이션을 위한 <code>total</code>이 포함됩니다.",
    "Session detail with agents and events": "에이전트와 이벤트를 포함한 세션 상세 정보",
    "Create session (idempotent on <code>id</code>)": "세션 생성(<code>id</code> 기준 멱등적)",
    "Update session status / metadata": "세션 상태/메타데이터 업데이트",
    "List agents — params: <code>status</code>, <code>session_id</code>, <code>limit</code>, <code>offset</code>":
      "에이전트 목록 조회 — 매개변수: <code>status</code>, <code>session_id</code>, <code>limit</code>, <code>offset</code>",
    "Single agent detail": "단일 에이전트 상세 정보",
    "Create agent": "에이전트 생성",
    "Update agent status / task / current_tool": "에이전트 상태/작업/current_tool 업데이트",
    "List events newest-first — params: <code>session_id</code>, <code>limit</code>, <code>offset</code>":
      "최신순으로 이벤트 목록 조회 — 매개변수: <code>session_id</code>, <code>limit</code>, <code>offset</code>",
    "Aggregate counts + status distributions + WS connections":
      "집계된 카운트, 상태 분포, WS 연결 수",
    "Token totals, tool usage, daily trends, agent types, event types, averages":
      "토큰 합계, 도구 사용량, 일별 추세, 에이전트 유형, 이벤트 유형, 평균값",
    "Receive and process a Claude Code hook event (called by hook-handler.js)":
      "Claude Code 훅 이벤트를 수신하고 처리합니다(hook-handler.js에서 호출)",
    "List all model pricing rules": "모든 모델 가격 규칙 조회",
    "Create or update a pricing rule": "가격 규칙 생성 또는 업데이트",
    "Delete a pricing rule": "가격 규칙 삭제",
    "Total cost across all sessions": "모든 세션에 대한 총 비용",
    "Cost breakdown for a specific session": "특정 세션의 비용 내역",
    "System info, DB stats, hook installation status": "시스템 정보, DB 통계, 훅 설치 상태",
    "Delete all sessions, agents, events, token usage":
      "모든 세션, 에이전트, 이벤트, 토큰 사용량 삭제",
    "Reinstall Claude Code hooks": "Claude Code 훅 재설치",
    "Reset Claude, Codex, or both pricing tables to defaults":
      "Claude, Codex 또는 두 가격표를 기본값으로 재설정",
    "Export all data as JSON download": "모든 데이터를 JSON 파일로 내보내기",
    "Abandon stale sessions (by hours), purge old data (by days)":
      "오래된 세션 중단 처리(시간 단위), 오래된 데이터 삭제(일 단위)",
    "OS-aware paths, archive command, supported extensions, step-by-step instructions; includes live stats for the default <code>~/.claude/projects</code> folder":
      "OS별 경로, 아카이브 명령어, 지원되는 확장자, 단계별 안내를 제공합니다. 기본 <code>~/.claude/projects</code> 폴더에 대한 실시간 통계도 포함됩니다",
    "Re-scan the default <code>~/.claude/projects</code> directory; safe to re-run (idempotent via session-ID dedup)":
      "기본 <code>~/.claude/projects</code> 디렉터리를 다시 스캔합니다. 세션 ID 기반 중복 제거로 멱등적이므로 반복 실행해도 안전합니다",
    "Scan any absolute directory (body <code>{ path }</code>); tilde (<code>~</code>) is expanded; walks subdirectories recursively and imports every <code>.jsonl</code> found":
      "임의의 절대 경로 디렉터리를 스캔합니다(요청 본문 <code>{ path }</code>). 물결표(<code>~</code>)는 확장되며, 하위 디렉터리를 재귀적으로 순회하여 발견되는 모든 <code>.jsonl</code> 파일을 임포트합니다",
    "Multipart upload of <code>.jsonl</code>, <code>.meta.json</code>, <code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>, <code>.gz</code>. Per-request staging dir, path-traversal and extraction-size guards. Returns 413 <code>EXTRACTION_LIMIT_EXCEEDED</code> on suspected bomb archives":
      "<code>.jsonl</code>, <code>.meta.json</code>, <code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>, <code>.gz</code> 파일의 멀티파트 업로드를 지원합니다. 요청별 스테이징 디렉터리, 경로 순회(path-traversal) 및 압축 해제 크기 방어 기능이 있습니다. 압축 폭탄(bomb archive)으로 의심되는 경우 413 <code>EXTRACTION_LIMIT_EXCEEDED</code>를 반환합니다",
    "Aggregate workflow data — orchestration graphs, tool flows, effectiveness, patterns, model delegation, error propagation, concurrency, complexity, compaction impact. Accepts <code>?status=active|completed</code> query param to filter by workflow status":
      "워크플로 데이터를 집계합니다 — 오케스트레이션 그래프, 도구 흐름, 효과성, 패턴, 모델 위임, 오류 전파, 동시성, 복잡도, 압축 영향 등을 포함합니다. 워크플로 상태로 필터링하기 위한 <code>?status=active|completed</code> 쿼리 매개변수를 지원합니다",
    "Per-session drill-in — agent tree, tool timeline, event details":
      "세션별 드릴인 — 에이전트 트리, 도구 타임라인, 이벤트 상세 정보",
    "Fired-alert feed, newest first (<code>?unacked=true</code>, <code>limit</code>, <code>offset</code>; carries <code>total</code> and <code>unacked</code> counts)":
      "발생한 알림 피드를 최신순으로 조회합니다(<code>?unacked=true</code>, <code>limit</code>, <code>offset</code>; <code>total</code> 및 <code>unacked</code> 개수를 포함)",
    "Acknowledge one alert": "알림 하나 확인 처리",
    "Acknowledge every unacked alert": "확인되지 않은 모든 알림 확인 처리",
    "List alert rules": "알림 규칙 목록 조회",
    "Create a rule (<code>event_pattern</code> | <code>inactivity</code> | <code>status_duration</code> | <code>token_threshold</code>)":
      "규칙 생성(<code>event_pattern</code> | <code>inactivity</code> | <code>status_duration</code> | <code>token_threshold</code>)",
    "Update name / config / enabled / cooldown": "이름/설정/활성화 여부/쿨다운 업데이트",
    "Delete a rule and its fired-alert history": "규칙과 해당 알림 발생 이력 삭제",
    "Supported providers + their config fields (drives the UI form)":
      "지원되는 제공업체 및 해당 설정 필드 목록(UI 폼을 구성하는 데 사용됨)",
    "List webhook targets (URLs masked, secrets redacted)":
      "웹훅 대상 목록 조회(URL은 마스킹, 시크릿은 삭제 표시됨)",
    "Create a target — 14 first-class providers (Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream) + a generic JSON endpoint":
      "대상 생성 — 14개의 기본 지원 제공업체(Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream)와 범용 JSON 엔드포인트를 지원합니다",
    "Update name / url / enabled / secret / headers / config / rule scope (<code>type</code> is immutable)":
      "이름/URL/활성화 여부/시크릿/헤더/설정/규칙 범위 업데이트(<code>type</code>은 변경 불가)",
    "Delete a target and its delivery log": "대상과 해당 전송 로그 삭제",
    "Send a synthetic test alert and report the result":
      "테스트용 알림을 전송하고 결과를 보고합니다",
    "Recent delivery log for a target": "대상에 대한 최근 전송 로그",
    Documentation: "문서",
    Architecture: "아키텍처",
    "Relevant Links": "관련 링크",
    "GitHub Repo": "GitHub 저장소",
    '<span class="caption-icon">🔔</span> <span><strong>Settings · Alerts</strong> — rules-based alerting engine and outbound webhooks in one place: alert rules (event pattern / inactivity / stuck agent / token threshold) with per-rule cooldown, a live fired-alert feed, and 14 first-class webhook providers plus a generic JSON endpoint with optional HMAC signing</span>':
      '<span class="caption-icon">🔔</span> <span><strong>Settings · Alerts</strong> — 규칙 기반 알림 엔진과 아웃바운드 웹훅을 한곳에서 관리합니다: 규칙별 쿨다운이 설정된 알림 규칙(이벤트 패턴 / 비활성 / 정체된 에이전트 / 토큰 임계값), 실시간 알림 발생 피드, 그리고 선택적 HMAC 서명을 지원하는 14개의 기본 지원 웹훅 제공업체와 범용 JSON 엔드포인트를 제공합니다</span>',
    '<span class="caption-icon">📗</span> <span><strong>API Docs · ReDoc</strong> — a self-hosted, read-optimized rendering of the full OpenAPI 3.0 spec at <code>/api/redoc</code>, served entirely offline with no CDN. Complements the interactive Swagger UI at <code>/api/docs</code>; every backend route is documented with parameters, schemas, and examples</span>':
      '<span class="caption-icon">📗</span> <span><strong>API Docs · ReDoc</strong> — <code>/api/redoc</code>에서 제공되는, 읽기에 최적화된 자체 호스팅 방식의 전체 OpenAPI 3.0 스펙 렌더링으로, CDN 없이 완전히 오프라인에서 제공됩니다. <code>/api/docs</code>의 인터랙티브 Swagger UI를 보완하며, 모든 백엔드 라우트가 매개변수, 스키마, 예제와 함께 문서화되어 있습니다</span>',
    '<span class="caption-icon">📘</span> <span><strong>API Docs · Swagger UI</strong> — interactive OpenAPI 3.0 playground at <code>/api/docs</code>: collapsible endpoint groups, request/response schemas, auth headers, and try-it-out request execution against the live local server</span>':
      '<span class="caption-icon">📘</span> <span><strong>API Docs · Swagger UI</strong> — <code>/api/docs</code>에서 제공되는 인터랙티브 OpenAPI 3.0 플레이그라운드로, 접고 펼칠 수 있는 엔드포인트 그룹, 요청/응답 스키마, 인증 헤더, 그리고 실제 로컬 서버에 대해 실행해볼 수 있는 try-it-out 기능을 제공합니다</span>',
    '<span class="caption-icon">📗</span> <span>ReDoc at <code>/api/redoc</code> — a self-hosted, read-optimized three-panel rendering of the same OpenAPI spec: deep-linkable sections, search, and full schema/example detail. Works entirely offline (no CDN)</span>':
      '<span class="caption-icon">📗</span> <span><code>/api/redoc</code>의 ReDoc — 동일한 OpenAPI 스펙을 읽기에 최적화된 3단 패널로 자체 호스팅하여 렌더링합니다: 딥링크 가능한 섹션, 검색, 그리고 전체 스키마/예제 상세 정보를 제공합니다. CDN 없이 완전히 오프라인에서 동작합니다</span>',
    '<span class="caption-icon">🔔</span> Settings · Alerts — the rules-based alerting engine, a live fired-alert feed, and outbound webhook channels (14 first-class providers + a generic JSON endpoint) managed together in one place':
      '<span class="caption-icon">🔔</span> Settings · Alerts — 규칙 기반 알림 엔진, 실시간 알림 발생 피드, 그리고 아웃바운드 웹훅 채널(14개의 기본 지원 제공업체 + 범용 JSON 엔드포인트)을 한곳에서 함께 관리합니다',
    'Surfaces "dynamic workflows" — the fleets of sub-agents spawned by the <code>Workflow</code> tool and self-paced <code>/loop</code> runs. These emit no hooks, so they are reconstructed from the on-disk run journal written when a workflow finishes (<code>workflows/wf_&lt;runId&gt;.json</code>) plus the inner <code>subagents/agent-*.jsonl</code> transcripts. Each run shows its phases and a per-agent token / tool-call / duration breakdown; a running workflow is detected from its launch script before the journal exists. Runs appear in a panel on the Workflows page and as a linked subsection on each session.':
      '"동적 워크플로(dynamic workflows)" — <code>Workflow</code> 도구와 자체 페이싱되는 <code>/loop</code> 실행에 의해 생성되는 서브에이전트 플릿(fleet)을 표시합니다. 이들은 훅을 전혀 발생시키지 않으므로, 워크플로가 완료될 때 기록되는 온디스크 실행 저널(<code>workflows/wf_&lt;runId&gt;.json</code>)과 내부 <code>subagents/agent-*.jsonl</code> 트랜스크립트로부터 재구성됩니다. 각 실행은 자신의 단계(phase)와 에이전트별 토큰/도구 호출/소요 시간 내역을 보여주며, 실행 중인 워크플로는 저널이 생성되기 전에도 실행 스크립트를 통해 감지됩니다. 실행 내역은 Workflows 페이지의 패널과 각 세션에 연결된 하위 섹션으로 표시됩니다.',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs</strong> — "dynamic workflows" spawned by the Workflow tool, reconstructed from on-disk run journals: status, agent count, tokens, and tool calls, expandable into a per-agent breakdown (phase, state, tokens, tools, duration) with humanized result previews</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs</strong> — Workflow 도구에 의해 생성되고 온디스크 실행 저널로부터 재구성되는 "동적 워크플로": 상태, 에이전트 수, 토큰, 도구 호출을 보여주며, 사람이 읽기 쉬운 결과 미리보기와 함께 에이전트별 내역(단계, 상태, 토큰, 도구, 소요 시간)으로 펼쳐볼 수 있습니다</span>',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · in a session</strong> — the same fleets linked to their launching session, so a session\'s dynamic-workflow sub-agents and their folded-in token cost are visible inline</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · in a session</strong> — 동일한 플릿을 이를 실행한 세션에 연결하여, 세션의 동적 워크플로 서브에이전트와 그에 포함된 토큰 비용을 인라인으로 확인할 수 있습니다</span>',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · expanded</strong> — a run opened up: clickable color-coded phase filters, the per-agent metrics table, and a full list of clickable result items that expand to each agent\'s complete prompt and result</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · expanded</strong> — 펼쳐본 실행 화면: 클릭 가능한 색상별 단계 필터, 에이전트별 지표 테이블, 그리고 클릭하면 각 에이전트의 전체 프롬프트와 결과로 펼쳐지는 결과 항목 전체 목록을 제공합니다</span>',
    'Live counters in Prometheus text-exposition format (v0.0.4): sessions and agents by status, total events, cumulative tokens by kind, connected WebSocket clients, configured remote sources, process uptime and RSS, and build version — so CCAM can be scraped into Prometheus / Grafana. Read-only, and — being under <code>/api</code> — behind the DNS-rebinding Host-header guard and the optional <code>DASHBOARD_TOKEN</code>: a non-loopback scraper (e.g. Prometheus in Docker via <code>host.docker.internal</code>) must be allowlisted with <code>DASHBOARD_ALLOWED_HOSTS</code> or the scrape returns <code>403 EBADHOST</code>, and must send the token when one is set. A ready-to-run Prometheus + Grafana stack with a pre-provisioned "CCAM — Overview" dashboard lives in <code>monitoring/</code> (<code>cd monitoring &amp;&amp; docker compose up -d</code>; Grafana on port 3000).':
      'Prometheus 텍스트 노출 형식(v0.0.4)의 실시간 카운터: 상태별 sessions 및 agents, 총 이벤트 수, 종류별 누적 token, 연결된 WebSocket 클라이언트, 구성된 원격 데이터 소스, 프로세스 가동 시간 및 RSS, 그리고 빌드 버전 — CCAM을 Prometheus / Grafana로 스크레이핑할 수 있도록 합니다. 읽기 전용이며 — <code>/api</code> 아래에 있으므로 — DNS 리바인딩 Host 헤더 가드와 선택적 <code>DASHBOARD_TOKEN</code> 뒤에 위치합니다: 루프백이 아닌 스크레이퍼(예: <code>host.docker.internal</code>을 통해 접근하는 Docker 내 Prometheus)는 <code>DASHBOARD_ALLOWED_HOSTS</code>로 허용 목록에 추가해야 하며, 그렇지 않으면 스크레이프가 <code>403 EBADHOST</code>를 반환하고, 토큰이 설정된 경우 해당 토큰을 함께 보내야 합니다. "CCAM — Overview" 대시보드가 미리 프로비저닝된 즉시 실행 가능한 Prometheus + Grafana 스택이 <code>monitoring/</code>에 있습니다(<code>cd monitoring &amp;&amp; docker compose up -d</code>; Grafana는 3000 포트).',
    "Comma-separated extra <code>Host</code>-header names allowed past the DNS-rebinding guard — needed for a non-loopback client or scraper, such as Prometheus in Docker reaching <code>/api/metrics</code> via <code>host.docker.internal</code>; an unlisted host is rejected with <code>403 EBADHOST</code>":
      "쉼표로 구분된 추가 <code>Host</code> 헤더 이름으로, DNS 리바인딩 가드를 통과하도록 허용합니다 — 루프백이 아닌 클라이언트나 스크레이퍼에 필요하며, 예를 들어 <code>host.docker.internal</code>을 통해 <code>/api/metrics</code>에 접근하는 Docker 내 Prometheus가 있습니다; 목록에 없는 host는 <code>403 EBADHOST</code>로 거부됩니다",
    "The entire UI ships in <strong>five languages — English, 简体中文, Tiếng Việt, 한국어, and Español</strong> — built on <code>i18next</code> + <code>react-i18next</code> with <code>i18next-browser-languagedetector</code>. Coverage is end-to-end: every page, chart tooltip, Settings flow, Workflow narrative, Config Explorer tab, Run page, and the Alerts rule-help tooltips + webhook setup guides are translated. Switch languages from the sidebar's custom language dropdown — the choice persists in <code>localStorage</code>.":
      "전체 UI는 <strong>다섯 가지 언어 — English, 简体中文, Tiếng Việt, 한국어, Español</strong> — 로 제공되며 <code>i18next</code> + <code>react-i18next</code>와 <code>i18next-browser-languagedetector</code>를 기반으로 구축되었습니다. 모든 페이지, 차트 툴팁, Settings 흐름, Workflow 내러티브, Config Explorer 탭, Run 페이지, Alerts 규칙 도움말 툴팁과 webhook 설정 가이드까지 전 영역이 번역됩니다. 사이드바의 맞춤 언어 드롭다운에서 전환할 수 있으며 선택은 <code>localStorage</code>에 저장됩니다.",
    'Every alert-rule field has a help tooltip — the event-type, tool-name, and summary-contains fields include example chips of real hook events and built-in tool names. Each webhook provider ships a collapsible step-by-step setup guide linking to the official docs. A one-click <strong>"Send test"</strong> probe fires a synthetic alert and reports the delivery result inline, and targets can be scoped to specific rules. Fully localized (en / zh / vi / ko / es).':
      '모든 알림 규칙 필드에는 도움말 툴팁이 있습니다 — 이벤트 유형, 도구 이름, 요약 포함 필드에는 실제 훅 이벤트와 내장 도구 이름의 예시 칩이 함께 제공됩니다. 각 웹훅 프로바이더는 공식 문서로 연결되는 접을 수 있는 단계별 설정 가이드를 제공합니다. 클릭 한 번으로 실행하는 <strong>"테스트 전송"</strong> 기능은 가상의 알림을 발생시켜 전달 결과를 즉시 표시하며, 대상을 특정 규칙으로 범위를 제한할 수도 있습니다. en / zh / vi / ko / es로 완전히 현지화되어 있습니다.',
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. A searchable checkbox project picker supports selecting multiple directories; long paths remain available in full, while compact trigger text keeps the filter bar readable. Sorting uses the same custom dropdown pattern. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, and estimated cost. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "<strong>서버 페이지네이션</strong> 테이블은 기록된 모든 세션을 보여줍니다. 각 페이지는 해당 구간만 가져오므로 세션 수와 관계없이 비용 계산 범위가 제한됩니다. <code>id</code>, <code>name</code>, <code>cwd</code>에 대한 대소문자 구분 없는 검색은 300 ms 디바운스와 함께 서버 측에서 실행되며 상태 필터와 조합할 수 있습니다. 검색 가능한 체크박스 프로젝트 선택기로 여러 디렉터리를 고를 수 있고, 긴 경로는 전체를 확인할 수 있으며 간결한 트리거 텍스트가 필터 바를 읽기 쉽게 유지합니다. 정렬도 같은 맞춤 드롭다운 패턴을 사용합니다. 각 행은 세션의 실제 이름(트랜스크립트에서 실시간으로 동기화됨: <code>/rename</code> 또는 <code>claude -n</code> 제목, 없으면 자동 생성된 제목, 그마저도 없으면 첫 사용자 프롬프트, 최종적으로 짧은 ID로 대체), 상태 배지, 에이전트 수, 지속 시간, 모델, 예상 비용을 표시합니다. 행을 클릭하면 대화 트랜스크립트와 에이전트 계층 구조가 포함된 전체 세션 상세 화면으로 이동합니다.",
  },
  es: {
    "<span class=\"caption-icon\">⌘</span> <span><strong>Command Palette</strong> — <code>Cmd/Ctrl+K</code> from anywhere opens the dashboard's only launcher. One query resolves nine groups: recent commands, the nine pages, live server-side session search, the current page's own actions, every known project directory, page sub-views and list filters, all 13 Settings sections, all 12 Agent Config tabs, and preference, data-scope and language actions. Matching is subsequence-based with the matched characters highlighted</span>":
      '<span class="caption-icon">⌘</span> <span><strong>Paleta de comandos</strong> — <code>Cmd/Ctrl+K</code> desde cualquier parte abre el único lanzador del panel. Una consulta resuelve nueve grupos: comandos recientes, las nueve páginas, búsqueda de sesiones en el servidor en tiempo real, las acciones propias de la página actual, cada directorio de proyecto conocido, subvistas y filtros de lista, las 13 secciones de Configuración, las 12 pestañas de Configuración del agente, y acciones de preferencias, alcance de datos e idioma. La coincidencia es por subsecuencia y resalta los caracteres coincidentes</span>',
    "Command Palette": "Paleta de comandos",
    "Press <strong>Cmd/Ctrl+K</strong> anywhere to open a launcher over the whole dashboard. One query resolves nine groups: recently run commands; the current page's own actions; every known project directory; the nine sidebar routes matched on their translated labels, so it works in every locale; live session search backed by <code>/api/sessions?q=</code>, debounced and scoped to the active machine and provider rather than a stale client-side index of thousands of sessions; every page sub-view and list filter; all thirteen Settings sections; all twelve Agent Config tabs; and actions for preferences, data scope, language, and page operations. Ranking is subsequence matching with the matched characters highlighted, so <code>mcp</code> finds MCP servers and <code>kbrd</code> finds Kanban Board, and a leading <code>&gt;</code>, <code>@</code>, or <code>#</code> narrows to actions, pages, or sessions. Arrows, Home/End, PageUp/PageDown and Tab move; Enter opens; Escape closes; a failed session query degrades quietly instead of blocking the palette. Destructive operations are deliberately absent — the palette navigates to them, never performs them.":
      "Pulsa <strong>Cmd/Ctrl+K</strong> en cualquier parte para abrir un lanzador sobre todo el panel. Una consulta resuelve nueve grupos: los comandos ejecutados recientemente; las acciones propias de la página actual; cada directorio de proyecto conocido; las nueve rutas de la barra lateral, coincidiendo con sus etiquetas traducidas, así que funciona en cualquier idioma; búsqueda de sesiones en vivo respaldada por <code>/api/sessions?q=</code>, con debounce y acotada a la máquina y el proveedor activos en lugar de un índice obsoleto de miles de sesiones en el cliente; cada subvista de página y filtro de lista; las trece secciones de Configuración; las doce pestañas de Configuración del agente; y acciones para preferencias, alcance de datos, idioma y operaciones de página. El orden usa coincidencia por subsecuencia resaltando los caracteres que coinciden, así que <code>mcp</code> encuentra MCP servers y <code>kbrd</code> encuentra Kanban Board, y un <code>&gt;</code>, <code>@</code> o <code>#</code> inicial acota a acciones, páginas o sesiones. Las flechas, Home/End, PageUp/PageDown y Tab mueven; Enter abre; Escape cierra; y una consulta de sesiones fallida se degrada en silencio en vez de bloquear la paleta. Las operaciones destructivas se excluyen a propósito: la paleta navega hasta ellas, nunca las ejecuta.",
    "Serve-stale window (ms) for per-transcript task-progress summaries behind <code>include_task_progress</code> list requests and the session-detail <code>todo_snapshot</code>. A transcript being actively appended to rarely hits the size+mtime cache key, so without this floor every list reload fully re-parses each multi-MB live transcript. <code>0</code> restores immediate re-parse on every change.":
      "Ventana de tolerancia para servir datos obsoletos (ms) para los res\u00famenes de progreso de tareas por transcripci\u00f3n detr\u00e1s de las peticiones de lista <code>include_task_progress</code> y del <code>todo_snapshot</code> del detalle de sesi\u00f3n. Una transcripci\u00f3n a la que se a\u00f1ade contenido activamente casi nunca acierta en la clave de cach\u00e9 size+mtime, as\u00ed que sin este m\u00ednimo cada recarga de lista vuelve a analizar por completo cada transcripci\u00f3n activa de varios MB. <code>0</code> restaura el rean\u00e1lisis inmediato en cada cambio.",
    "Closes an idle MCP HTTP or SSE session after this long with no request, reclaiming its server; <code>0</code> disables reaping":
      "Cierra una sesión MCP HTTP o SSE inactiva tras este tiempo sin solicitudes y recupera su Server; <code>0</code> desactiva la recuperación.",
    "Every transport reclaims its own resources. A stdio server self-terminates once its host process is gone — stdin closing, or the process being re-parented away from the parent it started under — so a host that dies without signalling leaves no orphan behind. An HTTP session is released by <code>DELETE /mcp</code>, by an SSE disconnect, or by an idle sweep governed by <code>MCP_HTTP_SESSION_TIMEOUT_MS</code>, because terminating a session is optional in the protocol and a vanished client would otherwise hold its server forever.":
      "Cada transporte recupera sus propios recursos. Un Server stdio se termina solo cuando su proceso anfitrión desaparece — al cerrarse stdin, o al reasignarse el proceso a un padre distinto de aquel bajo el que se inició — de modo que un anfitrión que muere sin señalizar no deja ningún proceso huérfano. Una sesión HTTP se libera con <code>DELETE /mcp</code>, con una desconexión SSE, o mediante un barrido de inactividad regido por <code>MCP_HTTP_SESSION_TIMEOUT_MS</code>, porque terminar una sesión es opcional en el protocolo y un cliente desaparecido retendría su Server para siempre.",
    "One-time automatic repair of token totals inflated before usage was reconciled per <code>message.id</code>. <code>replaceTokenUsage</code> is a high-water mark, so the parser fix alone can never lower a historical total. Marker-gated, deferred off the boot path, skipped while another dashboard shares the data directory, and it snapshots the old rows to <code>token_usage_pre_repair</code> first. <code>0</code> skips it \u2014 repair manually with <code>npm run repair-tokens</code>.":
      "Reparaci\u00f3n autom\u00e1tica \u00fanica de los totales de Token inflados antes de que el uso se reconciliara por <code>message.id</code>. <code>replaceTokenUsage</code> es una marca de nivel m\u00e1ximo, as\u00ed que la correcci\u00f3n del analizador por s\u00ed sola nunca puede bajar un total hist\u00f3rico. Lleva marcador, se difiere fuera del arranque, se omite si otro dashboard comparte el directorio de datos y guarda primero las filas antiguas en <code>token_usage_pre_repair</code>. Con <code>0</code> se omite: repara a mano con <code>npm run repair-tokens</code>.",
    "<strong>Monitor</strong> combines overview stats, active agent cards, collapsible subagent trees, recent activity, and the same task-progress previews used by Sessions. <strong>Health</strong> summarizes system score, storage, cache, errors, tool use, subagent effectiveness, model tokens, and compactions. Both tabs refresh every 5 seconds through WebSocket push, while the activity feed adapts its item count to the available viewport height.":
      "<strong>Monitor</strong> combina estadísticas generales, tarjetas de Agent activos, árboles de subagentes plegables, actividad reciente y las mismas vistas previas de progreso usadas en Sessions. <strong>Health</strong> resume la puntuación del sistema, almacenamiento, caché, errores, uso de herramientas, eficacia de subagentes, Token por modelo y compactaciones. Ambas pestañas se actualizan cada 5 segundos mediante WebSocket, mientras el feed adapta su número de elementos a la altura disponible.",
    "Switch between <strong>Agent</strong> and <strong>Session</strong> swim lanes for working, waiting, completed, error, and abandoned states. Yellow Waiting cards identify work paused for user input, a permission gate, or the next prompt; hover badges and column headers for the exact reason and lifecycle rules. Cards show model, cumulative cost, and current tool, while WebSocket updates keep lane counts synchronized with the live event store.":
      "Alterna entre carriles de <strong>Agent</strong> y <strong>Session</strong> para estados de trabajo, espera, finalización, error y abandono. Las tarjetas Waiting amarillas identifican trabajo pausado por la entrada del usuario, un permiso o el siguiente Prompt; pasa el cursor por insignias y encabezados para ver la razón y las reglas de ciclo de vida. Las tarjetas muestran el modelo, el coste acumulado y la herramienta actual, mientras WebSocket sincroniza el recuento de carriles con los eventos en vivo.",
    "Server-side rules detect event patterns, inactivity, stuck agents, and token thresholds with per-session cooldown dedup. Fired alerts appear in a live feed and can reach 14 built-in providers or a generic JSON endpoint with optional HMAC-SHA256 signing and custom headers. Delivery stays fail-safe through bounded timeouts, retries, secret redaction, test probes, and per-target logs. Configure rules and channels together in <strong>Settings → Alerts</strong>.":
      "Las reglas del servidor detectan patrones de eventos, inactividad, Agent atascados y umbrales de Token con deduplicación por sesión. Las alertas aparecen en un feed en vivo y llegan a 14 Provider integrados o a un endpoint JSON genérico con firma HMAC-SHA256 y Header personalizados opcionales. Los tiempos limitados, reintentos, ocultación de Secret, pruebas y registros por destino mantienen una entrega segura ante fallos. Configura reglas y canales juntos en <strong>Settings → Alerts</strong>.",
    "Electron packages CCAM as a macOS <code>.dmg</code> and Windows installer or portable <code>.exe</code>. The app embeds the Express server in-process and renders the React client in a native window, with tray controls, application menus, login startup, and a single-instance lock. Closing the window leaves the local server running, and first boot installs Claude Code hooks automatically so events flow without a repository checkout.":
      "Electron empaqueta CCAM como <code>.dmg</code> de macOS y como instalador o <code>.exe</code> portátil de Windows. La aplicación integra el servidor Express en el proceso y renderiza el cliente React en una ventana nativa con controles de bandeja, menús, inicio de sesión y bloqueo de instancia única. Cerrar la ventana mantiene el servidor local activo, y el primer inicio instala automáticamente los Hooks de Claude Code para recibir eventos sin descargar el repositorio.",
    "Compare model tokens, tool frequency, agent types, session outcomes, and total input, output, and cache spend. A 52-week activity heatmap and 30-day cost and session sparklines reveal longer trends, while WebSocket refresh and a live indicator keep current values clear. Responsive charts adapt to mobile screens; long legends paginate with localized controls without changing compact legends.":
      "Compara Token por modelo, frecuencia de herramientas, tipos de Agent, resultados de sesiones y gasto total de entrada, salida y caché. Un mapa de calor de 52 semanas y minigráficos de costes y sesiones de 30 días muestran tendencias largas, mientras WebSocket y un indicador en vivo aclaran los valores actuales. Los gráficos se adaptan al móvil; las leyendas largas se paginan con controles localizados sin cambiar las compactas.",
    "Measure subagent effectiveness, workflow patterns, model delegation, error propagation, concurrency, and complexity in one view. Expand detected patterns for their full step chain, statistics, and loop-aware narrative; info popovers explain each headline metric, and the status filter applies across the page. Data-driven legends paginate when needed so every label remains reachable without making chart cards excessively tall.":
      "Mide la eficacia de subagentes, patrones de workflow, delegación de modelos, propagación de errores, concurrencia y complejidad en una sola vista. Expande los patrones detectados para ver toda la cadena, estadísticas y una narración consciente de bucles; los popovers explican cada métrica principal y el filtro de estado se aplica a toda la página. Las leyendas basadas en datos se paginan cuando hace falta para mantener accesible cada etiqueta sin alargar demasiado las tarjetas.",
    "Start Claude Code or Codex from a streaming browser chat, using multi-turn, headless, or resumable sessions. Re-attachment reconciles memory with the saved transcript, while slash-command completion, file references, token context, and effort controls bring core TUI workflows to the dashboard. Codex uses a native interactive <code>app-server</code> thread with provider approval, sandbox, and signed-in model discovery; same-origin checks protect every spawn.":
      "Inicia Claude Code o Codex desde un chat web en streaming con sesiones multi-turn, headless o reanudables. La reconexión concilia la memoria con el transcript guardado, mientras el autocompletado slash, las referencias de archivos, el contexto de Token y los controles de effort llevan los flujos TUI esenciales al Dashboard. Codex usa un hilo <code>app-server</code> interactivo nativo con aprobación del Provider, Sandbox y descubrimiento de modelos de la cuenta; las comprobaciones de mismo origen protegen cada inicio.",
    "Open any session for overview counters, top-tool and subagent breakdowns, a collapsible agent tree, and a chronological event timeline. When task state exists, an owner-aware panel adds a segmented donut, active task, completion bar, and paginated rows. The conversation view renders Markdown, highlighted code, tool blocks, slash-command output, renames, and correctly attributed queued messages. Waiting sessions explain why and for how long; export the record as JSON or share its permalink.":
      "Abre cualquier sesión para ver contadores generales, distribuciones de herramientas y subagentes, un árbol de Agent plegable y una cronología de eventos. Cuando existe estado de tareas, un panel por propietario añade un donut segmentado, la tarea activa, una barra de finalización y filas paginadas. La conversación renderiza Markdown, código resaltado, bloques de herramientas, salida de comandos slash, cambios de nombre y mensajes en cola correctamente atribuidos. Las sesiones en espera explican el motivo y la duración; exporta el registro como JSON o comparte su enlace permanente.",
    "Background sync keeps new Claude project folders and session files visible after the one-time startup import. An immediate sweep, debounced filesystem watcher, and optional 30-second safety poll share one mtime cache and coalesce overlapping work; set <code>DASHBOARD_SESSION_SYNC_MS</code> to tune or disable only the poll. Each sweep parses only changed transcripts, skips unchanged imports, and broadcasts session and main-agent updates so the UI refreshes live across macOS, Windows, and Linux.":
      "La sincronización en segundo plano mantiene visibles las carpetas de proyecto y los archivos de sesión Claude nuevos tras la importación inicial única. Un barrido inmediato, un observador del sistema de archivos con debounce y una comprobación de seguridad opcional cada 30 segundos comparten una caché mtime y agrupan el trabajo solapado; usa <code>DASHBOARD_SESSION_SYNC_MS</code> para ajustar o desactivar solo la comprobación. Cada barrido analiza únicamente transcripts modificados, omite importaciones sin cambios y transmite actualizaciones de sesión y del Agent principal para refrescar la UI en vivo en macOS, Windows y Linux.",
    "Configure per-model input, output, cache, and Codex reasoning rates in Settings, then see consistent totals across Sessions, Session Detail, Analytics, Kanban, and agent cards. Compaction-aware accounting preserves usage across context compression, imports reuse the same pricing table, and rate edits recalculate stored sessions. Subagent cards show only their own transcript cost while main agents retain session totals. GPT pricing distinguishes standard, long-context, and Fast modes, leaving unpublished tiers explicitly unpriced.":
      "Configura en Settings las tarifas de entrada, salida, caché y razonamiento de Codex por modelo y consulta totales coherentes en Sessions, Session Detail, Analytics, Kanban y tarjetas de Agent. La contabilidad compatible con compactación conserva el uso tras comprimir el contexto, las importaciones reutilizan la misma tabla de precios y los cambios de tarifa recalculan las sesiones guardadas. Las tarjetas de subagentes muestran solo el coste de su propio transcript, mientras que los Agent principales conservan el total de la sesión. Los precios GPT distinguen los modos estándar, contexto largo y Fast; los niveles no publicados quedan explícitamente sin precio.",
    "Explore Claude Code configuration in 12 tabs covering skills, subagents, commands, styles, plugins, MCP, hooks, settings, memory, keybindings, and statusline scripts. The page resolves <code>/config</code> values across scopes, searches project memory, links <code>MEMORY.md</code> entries to their source facts, and shows plugin authorship. Low-risk text, memory, and keybinding files support create, edit, and delete with timestamped backups; plugins, MCP, hooks, and live settings remain read-only with copyable CLI guidance.":
      "Explora la configuración de Claude Code en 12 pestañas que cubren skills, subagentes, comandos, estilos, plugins, MCP, hooks, ajustes, memoria, atajos y scripts de statusline. La página resuelve valores de <code>/config</code> entre ámbitos, busca en la memoria del proyecto, enlaza entradas de <code>MEMORY.md</code> con sus datos de origen y muestra la autoría de los plugins. Los archivos de texto, memoria y atajos de bajo riesgo permiten crear, editar y eliminar con copias de seguridad fechadas; plugins, MCP, hooks y ajustes activos siguen siendo de solo lectura con instrucciones CLI copiables.",
    "Browse every recorded session in a <strong>server-paginated</strong> table with fast search, project and status filters, sorting, cost, model, duration, and agent counts. The first page can show a temporary local Codex startup row until its durable session arrives. Rows with task state add a compact progress donut and owner-aware preview; stale trackers disappear when newer work no longer uses them or a turn ends unfinished, while completed progress remains available. Open any durable row for its conversation, events, and agent hierarchy.":
      "Explora todas las sesiones registradas en una tabla con <strong>paginación del servidor</strong>, búsqueda rápida, filtros de proyecto y estado, ordenación, coste, modelo, duración y número de Agent. La primera página puede mostrar temporalmente una fila local de inicio de Codex hasta que llegue su sesión duradera. Las filas con estado de tareas añaden un donut compacto y una vista previa por propietario; los trackers obsoletos desaparecen cuando el trabajo nuevo deja de usarlos o un turno termina incompleto, mientras el progreso completado sigue disponible. Abre cualquier fila duradera para ver su conversación, eventos y jerarquía de Agent.",
    "Reads Claude and Codex transcripts incrementally, keeping long-running sessions fast while powering conversation history, live titles, prompt previews, attachments, and turn statistics. Renames and recent human prompts flow to session and agent cards in real time, older conversation pages load on demand, and duplicate Codex records collapse into one readable turn. Stable transcript identities also prevent replayed events from inflating totals, while provider-aware process matching keeps historical sessions from appearing active.":
      "Lee los transcripts de Claude y Codex de forma incremental para mantener rápidas las sesiones largas y alimentar el historial de conversaciones, los títulos en vivo, las vistas previas de prompts, los adjuntos y las estadísticas de turnos. Los cambios de nombre y prompts humanos recientes llegan a las tarjetas de sesión y Agent en tiempo real, las páginas antiguas se cargan bajo demanda y los registros duplicados de Codex se agrupan en un turno legible. Las identidades estables también evitan que los eventos repetidos inflen los totales, mientras la detección de procesos por provider impide que sesiones históricas aparezcan activas.",
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. The first page also shows the same local, in-memory Codex startup row used by Dashboard and Kanban. It appears immediately but stays non-navigable until a durable session ID replaces it, without changing durable totals or pagination. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, estimated cost, and — when the provider exposed task state — a compact completion donut beside the status badge. Hovering or focusing the donut opens a viewport-clamped preview with current work, owner labels, source, and up to five tasks. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "Tabla <strong>paginada por servidor</strong> de todas las sesiones registradas. Cada página obtiene solo su parte, por lo que el cálculo de costes se mantiene acotado sin importar cuántas sesiones haya. La primera página también muestra la misma fila local y en memoria de inicio de Codex que Dashboard y Kanban. Aparece de inmediato, pero no es navegable hasta que la reemplaza un ID de sesión duradero, sin cambiar los totales duraderos ni la paginación. La búsqueda sin distinción de mayúsculas en <code>id</code>, <code>name</code> y <code>cwd</code> se ejecuta en el servidor con una espera de 300 ms; el filtro de estado se combina con la búsqueda. Cada fila muestra el nombre real sincronizado desde el transcript (título de <code>/rename</code> o <code>claude -n</code>, título automático, primer prompt o ID corto), la insignia de estado, el número de Agent, la duración, el modelo y el coste estimado. Cuando el provider expone estado de tareas, también aparece un donut compacto junto al estado. Al pasar el cursor o enfocar el donut se abre una vista previa limitada al viewport con el trabajo actual, propietarios, origen y hasta cinco tareas. Haz clic en una fila duradera para abrir el detalle completo con la conversación y la jerarquía de Agent.",
    "A fresh interactive Codex process first appears as an in-memory Waiting card, including while Codex is still at its trust screen or first prompt and has no stable session ID. The card is local-only, non-navigable, excluded from durable APIs unless <code>include_transient=true</code>, and removed on process exit. A <code>SessionStart</code> hook, native live-thread row, or rollout then creates the durable session and main-agent rows without copying the temporary card into history. If the user selects an existing thread in Codex's Resume picker, that exact Codex PID opens the resumed rollout or writer lock immediately. CCAM uses that signal to switch from the temporary card to the durable resumed session before the first new message.":
      "Un nuevo proceso interactivo de Codex aparece primero como una tarjeta Waiting en memoria, incluso mientras permanece en la pantalla de confianza o en el primer prompt sin un ID de sesión estable. La tarjeta es solo local, no navegable, queda fuera de las API duraderas salvo que se use <code>include_transient=true</code> y se elimina al terminar el proceso. Después, un Hook <code>SessionStart</code>, una fila live-thread nativa o un rollout crea las filas duraderas de sesión y Agent principal sin copiar la tarjeta temporal al historial. Si el usuario selecciona un hilo existente en el selector Resume de Codex, ese PID exacto abre inmediatamente el rollout o writer lock reanudado. CCAM usa esa señal para cambiar de la tarjeta temporal a la sesión duradera reanudada antes del primer mensaje nuevo.",
    '<span class="caption-icon">📋</span> <span><strong>Task Progress · Overview</strong> — Dashboard agent cards and Sessions rows reuse the same compact completion donut beside status; hover or focus opens an owner-aware preview of current work and task states</span>':
      '<span class="caption-icon">📋</span> <span><strong>Progreso de tareas · Resumen</strong> — las tarjetas de Agent del Dashboard y las filas de Sessions reutilizan el mismo donut compacto de finalización junto al estado; al pasar el cursor o enfocar se abre una vista previa por propietario del trabajo actual y los estados de las tareas</span>',
    '<span class="caption-icon">✅</span> <span><strong>Task Progress · Session Detail</strong> — the full owner-aware tracker combines a segmented completion donut, active task, completion bar, owner breakdown, and a task list paginated at 10 rows per page</span>':
      '<span class="caption-icon">✅</span> <span><strong>Progreso de tareas · Detalles de la sesión</strong> — el rastreador completo por propietario combina un donut segmentado de finalización, la tarea activa, una barra de finalización, el desglose por propietario y una lista paginada de 10 filas por página</span>',
    "Two tabs: <strong>Monitor</strong> shows overview stats, active agent cards with collapsible subagent hierarchy, and a recent activity feed whose item count fills available viewport height. When an active session exposes task state, its card shows the same completion donut as the Sessions table immediately before the status badge; hover or focus opens the detailed task preview. <strong>Health</strong> renders a composite system health score ring, storage engine donut chart, cache/error/success gauges, tool invocation bars, subagent effectiveness ratios, model token distribution, and compaction stats. Both tabs auto-refresh every 5 seconds via WebSocket push so the view is always current without manual reload.":
      "Dos pestañas: <strong>Monitor</strong> muestra estadísticas generales, tarjetas de Agent activos con una jerarquía de subagentes plegable y una fuente de actividad reciente cuyo número de elementos ocupa la altura disponible. Cuando una sesión activa expone el estado de las tareas, su tarjeta muestra el mismo donut de finalización que la tabla Sessions justo antes de la insignia de estado; al pasar el cursor o enfocar se abre la vista previa detallada de tareas. <strong>Health</strong> presenta un anillo de puntuación de salud del sistema, un donut del motor de almacenamiento, indicadores de caché/error/éxito, barras de invocación de herramientas, proporciones de eficacia de subagentes, distribución de Token por modelo y estadísticas de compactación. Ambas pestañas se actualizan automáticamente cada 5 segundos mediante WebSocket para mantenerse al día sin recarga manual.",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. When observable task state exists, an owner-aware progress panel appears directly below the overview with a segmented donut, active task, completion bar, owner breakdown, and dense task rows paginated at 10 tasks per page. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "Vista detallada por sesión con un árbol jerárquico de Agent plegable y una cronología completa que muestra el nombre y el resumen de cada llamada de herramienta. Un panel general en la parte superior muestra contadores de eventos, llamadas de herramientas, subagentes, compactaciones, errores y duración. Las barras de uso de las herramientas principales y el desglose por tipo de subagente permiten leer la distribución rápidamente. Cuando existe un estado de tareas observable, aparece justo debajo del resumen un panel de progreso con propietario que incluye un donut segmentado, la tarea activa, una barra de finalización, un desglose por propietario y filas de tareas densas paginadas a 10 tareas por página. El visor de conversaciones renderiza Markdown con resaltado de sintaxis, bloques con estilo por herramienta, píldoras de comandos slash con su salida TUI capturada y marcadores de cambio de nombre de sesión en línea. Los mensajes escritos a mitad de turno, en cola mientras Claude seguía trabajando, aparecen donde Claude los recibió realmente y las notificaciones del harness se atribuyen a System. Exporta toda la sesión como JSON o comparte el enlace permanente para una revisión asíncrona. Cuando la sesión está esperando al usuario, un banner amarillo bajo el encabezado indica la razón y el tiempo de espera.",
    '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — live overview of active sessions and agents. Cards with task state show a progress donut beside status with the same detailed preview as Sessions. Stats tiles, collapsible subagent hierarchy cards, and a recent activity feed auto-refresh via WebSocket</span>':
      '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — vista en vivo de las sesiones y los Agent activos. Las tarjetas con estado de tareas muestran un donut de progreso junto al estado con la misma vista previa detallada que Sessions. Las estadísticas, las tarjetas jerárquicas de subagentes plegables y la actividad reciente se actualizan automáticamente mediante WebSocket</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles, an owner-aware task-progress panel with 10 tasks per page when available, top-tool usage, subagent breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — estadísticas generales, un panel de progreso de tareas con propietario y 10 tareas por página cuando está disponible, uso de herramientas principales, desglose de subagentes y un árbol jerárquico plegable de Agent padre-hijo</span>',
    "Base URL used by the local MCP server when calling dashboard APIs. Direct loopback HTTP is allowed with a bearer token; tokenized container-host aliases require HTTPS.":
      "URL base que usa el servidor MCP local al llamar a las API del panel. HTTP de loopback directo puede llevar un Bearer Token; los alias de host de contenedor con token requieren HTTPS.",
    "Bearer token for a protected dashboard. Falls back to <code>DASHBOARD_API_TOKEN</code>.":
      "Bearer Token para un panel protegido. Usa <code>DASHBOARD_API_TOKEN</code> como respaldo.",
    "Restore one export bundle up to 25 MiB (multipart <code>file</code> or JSON <code>{ path }</code>). The restore is idempotent and non-destructive, so it can consolidate machines.":
      "Restaura un paquete exportado de hasta 25 MiB (multipart <code>file</code> o JSON <code>{ path }</code>). La restauración es idempotente y no destructiva, por lo que permite consolidar máquinas.",
    "All dashboard requests reject redirects. Direct loopback HTTP may carry the dashboard bearer token, while tokenized container-host aliases require HTTPS. History uploads are capped at 50 MiB per file and 100 MiB per call, binary responses at 10 MiB, and backup restore at 25 MiB. Only GET requests retry automatically.":
      "Todas las solicitudes al panel rechazan redirecciones. HTTP de loopback directo puede llevar el Bearer Token del panel, mientras que los alias de host de contenedor con token requieren HTTPS. Las cargas de historial se limitan a 50 MiB por archivo y 100 MiB por llamada, las respuestas binarias a 10 MiB y la restauración de copias a 25 MiB. Solo las solicitudes GET se reintentan automáticamente.",
    "Configuration file reads canonicalize the target and allowed roots, so symlinks cannot escape the trusted Claude directories.":
      "Las lecturas de archivos de configuración canonizan el destino y las raíces permitidas, por lo que los enlaces simbólicos no pueden salir de los directorios Claude confiables.",
    "Codex preview targets are canonicalized before containment checks. Edits reject symlinked path components and refuse content containing <code>[redacted]</code>.":
      "Los destinos de vista previa de Codex se canonizan antes de comprobar su contención. Las ediciones rechazan componentes de ruta con enlaces simbólicos y contenido que incluya <code>[redacted]</code>.",
    "Redirects are rejected rather than forwarding credentials or signed headers to another URL.":
      "Las redirecciones se rechazan en lugar de reenviar credenciales o encabezados firmados a otra URL.",
    "Hosted providers require HTTPS. Generic and n8n targets may use HTTP for local or self-hosted receivers.":
      "Los proveedores alojados requieren HTTPS. Los destinos Generic y n8n pueden usar HTTP para receptores locales o autohospedados.",
    "Stakeholder-ready executive, cost, reliability, and workflow reports with explicit scope and evidence":
      "Informes ejecutivos, de costos, fiabilidad y flujos de trabajo listos para interesados, con alcance y evidencia explícitos",
    "Long Analytics legends use compact pages with localized controls. Legends that fit on one page stay unchanged.":
      "Las leyendas largas de Analytics usan páginas compactas con controles localizados. Las que caben en una página no cambian.",
    "Data-driven Workflows legends also paginate when labels would make a chart card too tall, while keeping every label reachable.":
      "Las leyendas de Workflows basadas en datos también se paginan cuando las etiquetas alargarían demasiado una tarjeta, sin dejar ninguna etiqueta inaccesible.",
    "First-run readiness follows the selected scope: Claude Code requires Claude hooks, Codex requires Codex hooks, and Both requires both. Ready selections open the dashboard immediately; otherwise only missing selected providers appear in setup.":
      "La preparación inicial sigue el ámbito elegido: Claude Code requiere hooks de Claude, Codex requiere hooks de Codex y Ambos requiere los dos conjuntos. Las selecciones listas abren el panel de inmediato; de lo contrario, la configuración solo muestra los proveedores seleccionados que faltan.",
    "Local MCP sidecar with three transport modes and 97 typed tools across 16 domain modules. It covers the complete supported app surface, including scoped data, transcripts and images, Claude/GPT pricing, workflows, alerts, webhooks, imports and restore, Claude/Codex config, Run Agent, remote sources, push, and maintenance. Every transport shares one validated catalog with bearer-token support and tiered mutation/destructive gates.":
      "El sidecar MCP local ofrece tres modos de transporte y 97 herramientas tipadas en 16 módulos de dominio. Cubre toda la superficie compatible de la aplicación, incluidos datos con ámbito, transcripciones e imágenes, precios Claude/GPT, flujos de trabajo, alertas, webhooks, importación y restauración, configuración Claude/Codex, Run Agent, fuentes remotas, push y mantenimiento. Todos los transportes comparten un catálogo validado con Bearer Token y puertas de mutación/destrucción por niveles.",
    "One shared marketplace ships 14 plugins for Claude Code and Codex, with 66 packaged skills, 18 Claude subagents, 34 Claude commands, 3 CLI helpers, and 2 MCP-enabled plugins. Every skill includes canonical frontmatter and OpenAI metadata. The skills.sh CLI discovers 76 repository skills from the same public source. Install with <code>claude plugin marketplace add</code>, <code>codex plugin marketplace add</code>, or <code>npx skills add</code>.":
      "Un Marketplace compartido distribuye 14 plugins para Claude Code y Codex, con 66 habilidades empaquetadas, 18 subagentes de Claude, 34 comandos de Claude, 3 utilidades CLI y 2 plugins con MCP. Cada habilidad incluye Frontmatter canónico y metadatos OpenAI. La CLI de skills.sh descubre 76 habilidades del repositorio desde la misma fuente pública. Instala con <code>claude plugin marketplace add</code>, <code>codex plugin marketplace add</code> o <code>npx skills add</code>.",
    "The Agent Monitor ships one shared marketplace containing 14 plugins for Claude Code and Codex, 66 packaged plugin skills, 18 Claude subagents, 34 Claude commands, 3 CLI helpers, 3 hook configurations, and 2 MCP-enabled plugins. The skills.sh CLI discovers 76 total repository skills. Product-specific manifests stay separate while skill instructions and validation remain shared.":
      "Agent Monitor incluye un Marketplace compartido con 14 plugins para Claude Code y Codex, 66 habilidades de plugin empaquetadas, 18 subagentes de Claude, 34 comandos de Claude, 3 utilidades CLI, 3 configuraciones de hooks y 2 plugins con MCP. La CLI de skills.sh descubre 76 habilidades totales del repositorio. Los Manifest específicos de cada producto permanecen separados, mientras que las instrucciones y la validación de habilidades se comparten.",
    "Each plugin carries product-specific Claude Code and Codex manifests. The catalogs live at <code>.claude-plugin/marketplace.json</code> and <code>.agents/plugins/marketplace.json</code>. Each plugin directory contains:":
      "Cada plugin incluye Manifest específicos para Claude Code y Codex. Los Catalog viven en <code>.claude-plugin/marketplace.json</code> y <code>.agents/plugins/marketplace.json</code>. Cada directorio de plugin contiene:",
    "Safe monitored Claude Code/Codex launch, follow-up, stop, resume, and run history":
      "Inicio, seguimiento, detención, reanudación e historial de ejecuciones de Claude Code/Codex con supervisión segura.",
    "Alert rules, webhook providers, push notifications, and SSH remote collection":
      "Reglas de alerta, proveedores de webhook, notificaciones push y recopilación remota por SSH.",
    "Claude/Codex config, hooks, provider imports, backup restore, updates, and MCP operations":
      "Configuración Claude/Codex, hooks, importaciones por proveedor, restauración de copias, actualizaciones y operaciones MCP.",
    "Remote sessions never emit live hooks to this dashboard, so a healthy Claude Code or Codex provider mirror remains exempt from the local process-liveness reap and watchdog transcript scan. Every successful provider sync still reconciles live status from the matching mirrored transcript: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime); within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) keeps the session <code>active</code>, otherwise it is reconciled to <code>completed</code>. If that provider's SSH sync fails, is unavailable, or is stranded in <code>syncing</code> longer than <code>DASHBOARD_STALE_MINUTES</code> — and an active session is itself older than that same window — the ordinary stale-session sweep marks it <code>abandoned</code> and completes its agents. A later fresh mirror reactivates a session that is still writing, so unavailable providers never leave permanent Waiting cards.":
      "Las sesiones remotas nunca emiten hooks en vivo a este panel, por lo que una réplica sana del proveedor Claude Code o Codex sigue excluida de la comprobación local de procesos y del análisis de transcript del watchdog. Cada sincronización correcta de proveedor reconcilia el estado en vivo desde el transcript reflejado correspondiente: la actividad se determina a partir de la <b>marca de tiempo del evento más reciente dentro del JSONL</b> (con mtime de la réplica como respaldo); estar dentro de <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (10 minutos de forma predeterminada) mantiene la sesión <code>active</code>; de lo contrario se reconcilia como <code>completed</code>. Si falla la sincronización SSH de ese proveedor, no está disponible o permanece en <code>syncing</code> más de <code>DASHBOARD_STALE_MINUTES</code> —y una sesión activa supera esa misma ventana— el barrido normal de sesiones obsoletas la marca <code>abandoned</code> y completa sus agentes. Una réplica nueva posterior reactiva una sesión que sigue escribiendo, por lo que los proveedores no disponibles nunca dejan tarjetas En espera permanentes.",
    '<span class="caption-icon">🛰️</span> <span><strong>Settings · Remote Data Sources</strong> — pull Claude Code and Codex activity from other machines over SSH: optionally set independent Remote Claude home and Remote Codex home paths, test each provider, sync manually or on a background poller, and switch the global data scope between local, all sources, or a specific machine with per-session source badges</span>':
      '<span class="caption-icon">🛰️</span> <span><strong>Configuración · Fuentes de datos remotas</strong> — obtenga la actividad de Claude Code y Codex de otras máquinas a través de SSH: configure de forma opcional rutas independientes de Inicio remoto de Claude e Inicio remoto de Codex, pruebe cada proveedor, sincronice manualmente o en un sondeo de fondo y cambie el alcance global de los datos entre local, todas las fuentes o una máquina específica, con insignias de fuente por sesión</span>',
    '<span class="caption-icon">🧰</span> <span><strong>Codex Config Explorer</strong> — the Codex workspace brings together <code>config.toml</code>, account models, profiles, MCP servers, projects, skills, hooks, rules, plugins, and instructions. Edit supported user-managed files with timestamped backups; <code>config.toml</code> is edit-only.</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Explorador de configuración de Codex</strong> — el espacio de trabajo de Codex reúne <code>config.toml</code>, modelos de la cuenta, perfiles, servidores MCP, proyectos, habilidades, hooks, reglas, complementos e instrucciones. Edita los archivos compatibles gestionados por el usuario con copias de seguridad con marca de tiempo; <code>config.toml</code> es solo editable.</span>',
    "Claude Code and Codex cards show a compact two-row history of their latest distinct human prompts beneath the provider-native title, so a short friendly name or terse follow-up never hides the active task. Claude refreshes this context from its local transcript cache during live hooks, imports, and watchdog sweeps; Codex refreshes it from rollout records and falls back to persisted <code>user_message</code> events for older imports. The transcript renders persisted PNG/JPEG/GIF/WebP attachments for Claude Code and Codex when available, while duplicate Codex response/event copies collapse into one human turn.":
      "Las tarjetas de Claude Code y Codex muestran un historial compacto de dos líneas de sus últimas indicaciones humanas distintas bajo el título nativo del proveedor, de modo que un nombre breve o un seguimiento conciso nunca oculten la tarea activa. Claude actualiza este contexto desde su caché local de transcripciones durante los hooks en vivo, las importaciones y los barridos del watchdog; Codex lo actualiza desde los registros de rollout y recurre a eventos <code>user_message</code> conservados para las importaciones antiguas. La transcripción renderiza adjuntos PNG/JPEG/GIF/WebP persistidos de Claude Code y Codex cuando están disponibles, y las copias response/event duplicadas de Codex se contraen en un único turno humano.",
    "<strong>Run Agent</strong> now begins by choosing Claude Code or Codex. Codex starts a native interactive <code>app-server</code> thread, offers provider-native approval and sandbox controls, and discovers models directly from the signed-in CLI so new releases appear without a dashboard update.":
      "<strong>Ejecutar agente</strong> ahora comienza eligiendo Claude Code o Codex. Codex inicia un hilo <code>app-server</code> interactivo nativo, ofrece controles de aprobación y sandbox propios del proveedor, y detecta modelos directamente desde la CLI con sesión iniciada para que los lanzamientos nuevos aparezcan sin actualizar el panel.",
    "The same page also has a <strong>Codex</strong> switch. Its workspace shows defaults, the model cache, profiles, MCP servers, projects, skills, rules, hooks, installed plugins, and instruction files from <code>CODEX_HOME</code>. Plugin cards use <code>codex plugin list</code> rather than exposing cache folders. Secret-like TOML and JSON values are redacted in previews; an explicit local editor supports <code>config.toml</code>, <code>hooks.json</code>, user rules, skills, and instruction files with atomic saves and timestamped backups. It warns that syntax is not validated, and <code>codex_config_changed</code> refreshes the view when those files change.":
      "La misma página también tiene un selector de <strong>Codex</strong>. Su espacio de trabajo muestra valores predeterminados, la caché de modelos, perfiles, servidores MCP, proyectos, habilidades, reglas, ganchos, complementos instalados y archivos de instrucciones de <code>CODEX_HOME</code>. Las tarjetas de complementos usan <code>codex plugin list</code> en lugar de exponer carpetas de caché. Los valores TOML y JSON similares a secretos se ocultan en las vistas previas; un editor local explícito admite <code>config.toml</code>, <code>hooks.json</code>, reglas, habilidades y archivos de instrucciones del usuario con guardados atómicos y copias de seguridad con marca de tiempo. Advierte que la sintaxis no se valida, y <code>codex_config_changed</code> actualiza la vista cuando cambian esos archivos.",
    "The same page also has a <strong>Codex</strong> switch. Its workspace shows defaults, the full local account model catalog, profiles, MCP servers, projects, skills, rules, hooks, installed plugins, and instruction files from <code>CODEX_HOME</code>. The model catalog is read apart from the small redacted-preview limit, so long account instructions cannot make the Models tab report zero. Create a standard named <code>&lt;name&gt;.config.toml</code> profile in place; Codex overlays it only for <code>codex --profile &lt;name&gt;</code>, and its card copies that exact command in one click. Plugin cards use <code>codex plugin list</code> rather than exposing cache folders. Secret-like TOML and JSON values are redacted in previews; an explicit local editor supports <code>config.toml</code>, profile overlays, <code>hooks.json</code>, user rules, skills, and instruction files with atomic saves and timestamped backups. User-maintained profiles, hooks, rules, skills, and instructions also have confirmed backup-backed <strong>View source / Copy path / Edit / Delete</strong> actions; a skill deletion preserves its whole directory, while <code>config.toml</code> stays edit-only. It warns that syntax is not validated, and <code>codex_config_changed</code> refreshes the view when those files change.":
      "La misma página también tiene un selector de <strong>Codex</strong>. Su espacio de trabajo muestra valores predeterminados, el catálogo completo de modelos de la cuenta local, perfiles, servidores MCP, proyectos, habilidades, reglas, hooks, complementos instalados y archivos de instrucciones de <code>CODEX_HOME</code>. El catálogo de modelos se lee aparte del pequeño límite de vistas previas con datos ocultos, por lo que instrucciones de cuenta extensas no pueden hacer que la pestaña Models informe cero. Crea directamente un perfil estándar con nombre <code>&lt;name&gt;.config.toml</code>; Codex solo lo superpone al usar <code>codex --profile &lt;name&gt;</code>, y su tarjeta copia ese comando exacto con un clic. Las tarjetas de complementos usan <code>codex plugin list</code> en lugar de exponer carpetas de caché. Los valores TOML y JSON similares a secretos se ocultan en las vistas previas; un editor local explícito admite <code>config.toml</code>, superposiciones de perfil, <code>hooks.json</code>, reglas, habilidades y archivos de instrucciones del usuario con guardados atómicos y copias de seguridad con marca de tiempo. Los perfiles, hooks, reglas, habilidades e instrucciones mantenidos por el usuario también tienen acciones confirmadas y respaldadas de <strong>Ver fuente / Copiar ruta / Editar / Eliminar</strong>; eliminar una habilidad conserva todo su directorio, mientras que <code>config.toml</code> sigue siendo solo editable. Advierte que la sintaxis no se valida, y <code>codex_config_changed</code> actualiza la vista cuando cambian esos archivos.",
    '<span class="caption-icon">🧰</span> <span><strong>Agent Config</strong> — switch between the editable Claude Code explorer and a live Codex workspace for defaults, models, profiles, MCP, projects, skills, rules, hooks, plugins, and instructions. Previews redact secret-like values; user-maintained files receive backup-backed edits.</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Configuración de agentes</strong> — alterna entre el explorador editable de Claude Code y el espacio de trabajo de Codex en vivo para valores predeterminados, modelos, perfiles, MCP, proyectos, habilidades, reglas, ganchos, complementos e instrucciones. Las vistas previas ocultan valores secretos; los archivos mantenidos por el usuario reciben ediciones con copia de seguridad.</span>',
    '<span class="caption-icon">▶️</span> <span><strong>Run Agent</strong> — choose Claude Code or Codex each time you open the launcher. Claude supports Conversation or Headless mode; Codex starts a native interactive thread with approval and sandbox controls.</span>':
      '<span class="caption-icon">▶️</span> <span><strong>Ejecutar agente</strong> — elige Claude Code o Codex cada vez que abras el iniciador. Claude admite los modos Conversación o Sin interfaz; Codex inicia un hilo interactivo nativo con controles de aprobación y sandbox.</span>',
    '<span class="caption-icon">💬</span> <span><strong>Run Agent · live stream</strong> — Claude stream-json and Codex app-server events render together as chat, including reasoning, commands, file changes, and tool activity. Dashboard Runs lets you re-attach later.</span>':
      '<span class="caption-icon">💬</span> <span><strong>Ejecutar agente · transmisión en vivo</strong> — las envolturas stream-json de Claude y los eventos de app-server de Codex se muestran juntos como chat, incluidos razonamiento, comandos, cambios de archivos y actividad de herramientas. Dashboard Runs permite volver a conectarte después.</span>',
    "Codex also reads its native session index, so <code>/rename</code> titles reach session and agent cards in real time. Its conversation reader includes human turns plus <code>exec</code> custom-tool calls and outputs, and loads older pages when you reach the top of the transcript.":
      "Codex también lee su índice de sesiones nativo, por lo que los títulos de <code>/rename</code> llegan a las tarjetas de sesión y agente en tiempo real. Su lector de conversaciones incluye turnos humanos, llamadas y salidas de herramientas personalizadas <code>exec</code>, y carga páginas anteriores al llegar a la parte superior de la transcripción.",
    "Optional Codex home. Settings saves this dashboard-only override, re-arms live rollout watching, and immediately scans the new <code>sessions/</code> tree.":
      "Directorio de Codex opcional. Configuración guarda esta anulación exclusiva del panel, reactiva la supervisión en vivo de los rollouts y analiza inmediatamente el nuevo árbol <code>sessions/</code>.",
    "<strong>Dashboard Data</strong> lets you switch the entire app between Claude Code, Codex, or both. Sessions, agents, events, token totals, workflows, analytics, and costs refetch immediately. The same page saves separate Claude Code and Codex homes; saving the Codex home re-arms live rollout watching and performs a fresh scan.":
      "<strong>Datos del panel</strong> permite cambiar toda la aplicación entre Claude Code, Codex o ambos. Las sesiones, agentes, eventos, totales de tokens, flujos de trabajo, analítica y costes se vuelven a consultar de inmediato. La misma página guarda directorios separados de Claude Code y Codex; guardar el directorio de Codex reactiva la supervisión de rollouts en vivo y realiza un análisis nuevo.",
    "The manual installer is interactive: Claude Code starts selected; use arrow keys, <kbd>Space</kbd>, then <kbd>Enter</kbd> to choose Claude Code, Codex (beta), or both. If a selected dashboard hook is already present, it warns before replacing only that entry and preserves unrelated hooks. The same guided installer is available from <strong>Settings → Hook Configuration</strong>. On the first dashboard visit, choose a data source, then use the provider-locked live-monitoring setup gate to install the selected hooks in place and review its output. You can also explicitly confirm hooks are already installed before continuing.":
      "El instalador manual es interactivo: Claude Code comienza seleccionado; usa las teclas de flecha, <kbd>Space</kbd> y <kbd>Enter</kbd> para elegir Claude Code, Codex (beta) o ambos. Si ya existe un hook del panel para la selección, avisa antes de reemplazar solo esa entrada y conserva los hooks no relacionados. El mismo instalador guiado está disponible desde <strong>Settings → Hook Configuration</strong>. En la primera visita al panel, elige una fuente de datos y usa la puerta de configuración de monitorización en vivo vinculada al proveedor para instalar los hooks seleccionados directamente y revisar su salida. También puedes confirmar explícitamente que los hooks ya están instalados antes de continuar.",
    "Codex rollouts are accounted separately with fresh, cached, cache-write, output, and reasoning-output deltas. The GPT rate card distinguishes standard short context (≤272K input tokens), long context, and Fast mode; unavailable published tiers remain explicitly unpriced until you set a rate.":
      "Los rollouts de Codex se contabilizan por separado mediante diferencias de entrada nueva, entrada en caché, escritura de caché, salida y salida de razonamiento. La tabla de tarifas de GPT distingue entre contexto corto estándar (≤272K tokens de entrada), contexto largo y modo Fast; los niveles sin tarifa publicada se mantienen explícitamente sin precio hasta que configures uno.",
    "Codex rollout records are the source of truth for live cards: <code>user_message</code> and <code>task_started</code> mark the main agent working; <code>task_complete</code> keeps the session active while showing Waiting; and <code>turn_aborted</code> shows interrupted Waiting. Newest rollouts are processed first, and an unreadable historical file is retried without delaying the rest.":
      "Los registros de rollout de Codex son la fuente de verdad de las tarjetas en vivo: <code>user_message</code> y <code>task_started</code> marcan al agente principal como trabajando; <code>task_complete</code> mantiene activa la sesión mientras muestra Esperando; y <code>turn_aborted</code> muestra Esperando interrumpido. Los rollouts más recientes se procesan primero, y un archivo histórico ilegible se vuelve a intentar sin retrasar el resto.",
    "Codex <code>response_item</code> tool calls are indexed exactly once through a separate rollout cursor, so provider-scoped Workflows shows recorded tool flows, session drill-ins, token/model totals, and <code>context_compacted</code> counts without replaying lifecycle or token accounting. Dynamic Workflows stays hidden in a Codex-only scope because its on-disk journals are a Claude Code feature.":
      "Las llamadas de herramienta <code>response_item</code> de Codex se indexan exactamente una vez mediante un cursor de rollout independiente; por eso Workflows con ámbito de proveedor muestra flujos de herramientas registrados, detalles de sesión, totales de tokens/modelos y recuentos de <code>context_compacted</code> sin repetir el ciclo de vida ni la contabilidad de tokens. Dynamic Workflows permanece oculto en el ámbito exclusivo de Codex porque sus journals en disco son una función de Claude Code.",
    "Safety-net interval (ms) for incremental Codex rollout scanning. Default <code>4000</code>; <code>0</code> disables polling but keeps the watcher.":
      "Intervalo de seguridad (ms) para el análisis incremental de rollouts de Codex. El valor predeterminado es <code>4000</code>; <code>0</code> desactiva el sondeo, pero conserva el vigilante.",
    "How long a hook-only Codex session may leave a reported-finished turn unanswered before its <code>SessionEnd</code> is presumed lost. A run started with <code>codex exec --ephemeral</code> writes no rollout, so it has neither a transcript mtime nor an open file for the liveness probes — the fallback that matters most on Windows, where both probes are unavailable. Only a session already parked by a real <code>Stop</code> hook qualifies, because <code>SessionEnd</code> follows <code>Stop</code> within a few hundred milliseconds. Silence never qualifies: a rollout-less run emits no hooks at all while a tool runs, so an idle timer would complete a live CI build. A session still running self-heals on its next hook.":
      "Cuánto puede una sesión de Codex solo de hooks dejar sin respuesta un turno ya reportado como terminado antes de dar su <code>SessionEnd</code> por perdido. Una ejecución iniciada con <code>codex exec --ephemeral</code> no escribe ningún rollout, por lo que no tiene mtime de transcripción ni archivo abierto para las sondas de vida — el respaldo que más importa en Windows, donde ninguna de las dos sondas está disponible. Solo califica una sesión ya detenida por un hook <code>Stop</code> real, porque <code>SessionEnd</code> llega unos cientos de milisegundos después de <code>Stop</code>. El silencio nunca califica: una ejecución sin rollout no emite ningún hook mientras corre una herramienta, así que un temporizador de inactividad completaría una compilación de CI en curso. Una sesión que siga ejecutándose se autorrepara en su siguiente hook.",
    '<span class="caption-icon">📊</span> <span><strong>Observability · Grafana</strong> — default home dashboard <em>CCAM — Overview</em> (four boards auto-provisioned): live fleet snapshot, database totals, breakdown charts, and rates from <code>/api/metrics</code> scrapes. The npm-only local helper uses <code>admin</code> / <code>admin</code>; container stacks read the admin password from <code>deployments/secrets/grafana-admin-password</code>.</span>':
      '<span class="caption-icon">📊</span> <span><strong>Observabilidad · Grafana</strong> — panel de inicio predeterminado <em>CCAM — Overview</em> con cuatro tableros aprovisionados automáticamente: instantánea de la flota, totales de la base de datos, gráficos de desglose y tasas de los scrapes de <code>/api/metrics</code>. Solo el ayudante local administrado por npm usa <code>admin</code> / <code>admin</code>; las pilas de contenedores leen la contraseña de administración desde <code>deployments/secrets/grafana-admin-password</code>.</span>',
    '<span class="caption-icon">🔥</span> <span><strong>Observability · Prometheus console</strong> — pre-built landing page at <code>/consoles/index.html</code> with live metric cards, session/token tables, and drill-down links into the Graph UI (Prometheus 3.x compatible — queries the HTTP API directly)</span>':
      '<span class="caption-icon">🔥</span> <span><strong>Observabilidad · Consola Prometheus</strong> — página de destino preconstruida en <code>/consoles/index.html</code> Con tarjetas métricas en vivo, tablas de sesiones/tokens y enlaces de profundización a la interfaz de usuario gráfica (compatible con Prometheus 3.x, consulta directamente la API HTTP)</span>',
    '<span class="caption-icon">📈</span> <span><strong>Observability · Prometheus Graph</strong> — run PromQL against scraped CCAM series (e.g. <code>sum(ccam_sessions)</code>, <code>ccam_events_total</code>, <code>rate(ccam_tokens_total[5m])</code>) with starter expressions from the CCAM console and recording rules in <code>monitoring/prometheus/ccam-rules.yml</code></span>':
      '<span class="caption-icon">📈</span> <span><strong>Observabilidad · Gráfico Prometheus</strong> - Ejecute PromQL contra series CCAM raspadas (por ejemplo, <code>sum(ccam_sessions)</code>, <code>ccam_events_total</code>, <code>rate(ccam_tokens_total[5m])</code>) con expresiones iniciales de la consola CCAM y reglas de grabación en <code>monitoring/prometheus/ccam-rules.yml</code></span>',
    '<span class="caption-icon">📊</span> <span><strong>Grafana · CCAM — Overview</strong> — default home dashboard after login on port 3000: fleet snapshot, database totals, breakdown charts, and rates from live <code>/api/metrics</code> scrapes — no sample or synthetic data. The npm-only local helper uses <code>admin</code> / <code>admin</code>; container stacks use <code>deployments/secrets/grafana-admin-password</code>.</span>':
      '<span class="caption-icon">📊</span> <span><strong>Grafana · CCAM — Overview</strong> — panel de inicio predeterminado después de iniciar sesión en el puerto 3000: instantánea de la flota, totales de la base de datos, gráficos de desglose y tasas de scrapes de <code>/api/metrics</code> en vivo, sin datos de muestra ni sintéticos. Solo el ayudante local administrado por npm usa <code>admin</code> / <code>admin</code>; las pilas de contenedores usan <code>deployments/secrets/grafana-admin-password</code>.</span>',
    '<span class="caption-icon">🔥</span> <span><strong>Prometheus · CCAM console</strong> — static HTML at <code>/consoles/index.html</code> (Prometheus 3.x compatible): live metric cards, session/token tables, and one-click Graph drill-down links that query the Prometheus HTTP API directly</span>':
      '<span class="caption-icon">🔥</span> <span><strong>Prometeo · Consola CCAM</strong> — HTML estático en <code>/consoles/index.html</code> (Compatible con Prometheus 3.x): tarjetas métricas en vivo, tablas de sesiones/tokens y enlaces de profundización gráfica con un solo clic que consultan la API HTTP de Prometheus directamente.</span>',
    '<span class="caption-icon">📈</span> <span><strong>Prometheus · Graph</strong> — ad-hoc PromQL against scraped CCAM series; starter expressions ship in the CCAM console and <code>monitoring/README.md</code>, with derived rates in <code>monitoring/prometheus/ccam-rules.yml</code></span>':
      '<span class="caption-icon">📈</span> <span><strong>Prometeo · Gráfico</strong> — PromQL ad hoc contra series CCAM raspadas; las expresiones iniciales se envían en la consola CCAM y <code>monitoring/README.md</code>, con tasas derivadas en <code>monitoring/prometheus/ccam-rules.yml</code></span>',
    "CCAM exposes a read-only <strong>Prometheus text-exposition</strong> endpoint at <code>GET /api/metrics</code> — sessions and agents by status, cumulative events and tokens, connected WebSocket clients, configured remote sources, process uptime/RSS, and build version — so the same dashboard you use for Claude Code monitoring can be scraped into your own observability stack. A turnkey stack lives in <code>monitoring/</code>: npm-managed binaries (no Homebrew/apt) or optional Docker Compose, with Grafana datasource and four dashboards auto-provisioned on first start.":
      "CCAM expone un solo modo de lectura <strong>Exposición textual de Prometeo</strong> Punto final en <code>GET /api/metrics</code> — sesiones y agentes por estado, eventos acumulativos y tokens, clientes WebSocket conectados, fuentes remotas configuradas, tiempo de actividad del proceso/RSS y versión de la compilación, para que el mismo panel de control que utiliza para el monitoreo de Claude Code pueda escrapearse en su propia pila de observabilidad. Una pila llave en mano reside en <code>monitoring/</code>: binarios gestionados por npm (sin Homebrew/apt) o Docker Compose opcional, con fuente de datos Grafana y cuatro paneles de control provistos automáticamente en la primera iniciación.",
    "<strong>Quick start (npm):</strong> run the dashboard on loopback, then <code>npm run monitoring:install</code> once and <code>npm run monitoring:up</code>. Open Grafana at <code>http://localhost:3000</code> and Prometheus at <code>http://localhost:9090</code>. The pre-built CCAM console is at <code>http://localhost:9090/consoles/index.html</code> (also reachable from the Prometheus UI under <strong>Consoles</strong>). For an all-Docker path use <code>npm run monitoring:docker:up</code> or <code>npm run docker:full:up</code> for dashboard + Prometheus + Grafana together. Verify with <code>npm run monitoring:verify</code> — the <code>ccam</code> scrape target should read <strong>UP</strong>.":
      "<strong>Inicio rápido (npm):</strong> Ejecuta el panel de control en bucle, luego <code>npm run monitoring:install</code> Una vez y <code>npm run monitoring:up</code>. Grafana en <code>http://localhost:3000</code> Y Prometeo en <code>http://localhost:9090</code>. console CCAM preconstruida está en <code>http://localhost:9090/consoles/index.html</code> (también accesible desde la interfaz de usuario de Prometheus en <strong>Consolas</strong>). Para un uso de ruta todo-Docker <code>npm run monitoring:docker:up</code> Oregón <code>npm run docker:full:up</code> Para panel de control + Prometheus + Grafana juntos. Verifique con <code>npm run monitoring:verify</code> — el <code>ccam</code> El objetivo de la raspadura debería leer <strong>UP</strong>.",
    "Four Grafana boards": "Cuatro tableros Grafana",
    "<strong>CCAM — Overview</strong> (default home), <strong>CCAM — Sessions &amp; Agents</strong>, <strong>CCAM — Tokens &amp; Events</strong>, and <strong>CCAM — Platform</strong> — all provisioned from <code>monitoring/grafana/dashboards/</code> with PromQL against your live scrape. The npm-only local helper uses <code>admin</code> / <code>admin</code> via <code>monitoring/grafana.defaults.env</code>. Container stacks read the password from <code>deployments/secrets/grafana-admin-password</code>.":
      "<strong>CCAM — Overview</strong> (inicio predeterminado), <strong>CCAM — Sessions &amp; Agents</strong>, <strong>CCAM — Tokens &amp; Events</strong> y <strong>CCAM — Platform</strong> se aprovisionan desde <code>monitoring/grafana/dashboards/</code> con PromQL sobre el scrape en vivo. Solo el ayudante local administrado por npm usa <code>admin</code> / <code>admin</code> mediante <code>monitoring/grafana.defaults.env</code>. Las pilas de contenedores leen la contraseña desde <code>deployments/secrets/grafana-admin-password</code>.",
    "Prometheus console": "Consola Prometheus",
    "Prometheus 3.x dropped the legacy console template libraries, so CCAM ships a <strong>static HTML console</strong> at <code>monitoring/prometheus/consoles/index.html</code> that fetches <code>/api/v1/query</code> directly — live cards, session tables, and Graph links without Go-template errors on <code>/consoles/index.html</code>.":
      "Prometheus 3.x eliminó las bibliotecas de plantillas de consola heredadas, por lo que CCAM incluye un <strong>Consola HTML estática</strong> att <code>monitoring/prometheus/consoles/index.html</code> Que recoge <code>/api/v1/query</code> Directamente: tarjetas en vivo, tablas de sesión y enlaces a gráficos sin errores de plantilla Go en <code>/consoles/index.html</code>.",
    "Recording rules": "Reglas de grabación",
    "<code>monitoring/prometheus/ccam-rules.yml</code> derives convenient rates and aggregates from raw <code>ccam_*</code> series (e.g. <code>rate(ccam_tokens_total[5m])</code>) so Grafana panels and ad-hoc Graph queries stay fast without hand-rolling every expression.":
      "<code>monitoring/prometheus/ccam-rules.yml</code> Deriva tarifas convenientes y agrupa datos brutos <code>ccam_*</code> Serie (por ejemplo <code>rate(ccam_tokens_total[5m])</code>) por lo que los paneles de Grafana y las consultas gráficas ad hoc se mantienen rápidas sin tener que ejecutar manualmente cada expresión.",
    "Scrape &amp; auth guard": "Raspar y proteger la autenticación",
    "Loopback scrapes (<code>127.0.0.1:4820</code>) work out of the box. When Prometheus runs in Docker and reaches the host via <code>host.docker.internal</code>, add that host to <code>DASHBOARD_ALLOWED_HOSTS</code> or the scrape returns <code>403 EBADHOST</code>. If <code>DASHBOARD_TOKEN</code> is set, scrapers must send the same bearer token.":
      "Rasguños de retroceso (<code>127.0.0.1:4820</code>) funciona de inmediato. Cuando Prometheus se ejecuta en Docker y llega al host a través de <code>host.docker.internal</code>, agrega ese anfitrión a <code>DASHBOARD_ALLOWED_HOSTS</code> O el rasguño vuelve <code>403 EBADHOST</code>. si <code>DASHBOARD_TOKEN</code> Está establecido, los raspadores deben enviar el mismo token portador.",
    "Full reference": "Referencia completa",
    'Commands, Docker vs npm paths, dashboard UIDs, starter PromQL, and troubleshooting (Grafana login reset, target DOWN) are documented in <a href="../monitoring/README.md"><code>monitoring/README.md</code></a> and the <a href="#api-reference">API reference</a> entry for <code>GET /api/metrics</code>.':
      'Los comandos, rutas de Docker vs npm, UIDs del panel de control, PromQL de inicio y resolución de problemas (restablecimiento de la sesión de Grafana, objetivo DESPUESTO) están documentados en <a href="../monitoring/README.md"><code>monitoring/README.md</code></a> Y el <a href="#api-reference">Referencia de API</a> Entrada para <code>GET /api/metrics</code>.',
    "Pull Claude Code history from other machines over SSH. Each remote's <code>~/.claude/projects</code> tree is mirrored via <code>scp</code> into a sandboxed staging directory, fed through the same importer as local history, and every session is tagged with its source. A background poller keeps remote data near-real-time, a global data-scope selector (Local only / All / a specific source) narrows the whole app, and sessions from a remote show a source badge. Auth defers entirely to the host's SSH stack — no passwords or secrets are stored. Managed in <b>Settings → Remote Data Sources</b>.":
      "Sube la historia del código Claude de otras máquinas a través de SSH. Cada máquina remota <code>~/.claude/projects</code> El árbol se refleja a través de <code>scp</code> En una carpeta de ensayo sandbox, alimentada por el mismo importador que el historial local, y cada sesión se etiqueta con su fuente. Un monitor de fondo mantiene los datos remotos casi en tiempo real, un selector global de alcance de datos (Solo local / Todos / una fuente específica) reduce toda la aplicación, y las sesiones de un remoto muestran un distintivo de fuente. La autenticación se deja completamente en manos de la pila SSH del host, no se almacenan contraseñas ni secretos. Administrado en <b>Configuración → Fuentes de datos remotas</b>.",
    "Remote Data Sources": "Fuentes de datos remotas",
    "One remote source can independently import <b>Claude Code</b>, <b>Codex</b>, or both. Codex mirrors <code>~/.codex/sessions</code> and its lightweight <code>session_index.jsonl</code> title index; the connection test and sync result show a separate state for each provider, so a missing provider never hides fresh data from the other.":
      "Una fuente remota puede importar de forma independiente <b>Claude Code</b>, <b>Codex</b> o ambos. Codex refleja <code>~/.codex/sessions</code> y su ligero índice de títulos <code>session_index.jsonl</code>; la prueba de conexión y el resultado de sincronización muestran un estado separado para cada proveedor, por lo que uno ausente nunca oculta los datos recientes del otro.",
    "List, add, and test SSH data sources; sync one source (or all when no id is given); and remove a source, optionally purging the sessions it imported":
      "Listar, agregar y probar fuentes de datos SSH; sincronizar una fuente (o todas cuando no se proporciona un ID); y eliminar una fuente, opcionalmente borrando las sesiones que importó.",
    "Beyond local history, the dashboard can pull Claude Code activity from <b>other machines over SSH</b> and fold it into the same views as your local sessions. Each remote's <code>~/.claude/projects</code> tree is mirrored via <code>scp</code> (or <code>wsl.exe</code> + <code>tar</code> when Claude runs in WSL on a Windows SSH host) into a sandboxed staging directory, fed through the exact same importer used for local history, and every imported session is tagged with the source it came from. A background poller (<code>DASHBOARD_REMOTE_SYNC_MS</code>, default <code>15000</code> ms) keeps remote data near-real-time — adding or re-enabling a source also triggers an immediate pull — and successful syncs broadcast <code>remote_data.updated</code> plus per-session <code>session_created</code> / <code>session_updated</code> frames. A global <b>data scope</b> selector lets you narrow the entire app to local sessions only, all sources, or a specific machine. Sources are managed in <b>Settings → Remote Data Sources</b>.":
      "Más allá de la historia local, el panel de control puede obtener la actividad de Claude Code de <b>Otras máquinas a través de SSH</b> Y cómpralo en las mismas vistas que tus sesiones locales. Cada remoto <code>~/.claude/projects</code> El árbol se refleja a través de <code>scp</code> (o <code>wsl.exe</code> + <code>tar</code> Cuando Claude se ejecuta en WSL en un host SSH de Windows) en una carpeta de ensayo sandbox, alimentada por el mismo importador exacto utilizado para el historial local, y cada sesión importada se etiqueta con la fuente de la que procede. Un píldora de seguimiento en segundo plano (<code>DASHBOARD_REMOTE_SYNC_MS</code>, predeterminado <code>15000</code> ms) mantiene los datos remotos en tiempo real cercano, agregar o volver a habilitar una fuente también desencadena una extracción inmediata, y las sincronizaciones exitosas transmiten <code>remote_data.updated</code> Más por sesión <code>session_created</code> / <code>session_updated</code> Marco. Un global <b>Ámbito de los datos</b> El selector te permite restringir toda la aplicación a sesiones locales solo, todas las fuentes o una máquina específica. Las fuentes se gestionan en <b>Configuración → Fuentes de datos remotas</b>.",
    "Each remote is mirrored over SSH, imported through the local pipeline, and tagged by source — the poller (default 15 s) keeps it near-real-time":
      "Cada remoto se refleja a través de SSH, se importa a través del pipeline local y se etiqueta por fuente: el poller (por defecto 15 s) lo mantiene casi en tiempo real.",
    "Add a source with an SSH destination — either a <code>user@host</code> pair or an alias defined in your <code>~/.ssh/config</code> — plus an optional port, identity file, and remote home directory. Use <b>Test connection</b> to verify reachability before saving, then <b>Sync now</b> to pull immediately, or let the background poller do it on its own schedule. Removing a source can optionally purge the sessions it imported.":
      "Agregue una fuente con un destino SSH, ya sea un <code>user@host</code> Pareja o un alias definido en tu <code>~/.ssh/config</code> — además de un puerto opcional, un archivo de identidad y una carpeta de inicio remota. uso <b>Prueba de conexión</b> Para verificar la accesibilidad antes de guardar, luego <b>Sincronizar ahora</b> Para descargar inmediatamente, o dejar que el monitor de fondo lo haga a su propio ritmo. Eliminar una fuente puede purgar opcionalmente las sesiones que importó.",
    "A global data-scope selector narrows the whole app: choose <b>Local only</b>, <b>All</b> sources, or one specific machine. The scope flows into every data view through a <code>sources</code> query parameter on the sessions, events, agents, stats, and analytics endpoints, and sessions imported from a remote carry a <b>source badge</b> so their origin is always visible.":
      "Un selector de alcance de datos global reduce toda la aplicación: elija <b>Solo local</b>, <b>todos</b> Fuentes, o una máquina específica. El alcance fluye a través de cada vista de datos a través de un <code>sources</code> Parámetro de consulta en los puntos finales de sesiones, eventos, agentes, estadísticas y análisis, y las sesiones importadas desde una ubicación remota llevan un <b>Insignia de fuente</b> Así que su origen siempre es visible.",
    "Authentication defers entirely to the host's own SSH stack — <code>~/.ssh/config</code>, <code>ssh-agent</code>, on-disk keys, and <code>known_hosts</code>. The dashboard stores <b>no passwords or secrets</b>; it only records the SSH destination and connection options you supply. <code>StrictHostKeyChecking</code> is left at its default, so the remote host must already be present in <code>known_hosts</code> — an unknown host fails the connection rather than being trusted blindly.":
      "La autenticación se deja totalmente a la propia pila SSH del host — <code>~/.ssh/config</code>, <code>ssh-agent</code>, claves en disco y <code>known_hosts</code>. del panel de instrumentos almacena <b>Sin contraseñas ni secretos</b>; solo registra el destino SSH y las opciones de conexión que proporcione. <code>StrictHostKeyChecking</code> Se deja en su configuración predeterminada, por lo que el host remoto ya debe estar presente en <code>known_hosts</code> — un anfitrión desconocido falla la conexión en lugar de ser confiado ciegamente.",
    "Interval for the background remote poller (add/re-enable also syncs immediately); <code>0</code> disables the poller while leaving manual sync available":
      "Intervalo para el recuento remoto de fondo (agregar/reactivar también se sincroniza inmediatamente); <code>0</code> Desactiva el poller mientras deja la sincronización manual disponible",
    "Ceiling on a single scp-and-import run before it is aborted":
      "Techo en una sola ejecución de scp-and-import antes de que se aborte",
    "Timeout for the <b>Test connection</b> probe":
      "Tiempo de espera para el <b>Prueba de conexión</b> catéter",
    "Freshness window (10 min) for a Remote Data Source session's live status — judged from the newest JSONL event timestamp in the mirror (mtime fallback); remote sessions receive no live hooks, so this replaces the local liveness and stale sweeps (which skip them)":
      "Ventana de frescura (10 min) para el estado en vivo de una sesión de Fuente de Datos Remota, evaluado por el tiempo de inicio del evento JSONL más reciente en el espejo (mtime fallback); las sesiones remotas no reciben ningún gancho en vivo, por lo que esto reemplaza la vitalidad local y las escaneos obsoletos (que los saltan)",
    "Freshness window (10 min) for a Remote Data Source session's live status — judged from the newest JSONL event timestamp in the mirror (mtime fallback); healthy sources stay out of local liveness/stale checks, while errored or stale-syncing sources fall back to the global abandon sweep":
      "Ventana de frescura (10 min) para el estado en vivo de una sesión de Fuente de Datos Remota, evaluado por la marca de tiempo del evento JSONL más reciente en la réplica (con mtime como respaldo); las fuentes sanas quedan fuera de las comprobaciones locales de actividad/antigüedad, mientras que las fuentes con error o syncing prolongado recurren al barrido global de abandono",
    "Because a remote session is pulled over SSH, it never emits live hooks to this dashboard, so remote sessions (<code>source</code> ≠ <code>local</code>) are excluded from every local liveness and stale sweep — the process liveness reap, the watchdog transcript scan, the startup cleanup, and the periodic abandon sweep all skip them. Instead, a remote session's live status is reconciled from the mirrored transcript on every sync: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime). If the last event is within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) the session stays <code>active</code>; once the mirror stops advancing for longer than that window, the session is reconciled to <code>completed</code>. This keeps a still-running remote session from being wrongly shown as Completed.":
      "Debido a que una sesión remota se ejecuta a través de SSH, nunca emite ganchos en vivo a este panel de control, por lo que las sesiones remotas (<code>source</code> ≠ <code>local</code>) están excluidos de cada limpieza local de actividad y antigüedad: la actividad de recogida del proceso, la escanear de transcripciones de vigilancia, la limpieza de inicio de sesión y la limpieza periódica de abandono todos los pasan por alto. En cambio, el estado de actividad de una sesión remota se reconcilia a partir de la transcripción reflejada en cada sincronización: la actividad se juzga a partir del <b>Fecha y hora del último evento dentro del JSONL</b> (volviendo al tiempo de mapeo del espejo). Si el último evento está dentro de <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (por defecto 10 minutos) la sesión se mantiene <code>active</code>; una vez que el espejo deja de avanzar durante más tiempo que esa ventana, la sesión se reconcilia con <code>completed</code>. sesión remota en ejecución se muestra correctamente como completada.",
    "Remote sessions never emit live hooks to this dashboard, so a healthy source remains exempt from the local process-liveness reap and watchdog transcript scan. Every successful sync still reconciles live status from the mirrored transcript: activity is judged from the <b>newest event timestamp inside the JSONL</b> (falling back to mirror mtime); within <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (default 10 minutes) keeps the session <code>active</code>, otherwise it is reconciled to <code>completed</code>. If an SSH sync fails — or a source is stranded in <code>syncing</code> longer than <code>DASHBOARD_STALE_MINUTES</code> — and an active session is itself older than that same window, the ordinary stale-session sweep marks it <code>abandoned</code> and completes its agents. A later fresh mirror reactivates a session that is still writing, so unavailable remotes never leave permanent Waiting cards.":
      "Las sesiones remotas nunca emiten hooks en vivo a este panel, por lo que una fuente en buen estado sigue excluida de la comprobación local de procesos y del análisis de transcript del watchdog. Cada sincronización correcta sigue reconciliando el estado en vivo desde el transcript replicado: la actividad se determina a partir de la <b>marca de tiempo del evento más reciente dentro del JSONL</b> (con mtime de la réplica como respaldo); estar dentro de <code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code> (10 minutos de forma predeterminada) mantiene la sesión <code>active</code>; de lo contrario se reconcilia como <code>completed</code>. Si falla una sincronización SSH —o una fuente permanece en <code>syncing</code> más de <code>DASHBOARD_STALE_MINUTES</code>— y una sesión activa supera esa misma ventana, el barrido normal de sesiones obsoletas la marca <code>abandoned</code> y completa sus agentes. Una réplica nueva posterior reactiva una sesión que sigue escribiendo, por lo que los remotos no disponibles nunca dejan tarjetas En espera permanentes.",
    "List configured sources with their status": "Lista de fuentes configuradas con su estado",
    "Add a source — SSH destination plus optional port, identity file, and remote home":
      "Agregue una fuente: destino SSH más puerto opcional, archivo de identidad y hogar remoto",
    "Edit an existing source": "Editar una fuente existente",
    "Remove a source; optionally purge the sessions it imported":
      "Eliminar una fuente; opcionalmente purgar las sesiones que importó",
    "Test SSH reachability without importing anything":
      "Prueba la accesibilidad SSH sin importar nada",
    "Trigger an immediate scp-and-import for that source":
      "Desatará un scp-y-importación inmediata para esa fuente",
    "The <code>sources</code> query parameter is also accepted on <code>/api/sessions</code>, <code>/api/events</code>, <code>/api/agents</code>, <code>/api/stats</code>, and <code>/api/analytics</code> to scope any view to <code>local</code>, <code>all</code>, or a comma-separated list of source IDs.":
      "el <code>sources</code> El parámetro de consulta también se acepta en <code>/api/sessions</code>, <code>/api/events</code>, <code>/api/agents</code>, <code>/api/stats</code>, y <code>/api/analytics</code> Para delimitar cualquier vista a <code>local</code>, <code>all</code>, o una lista separada por comas de identificadores de origen.",
    "Sync progress and connection state broadcast on <code>/ws</code> as <code>remote_source.status</code> messages, so the Settings panel and the source badges update live as a remote connects, syncs, or fails.":
      "Transmite el progreso de sincronización y el estado de conexión en <code>/ws</code> arsénico <code>remote_source.status</code> Mensajes, por lo que el panel de Configuración y las insignias de origen se actualizan en vivo a medida que se conecta, sincroniza o falla una fuente remota.",
    "The same operations are available from the terminal via <code>ccam remote-sources</code>, with subcommands <code>list</code>, <code>add</code>, <code>test &lt;id&gt;</code>, <code>sync [id]</code> (all sources when no id is given), and <code>rm &lt;id&gt; [--purge]</code> to remove a source and optionally drop the sessions it imported.":
      "Las mismas operaciones están disponibles desde el terminal a través de <code>ccam remote-sources</code>, con subcomandos <code>list</code>, <code>add</code>, <code>test &lt;id&gt;</code>, <code>sync [id]</code> (todas las fuentes cuando no se da un ID), y <code>rm &lt;id&gt; [--purge]</code> Para eliminar una fuente y, opcionalmente, eliminar las sesiones que importó.",
    "Local-only by default": "Solo local por defecto",
    "The image bakes <code>DASHBOARD_HOST=0.0.0.0</code> (the server binds loopback by default, but a container's loopback is a separate namespace the published port cannot reach) and <code>DASHBOARD_DATA_DIR=/app/data</code> (the read-only <code>~/.claude</code> mount cannot hold the SQLite data dir). The trust boundary is the host port publish: the examples use <code>-p 127.0.0.1:4820:4820</code>, so the dashboard is local-only. To expose it on a LAN, publish on <code>0.0.0.0</code> (<code>-p 4820:4820</code>) and set <code>DASHBOARD_TOKEN</code>.":
      "La imagen se hornea <code>DASHBOARD_HOST=0.0.0.0</code> (el servidor vincula el bucle de vuelta por defecto, pero el bucle de vuelta de un contenedor es un namespace separado al que la puerto publicado no puede llegar) y <code>DASHBOARD_DATA_DIR=/app/data</code> (el de solo lectura <code>~/.claude</code> El montaje no puede mantener el directorio de datos SQLite). El límite de confianza es la publicación de la puerto del host: los ejemplos utilizan <code>-p 127.0.0.1:4820:4820</code>, por lo que el panel de control es solo local. Para exponerlo en una LAN, publique en <code>0.0.0.0</code> (<code>-p 4820:4820</code>) y establecer <code>DASHBOARD_TOKEN</code>.",
    "The CLI also manages the server itself: <code>ccam status</code> shows an up/down indicator and <code>ccam start</code> boots a detached production server. When the server is down, read-only commands fall back to reading <code>data/dashboard.db</code> directly under an explicit offline banner, while live or server-computed commands refuse with the specific reason.":
      "El CLI también gestiona el propio servidor: <code>ccam status</code> Muestra un indicador de arriba/abajo y <code>ccam start</code> Inicia sesión en un servidor de producción independiente. Cuando el servidor está inactivo, los comandos de solo lectura se vuelven a leer <code>data/dashboard.db</code> Directamente debajo de un banner explícito de modo sin conexión, mientras que los comandos en vivo o calculados por el servidor se niegan con la razón específica.",
    "The full dashboard feature surface is also available from any terminal through the dependency-free <code>ccam</code> CLI (<code>bin/ccam.js</code>). It is linked automatically by <code>npm run setup</code> (via a fail-soft <code>npm link</code>), discovers the running server through the same <code>~/.claude/.agent-dashboard.json</code> registry the hook handler uses (override with <code>CLAUDE_DASHBOARD_PORT</code> / <code>DASHBOARD_PORT</code>), and renders a full terminal UI — box-drawn tables, status icons, inline bar charts, and agent trees — that degrades to plain text when piped (<code>--no-color</code> / <code>NO_COLOR</code> / <code>FORCE_COLOR</code> respected), so it is equally at home in an interactive shell, <code>grep</code> pipelines, or CI.":
      "La superficie completa de la función del panel de control también está disponible desde cualquier terminal a través del sin dependencias <code>ccam</code> CLI (<code>bin/ccam.js</code>). Está vinculado automáticamente por <code>npm run setup</code> (a través de un fallo suave <code>npm link</code>), descubre el servidor en ejecución a través del mismo <code>~/.claude/.agent-dashboard.json</code> Registrar el gestor de ganchos que utiliza (sustituir con <code>CLAUDE_DASHBOARD_PORT</code> / <code>DASHBOARD_PORT</code>), y genera una interfaz de usuario completa para terminales: tablas dibujadas en cuadrados, iconos de estado, gráficos de barras en línea y árboles de agentes, que se degradan a texto plano cuando se canaliza (<code>--no-color</code> / <code>NO_COLOR</code> / <code>FORCE_COLOR</code> respetado), por lo que está igualmente en su salsa en una cáscara interactiva, <code>grep</code> Trenes de gas, o CI.",
    "For a live monitoring session, <code>ccam repl</code> (aliases <code>shell</code> / <code>i</code>) opens an interactive shell where you type commands without the <code>ccam</code> prefix — with a CCAM welcome banner, tab-completion, persisted arrow-key history, a live server-status prompt (<code>●</code> up / <code>○</code> offline), a grouped <code>help</code> / <code>help &lt;cmd&gt;</code> menu, and a <code>watch [secs] &lt;cmd&gt;</code> built-in that auto-refreshes any command. Each entered line runs as an isolated child process, so an offline refusal or a blocking <code>tail</code> never takes the shell down.":
      "Para una sesión de monitoreo en vivo, <code>ccam repl</code> (alias <code>shell</code> / <code>i</code>) abre una cáscara interactiva donde escribes comandos sin el <code>ccam</code> prefijo — con un banner de bienvenida CCAM, completación de pestañas, historial persistente de teclas de flecha, una solicitud de estado del servidor en vivo (<code>●</code> Arriba / <code>○</code> Desconectado), un agrupado <code>help</code> / <code>help &lt;cmd&gt;</code> Menú, y un <code>watch [secs] &lt;cmd&gt;</code> Instalado que se actualiza automáticamente cualquier comando. Cada línea ingresada se ejecuta como un proceso hijo aislado, por lo que una negativa sin conexión o un bloqueo <code>tail</code> Nunca baja la cáscara.",
    Group: "grupo",
    Commands: "Comandos",
    "What you get": "Lo que obtienes",
    Monitoring: "monitoreo",
    "Reachability, totals and status distributions, the Kanban board rendered as text columns, and a live event feed that polls for new rows every 2 seconds":
      "Alcance, totales y distribuciones de estado, la pizarra Kanban renderizada como columnas de texto y un feed de eventos en vivo que encuesta por nuevas filas cada 2 segundos",
    "Up/down indicator, a background server start that waits for health, and the interactive <code>ccam repl</code> shell — tab-completion, persisted history, a live server-status prompt, and a <code>watch</code> auto-refresh":
      "Indicador de arriba/abajo, un inicio de servidor de fondo que espera la salud, y el interactivo <code>ccam repl</code> Shell — completación de pestañas, historial persistente, una solicitud de estado del servidor en vivo y un <code>watch</code> Actualizar automático",
    Data: "información",
    "Filterable tables plus a per-session deep dive with an indented agent tree, per-session cost, and the most recent events":
      "Tablas filtrables más un análisis profundo por sesión con un árbol de agentes hundido, costo por sesión y los eventos más recientes",
    Insights: "Perspectivas",
    "Token totals and top tools, workflow-intelligence stats with detected patterns, dynamic Workflow-tool runs, and the per-model cost breakdown, which can be scoped to a single session with <code>--session</code>, surfaces server-tool surcharges (web search / code execution), warns about models with usage but no matching pricing rule (priced at $0, excluded from the total), and points to <code>ccam pricing set</code>":
      "Totales de tokens y herramientas principales, estadísticas de inteligencia de flujo de trabajo con patrones detectados, ejecuciones dinámicas de herramientas de flujo de trabajo y la desglose de costos por modelo, que se pueden limitar a una sola sesión con <code>--session</code>, muestra recargos por herramientas de servidor de las superficies (búsqueda web / ejecución de código), advierte sobre modelos con uso pero sin una regla de precios correspondiente (preciados a 0 $, excluidos del total) y señala a <code>ccam pricing set</code>",
    "Alerts &amp; Webhooks": "Alertas y Webhooks",
    "The fired-alert feed with acknowledge / acknowledge-all, the rule list, and synthetic test deliveries against any webhook target":
      "El feed de alerta de disparo con reconocimiento / reconocimiento-todo, la lista de reglas y entregas de pruebas sintéticas contra cualquier objetivo webhook",
    Pricing: "fijación de precios",
    "Full pricing-rule CRUD with per-mtok rates — including 1-hour cache-write, fast-tier, and time-limited intro rates — straight from the terminal, with Fast In/Out and Intro In/Out columns in the list view":
      "Regla de precios completa CRUD con tarifas por mtok, incluyendo tarifas de escritura de caché de 1 hora, nivel rápido y tarifas de introducción con tiempo limitado, directamente desde el terminal, con las columnas Fast In/Out y Intro In/Out en la vista de lista.",
    Import: "importación",
    "The same idempotent ingestion pipeline the Settings page uses, reporting imported / backfilled / skipped / error counts":
      "El mismo pipeline de ingestión idempotente que utiliza la página de Configuración, que informa de los conteos de importación / rellenado / saltos / errores",
    Administration: "administración",
    "Connectivity and hook diagnosis, raw system info, full JSON export, session cleanup, hook reinstallation, an update check that prints how far behind the canonical remote you are plus the copy-paste update command, and a guarded wipe that refuses to run without <code>--yes</code>":
      "Conectividad y diagnóstico de ganchos, información del sistema bruta, exportación completa de JSON, limpieza de sesiones, reinstalación de ganchos, una comprobación de actualización que imprime cuánto estás atrasado con respecto al remoto canónico, además del comando de actualización de copia y pegar, y una borrado protegido que se niega a ejecutarse sin <code>--yes</code>",
    "Safe by default": "Seguro por defecto",
    "Read commands only issue GETs; mutating commands map one-to-one to explicit dashboard actions; and the single destructive command, <code>clear-data</code>, always refuses to run without an explicit <code>--yes</code> flag. Exit codes are script-friendly: 0 on success, 1 on any failure. See <code>docs/CLI.md</code> for the complete reference.":
      "Los comandos de lectura solo emiten GETs; los comandos mutantes se mapean de forma uno a uno a las acciones explícitas del panel de control; y el único comando destructivo, <code>clear-data</code>, siempre se niega a correr sin un explícito <code>--yes</code> Bandera. Los códigos de salida son compatibles con scripts: 0 en caso de éxito, 1 en cualquier fallo. ver <code>docs/CLI.md</code> Para la referencia completa.",
    "The whole dashboard as terminal commands — <code>ccam stats</code>, <code>kanban</code>, <code>tail</code>, <code>session &lt;id&gt;</code>, <code>analytics</code>, <code>alerts</code>, <code>pricing</code>, <code>import</code>, <code>doctor</code>, <code>export</code> and more. Zero dependencies, linked by <code>npm run setup</code>, auto-discovers the running server, renders a full terminal UI (box-drawn tables, status icons, inline bar charts, agent trees), and pipes cleanly (colors off when not a TTY). The one destructive command is gated behind <code>--yes</code>.":
      "Todo el panel de control como comandos terminales — <code>ccam stats</code>, <code>kanban</code>, <code>tail</code>, <code>session &lt;id&gt;</code>, <code>analytics</code>, <code>alerts</code>, <code>pricing</code>, <code>import</code>, <code>doctor</code>, <code>export</code> Y más. Cero dependencias, vinculadas por <code>npm run setup</code>, descubre automáticamente el servidor en ejecución, genera una interfaz de usuario completa del terminal (tablas dibujadas en cuadrados, iconos de estado, gráficos de barras en línea, árboles de agentes) y canaliza de forma limpia (colores apagados cuando no es un TTY). El único comando destructivo está bloqueado detrás de <code>--yes</code>.",
    '<span class="caption-icon">📡</span> Live dashboard — real-time agent cards, stats, and activity feed':
      '<span class="caption-icon">📡</span> Panel de control en vivo: tarjetas de agentes en tiempo real, estadísticas y flujo de actividad',
    "Claude Code Agent Monitor integrates with Claude Code through its native hook system. When Claude Code performs any action — tool use, session start, subagent orchestration, session end — it fires a hook that calls a small Node.js script bundled with this project. That script forwards the event over HTTP to the dashboard server, which stores it in SQLite and broadcasts it to the browser over WebSocket.":
      "El Monitor de Agentes de Claude Code se integra con Claude Code a través de su sistema de ganchos nativo. Cuando Claude Code realiza cualquier acción, como el uso de herramientas, el inicio de sesión, la orquestación de subagentes o el final de sesión, dispara un gancho que llama a un pequeño script Node.js incluido en este proyecto. Ese script envia el evento por HTTP al servidor del panel de control, que lo almacena en SQLite y lo transmite al navegador a través de WebSocket.",
    "End-to-end data pipeline from Claude Code to the browser":
      "Pipeline de datos de extremo a extremo desde Claude Code hasta el navegador",
    "Local-first by design": "Primero local por diseño",
    "The server binds <code>127.0.0.1</code> (loopback) by default, so it is not network-reachable and everything runs on your machine. No data leaves your system. No API keys. No external services. Exposing it more widely is opt-in via <code>DASHBOARD_HOST</code> and should be paired with <code>DASHBOARD_TOKEN</code>.":
      "El servidor se vincula <code>127.0.0.1</code> (loopback) por defecto, por lo que no es accesible desde la red y todo se ejecuta en su máquina. Ningún dato sale de su sistema. Ningunas claves API. Ningún servicio externo. Exponerlo más ampliamente es opcional a través de <code>DASHBOARD_HOST</code> Y debería combinarse con <code>DASHBOARD_TOKEN</code>.",
    "Every feature is driven by real hook events — nothing is hardcoded or simulated in production mode.":
      "Cada función está impulsada por eventos de conexión reales, nada está codificado de forma estática ni se simula en modo de producción.",
    "Two tabs: <strong>Monitor</strong> shows overview stats, active agent cards with collapsible subagent hierarchy, and a recent activity feed whose item count fills available viewport height. <strong>Health</strong> renders a composite system health score ring, storage engine donut chart, cache/error/success gauges, tool invocation bars, subagent effectiveness ratios, model token distribution, and compaction stats. Both tabs auto-refresh every 5 seconds via WebSocket push so the view is always current without manual reload.":
      "Dos pestañas: <strong>monitor</strong> muestra estadísticas generales, tarjetas de agentes activos con jerarquía de subagentes colapsable y un feed de actividad reciente cuyo número de elementos llena la altura disponible de la ventana de visualización. <strong>bienestar</strong> Genera un anillo de puntuación de salud del sistema compuesto, un gráfico de donut del motor de almacenamiento, indicadores de caché/errores/éxito, barras de invocación de herramientas, índices de eficacia de subagentes, distribución de tokens del modelo y estadísticas de compactación. Ambas pestañas se actualizan automáticamente cada 5 segundos a través de WebSocket push, por lo que la vista siempre es actual sin necesidad de recarga manual.",
    "Toggle between <strong>Agents</strong> (Working / Waiting / Completed / Error) and <strong>Sessions</strong> (Active / Waiting / Completed / Error / Abandoned) swim lanes. A yellow <strong>Waiting</strong> column flags items sitting on the user — fresh prompt, between turns, or permission gate. Hover any column header for lifecycle tooltips explaining each state transition. Cards surface model name, cumulative cost, and the current tool being called. Counts update in real time via WebSocket so the board is always in sync with the live event store. Hover a Waiting badge to see why — needs input, turn done, at prompt, or interrupted.":
      "Alternar entre <strong>Agentes</strong> (Trabajando / Esperando / Terminado / Error) y <strong>sesión</strong> (Lanes de natación activas / en espera / completadas / error / abandonadas). Un amarillo <strong>espera</strong> Las banderas de la columna indican los elementos que se encuentran en el usuario: mensaje de inicio de sesión actual, entre turnos o puerta de permisos. Haga clic sobre cualquier encabezado de columna para ver las herramientas de ayuda del ciclo de vida que explican cada transición de estado. Las tarjetas muestran el nombre del modelo de superficie, el costo acumulado y la herramienta actual que se está llamando. Las cuentas se actualizan en tiempo real a través de WebSocket, por lo que la tabla siempre está sincronizada con el almacén de eventos en vivo. Haga clic en un distintivo de Esperando para ver por qué: necesita entrada, turno terminado, en el mensaje de inicio de sesión o interrumpido.",
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, and estimated cost. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "<strong>Paginado por servidor</strong> Tabla de cada sesión registrada: cada página solo recupera su fracción, por lo que el cálculo de costos se mantiene limitado independientemente de cuántas sesiones existan. Búsqueda insensible a mayúsculas y minúsculas en todo el <code>id</code>, <code>name</code>, y <code>cwd</code> Funciona en el lado del servidor con un retraso de 300 ms; el filtro de estado se compone con la búsqueda para un estrechamiento preciso. Cada fila muestra el nombre real de la sesión (sincronizado en vivo desde la transcripción, un <code>/rename</code> Oregón <code>claude -n</code> Título, de lo contrario el título automático, de lo contrario la primera solicitud del usuario, con un fallback de ID corto), distintivo de estado, número de agentes, duración, modelo y costo estimado. Haga clic en cualquier fila para profundizar en la vista detallada completa de la sesión con transcripción de la conversación y jerarquía de agentes.",
    "Per-session deep dive with a collapsible agent hierarchy tree and a full chronological event timeline showing every tool call name and summary. An overview panel at the top surfaces tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and a subagent type breakdown give quick distribution reads. The conversation viewer renders markdown with syntax highlighting, per-tool styled blocks, slash-command pills with their captured TUI output, and inline session-rename markers. Messages typed mid-turn (queued while Claude was still working) appear where Claude actually received them, and harness notifications are attributed to System. Export the entire session as JSON or share the permalink for async review. When the session is waiting on you, a yellow banner under the header names the reason and how long it has been waiting.":
      "Un análisis profundo por sesión con un árbol jerárquico de agentes colapsables y una cronología completa de eventos cronológicos que muestra el nombre y el resumen de cada llamada de herramienta. Un panel general en la parte superior muestra los contadores de superficies de los eventos, las llamadas de herramientas, los subagentes, las compresiones, los errores y la duración. Las barras de uso de las herramientas principales y la desglose del tipo de subagente proporcionan lecturas de distribución rápidas. El visualizador de conversaciones renderiza markdown con resaltado de sintaxis, bloques con estilo por herramienta, píldoras de comandos de guión con su salida TUI capturada y marcadores de renombramiento de sesión en línea. Los mensajes ingresados a mitad de turno (que se agendaron mientras Claude seguía trabajando) aparecen donde Claude los recibió realmente, y las notificaciones de gestión se atribuyen al Sistema. Exporta toda la sesión como JSON o comparte el enlace permanente para la revisión asincrónica. Cuando la sesión está esperando a que la realices, un banner amarillo debajo de los nombres del encabezado indica la razón y cuánto tiempo ha estado esperando.",
    "A rules-based alerting engine evaluates the live event stream server-side: <strong>event pattern</strong> (match event type / tool / summary text, optionally N matches within a time window), <strong>inactivity</strong>, <strong>stuck agent</strong>, and <strong>token threshold</strong> — each with per-(rule, session, agent) cooldown dedup. Fired alerts surface in a live feed and fan out to <strong>14 first-class webhook providers</strong> — Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream — plus any generic JSON endpoint (with optional HMAC-SHA256 signing and custom headers). Delivery is detached and fail-safe with a request timeout, bounded retry/backoff, secret redaction, a one-click test probe, and a per-target delivery log. Rules and channels are managed together in <strong>Settings → Alerts</strong>.":
      "Un motor de alerta basado en reglas evalúa el flujo de eventos en vivo desde el lado del servidor: <strong>Patrón de eventos</strong> (tipo de evento de coincidencia / herramienta / texto de resumen, opcionalmente N coincidencias dentro de un rango de tiempo), <strong>inactividad</strong>, <strong>Agente atascado</strong>, y <strong>Límite de tokens</strong> — cada uno con deduplicación de tiempo de espera por (regla, sesión, agente). Las alertas lanzadas aparecen en un feed en vivo y se extienden a <strong>14 proveedores de webhook de primera clase</strong> — Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream — además de cualquier punto final JSON genérico (con firma HMAC-SHA256 opcional y encabezados personalizados). La entrega es separada y segura con un tiempo de espera de solicitud, retiro/retraso limitado, redacción secreta, una sonda de prueba de un solo clic y un registro de entrega por objetivo. Las reglas y canales se gestionan juntos en <strong>Configuración → Alertas</strong>.",
    "A native desktop app — a macOS <code>.app</code> (shipped as a <code>.dmg</code>) and a Windows <code>.exe</code> (NSIS installer plus a no-install portable build) — built with Electron 35. It <strong>embeds the Express server in-process</strong> — <code>require()</code>-ing <code>server/index.js</code> directly, with no child process and no IPC — and renders the built React client in a <code>BrowserWindow</code>. Adds a menu-bar / notification-area (tray) icon, a native application menu, auto-start at login (macOS Login Items via <code>SMAppService</code>; Windows per-user <code>HKCU\\…\\Run</code>), and a single-instance lock. Closing the window hides it while the server keeps running, and the app auto-installs Claude Code hooks on first boot so an install-only user gets events flowing without a checkout.":
      "Una aplicación nativa para escritorio, un macOS <code>.app</code> (enviado como un <code>.dmg</code>) y una ventana de Windows <code>.exe</code> (Instalador NSIS más una versión portátil sin instalación) - construida con Electron 35. lo <strong>Incorpora el servidor Express en el proceso</strong> — <code>require()</code>-ando <code>server/index.js</code> Directamente, sin proceso de niño ni IPC, y renderiza el cliente React construido en un <code>BrowserWindow</code>. eñade un icono de barra de menú / área de notificaciones ( bandeja), un menú de aplicación nativo, inicio automático al iniciar sesión (elementos de inicio de sesión de macOS a través de <code>SMAppService</code>; Ventanas por usuario <code>HKCU\\…\\Run</code>), y un bloqueo de instancia única. Cerrar la ventana lo oculta mientras el servidor sigue funcionando, y la aplicación instala automáticamente los ganchos de Claude Code en la primera iniciación, por lo que un usuario que solo instala recibe eventos sin necesidad de realizar una compra.",
    "Real-time streaming event log showing tool calls, agent state changes, errors, and compaction events as they arrive. Pause/resume with automatic buffering, paginated history for scrollback, and auto-scrolling to the latest entry. Click any row to expand its full hook payload inline. A dedicated <strong>Session →</strong> button navigates directly to session detail without collapsing the expanded state. Every entry carries the same status badge the Dashboard and Session Detail show for that event — one shared mapping that understands both Claude and Codex event types — and rows are grouped by session for quick scanning of concurrent work.":
      "Registro de eventos en tiempo real que muestra llamadas de herramientas, cambios de estado del agente, errores y eventos de compactación a medida que llegan. Pausa y reanudación con almacenamiento en búfer automático, historial paginado para volver atrás y desplazamiento automático hasta la última entrada. Haz clic en cualquier fila para expandir en línea su carga útil de hook completa. Un botón <strong>Sesión →</strong> dedicado navega directamente al detalle de la sesión sin colapsar el estado expandido. Cada entrada lleva la misma insignia de estado que el Dashboard y el detalle de sesión muestran para ese evento — una única correspondencia compartida que entiende tanto los tipos de evento de Claude como los de Codex — y las filas se agrupan por sesión para revisar rápidamente el trabajo simultáneo.",
    "Token usage breakdown by model with stacked bar charts, tool frequency rankings, agent type distribution donuts, and session outcome pie charts. A 52-week activity heatmap aligned by day-of-week shows density with hover tooltips. 30-day sparkline trends track cost and session volume at a glance. The cost summary panel totals input, output, and cache spend across all models. A live/offline indicator and auto-refresh via WebSocket keep everything current. All charts are responsive and adapt to mobile viewports.":
      "Desglose del uso de tokens por modelo con gráficos de barras apiladas, clasificaciones de frecuencia de herramientas, rosquillas de distribución de tipos de agentes y gráficos circulares de resultados de sesiones. Un mapa de calor de actividad de 52 semanas alineado por día de la semana muestra la densidad con etiquetas de herramientas flotantes. Las tendencias de sparkline de 30 días rastrean el costo y el volumen de la sesión de un vistazo. El panel de resumen de costos suma la entrada, la salida y el gasto en caché en todos los modelos. Un indicador en vivo/desconectado y la actualización automática a través de WebSocket mantienen todo actualizado. Todos los gráficos son responsivos y se adaptan a las vistas móviles.",
    "Every UI update is pushed over a persistent WebSocket with sub-5 ms dispatch latency — zero polling anywhere. If the connection drops, automatic 2-second reconnect kicks in while a ping/pong heartbeat detects stale connections early. A sidebar indicator turns green/red so you always know whether you're live. The WebSocket carries typed JSON envelopes for new events, session updates, agent transitions, compaction results, and import progress — all parsed into the same eventBus the REST layer uses.":
      "Cada actualización de la interfaz de usuario se envía a través de un WebSocket persistente con una latencia de despacho inferior a 5 ms, sin necesidad de consultar la conexión en ningún momento. Si la conexión se pierde, se inicia una reconectividad automática de 2 segundos mientras que un latido de ping/pong detecta temprano las conexiones obsoletas. Un indicador en la barra lateral se vuelve verde/rojo para que siempre sepa si está conectado. El WebSocket transporta envoltorios JSON escritos por teclado para nuevos eventos, actualizaciones de sesión, transiciones de agente, resultados de compactación y progreso de importación, todo ello analizado en el mismo eventBus que utiliza la capa REST.",
    "Standalone CLI statusline for Claude Code that prints model name, user, working directory, git branch, and a color-coded context-window bar (green → yellow → red). Token counts show input (green ↑), output (cyan ↓), and cache (dim) separately. Session cost in USD shifts color by configurable thresholds. ANSI-colored output updates on every turn. Python-based with a thin shell wrapper — drop it into your prompt or tmux status line. Works with any terminal emulator that supports 256-color ANSI.":
      "Línea de estado CLI independiente para Claude Code que imprime el nombre del modelo, el usuario, el directorio de trabajo, la rama de git y una barra de ventana de contexto codificada por colores (verde → amarillo → rojo). Los conteos de tokens muestran la entrada (verde ↑), la salida (cian ↓) y la caché (mala) por separado. El costo de la sesión en USD cambia el color según los umbrales configurables. La salida de color ANSI se actualiza en cada turno. Basado en Python con un envoltorio de cáscara delgada: simplemente colócalo en tu línea de comando o en la línea de estado de tmux. Funciona con cualquier emulador de terminal que admita ANSI de 256 colores.",
    "Import existing Claude Code or Codex sessions from three sources — rescan the selected default <code>~/.claude/projects</code> or <code>~/.codex/sessions</code> folder, scan any absolute path on disk, or drag-drop <code>.jsonl</code>, <code>.zip</code>, <code>.tar.gz</code>, and <code>.gz</code> archives through <b>Settings → Import History</b>. All paths use each provider's live ingestion pipeline, so imported tokens, per-model cost, and tool data match real-time capture exactly. Codex uploads are snapshotted before temporary files are cleaned up, and re-imports are idempotent.":
      "Importa sesiones existentes de Claude Code o Codex desde tres fuentes: vuelve a escanear la carpeta predeterminada elegida <code>~/.claude/projects</code> o <code>~/.codex/sessions</code>, escanea cualquier ruta absoluta del disco o arrastra archivos <code>.jsonl</code>, <code>.zip</code>, <code>.tar.gz</code> y <code>.gz</code> mediante <b>Configuración → Importar historial</b>. Todas las rutas usan el flujo de ingestión en tiempo real de cada proveedor, por lo que los tokens, el costo por modelo y los datos de herramientas importados coinciden exactamente con la captura en vivo. Las cargas de Codex se guardan como instantáneas antes de limpiar los archivos temporales y las reimportaciones son idempotentes.",
    "The startup auto-import of <code>~/.claude/projects</code> is one-time and marker-gated, so a project folder created after first launch — whose sessions never flow through hooks (for example with host-only hooks disabled) — would stay invisible until a manual rescan. A background sync closes that gap with three triggers sharing one mtime cache and a single coalesced sweep: an immediate sweep at startup, a debounced <code>fs.watch</code> (recursive on macOS and Windows; root plus immediate child folders on Linux to avoid the userland recursive-watcher hazard) that fires the moment a new session file or project folder appears, and a periodic safety-net poll tunable via <code>DASHBOARD_SESSION_SYNC_MS</code> (default <code>30000</code> ms; <code>0</code> disables the poll while leaving the watcher running). Each sweep re-parses only files whose mtime advanced and broadcasts <code>session_created</code> / <code>session_updated</code> (plus the main agent) so the UI refreshes live, while an already-imported unchanged session is skipped without re-parsing.":
      "La importación automática inicial de <code>~/.claude/projects</code> Es único y con puerta de marcador, por lo que una carpeta de proyecto creada después del primer lanzamiento, cuyas sesiones nunca fluyen a través de ganchos (por ejemplo, con los ganchos solo para el host desactivados), permanecería invisible hasta una escanear manual. Una sincronización en segundo plano cierra esa brecha con tres desencadenantes que comparten una caché mtime y una sola escanear coalescida: una escanear inmediata al inicio, una debounced <code>fs.watch</code> (recursivo en macOS y Windows; directorio raíz más hijos inmediatos en Linux para evitar el peligro del observador recursivo en el sistema operativo) que dispara en el momento en que aparece un nuevo archivo de sesión o directorio de proyecto, y una encuesta periódica de red de seguridad ajustable a través de <code>DASHBOARD_SESSION_SYNC_MS</code> (por defecto <code>30000</code> Sra.; <code>0</code> desactiva la encuesta mientras deja que el observador siga funcionando). Cada escaneo solo reparse los archivos cuyo mtime ha avanzado y transmite <code>session_created</code> / <code>session_updated</code> (más el agente principal) por lo que la interfaz de usuario se actualiza en vivo, mientras que una sesión ya importada e inalterada se omite sin volver a analizarla.",
    "Incremental JSONL reader shared across the hook handler, compaction scanner, conversation viewer, and import pipeline. Byte-offset tracking skips already-parsed content; cache hits short-circuit disk I/O so even sessions with tens of thousands of turns stay fast. It also extracts the live session title (<code>custom-title</code> / <code>ai-title</code>) so renames surface in real time, plus the first user prompt as a fallback descriptor for placeholder-named sessions and agents.":
      "El lector JSONL incremental compartido entre el gestor de ganchos, el escáner de compactación, el visualizador de conversaciones y la tubería de importación. El seguimiento del desplazamiento de bytes omite el contenido ya procesado; los accesos a la caché cortocircuita la E/S del disco, por lo que incluso las sesiones con decenas de miles de giros siguen siendo rápidas. También extrae el título de la sesión en vivo (<code>custom-title</code> / <code>ai-title</code>) renombra la superficie en tiempo real, además del primer mensaje de usuario como descriptor de respaldo para sesiones y agentes con nombres de lugar de almacenamiento.",
    "LRU eviction of cold session buffers plus a tail-cap on per-entry growable arrays (turn durations, API errors, compaction entries). A session that runs for days cannot grow a single cache entry without bound, and each entry stores its parsed result only once — no shadow copy.":
      "Desalojo LRU de los búferes de sesiones frías, además de una tapa de cola en matrices crecientes por entrada (duraciones de turno, errores API, entradas de compactación). Una sesión que se ejecuta durante días no puede crecer una sola entrada de caché sin límites, y cada entrada almacena su resultado analizado solo una vez, sin copia de sombra.",
    "The periodic compaction sweep reads each active session's transcript path directly from <code>sessions.transcript_path</code> (a partial index covers exactly those rows), so the work is O(active sessions) instead of a <code>json_extract</code> scan over the whole events table.":
      "La escanearía de compactación periódica lee el camino de transcripción de cada sesión activa directamente desde <code>sessions.transcript_path</code> (un índice parcial cubre exactamente esas filas), por lo que el trabajo es O(sesiones activas) en lugar de un <code>json_extract</code> Escanee toda la tabla de eventos.",
    "Collapsible parent–child agent tree rendered on both Dashboard and Session Detail. Agents with subagents display expand/collapse chevrons; leaf agents show a dot indicator. The tree auto-expands when any child transitions to active and correctly tracks backgrounded subagents without premature completion. Depth is unlimited — deeply nested chains render as indented rows with connecting lines. Each node shows model, current tool, status badge, and cumulative token cost for tracing spend down the spawn chain.":
      "Árbol de agentes padre-hijo colapsable renderizado tanto en el Panel de control como en los Detalles de la sesión. Los agentes con subagentes muestran flechas de expansión/colapso; los agentes de hoja muestran un indicador de punto. El árbol se expande automáticamente cuando cualquier hijo pasa a activo y rastrea correctamente los subagentes en segundo plano sin completar prematuramente. La profundidad es ilimitada: las cadenas profundamente anidadas se muestran como filas recortadas con líneas de conexión. Cada nodo muestra el modelo, la herramienta actual, el distintivo de estado y el costo acumulativo de los tokens para rastrear el gasto a lo largo de la cadena de generación.",
    "Per-model cost estimation with configurable pricing rules — set input, output, and cache-read rates per model variant through the Settings UI. View total and per-session breakdowns on Sessions, Session Detail, and Analytics. Compaction- aware token accounting preserves baselines across context compressions so no usage is silently dropped. Cost chips appear on Kanban cards, session rows, and the sidebar summary. Subagent cards show each subagent's own cost, computed from that subagent's transcript token usage priced at current rates, so a subagent card no longer misleadingly reads as if it cost the whole session; main-agent cards still show the session total. Pricing changes retroactively recalculate all stored sessions, and imports apply the same rate table.":
      "Estimación de costos por modelo con reglas de precios configurables: establece tasas de entrada, salida y lectura de caché por variante de modelo a través de la interfaz de usuario de Configuración. Ve los desgloses totales y por sesión en Sesiones, Detalles de sesión y Análisis. La contabilidad de tokens consciente de la compactación preserva las bases de datos a través de las compresiones de contexto, por lo que ningún uso se elimina silenciosamente. Las fichas de costo aparecen en las tarjetas Kanban, las filas de sesión y el resumen de la barra lateral. Las tarjetas de subagente muestran el propio costo de cada subagente, calculado a partir del uso de tokens de la transcripción de ese subagente con precios a las tasas actuales, por lo que una tarjeta de subagente ya no se lee engañosamente como si costara toda la sesión; las tarjetas de agente principal todavía muestran el total de la sesión. Los cambios de precios recalculan retroactivamente todas las sesiones almacenadas y las importaciones aplican la misma tabla de tarifas.",
    "Model pricing editor with per-token rate configuration for every Claude variant. Hook installation status with one-click reinstall and per-hook health checks. Full JSON data export covering sessions, agents, events, tokens, and pricing rules. Session cleanup controls to abandon stale sessions or purge old data by age. Browser notification preferences with per-event toggles. A system information panel shows database row counts, file sizes, server uptime, and WebSocket connection status at a glance.":
      "Editor de precios de modelos con configuración de tasa por token para cada variante de Claude. Estado de instalación del gancho con reinstalación con un solo clic y comprobaciones de salud por gancho. Exportación completa de datos JSON que cubre sesiones, agentes, eventos, tokens y reglas de precios. Control de limpieza de sesiones para abandonar sesiones obsoletas o purgar datos antiguos por edad. Preferencias de notificación del navegador con interruptores por evento. Un panel de información del sistema muestra el número de filas de la base de datos, los tamaños de los archivos, el tiempo de actividad del servidor y el estado de la conexión WebSocket de un vistazo.",
    "Local MCP sidecar with three transport modes — stdio for Claude Code native integration, HTTP+SSE for remote clients, and an interactive REPL for ad-hoc terminal queries. Exposes 25 typed tools across 6 domains: sessions, agents, events, analytics, settings, and system health. Every mutation is gated behind a tiered policy so nothing dangerous fires without opt-in. Retry-aware API access handles transient failures. Runs as a standalone Node process with no Docker or cloud dependency.":
      "MCP local con tres modos de transporte: stdio para la integración nativa del código Claude, HTTP+SSE para clientes remotos y un REPL interactivo para consultas terminales ad hoc. Exponen 25 herramientas de tipo en 6 dominios: sesiones, agentes, eventos, análisis, configuración y estado del sistema. Cada mutación está bloqueada detrás de una política jerárquica, por lo que nada peligroso se activa sin consentimiento. El acceso a la API con detección de intentos de reintento maneja los fallos transitorios. Funciona como un proceso Node independiente sin dependencias de Docker ni de la nube.",
    "Instruction, skills, rules, and custom-agent layers for both Claude Code and Codex. Path-scoped rules target backend, frontend, MCP, and docs directories with context-appropriate guidelines. Reusable skills cover onboarding, feature shipping, live-issue debugging, release-readiness, and MCP operations. Specialized subagents for backend, frontend, and MCP code review run in parallel with focused tooling. Everything lives in <code>.claude/</code> and is version-controlled alongside the codebase.":
      "Instrucciones, habilidades, reglas y capas de agente personalizado para tanto Claude Code como Codex. Las reglas de alcance de ruta apuntan a los directorios de backend, frontend, MCP y documentos con directrices apropiadas para el contexto. Las habilidades reutilizables cubren la incorporación, el envío de funciones, el depuración de problemas en vivo, la preparación para el lanzamiento y las operaciones MCP. Los subagentes especializados para la revisión de código de backend, frontend y MCP se ejecutan en paralelo con herramientas enfocadas. Todo vive en <code>.claude/</code> Y está controlado por versiones junto con el código fuente.",
    "D3.js-powered visualizations: an agent orchestration DAG showing spawn patterns across sessions, a tool-execution Sankey diagram mapping tool-to- tool transitions, and a directed pipeline graph with frequency labels. Every chart title carries an info icon that opens a popover explaining what it shows and how to read it. Hovering nodes, edges, and bars surfaces tooltips with share-of-source percentages, success-rate buckets, and timing patterns. All labels are translated to English, Vietnamese, and Chinese.":
      "Visualizaciones impulsadas por D3.js: una DAG de orquestación de agentes que muestra patrones de generación a lo largo de las sesiones, una herramienta de mapeo de diagrama Sankey de ejecución de herramientas que muestra las transiciones de herramienta a herramienta y un gráfico de tuberías dirigidas con etiquetas de frecuencia. Cada título de gráfico lleva un icono de información que abre un popover que explica lo que muestra y cómo leerlo. Al pasar el cursor sobre nodos, bordes y superficies de barras, aparecen herramientas de información con porcentajes de participación en el código fuente, cuencos de tasa de éxito y patrones de tiempo. Todas las etiquetas se traducen al inglés, vietnamita y chino.",
    "Subagent effectiveness scorecards with success-rate rings and day-of-week sparklines. Auto-detected workflow patterns expand on click into a detail panel with the full step chain, stats grid, and a narrative with loop detection. Model delegation flow, error propagation bars with API error cards, concurrency swim-lanes, and complexity bubble charts round out the view. Six headline stat cards each include an info popover explaining the metric and its current value. Status filter applies globally.":
      "Tarjetas de puntuación de eficacia del subagente con anillos de tasa de éxito y líneas de chispa del día de la semana. Los patrones de flujo de trabajo detectados automáticamente se amplían al hacer clic en un panel de detalles con la cadena completa de pasos, la cuadrícula de estadísticas y una narración con detección de bucle. El flujo de delegación del modelo, las barras de propagación de errores con tarjetas de error de API, las pistas de natación de concurrencia y los gráficos de burbujas de complejidad completan la vista. Se incluyen en cada una de las seis tarjetas estadísticas principales una ventana emergente de información que explica la métrica y su valor actual. El filtro de estado se aplica globalmente.",
    "Searchable session selector with pagination to explore any session's agent tree, tool-call timeline, and event sequence. The detail page opens with a live-updating overview — tile counters for events, tool calls, subagents, compactions, errors, and duration. Top-tool usage bars and subagent breakdown give quick reads. The conversation viewer renders markdown with syntax highlighting. Cross-filter from DAG nodes, run compaction analysis, or export as JSON — all with real-time WebSocket auto-refresh.":
      "Selector de sesiones buscable con paginación para explorar el árbol de agentes, la línea de tiempo de llamadas de herramientas y la secuencia de eventos de cualquier sesión. La página de detalles se abre con una descripción general actualizada en tiempo real: contadores de baldosas para eventos, llamadas de herramientas, subagentes, compactaciones, errores y duración. Las barras de uso de herramientas principales y la desglose de subagentes proporcionan lecturas rápidas. El visualizador de conversaciones renderiza Markdown con resaltado de sintaxis. Filtra cruzado por nodos DAG, realiza el análisis de compactación o exporta como JSON, todo con la actualización automática de WebSocket en tiempo real.",
    "Persistent browser notifications via Web Push (VAPID) for real-time alerts even when the tab is not focused or the browser is backgrounded. Includes macOS audio support so notifications are audible alongside system sounds. Per-event toggles let you choose which events fire — session starts, completions, errors, compactions, or agent spawns. Server-side subscription management ensures one push per event per browser. Works on Chrome, Edge, Firefox, and Safari 17+ with graceful degradation elsewhere.":
      "Notificaciones persistentes del navegador a través de Web Push (VAPID) para alertas en tiempo real incluso cuando la pestaña no está enfocada o el navegador está en segundo plano. Incluye soporte de audio para macOS, por lo que las notificaciones son audibles junto con los sonidos del sistema. Los interruptores por evento le permiten elegir qué eventos se ejecutan: inicio, finalización, errores, compactaciones o aparecimientos de agentes de sesión. La gestión de suscripciones en el lado del servidor garantiza una notificación por evento por navegador. Funciona en Chrome, Edge, Firefox y Safari 17+ con degradación suave en otros lugares.",
    "Ready-to-use Dockerfile and docker-compose.yml for one-command deployment. Supports both Docker and Podman with persistent volume mounts for the SQLite database and hook data. Configurable port mapping via environment variables and a health-check endpoint the container runtime can poll. Multi-stage build keeps the image lean — only production deps and the compiled bundle ship. Run <code>docker compose up -d --build</code> and the dashboard is live with zero additional setup or configuration required.":
      "Dockerfile y docker-compose.yml listos para usar para el despliegue con un solo comando. Soporta tanto Docker como Podman con montajes de volumen persistentes para la base de datos SQLite y datos de gancho. Mapeo de puertos configurables a través de variables de entorno y un punto final de verificación de salud que el tiempo de ejecución del contenedor puede consultar. La construcción multiestadio mantiene la imagen ligera: solo se envían las dependencias de producción y el paquete compilado. carrera <code>docker compose up -d --build</code> Y el panel de control está en vivo sin necesidad de ninguna configuración o instalación adicional.",
    "Official Claude Code plugin marketplace shipping 10 plugins with 53 skills, 14 agents, 30 slash commands, 3 CLI tools, 3 hook configs, and 1 MCP server. Deep analytics with compaction-aware baselines, productivity automation, developer diagnostics, AI-powered workflow intelligence, and dashboard MCP integration. Five newer plugins go further: <code>ccam-cost-guard</code> (budget guardrails, spend forecasts, and cost-threshold alerts), <code>ccam-sessions</code> (session forensics — search, timeline, and transcript replay), <code>ccam-workflows</code> (multi-agent orchestration and fleet intelligence), <code>ccam-quality</code> (reliability and SLO checks), and <code>ccam-config</code> (Claude Code config and memory governance). Install with <code>claude plugin install</code> — no restart needed. Each listing shows author, license, homepage, and per-skill contribution breakdown. The Config Explorer's Plugins tab surfaces installed plugins with live status.":
      "Mercado oficial de plugins del código Claude con envío de 10 plugins con 53 habilidades, 14 agentes, 30 comandos slash, 3 herramientas CLI, 3 configuraciones de ganchos y 1 servidor MCP. Análisis profundo con bases de datos conscientes de la compactación, automatización de la productividad, diagnóstico de desarrolladores, inteligencia de flujo de trabajo impulsada por IA e integración de MCP en el panel de control. Cinco plugins más nuevos van más allá: <code>ccam-cost-guard</code> (barandillas de presupuesto, pronósticos de gasto y alertas de umbral de costo), <code>ccam-sessions</code> (forense de sesiones: búsqueda, cronología y reproducción de transcripciones), <code>ccam-workflows</code> (orquestación de múltiples agentes e inteligencia de flota), <code>ccam-quality</code> (comprobaciones de fiabilidad y SLO), y <code>ccam-config</code> (Configuración de Claude Code y gobernanza de la memoria). Instalar con <code>claude plugin install</code> — no se necesita reiniciar. Cada lista muestra el autor, la licencia, la página de inicio y la desglose de las contribuciones por habilidad. La pestaña de complementos del Explorador de Configuración muestra los complementos instalados con estado en vivo.",
    "Spawn <code>claude</code> subprocesses straight from the dashboard with a chat-style streaming UI — multi-turn <b>Conversation</b> or single-shot <b>Headless</b> mode. One-click <b>Resume</b> on any past conversation spawns <code>claude --resume</code> seeded with the prior transcript. Re- attach reconciles in-memory logs with the on-disk JSONL so navigating away never loses history. Slash-command autocomplete, file references, live token/context-window meter, and a thinking-effort dial bring TUI parity to the browser. Same-origin guard blocks drive-by spawns.":
      "desovar <code>claude</code> Subprocesos directamente desde el panel de control con una interfaz de usuario de streaming en estilo chat, multidireccional <b>conversación</b> O disparo único <b>sin cabeza</b> Modo. Un clic <b>currículum</b> En cualquier conversación pasada se generan <code>claude --resume</code> Semillado con la transcripción anterior. Reajusta los registros de reconciliación en memoria con el JSONL en disco para que la navegación nunca pierda el historial. Completa automáticamente el comando de guión, las referencias de archivos, el medidor de tokens/ventana de contexto en vivo y un dial de esfuerzo de pensamiento traen paridad TUI al navegador. Los bloqueos de guardia de origen compartido bloquean los spawning de paso rápido.",
    "A 12-tab inspector at <code>/cc-config</code> for everything Claude Code knows about: skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts. The Settings tab leads with a Current configuration summary of the options <code>/config</code> controls — model, verbose, theme, output style, auto-compact, notifications — resolved across scopes. The Memory tab surfaces both the user and project <code>CLAUDE.md</code> files and the per-project file-based memory store — every auto-memory <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+), grouped by project, searchable, and editable. Create, edit, and delete the low-risk text-file surfaces with mandatory timestamped backups before every write. Plugins, MCP, hooks, and live settings stay read-only with explainer banners and copy-able CLI commands. Per-plugin contribution breakdowns show author and license.":
      "Un inspector de 12 tablas en <code>/cc-config</code> Para todo lo que Claude Code sabe sobre: habilidades, subagentes, comandos slash, estilos de salida, complementos, mercados, servidores MCP, ganchos, configuraciones, memoria, atajos de teclado y scripts de línea de estado. La pestaña Configuración encabeza con un resumen de la configuración actual de las opciones <code>/config</code> Controls: modelo, verboso, tema, estilo de salida, compacto automático, notificaciones: resueltos en todos los ámbitos. La pestaña Memoria muestra tanto al usuario como al proyecto. <code>CLAUDE.md</code> Archivos y el almacenamiento de memoria basado en archivos por proyecto, cada memoria automática <code>*.md</code> bajo <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> Índice más un archivo por hecho recordado, a menudo más de 100), agrupados por proyecto, buscables y editable. Cree, edite y elimine las superficies de archivos de texto de bajo riesgo con copias de seguridad con hora y fecha obligatorias antes de cada escritura. Los complementos, MCP, ganchos y configuraciones en vivo permanecen en modo solo lectura con banners explicativos y comandos CLI copiables. Los desgloses de contribuciones por complemento muestran el autor y la licencia.",
    "Mobile-first layouts with stacking grids, horizontally scrollable tables, and a collapsible sidebar that auto-hides below 1400 px. All pages adapt from phone to ultrawide with consistent navigation and readable typography. Kanban columns stack vertically on narrow screens, analytics charts reflow to single-column, and the activity feed stays fully swipeable. Touch targets meet 44 px minimum. Dark theme renders consistently across iOS Safari, Chrome, and Firefox with no flash of unstyled content.":
      "Diseños móviles primero con cuadrículas apilables, tablas que se pueden desplazar horizontalmente y una barra lateral plegable que se oculta automáticamente por debajo de 1400 píxeles. Todas las páginas se adaptan desde teléfonos hasta pantallas ultra anchas con una navegación consistente y una tipografía legible. Las columnas Kanban se apilan verticalmente en pantallas estrechas, los gráficos analíticos se rediseñan para una sola columna y el feed de actividad sigue siendo completamente deslizable. Los objetivos táctiles cumplen con un mínimo de 44 píxeles. El tema oscuro se renderiza de forma consistente en iOS Safari, Chrome y Firefox sin ningún parpadeo de contenido sin estilo.",
    "Visualize parallel agent execution with a Gantt-style timeline showing overlapping subagent lifetimes, tool-call concurrency windows, and wait gaps. Color-coded bars distinguish working, waiting, and errored states so bottlenecks are immediately visible. Hover any bar for exact timestamps and duration. Zoom and pan across long-running sessions with hundreds of agents. The timeline shares the Workflows status filter so you can isolate active, completed, or errored sessions without leaving the view.":
      "Visualice la ejecución paralela de agentes con una línea de tiempo en estilo Gantt que muestra las vidas útiles superpuestas de los subagentes, las ventanas de concurrencia de llamadas de herramientas y los intervalos de espera. Las barras codificadas por color distinguen los estados de trabajo, espera y error, por lo que los cuellos de botella son inmediatamente visibles. Haga clic con el ratón sobre cualquier barra para ver las fechas y horas exactas y la duración. Aproxime y desplácese por sesiones de larga duración con cientos de agentes. La línea de tiempo comparte el filtro de estado de flujos de trabajo, para que pueda aislar sesiones activas, completadas o con errores sin salir de la vista.",
    "Professional VS Code extension with a real-time Activity Bar sidebar showing active sessions, agent counts, and recent events without leaving your editor. A status bar pulse monitor surfaces connection health and the latest event type at a glance. Deep navigation links open any session or analytics view directly in your browser. An embedded webview renders the full dashboard inside a VS Code tab with WebSocket push, theme sync, and responsive layout. Install from the marketplace or build from source.":
      "Extensión profesional de VS Code con una barra lateral de la barra de actividad en tiempo real que muestra sesiones activas, el número de agentes y eventos recientes sin salir de su editor. Una barra de estado que muestra el pulso de la conexión y el último tipo de evento de un vistazo. Los enlaces de navegación profunda abren cualquier sesión o vista de análisis directamente en su navegador. Una vista web incorporada muestra el panel completo dentro de una pestaña de VS Code con WebSocket push, sincronización de temas y diseño responsive. Instálelo desde el mercado o construya desde la fuente.",
    "Trace how errors cascade across agents and tool calls with a directed graph showing failure origins, retry paths, and recovery points. Each node displays the agent or tool that errored, the error message, and whether a retry succeeded or propagated upstream. Pinpoint root causes in deeply nested subagent chains. Horizontal bar charts rank the most error-prone tools and models. API error cards group failures by HTTP status and endpoint. Filter by session, time range, or error severity to narrow the view.":
      "Rastree cómo los errores se propagan a través de los agentes y las llamadas a las herramientas con un gráfico dirigido que muestra las orígenes de los fallos, los caminos de intento de nuevo y los puntos de recuperación. Cada nodo muestra el agente o la herramienta que tuvo un error, el mensaje de error y si el intento de nuevo tuvo éxito o se propagó hacia arriba. Identifique las causas raíz en cadenas de subagentes profundamente anidadas. Los gráficos de barras horizontales clasifican las herramientas y modelos más propensos a errores. Las tarjetas de error de la API agrupan los fallos por estado HTTP y punto final. Filtre por sesión, rango de tiempo o gravedad del error para restringir la vista.",
    'Three independent PWAs — dashboard, landing page, and wiki — each with its own Web App Manifest and Service Worker. Install to your home screen or dock for a standalone, chrome-less experience. SVG icons with <code>sizes="any"</code> and iOS standalone meta tags included.':
      'Tres PWAs independientes: panel de control, página de destino y wiki, cada uno con su propio manifiesto de aplicaciones web y trabajador de servicios. Instálelos en su pantalla de inicio o dock para una experiencia independiente y sin Chrome. Íconos SVG con <code>sizes="any"</code> y etiquetas meta independientes de iOS incluidas.',
    "The dashboard SW serves Vite's hashed <code>/assets/*</code> bundles cache-first (URLs are immutable per build) and treats everything else as network-first with cache fallback. Explicit <code>Cache-Control</code> headers on the production Express static middleware reinforce the policy, so a rebuild replaces the in-browser code without a hard refresh.":
      "El SW del panel de control sirve a Vite's hashed <code>/assets/*</code> bundles almacena primero la caché (los URL son inmutables por construcción) y trata todo lo demás como primero la red con fallback de caché. explícito <code>Cache-Control</code> Los encabezados en el middleware estático de producción Express refuerzan la política, por lo que una reconstrucción reemplaza el código en el navegador sin una actualización forzosa.",
    "A <code>controllerchange</code> listener in <code>client/src/main.tsx</code> reloads the page exactly once when a new SW takes over an already-controlled page. First installs do not reload, so the very first visit is never interrupted.":
      "A <code>controllerchange</code> Escuchador en <code>client/src/main.tsx</code> Recarga la página exactamente una vez cuando un nuevo SW toma el control de una página ya controlada. Las primeras instalaciones no se recargan, por lo que la primera visita nunca se interrumpe.",
    '<span class="caption-icon">📡</span> <span><strong>Dashboard · Monitor</strong> — live overview of active sessions and agents. Stats tiles, collapsible subagent hierarchy cards, and a recent activity feed. Auto-refreshes every 5 s via WebSocket</span>':
      '<span class="caption-icon">📡</span> <span><strong>Panel de control · Monitor</strong> - visión general en vivo de las sesiones activas y los agentes. Píxeles estadísticos, tarjetas de jerarquía de subagentes colapsables y un feed de actividad reciente. Se actualiza automáticamente cada 5 segundos a través de WebSocket</span>',
    '<span class="caption-icon">🩺</span> <span><strong>Dashboard · Health</strong> — composite health score ring, storage engine donut, cache/error/success gauges, tool invocation bars, subagent effectiveness, and model token distribution</span>':
      '<span class="caption-icon">🩺</span> <span><strong>Panel de control · Salud</strong> — anillo de puntuación de salud compuesta, donut de motor de almacenamiento, indicadores de caché/errores/éxito, barras de invocación de herramientas, eficacia del subagente y distribución de tokens del modelo</span>',
    '<span class="caption-icon">📋</span> <span><strong>Kanban Board (agents)</strong> — agents swim-laned by status: Working, Waiting, Completed, Error. Cards show model, cost, and current tool call. Yellow column flags agents waiting on user input</span>':
      '<span class="caption-icon">📋</span> <span><strong>Tablero Kanban (agentes)</strong> — los agentes se ubican en carriles de natación según su estado: Trabajando, Esperando, Completado, Error. Las tarjetas muestran el modelo, el costo y la llamada de herramienta actual. La columna amarilla marca a los agentes que esperan la entrada del usuario.</span>',
    '<span class="caption-icon">🗂️</span> <span><strong>Kanban Board (sessions)</strong> — sessions swim-laned across 5 columns: Active, Waiting, Completed, Error, Abandoned. Each card shows agent count, duration, model, and cumulative cost</span>':
      '<span class="caption-icon">🗂️</span> <span><strong>Tablero Kanban (sesiones)</strong> — sesiones de natación en carriles a través de 5 columnas: Activa, Esperando, Completada, Error, Abandonada. Cada tarjeta muestra el número de agentes, la duración, el modelo y el costo acumulado</span>',
    '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — searchable, filterable, server-paginated table. Each row shows status badge, agent count, duration, model, and cost. Click any row to drill into session detail</span>':
      '<span class="caption-icon">📂</span> <span><strong>sesión</strong> — tabla indexable, filtrable y paginada por servidor. Cada fila muestra el distintivo de estado, el número de agentes, la duración, el modelo y el costo. Haga clic en cualquier fila para profundizar en los detalles de la sesión.</span>',
    '<span class="caption-icon">🤖</span> <span><strong>Session Detail · Agents</strong> — overview tiles (events, tool calls, subagents, errors, duration), top-tool usage bars, subagent type breakdown, and a collapsible parent–child agent hierarchy tree</span>':
      '<span class="caption-icon">🤖</span> <span><strong>Detalles de la sesión · Agentes</strong> — baldosas de visión general (eventos, llamadas de herramientas, subagentes, errores, duración), barras de uso de herramientas principales, desglose del tipo de subagente y un árbol jerárquico de agentes padre-hijo colapsable</span>',
    '<span class="caption-icon">💬</span> <span><strong>Session Detail · Conversation</strong> — full transcript viewer with markdown rendering, syntax-highlighted code blocks, per-tool sections, and collapsible thinking blocks</span>':
      '<span class="caption-icon">💬</span> <span><strong>Detalles de la sesión · Conversación</strong> — visualizador de transcripciones completas con renderizado de markdown, bloques de código resaltados por sintaxis, secciones por herramienta y bloques de pensamiento colapsables</span>',
    '<span class="caption-icon">🔬</span> <span><strong>Session Detail · Timeline</strong> — chronological event timeline with multi-dimension filters, color-coded entries by type, expandable hook payloads, and direct links to the owning session and agent</span>':
      '<span class="caption-icon">🔬</span> <span><strong>Detalles de la sesión · Línea de tiempo</strong> — línea de tiempo cronológica de eventos con filtros multidimensionales, entradas codificadas por color por tipo, cargas útiles de ganchos expandibles y enlaces directos a la sesión y agente propietarios</span>',
    '<span class="caption-icon">📰</span> <span><strong>Activity Feed</strong> — real-time streaming event log with pause/resume buffering, multi-dimension filters, expandable hook payloads, color-coded entries, and per-row session navigation buttons</span>':
      '<span class="caption-icon">📰</span> <span><strong>Feed de actividad</strong> — registro de eventos de transmisión en tiempo real con buffer de pausa/resume, filtros multidimensionales, cargas útiles de ganchos expandibles, entradas codificadas por color y botones de navegación de sesiones por fila</span>',
    '<span class="caption-icon">📊</span> <span><strong>Analytics</strong> — token usage by model, tool frequency bars, 52-week activity heatmap, 30-day sparkline trends, session outcome donuts, and cost summary with WebSocket auto-refresh</span>':
      '<span class="caption-icon">📊</span> <span><strong>análisis</strong> — uso de tokens por modelo, barras de frecuencia de herramientas, mapa de calor de actividad de 52 semanas, tendencias de sparkline de 30 días, rosquillas de resultado de sesión y resumen de costos con actualización automática de WebSocket</span>',
    '<span class="caption-icon">🔀</span> <span><strong>Workflows</strong> — D3.js agent orchestration DAG, tool-execution Sankey diagram, directed pipeline graph, effectiveness scorecards, concurrency swim-lanes, and complexity bubble charts</span>':
      '<span class="caption-icon">🔀</span> <span><strong>Flujos de trabajo</strong> — DAG de orquestación de agentes D3.js, diagrama Sankey de ejecución de herramientas, gráfico de tuberías dirigidas, tarjetas de puntuación de eficacia, carriles de natación de concurrencia y gráficos de burbujas de complejidad</span>',
    '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — 12-tab inspector for skills, subagents, slash commands, plugins, MCP servers, hooks, settings, memory, keybindings, and statusline. Safe edits with backups</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Explorador de configuración de Claude</strong> — Inspector de 12 pestañas para habilidades, subagentes, comandos slash, complementos, servidores MCP, ganchos, configuraciones, memoria, atajos de teclado y línea de estado. Ediciones seguras con copias de seguridad</span>',
    '<span class="caption-icon">▶️</span> <span><strong>Run Claude</strong> — spawn or resume Claude subprocesses from the browser. Pick Conversation or Headless mode, set cwd, model, permission level, and thinking effort. Same-origin guard included</span>':
      '<span class="caption-icon">▶️</span> <span><strong>Corra Claude</strong> — Crear o reanudar subprocesos de Claude desde el navegador. Elija Conversación o modo sin cabeza, establezca el directorio actual, el modelo, el nivel de permiso y el esfuerzo de pensamiento. Incluye guardia de origen compartido</span>',
    '<span class="caption-icon">💬</span> <span><strong>Run Claude · live stream</strong> — character-by-character streaming output. Tool uses, tool results, and thinking blocks are collapsible. Active runs switcher juggles multiple sessions</span>':
      '<span class="caption-icon">💬</span> <span><strong>Run Claude · transmisión en vivo</strong> - salida de streaming por carácter. Los usos de las herramientas, los resultados de las herramientas y los bloques de pensamiento son colapsables. Las ejecuciones activas del interruptor manejan múltiples sesiones</span>',
    '<span class="caption-icon">⚙️</span> <span><strong>Settings</strong> — model pricing editor with per-token rates, hook installation status, JSON data export, session cleanup controls, browser notification toggles, and system info panel with DB stats</span>':
      '<span class="caption-icon">⚙️</span> <span><strong>Configuración</strong> — editor de precios de modelos con tarifas por token, estado de instalación de los enlaces, exportación de datos JSON, controles de limpieza de sesiones, alternaciones de notificaciones del navegador y panel de información del sistema con estadísticas de la base de datos</span>',
    "This chart tracks how interest in Claude Code Agent Monitor has grown over time. The curve keeps climbing as more developers discover the project, share it, and use it in real workflows. Each new star is a small vote of confidence from the community.":
      "Este gráfico rastrea cómo ha crecido el interés en Claude Code Agent Monitor con el tiempo. La curva sigue subiendo a medida que más desarrolladores descubren el proyecto, lo comparten y lo utilizan en flujos de trabajo reales. Cada nueva estrella es un pequeño voto de confianza de la comunidad.",
    '<span class="caption-icon">⭐</span> <span> Enjoying the project? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer" >Give it a star on GitHub</a > and help more builders discover it. </span>':
      '<span class="caption-icon">⭐</span> <span> ¿Disfrutando del proyecto? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer" >Dale una estrella en GitHub</a > Y ayudar a más constructores a descubrirlo. </span>',
    "Hook Type": "Tipo de gancho",
    Trigger: "gatillo",
    "Dashboard Action": "Acción del panel de control",
    "Claude Code session begins": "Comienza la sesión de Claude Code",
    "Creates session and main agent. Stamps <code>awaiting_input_since</code> (with <code>awaiting_reason</code> = <code>session_start</code>) so the row lands in <strong>Waiting</strong> from the start (the CLI is at a prompt) — except a <code>compact</code>-source SessionStart (mid-turn auto-compaction), which leaves the flag untouched so a working session stays <strong>Active</strong>. Reactivates resumed sessions. Abandons orphaned sessions with no activity for <code>DASHBOARD_STALE_MINUTES</code> (default 180).":
      "Crea sesión y agente principal. sellos <code>awaiting_input_since</code> (con <code>awaiting_reason</code> = <code>session_start</code>) así que la fila aterriza en <strong>espera</strong> Desde el principio (la CLI está en un mensaje de inicio de sesión) - excepto un <code>compact</code>-Fuente SessionStart (compactación automática a mitad de turno), que deja la bandera sin tocar para que una sesión funcional se mantenga <strong>activo</strong>. va activa las sesiones suspendidas. Abandona las sesiones huérfanas sin actividad durante <code>DASHBOARD_STALE_MINUTES</code> (por defecto 180).",
    "User hits enter on a prompt": "El usuario pulsa enter en una solicitud",
    'Clears the waiting flag and promotes the main agent to <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >. The only reliable signal that text-only assistant turns have started — they emit no <code>PreToolUse</code> before <code>Stop</code>.':
      'Borra la bandera de espera y promueve al agente principal a <span class="status-chip chip-working" ><span class="chip-dot"></span>de trabajo</span >. señal confiable de que los teléfonos con asistente de texto solo han comenzado a funcionar: no emiten ninguna <code>PreToolUse</code> antes <code>Stop</code>.',
    "Agent begins using a tool": "El agente comienza a usar una herramienta",
    'Clears the waiting flag, sets agent → <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >, <code>current_tool</code> set. If tool is <code>Agent</code>, subagent record created.':
      'Borra la bandera de espera, establece agente → <span class="status-chip chip-working" ><span class="chip-dot"></span>de trabajo</span >, <code>current_tool</code> Configurar. Si la herramienta es <code>Agent</code>, registro del subagente creado.',
    "Tool execution completes": "La ejecución de la herramienta se completa",
    'Clears the waiting flag (covers permission-prompt approvals mid-tool). <code>current_tool</code> cleared. Agent stays <span class="status-chip chip-working" ><span class="chip-dot"></span>Working</span >.':
      'Borra la bandera de espera (cubre las aprobaciones de la solicitud de permiso a mitad de la herramienta). <code>current_tool</code> Liberado. El agente permanece <span class="status-chip chip-working" ><span class="chip-dot"></span>de trabajo</span >.',
    "Claude finishes a turn": "Claude termina una vuelta",
    'Non-error: main agent → <code>waiting</code> — UI shows <span class="status-chip chip-waiting" ><span class="chip-dot"></span>Waiting</span > until the next user input. <code>stop_reason=error</code>: marks the agent and session <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >. Background subagents keep running.':
      'Sin error: agente principal → <code>waiting</code> — Muestra de interfaz de usuario <span class="status-chip chip-waiting" ><span class="chip-dot"></span>espera</span > Hasta la próxima entrada del usuario. <code>stop_reason=error</code>: marca al agente y la sesión <span class="status-chip chip-error" ><span class="chip-dot"></span>error informático</span >. de los subagentes de fondo siguen funcionando.',
    "Background agent finished": "Agente de antecedentes terminado",
    "Matched subagent → <span class=\"status-chip chip-completed\" ><span class=\"chip-dot\"></span>Completed</span >. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session's <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent's own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks. The same scan also rebuilds the nested-subagent hierarchy — it repoints each subagent's <code>parent_agent_id</code> to the true spawner recovered from the transcript's Task tool result (<code>toolUseResult.agentId</code>), so subagents that spawn their own subagents nest correctly instead of flattening under main.":
      'Subagente coincidente → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completado</span >. idamente lo hace <strong>no</strong> Borrar la bandera de espera: un subagente en segundo plano que termina no nos dice nada sobre el humano. También inicia una escaneo JSONL de fuego y olvido (<code>scanAndImportSubagents</code>) que camina por la sesión <code>subagents/agent-*.jsonl</code> Archivos, pares <code>tool_use</code> ↔ <code>tool_result</code> Bloques por <code>tool_use_id</code>, y emite por herramienta <code>PreToolUse</code> + <code>PostToolUse</code> Eventos bajo los propios de cada subagente <code>agent_id</code> — las llamadas de herramientas de superficies que los subagentes realizan internamente y que nunca disparan ningún gancho. El mismo escaneo también reconstruye la jerarquía de subagentes anidados: reubica el punto de referencia de cada subagente <code>parent_agent_id</code> Al verdadero generador recuperado del resultado de la herramienta de tareas de la transcripción (<code>toolUseResult.agentId</code>), por lo que los subagentes que generan sus propios subagentes anidan correctamente en lugar de aplanarse bajo el principal.',
    "Agent sends notification": "El agente envía una notificación",
    'Event logged to activity feed. Permission/input-prompt patterns (e.g. "needs your permission", "waiting for your input") set the agent to <code>waiting</code> and stamp <code>awaiting_input_since</code> (with <code>awaiting_reason</code> = <code>notification</code>). Compaction-related notifications tagged as <code>Compaction</code> events. Triggers a browser notification if enabled.':
      'Evento registrado en el feed de actividad. Los patrones de permiso/petición de entrada (por ejemplo, "necesita su permiso", "esperando su entrada") establecen al agente para que <code>waiting</code> Y sello <code>awaiting_input_since</code> (con <code>awaiting_reason</code> = <code>notification</code>). Notificaciones relacionadas con la compactación etiquetadas como <code>Compaction</code> Eventos. Desactiva una notificación del navegador si está habilitada.',
    "<code>/compact</code> detected in JSONL": "<code>/compact</code> detectado en JSONL",
    'Creates a compaction subagent → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Detected via <code>isCompactSummary</code> entries in the transcript. Token baselines preserve pre-compaction totals. Periodic scanner (cadence ~¼ of <code>DASHBOARD_STALE_MINUTES</code>) catches compactions when no hooks fire.':
      'Crea un subagente de compactación → <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completado</span >. detectado a través de <code>isCompactSummary</code> Entradas en la transcripción. Las bases de referencia de tokens conservan los totales previos a la compactación. Escáner periódico (cadencia ~¼ de <code>DASHBOARD_STALE_MINUTES</code>) atrapa compactaciones cuando no disparan los ganchos.',
    "API error detected in transcript": "Se ha detectado un error de API en la transcripción",
    "Extracted from JSONL during history import, real-time transcript scanning, or the error detection watchdog. Captures quota limits, rate limits, auth failures, and other API errors. <strong>Immediately marks sessions and agents as error</strong> — previously recorded as events without changing status.":
      "Extraído de JSONL durante la importación de historial, el escaneo de transcripciones en tiempo real o el rastreador de detección de errores. Captura los límites de cuota, los límites de tasa, los fallos de autenticación y otros errores de API. <strong>Marca inmediatamente las sesiones y los agentes como errores</strong> — previamente registrados como eventos sin cambiar el estado.",
    "Turn cancelled by the user (<code>Esc</code>)":
      "Giro cancelado por el usuario (<code>Esc</code>)",
    "Synthesized by the watchdog because pressing <code>Esc</code> fires no hook. Recovered either from the transcript <code>[Request interrupted by user]</code> marker (flagged as <code>pendingInterrupt</code>) or, when <code>Esc</code> preceded any output and left no marker, from the idle-working timeout (<code>DASHBOARD_WORKING_IDLE_SECONDS</code>, default 120). Moves the session to <strong>Waiting</strong> — the same state a normal <code>Stop</code> produces.":
      "Sintetizado por el vigilante porque presionando <code>Esc</code> Fuegos sin gancho. Recuperado ya sea de la transcripción <code>[Request interrupted by user]</code> Marcador (marcado como <code>pendingInterrupt</code>) o, cuando <code>Esc</code> Precedió cualquier salida y no dejó ningún marcador, desde el tiempo de espera de trabajo inactivo (<code>DASHBOARD_WORKING_IDLE_SECONDS</code>, por defecto 120). Mueve la sesión a <strong>espera</strong> — el mismo estado que un normal <code>Stop</code> Produce.",
    "Per-turn timing recorded": "Tiempo por turno registrado",
    "Extracted from JSONL turn boundaries. Records the duration of each assistant turn for latency analysis.":
      "Extraído de los límites de turno JSONL. Registra la duración de cada turno asistido para el análisis de latencia.",
    "Claude Code CLI process exits": "Salida del proceso CLI de Claude Code",
    'Drops the waiting flag. If the session is already in <span class="status-chip chip-error" ><span class="chip-dot"></span>Error</span >, the error state is preserved; otherwise marks all agents and the session as <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completed</span >. Evicts the session\'s transcript from the shared cache.':
      'Deja caer la bandera de espera. Si la sesión ya está en <span class="status-chip chip-error" ><span class="chip-dot"></span>error informático</span >, el estado de error se preserva; de lo contrario, marca a todos los agentes y la sesión como <span class="status-chip chip-completed" ><span class="chip-dot"></span>Completado</span >. la transcripción de la sesión del caché compartido.',
    "Clone the repository to your machine": "Clona el repositorio a tu máquina",
    "Run <code>npm run setup</code> to install all dependencies":
      "carrera <code>npm run setup</code> Para instalar todas las dependencias",
    "Run <code>npm run dev</code> — server + client launch automatically":
      "carrera <code>npm run dev</code> — lanzamiento automático del servidor + cliente",
    "Start a new Claude Code session — events appear in real-time":
      "Inicia una nueva sesión de Claude Code: los eventos aparecen en tiempo real.",
    "A multi-stage <code>Dockerfile</code> and <code>docker-compose.yml</code> are included. You can run the monitor with either Docker or Podman and keep the SQLite database in a named volume.":
      "Un multiestadio <code>Dockerfile</code> y <code>docker-compose.yml</code> están incluidos. Puedes ejecutar el monitor con Docker o Podman y mantener la base de datos SQLite en un volumen con nombre.",
    "Hooks auto-install in local mode": "Los ganchos se instalan automáticamente en modo local",
    "When you run the server directly on the host with <code>npm run dev</code> or <code>npm start</code>, it automatically writes Claude Code hook entries to <code>~/.claude/settings.json</code>. If you run the dashboard in Docker or Podman, install hooks from the host with <code>npm run install-hooks</code> after the container is up, then restart Claude Code. The installer refuses to run inside a container (issue #193) so it never writes a container-internal handler path into a bind-mounted host <code>~/.claude</code>; override with <code>CCAM_ALLOW_CONTAINER_HOOKS=1</code> only if Claude Code itself runs in the container.":
      "Cuando ejecutas el servidor directamente en el host con <code>npm run dev</code> Oregón <code>npm start</code>, escribe automáticamente entradas de gancho de código Claude en <code>~/.claude/settings.json</code>. es si ejecutas el panel de control en Docker o Podman, instala los ganchos del host con <code>npm run install-hooks</code> Después de que el contenedor esté activo, reinicie Claude Code. El instalador se niega a ejecutarse dentro de un contenedor (problema #193), por lo que nunca escribe una ruta de manejador interna del contenedor en un host montado en vinculación. <code>~/.claude</code>; anular con <code>CCAM_ALLOW_CONTAINER_HOOKS=1</code> Solo si Claude Code en sí mismo se ejecuta en el contenedor.",
    "This repository also ships a local MCP server under <code>mcp/</code> and extension scaffolding for both Claude Code and Codex. These are optional for the dashboard UI, but recommended for complete local-agent workflows. The MCP server supports stdio (for host integration), HTTP+SSE (for remote clients), and an interactive REPL (for operator debugging).":
      "Este repositorio también envía un servidor MCP local bajo <code>mcp/</code> Y andamios de extensión para tanto Claude Code como Codex. Estos son opcionales para la interfaz de usuario del panel de control, pero recomendados para flujos de trabajo completos de agentes locales. El servidor MCP admite stdio (para la integración del host), HTTP+SSE (para clientes remotos) y un REPL interactivo (para el depuración del operador).",
    "After starting a Claude Code session, you should see:":
      "Después de iniciar una sesión de Claude Code, deberías ver:",
    Page: "página",
    Expected: "Esperado",
    Sessions: "sesión",
    'Your session listed with status <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> (a fresh CLI is sitting at the prompt) — flips to <span class="status-chip chip-active"><span class="chip-dot"></span>Active</span> the moment Claude starts a turn':
      'Su sesión listada con estado <span class="status-chip chip-waiting"><span class="chip-dot"></span>espera</span> (una CLI nueva está en la consola de comandos) - cambia a <span class="status-chip chip-active"><span class="chip-dot"></span>activo</span> En el momento en que Claude comienza una vuelta',
    "Kanban Board": "Tablero Kanban",
    "A <em>Main Agent</em> card in the <strong>Waiting</strong> column until you type your first message; flips to <em>Working</em> on <code>UserPromptSubmit</code> / <code>PreToolUse</code> and back to <em>Waiting</em> after each <code>Stop</code>":
      "A <em>Agente principal</em> Tarjeta en el <strong>espera</strong> Columna hasta que escribas tu primer mensaje; se voltea a <em>de trabajo</em> en <code>UserPromptSubmit</code> / <code>PreToolUse</code> Y de vuelta a <em>espera</em> Después de cada uno <code>Stop</code>",
    "Activity Feed": "Feed de actividad",
    'Events streaming in; click any row to expand payload, use "Session →" to drill into session details':
      'Se están transmitiendo eventos; haga clic en cualquier fila para expandir el contenido de la carga útil, use "Sesión →" para profundizar en los detalles de la sesión',
    Dashboard: "panel de control",
    "Stats updating in real-time": "Actualización de estadísticas en tiempo real",
    "Start server before Claude Code": "Inicia el servidor antes de Claude Code",
    "Hooks only fire to a running server. If Claude Code was already running when you started the dashboard, restart the Claude Code session.":
      "Los ganchos solo se disparan a un servidor en ejecución. Si Claude Code ya estaba ejecutándose cuando iniciaste el panel de control, reinicia la sesión de Claude Code.",
    Variable: "variable",
    Default: "valor por defecto",
    Description: "descripción",
    "Port the Express server listens on": "Puerto en el que el servidor Express escucha",
    "Port used by the hook handler to reach the server (for custom port setups)":
      "Puerto utilizado por el gestor de ganchos para llegar al servidor (para configuraciones de puertos personalizadas)",
    "Base URL used by the local MCP server when calling dashboard APIs":
      "URL base utilizada por el servidor MCP local cuando se llaman a las API del panel de control",
    "MCP transport mode: <code>stdio</code>, <code>http</code>, <code>repl</code>":
      "Modo de transporte MCP: <code>stdio</code>, <code>http</code>, <code>repl</code>",
    "Port for the MCP HTTP+SSE server (only when <code>MCP_TRANSPORT=http</code>)":
      "Puerto para el servidor MCP HTTP+SSE (solo cuando <code>MCP_TRANSPORT=http</code>)",
    "Bind address for the MCP HTTP server": "Vincular la dirección para el servidor HTTP MCP",
    "Path to the SQLite database file": "Ruta al archivo de la base de datos SQLite",
    "Set to <code>0</code> to disable the dead-session liveness reap — the watchdog completes active local sessions only when their matching <code>claude</code> or <code>codex</code> process is gone; auto-disabled on Windows and in containers. Sessions forwarded from another machine (household hooks) report a non-POSIX <code>cwd</code> and are auto-skipped, so a mixed local + forwarded deployment no longer needs this off.":
      "Configura <code>0</code> para desactivar la recolección de actividad de sesiones inactivas: el vigilante solo completa sesiones locales activas cuando el proceso <code>claude</code> o <code>codex</code> correspondiente ya terminó; se desactiva automáticamente en Windows y contenedores. Las sesiones enviadas desde otra máquina (ganchos domésticos) informan de un <code>cwd</code> no POSIX y se omiten automáticamente, por lo que una implementación mixta local + reenviada ya no necesita desactivar esta opción.",
    "Idle gate for watchdog-tick liveness reaps — the transcript must not have been written for at least this long (last hook write is the fallback clock); startup passes skip the gate":
      "La puerta inactiva para la vigilia de la vida del reloj de pulso cosecha - la transcripción no debe haber sido escrita durante al menos tanto tiempo (la última escritura del gancho es el reloj de respaldo); las transiciones de arranque saltan la puerta",
    "Idle-working timeout the watchdog uses to recover an <code>Esc</code> cancel that left no transcript marker":
      "El tiempo de espera de trabajo inactivo que utiliza el rastreador para recuperar un <code>Esc</code> Cancelar eso no dejó ningún marcador de transcripción",
    "Poll interval for the background sync of <code>~/.claude/projects</code>; <code>0</code> disables the poll but keeps the filesystem watcher":
      "Intervalo de encuesta para la sincronización de fondo de <code>~/.claude/projects</code>; <code>0</code> Desactiva la encuesta pero mantiene el observador del sistema de archivos",
    "Set to <code>production</code> to serve built client from <code>client/dist/</code>":
      "Configurado en <code>production</code> Para servir clientes construidos desde <code>client/dist/</code>",
    "The server writes the following to <code>~/.claude/settings.json</code> on every startup:":
      "El servidor escribe lo siguiente a <code>~/.claude/settings.json</code> En cada inicio de sesión:",
    "Existing hooks are preserved. The installer only adds or updates entries containing <code>hook-handler.js</code>.":
      "Los ganchos existentes se conservan. El instalador solo agrega o actualiza entradas que contienen <code>hook-handler.js</code>.",
    Script: "guion",
    Command: "mando",
    "Install all dependencies (server + client)":
      "Instalar todas las dependencias (servidor + cliente)",
    "Start server + client in development mode with hot reload":
      "Iniciar el servidor + cliente en modo de desarrollo con carga rápida",
    "Start only the Express server with <code>--watch</code>":
      "Inicie solo el servidor Express con <code>--watch</code>",
    "Start only the Vite dev server": "Inicie solo el servidor de desarrollo Vite",
    "TypeScript check + Vite production build to <code>client/dist/</code>":
      "Verificación de TypeScript + construcción de producción de Vite para <code>client/dist/</code>",
    "Start Express in production mode serving built client":
      "Iniciar Express en modo de producción sirviendo al cliente construido",
    "Manually write Claude Code hooks to <code>~/.claude/settings.json</code>":
      "Escriba manualmente los ganchos del código Claude para <code>~/.claude/settings.json</code>",
    "Insert demo sessions, agents, and events (8 sessions / 23 agents / 106 events)":
      "Insertar sesiones de demostración, agentes y eventos (8 sesiones / 23 agentes / 106 eventos)",
    "Refresh token totals for imported sessions from their transcripts. Never lowers an existing total":
      "Actualiza los totales de Token de las sesiones importadas a partir de sus transcripciones. Nunca reduce un total existente",
    "Re-derive non-workflow token totals for Claude sessions whose transcripts are still on disk and reset the compaction baselines, preserving workflow and Codex rows. One-time repair for databases inflated by the old per-record usage sum; stop the dashboard before running it":
      "Vuelve a derivar los totales de Token ajenos a workflow de las sesiones de Claude cuya transcripci\u00f3n siga en disco y pone a cero las l\u00edneas base de compactaci\u00f3n, conservando las filas de workflow y de Codex. Es una reparaci\u00f3n \u00fanica para bases de datos infladas por la antigua suma de usage por registro; det\u00e9n el Dashboard antes de ejecutarlo",
    "Import historical Claude Code sessions from <code>~/.claude</code> with deep JSONL extraction (API errors, turn durations, thinking blocks, subagent data)":
      "Importar sesiones históricas del Código Claude desde <code>~/.claude</code> Con extracción profunda de JSONL (errores de API, duraciones de turno, bloqueos de pensamiento, datos de subagentes)",
    "Delete all data from the database (keeps schema)":
      "Borrar todos los datos de la base de datos (mantiene el esquema)",
    "Run all server and client tests": "Ejecutar todas las pruebas del servidor y del cliente",
    "Server integration tests only (Node built-in test runner)":
      "Solo pruebas de integración del servidor (ejecutador de pruebas integrado en Node)",
    "Client unit tests only (Vitest + Testing Library)":
      "Solo pruebas unitarias del cliente (Vitest + Biblioteca de pruebas)",
    "Install dependencies for the local MCP package under <code>mcp/</code>":
      "Instale las dependencias del paquete MCP local en <code>mcp/</code>",
    "Type-check MCP source without emitting build output":
      "Comprueba el tipo de la fuente MCP sin emitir la salida de la compilación",
    "Compile MCP server into <code>mcp/build/</code>":
      "Compilar el servidor MCP en <code>mcp/build/</code>",
    "Start MCP server (stdio transport — for MCP hosts)":
      "Iniciar el servidor MCP (transporte stdio - para hosts MCP)",
    "Start MCP HTTP+SSE server on port 8819 (Streamable HTTP + legacy SSE)":
      "Inicie el servidor MCP HTTP+SSE en el puerto 8819 (HTTP transmisible + SSE heredado)",
    "Start interactive MCP REPL with tab completion and colored output":
      "Inicie MCP REPL interactivo con completación de pestañas y salida coloreada",
    "Run MCP server in dev mode with <code>tsx</code> (stdio)":
      "Ejecute el servidor MCP en modo de desarrollo con <code>tsx</code> (estdio)",
    "Run MCP HTTP server in dev mode with <code>tsx</code>":
      "Ejecute el servidor HTTP MCP en modo de desarrollo con <code>tsx</code>",
    "Run MCP REPL in dev mode with <code>tsx</code>":
      "Ejecute MCP REPL en modo de desarrollo con <code>tsx</code>",
    "Build MCP container image with Docker (<code>agent-dashboard-mcp:local</code>)":
      "Construye la imagen de contenedor MCP con Docker (<code>agent-dashboard-mcp:local</code>)",
    "Build MCP container image with Podman (<code>localhost/agent-dashboard-mcp:local</code>)":
      "Construye la imagen de contenedor MCP con Podman (<code>localhost/agent-dashboard-mcp:local</code>)",
    "Run MCP server unit tests": "Ejecutar pruebas unitarias del servidor MCP",
    "Install Electron + electron-builder under <code>desktop/</code>; rebuilds <code>better-sqlite3</code> for Electron's ABI. Preflights the native <code>better-sqlite3</code> build; prints actionable setup help (incl. a no-toolchain alternative) on failure":
      "Instala Electron + electron-builder bajo <code>desktop/</code>; reconstruye <code>better-sqlite3</code> Para la ABI de Electron. Previa al nativo <code>better-sqlite3</code> Construir; imprime ayuda de configuración factible (incluyendo una alternativa sin cadena de herramientas) en caso de fallo",
    "Prebuild guard + <code>tsc</code> compile of the Electron main process into <code>desktop/out/</code>":
      "Guardia preconstruida + <code>tsc</code> Compilación del proceso principal de Electron en <code>desktop/out/</code>",
    "Build, then launch the desktop app against <code>desktop/out/main.js</code>":
      "Cree, luego lance la aplicación de escritorio contra <code>desktop/out/main.js</code>",
    "Desktop smoke test — spawn Electron and probe <code>/api/health</code>":
      "Prueba de humo en el escritorio: genera Electron y prueba <code>/api/health</code>",
    "Build <strong>both</strong> per-arch DMGs (arm64 + x64). Correct for release — slower (packages each arch)":
      "cuerpo <strong>ambos</strong> DMG por archivo (arm64 + x64). Correcto para la versión de lanzamiento, más lento (paquetes por cada archivo)",
    "Build an Apple-Silicon-only DMG — fast (~1 min), recommended for a single machine":
      "Cree un DMG exclusivo de Apple-Silicona: rápido (~1 min), recomendado para una sola máquina.",
    "Build an Intel-only DMG — fast (macOS host)":
      "Cree un DMG exclusivo para Intel: rápido (host macOS)",
    "Build one merged universal DMG (arm64 + x86_64 in a single file). Optional, slowest — not what the release ships":
      "Cree un DMG universal fusionado (arm64 + x86_64 en un solo archivo). Opcional, el más lento, no es lo que se envía con la versión de lanzamiento",
    "Build the Windows NSIS installer <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> (Windows host)":
      "Construir el instalador NSIS de Windows <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> (Hospedador de Windows)",
    "Build the no-install portable <code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code> (Windows host)":
      "Construye el portátil sin instalación <code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code> (Hospedador de Windows)",
    "Regenerate <code>desktop/assets/icon.ico</code> from <code>icon.png</code> (PowerShell + .NET; Windows host)":
      "regenerar <code>desktop/assets/icon.ico</code> de <code>icon.png</code> (PowerShell + .NET; host Windows)",
    "Format all files with Prettier": "Formatea todos los archivos con Prettier",
    "Check formatting without writing": "Comprueba el formato sin escribir nada",
    "Core dashboard telemetry is composed of three processes (Claude hook source, dashboard server, browser UI). When the local MCP sidecar is enabled, it integrates with the same dashboard API via stdio, HTTP+SSE, or interactive REPL transport.":
      "La telemetría del panel de control principal está compuesta por tres procesos (fuente de gancho Claude, servidor del panel de control, interfaz de usuario del navegador). Cuando se activa el sidecar MCP local, se integra con la misma API del panel de control a través de stdio, HTTP+SSE o transporte REPL interactivo.",
    "Full system architecture — Claude Code process → Hook Layer → Server → Browser":
      "Arquitectura completa del sistema: proceso Claude Code → Capa de conexión → Servidor → Navegador",
    "Agent status transitions driven by hook events. <code>waiting</code> is a real persisted status — agents start as <code>waiting</code> and return to it after each turn. Error recovery requires active user retry (<code>UserPromptSubmit</code> or <code>PreToolUse</code>). A background watchdog detects API errors in transcripts every 15 s. The same watchdog also recovers <code>Esc</code>-cancelled turns — via either the transcript <code>[Request interrupted by user]</code> marker or the idle-working timeout when <code>Esc</code> preceded any output — and moves the session to Waiting.":
      "Transiciones de estado del agente impulsadas por eventos de gancho. <code>waiting</code> Es un estado persistente real: los agentes comienzan como <code>waiting</code> Y vuelva a él después de cada turno. La recuperación de errores requiere que el usuario activo intente de nuevo (<code>UserPromptSubmit</code> Oregón <code>PreToolUse</code>). Un vigilante de fondo detecta errores de API en las transcripciones cada 15 segundos. El mismo vigilante también recupera <code>Esc</code>-vueltas canceladas, a través de la transcripción o de otra manera. <code>[Request interrupted by user]</code> Marcador o el tiempo de espera de funcionamiento inactivo cuando <code>Esc</code> precedió cualquier salida y mueve la sesión a Esperando.",
    "Session status lifecycle. <code>waiting</code> is a UI overlay — persisted as <code>active</code> with <code>awaiting_input_since</code> set and <code>awaiting_reason</code> recording why (<code>session_start</code>, <code>stop</code>, <code>notification</code>, or <code>interrupted</code>). <code>SessionEnd</code> preserves error state. Error recovery requires <code>UserPromptSubmit</code> or <code>PreToolUse</code>. The watchdog also recovers <code>Esc</code>-cancelled turns (marker or idle-timeout path) and moves the session to Waiting. The dashboard surfaces the reason on every Waiting badge (hover tooltip, plus an inline chip on the Sessions table and session detail header) and in a waiting-for-input banner on the session detail page.":
      "Ciclo de vida del estado de la sesión. <code>waiting</code> Es una superposición de interfaz de usuario - persistente como <code>active</code> con <code>awaiting_input_since</code> Configurar y <code>awaiting_reason</code> Grabando por qué (<code>session_start</code>, <code>stop</code>, <code>notification</code>, o <code>interrupted</code>). <code>SessionEnd</code> Preserva el estado de error. La recuperación de errores requiere <code>UserPromptSubmit</code> Oregón <code>PreToolUse</code>. también recupera <code>Esc</code>-cancela los turnos (ruta de marcador o tiempo de espera inactivo) y mueve la sesión a Esperando. La interfaz de usuario muestra la razón en cada distintivo de Esperando (pestaña de información al pasar el cursor sobre el objeto, además de un chip inline en la tabla de Sesiones y en el encabezado de detalles de la sesión) y en un banner de espera de entrada en la página de detalles de la sesión.",
    "Complete event ingestion from hook fire to browser re-render":
      "Ingestión completa del evento desde el fuego del gancho hasta la re-renderización del navegador",
    "Initial load + WebSocket subscription lifecycle":
      "Carga inicial + ciclo de vida de la suscripción de WebSocket",
    "Server module dependency graph": "Gráfico de dependencias del módulo del servidor",
    Module: "circuito",
    Responsibility: "responsabilidad",
    "Express app setup, middleware (CORS, JSON 1MB limit), route mounting, static serving in production, HTTP server, auto-hook installation on startup":
      "Configuración de la aplicación Express, middleware (CORS, límite de JSON de 1 MB), montaje de rutas, servicio estático en producción, servidor HTTP, instalación automática de ganchos al inicio de sesión",
    "SQLite connection, WAL/FK pragmas, schema migrations (<code >CREATE TABLE IF NOT EXISTS</code >), all prepared statements as a reusable <code>stmts</code> object. Tries <code>better-sqlite3</code> first, falls back to built-in <code>node:sqlite</code> via <code>compat-sqlite.js</code>":
      "Conexión SQLite, pragmas WAL/FK, migraciones de esquema (<code >CREATE TABLE IF NOT EXISTS</code >), todas las declaraciones preparadas como reutilizables <code>stmts</code> Objeto. Intenta <code>better-sqlite3</code> En primer lugar, vuelve a lo incorporado <code>node:sqlite</code> a través de <code>compat-sqlite.js</code>",
    "Compatibility wrapper giving Node.js built-in <code>node:sqlite</code> (<code>DatabaseSync</code>) the same API as <code>better-sqlite3</code> — pragma, transaction, prepare. Used as automatic fallback on Node 22+":
      "Envoltorio de compatibilidad que da Node.js incorporado <code>node:sqlite</code> (<code>DatabaseSync</code>) la misma API que <code>better-sqlite3</code> — pragma, transacción, preparar. Se utiliza como fallback automático en Node 22+",
    "WebSocket server on <code>/ws</code> path, 30s ping/pong heartbeat, typed <code>broadcast(type, data)</code> function":
      "Servidor WebSocket encendido <code>/ws</code> Camino, latido de ping/pong de 30s, escrito a mano <code>broadcast(type, data)</code> función",
    "Core event processing inside SQLite transactions. Auto-creates sessions/agents. Switch-case dispatch by hook type. Extracts token usage from Stop events.":
      "Procesamiento de eventos centrales dentro de las transacciones de SQLite. Crea automáticamente sesiones/agentes. Despacho de casos por tipo de gancho. Extrae el uso de tokens de los eventos Stop.",
    "CRUD with pagination. GET includes agent count via LEFT JOIN. POST is idempotent on session ID.":
      "CRUD con paginación. GET incluye el número de agentes a través de LEFT JOIN. POST es idempotente en el ID de la sesión.",
    "CRUD with status/session_id filtering. PATCH broadcasts <code>agent_updated</code>.":
      "CRUD con filtrado por estado/session_id. Transmisiones PATCH <code>agent_updated</code>.",
    "Read-only event listing with session_id filter and pagination.":
      "Lista de eventos de solo lectura con filtro session_id y paginación.",
    "Single aggregate query — total/active counts, status distributions, WS connection count.":
      "Consulta agregada única: conteos totales/activos, distribuciones de estado, conteo de conexiones WS.",
    "Extended analytics — token totals, tool usage counts, daily event/session trends, agent type distribution, event type breakdown, average events per session.":
      "Análisis extendido: totales de tokens, conteos de uso de herramientas, tendencias diarias de eventos/sesiones, distribución del tipo de agente, desglose del tipo de evento, eventos promedio por sesión.",
    "Model pricing CRUD (list, upsert, delete). Per-session and global cost calculation with pattern-based model matching and specificity sorting.":
      "Precio de modelos CRUD (lista, inserción, eliminación). Cálculo de costos por sesión y global con coincidencia de modelos basada en patrones y clasificación por especificidad.",
    "System info (DB size, row counts, hook status, server uptime). Data export as JSON. Session cleanup (abandon stale active sessions, purge old completed sessions). Clear all data. Reset pricing to defaults. Reinstall hooks.":
      "Información del sistema (tamaño de la base de datos, número de filas, estado del gancho, tiempo de actividad del servidor). Exportación de datos como JSON. Limpieza de sesiones (abandonar sesiones activas obsoletas, purgar sesiones antiguas completadas). Borrar todos los datos. Restablecer los precios a los valores predeterminados. Reinstalar los ganchos.",
    "Aggregate workflow visualization data (agent orchestration, tool transitions, collaboration networks, workflow patterns, model delegation, error propagation, concurrency, session complexity, compaction impact). Accepts <code>?status=active|completed</code> filter. Per-session drill-in with agent tree, tool timeline, and events.":
      "Datos de visualización agregada del flujo de trabajo (orquestación de agentes, transiciones de herramientas, redes de colaboración, patrones de flujo de trabajo, delegación de modelos, propagación de errores, concurrencia, complejidad de sesiones, impacto de la compactación). Acepta <code>?status=active|completed</code> Filtro. Entrenamiento detallado por sesión con árbol de agentes, línea de tiempo de herramientas y eventos.",
    "React component tree": "Árbol de componentes React",
    Purpose: "propósito",
    "<code>lib/api.ts</code>": "<code>lib/api.ts</code>",
    "Typed fetch wrapper — one method per REST endpoint. All return typed promises.":
      "Envoltorio de recuperación de texto — un método por punto final REST. Todo devuelve promesas de texto.",
    "<code>lib/types.ts</code>": "<code>lib/types.ts</code>",
    "TypeScript interfaces: <code>Session</code>, <code>Agent</code>, <code>DashboardEvent</code>, <code>Stats</code>, <code>Analytics</code>, <code>WSMessage</code>, plus all workflow-related types (<code>WorkflowData</code>, <code>SessionDrillIn</code>, etc). Status config maps.":
      "Interfaces de TypeScript: <code>Session</code>, <code>Agent</code>, <code>DashboardEvent</code>, <code>Stats</code>, <code>Analytics</code>, <code>WSMessage</code>, además de todos los tipos relacionados con el flujo de trabajo (<code>WorkflowData</code>, <code>SessionDrillIn</code>, etc). Mapa de configuración de estado.",
    "<code>lib/eventBus.ts</code>": "<code>lib/eventBus.ts</code>",
    "Set-based pub/sub. <code>subscribe(fn)</code> returns an unsubscribe function for clean useEffect teardown.":
      "Pub/sub basado en conjuntos. <code>subscribe(fn)</code> Devuelve una función de cancelación de suscripción para la desinstalación limpia de useEffect.",
    "<code>lib/format.ts</code>": "<code>lib/format.ts</code>",
    "Date/time formatting helpers — relative time, duration, ISO display.":
      "Ayudantes de formateo de fecha/hora: tiempo relativo, duración, visualización ISO.",
    "<code>hooks/useWebSocket.ts</code>": "<code>hooks/useWebSocket.ts</code>",
    "Auto-reconnecting WebSocket React hook. 2-second reconnect interval. Publishes messages to eventBus.":
      "Gancho React WebSocket de reconectividad automática. Intervalo de reconectividad de 2 segundos. Publica mensajes en eventBus.",
    "The dashboard is a Progressive Web App with its own <code>manifest.json</code> and Service Worker (<code>client/public/sw.js</code>). The landing page and wiki are also independent PWAs with separate manifests and service workers.":
      "El panel de control es una aplicación web progresiva con su propia <code>manifest.json</code> Y Trabajador de Servicio (<code>client/public/sw.js</code>). La página de inicio y la wiki también son PWAs independientes con manifiestos y trabajadores de servicio separados.",
    Surface: "superficie",
    Manifest: "manifiesto",
    "Service Worker": "Trabajador de servicio",
    Strategy: "estrategia",
    "<code>client/public/manifest.json</code>": "<code>client/public/manifest.json</code>",
    "<code>client/public/sw.js</code>": "<code>client/public/sw.js</code>",
    "Precaches app shell. Cache-first for static assets (JS/CSS bundles). Network-first for navigation with offline fallback. Skips <code>/api/*</code>, <code>/ws</code>, and Vite HMR. Preserves push notification handlers.":
      "Precarga la cáscara de la aplicación. Cache primero para activos estáticos (paquetes JS/CSS). Red primero para la navegación con fallback sin conexión. Saltos <code>/api/*</code>, <code>/ws</code>, y Vite HMR. Preserva los manejadores de notificaciones push.",
    "Landing page": "página de aterrizaje",
    "<code>manifest.json</code> (root)": "<code>manifest.json</code> (raíz)",
    "<code>sw.js</code> (root)": "<code>sw.js</code> (raíz)",
    "Precaches HTML shell, favicon, OG image. Lazy-caches screenshot PNGs on first view. Network-first HTML, cache-first assets.":
      "Precarga la cáscara HTML, el icono favorito e la imagen OG. Carga en caché las capturas de pantalla PNG en la primera vista. HTML primero en la red, activos primero en la caché.",
    Wiki: "wiki",
    "<code>wiki/manifest.json</code>": "<code>wiki/manifest.json</code>",
    "<code>wiki/sw.js</code>": "<code>wiki/sw.js</code>",
    "Precaches <code>index.html</code>, <code>style.css</code>, <code>script.js</code>. Fully offline after one visit.":
      "Precarga <code>index.html</code>, <code>style.css</code>, <code>script.js</code>. completamente desactivado después de una visita.",
    'All three SWs call <code>skipWaiting()</code> on install and delete stale caches on activate (keyed by version strings like <code>dashboard-v1</code>). Manifests use SVG icons (<code>favicon.svg</code>) with <code>sizes="any"</code>. iOS standalone mode is enabled via <code>apple-mobile-web-app-capable</code> meta tags.':
      'Los tres SW llaman <code>skipWaiting()</code> Al instalar y eliminar cachés obsoletos al activar (claveada por cadenas de versión como <code>dashboard-v1</code>). Manifesta el uso de iconos SVG (<code>favicon.svg</code>) con <code>sizes="any"</code>. El modo independiente de iOS está habilitado a través de <code>apple-mobile-web-app-capable</code> Etiquetas meta.',
    "The client deliberately avoids Redux / Zustand / Context. Each page owns its data and lifecycle. WebSocket events trigger a reload or append — no complex state merging.":
      "El cliente evita deliberadamente Redux / Zustand / Context. Cada página posee sus propios datos y ciclo de vida. Los eventos WebSocket desencadenan una recarga o un apéndice, sin fusión compleja de estados.",
    "No global store — by design": "Sin tienda global, por diseño",
    "There is no cross-page shared state. Each page fetches and owns exactly the data it displays. This simplifies debugging and avoids stale-closure hazards that are common with global stores in long-running WebSocket apps.":
      "No hay estado compartido entre páginas. Cada página recupera y posee exactamente los datos que muestra. Esto simplifica el depuración y evita los peligros de cierre obsoleto que son comunes con las almacenes globales en aplicaciones WebSocket de larga duración.",
    Index: "índice",
    Table: "mesa",
    "Column(s)": "Columna(s)",
    "Fast agent lookup by session": "Búsqueda rápida de agentes por sesión",
    "Kanban column queries": "Consultas de columnas Kanban",
    "Session detail event list": "Lista de eventos de detalles de la sesión",
    "Filter events by type": "Filtrar eventos por tipo",
    "Activity feed ordering": "Pedido de alimentación de actividad",
    "Status filter on sessions page": "Filtro de estado en la página de sesiones",
    "Default sort order": "Orden de clasificación predeterminado",
    Pragma: "Pragma",
    Value: "valor",
    Rationale: "razones",
    "Concurrent reads during writes. Far better for read-heavy dashboards.":
      "Lecturas concurrentes durante las escrituras. Mucho mejor para paneles de control con mucha carga de lectura.",
    "Referential integrity — prevents orphaned agents/events.":
      "Integridad referencial: evita agentes/eventos huérfanos.",
    "Wait up to 5s for write lock instead of failing immediately under load.":
      "Espera hasta 5 segundos para el bloqueo de escritura en lugar de fallar inmediatamente bajo carga.",
    'All endpoints return JSON. Errors follow <code>{ "error": { "code", "message" } }</code>. The OpenAPI 3.0 spec comprehensively documents every backend route - parameters, request/response schemas, field descriptions, and examples. It is served at <code>/api/openapi.json</code> (with a committed <code>openapi.yaml</code> mirror), rendered as interactive Swagger UI at <code>/api/docs</code>, and as a clean, read-optimized ReDoc reference at <code>/api/redoc</code>. ReDoc is self-hosted, so it works fully offline.':
      'Todos los puntos finales devuelven JSON. Seguen los errores <code>{ "error": { "code", "message" } }</code>. API 3.0 especifica documenta de forma exhaustiva cada ruta de backend: parámetros, esquemas de solicitud/respuesta, descripciones de campos y ejemplos. Se sirve en <code>/api/openapi.json</code> (con un comprometido <code>openapi.yaml</code> Espejo), renderizado como una interfaz de usuario interactiva de Swagger en <code>/api/docs</code>, y como una referencia ReDoc limpia y optimizada para la lectura en <code>/api/redoc</code>. Doc es auto alojado, por lo que funciona completamente sin conexión.',
    '<span class="caption-icon">📘</span> <span>Swagger UI at <code>/api/docs</code> — auto-generated interactive playground for every REST endpoint. Try-it-out forms, request/response schema, auth headers, and curl snippets</span>':
      '<span class="caption-icon">📘</span> <span>Interfaz de usuario Swagger en <code>/api/docs</code> — patio de recreo interactivo generado automáticamente para cada punto final REST. Formularios de prueba, esquema de solicitud/respuesta, encabezados de autenticación y fragmentos de curl</span>',
    Property: "propiedad",
    Path: "recorrido",
    Protocol: "protocolo de comunicación",
    "Standard WebSocket (RFC 6455)": "WebSocket estándar (RFC 6455)",
    Heartbeat: "latido",
    "Server pings every 30s — clients that don't pong are terminated":
      "El servidor envía ping cada 30 segundos, los clientes que no envían ping se terminan.",
    Reconnect: "volver a conectar",
    "Client retries every 2 seconds on disconnect":
      "El cliente vuelve a intentarlo cada 2 segundos al desconectarse",
    "Client WebSocket auto-reconnect state machine":
      "Máquina de estado de conexión automática de WebSocket del cliente",
    "<code>scripts/hook-handler.js</code> is a minimal, fail-safe forwarder. It always exits 0 so it can never block Claude Code regardless of server state.":
      "<code>scripts/hook-handler.js</code> Es un reenvío mínimo y seguro de fallo. Siempre sale 0 para que nunca pueda bloquear el código Claude independientemente del estado del servidor.",
    "hook-handler.js flow — always exits 0, never blocks Claude Code":
      "Flow de hook-handler.js: siempre sale 0, nunca bloquea el código de Claude.",
    "Hook installation is idempotent — safe to run multiple times":
      "La instalación del gancho es idempotente, es decir, es segura para ejecutarse varias veces.",
    '<span class="caption-icon">📥</span> <span>Settings → Import History — rescan default paths, set a custom directory, or drag-and-drop <code>.gz</code> archives. Progress bar and result card show counts for every run</span>':
      '<span class="caption-icon">📥</span> <span>Configuración → Historial de importación — volver a escanear los rutas predeterminadas, establecer una carpeta personalizada o arrastrar y soltar <code>.gz</code> Archivos. La barra de progreso y la tarjeta de resultados muestran los conteos para cada ejecución</span>',
    "The dashboard ships with a first-class <b>history importer</b> that backfills sessions, agents, events, tokens, and costs from Claude Code JSONL transcripts. Live hook ingestion and manual import share the exact same parser — <code>parseSessionFile</code> + <code>importSession</code> in <code>scripts/import-history.js</code> — which is the architectural contract that guarantees imported token totals and cost values are identical to those captured in real time. Re-imports are idempotent: session IDs are the dedup key and compaction <code>baseline_*</code> columns preserve pre-compaction token totals.":
      "El panel de instrumentos viene con una primera clase <b>Importador de historial</b> que rellena las sesiones, agentes, eventos, tokens y costos de las transcripciones de Claude Code JSONL. La ingestión de enlaces en vivo e importación manual comparten el mismo parser exactamente - <code>parseSessionFile</code> + <code>importSession</code> indio <code>scripts/import-history.js</code> — que es el contrato arquitectónico que garantiza que los totales de tokens importados y los valores de costo sean idénticos a los capturados en tiempo real. Las reimportaciones son idempotentes: los ID de sesión son la clave de deduplicación y la compactación <code>baseline_*</code> Las columnas conservan los totales de tokens de precompactación.",
    "All three modes funnel into the same parser and DB transaction — imported numbers match live capture bit-for-bit":
      "Los tres modos convergen en el mismo analizador y transacción de la base de datos: los números importados coinciden bit a bit con la captura en vivo.",
    "Upload path: multipart → safe extract → walk → parse → import — every temp dir reclaimed in <code>finally</code>":
      "Ruta de subida: multipart → extracto seguro → caminar → analizar → importar, cada directorio temporal recuperado en <code>finally</code>",
    "The <code>baseline_*</code> columns make cost monotonic under re-imports — compacted sessions retain pre-compaction usage for billing":
      "el <code>baseline_*</code> Las columnas hacen que el costo sea monótono bajo las reimportaciones: las sesiones compactadas conservan el uso previo a la compactación para la facturación",
    Layout: "diseño",
    Example: "ejemplo",
    Handling: "manipulación",
    "Default Claude Code": "Código Claude predeterminado",
    "Session transcript — extracts tokens, compactions, tool uses, turn durations":
      "Transcripción de la sesión: extrae tokens, compactaciones, usos de herramientas, duraciones de las rondas",
    "Default subagent": "Subagente predeterminado",
    "Paired with parent on discovery via <code>findSessionSubagents</code>":
      "Emparejado con el progenitor en la descubrimiento a través de <code>findSessionSubagents</code>",
    "Alternative subagent": "Subagente alternativo",
    "Paired with parent on discovery (second layout probed automatically)":
      "Emparejado con el padre en la exploración (segundo diseño explorado automáticamente)",
    "Orphan subagent": "Subagente huérfano",
    "No parent JSONL in source, but <code>sid</code> exists in DB":
      "No hay JSONL padre en el código fuente, pero <code>sid</code> existe en la BD",
    "<code>importFromDirectory</code> probes both layouts; attaches if the parent is found":
      "<code>importFromDirectory</code> Probará ambos diseños; se adjunta si se encuentra el padre",
    "Flat JSONL drop": "Caída plana de JSONL",
    "Recognized as a loose session transcript":
      "Reconocido como una transcripción de sesión suelta",
    Archives: "archivo",
    "Extracted into a per-request temp dir, then walked by the same importer":
      "Extraído en una carpeta temporal por solicitud, luego pasó por el mismo importador",
    "Single-file gzip": "Gzip de archivo único",
    "Gunzipped in streaming mode with running byte-counter size cap":
      "Desbloqueado en modo de transmisión con límite de tamaño del contador de bytes en ejecución",
    Threat: "amenaza",
    Mitigation: "moderación",
    "Path traversal via archive entries":
      "Recorrido del camino a través de las entradas del archivo",
    "<code>archive.safeJoin</code> resolves under the extraction root; any <code>..</code> or absolute path returns <code>null</code>":
      "<code>archive.safeJoin</code> Resuelve bajo la raíz de extracción; cualquier <code>..</code> O devoluciones de ruta absoluta <code>null</code>",
    "Zip / tar / gzip bombs": "Bombas Zip / tar / gzip",
    "<code>MAX_EXTRACT_BYTES</code> (default 4 GB) enforced by running byte counter; aborts with <code>ExtractionLimitError</code> → HTTP 413":
      "<code>MAX_EXTRACT_BYTES</code> (por defecto 4 GB) aplicado mediante el funcionamiento del contador de bytes; se interrumpe con <code>ExtractionLimitError</code> → HTTP 413",
    "Per-file upload size abuse": "Abuso del tamaño de carga de archivos por archivo",
    "multer <code>limits.fileSize = MAX_UPLOAD_BYTES</code> (default 1 GB)":
      "Abusar sexualmente <code>limits.fileSize = MAX_UPLOAD_BYTES</code> (por defecto 1 GB)",
    "Too many files per request": "Demasiados archivos por solicitud",
    "multer <code>limits.files = MAX_UPLOAD_FILES</code> (default 2000)":
      "Abusar sexualmente <code>limits.files = MAX_UPLOAD_FILES</code> (por defecto 2000)",
    "Unsupported file types": "Tipos de archivos no compatibles",
    "<code>fileFilter</code> drops them early and reports them in <code>rejected_files[]</code>":
      "<code>fileFilter</code> Los deja temprano y los informa <code>rejected_files[]</code>",
    "Concurrent upload temp-dir collisions":
      "Colisiones temporales de directorios de carga en paralelo",
    "Per-request temp dir on <code>req._ccamUploadDir</code>; created in multer <code>destination</code>, reclaimed in <code>finally</code>":
      "Directorio temporal por solicitud activado <code>req._ccamUploadDir</code>; creado en multer <code>destination</code>, recuperado en <code>finally</code>",
    "Arbitrary absolute path on <code>scan-path</code>":
      "Ruta absoluta arbitraria en <code>scan-path</code>",
    "Validated: must be absolute (after <code>~</code> expansion), exist, and be a directory":
      "Validado: debe ser absoluto (después de <code>~</code> Expansión), existir y ser un directorio",
    "Relative / traversal paths on <code>scan-path</code>":
      "Rutas relativas / de recorrido en <code>scan-path</code>",
    "Rejected with <code>INVALID_INPUT</code>": "Rechazado con <code>INVALID_INPUT</code>",
    "Maximum size per uploaded file on <code>/api/import/upload</code>":
      "Tamaño máximo por archivo cargado en <code>/api/import/upload</code>",
    "Maximum files per upload request": "Archivos máximos por solicitud de carga",
    "Ceiling on total uncompressed bytes from any single archive (zip-bomb defense)":
      "Techo sobre los bytes totales no comprimidos de cualquier archivo individual (defensa contra zip-bomb)",
    "Every import emits <code>import.progress</code> messages on <code>/ws</code>. Messages are throttled to at most one every ~150 ms to avoid flooding the channel on multi-thousand-session imports; the terminal <code>complete</code> and <code>error</code> frames are never throttled.":
      "Cada importación emite <code>import.progress</code> Mensajes en <code>/ws</code>. se limitan a un máximo de un mensaje cada ~150 ms para evitar inundar el canal en importaciones de múltiples sesiones de miles de usuarios; el terminal <code>complete</code> y <code>error</code> Los fotogramas nunca se limitan.",
    "Phases: <code>start</code> → <code>scan</code> → <code>extract</code> (upload only) → <code>parse</code> → <code>complete</code>, with <code>error</code> / <code>extract_error</code> replacing <code>complete</code> on failure.":
      "Fases: <code>start</code> → <code>scan</code> → <code>extract</code> (solo para subir archivos) → <code>parse</code> → <code>complete</code>, con <code>error</code> / <code>extract_error</code> Reemplazando <code>complete</code> En caso de fracaso.",
    "In addition to dashboard telemetry, this project includes a production-grade local MCP server and complete extension scaffolding for both Claude Code and Codex. This gives agents a richer local tool surface while keeping all execution local-first. The MCP server supports three transport modes: stdio for host integration, HTTP+SSE for remote clients, and an interactive REPL for operator debugging.":
      "Además de la telemetría del panel de control, este proyecto incluye un servidor MCP local de grado de producción y una estructura de escalera de extensión completa para tanto Claude Code como Codex. Esto da a los agentes una superficie de herramienta local más rica mientras mantiene toda la ejecución local primero. El servidor MCP admite tres modos de transporte: stdio para la integración del host, HTTP+SSE para los clientes remotos y un REPL interactivo para el depuración del operador.",
    '<span class="caption-icon">🔧</span> MCP Server REPL — interactive tool invocation terminal with colored JSON output, argument prompts, error formatting, and session-aware context for rapid testing':
      '<span class="caption-icon">🔧</span> MCP Server REPL: terminal de invocación de herramientas interactivas con salida JSON coloreada, mensajes de inicio de sesión, formato de errores y contexto consciente de sesiones para pruebas rápidas.',
    "Local extension architecture: host instructions + skills + multi-transport MCP sidecar":
      "Arquitectura de extensión local: instrucciones del host + habilidades + MCP sidecar de transporte múltiple",
    "The <code>mcp/</code> package exposes dashboard-oriented tools for AI agents across three transport modes. Mutation and destructive operations are policy-gated by environment variables and disabled by default. HTTP mode serves both Streamable HTTP (protocol 2025-11-25) and legacy SSE (protocol 2024-11-05). REPL mode provides tab-completed interactive tool invocation with colored output and JSON syntax highlighting.":
      "el <code>mcp/</code> El paquete expone herramientas orientadas a la consola para agentes de IA en tres modos de transporte. Las mutaciones y las operaciones destructivas están restringidas por políticas mediante variables ambientales y están desactivadas por defecto. El modo HTTP sirve tanto para HTTP transmisible (protocolo 2025-11-25) como para SSE heredado (protocolo 2024-11-05). El modo REPL proporciona la invocación interactiva de herramientas completadas con pestañas con salida coloreada y resaltado de sintaxis JSON.",
    Component: "componente",
    Location: "lugar",
    Notes: "Notas",
    "MCP source": "Fuente MCP",
    "TypeScript server, tools, policy guards, transport layer, CLI UI":
      "Servidor TypeScript, herramientas, guardias de políticas, capa de transporte, interfaz de usuario de línea de comandos",
    "MCP build output": "Salida de la construcción de MCP",
    "Compiled JavaScript runtime for all transport modes":
      "Ejecución compilada de JavaScript para todos los modos de transporte",
    "MCP docs": "Documentos MCP",
    "Tool catalog, architecture diagrams, host integration examples, REPL guide":
      "Catálogo de herramientas, diagramas de arquitectura, ejemplos de integración de host, guía de REPL",
    "Transport layer": "Nivel de transporte",
    "HTTP+SSE server, interactive REPL, tool handler collector":
      "Servidor HTTP+SSE, REPL interactivo, recopilador de manejadores de herramientas",
    "CLI UI": "CLI UI",
    "ANSI banner, colors, formatter with tables, boxes, JSON highlighting":
      "Banner ANSI, colores, formateador con tablas, cajas, resaltado JSON",
    "Runtime commands": "Comandos de tiempo de ejecución",
    "Start MCP in stdio, HTTP+SSE, or REPL mode (production or dev)":
      "Inicie MCP en modo stdio, HTTP+SSE o REPL (producción o desarrollo)",
    Target: "blanco",
    Files: "Archivos",
    "Claude Code": "Código Claude",
    "Persistent project instructions + path-scoped coding rules":
      "Instrucciones persistentes del proyecto + reglas de codificación con alcance de ruta",
    "Claude Code Skills": "Habilidades de código Claude",
    "Reusable workflows (onboarding, shipping, MCP ops, live debugging)":
      "Procesos de trabajo reutilizables (integración, envío, operaciones MCP, depuración en vivo)",
    "Claude Code Subagents": "Subagentes del código Claude",
    "Specialized reviewers for backend, frontend, and MCP code paths":
      "Revisores especializados para rutas de código de backend, frontend y MCP",
    "Codex Base Instructions": "Instrucciones básicas del Codex",
    "Project-wide guidance + execution policy defaults":
      "Guía general para todo el proyecto + valores predeterminados de la política de ejecución",
    "Codex Skills": "Habilidades del Codex",
    "Task-specific skills aligned to this repository":
      "Habilidades específicas para tareas alineadas con este repositorio",
    "Codex Agents": "Agentes del Codex",
    "Reusable custom-agent templates for implementation and review":
      "Plantillas de agentes personalizadas reutilizables para la implementación y revisión",
    Role: "papel",
    "Receives Claude hook payloads over stdin and forwards them to dashboard API":
      "Recibe cargas útiles de gancho Claude a través de stdin y las envía a la API del panel de control",
    "Writes/updates hook registration in <code>~/.claude/settings.json</code>":
      "Escribe/actualiza el registro del gancho en <code>~/.claude/settings.json</code>",
    "Batch history importer used by server startup auto-import, the <code>/api/import/*</code> routes, and the <code>import-history</code> CLI. Exposes <code>importAllSessions()</code> for the default projects dir and the generalized <code>importFromDirectory(dbModule, rootDir, {onProgress})</code> which walks any directory recursively, classifies session vs subagent JSONLs (probes both <code>&lt;proj&gt;/&lt;sid&gt;/subagents/*</code> and <code>&lt;proj&gt;/subagents/&lt;sid&gt;/*</code> layouts), and funnels everything through the shared <code>parseSessionFile</code> + <code>importSession</code> pipeline — identical to live ingest. <b>Re-import is fully incremental</b>: a per-event-type high-water mark (<code>MAX(created_at) GROUP BY event_type</code> for the session) drives <code>ts &gt; cutoff[type]</code> dedup for Stop / PostToolUse / TurnDuration / ToolError, so long-running sessions whose transcripts grow across multiple days keep receiving new events on every re-run. <code>sessions.ended_at</code> is rolled forward when the JSONL has progressed past the stored value, and message-count metadata is refreshed on every pass. Session-ID dedup and <code>baseline_*</code> preservation keep token totals stable. Extracts tokens, API errors, turn durations, thinking blocks, usage extras, and per-subagent breakdowns":
      "Importador de historial de lotes utilizado por la importación automática de inicio del servidor, el <code>/api/import/*</code> Rutas, y el <code>import-history</code> CLI. Expone <code>importAllSessions()</code> Para la carpeta de proyectos predeterminada y la generalizada <code>importFromDirectory(dbModule, rootDir, {onProgress})</code> Que recorre cualquier directorio recursivamente, clasifica JSONL de sesiones vs subagentes (probar ambos <code>&lt;proj&gt;/&lt;sid&gt;/subagents/*</code> y <code>&lt;proj&gt;/subagents/&lt;sid&gt;/*</code> Diseños de plantillas), y canaliza todo a través del compartido <code>parseSessionFile</code> + <code>importSession</code> túnel de tuberías — idéntico a la ingestión en vivo. <b>La reimportación es totalmente incremental</b>: un punto máximo de agua alta por tipo de evento (<code>MAX(created_at) GROUP BY event_type</code> Para la sesión) conduce <code>ts &gt; cutoff[type]</code> Deduplicación para Stop / PostToolUse / TurnDuration / ToolError, por lo que las sesiones de larga duración cuyas transcripciones crecen a lo largo de varios días siguen recibiendo nuevos eventos en cada ejecución. <code>sessions.ended_at</code> Se desplaza hacia adelante cuando el JSONL ha avanzado más allá del valor almacenado, y los metadatos de contador de mensajes se actualizan en cada pasada. Deduplicación de ID de sesión y <code>baseline_*</code> preservación mantiene los totales de tokens estables. Extrae tokens, errores de API, duraciones de turnos, bloqueos de pensamiento, extras de uso y desgloses por subagente.",
    "Express router for Import History. Four endpoints: <code>GET /api/import/guide</code> (OS-aware instructions + default-dir stats), <code>POST /api/import/rescan</code> (default <code>~/.claude/projects</code>), <code>POST /api/import/scan-path</code> (arbitrary absolute dir with <code>~</code> expansion), <code>POST /api/import/upload</code> (multer multipart). Each request uses a per-request temp dir reclaimed in <code>finally</code>. Progress broadcast as throttled <code>import.progress</code> WebSocket messages. Limits tunable via <code>CCAM_IMPORT_MAX_BYTES</code>, <code>CCAM_IMPORT_MAX_FILES</code>, <code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>":
      "Router Express para Historial de Importaciones. Cuatro puntos finales: <code>GET /api/import/guide</code> (Instrucciones conscientes del sistema operativo + estadísticas de la carpeta predeterminada), <code>POST /api/import/rescan</code> (por defecto <code>~/.claude/projects</code>), <code>POST /api/import/scan-path</code> (dirección absoluta arbitraria con <code>~</code> Expansión), <code>POST /api/import/upload</code> (multipart multipart). Cada solicitud utiliza una carpeta temporal por solicitud recuperada en <code>finally</code>. de transmisión de progreso como restringido <code>import.progress</code> Mensajes WebSocket. Los límites se pueden ajustar a través de <code>CCAM_IMPORT_MAX_BYTES</code>, <code>CCAM_IMPORT_MAX_FILES</code>, <code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>",
    "Safe archive extraction: <code>.zip</code> via <code>adm-zip</code>, <code>.tar</code>/<code>.tar.gz</code>/<code>.tgz</code> via <code>tar</code>, plain <code>.gz</code> streaming via <code>zlib</code>. Every entry validated through <code>safeJoin</code> which rejects absolute paths and <code>..</code> traversal before any bytes are written. Enforces a hard <code>MAX_EXTRACT_BYTES</code> cap (default 4 GB) with <code>ExtractionLimitError</code> surfaced as HTTP 413 — defense against zip/tar/gzip bombs":
      "Extracción segura del archivo: <code>.zip</code> a través de <code>adm-zip</code>, <code>.tar</code>/<code>.tar.gz</code>/<code>.tgz</code> a través de <code>tar</code>, simple <code>.gz</code> Transmisión en streaming a través de <code>zlib</code>. validada por cada entrada a través de <code>safeJoin</code> Que rechaza caminos absolutos y <code>..</code> Recorrido antes de que se escriban ningún byte. Impone un duro <code>MAX_EXTRACT_BYTES</code> Límite de almacenamiento (por defecto 4 GB) con <code>ExtractionLimitError</code> Surge como HTTP 413: defensa contra bombas zip/tar/gzip.",
    "Loads deterministic demo data for testing and demos":
      "Carga datos de demostración deterministas para pruebas y demostraciones",
    "Removes persisted rows while preserving schema":
      "Elimina las filas persistentes mientras preserva el esquema",
    "The Agent Monitor ships with an official Claude Code plugin marketplace containing ten production-ready plugins (53 skills, 14 agents, 30 slash commands, 3 CLI tools, 3 hook configs, and 1 MCP server). These extend Claude Code with skills, agents, hooks, CLI tools, and MCP integration — all grounded in the real data model (token tracking with compaction baselines, cost calculation via pattern-matched pricing rules, workflow intelligence with 11 datasets per session, and session metadata including thinking blocks, turn counts, and inference geography).":
      "El Monitor de Agentes viene con un mercado oficial de plugins de Claude Code que contiene diez plugins listos para producción (53 habilidades, 14 agentes, 30 comandos slash, 3 herramientas CLI, 3 configuraciones de ganchos y 1 servidor MCP). Estos amplían Claude Code con habilidades, agentes, ganchos, herramientas CLI e integración MCP, todo ello basado en el modelo de datos real (seguimiento de tokens con bases de compresión, cálculo de costos a través de reglas de precios que coinciden con patrones, inteligencia de flujo de trabajo con 11 conjuntos de datos por sesión y metadatos de sesión que incluyen bloques de pensamiento, conteos de turnos y geografía de inferencia).",
    Plugin: "Plugin",
    Skills: "Habilidades",
    Agent: "representante",
    "CLI Tools": "Herramientas CLI",
    Focus: "manifiesto",
    "Token usage (4 types + baselines), cost via pricing engine, daily trends, productivity scoring":
      "Uso de tokens (4 tipos + bases de datos), costo a través del motor de precios, tendencias diarias, puntuación de productividad",
    "Standup reports, sprint tracking, workflow optimization via 11 workflow intelligence datasets":
      "Informes de pie, seguimiento de sprints, optimización del flujo de trabajo a través de 11 conjuntos de datos de inteligencia de flujo de trabajo",
    "Session debugging, hook diagnostics, data export (JSON/CSV), system health":
      "Depuración de sesiones, diagnóstico de ganchos, exportación de datos (JSON/CSV), estado del sistema",
    "Pattern detection via tool flow transitions, anomaly alerting, optimization, session comparison":
      "Detección de patrones a través de transiciones de flujo de herramientas, alerta de anomalías, optimización, comparación de sesiones",
    "Budget guardrails: set budgets, forecast week/month-end spend, cost-threshold alerts, model-routing savings (fail-safe Stop hook)":
      "Barandas de presupuesto: establezca presupuestos, pronostique los gastos a finales de semana/mes, alertas de umbral de costo, ahorros de enrutamiento de modelos (gancho de parada de seguridad)",
    "Session forensics: search, timeline, transcript replay, per-cwd rollup, cleanup":
      "Perfil forense de sesiones: búsqueda, cronología, reproducción de transcripciones, agregación por directorio de trabajo, limpieza",
    "Multi-agent orchestration &amp; fleet intelligence: DAG map, delegation audit, concurrency, error propagation, fleet runs (11-dataset workflow intelligence API)":
      "Orquestación multiagente e inteligencia de flota: mapa DAG, auditoría de delegación, concurrencia, propagación de errores, ejecuciones de flota (API de inteligencia de flujo de trabajo de 11 conjuntos de datos)",
    "Reliability &amp; SLOs: error scan, API-error report, hook-failure audit, SLO check, regression alert":
      "Reliability &amp; SLOs: escaneo de errores, informe de errores API, auditoría de fallos de conexión, verificación de SLOs, alerta de regresión",
    "Claude Code config &amp; memory governance: config audit, memory review, skill/MCP/hook inventory (via the Config Explorer API)":
      "Configuración y gobernanza de la memoria de Claude Code: auditoría de configuración, revisión de memoria, inventario de habilidades/MCP/ganchos (a través de la API Config Explorer)",
    "Dashboard connector with MCP integration and one-line metric summaries":
      "Conector de panel de control con integración MCP y resúmenes métricos de una línea",
    "Each plugin follows the official Claude Code plugin specification. The marketplace manifest at <code>.claude-plugin/marketplace.json</code> catalogs all ten plugins. Each plugin directory contains:":
      "Cada complemento sigue la especificación oficial del complemento Claude Code. El manifiesto del mercado en <code>.claude-plugin/marketplace.json</code> catala los diez complementos. Cada directorio de complementos contiene:",
    "All plugins query the Agent Monitor API at <code>http://localhost:4820</code>. Key capabilities they leverage:":
      "Todos los complementos consultan la API del Monitor de Agentes en <code>http://localhost:4820</code>. capacidades clave que utilizan:",
    Capability: "aptitud",
    Details: "información interna",
    "Token tracking": "Seguimiento de tokens",
    "4 types (input, output, cache_read, cache_write) + 4 compaction baselines per model per session":
      "4 tipos (entrada, salida, cache_read, cache_write) + 4 líneas de base de compactación por modelo por sesión",
    "Cost calculation": "Cálculo de costos",
    "<code>(tokens / 1M) × rate_per_mtok</code> for each type; longest pattern match wins":
      "<code>(tokens / 1M) × rate_per_mtok</code> Para cada tipo; la coincidencia de patrones más larga gana",
    "Session metadata": "Métadatos de la sesión",
    "Event types": "Tipos de eventos",
    "Workflow intelligence": "Inteligencia de flujo de trabajo",
    "11 datasets: stats, orchestration (DAG), toolFlow, effectiveness, patterns, modelDelegation, errorPropagation, concurrency, complexity, compaction, cooccurrence":
      "11 conjuntos de datos: estadísticas, orquestación (DAG), toolFlow, eficacia, patrones, modelDelegation, errorPropagation, concurrencia, complejidad, compactación, coocurrencia",
    "Agent hierarchy": "Jerarquía de agentes",
    "Recursive parent/child tree with subagent_type, depth tracking via recursive CTE":
      "Árbol recursivo padre/hijo con subagente_type, seguimiento de profundidad a través de CTE recursivo",
    '📖 Full documentation: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/docs/PLUGINS.md"><code>docs/plugins.md</code></a>':
      '📖 Documentación completa: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/docs/PLUGINS.md"><code>docs/plugins.md</code></a>',
    '<span class="caption-icon">🖥️</span> Statusline — always-visible bar showing context window usage, token counts, active model, git branch, and session ID. Configurable segments with theme support':
      '<span class="caption-icon">🖥️</span> Línea de estado: barra siempre visible que muestra el uso de la ventana de contexto, el número de tokens, el modelo activo, la rama de git y el ID de la sesión. Segmentos configurables con soporte de tema',
    "The <code>statusline/</code> directory contains a standalone CLI statusline for Claude Code — completely independent of the web dashboard. It renders a color-coded bar at the bottom of the Claude Code terminal showing context window usage, per-direction token counts, session cost in USD, and git branch.":
      "el <code>statusline/</code> El directorio contiene una línea de estado CLI independiente para Claude Code, completamente independiente del panel de control web. Renderiza una barra codificada por colores en la parte inferior del terminal de Claude Code que muestra el uso de la ventana de contexto, el número de tokens por dirección, el costo de la sesión en USD y la rama de git.",
    Segment: "segmento",
    Source: "fuente",
    "Color Logic": "Lógica de color",
    "Always cyan": "Siempre cian",
    "Always green": "Siempre verde",
    "Always yellow, <code>~</code> prefix for home":
      "Siempre amarillo, <code>~</code> prefijo para casa",
    "Always magenta, hidden outside git repos":
      "Siempre magenta, oculto fuera de los repositorios de git",
    "Green &lt; 50%, Yellow 50–79%, Red ≥ 80%": "Verde < 50 %, Amarillo 50-79 %, Rojo ≥ 80 %",
    "Green <code>↑</code> input, cyan <code>↓</code> output, dim <code>c</code> cache reads":
      "verde <code>↑</code> Entrada, cian <code>↓</code> Salida, atenuación <code>c</code> Lecturas de caché",
    "Green &lt; $5, Yellow $5–$20, Red ≥ $20 (shown on API and subscription plans)":
      "Verde < 5 $, Amarillo 5-20 $, Rojo ≥ 20 $ (mostrado en la API y planes de suscripción)",
    "Add this to <code>~/.claude/settings.json</code>:":
      "Agregar esto a <code>~/.claude/settings.json</code>:",
    "No dependencies required": "No se requieren dependencias",
    "The statusline uses only Python 3.6+ stdlib (<code>sys</code>, <code>json</code>, <code>os</code>, <code>subprocess</code>). It fails silently on empty input or JSON errors and never blocks Claude Code.":
      "La línea de estado solo utiliza la biblioteca estándar de Python 3.6+ (<code>sys</code>, <code>json</code>, <code>os</code>, <code>subprocess</code>). Falla silenciosamente en entradas vacías o errores JSON y nunca bloquea el código Claude.",
    '<span class="caption-icon">🔌</span> Sidebar — live health, analytics, and deep navigation links':
      '<span class="caption-icon">🔌</span> Barra lateral: salud en vivo, análisis y enlaces de navegación profunda',
    "The <b>Claude Code Agent Monitor</b> is a premium, high-fidelity extension designed to minimize context switching for AI engineers. It brings the full power of the dashboard directly into VS Code, allowing you to monitor complex subagent orchestration without ever leaving your active code file.":
      "el <b>Monitor de agente de código Claude</b> Es una extensión premium de alta fidelidad diseñada para minimizar el cambio de contexto para los ingenieros de IA. Trae toda la potencia del panel de control directamente a VS Code, lo que le permite monitorear la orquestación compleja de subagentes sin nunca salir de su archivo de código activo.",
    "A dedicated Activity Bar view that performs background polling every 5 seconds. Includes a real-time <b>Agent Health</b> monitor tracking all 5 states (Working, Connected, Idle, Completed, Error) with native VS Code theme-aware icons and colors.":
      "Una vista dedicada de la barra de actividades que realiza encuestas en segundo plano cada 5 segundos. Incluye una en tiempo real <b>Salud del agente</b> Monitoriza el seguimiento de los 5 estados (Trabajando, Conectado, Inactivo, Completado, Error) con iconos y colores nativos de VS Code que son conscientes del tema.",
    "Aggregates data from multiple API endpoints to display high-signal metrics directly in the sidebar: <ul style=\"margin-top: 8px; color: var(--text-muted); font-size: 13px; list-style-type: '→ '; padding-left: 15px;\"> <li><b>Token Consumption</b>: Scaled tracking from 1k to 1.0B+ tokens.</li> <li><b>Live Cost Estimates</b>: Automatic USD cost calculation based on model pricing rules.</li> <li><b>Event Frequency</b>: Total events, daily sessions, and subagent spawning rates.</li> </ul>":
      "Agrupa datos de múltiples puntos finales de API para mostrar métricas de alto señal directamente en la barra lateral: <ul style=\"margin-top: 8px; color: var(--text-muted); font-size: 13px; list-style-type: '→ '; padding-left: 15px;\"> <li><b>Consumo de tokens</b>: Seguimiento escalonado desde 1k hasta más de 1.0B tokens.</li> <li><b>Estimaciones de costos en vivo</b>: Cálculo automático del costo en USD basado en las reglas de precios del modelo.</li> <li><b>Frecuencia de eventos</b>: Total de eventos, sesiones diarias y tasas de apareamiento de subagentes.</li> </ul>",
    "<b>Token Consumption</b>: Scaled tracking from 1k to 1.0B+ tokens.":
      "<b>Consumo de tokens</b>: Seguimiento escalonado desde 1k hasta más de 1.0B tokens.",
    "<b>Live Cost Estimates</b>: Automatic USD cost calculation based on model pricing rules.":
      "<b>Estimaciones de costos en vivo</b>: Cálculo automático del costo en USD basado en las reglas de precios del modelo.",
    "<b>Event Frequency</b>: Total events, daily sessions, and subagent spawning rates.":
      "<b>Frecuencia de eventos</b>: Total de eventos, sesiones diarias y tasas de apareamiento de subagentes.",
    "Renders the full React application within a native webview tab. Supports <b>Deep Linking</b>: one-click jump from the sidebar directly to specific views like the <i>Kanban Board</i>, <i>Analytics Hub</i>, or your <i>Last 10 Sessions</i>.":
      "Rendiza la aplicación React completa dentro de una pestaña de webview nativa. Soporta <b>Enlace profundo</b>: salto de un solo clic desde la barra lateral directamente a vistas específicas como la <i>Tablero Kanban</i>, <i>Centro de análisis</i>, o tu <i>Últimas 10 sesiones</i>.",
    "Seamlessly scans ports <code>5173</code> (Vite Dev) and <code>4820</code> (Production) on localhost. Automatically toggles between <b>Online</b> and <b>Offline</b> modes in the sidebar as you start or stop your local server.":
      "Escanea puertos sin problemas <code>5173</code> (Vite Dev) y <code>4820</code> (Producción) en localhost. Cambia automáticamente entre <b>en línea</b> y <b>autónomo</b> Modos en la barra lateral a medida que inicies o detengas tu servidor local.",
    "<strong>Zero-Config Setup</strong>": "<strong>Configuración de cero configuración</strong>",
    "The extension is designed to be plug-and-play. Once your server is running, the extension automatically discovers the API and begins streaming telemetry — no manual URL configuration required.":
      "La extensión está diseñada para ser fácil de usar. Una vez que su servidor esté en funcionamiento, la extensión descubre automáticamente la API y comienza a transmitir telemetría, sin necesidad de configuración manual de la URL.",
    '📖 Full developer guide: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/vscode-extension/README.md"><code>vscode-extension/README.md</code></a>':
      '📖 Guía completa para desarrolladores: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/vscode-extension/README.md"><code>vscode-extension/README.md</code></a>',
    "The dashboard ships as an optional <strong>native desktop application</strong> — a <code>desktop/</code> workspace that wraps the existing server and client into a macOS <code>.app</code> (distributed as a <code>.dmg</code>) and a Windows <code>.exe</code> (an NSIS installer plus a no-install portable build) you install once and forget. <code>desktop/</code> is a sibling workspace to <code>client/</code>, <code>server/</code>, <code>mcp/</code>, and <code>vscode-extension/</code>, built with <strong>Electron 35</strong>. It <strong>embeds the Express server in-process</strong> — it <code>require()</code>s <code>server/index.js</code> directly in the same Node runtime as the Electron main process (no child process, no IPC) — and renders the already-built React client in a <code>BrowserWindow</code>. Everything you see in the browser at <code>localhost:4820</code> lives inside this window, with native OS lifecycle on top.":
      "El panel de instrumentos se envía como opcional <strong>Aplicación nativa para escritorio</strong> — a <code>desktop/</code> Espacio de trabajo que envuelve el servidor y el cliente existentes en un macOS <code>.app</code> (distribuido como un <code>.dmg</code>) y una ventana de Windows <code>.exe</code> (un instalador NSIS más una construcción portátil sin instalación) lo instales una vez y olvidaslo. <code>desktop/</code> Es un espacio de trabajo hermano de <code>client/</code>, <code>server/</code>, <code>mcp/</code>, y <code>vscode-extension/</code>, construido con <strong>Electrón 35</strong>. lo <strong>Incorpora el servidor Express en el proceso</strong> — eso <code>require()</code>s <code>server/index.js</code> directamente en el mismo tiempo de ejecución del Nodo que el proceso principal de Electron (sin proceso hijo, sin IPC) y renderiza el cliente React ya construido en un <code>BrowserWindow</code>. Todo lo que ves en el navegador en <code>localhost:4820</code> Vive dentro de esta ventana, con el ciclo de vida del sistema operativo nativo encima.",
    '<span class="caption-icon">🍎🪟</span> <span>The full dashboard, natively on macOS <strong>and</strong> Windows — same React client, same Express server, real <code>BrowserWindow</code>. Menu-bar / notification-area (tray) icon included. Shipped as a macOS DMG and a Windows EXE (macOS shown) — see <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a>.</span>':
      '<span class="caption-icon">🍎🪟</span> <span>El panel completo, nativo en macOS <strong>y</strong> Windows: mismo cliente React, mismo servidor Express, real <code>BrowserWindow</code>. - Barra de menú / icono de área de notificaciones ( bandeja) incluido. Envío como DMG de macOS y EXE de Windows (se muestra macOS) - ver <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a>.</span>',
    '<span class="caption-icon">🪟</span> <span>The same dashboard as a native <strong>Windows</strong> app — real <code>BrowserWindow</code> with the native Windows window menu, live Activity Feed, and the Tabby companion. A notification-area (system tray) icon sits beside the clock for quick access.</span>':
      '<span class="caption-icon">🪟</span> <span>El mismo panel de control que un nativo <strong>ventana</strong> Aplicación — real <code>BrowserWindow</code> Con el menú de ventanas nativo de Windows, el Feed de Actividades en vivo y el compañero Tabby. Un icono de área de notificaciones (mesa de inicio del sistema) se encuentra junto al reloj para un acceso rápido.</span>',
    "<strong>One-line mental model</strong>": "<strong>Modelo mental de una línea</strong>",
    "<em>Electron is a window onto the same code.</em> The desktop app does not reimplement the dashboard — it hosts the exact server and client the standalone deployment runs. The only change outside <code>desktop/</code> is a behavior-preserving refactor of <code>server/index.js</code>: its post-listen bootstrap was extracted into an exported <code>startBackgroundServices()</code> so the embedded server runs exactly what <code>node server/index.js</code> runs.":
      "<em>Electron es una ventana a el mismo código.</em> La aplicación de escritorio no reimplementa el panel de control; alberga el servidor y el cliente exactos que ejecuta la implementación independiente. El único cambio fuera <code>desktop/</code> Es una refactorización que preserva el comportamiento de <code>server/index.js</code>: su bootstrap post-escucha se extrajo en un exportado <code>startBackgroundServices()</code> Así que el servidor incorporado ejecuta exactamente lo que <code>node server/index.js</code> Corre.",
    "The Electron main process hosts the embedded server <em>and</em> manages the window, tray, and menus. The renderer is just Chromium loading <code>http://127.0.0.1:&lt;port&gt;</code> — the same origin a normal browser would use.":
      "El proceso principal de Electron alberga el servidor integrado <em>y</em> Gestiona la ventana, la bandeja y los menús. El renderizador solo está cargando Chromium <code>http://127.0.0.1:&lt;port&gt;</code> — la misma origen que un navegador normal usaría.",
    "The desktop app embeds the Express server in-process — no child process, no IPC":
      "La aplicación de escritorio incorpora el servidor Express en el proceso, sin proceso hijo, sin IPC",
    "An always-on tray icon — the macOS menu bar (a tinted template glyph) or the Windows notification area (the colored <code>icon.ico</code>). A single click (left or right) opens a dropdown with a <strong>live status snapshot</strong> queried straight from SQLite at click time — server port, active sessions, working agents, events today — followed by <strong>Open Dashboard</strong>, <strong>Open in Browser</strong>, <strong>Restart Server</strong>, <strong>Show Logs</strong>, <strong>Open at Login</strong> (toggle), and <strong>Quit</strong>. The snapshot rows are clickable — they open the dashboard. The menu is rebuilt on each open so every value is current.":
      "Un icono de bandeja siempre encendido: la barra de menú de macOS (un símbolo de plantilla con color) o el área de notificaciones de Windows (el colorido <code>icon.ico</code>). Un solo clic (izquierda o derecha) abre un menú desplegable con un <strong>Captura instantánea del estado en vivo</strong> Consultado directamente desde SQLite en el momento del clic: puerto del servidor, sesiones activas, agentes en funcionamiento, eventos de hoy, seguido de <strong>Abrir Panel de control</strong>, <strong>Abrir en el navegador</strong>, <strong>Reiniciar el servidor</strong>, <strong>Mostrar registros</strong>, <strong>Abrir en el inicio de sesión</strong> (alternar), y <strong>dejar</strong>. Las filas de instantáneas son clicables, abren el panel de control. El menú se reconstruye en cada apertura para que cada valor sea actual.",
    "A standard native application menu — <code>About</code>, <code>Open at Login</code>, <code>File</code>, <code>Edit</code>, <code>View</code>, <code>Window</code>, <code>Help</code> — with <code>⌘R</code> / <code>Ctrl+R</code> wired to <em>View ▸ reload</em>. External links open in the system browser, never inside Electron. The <code>File ▸ Open Dashboard</code> item (<code>⌘1</code>) is macOS-only; on Windows/Linux the window-attached menu can't reopen a hidden window, so reopen from the tray's <strong>Open Dashboard</strong>.":
      "Un menú de aplicaciones nativas estándar - <code>About</code>, <code>Open at Login</code>, <code>File</code>, <code>Edit</code>, <code>View</code>, <code>Window</code>, <code>Help</code> — con <code>⌘R</code> / <code>Ctrl+R</code> Conectado a <em>Ver ▸ volver a cargar</em>. enlaces externos se abren en el navegador del sistema, nunca dentro de Electron. el <code>File ▸ Open Dashboard</code> Artículo (<code>⌘1</code>) solo es para macOS; en Windows/Linux el menú adjunto a la ventana no puede volver a abrir una ventana oculta, así que vuelva a abrir desde la bandeja <strong>Abrir Panel de control</strong>.",
    "Flip <em>Open at Login</em> in the tray or app menu — both platforms go through Electron's first-party <code>app.*LoginItemSettings</code> API. On macOS it registers via the modern <code>SMAppService</code> API and appears under <strong>System Settings → General → Login Items</strong>; on Windows it writes a per-user <code>HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run</code> entry, visible in <strong>Task Manager → Startup</strong>. When the app is launched at login it starts tray-only, with no window jumping into view (on Windows the login launch is detected via a <code>--ccam-hidden</code> argument).":
      "ponche de huevo <em>Abrir en el inicio de sesión</em> en la bandeja o en el menú de la aplicación, ambas plataformas pasan por el primero de Electron <code>app.*LoginItemSettings</code> API. En macOS se registra a través del moderno <code>SMAppService</code> API y aparece debajo de <strong>Configuración del sistema → General → Elementos de inicio de sesión</strong>; en Windows escribe un por usuario <code>HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run</code> Entrada, visible en <strong>Gestor de tareas → Inicio de sesión</strong>. cuando la aplicación se lanza al iniciar sesión, solo se inicia en la bandeja, sin que ninguna ventana salte a la vista (en Windows, el inicio de sesión se detecta a través de un <code>--ccam-hidden</code> argumento).",
    'Closing the window hides it — the embedded server keeps running, the tray icon stays, and the dock / taskbar icon stays too (a clickable "still alive" indicator). <strong>Quit</strong> (<code>⌘Q</code> / <code>Ctrl+Q</code>, app menu, or tray → Quit) pops a confirmation modal — press the Quit button or hit <code>⌘Q</code> / <code>Ctrl+Q</code> a second time to skip the prompt — and only then does the embedded server shut down, closing SQLite cleanly with a WAL checkpoint and removing this PID\'s entry from the discovery file.':
      'Cerrar la ventana lo oculta: el servidor incorporado sigue funcionando, el icono de la bandeja permanece y el icono del dock / barra de tareas también permanece (un indicador "sigue vivo" que se puede hacer clic). <strong>dejar</strong> (<code>⌘Q</code> / <code>Ctrl+Q</code>, menú de la aplicación o bandeja → Salir) aparece una modalidad de confirmación, presiona el botón Salir o pulsa <code>⌘Q</code> / <code>Ctrl+Q</code> una segunda vez para saltar el mensaje de inicio de sesión, y solo entonces el servidor incorporado se apaga, cerrando SQLite de forma limpia con un control de punto de vista WAL y eliminando la entrada de este PID del archivo de descubrimiento.',
    "Launch the desktop app and <code>npm run dev</code> at the same time and both stay real-time. Each server appends its <code>{port, pid, startedAt}</code> entry to <code>~/.claude/.agent-dashboard.json</code> on startup; the Claude Code hook handler reads that list and fan-outs every event to every live entry in parallel. Stale entries self-evict via a PID liveness check on read, so a crashed server can't misroute events to a dead port.":
      "Lanzar la aplicación para escritorio y <code>npm run dev</code> Al mismo tiempo y ambos permanecen en tiempo real. Cada aplicación del servidor añade su <code>{port, pid, startedAt}</code> Entrada a <code>~/.claude/.agent-dashboard.json</code> Al arrancar; el gestor de enlaces del código Claude lee esa lista y distribuye cada evento a cada entrada en vivo de forma paralela. Las entradas obsoletas se desalojan automáticamente mediante una comprobación de vitalidad PID al leer, por lo que un servidor que se bloquee no puede redirigir los eventos a una puerto muerto.",
    "Double-launching the app just focuses the existing window — no second server, no port collision, on every platform. The lock is acquired via <code>requestSingleInstanceLock()</code> before any server boots.":
      "El doble lanzamiento de la aplicación solo centra la ventana existente, sin un segundo servidor, sin colisión de puertos, en todas las plataformas. El bloqueo se adquiere a través de <code>requestSingleInstanceLock()</code> Antes de que cualquier servidor arranque.",
    "On its first owned-server boot the app auto-installs the Claude Code hooks into <code>~/.claude/settings.json</code> and starts the background services (update scheduler, config watcher, orphaned-run reconciliation) — so an install-only user (DMG or EXE) gets events flowing without ever running <code>npm run install-hooks</code> from a checkout.":
      "En su primer arranque en un servidor propio, la aplicación instala automáticamente los ganchos del código Claude en <code>~/.claude/settings.json</code> y inicia los servicios de fondo (actualizador de programadores, observador de configuración, reconciliación de ejecución huérfana), por lo que un usuario que solo instala (DMG o EXE) recibe eventos sin nunca ejecutarse. <code>npm run install-hooks</code> Desde una caja registradora.",
    "Two packaging realities — a read-only application bundle / install directory and (on macOS) the minimal <code>PATH</code> a Finder-launched app inherits — are handled automatically so installs survive updates and the <strong>Run Claude</strong> feature works out of the box on both macOS and Windows.":
      "Dos realidades de empaquetado: un paquete de aplicaciones de lectura única / directorio de instalación y (en macOS) el mínimo <code>PATH</code> Una aplicación lanzada por Finder hereda - se manejan automáticamente para que las instalaciones sobrevivan a las actualizaciones y el <strong>Corra Claude</strong> La función funciona de inmediato en macOS y Windows.",
    "<strong>Your data survives reinstalls and updates</strong>":
      "<strong>Sus datos sobreviven a las reinstalaciones y actualizaciones</strong>",
    "The SQLite database and VAPID keys live in a per-user app-data directory <em>outside</em> the application bundle / install dir — <code>~/Library/Application Support/Claude Code Monitor/data/</code> on macOS, <code>%APPDATA%\\Claude Code Monitor\\data\\</code> on Windows. <code>server-host.ts</code> points <code>DASHBOARD_DATA_DIR</code> at that per-user directory on boot. Because a packaged, code-signed, or app-translocated bundle is read-only, older builds that stored the database inside the bundle broke History Import; with the data directory now in app-data, your imported history and events persist across app reinstalls and updates (the Windows NSIS uninstaller keeps this data by default). After upgrading from a pre-fix build, re-run <strong>Import History → Rescan</strong> once to bridge the one-time gap.":
      "La base de datos SQLite y las claves VAPID viven en un directorio de datos de aplicaciones por usuario <em>afuera</em> El paquete de aplicaciones / directorio de instalación — <code>~/Library/Application Support/Claude Code Monitor/data/</code> En macOS, <code>%APPDATA%\\Claude Code Monitor\\data\\</code> En Windows. <code>server-host.ts</code> Puntos <code>DASHBOARD_DATA_DIR</code> en ese directorio por usuario al arrancar. Debido a que un paquete empaquetado, firmado con código o trasladado de aplicación es de solo lectura, las versiones anteriores que almacenaban la base de datos dentro del paquete rompieron la Importación de Historial; con el directorio de datos ahora en datos de la aplicación, su historial e eventos importados persisten a través de las reinstalaciones y actualizaciones de la aplicación (el desinstalador de Windows NSIS guarda estos datos por defecto). Después de actualizar desde una versión con prefijo, vuelva a ejecutar <strong>Historial de importación → Recanso de escaneo</strong> Una vez para cerrar la brecha única.",
    "<strong>The <code>claude</code> CLI is found automatically</strong>":
      "<strong>el <code>claude</code> El CLI se encuentra automáticamente</strong>",
    "A Finder- or Dock-launched macOS app inherits only launchd's minimal <code>PATH</code>, not your login shell's. At startup <code>shell-path.ts</code> recovers the user's login-shell <code>PATH</code> so the <strong>Run Claude</strong> feature can locate and spawn the <code>claude</code> CLI. (On Windows the process already inherits the user <code>PATH</code>, so no recovery step is needed.) If it still cannot be found, make sure <code>claude</code> is a real executable on your <code>PATH</code> — a shell alias or function cannot be spawned — and check the <code>user PATH resolved</code> line in the desktop log (<code>~/Library/Logs/Claude Code Monitor/desktop.log</code> on macOS, <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> on Windows).":
      "Una aplicación macOS lanzada desde Finder o Dock hereda solo el mínimo de launchd <code>PATH</code>, no del shell de inicio de sesión. Al arrancar <code>shell-path.ts</code> Recupera la cáscara de inicio de sesión del usuario <code>PATH</code> Así que el <strong>Corra Claude</strong> La función puede localizar y generar el <code>claude</code> CLI. (En Windows, el proceso ya hereda al usuario <code>PATH</code>, por lo que no se necesita ningún paso de recuperación.) Si aún no se puede encontrar, asegúrese de <code>claude</code> Es un ejecutable real en tu <code>PATH</code> — no se puede crear un alias o función de shell — y compruebe el <code>user PATH resolved</code> Línea en el registro del escritorio (<code>~/Library/Logs/Claude Code Monitor/desktop.log</code> En macOS, <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> En Windows).",
    "On launch the Electron main process picks a free port. If a healthy dashboard server already answers <code>/api/health</code> on port <code>4820</code> (for example, you ran <code>npm start</code> in a terminal), the app <strong>adopts</strong> that server instead of starting a second one — no double-binding, no SQLite contention. An adopted server is not owned by the app, so quitting leaves it running.":
      "Al iniciar, el proceso principal de Electron selecciona un puerto libre. Si un servidor de panel de control saludable ya responde <code>/api/health</code> En puerto <code>4820</code> (por ejemplo, corriste <code>npm start</code> En un terminal), la aplicación <strong>Adopta</strong> Ese servidor en lugar de iniciar uno segundo, sin doble vinculación, sin competencia SQLite. Un servidor adoptado no es propiedad de la aplicación, por lo que salir lo deja ejecutándose.",
    Step: "paso",
    "Port choice": "Elección del puerto",
    Adopt: "adoptar",
    "A healthy server already on <code>4820</code> is adopted as-is":
      "Un servidor saludable ya encendido <code>4820</code> Se adopta tal cual",
    Preferred: "preferido",
    "<code>4820</code> when free": "<code>4820</code> Cuando esté libre",
    Fallback: "Alternativa de respaldo",
    "The first free port in <code>4821</code>–<code>4829</code>":
      "El primer puerto libre en <code>4821</code>–<code>4829</code>",
    "Last resort": "última instancia",
    "A random high port when all of the above are taken":
      "Una alta de puerto aleatoria cuando se toman todos los anteriores",
    "Three ways to obtain the desktop app — the latest GitHub Release (best for most users), a per-commit CI artifact (fresher than the latest release), or a local build.":
      "Tres formas de obtener la aplicación para escritorio: la última versión de GitHub (la mejor para la mayoría de los usuarios), un artefacto CI por commit (más reciente que la última versión) o una compilación local.",
    'Open <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg> Releases → latest </a> and download the asset for your platform. The macOS and Windows Desktop CI jobs auto-publish a new <code>vX.Y.Z</code> release every time the version in <code>package.json</code> is bumped on <code>master</code>, so this link always points at the current build. Releases are public — no GitHub sign-in required.':
      'aire libre <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg> Lanzamientos → más recientes </a> y descargue el recurso para su plataforma. Los trabajos de CI de escritorio de macOS y Windows publican automáticamente un nuevo <code>vX.Y.Z</code> Lanzamiento cada vez que la versión en <code>package.json</code> Se golpea en <code>master</code>, por lo que este enlace siempre señala la versión actual. Las publicaciones son públicas, no se requiere iniciar sesión en GitHub.',
    Platform: "plataforma",
    Asset: "activo",
    "macOS (Apple Silicon)": "macOS (Silicona de Apple)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-arm64.dmg</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-arm64.dmg</code>",
    "Drag the <code>.app</code> into <code>/Applications</code>":
      "Arrastra el <code>.app</code> En <code>/Applications</code>",
    "macOS (Intel)": "macOS (Intel)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64.dmg</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64.dmg</code>",
    "Windows (installer)": "Windows (instalador)",
    "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>":
      "<code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code>",
    "NSIS installer — per-user, no admin elevation":
      "Instalador de NSIS: por usuario, sin elevación de administrador",
    "Windows (portable)": "Windows (portátil)",
    "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>":
      "<code>ClaudeCodeMonitor-&lt;ver&gt;-x64-portable.exe</code>",
    "Run without installing": "Ejecutar sin instalar",
    'Want a build straight off the tip of <code>master</code>, ahead of the next tagged release? Every green run of the <code>🍎 macOS Desktop (DMG)</code> job on <code>macos-latest</code> uploads both per-arch DMGs as the <code>ClaudeCodeMonitor-dmg</code> workflow artifact, and the <code>🪟 Windows Desktop (EXE)</code> job on <code>windows-latest</code> uploads the installer + portable EXEs as the <code>ClaudeCodeMonitor-win</code> artifact. Open the <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg> latest passing run </a>, scroll to its Artifacts section, and download <code>ClaudeCodeMonitor-dmg</code> or <code>ClaudeCodeMonitor-win</code>. (GitHub sign-in required; 14-day retention.)':
      'Quieres una construcción directamente desde la punta de <code>master</code>, ¿antes del próximo lanzamiento etiquetado? Cada carrera verde del <code>🍎 macOS Desktop (DMG)</code> Trabajo en curso <code>macos-latest</code> Sube tanto los DMG por archivo como el <code>ClaudeCodeMonitor-dmg</code> Artefacto del flujo de trabajo y el <code>🪟 Windows Desktop (EXE)</code> Trabajo en curso <code>windows-latest</code> Sube el instalador + los EXEs portátiles como el <code>ClaudeCodeMonitor-win</code> Artefacto. Abre el <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><polyline points="12 7 12 12 15 14"/></svg> Última carrera de paso </a>, desplázate hasta la sección de Artifactos y descarga <code>ClaudeCodeMonitor-dmg</code> Oregón <code>ClaudeCodeMonitor-win</code>. (Se requiere iniciar sesión en GitHub; retención de 14 días).',
    "From the project root, after <code>git clone</code>:":
      "Desde la raíz del proyecto, después de <code>git clone</code>:",
    "Use the arch-specific build on your own machine":
      "Utilice la construcción específica del arco en su propia máquina",
    "<code>desktop:dmg</code> is slower because it builds the full app tree <em>twice</em> (once per architecture) and ad-hoc-signs each — emitting two separate per-arch DMGs (<code>…-arm64.dmg</code> + <code>…-x64.dmg</code>). There is no <code>@electron/universal</code> merge; the release ships the two per-arch DMGs. For running on a single Mac, use <code>desktop:dmg:arm64</code> (Apple Silicon) or <code>desktop:dmg:x64</code> (Intel) — one architecture, finishing in roughly a minute. CI runs <code>desktop:dmg</code> and uploads both DMGs as <code>ClaudeCodeMonitor-dmg</code>, so you rarely need to build them yourself.":
      "<code>desktop:dmg</code> Es más lento porque construye el árbol completo de la aplicación <em>dos veces</em> (una vez por arquitectura) y firmas ad hoc para cada una de ellas, emitiendo dos DMG separados por arquitectura (<code>…-arm64.dmg</code> + <code>…-x64.dmg</code>). No hay <code>@electron/universal</code> Mezclar; la versión de lanzamiento envía los dos DMG por archivo. Para ejecutar en un solo Mac, use <code>desktop:dmg:arm64</code> (Silicona de Apple) o <code>desktop:dmg:x64</code> (Intel) - una arquitectura, terminando en aproximadamente un minuto. CI se ejecuta <code>desktop:dmg</code> Y sube ambos DMG como <code>ClaudeCodeMonitor-dmg</code>, por lo que rara vez necesitas construirlos tú mismo.",
    "Double-click the downloaded <code>.dmg</code> to mount it":
      "Haga doble clic en el descargado <code>.dmg</code> Para montarlo",
    "Drag <code>Claude Code Monitor.app</code> into your <code>Applications</code> folder":
      "resistencia <code>Claude Code Monitor.app</code> En tu <code>Applications</code> carpeta",
    "Run <code>xattr -cr</code> on the app to get past Gatekeeper (see below)":
      "carrera <code>xattr -cr</code> En la aplicación para pasar por el Guardián de Puerta (ver a continuación)",
    "Open the app — the tray icon appears and the dashboard window loads":
      "Abre la aplicación, aparece el icono de bandeja y se carga la ventana del panel de control.",
    "Gatekeeper warning on first launch":
      "Advertencia del guardián de puertas en el primer lanzamiento",
    'The DMG is ad-hoc signed by default — that is all the project can offer without a paid Apple Developer ID. macOS warns the first time you open it (<em>"Apple could not verify…"</em>). Strip the quarantine attribute to get past it:':
      'El DMG se firma de forma ad hoc por defecto, eso es todo lo que el proyecto puede ofrecer sin un ID de desarrollador de Apple pagado. macOS advierte la primera vez que lo abres (<em>"Apple no pudo verificar..."</em>). Quita el atributo de cuarentena para pasarlo:',
    "Alternatively, open <strong>System Settings → Privacy &amp; Security</strong>, find the blocked app, and click <em>Open Anyway</em>. Code signing and Apple notarization are opt-in for the maintainer — when configured, this warning goes away for everyone.":
      "Alternativamente, abrir <strong>Configuración del sistema → Privacidad y seguridad</strong>, encuentra la aplicación bloqueada y haz clic <em>Abrir de todos modos</em>. signing y notificación de Apple son opcionales para el mantenedor — cuando se configure, este aviso desaparece para todos.",
    "Run <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> — a per-user NSIS install (no admin), or run the <code>*-portable.exe</code> to skip installing":
      "carrera <code>ClaudeCodeMonitor-Setup-&lt;ver&gt;-x64.exe</code> — una instalación de NSIS por usuario (sin administrador), o ejecute el <code>*-portable.exe</code> Para saltar la instalación",
    "The EXE is unsigned by default, so SmartScreen may warn — click <em>More info → Run anyway</em>":
      "El EXE no está firmado por defecto, por lo que SmartScreen puede advertir: haga clic <em>Más información → Corra de todos modos</em>",
    "Open from the Start menu / desktop shortcut — the notification-area (tray) icon appears and the dashboard window loads":
      "Abrir desde el menú Inicio / atajo de escritorio: aparece el icono de la zona de notificaciones ( bandeja) y se carga la ventana del panel de control.",
    '<span class="caption-icon">1️⃣</span> <span>NSIS installer, step 1 — <strong>Choose Installation Options</strong>: pick per-user setup and optional shortcuts.</span>':
      '<span class="caption-icon">1️⃣</span> <span>Instalador de NSIS, paso 1 — <strong>Elija las opciones de instalación</strong>: seleccione la configuración por usuario y las pestañas rápidas opcionales.</span>',
    '<span class="caption-icon">2️⃣</span> <span>NSIS installer, step 2 — <strong>Choose Install Location</strong>: defaults to <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code>, or point it anywhere.</span>':
      '<span class="caption-icon">2️⃣</span> <span>Instalador de NSIS, paso 2 — <strong>Elija la ubicación de instalación</strong>: por defecto es <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code>, o apuntarlo a cualquier lugar.</span>',
    '<span class="caption-icon">3️⃣</span> <span>NSIS installer, step 3 — <strong>Completing Setup</strong>: click <em>Finish</em> to launch the app and drop the tray icon in the notification area.</span>':
      '<span class="caption-icon">3️⃣</span> <span>Instalador de NSIS, paso 3 — <strong>Completando la configuración</strong>: hacer clic <em>acabado</em> Para lanzar la aplicación y dejar el icono de bandeja en la zona de notificaciones.</span>',
    "SmartScreen warning on first launch": "Advertencia de SmartScreen en el primer lanzamiento",
    'The installer and portable EXE are <strong>unsigned</strong> by default — that is all the project can offer without a paid code-signing certificate. Windows <strong>SmartScreen</strong> may show <em>"Windows protected your PC"</em> the first time you run it; click <strong>More info → Run anyway</strong>. The installer lays the app down <strong>per-user</strong> under <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code> (and lets you choose the install directory) and sets an <code>AppUserModelId</code> (<code>com.hoangsonww.ccam.desktop</code>) so native toast notifications are attributed correctly and the window groups under one taskbar entry.':
      'El instalador y el EXE portátil son <strong>sin firmar</strong> Por defecto, eso es todo lo que el proyecto puede ofrecer sin un certificado de firma de código pagado. ventana <strong>SmartScreen</strong> Puede mostrar <em>"Windows protege tu PC"</em> La primera vez que lo ejecutes; haz clic <strong>Más información → Corra de todos modos</strong>. instala la aplicación. <strong>Por usuario</strong> bajo <code>%LOCALAPPDATA%\\Programs\\Claude Code Monitor</code> (y te permite elegir el directorio de instalación) y establece un <code>AppUserModelId</code> (<code>com.hoangsonww.ccam.desktop</code>) para que las notificaciones nativas de Toast se atribuyan correctamente y los grupos de ventanas se encuentren bajo una entrada de barra de tareas.',
    "Bundle size": "Tamaño del paquete",
    "The DMG is roughly 80&nbsp;MB, about 250&nbsp;MB installed on disk — the standard Electron tax; the Windows installer is comparable. The app runs natively on <strong>macOS and Windows</strong>; Linux is tracked as a follow-up. Logs live at <code>~/Library/Logs/Claude Code Monitor/desktop.log</code> on macOS or <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> on Windows (reach them from the tray menu → <em>Show Logs</em>).":
      "El DMG tiene aproximadamente 80 MB, unos 250 MB instalados en el disco, el impuesto estándar de Electron; el instalador de Windows es comparable. La aplicación se ejecuta nativamente en <strong>macOS y Windows</strong>; Linux se rastrea como seguimiento. Los registros están disponibles en <code>~/Library/Logs/Claude Code Monitor/desktop.log</code> En macOS o <code>%APPDATA%\\Claude Code Monitor\\logs\\desktop.log</code> En Windows (accedé a ellos desde el menú de la bandeja de inicio → <em>Mostrar registros</em>).",
    '📖 User-facing guide: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a> · architecture &amp; contributor reference: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/desktop/README.md"><code>desktop/README.md</code></a>':
      '📖 Guía para el usuario: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/DESKTOP.md"><code>DESKTOP.md</code></a> · arquitectura y referencia del colaborador: <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/blob/master/desktop/README.md"><code>desktop/README.md</code></a>',
    '<span class="caption-icon">⚙️</span> Settings — model pricing editor, hook installation toggle, JSON data export, session cleanup, browser notification preferences, and system info panel with DB stats':
      '<span class="caption-icon">⚙️</span> Configuración: editor de precios de modelos, interruptor de instalación de ganchos, exportación de datos JSON, limpieza de sesiones, preferencias de notificación del navegador e información del sistema con estadísticas de la base de datos.',
    "The <code>/settings</code> route provides a comprehensive management interface with six sections:":
      "el <code>/settings</code> Route proporciona una interfaz de gestión integral con seis secciones:",
    "Editable table of per-model pricing rules. Each Claude model variant has its own explicit pattern (e.g., <code>claude-opus-4-6%</code>). Rates cover input, output, cache read, and cache write tokens. Each rule's editor also has a collapsible <strong>Introductory rates</strong> block — a <code>YYYY-MM-DD</code> promo cutoff plus per-category intro prices (input / output / cache-read / cache-write 5m &amp; 1h); an empty date means no promo, and any future model-launch promo needs no code change. Reset to defaults or add custom models. The section header carries an info popover (the <code>i</code> icon) that explains how rule lookup works (first matching pattern wins), the SQL-style <code>%</code> wildcard syntax with concrete examples (<code>claude-opus-4-7%</code>, <code>claude-%-haiku</code>, exact ids), and reminds the user that prices must be updated manually when Anthropic publishes new rates — already-stored sessions keep the price applied at ingest time. The CLAUDE_HOME panel and Import History flow are fully i18n-driven across en/vi/zh.":
      "Tabla editable de las reglas de precios por modelo. Cada variante de modelo Claude tiene su propio patrón explícito (por ejemplo, <code>claude-opus-4-6%</code>). Las tarifas cubren tokens de entrada, salida, lectura de caché y escritura de caché. El editor de cada regla también tiene un colapsible <strong>Tarifas introductorias</strong> Bloqueo — un <code>YYYY-MM-DD</code> Límite de promoción más precios de introducción por categoría (entrada / salida / lectura de caché / escritura de caché 5 m y 1 h); una fecha vacía significa que no hay promoción, y cualquier promoción futura de lanzamiento de modelos no requiere ningún cambio de código. Reinicia a los valores predeterminados o agrega modelos personalizados. El encabezado de la sección contiene un popover de información (el <code>i</code> Icono) que explica cómo funciona la búsqueda de reglas (el patrón que coincida primero gana), el estilo SQL <code>%</code> Sintaxis de asterisco con ejemplos concretos (<code>claude-opus-4-7%</code>, <code>claude-%-haiku</code>, identificadores exactos), y recuerda al usuario que los precios deben actualizarse manualmente cuando Anthropic publique nuevas tarifas. Las sesiones almacenadas previamente mantienen el precio aplicado en el momento de la ingestión. El panel CLAUDE_HOME y el flujo de historial de importación están completamente impulsados por i18n en en/vi/zh.",
    "Shows per-hook installation status (SessionStart, PreToolUse, PostToolUse, Stop, SubagentStop, Notification, SessionEnd). One-click reinstall if hooks are missing or outdated. Validates paths and permissions automatically.":
      "Muestra el estado de instalación por gancho (SessionStart, PreToolUse, PostToolUse, Stop, SubagentStop, Notification, SessionEnd). Reinstalación con un solo clic si faltan o están desactualizados los ganchos. Valida automáticamente los rutas y permisos.",
    "View database row counts and size. Session cleanup: abandon stale active sessions after N hours, purge old completed sessions after N days. Danger zone: clear all data with confirmation dialog to prevent accidental loss.":
      "Ver el número de filas y el tamaño de la base de datos. Limpieza de sesiones: abandone las sesiones activas obsoletas después de N horas, purga las sesiones antiguas completadas después de N días. Zona de peligro: elimine todos los datos con el cuadro de diálogo de confirmación para evitar la pérdida accidental.",
    "Download all sessions, agents, events, token usage, and pricing rules as a single JSON file for backup or analysis. Includes full event history, model metadata, and cost breakdowns in one portable archive.":
      "Descargue todas las sesiones, agentes, eventos, uso de tokens y reglas de precios como un solo archivo JSON para respaldo o análisis. Incluye el historial completo de eventos, metadatos del modelo y desgloses de costos en un archivo portátil.",
    "Dedicated Health tab on the Dashboard with a composite health score (weighted from success rate, cache hit rate, error rate, and heap usage), storage engine donut chart, tool invocation frequency bars, subagent effectiveness, model token distribution, and compaction impact — all with cursor-following tooltips and 5-second auto-refresh.":
      "Pestaña de Salud dedicada en el Panel de Control con una puntuación de salud compuesta (pesada por la tasa de éxito, la tasa de éxito de la caché, la tasa de errores y el uso de la pila), gráfico de donut del motor de almacenamiento, barras de frecuencia de invocación de herramientas, eficacia del subagente, distribución de tokens del modelo e impacto de la compactación, todo con herramientas de ayuda que siguen el cursor y una actualización automática de 5 segundos.",
    "Configure native browser notifications with per-event toggles for session starts, completions, errors, and subagent spawns. Automatic permission management with test-send button and graceful fallback when denied.":
      "Configura las notificaciones del navegador nativo con interruptores por evento para el inicio, la finalización, los errores y el aparecimiento de subagentes de la sesión. Gestión automática de permisos con botón de envío de prueba y fallback elegante cuando se niega.",
    "Per-model pricing — no catch-all grouping": "Precio por modelo: sin agrupación general",
    "Each Claude model variant (e.g., Opus 4.6 vs Opus 4.1) has its own explicit pricing pattern because different model versions have different rates. The cost engine uses specificity sorting — longer patterns match before shorter ones.":
      "Cada variante de modelo Claude (por ejemplo, Opus 4.6 vs Opus 4.1) tiene su propio patrón de precios explícito porque las diferentes versiones de modelos tienen diferentes tarifas. El motor de costos utiliza la clasificación por especificidad: los patrones más largos coinciden antes que los más cortos.",
    "Turns the dashboard from passive viewing into active monitoring. A rules-based alerting engine evaluates the live event stream <strong>server-side</strong>, and fired alerts fan out to outbound <strong>webhook channels</strong>. Everything lives in one place — <strong>Settings → Alerts</strong> — behind a segmented control with three tabs: <strong>Rules</strong> (what triggers an alert), <strong>Channels</strong> (where alerts are delivered), and <strong>Activity</strong> (the live fired-alert feed with acknowledge / acknowledge-all).":
      "Convierte el panel de instrumentos de visualización pasiva en monitoreo activo. Un motor de alertas basado en reglas evalúa el flujo de eventos en vivo <strong>Lado del servidor</strong>, y las alertas lanzadas se extienden hacia el exterior <strong>Canales webhook</strong>. todo vive en un solo lugar — <strong>Configuración → Alertas</strong> — detrás de un control segmentado con tres pestañas: <strong>Reglas</strong> (lo que desencadena una alerta), <strong>canal</strong> (donde se entregan las alertas), y <strong>actividad</strong> (el feed de alerta de disparos en vivo con reconocer / reconocer-todos).",
    'Four condition types: <strong>event pattern</strong> (match <code>event_type</code> / <code>tool_name</code> / a summary substring, optionally requiring ≥ N matches within a rolling window — e.g. "5 errors in 2 minutes"), <strong>inactivity</strong> (an active session goes quiet for N minutes), <strong>status duration</strong> (an agent is stuck in <code>working</code> / <code>waiting</code> for N minutes), and <strong>token threshold</strong> (a session\'s cumulative tokens cross a limit). Each rule has a configurable <strong>cooldown</strong> that dedups repeat alerts per (rule, session, agent).':
      'Cuatro tipos de condiciones: <strong>patrón de eventos</strong> (coincidencia con <code>event_type</code>, <code>tool_name</code> o una subcadena del resumen; opcionalmente exige ≥ N coincidencias dentro de una ventana móvil, por ejemplo, "5 errores en 2 minutos"), <strong>inactividad</strong> (una sesión activa permanece inactiva durante N minutos), <strong>duración del estado</strong> (un Agent permanece en <code>working</code> o <code>waiting</code> durante N minutos) y <strong>umbral de tokens</strong> (los tokens acumulados de una sesión superan un límite). Cada regla tiene un <strong>tiempo de espera</strong> configurable que elimina alertas repetidas por regla, sesión y Agent.',
    "Event-driven rules (<code>event_pattern</code>, <code>token_threshold</code>) run on every hook ingest — <em>after</em> the transaction commits and the response is sent, fully try/catch-guarded, so alerting can never slow or fail hook delivery. Time-based rules (<code>inactivity</code>, <code>status_duration</code>) run on an unref'd 60-second sweep. Enabled rules are cached in memory and invalidated on every edit. Fired alerts persist to <code>alert_events</code> and broadcast an <code>alert_triggered</code> WebSocket message.":
      "Las reglas basadas en eventos (<code>event_pattern</code> y <code>token_threshold</code>) se evalúan con cada hook recibido, <em>después</em> de confirmar la transacción y enviar la respuesta. Todo está protegido con try/catch, por lo que las alertas nunca ralentizan ni interrumpen la entrega de hooks. Las reglas temporales (<code>inactivity</code> y <code>status_duration</code>) se evalúan cada 60 segundos con un temporizador sin referencias. Las reglas activas se guardan en memoria y la caché se invalida al editarlas. Las alertas disparadas se almacenan en <code>alert_events</code> y emiten el mensaje WebSocket <code>alert_triggered</code>.",
    "Slack, Discord, Microsoft Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, and Pipedream — plus a generic JSON endpoint. A declarative <strong>provider registry</strong> describes each one's payload formatter, URL resolution, auth headers, and credential fields, so adding a provider is a single server-side entry that surfaces in the UI with no front-end change.":
      "Slack, Discord, Microsoft Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n y Pipedream, además de un punto final JSON genérico. Un declarativo <strong>Registro de proveedores</strong> describe el formateador de carga útil de cada uno, la resolución de la URL, los encabezados de autenticación y los campos de credenciales, por lo que agregar un proveedor es una sola entrada del lado del servidor que aparece en la interfaz de usuario sin cambios en el frontend.",
    'Each delivery POSTs with an <code>AbortController</code> timeout and bounded retry/backoff (retries transport errors, 429, and 5xx — never other 4xx), then records the attempt-chain in <code>webhook_deliveries</code>. A provider can also veto a 2xx whose body signals failure (Splunk On-Call returns 200 with <code>result:"failure"</code>). Delivery is <strong>detached and fail-safe</strong> — it never throws into, slows, or blocks the alert path.':
      'Cada entrega hace un POST con tiempo de espera mediante <code>AbortController</code> y reintentos limitados con espera progresiva (para errores de transporte, 429 y 5xx; nunca para otros 4xx), y registra la cadena de intentos en <code>webhook_deliveries</code>. Un proveedor también puede rechazar una respuesta 2xx cuyo cuerpo indique un fallo (Splunk On-Call devuelve 200 con <code>result:"failure"</code>). La entrega está <strong>desacoplada y es tolerante a fallos</strong>: nunca genera errores en la ruta de alertas ni la ralentiza o bloquea.',
    "Target URLs are masked (host + last 4 chars), and secrets / credential fields (routing keys, API keys, bot tokens) plus custom-header values are redacted in every API response — the full URL and secrets are stored server-side and never leave it. Generic endpoints support optional <strong>HMAC-SHA256</strong> body signing (<code>X-Webhook-Signature</code> + <code>X-Webhook-Timestamp</code>) so receivers can verify authenticity.":
      "Las URL de destino se enmascaran (host + últimos 4 caracteres), y los secretos, las credenciales (claves de enrutamiento, claves API y tokens de bot) y los encabezados personalizados se ocultan en todas las respuestas de la API. La URL completa y los secretos se almacenan únicamente en el servidor. Los endpoints genéricos admiten una firma opcional del cuerpo con <strong>HMAC-SHA256</strong> (<code>X-Webhook-Signature</code> + <code>X-Webhook-Timestamp</code>) para que los receptores puedan verificar su autenticidad.",
    'Every alert-rule field has a help tooltip — the event-type, tool-name, and summary-contains fields include example chips of real hook events and built-in tool names. Each webhook provider ships a collapsible step-by-step setup guide linking to the official docs. A one-click <strong>"Send test"</strong> probe fires a synthetic alert and reports the delivery result inline, and targets can be scoped to specific rules. Fully localized (en / zh / vi / ko).':
      'Cada campo de las reglas de alerta tiene una ayuda contextual. Los campos de tipo de evento, nombre de herramienta y texto del resumen incluyen ejemplos de eventos de hook reales y nombres de herramientas integradas. Cada proveedor de webhook incluye una guía de configuración paso a paso y plegable con enlaces a la documentación oficial. La opción <strong>"Enviar prueba"</strong> genera una alerta sintética e informa del resultado en la propia interfaz; además, los destinos pueden limitarse a reglas concretas. Completamente localizado (en / zh / vi / ko).',
    "Provider(s)": "Proveedor(es)",
    "Payload format": "Formato de carga útil",
    "URL / credentials": "URL / credenciales",
    "Block Kit (header + section + context)": "Kit de bloques (encabezado + sección + contexto)",
    "Rich embed": "Incorporación rica",
    "Adaptive Card in a Workflows <code>message</code> envelope":
      "Tarjeta adaptativa en flujos de trabajo <code>message</code> sobre",
    "Power Automate Workflows URL": "URL de los flujos de trabajo de Power Automate",
    "Text message (basic markdown)": "Mensaje de texto (marcado básico)",
    "Space webhook URL": "URL de webhook espacial",
    "Slack-style legacy attachments": "Anexos heredados de estilo Slack",
    "Bot API <code>sendMessage</code> (HTML)": "API de bots <code>sendMessage</code> (HTML)",
    "Bot token + chat ID (URL derived)": "Token de bot + ID de chat (derivado de la URL)",
    "Events API v2 trigger (with <code>dedup_key</code>)":
      "Despliegue de la API de eventos v2 (con <code>dedup_key</code>)",
    "Routing key (URL prefilled)": "Clave de enrutamiento (URL prellenada)",
    "Alert API": "API de alerta",
    "API key (GenieKey header) + region": "Clave API (cabecera GenieKey) + región",
    "VictorOps REST": "REST de VictorOps",
    "REST endpoint URL (key embedded)": "URL del punto final REST (clave incorporada)",
    "Stable <code>{ event, alert }</code> JSON envelope":
      "estable <code>{ event, alert }</code> Envelope JSON",
    "Endpoint URL (+ optional HMAC &amp; headers)":
      "URL del punto final (+ HMAC opcional y encabezados)",
    "Additive &amp; non-blocking by design": "Aditivo y no bloqueante por diseño",
    "Two new tables — <code>webhook_targets</code> (config; survives Clear Data like alert rules) and <code>webhook_deliveries</code> (audit log) — with no changes to existing tables, response shapes, or WebSocket message types. Webhook dispatch is fire-and-forget off the alert path, so a slow or failing endpoint can never slow or break alert firing or hook ingestion.":
      "Dos tablas nuevas —<code>webhook_targets</code> (configuración; se conserva al borrar datos, igual que las reglas de alerta) y <code>webhook_deliveries</code> (registro de auditoría)— sin cambios en las tablas existentes, los formatos de respuesta ni los tipos de mensajes WebSocket. El envío de Webhooks ocurre en segundo plano fuera de la ruta de alertas, por lo que un endpoint lento o con errores nunca ralentiza ni interrumpe las alertas o la recepción de hooks.",
    "Provider setup steps can drift": "Los pasos de configuración del proveedor pueden variar",
    "Microsoft retired classic Office 365 connectors in 2025, so Teams uses an Adaptive Card delivered via Power Automate <strong>Workflows</strong>. More broadly, provider setup UIs change often — the in-app guides say so and link to each provider's official docs. Always confirm against the source.":
      "Microsoft retiró los conectores clásicos de Office 365 en 2025, por lo que Teams utiliza una Tarjeta Adaptativa entregada a través de Power Automate <strong>Flujos de trabajo</strong>. más ampliamente, las interfaces de usuario de configuración del proveedor cambian con frecuencia, lo dicen las guías de la aplicación y enlazan con los documentos oficiales de cada proveedor. Siempre confirme con la fuente.",
    '<span class="caption-icon">⬆</span> Update Notifier — version comparison modal with one-click copy of the update command. No automatic self-restart; you stay in control of when upgrades happen':
      '<span class="caption-icon">⬆</span> Notificador de actualización: modal de comparación de versiones con copia del comando de actualización con un solo clic. Sin reinicio automático automático; usted permanece al control de cuándo ocurren las actualizaciones.',
    "A detection-only subsystem that tells the user when the dashboard's git checkout is behind the canonical default branch. <strong>Branch- and fork-aware:</strong> if an <code>upstream</code> remote is configured (the standard convention for forks), it takes priority over <code>origin</code>; the chosen remote's <code>master</code> / <code>main</code> / <code>HEAD</code> is the comparison ref. The printed command adapts to the user's situation — <code>git pull --ff-only</code> only when their branch actually tracks the canonical ref, otherwise <code>git fetch</code> (with a fast-forward merge in the fork case). The server <strong>never</strong> pulls or restarts itself — the user runs the command in a terminal — so the mechanism cannot break dev sessions, pm2/systemd/launchd/Docker supervision, or leave orphaned processes.":
      "Un subsistema de detección exclusiva que informa al usuario cuando la extracción de git del panel de control está detrás de la rama predeterminada canónica. <strong>Conocedor de ramas y ramificaciones:</strong> Si un <code>upstream</code> El mando a distancia está configurado (la convención estándar para puentes), tiene prioridad sobre <code>origin</code>; el mando a distancia elegido <code>master</code> / <code>main</code> / <code>HEAD</code> Es la referencia de la comparación. El comando impreso se adapta a la situación del usuario — <code>git pull --ff-only</code> Solo cuando su rama realmente rastrea la referencia canónica, de lo contrario <code>git fetch</code> (con una fusión de avance rápido en el caso de bifurcación). El servidor <strong>nunca</strong> Se tira o se reinicia por sí mismo: el usuario ejecuta el comando en un terminal, por lo que el mecanismo no puede romper las sesiones de desarrollo, la supervisión de pm2/systemd/launchd/Docker o dejar procesos huérfanos.",
    "The same status is available from any terminal: <code>ccam update-check</code> asks the server for the branch- and fork-aware comparison, prints the behind-by count and the copy-paste update command, and triggers the <code>update_status</code> WebSocket broadcast so any open dashboard tab refreshes at the same time.":
      "El mismo estado está disponible desde cualquier terminal: <code>ccam update-check</code> Pide al servidor la comparación consciente de ramas y bifurcaciones, imprime el conteo de retraso y el comando de actualización de copia y pegar, y activa el <code>update_status</code> Transmisión WebSocket, por lo que cualquier pestaña del panel de control abierta se actualiza al mismo tiempo.",
    "A shell-less <code>git fetch</code> with a 120-second timeout, followed by a <code>rev-list</code> against the tracked upstream. Each call runs from <code>server/lib/update-check.js</code> and returns a structured payload — never throws — so a flaky remote can&apos;t stall the dashboard.":
      "Sin cáscara <code>git fetch</code> Con un tiempo de espera de 120 segundos, seguido de un <code>rev-list</code> Contra el rastreado en el curso superior. Cada llamada se ejecuta desde <code>server/lib/update-check.js</code> y devuelve una carga útil estructurada, nunca lanza, por lo que un control remoto inestable no puede estancar el panel de instrumentos.",
    "<code>update-scheduler.js</code> polls every five minutes with <code>.unref()</code> timers so it never blocks shutdown, de-duplicates with a fingerprint over the status payload, and announces up-to-date → behind transitions in a framed stdout block. Disable entirely with <code>DASHBOARD_UPDATE_CHECK=0</code>.":
      "<code>update-scheduler.js</code> Encuestas cada cinco minutos con <code>.unref()</code> Timer para que nunca bloquee el apagado, desduplique con una huella digital sobre el contenido de la carga útil del estado y anuncie las transiciones → atrasadas actualizadas en un bloque de stdout enmarcado. Deshabilitar completamente con <code>DASHBOARD_UPDATE_CHECK=0</code>.",
    "Each status payload carries a <code>manual_command</code> shaped for the user's actual situation: <code>git pull --ff-only</code> on a tracked canonical branch, <code>git fetch &amp;&amp; git merge --ff-only</code> for forks where local tracks the wrong remote, and a plain <code>git fetch</code> on a feature branch where pulling would update the wrong branch. Install / build steps are appended only when the working tree is actually being rewritten.":
      "Cada carga útil de estado lleva un <code>manual_command</code> Diseñado para la situación actual del usuario: <code>git pull --ff-only</code> En una rama canónica rastreada, <code>git fetch &amp;&amp; git merge --ff-only</code> Para tenedores donde la pista local sigue el mando a distancia equivocado, y un plano <code>git fetch</code> En una rama de características donde la extracción actualizaría la rama equivocada. Los pasos de instalación / construcción se adjuntan solo cuando el árbol de trabajo está siendo reescrito de hecho.",
    "A modal opens automatically when upstream is ahead; ESC or a backdrop click dismisses it. A persistent sidebar button stays in the footer — emerald when behind, amber when the last check errored — so users can always trigger a fresh check on demand.":
      "Un modal se abre automáticamente cuando upstream está por delante; ESC o un clic en el fondo desactiva el modal. Un botón persistente en la barra lateral permanece en el pie de página: es esmeralda cuando está detrás, ámbar cuando el último control ha fallado, para que los usuarios siempre puedan activar un nuevo control a demanda.",
    "Non-git installs, no remotes configured, offline fetches, and unresolvable upstream refs all return tagged payloads instead of throwing. The sidebar badge turns amber on fetch errors and the modal stays suppressed until a successful check arrives — no spinners, no stuck state.":
      "Las instalaciones no git, las no configuradas remotas, las consultas fuera de línea y las referencias de origen no resolubles devuelven todos los paquetes de carga etiquetados en lugar de lanzar. El distintivo de la barra lateral se vuelve ámbar en los errores de consulta y el modal se mantiene suprimido hasta que llegue una comprobación exitosa, sin carretes giratorios ni estado atascado.",
    "Dismissal is keyed by the upstream SHA in <code>localStorage</code>, so closing the modal silences it only for <em>that</em> commit — a newer upstream commit re-opens it automatically. Clicking the sidebar button is an explicit intent signal and clears the stored dismissal before firing a fresh check.":
      "El rechazo está controlado por el SHA de entrada en <code>localStorage</code>, por lo que cerrar la modalidad lo silencia solo por <em>eso</em> comit — un commit más reciente de arriba abre automáticamente. Hacer clic en el botón de la barra lateral es una señal de intención explícita y borra el despido almacenado antes de lanzar una verificación nueva.",
    "Read-only check — runs <code>git fetch</code>, compares, returns the payload.":
      "Comprobación de lectura única - se ejecuta <code>git fetch</code>, compara, devuelve la carga útil.",
    "Same check, and broadcasts <code>update_status</code> over WebSocket so every connected client re-syncs at once.":
      "Mismo cheque, y transmisiones <code>update_status</code> A través de WebSocket, por lo que todos los clientes conectados se sincronizan de una vez.",
    "<strong>Detection-only by design</strong>": "<strong>Detección solo por diseño</strong>",
    "There is no <code>POST /api/updates/apply</code> and no in-process restart helper. A process cannot reliably replace itself without an external supervisor, and <code>npm run dev</code>, <code>npm start</code>, pm2, systemd, launchd, and Docker each need different restart logic. Detection-only keeps the mechanism portable across every supervisor and OS, and leaves the dashboard's lifecycle owned by whatever started it. The user runs the printed command in their own shell.":
      "No hay <code>POST /api/updates/apply</code> Y no hay asistente de reinicio en proceso. Un proceso no puede reemplazarse de forma fiable sin un supervisor externo, y <code>npm run dev</code>, <code>npm start</code>, pm2, systemd, launchd y Docker necesitan cada uno una lógica de reinicio diferente. La detección solo mantiene el mecanismo portátil en todos los supervisores y sistemas operativos, y deja que el ciclo de vida del panel de control sea propiedad de quien lo inició. El usuario ejecuta el comando impreso en su propia shell.",
    '<span class="caption-icon">◈</span> Connection Status — sidebar-launched details modal with WebSocket endpoint, connection uptime, 60-second throughput sparkline, top event-type breakdown, and recent activity list':
      '<span class="caption-icon">◈</span> Estado de la conexión: modal de detalles lanzada desde la barra lateral con punto final WebSocket, tiempo de actividad de la conexión, gráfico de flujo de datos de 60 segundos, desglose del tipo de evento más común y lista de actividad reciente.',
    'The <strong>Live</strong> / <strong>Disconnected</strong> pill in the sidebar footer opens a small details panel about the dashboard\'s WebSocket transport. It surfaces the active <code>ws://</code> endpoint, how long the current socket has been up, total events received, the top event types as a horizontal bar chart, a 60-second throughput sparkline, and the most recent 8 events as an activity list. Cumulative stats (totals, type breakdown, recent list) persist across reloads via <code>localStorage</code> under <code>sidebar-connection-stats</code>; the rolling sparkline and "connected since" timer are intentionally ephemeral since they only make sense relative to "now". A <strong>Reset</strong> button clears everything on demand.':
      'el <strong>en vivo</strong> / <strong>desconectado</strong> La píldora en el pie de página de la barra lateral abre un pequeño panel de detalles sobre el transporte WebSocket del panel de control. Muestra el activo <code>ws://</code> Punto final, cuánto tiempo ha estado activo el socket actual, los eventos totales recibidos, los tipos de eventos más comunes como un gráfico de barras horizontal, una línea de chispa de rendimiento de 60 segundos y los 8 eventos más recientes como una lista de actividades. Las estadísticas acumulativas (totales, desglose por tipo, lista reciente) persisten a través de las recargas a través de <code>localStorage</code> bajo <code>sidebar-connection-stats</code>; la línea de flechas en movimiento y el temporizador "conectado desde" son intencionalmente efímeros ya que solo tienen sentido en relación con "ahora". A <strong>volver a poner</strong> El botón borra todo a pedido.',
    "Implementation note: per-event state lives in <code>useRef</code> buffers on the sidebar so the WS firehose never re-renders the navigation tree — the modal does its own one-second tick to sample the refs while open. Writes are throttled (single-flight timer, 2 s window) and flushed on <code>pagehide</code> / <code>visibilitychange</code> so the latest events aren't lost to the throttle window. The modal itself is portalled to <code>document.body</code> so the sidebar's stacking context can't trap it.":
      "Nota de implementación: el estado por evento vive en <code>useRef</code> Bufers en la barra lateral para que el grifo de fuego WS nunca vuelva a renderizar el árbol de navegación, el modal hace su propio tic de un segundo para muestrar las referencias mientras está abierto. Las escrituras se limitan (timer de vuelo único, ventana de 2 s) y se borran en <code>pagehide</code> / <code>visibilitychange</code> Así que los últimos eventos no se pierden en la ventana de aceleración. El modal en sí se redirige a <code>document.body</code> Así que el contexto de empalme de la barra lateral no puede atraparlo.",
    "The entire UI ships in <strong>four languages — English, 简体中文, Tiếng Việt, and 한국어</strong> — built on <code>i18next</code> + <code>react-i18next</code> with <code>i18next-browser-languagedetector</code>. Coverage is end-to-end: every page, chart tooltip, Settings flow, Workflow narrative, Config Explorer tab, Run page, and the Alerts rule-help tooltips + webhook setup guides are translated. Switch languages from the sidebar (EN / 中文 / VI / 한국어) — the choice persists in <code>localStorage</code>.":
      "Toda la interfaz de usuario se envía en <strong>Cuatro idiomas: inglés, chino simplificado, vietnamita y coreano</strong> — construido sobre <code>i18next</code> + <code>react-i18next</code> con <code>i18next-browser-languagedetector</code>. la cobertura es de extremo a extremo: todas las páginas, las etiquetas de herramientas de gráficos, el flujo de Configuración, la narrativa del flujo de trabajo, la pestaña Config Explorer, la página Ejecutar y las etiquetas de ayuda de las reglas de Alertas + las guías de configuración de webhook se traducen. Cambie el idioma desde la barra lateral (EN / 中文 / VI / 한국어) - la elección persiste en <code>localStorage</code>.",
    'Translations are split into per-area JSON namespaces (<code>common</code>, <code>nav</code>, <code>dashboard</code>, <code>sessions</code>, <code>analytics</code>, <code>workflows</code>, <code>settings</code>, <code>kanban</code>, <code>run</code>, <code>ccConfig</code>, <code>alerts</code>, <code>errors</code>, <code>updates</code>) under <code>client/src/i18n/locales/&lt;lng&gt;/</code>. Components load only the namespaces they need via <code>useTranslation("…")</code>.':
      'Las traducciones se dividen en nombrespaces JSON por área (<code>common</code>, <code>nav</code>, <code>dashboard</code>, <code>sessions</code>, <code>analytics</code>, <code>workflows</code>, <code>settings</code>, <code>kanban</code>, <code>run</code>, <code>ccConfig</code>, <code>alerts</code>, <code>errors</code>, <code>updates</code>) debajo <code>client/src/i18n/locales/&lt;lng&gt;/</code>. Los componentes cargan solo los espacios de nombres que necesitan a través de <code>useTranslation("…")</code>.',
    "Language is detected from <code>localStorage</code> (<code>i18nextLng</code>) then the browser's <code>navigator</code> setting, and the choice is cached back to <code>localStorage</code>. <code>fallbackLng</code> is English and <code>nonExplicitSupportedLngs</code> resolves regional tags (e.g. <code>vi-VN</code> → <code>vi</code>), so any unmapped key falls back gracefully rather than rendering a raw key.":
      "Se detecta el idioma de <code>localStorage</code> (<code>i18nextLng</code>) entonces el del navegador <code>navigator</code> Configuración, y la elección se almacena en caché de vuelta a <code>localStorage</code>. <code>fallbackLng</code> Es inglés y <code>nonExplicitSupportedLngs</code> Resuelve etiquetas regionales (por ejemplo, <code>vi-VN</code> → <code>vi</code>), por lo que cualquier clave no asignada se recupera con gracia en lugar de renderizar una clave bruta.",
    "Numbers, costs, dates, and relative times format against the active locale via a shared <code>getCurrentLocale()</code> helper, and plurals use i18next's <code>_one</code> / <code>_other</code> suffixes. Interpolated values (<code>{{count}}</code>, <code>{{provider}}</code>, …) keep sentences natural across languages.":
      "Los números, los costos, las fechas y los tiempos relativos se formatean contra el lugar de origen activo a través de un compartido <code>getCurrentLocale()</code> Ayudante, y los plurales usan los de i18next <code>_one</code> / <code>_other</code> Sufijos. Valores interpolados (<code>{{count}}</code>, <code>{{provider}}</code>, ...) mantenga las oraciones naturales a través de idiomas.",
    "Domain terms that are proper nouns or code stay untranslated in every locale — <em>Agent</em>, <em>Subagent</em>, hook event names (<code>PostToolUse</code>), tool names (<code>Bash</code>), and webhook provider names (Slack, PagerDuty). Only the surrounding prose is localized, so instructions stay accurate.":
      "Los términos de dominio que son nombres propios o códigos permanecen sin traducir en cada ubicación — <em>representante</em>, <em>Subagente</em>, conectar los nombres de los eventos (<code>PostToolUse</code>), nombres de herramientas (<code>Bash</code>), y nombres de proveedores de webhook (Slack, PagerDuty). Solo la prosa circundante se localiza, por lo que las instrucciones permanecen precisas.",
    "<strong>Adding a language</strong>": "<strong>Añadir un idioma</strong>",
    "Copy <code>client/src/i18n/locales/en/</code> to a new locale folder, translate the JSON values (leaving keys and technical terms intact), then register the bundle and add the tag to <code>supportedLngs</code> in <code>client/src/i18n/index.ts</code>. Missing keys fall back to English automatically, so even a partial translation ships cleanly.":
      "copia <code>client/src/i18n/locales/en/</code> A una nueva carpeta de ubicación, traduzca los valores JSON (dejando intactas las claves y los términos técnicos), luego regístre el paquete y agregue la etiqueta a <code>supportedLngs</code> indio <code>client/src/i18n/index.ts</code>. claves faltantes se vuelven automáticamente al inglés, por lo que incluso una traducción parcial se envía sin problemas.",
    "<strong>Tabby</strong> is a cute SVG cat companion pinned to the <strong>edges of every page</strong> of the dashboard. It is always present and turns the live session stream into glanceable, ambient feedback — calm when idle, alert when something needs attention, and celebratory when a run finishes. Tabby is built entirely on the existing <code>eventBus</code> WebSocket stream: <strong>no new backend, no API key, and no new dependencies</strong>. The component lives in <code>client/src/components/Tabby/</code> and can be toggled on or off in Settings page.":
      "<strong>gata</strong> Es un lindo compañero de gato SVG atado al <strong>Líneas de cada página</strong> del panel de control. Siempre está presente y convierte el flujo de la sesión en vivo en un retroalimentación ambiental que se puede ver de un vistazo: tranquilo cuando está inactivo, alerta cuando algo necesita atención y festivo cuando una carrera termina. Tabby está construido enteramente sobre el existente <code>eventBus</code> Stream WebSocket: <strong>Sin nuevo backend, sin clave API y sin nuevas dependencias</strong>. componente vive en <code>client/src/components/Tabby/</code> y se puede activar o desactivar en la página de Configuración.",
    '<span class="caption-icon">📥</span> Tabby Companion — a cute SVG cat in the edges of every page, reacting in real time to the live session stream with eight distinct moods and animations, auto-surfacing speech bubbles for notable events, and serving as the gateway to a status panel and Ask box':
      '<span class="caption-icon">📥</span> Tabby Companion — un simpático gato SVG situado en el borde de cada página que reacciona en tiempo real a las sesiones con ocho estados de ánimo y animaciones, muestra bocadillos automáticamente ante eventos destacados y da acceso al panel de estado y al cuadro Ask',
    "Tabby derives one of eight moods from the live session WebSocket stream, each with its own animation. The eyes track your cursor, and the active mood drives a distinct motion cue.":
      "Tabby deriva uno de los ocho estados de ánimo del flujo WebSocket de la sesión en vivo, cada uno con su propia animación. Los ojos rastrean el cursor y el estado de ánimo activo impulsa una señal de movimiento distinta.",
    "Notable events — session started or finished, errors, and run completed — automatically surface a speech bubble. Bubbles are <strong>throttled and coalesced</strong> so bursts of events never spam you, and they can be muted on demand. Everything reflects in real time over the existing <code>eventBus</code> WebSocket channel, with no polling and no extra services.":
      "Los eventos destacados —inicio o fin de una sesión, errores y ejecuciones completadas— muestran automáticamente un bocadillo. Los bocadillos <strong>se limitan y agrupan</strong> para que una ráfaga de eventos no te abrume, y puedes silenciarlos cuando quieras. Todo se actualiza en tiempo real mediante el canal WebSocket <code>eventBus</code> existente, sin sondeo ni servicios adicionales.",
    "Click the cat — or press <code>⌘B</code> / <code>Ctrl+B</code> — to open Tabby's panel (<code>Esc</code> closes it). The panel groups a live status line, quick actions, and an Ask box.":
      "Haz clic en el gato —o pulsa <code>⌘B</code> / <code>Ctrl+B</code>— para abrir el panel de Tabby (<code>Esc</code> lo cierra). El panel reúne el estado en tiempo real, acciones rápidas y un cuadro Ask.",
    "<strong>Live status line:</strong> <em>N live · M errored · connection state</em>, updated from cached data.":
      "<strong>Línea de estado en vivo:</strong> <em>N en vivo · M error · estado de conexión</em>, actualizado a partir de datos almacenados en caché.",
    "<strong>Quick actions:</strong> jump to Run Claude, Activity, Sessions, or errored sessions; mute bubbles; clear alerts.":
      "<strong>Acciones rápidas:</strong> Ir a Ejecutar Claude, Actividad, Sesiones o sesiones con errores; silenciar burbujas; eliminar alertas.",
    "<strong>Ask box:</strong> answers simple status questions locally from cached data (&ldquo;what's running&rdquo;, &ldquo;any errors&rdquo;, &ldquo;status&rdquo;).":
      "<strong>Caja de preguntas:</strong> Responde preguntas de estado simples localmente a partir de datos caché (&quot;qué está ejecutando&quot;, &quot;cuáles son los errores&quot;, &quot;estado&quot;).",
    "The Ask box answers status questions instantly and offline from cached data. For anything beyond a simple status question, Tabby hands off to the existing <strong>Run Claude</strong> page (<code>/run?prompt=...</code>) to spawn a real Claude Code session — so there is never a separate model call, key, or service to manage.":
      "El cuadro Ask responde al instante y sin conexión a preguntas de estado usando datos en caché. Para cualquier consulta más compleja, Tabby la transfiere a la página <strong>Run Claude</strong> existente (<code>/run?prompt=...</code>) para iniciar una sesión real de Claude Code, sin llamadas, claves ni servicios de modelo independientes.",
    "Fully keyboard operable: <code>⌘B</code> / <code>Ctrl+B</code> to open, <code>Esc</code> to close.":
      "Totalmente operativo con teclado: <code>⌘B</code> / <code>Ctrl+B</code> Para abrir, <code>Esc</code> Cerrar.",
    "Status and bubbles announce via <code>aria-live</code> for screen readers.":
      "El estado y los bocadillos se anuncian mediante <code>aria-live</code> para los lectores de pantalla.",
    "Respects <code>prefers-reduced-motion</code> to calm animations.":
      "consideración <code>prefers-reduced-motion</code> Para calmar las animaciones.",
    "Degrades gracefully to a calm, dimmed disconnected state when offline.":
      "Se degrada con gracia a un estado tranquilo, apagado y desconectado cuando está desconectado.",
    Endpoint: "Punto final",
    Mood: "humor",
    "When it appears": "Cuando aparezca",
    Animation: "animación",
    Idle: "andar al ralentí",
    "Nothing notable happening": "Nada notable sucediendo",
    "Gentle tail flick": "Pelea suave de cola",
    Watching: "Observando",
    "Sessions active, observing the stream": "Sesiones activas, observando el flujo",
    "Ear perk, cursor-tracking eyes": "Apertura de los oídos, ojos que rastrean el cursor",
    Happy: "feliz",
    "A run completed successfully": "Una carrera completada con éxito",
    Sparkle: "brillo",
    Worried: "preocupado",
    "Something looks off": "Algo no cuadra",
    "Head bob": "Sacudir la cabeza",
    Stuck: "atascado",
    "A session appears blocked": "Una sesión parece estar bloqueada",
    "Shake + alert <code>!</code>": "Sacudir + alerta <code>!</code>",
    Thinking: "pensante",
    "Work in progress": "Trabajo en progreso",
    Sleeping: "sueño",
    "Quiet for a while": "Tranquilo por un rato",
    Zzz: "zzz",
    Disconnected: "desconectado",
    "WebSocket offline": "WebSocket sin conexión",
    "Calm, dimmed state": "Estado tranquilo y apagado",
    "Development vs production deployment topology":
      "Topología de despliegue de desarrollo vs producción",
    Aspect: "faceta",
    Development: "explotación",
    Production: "creación",
    Processes: "Procesos",
    "2 (Express + Vite)": "2 (Expreso + Vite)",
    "1 (Express only)": "1 (Solo exprés)",
    "Client URL": "URL del cliente",
    "API proxy": "Proxy de API",
    "Vite proxies <code>/api</code> + <code>/ws</code> to :4820":
      "Proxies Vite <code>/api</code> + <code>/ws</code> A: 4820",
    "Same origin, no proxy": "Misma origen, sin proxy",
    "File watching": "Seguimiento de archivos",
    "<code>node --watch</code> + Vite HMR": "<code>node --watch</code> + Vite HMR",
    None: "nona",
    "Source maps": "Mapa de fuentes",
    Inline: "En línea",
    "External files": "Archivos externos",
    "<strong>A third way to run: the Desktop App (macOS &amp; Windows)</strong>":
      "<strong>Una tercera forma de ejecutar: la aplicación de escritorio (macOS y Windows)</strong>",
    'Beyond development and standalone production, the dashboard also ships as a native desktop app — a macOS <code>.app</code> and a Windows <code>.exe</code> — that embeds the same production server in-process, no terminal required. See the <a href="#desktop-app">Desktop App (macOS &amp; Windows)</a> section for download, build, and install instructions.':
      'Más allá del desarrollo y la producción independiente, el panel de control también se envía como una aplicación nativa para escritorio, una macOS. <code>.app</code> Y una ventana <code>.exe</code> — que incorpora el mismo servidor de producción en el proceso, no se requiere terminal. Consulte el <a href="#desktop-app">Aplicación para escritorio (macOS y Windows)</a> Sección para descargar, construir e instalar instrucciones.',
    "The production image is OCI-compatible and works with both Docker and Podman. The server listens on <code>4820</code>, reads legacy Claude history from a read-only mount, and persists SQLite data under <code>/app/data</code>.":
      "La imagen de producción es compatible con OCI y funciona tanto con Docker como con Podman. El servidor escucha en <code>4820</code>, lee la historia heredada de Claude desde una unidad de lectura única y persiste los datos de SQLite bajo <code>/app/data</code>.",
    "Container image build and runtime mounts":
      "Construcción de la imagen del contenedor y montajes de tiempo de ejecución",
    Mount: "monte",
    "Read historical Claude session files for import without modifying them":
      "Lea los archivos históricos de sesiones de Claude para su importación sin modificarlos",
    "Persist the SQLite database across rebuilds and container restarts":
      "Persiste la base de datos SQLite a través de las reconstrucciones y reinicios de contenedores",
    "<strong>Hooks still run on the host</strong>":
      "<strong>Los ganchos todavía funcionan en el host</strong>",
    "Claude Code fires hooks from the host machine, not from inside the container. After the container is healthy on <code>http://localhost:4820</code>, run <code>npm run install-hooks</code> on the host so hook events post back to the containerized server.":
      "Claude Code dispara ganchos desde la máquina anfitriona, no desde dentro del contenedor. Después de que el contenedor esté sano en <code>http://localhost:4820</code>, correr <code>npm run install-hooks</code> en el host, así que envía los eventos de conexión de vuelta al servidor contenedorizado.",
    "A multi-stage <code>Dockerfile</code> and <code>docker-compose.yml</code> are included. Both <strong>Docker</strong> and <strong>Podman</strong> are fully supported — the image is OCI-compliant.":
      "Un multiestadio <code>Dockerfile</code> y <code>docker-compose.yml</code> Están incluidos. ambos <strong>estibador</strong> y <strong>Podman</strong> Son totalmente compatibles: la imagen cumple con la normativa OCI.",
    "Read-only access to legacy session history for automatic import on startup":
      "Acceso de lectura única al historial de sesiones heredadas para importación automática al inicio de sesión",
    "Persists the SQLite database across container restarts":
      "Persiste la base de datos SQLite a través de reinicios de contenedores",
    "The Dockerfile uses three stages to minimize the final image size:":
      "El Dockerfile utiliza tres etapas para minimizar el tamaño final de la imagen:",
    Stage: "etapa",
    "Installs production <code>node_modules</code> on digest-pinned <code>node:24.19.0-alpine3.24</code>. <code>better-sqlite3</code> is optional — if prebuilds are unavailable, the server falls back to built-in <code>node:sqlite</code>":
      "Instala los <code>node_modules</code> de producción sobre <code>node:24.19.0-alpine3.24</code> fijado por digest. <code>better-sqlite3</code> es opcional; si no hay precompilados, el servidor usa el <code>node:sqlite</code> integrado",
    "Runs <code>npm ci</code> + <code>vite build</code> to produce optimized static assets":
      "Corre <code>npm ci</code> + <code>vite build</code> Para producir activos estáticos optimizados",
    "Clean digest-pinned <code>node:24.19.0-alpine3.24</code> with only <code>node_modules</code>, server code, and <code>client/dist</code>":
      "<code>node:24.19.0-alpine3.24</code> limpio y fijado por digest, solo con <code>node_modules</code>, código del servidor y <code>client/dist</code>",
    "<strong>Hook note</strong>": "<strong>Nota de gancho</strong>",
    "Claude Code hooks run on the host, not inside the container. The containerized server receives hook events via HTTP on <code>localhost:4820</code>. Run <code>npm run install-hooks</code> on the host after starting the container.":
      "Los ganchos de Claude Code se ejecutan en el host, no dentro del contenedor. El servidor contenedorizado recibe eventos de gancho a través de HTTP en <code>localhost:4820</code>. carrera <code>npm run install-hooks</code> En el host después de iniciar el contenedor.",
    Metric: "métrico",
    "Server startup": "Inicio de sesión del servidor",
    "SQLite opens instantly; schema migration is idempotent":
      "SQLite se abre instantáneamente; la migración del esquema es idempotente",
    "Hook latency": "Latencia del gancho",
    "Transaction + broadcast, no async I/O beyond SQLite":
      "Transacción + transmisión, sin E/S asincrónica más allá de SQLite",
    "Client JS bundle": "Paquete JS del cliente",
    "WebSocket latency": "Latencia de WebSocket",
    "Local loopback, JSON serialization only": "Bucle local, solo serialización JSON",
    "SQLite write throughput": "Trafico de escritura de SQLite",
    "WAL mode on SSD; far exceeds any hook event rate":
      "Modo WAL en SSD; supera ampliamente cualquier tasa de eventos de conexión.",
    "Max events before slowdown": "Máximo número de eventos antes de la ralentización",
    "Pagination prevents full-table scans": "La paginación evita escaneos completos de la tabla",
    "Server memory": "Memoria del servidor",
    "SQLite in-process, no ORM overhead": "SQLite en proceso, sin sobrecostes de ORM",
    "Client memory": "Memoria del cliente",
    "React + Tailwind, minimal runtime deps":
      "React + Tailwind, dependencias mínimas de tiempo de ejecución",
    "Input validation": "Validación de la entrada",
    "Required fields checked before DB operations; CHECK constraints on status enums":
      "Campos requeridos marcados antes de las operaciones de la base de datos; CONSTRAINTES DE COMPROBACIÓN en los enums de estado",
    "Hook safety": "Seguridad del gancho",
    "Hook handler always exits 0; 5s max lifetime; uses <code>127.0.0.1</code> not external hosts":
      "El manipulador de ganchos siempre sale 0; vida útil máxima de 5 segundos; utiliza <code>127.0.0.1</code> No hosts externos",
    CORS: "CORS",
    "Restricted to loopback origins, so cross-origin pages can't read responses; no-Origin clients like curl still work":
      "Restringido a orígenes de retroceso, por lo que las páginas de origen cruzado no pueden leer las respuestas; los clientes sin origen como curl todavía funcionan",
    Authentication: "autentificación",
    "Off by default since the loopback bind is the trust boundary; set <code>DASHBOARD_TOKEN</code> to require a bearer token on every <code>/api/*</code> request and the WebSocket when exposing on a LAN.":
      "Desactivado por defecto ya que el enlace de retroceso es el límite de confianza; establecer <code>DASHBOARD_TOKEN</code> Para requerir un token portador en cada <code>/api/*</code> Solicitud y el WebSocket cuando se expone en una LAN.",
    Secrets: "Secretos",
    "No API keys, tokens, or credentials stored or transmitted anywhere":
      "No se almacenan ni transmiten claves API, tokens ni credenciales en ningún lugar.",
    "Dependency surface": "Superficie de dependencia",
    "5 runtime server deps, 6 runtime client deps (includes D3.js for Workflows) — minimal attack surface":
      "5 dependencias del servidor de tiempo de ejecución, 6 dependencias del cliente de tiempo de ejecución (incluye D3.js para flujos de trabajo) - superficie de ataque mínima",
    "Hooks only apply to sessions started <em>after</em> installation. Restart Claude Code after starting the dashboard.":
      "Los ganchos solo se aplican a las sesiones iniciadas <em>después</em> Instalación. Reinicie el código Claude después de iniciar el panel de control.",
    "On some systems the shell environment when Claude Code fires hooks may not include the full PATH. Test with <code>node --version</code>. If not found, use the absolute path to <code>node</code> in the hook command.":
      "En algunos sistemas, el entorno de la cáscara cuando Claude Code dispara los ganchos puede no incluir el PATH completo. Pruebe con <code>node --version</code>. Si no se encuentra, use el camino absoluto a <code>node</code> En el comando de gancho.",
    Problem: "problema",
    Solution: "solución",
    "<code>better-sqlite3</code> errors during install":
      "<code>better-sqlite3</code> Errores durante la instalación",
    "This is non-fatal — <code>better-sqlite3</code> is an optional dependency. On Node 22+ the server automatically falls back to built-in <code>node:sqlite</code>. On older Node versions, install Python 3 + C++ build tools, then run <code>npm rebuild better-sqlite3</code>. For the desktop app, the <code>desktop:install</code> preflight prints copy-pasteable per-OS setup guidance (incl. a no-toolchain alternative) when the native build fails.":
      "Esto no es mortal — <code>better-sqlite3</code> Es una dependencia opcional. En Node 22+ el servidor vuelve automáticamente a los incorporados. <code>node:sqlite</code>. versiones de Node más antiguas, instale las herramientas de compilación de Python 3 + C++, luego ejecute <code>npm rebuild better-sqlite3</code>. para la aplicación de escritorio, el <code>desktop:install</code> Impresiones de prevolado con instrucciones de configuración copiables y pegables por sistema operativo (incluyendo una alternativa sin cadena de herramientas) cuando la construcción nativa falla.",
    'Dashboard shows "Disconnected"': 'El panel de control muestra "Desconectado"',
    "Server is not running. Start it with <code>npm run dev</code>. Client auto-reconnects every 2s.":
      "El servidor no está funcionando. Inícialo con <code>npm run dev</code>. se reconecta automáticamente el cliente cada 2 segundos.",
    "Events Today shows 0": "Eventos de hoy muestran 0",
    "Ensure you are on the latest version (timezone bug was fixed). Restart the server.":
      "Asegúrese de estar en la última versión (se ha solucionado el error del huso horario). Reinicie el servidor.",
    "Port 4820 already in use": "Puerto 4820 ya en uso",
    "Run <code>DASHBOARD_PORT=4821 npm run dev</code>, update Vite proxy in <code>client/vite.config.ts</code>, and re-run <code>npm run install-hooks</code>.":
      "carrera <code>DASHBOARD_PORT=4821 npm run dev</code>, actualizar el proxy Vite en <code>client/vite.config.ts</code>, y volver a ejecutar <code>npm run install-hooks</code>.",
    "Stale seed data shown": "Se muestran datos de semillas caducas",
    "Run <code>npm run clear-data</code> to wipe all rows, then restart.":
      "carrera <code>npm run clear-data</code> Para borrar todas las filas, luego reinicia.",
    "Hooks show validation error about matcher":
      "Los ganchos muestran un error de validación sobre el coincidiente",
    'Ensure you\'re on the latest version — the hook format was updated to use <code>matcher: "*"</code> string (not object).':
      'Asegúrate de estar en la última versión: el formato de gancho se actualizó para usar <code>matcher: "*"</code> cadena (no objeto).',
    '"SQLite backend not available" on startup': '"Backend SQLite no disponible" al arrancar',
    "Neither <code>better-sqlite3</code> nor <code>node:sqlite</code> could load. Upgrade to Node.js 22+ (recommended), or install Python 3 + C++ build tools and run <code>npm rebuild better-sqlite3</code>.":
      "ninguno <code>better-sqlite3</code> tampoco <code>node:sqlite</code> podría cargar. Actualice a Node.js 22+ (recomendado) o instale herramientas de compilación de Python 3 + C++ y ejecute <code>npm rebuild better-sqlite3</code>.",
    "Docker container runs but no sessions appear":
      "El contenedor Docker se ejecuta, pero no aparecen sesiones",
    "Hooks run on the host, not inside the container. Run <code>npm run install-hooks</code> on the host after the container starts. Verify hooks in <code>~/.claude/settings.json</code> point to <code>localhost:4820</code>.":
      "Los ganchos funcionan en el host, no dentro del contenedor. carrera <code>npm run install-hooks</code> En el host después de que el contenedor comience. Verifique los ganchos en <code>~/.claude/settings.json</code> Puntar a <code>localhost:4820</code>.",
    Technology: "tecnología",
    "Why This Over Alternatives": "Por qué esto sobre alternativas",
    "Zero-config, embedded, no server process. WAL mode gives concurrent reads. Synchronous API is simpler than async for this use case. <code>better-sqlite3</code> is preferred when prebuilds are available; falls back to Node.js built-in <code>node:sqlite</code> on Node 22+ when the native module cannot be compiled.":
      "Configuración cero, incorporado, sin proceso de servidor. El modo WAL proporciona lecturas concurrentes. La API sincrónica es más sencilla que la asincrónica para este caso de uso. <code>better-sqlite3</code> Se prefiere cuando están disponibles las precompilaciones; se recurre a la incorporación de Node.js <code>node:sqlite</code> en Node 22+ cuando el módulo nativo no se puede compilar.",
    "Battle-tested, minimal, well-understood. Fastify would be overkill; raw <code>http</code> module would require too much boilerplate for routing.":
      "Probado en batalla, minimalista, bien entendido. Fastify sería una exageración; crudo <code>http</code> El módulo requeriría demasiada plantilla para el enrutamiento.",
    "Fastest, most lightweight WebSocket library for Node. No Socket.IO overhead needed — we only push typed JSON messages one-way.":
      "La biblioteca WebSocket más rápida y ligera para Node. No se necesita sobrecarga de Socket.IO, solo enviamos mensajes JSON de tipo de forma bidireccional.",
    "Current supported client runtime with strong TypeScript support. CCAM remains a client-rendered SPA and does not enable Server Components or RSC.":
      "Runtime de cliente actualmente compatible y con sólido soporte de TypeScript. CCAM sigue siendo una SPA renderizada en el cliente y no habilita Server Components ni RSC.",
    "Fast builds, native ESM, excellent dev experience. Proxy config handles the dev server split cleanly with no ejection.":
      "Construcciones rápidas, ESM nativo, excelente experiencia de desarrollo. La configuración del proxy maneja la división del servidor de desarrollo de forma limpia sin expulsión.",
    "Utility-first approach keeps styles colocated with markup. No CSS module boilerplate. Custom dark theme config for the dark UI.":
      "El enfoque de priorizar la utilidad mantiene los estilos colocalizados con el marcado. Sin plantilla de módulo CSS. Configuración personalizada del tema oscuro para la interfaz de usuario oscura.",
    "Standard declarative routing for React SPAs. Layout routes with <code>&lt;Outlet&gt;</code> give clean shell composition without enabling framework or RSC modes.":
      "Enrutamiento declarativo estándar para SPA React. Las rutas de diseño con <code>&lt;Outlet&gt;</code> ofrecen una composición limpia sin habilitar modos framework o RSC.",
    "Tree-shakeable icon library — only imports what's used (~20 icons). No heavy icon font.":
      "Biblioteca de iconos que se pueden sacudir como un árbol: solo importa lo que se utiliza (~20 iconos). Sin fuente de iconos pesada.",
    "Catches null/undefined bugs at compile time. <code>noUncheckedIndexedAccess</code> prevents array bounds issues in analytics aggregations.":
      "Captura errores nulos/indefinidos en el momento de la compilación. <code>noUncheckedIndexedAccess</code> previene problemas de límites de arreglo en las agregaciones analíticas.",
    "Industry-standard data visualization library. Powers the Workflows page's 11 interactive sections — DAG layouts, Sankey diagrams, force-directed graphs, bubble charts, and swim-lane timelines. No wrapper libraries needed; direct SVG rendering keeps bundle impact minimal.":
      "Biblioteca de visualización de datos estándar de la industria. Proporciona potencia a las 11 secciones interactivas de la página de flujos de trabajo: diseños DAG, diagramas Sankey, gráficos dirigidos por fuerza, gráficos de burbujas y cronogramas de carril de natación. No se necesitan bibliotecas de envoltorio; la renderización directa de SVG mantiene el impacto del paquete mínimo.",
    "Available on virtually all systems. Handles ANSI and JSON natively with stdlib only. No install step required.":
      "Disponible en prácticamente todos los sistemas. Maneja ANSI y JSON nativamente con stdlib solo. No se requiere ningún paso de instalación.",
    "Local-first monitoring for Claude Code sessions, agents, and tool events. Built for real-time visibility with zero external dependencies.":
      "Monitoreo local primero para sesiones de Claude Code, agentes y eventos de herramientas. Construido para visibilidad en tiempo real con cero dependencias externas.",
    Install: "instalar",
    Setup: "configuración",
    "About the Creator": "Sobre el creador",
    '<span class="caption-icon">⭐</span> <span> Enjoying the project? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer">Give it a star on GitHub</a> and help more builders discover it. </span>':
      '<span class="caption-icon">⭐</span> <span> ¿Disfrutando del proyecto? <a class="star-history-caption-link" href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor" target="_blank" rel="noopener noreferrer">Dale una estrella en GitHub</a> Y ayudar a más constructores a descubrirlo. </span>',
    'Clears the waiting flag and promotes the main agent to <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>. The only reliable signal that text-only assistant turns have started — they emit no <code>PreToolUse</code> before <code>Stop</code>.':
      'Borra la bandera de espera y promueve al agente principal a <span class="status-chip chip-working"><span class="chip-dot"></span>de trabajo</span>. señal confiable de que los teléfonos con asistente de texto solo han comenzado a funcionar: no emiten ninguna <code>PreToolUse</code> antes <code>Stop</code>.',
    'Clears the waiting flag, sets agent → <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>, <code>current_tool</code> set. If tool is <code>Agent</code>, subagent record created.':
      'Borra la bandera de espera, establece agente → <span class="status-chip chip-working"><span class="chip-dot"></span>de trabajo</span>, <code>current_tool</code> Configurar. Si la herramienta es <code>Agent</code>, registro del subagente creado.',
    'Clears the waiting flag (covers permission-prompt approvals mid-tool). <code>current_tool</code> cleared. Agent stays <span class="status-chip chip-working"><span class="chip-dot"></span>Working</span>.':
      'Borra la bandera de espera (cubre las aprobaciones de la solicitud de permiso a mitad de la herramienta). <code>current_tool</code> Liberado. El agente permanece <span class="status-chip chip-working"><span class="chip-dot"></span>de trabajo</span>.',
    'Non-error: main agent → <code>waiting</code> — UI shows <span class="status-chip chip-waiting"><span class="chip-dot"></span>Waiting</span> until the next user input. <code>stop_reason=error</code>: marks the agent and session <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>. Background subagents keep running.':
      'Sin error: agente principal → <code>waiting</code> — Muestra de interfaz de usuario <span class="status-chip chip-waiting"><span class="chip-dot"></span>espera</span> Hasta la próxima entrada del usuario. <code>stop_reason=error</code>: marca al agente y la sesión <span class="status-chip chip-error"><span class="chip-dot"></span>error informático</span>. de los subagentes de fondo siguen funcionando.',
    'Matched subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session\'s <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent\'s own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks.':
      'Subagente coincidente → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completado</span>. idamente lo hace <strong>no</strong> Borrar la bandera de espera: un subagente en segundo plano que termina no nos dice nada sobre el humano. También inicia una escaneo JSONL de fuego y olvido (<code>scanAndImportSubagents</code>) que camina por la sesión <code>subagents/agent-*.jsonl</code> Archivos, pares <code>tool_use</code> ↔ <code>tool_result</code> Bloques por <code>tool_use_id</code>, y emite por herramienta <code>PreToolUse</code> + <code>PostToolUse</code> Eventos bajo los propios de cada subagente <code>agent_id</code> — las llamadas de herramientas de superficies que hacen los subagentes internamente y que nunca disparan ningún gancho.',
    'Creates a compaction subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Detected via <code>isCompactSummary</code> entries in the transcript. Token baselines preserve pre-compaction totals. Periodic scanner (cadence ~¼ of <code>DASHBOARD_STALE_MINUTES</code>) catches compactions when no hooks fire.':
      'Crea un subagente de compactación → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completado</span>. detectado a través de <code>isCompactSummary</code> Entradas en la transcripción. Las bases de referencia de tokens conservan los totales previos a la compactación. Escáner periódico (cadencia ~¼ de <code>DASHBOARD_STALE_MINUTES</code>) atrapa compactaciones cuando no disparan los ganchos.',
    'Drops the waiting flag. If the session is already in <span class="status-chip chip-error"><span class="chip-dot"></span>Error</span>, the error state is preserved; otherwise marks all agents and the session as <span class="status-chip chip-completed"><span class="chip-dot"></span>Completed</span>. Evicts the session\'s transcript from the shared cache.':
      'Deja caer la bandera de espera. Si la sesión ya está en <span class="status-chip chip-error"><span class="chip-dot"></span>error informático</span>, el estado de error se preserva; de lo contrario, marca a todos los agentes y la sesión como <span class="status-chip chip-completed"><span class="chip-dot"></span>Completado</span>. la transcripción de la sesión del caché compartido.',
    "SQLite connection, WAL/FK pragmas, schema migrations (<code>CREATE TABLE IF NOT EXISTS</code>), all prepared statements as a reusable <code>stmts</code> object. Tries <code>better-sqlite3</code> first, falls back to built-in <code>node:sqlite</code> via <code>compat-sqlite.js</code>":
      "Conexión SQLite, pragmas WAL/FK, migraciones de esquema (<code>CREATE TABLE IF NOT EXISTS</code>), todas las declaraciones preparadas como reutilizables <code>stmts</code> Objeto. Intenta <code>better-sqlite3</code> En primer lugar, vuelve a lo incorporado <code>node:sqlite</code> a través de <code>compat-sqlite.js</code>",
    "Each page pulls initial data from REST then subscribes to eventBus for live updates":
      "Cada página obtiene los datos iniciales de REST y luego se suscribe a eventBus para recibir actualizaciones en vivo.",
    "Entity Relationship Diagram — SQLite schema":
      "Diagrama de relaciones de entidades - esquema SQLite",
    "Working Dir": "Director de trabajo",
    "Git Branch": "Ramificación de Git",
    "Context Bar": "Barra de contexto",
    "Token Counts": "Contas de tokens",
    "Session Cost": "Costo de la sesión",
    "Statusline rendering pipeline — invoked on each Claude Code update":
      "Pipeline de renderizado de Statusline: invocado en cada actualización del Código Claude.",
    "Aggregates data from multiple API endpoints to display high-signal metrics directly in the sidebar:":
      "Agrupa datos de múltiples puntos finales de API para mostrar métricas de alto señal directamente en la barra lateral:",
    "Zero-Config Setup": "Configuración de cero configuración",
    "One-line mental model": "Modelo mental de una línea",
    "Your data survives reinstalls and updates":
      "Sus datos sobreviven a las reinstalaciones y actualizaciones",
    "The <code>claude</code> CLI is found automatically":
      "el <code>claude</code> El CLI se encuentra automáticamente",
    'Open <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> Releases → latest </a> and download the asset for your platform. The macOS and Windows Desktop CI jobs auto-publish a new <code>vX.Y.Z</code> release every time the version in <code>package.json</code> is bumped on <code>master</code>, so this link always points at the current build. Releases are public — no GitHub sign-in required.':
      'aire libre <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/releases/latest" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> Lanzamientos → más recientes </a> y descargue el recurso para su plataforma. Los trabajos de CI de escritorio de macOS y Windows publican automáticamente un nuevo <code>vX.Y.Z</code> Lanzamiento cada vez que la versión en <code>package.json</code> Se golpea en <code>master</code>, por lo que este enlace siempre señala la versión actual. Las publicaciones son públicas, no se requiere iniciar sesión en GitHub.',
    'Want a build straight off the tip of <code>master</code>, ahead of the next tagged release? Every green run of the <code>🍎 macOS Desktop (DMG)</code> job on <code>macos-latest</code> uploads both per-arch DMGs as the <code>ClaudeCodeMonitor-dmg</code> workflow artifact, and the <code>🪟 Windows Desktop (EXE)</code> job on <code>windows-latest</code> uploads the installer + portable EXEs as the <code>ClaudeCodeMonitor-win</code> artifact. Open the <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><polyline points="12 7 12 12 15 14"></polyline></svg> latest passing run </a>, scroll to its Artifacts section, and download <code>ClaudeCodeMonitor-dmg</code> or <code>ClaudeCodeMonitor-win</code>. (GitHub sign-in required; 14-day retention.)':
      'Quieres una construcción directamente desde la punta de <code>master</code>, ¿antes del próximo lanzamiento etiquetado? Cada carrera verde del <code>🍎 macOS Desktop (DMG)</code> Trabajo en curso <code>macos-latest</code> Sube tanto los DMG por archivo como el <code>ClaudeCodeMonitor-dmg</code> Artefacto del flujo de trabajo y el <code>🪟 Windows Desktop (EXE)</code> Trabajo en curso <code>windows-latest</code> Sube el instalador + los EXEs portátiles como el <code>ClaudeCodeMonitor-win</code> Artefacto. Abre el <a href="https://github.com/hoangsonww/Claude-Code-Agent-Monitor/actions/workflows/ci.yml?query=branch%3Amaster+is%3Asuccess" target="_blank" rel="noopener noreferrer" class="dl-chip"> <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><polyline points="12 7 12 12 15 14"></polyline></svg> Última carrera de paso </a>, desplázate hasta la sección de Artifactos y descarga <code>ClaudeCodeMonitor-dmg</code> Oregón <code>ClaudeCodeMonitor-win</code>. (Se requiere iniciar sesión en GitHub; retención de 14 días).',
    "Incoming Webhook URL": "URL de Webhook entrante",
    "Webhook URL": "URL de Webhook",
    "Detection pipeline from scheduler to UI":
      "Pipeline de detección desde el programador hasta la interfaz de usuario",
    "A shell-less <code>git fetch</code> with a 120-second timeout, followed by a <code>rev-list</code> against the tracked upstream. Each call runs from <code>server/lib/update-check.js</code> and returns a structured payload — never throws — so a flaky remote can't stall the dashboard.":
      "Sin cáscara <code>git fetch</code> Con un tiempo de espera de 120 segundos, seguido de un <code>rev-list</code> Contra el rastreado en el curso superior. Cada llamada se ejecuta desde <code>server/lib/update-check.js</code> y devuelve una carga útil estructurada, nunca lanza, por lo que un control remoto inestable no puede paralizar el panel de instrumentos.",
    "Detection-only by design": "Detección solo por diseño",
    "Adding a language": "Añadir un idioma",
    "<strong>Ask box:</strong> answers simple status questions locally from cached data (“what's running”, “any errors”, “status”).":
      '<strong>Caja de preguntas:</strong> Responde preguntas de estado simples localmente a partir de datos almacenados ("qué está ejecutando", "algunos errores", "estado").',
    "A third way to run: the Desktop App (macOS &amp; Windows)":
      "Una tercera forma de ejecutar: la aplicación de escritorio (macOS y Windows)",
    "Hooks still run on the host": "Los ganchos todavía funcionan en el host",
    "Hook note": "Nota de gancho",
    "SQL injection": "Inyección SQL",
    "All queries use prepared statements with parameterized values — no string interpolation":
      "Todas las consultas utilizan declaraciones preparadas con valores parametrizados, sin interpolación de cadenas",
    "Request size": "Tamaño de la solicitud",
    "Express JSON body parser limited to 1MB":
      "El analizador de cuerpo JSON de Express está limitado a 1 MB.",
    "Dashboard — stats, active agents, recent events":
      "Panel de control: estadísticas, agentes activos, eventos recientes",
    "KanbanBoard — agent status columns": "KanbanBoard — columnas de estado del agente",
    "Sessions — searchable, filterable table": "Sesiones: tabla buscable y filtrable",
    "SessionDetail — agents + full event timeline":
      "Detalles de la sesión: agentes + cronología completa del evento",
    "ActivityFeed — real-time streaming event log":
      "ActivityFeed: registro de eventos de transmisión en tiempo real",
    "Analytics — token usage, heatmap (day-of-week aligned), tool charts, donut charts":
      "Análisis: uso de tokens, mapa de calor (alineado por día de la semana), gráficos de herramientas, gráficos de rosquillas",
    "Workflows — D3.js visualizations, cross-filtering, status filter, session drill-in":
      "Flujos de trabajo: visualizaciones D3.js, filtrado cruzado, filtro de estado, perforación de sesión",
    "Settings — model pricing, hook status, data export, session cleanup":
      "Configuración: precios del modelo, estado del gancho, exportación de datos, limpieza de sesiones",
    'Returns <code>{ "status": "ok", "timestamp": "..." }</code>':
      'Devoluciones <code>{ "status": "ok", "timestamp": "..." }</code>',
    "List sessions with agent counts and per-session cost. Params: <code>status</code>, <code>q</code> (case-insensitive search across <code>id</code>/<code>name</code>/<code>cwd</code>), <code>limit</code> (default 50, max 10000), <code>offset</code>. Response includes <code>total</code> for paginators.":
      "Listar sesiones con el número de agentes y el costo por sesión. Parámetros: <code>status</code>, <code>q</code> (búsqueda insensible a mayúsculas y minúsculas en todo <code>id</code>/<code>name</code>/<code>cwd</code>), <code>limit</code> (por defecto 50, máximo 10000), <code>offset</code>. incluye <code>total</code> Para paginadores.",
    "Session detail with agents and events": "Detalles de la sesión con agentes y eventos",
    "Create session (idempotent on <code>id</code>)":
      "Crear sesión (idempotente en <code>id</code>)",
    "Update session status / metadata": "Actualizar el estado de la sesión / metadatos",
    "List agents — params: <code>status</code>, <code>session_id</code>, <code>limit</code>, <code>offset</code>":
      "Listar agentes - parámetros: <code>status</code>, <code>session_id</code>, <code>limit</code>, <code>offset</code>",
    "Single agent detail": "Detalles del agente único",
    "Create agent": "Crear agente",
    "Update agent status / task / current_tool":
      "Actualizar el estado del agente / tarea / herramienta actual",
    "List events newest-first — params: <code>session_id</code>, <code>limit</code>, <code>offset</code>":
      "Listar eventos desde el más reciente al primero - parámetros: <code>session_id</code>, <code>limit</code>, <code>offset</code>",
    "Aggregate counts + status distributions + WS connections":
      "Contas agregadas + distribuciones de estado + conexiones WS",
    "Token totals, tool usage, daily trends, agent types, event types, averages":
      "Totales de tokens, uso de herramientas, tendencias diarias, tipos de agentes, tipos de eventos, promedios",
    "Receive and process a Claude Code hook event (called by hook-handler.js)":
      "Recibir y procesar un evento de gancho de código Claude (llamado por hook-handler.js)",
    "List all model pricing rules": "Lista todas las reglas de precios de los modelos",
    "Create or update a pricing rule": "Crear o actualizar una regla de precios",
    "Delete a pricing rule": "Eliminar una regla de precios",
    "Total cost across all sessions": "Costo total de todas las sesiones",
    "Cost breakdown for a specific session": "Desglose de costos para una sesión específica",
    "System info, DB stats, hook installation status":
      "Información del sistema, estadísticas de la base de datos, estado de instalación del gancho",
    "Delete all sessions, agents, events, token usage":
      "Borrar todas las sesiones, agentes, eventos, uso de tokens",
    "Reinstall Claude Code hooks": "Reinstalar los ganchos de Claude Code",
    "Reset Claude, Codex, or both pricing tables to defaults":
      "Restablecer las tablas de precios de Claude, Codex o ambas a los valores predeterminados",
    "Export all data as JSON download": "Exportar todos los datos como descarga JSON",
    "Abandon stale sessions (by hours), purge old data (by days)":
      "Abandone sesiones obsoletas (por horas), purgue datos antiguos (por días)",
    "OS-aware paths, archive command, supported extensions, step-by-step instructions; includes live stats for the default <code>~/.claude/projects</code> folder":
      "Rutas conscientes del sistema operativo, comando de archivo, extensiones compatibles, instrucciones paso a paso; incluye estadísticas en vivo para la predeterminada <code>~/.claude/projects</code> carpeta",
    "Re-scan the default <code>~/.claude/projects</code> directory; safe to re-run (idempotent via session-ID dedup)":
      "Reescanea el predeterminado <code>~/.claude/projects</code> directorio; seguro de volver a ejecutar (identidad empotrada mediante deduplicación de ID de sesión)",
    "Scan any absolute directory (body <code>{ path }</code>); tilde (<code>~</code>) is expanded; walks subdirectories recursively and imports every <code>.jsonl</code> found":
      "Escanea cualquier directorio absoluto (cuerpo <code>{ path }</code>); tilde (<code>~</code>) se expande; recorre subdirectorios recursivamente e importa cada <code>.jsonl</code> fundador",
    "Multipart upload of <code>.jsonl</code>, <code>.meta.json</code>, <code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>, <code>.gz</code>. Per-request staging dir, path-traversal and extraction-size guards. Returns 413 <code>EXTRACTION_LIMIT_EXCEEDED</code> on suspected bomb archives":
      "Subida multiparticular de <code>.jsonl</code>, <code>.meta.json</code>, <code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>, <code>.gz</code>. dirección de ensayo por solicitud, recorrido de rutas y guardias de tamaño de extracción. Devuelve 413 <code>EXTRACTION_LIMIT_EXCEEDED</code> En archivos sospechosos de bombas",
    "Aggregate workflow data — orchestration graphs, tool flows, effectiveness, patterns, model delegation, error propagation, concurrency, complexity, compaction impact. Accepts <code>?status=active|completed</code> query param to filter by workflow status":
      "Datos agregados del flujo de trabajo: gráficos de orquestación, flujos de herramientas, eficacia, patrones, delegación de modelos, propagación de errores, concurrencia, complejidad y impacto de la compactación. Acepta <code>?status=active|completed</code> Parámetro de consulta para filtrar por estado del flujo de trabajo",
    "Per-session drill-in — agent tree, tool timeline, event details":
      "Entrenamiento por sesión: árbol de agentes, cronología de herramientas, detalles de eventos",
    "Fired-alert feed, newest first (<code>?unacked=true</code>, <code>limit</code>, <code>offset</code>; carries <code>total</code> and <code>unacked</code> counts)":
      "Feed de alerta de disparos, primero los más recientes (<code>?unacked=true</code>, <code>limit</code>, <code>offset</code>; lleva <code>total</code> y <code>unacked</code> Cuenta)",
    "Acknowledge one alert": "Reconocer una alerta",
    "Acknowledge every unacked alert": "Reconocer cada alerta no atendida",
    "List alert rules": "Lista de reglas de alerta",
    "Create a rule (<code>event_pattern</code> | <code>inactivity</code> | <code>status_duration</code> | <code>token_threshold</code>)":
      "Crear una regla (<code>event_pattern</code> | <code>inactivity</code> | <code>status_duration</code> | <code>token_threshold</code>)",
    "Update name / config / enabled / cooldown":
      "Actualizar nombre / configuración / habilitado / tiempo de espera",
    "Delete a rule and its fired-alert history":
      "Borrar una regla y su historial de alertas activadas",
    "Supported providers + their config fields (drives the UI form)":
      "Proveedores compatibles + sus campos de configuración (dirigen el formulario de la interfaz de usuario)",
    "List webhook targets (URLs masked, secrets redacted)":
      "Lista de objetivos de webhook (URLs encriptadas, secretos redactados)",
    "Create a target — 14 first-class providers (Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream) + a generic JSON endpoint":
      "Crea un objetivo: 14 proveedores de primera clase (Slack, Discord, Teams, Google Chat, Mattermost, Rocket.Chat, Telegram, PagerDuty, Opsgenie, Splunk On-Call, Zapier, Make, n8n, Pipedream) + un punto final JSON genérico",
    "Update name / url / enabled / secret / headers / config / rule scope (<code>type</code> is immutable)":
      "Actualizar nombre / url / habilitado / secreto / encabezados / configuración / alcance de la regla (<code>type</code> Es inmutable)",
    "Delete a target and its delivery log": "Eliminar un objetivo y su registro de entrega",
    "Send a synthetic test alert and report the result":
      "Envíe una alerta de prueba sintética y informe el resultado",
    "Recent delivery log for a target": "Registro de entregas recientes para un objetivo",
    Documentation: "documentación",
    Architecture: "arquitectura",
    "Relevant Links": "Enlaces relevantes",
    "GitHub Repo": "Repositorio de GitHub",
    '<span class="caption-icon">🔔</span> <span><strong>Settings · Alerts</strong> — rules-based alerting engine and outbound webhooks in one place: alert rules (event pattern / inactivity / stuck agent / token threshold) with per-rule cooldown, a live fired-alert feed, and 14 first-class webhook providers plus a generic JSON endpoint with optional HMAC signing</span>':
      '<span class="caption-icon">🔔</span> <span><strong>Configuración · Alertas</strong> — motor de alertas basado en reglas y webhooks de salida en un solo lugar: reglas de alerta (patrón de eventos / inactividad / agente atascado / umbral de token) con tiempo de espera por regla, un feed de alertas lanzadas en vivo y 14 proveedores de webhooks de primera clase, además de un punto final JSON genérico con firma HMAC opcional</span>',
    '<span class="caption-icon">📗</span> <span><strong>API Docs · ReDoc</strong> — a self-hosted, read-optimized rendering of the full OpenAPI 3.0 spec at <code>/api/redoc</code>, served entirely offline with no CDN. Complements the interactive Swagger UI at <code>/api/docs</code>; every backend route is documented with parameters, schemas, and examples</span>':
      '<span class="caption-icon">📗</span> <span><strong>Documentación de API · ReDoc</strong> — una renderización alojada por sí misma, optimizada para la lectura, de la especificación completa de OpenAPI 3.0 en <code>/api/redoc</code>, servido completamente sin conexión a Internet sin CDN. Completa la interfaz de usuario interactiva de Swagger en <code>/api/docs</code>; cada ruta de backend está documentada con parámetros, esquemas y ejemplos</span>',
    '<span class="caption-icon">📘</span> <span><strong>API Docs · Swagger UI</strong> — interactive OpenAPI 3.0 playground at <code>/api/docs</code>: collapsible endpoint groups, request/response schemas, auth headers, and try-it-out request execution against the live local server</span>':
      '<span class="caption-icon">📘</span> <span><strong>Documentación de API · Interfaz de usuario Swagger</strong> — patio de recreo interactivo OpenAPI 3.0 en <code>/api/docs</code>: grupos de puntos finales plegables, esquemas de solicitud/respuesta, encabezados de autenticación y ejecución de solicitudes de prueba contra el servidor local en vivo</span>',
    '<span class="caption-icon">📗</span> <span>ReDoc at <code>/api/redoc</code> — a self-hosted, read-optimized three-panel rendering of the same OpenAPI spec: deep-linkable sections, search, and full schema/example detail. Works entirely offline (no CDN)</span>':
      '<span class="caption-icon">📗</span> <span>ReDoc en <code>/api/redoc</code> - una renderización de tres paneles alojada y optimizada para la lectura de la misma especificación OpenAPI: secciones vinculables a través de enlaces profundos, búsqueda y detalles completos del esquema/ejemplo. Funciona completamente sin conexión (sin CDN)</span>',
    '<span class="caption-icon">🔔</span> Settings · Alerts — the rules-based alerting engine, a live fired-alert feed, and outbound webhook channels (14 first-class providers + a generic JSON endpoint) managed together in one place':
      '<span class="caption-icon">🔔</span> Configuración · Alertas: el motor de alertas basado en reglas, un feed de alertas en vivo y canales webhook de salida (14 proveedores de primera clase + un punto final JSON genérico) gestionados juntos en un solo lugar.',
    'Surfaces "dynamic workflows" — the fleets of sub-agents spawned by the <code>Workflow</code> tool and self-paced <code>/loop</code> runs. These emit no hooks, so they are reconstructed from the on-disk run journal written when a workflow finishes (<code>workflows/wf_&lt;runId&gt;.json</code>) plus the inner <code>subagents/agent-*.jsonl</code> transcripts. Each run shows its phases and a per-agent token / tool-call / duration breakdown; a running workflow is detected from its launch script before the journal exists. Runs appear in a panel on the Workflows page and as a linked subsection on each session.':
      'Superficies "trabajos de flujo dinámicos" - las flotas de subagentes generadas por el <code>Workflow</code> Herramienta y a ritmo propio <code>/loop</code> Ejecuciones. Estas no emiten enlaces, por lo que se reconstruyen a partir del diario de ejecución en el disco escrito cuando un flujo de trabajo termina (<code>workflows/wf_&lt;runId&gt;.json</code>) más el interior <code>subagents/agent-*.jsonl</code> transcripciones. Cada ejecución muestra sus fases y una desglose por token/llamada a herramienta/duración por agente; se detecta un flujo de trabajo en ejecución desde su script de lanzamiento antes de que el diario exista. Las ejecuciones aparecen en un panel en la página de Flujos de trabajo y como una subsección vinculada en cada sesión.',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs</strong> — "dynamic workflows" spawned by the Workflow tool, reconstructed from on-disk run journals: status, agent count, tokens, and tool calls, expandable into a per-agent breakdown (phase, state, tokens, tools, duration) with humanized result previews</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Ejecuciones del flujo de trabajo</strong> — "procesos de trabajo dinámicos" generados por la herramienta Workflow, reconstruidos a partir de los registros de ejecución en disco: estado, número de agentes, tokens y llamadas a herramientas, expandibles en una desglose por agente (fase, estado, tokens, herramientas, duración) con vistas preliminares de resultados humanizados</span>',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · in a session</strong> — the same fleets linked to their launching session, so a session\'s dynamic-workflow sub-agents and their folded-in token cost are visible inline</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Ejecuciones de flujo de trabajo · en una sesión</strong> — las mismas flotas vinculadas a su sesión de lanzamiento, por lo que los subagentes del flujo de trabajo dinámico de una sesión y su costo de token incorporado son visibles en línea</span>',
    '<span class="caption-icon">🧬</span> <span><strong>Workflow Runs · expanded</strong> — a run opened up: clickable color-coded phase filters, the per-agent metrics table, and a full list of clickable result items that expand to each agent\'s complete prompt and result</span>':
      '<span class="caption-icon">🧬</span> <span><strong>Ejecuciones de flujo de trabajo · expandido</strong> — se ha abierto una ejecución: filtros de fase codificados por color interactivos, la tabla de métricas por agente y una lista completa de elementos de resultado interactivos que se expanden a la solicitud completa y el resultado de cada agente.</span>',
    'Live counters in Prometheus text-exposition format (v0.0.4): sessions and agents by status, total events, cumulative tokens by kind, connected WebSocket clients, configured remote sources, process uptime and RSS, and build version — so CCAM can be scraped into Prometheus / Grafana. Read-only, and — being under <code>/api</code> — behind the DNS-rebinding Host-header guard and the optional <code>DASHBOARD_TOKEN</code>: a non-loopback scraper (e.g. Prometheus in Docker via <code>host.docker.internal</code>) must be allowlisted with <code>DASHBOARD_ALLOWED_HOSTS</code> or the scrape returns <code>403 EBADHOST</code>, and must send the token when one is set. A ready-to-run Prometheus + Grafana stack with a pre-provisioned "CCAM — Overview" dashboard lives in <code>monitoring/</code> (<code>cd monitoring &amp;&amp; docker compose up -d</code>; Grafana on port 3000).':
      'Mostradores en vivo en formato de exposición de texto de Prometheus (v0.0.4): sesiones y agentes por estado, eventos totales, tokens acumulativos por tipo, clientes WebSocket conectados, fuentes remotas configuradas, tiempo de actividad del proceso y RSS, y versión de la compilación, para que CCAM pueda ser extraído a Prometheus / Grafana. Solo de lectura, y - estando bajo <code>/api</code> — detrás de la guardia del encabezado de Host de reasignación DNS y el opcional <code>DASHBOARD_TOKEN</code>: un raspador sin retroceso (por ejemplo, Prometheus en Docker a través de <code>host.docker.internal</code>) debe estar en la lista de autorización con <code>DASHBOARD_ALLOWED_HOSTS</code> O el rasguño vuelve <code>403 EBADHOST</code>, y debe enviar el token cuando se establece uno. Una pila Prometheus + Grafana lista para ejecutarse con un panel de control "CCAM - Visión general" preprovisionado se encuentra en <code>monitoring/</code> (<code>cd monitoring &amp;&amp; docker compose up -d</code>; Grafana en el puerto 3000).',
    "Comma-separated extra <code>Host</code>-header names allowed past the DNS-rebinding guard — needed for a non-loopback client or scraper, such as Prometheus in Docker reaching <code>/api/metrics</code> via <code>host.docker.internal</code>; an unlisted host is rejected with <code>403 EBADHOST</code>":
      "Extra separado por coma <code>Host</code>-se permiten nombres de encabezados después de la guardia de reasignación de DNS, necesaria para un cliente o scraper no de bucle, como Prometheus en Docker alcanzando <code>/api/metrics</code> a través de <code>host.docker.internal</code>; un host no listado es rechazado con <code>403 EBADHOST</code>",
    "The entire UI ships in <strong>five languages — English, 简体中文, Tiếng Việt, 한국어, and Español</strong> — built on <code>i18next</code> + <code>react-i18next</code> with <code>i18next-browser-languagedetector</code>. Coverage is end-to-end: every page, chart tooltip, Settings flow, Workflow narrative, Config Explorer tab, Run page, and the Alerts rule-help tooltips + webhook setup guides are translated. Switch languages from the sidebar's custom language dropdown — the choice persists in <code>localStorage</code>.":
      "Toda la interfaz se ofrece en <strong>cinco idiomas: English, 简体中文, Tiếng Việt, 한국어 y Español</strong>, con <code>i18next</code> + <code>react-i18next</code> e <code>i18next-browser-languagedetector</code>. La cobertura es integral: se traducen todas las páginas, las ayudas de los gráficos, los flujos de Settings, la narrativa de Workflow, la pestaña Config Explorer, la página Run y las ayudas de reglas de Alerts y de configuración de webhooks. Elige el idioma en el menú desplegable personalizado de la barra lateral; la preferencia se conserva en <code>localStorage</code>.",
    'Every alert-rule field has a help tooltip — the event-type, tool-name, and summary-contains fields include example chips of real hook events and built-in tool names. Each webhook provider ships a collapsible step-by-step setup guide linking to the official docs. A one-click <strong>"Send test"</strong> probe fires a synthetic alert and reports the delivery result inline, and targets can be scoped to specific rules. Fully localized (en / zh / vi / ko / es).':
      'Cada campo de las reglas de alerta tiene una ayuda contextual. Los campos de tipo de evento, nombre de herramienta y texto del resumen incluyen ejemplos de eventos de hook reales y nombres de herramientas integradas. Cada proveedor de webhook incluye una guía de configuración paso a paso y plegable con enlaces a la documentación oficial. La opción <strong>"Enviar prueba"</strong> genera una alerta sintética e informa del resultado en la propia interfaz; además, los destinos pueden limitarse a reglas concretas. Completamente localizado (en / zh / vi / ko / es).',
    "<strong>Server-paginated</strong> table of every recorded session — each page fetches only its slice so cost computation stays bounded no matter how many sessions exist. Case-insensitive search across <code>id</code>, <code>name</code>, and <code>cwd</code> runs server-side with a 300 ms debounce; the status filter composes with search for precise narrowing. A searchable checkbox project picker supports selecting multiple directories; long paths remain available in full, while compact trigger text keeps the filter bar readable. Sorting uses the same custom dropdown pattern. Each row shows the session's real name (synced live from the transcript — a <code>/rename</code> or <code>claude -n</code> title, else the auto title, else the first user prompt, with a short-ID fallback), status badge, agent count, duration, model, and estimated cost. Click any row to drill into the full session detail view with conversation transcript and agent hierarchy.":
      "Tabla <strong>paginada en el servidor</strong> de todas las sesiones registradas: cada página obtiene solo su parte, por lo que el cálculo de costes se mantiene acotado sin importar cuántas sesiones haya. La búsqueda sin distinción entre mayúsculas y minúsculas en <code>id</code>, <code>name</code> y <code>cwd</code> se ejecuta del lado del servidor con una espera de 300 ms; el filtro de estado se combina con la búsqueda para acotar con precisión. Un selector de proyectos con casillas y búsqueda permite seleccionar varios directorios; las rutas largas siguen estando disponibles completas, mientras que un texto compacto mantiene legible la barra de filtros. La ordenación usa el mismo patrón de menú desplegable personalizado. Cada fila muestra el nombre real de la sesión (sincronizado en vivo desde la transcripción: un título de <code>/rename</code> o <code>claude -n</code>, de lo contrario el título automático, luego el primer prompt de usuario, con un ID corto como alternativa), la insignia de estado, el número de agentes, la duración, el modelo y el coste estimado. Haz clic en cualquier fila para abrir la vista detallada completa de la sesión, con transcripción de la conversación y jerarquía de agentes.",
    'A 12-tab inspector at <code>/cc-config</code> for everything Claude Code knows about: skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts. The Settings tab leads with a Current configuration summary of the options <code>/config</code> controls — model, verbose, theme, output style, auto-compact, notifications — resolved across scopes. The Memory tab surfaces both the user and project <code>CLAUDE.md</code> files and the per-project file-based memory store — every auto-memory <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+), grouped by project, searchable, and editable — the <code>MEMORY.md</code> index links are clickable and jump to (scroll to + highlight) the matching fact file. Create, edit, and delete the low-risk surfaces — text-file artifacts, per-project auto-memory files, and <code>keybindings.json</code> (structured inline editor) — with mandatory timestamped backups before every write. Plugins, MCP, hooks, and live settings stay read-only with explainer banners and copy-able CLI commands. See the dedicated <a href="#config-explorer">Config Explorer</a> section for the full tour. Per-plugin contribution breakdowns show author and license.':
      'Un inspector de 12 pestañas en <code>/cc-config</code> para todo lo que Claude Code sabe: habilidades, subagentes, comandos slash, estilos de salida, plugins, mercados, servidores MCP, ganchos, configuraciones, memoria, atajos de teclas y scripts de línea de estado. La pestaña Configuración encabeza con un resumen de la configuración actual de las opciones controladas por <code>/config</code>: modelo, verbose, tema, estilo de salida, compactación automática, notificaciones, resolvidos en todos los ámbitos. La pestaña Memoria muestra tanto los archivos <code>CLAUDE.md</code> del usuario y del proyecto como el almacenamiento de memoria basado en archivos por proyecto: cada <code>.md</code> de memoria automática bajo <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (un índice <code>MEMORY.md</code> más un archivo por hecho recordado, a menudo más de 100), agrupados por proyecto, buscables y editable: los enlaces del índice <code>MEMORY.md</code> son clicables y se saltan al archivo de hecho correspondiente (desplázate hacia arriba + resaltar). Crea, edita y elimina las superficies de bajo riesgo: artefactos de archivos de texto, archivos de memoria automática por proyecto y <code>keybindings.json</code> (editor inline estructurado) con copias de seguridad con hora y fecha obligatorias antes de cada escritura. Los plugins, MCP, ganchos y configuraciones en vivo permanecen de solo lectura con banners explicativos y comandos CLI copiables. Consulta la sección dedicada <a href="#config-explorer">Explorador de Configuración</a> para un recorrido completo. Los desgloses de contribuciones por plugin muestran el autor y la licencia.',
    '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Skills</strong> — the Skills tab lists every discovered skill (user, project, and plugin) with its description and source, is searchable across the whole set, and opens any skill file for a safe, timestamp-backed edit</span>':
      '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Habilidades</strong> — la pestaña Habilidades enumera todas las habilidades descubiertas (usuario, proyecto y complemento) con su descripción y fuente, se puede buscar en todo el conjunto y abre cualquier archivo de habilidad para una edición segura respaldada por una hora y fecha</span>',
    "<code>SessionStart</code>": "<code>Inicio de sesión</code>",
    "<code>UserPromptSubmit</code>": "<code>UserPromptSubmit</code>",
    "<code>PreToolUse</code>": "<code>PreToolUse</code>",
    "<code>PostToolUse</code>": "<code>PostToolUse</code>",
    "<code>Stop</code>": "<code>Detener</code>",
    "<code>SubagentStop</code>": "<code>SubagentStop</code>",
    "Matched subagent → <span class=\"status-chip chip-completed\"><span class=\"chip-dot\"></span>Completed</span>. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session's <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent's own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks. The same scan also rebuilds the nested-subagent hierarchy — it repoints each subagent's <code>parent_agent_id</code> to the true spawner recovered from the transcript's Task tool result (<code>toolUseResult.agentId</code>), so subagents that spawn their own subagents nest correctly instead of flattening under main.":
      'Subagente coincidente → <span class="status-chip chip-completed"><span class="chip-dot"></span>Completado</span>. Deliberadamente <strong>no</strong> borra la marca de espera: que un subagente en segundo plano termine no dice nada sobre la persona. También inicia un análisis JSONL sin bloqueo (<code>scanAndImportSubagents</code>) que recorre los archivos <code>subagents/agent-*.jsonl</code> de la sesión, empareja los bloques <code>tool_use</code> ↔ <code>tool_result</code> por <code>tool_use_id</code> y emite eventos por herramienta <code>PreToolUse</code> + <code>PostToolUse</code> con el <code>agent_id</code> de cada subagente. Esto muestra las llamadas de herramientas internas que nunca activan hooks. El mismo análisis también reconstruye la jerarquía de subagentes anidados: vuelve a apuntar el <code>parent_agent_id</code> de cada subagente al generador real recuperado del resultado de la herramienta Task del transcrito (<code>toolUseResult.agentId</code>), para que los subagentes que generan sus propios subagentes se aniden correctamente en vez de quedar al mismo nivel que el principal.',
    "<code>Notification</code>": "<code>Notificación</code>",
    "<code>Compaction</code>": "<code>Compactación</code>",
    "<code>APIError</code>": "<code>Error API</code>",
    "<code>Interrupted</code>": "<code>Interrumpido</code>",
    "<code>TurnDuration</code>": "<code>Duración de la vuelta</code>",
    "<code>SessionEnd</code>": "<code>SessionEnd</code>",
    "<code>DASHBOARD_PORT</code>": "<code>DASHBOARD_PORT</code>",
    "<code>4820</code>": "<code>4820</code>",
    "<code>CLAUDE_DASHBOARD_PORT</code>": "<code>CLAUDE_DASHBOARD_PORT</code>",
    "<code>MCP_DASHBOARD_BASE_URL</code>": "<code>MCP_DASHBOARD_BASE_URL</code>",
    "<code>http://127.0.0.1:4820</code>": "<code>http://127.0.0.1:4820</code>",
    "<code>MCP_TRANSPORT</code>": "<code>MCP_TRANSPORT</code>",
    "<code>stdio</code>": "<code>stdio</code>",
    "<code>MCP_HTTP_PORT</code>": "<code>MCP_HTTP_PORT</code>",
    "<code>8819</code>": "<code>8819</code>",
    "<code>MCP_HTTP_HOST</code>": "<code>MCP_HTTP_HOST</code>",
    "<code>127.0.0.1</code>": "<code>127.0.0.1</code>",
    "<code>DASHBOARD_DB_PATH</code>": "<code>DASHBOARD_DB_PATH</code>",
    "<code>data/dashboard.db</code>": "<code>data/dashboard.db</code>",
    "<code>DASHBOARD_ALLOWED_HOSTS</code>": "<code>DASHBOARD_ALLOWED_HOSTS</code>",
    "<code>(empty)</code>": "<code>(vacío)</code>",
    "<code>DASHBOARD_WORKING_IDLE_SECONDS</code>": "<code>DASHBOARD_WORKING_IDLE_SECONDS</code>",
    "<code>120</code>": "<code>120</code>",
    "<code>DASHBOARD_LIVENESS_PROBE</code>": "<code>DASHBOARD_LIVENESS_PROBE</code>",
    "<code>1</code>": "<code>1</code>",
    "<code>DASHBOARD_LIVENESS_IDLE_SECONDS</code>": "<code>DASHBOARD_LIVENESS_IDLE_SECONDS</code>",
    "<code>60</code>": "<code>60</code>",
    "<code>DASHBOARD_SESSION_SYNC_MS</code>": "<code>DASHBOARD_SESSION_SYNC_MS</code>",
    "<code>30000</code>": "<code>30000</code>",
    "<code>NODE_ENV</code>": "<code>NODE_ENV</code>",
    "<code>development</code>": "<code>desarrollo</code>",
    "<code>setup</code>": "<code>configuración</code>",
    "<code>npm run setup</code>": "<code>npm run setup</code>",
    "<code>dev</code>": "<code>dev</code>",
    "<code>npm run dev</code>": "<code>npm run dev</code>",
    "<code>dev:server</code>": "<code>dev:server</code>",
    "<code>npm run dev:server</code>": "<code>npm run dev:server</code>",
    "<code>dev:client</code>": "<code>dev:client</code>",
    "<code>npm run dev:client</code>": "<code>npm run dev:client</code>",
    "<code>build</code>": "<code>construir</code>",
    "<code>npm run build</code>": "<code>npm run build</code>",
    "<code>start</code>": "<code>start</code>",
    "<code>npm start</code>": "<code>npm start</code>",
    "<code>install-hooks</code>": "<code>instalar-hooks</code>",
    "<code>npm run install-hooks</code>": "<code>npm run install-hooks</code>",
    "<code>seed</code>": "<code>semilla</code>",
    "<code>npm run seed</code>": "<code>npm run seed</code>",
    "<code>import-history</code>": "<code>import-historia</code>",
    "<code>npm run import-history</code>": "<code>npm run import-history</code>",
    "<code>clear-data</code>": "<code>limpiar-datos</code>",
    "<code>npm run clear-data</code>": "<code>npm run clear-data</code>",
    "<code>test</code>": "<code>prueba</code>",
    "<code>npm test</code>": "<code>npm test</code>",
    "<code>test:server</code>": "<code>test:server</code>",
    "<code>npm run test:server</code>": "<code>npm run test:server</code>",
    "<code>test:client</code>": "<code>test:client</code>",
    "<code>npm run test:client</code>": "<code>npm run test:client</code>",
    "<code>mcp:install</code>": "<code>mcp:instalar</code>",
    "<code>npm run mcp:install</code>": "<code>npm run mcp:install</code>",
    "<code>mcp:typecheck</code>": "<code>mcp:typecheck</code>",
    "<code>npm run mcp:typecheck</code>": "<code>npm run mcp:typecheck</code>",
    "<code>mcp:build</code>": "<code>mcp:build</code>",
    "<code>npm run mcp:build</code>": "<code>npm run mcp:build</code>",
    "<code>mcp:start</code>": "<code>mcp:start</code>",
    "<code>npm run mcp:start</code>": "<code>npm run mcp:start</code>",
    "<code>mcp:start:http</code>": "<code>mcp:start:http</code>",
    "<code>npm run mcp:start:http</code>": "<code>npm run mcp:start:http</code>",
    "<code>mcp:start:repl</code>": "<code>mcp:start:repl</code>",
    "<code>npm run mcp:start:repl</code>": "<code>npm run mcp:start:repl</code>",
    "<code>mcp:dev</code>": "<code>mcp:dev</code>",
    "<code>npm run mcp:dev</code>": "<code>npm run mcp:dev</code>",
    "<code>mcp:dev:http</code>": "<code>mcp:dev:http</code>",
    "<code>npm run mcp:dev:http</code>": "<code>npm run mcp:dev:http</code>",
    "<code>mcp:dev:repl</code>": "<code>mcp:dev:repl</code>",
    "<code>npm run mcp:dev:repl</code>": "<code>npm run mcp:dev:repl</code>",
    "<code>mcp:docker:build</code>": "<code>mcp:docker:build</code>",
    "<code>npm run mcp:docker:build</code>": "<code>npm run mcp:docker:build</code>",
    "<code>mcp:podman:build</code>": "<code>mcp:podman:build</code>",
    "<code>npm run mcp:podman:build</code>": "<code>npm run mcp:podman:build</code>",
    "<code>test:mcp</code>": "<code>test:mcp</code>",
    "<code>npm run test:mcp</code>": "<code>npm run test:mcp</code>",
    "<code>desktop:install</code>": "<code>desktop:install</code>",
    "<code>npm run desktop:install</code>": "<code>npm run desktop:install</code>",
    "<code>desktop:build</code>": "<code>desktop:build</code>",
    "<code>npm run desktop:build</code>": "<code>npm run desktop:build</code>",
    "<code>desktop:dev</code>": "<code>desktop:dev</code>",
    "<code>npm run desktop:dev</code>": "<code>npm run desktop:dev</code>",
    "<code>desktop:test</code>": "<code>desktop:test</code>",
    "<code>npm run desktop:test</code>": "<code>npm run desktop:test</code>",
    "<code>desktop:dmg</code>": "<code>desktop:dmg</code>",
    "<code>npm run desktop:dmg</code>": "<code>npm run desktop:dmg</code>",
    "<code>desktop:dmg:arm64</code>": "<code>desktop:dmg:arm64</code>",
    "<code>npm run desktop:dmg:arm64</code>": "<code>npm run desktop:dmg:arm64</code>",
    "<code>desktop:dmg:x64</code>": "<code>desktop:dmg:x64</code>",
    "<code>npm run desktop:dmg:x64</code>": "<code>npm run desktop:dmg:x64</code>",
    "<code>desktop:dmg:universal</code>": "<code>desktop:dmg:universal</code>",
    "<code>npm run desktop:dmg:universal</code>": "<code>npm run desktop:dmg:universal</code>",
    "<code>desktop:win</code>": "<code>desktop:win</code>",
    "<code>npm run desktop:win</code>": "<code>npm run desktop:win</code>",
    "<code>desktop:win:portable</code>": "<code>desktop:win:portable</code>",
    "<code>npm run desktop:win:portable</code>": "<code>npm run desktop:win:portable</code>",
    "<code>build:win-icon</code>": "<code>build:win-icon</code>",
    "<code>npm run build:win-icon</code>": "<code>npm run build:win-icon</code>",
    "<code>format</code>": "<code>format</code>",
    "<code>npm run format</code>": "<code>npm run format</code>",
    "<code>format:check</code>": "<code>formato:verificar</code>",
    "<code>npm run format:check</code>": "<code>npm run formato:check</code>",
    Server: "servidor",
    "<code>status</code> · <code>start</code> · <code>repl</code>":
      "<code>estado</code> · <code>inicio</code> · <code>repl</code>",
    "<code>health</code> · <code>stats</code> · <code>kanban</code> · <code>tail</code>":
      "<code>salud</code> · <code>estadísticas</code> · <code>kanban</code> · <code>cola</code>",
    "<code>sessions</code> · <code>session &lt;id&gt;</code> · <code>agents</code> · <code>events</code>":
      "<code>sessions</code> · <code>session &lt;id&gt;</code> · <code>agents</code> · <code>events</code>",
    "<code>analytics</code> · <code>workflows</code> · <code>runs</code> · <code>cost</code>":
      "<code>análisis</code> · <code>flujos de trabajo</code> · <code>ejecuciones</code> · <code>costo</code>",
    "<code>alerts</code> · <code>alerts ack</code> · <code>rules</code> · <code>webhooks</code> · <code>webhooks test</code>":
      "<code>alertas</code> · <code>alertas ack</code> · <code>reglas</code> · <code>webhooks</code> · <code>test de webhooks</code>",
    "<code>pricing</code> · <code>pricing set / delete / reset</code>":
      "<code>precios</code> · <code>establecer precios / eliminar / restablecer</code>",
    "<code>import rescan</code> · <code>import path &lt;dir&gt;</code>":
      "<code>import rescan</code> · <code>import path &lt;dir&gt;</code>",
    "<code>remote-sources list</code> · <code>remote-sources add</code> · <code>remote-sources test &lt;id&gt;</code> · <code>remote-sources sync [id]</code> · <code>remote-sources rm &lt;id&gt; [--purge]</code>":
      "<code>remote-sources list</code> · <code>remote-sources add</code> · <code>remote-sources test &lt;id&gt;</code> · <code>remote-sources sync [id]</code> · <code>remote-sources rm &lt;id&gt; [--purge]</code>",
    "<code>doctor</code> · <code>info</code> · <code>export</code> · <code>cleanup</code> · <code>reinstall-hooks</code> · <code>update-check</code> · <code>clear-data --yes</code> · <code>open</code>":
      "<code>doctor</code> · <code>info</code> · <code>exportar</code> · <code>limpiar</code> · <code>reinstalar-ganchos</code> · <code>actualizar-verificar</code> · <code>limpiar-datos --sí</code> · <code>abrir</code>",
    "<code>server/index.js</code>": "<code>server/index.js</code>",
    "<code>server/db.js</code>": "<code>server/db.js</code>",
    "<code>server/compat-sqlite.js</code>": "<code>server/compat-sqlite.js</code>",
    "<code>server/websocket.js</code>": "<code>server/websocket.js</code>",
    "<code>routes/hooks.js</code>": "<code>rutas/hooks.js</code>",
    "<code>routes/sessions.js</code>": "<code>rutas/sesiones.js</code>",
    "<code>routes/agents.js</code>": "<code>rutas/agentes.js</code>",
    "<code>routes/events.js</code>": "<code>rutas/eventos.js</code>",
    "<code>routes/stats.js</code>": "<code>rutas/stats.js</code>",
    "<code>routes/analytics.js</code>": "<code>rutas/analytics.js</code>",
    "<code>routes/pricing.js</code>": "<code>rutas/pricing.js</code>",
    "<code>routes/settings.js</code>": "<code>rutas/settings.js</code>",
    "System info (DB size, row counts, hook status, server uptime). Data export as one versioned JSON bundle and matching import/restore (<code>POST /api/settings/import</code> via <code>server/lib/data-transfer.js</code> — idempotent, session-atomic, non-destructive; consolidate machines). Session cleanup (abandon stale active sessions, purge old completed sessions). Clear all data. Reset pricing to defaults. Reinstall hooks.":
      "Información del sistema (tamaño de la base de datos, número de filas, estado del gancho, tiempo de actividad del servidor). Exportación de datos como un paquete JSON con versión y importación/restauración correspondientes (<code>POST /api/settings/import</code> a través de <code>server/lib/data-transfer.js</code> - idempotente, atómico de sesión, no destructivo; consolidar máquinas). Limpieza de sesiones (abandonar sesiones activas obsoletas, purgar sesiones antiguas completadas). Borrar todos los datos. Restablecer los precios a los valores predeterminados. Reinstalar los ganchos.",
    "<code>routes/workflows.js</code>": "<code>rutas/workflows.js</code>",
    "<code>idx_agents_session</code>": "<code>idx_agents_session</code>",
    agents: "Agentes",
    "<code>session_id</code>": "<code>session_id</code>",
    "<code>idx_agents_status</code>": "<code>idx_agents_status</code>",
    "<code>status</code>": "<code>estado</code>",
    "<code>idx_events_session</code>": "<code>idx_events_session</code>",
    events: "Eventos",
    "<code>idx_events_type</code>": "<code>idx_events_type</code>",
    "<code>event_type</code>": "<code>tipo de evento</code>",
    "<code>idx_events_created</code>": "<code>idx_events_created</code>",
    "<code>created_at DESC</code>": "<code>created_at DESC</code>",
    "<code>idx_sessions_status</code>": "<code>idx_sessions_status</code>",
    sessions: "sesión",
    "<code>idx_sessions_started</code>": "<code>idx_sessions_started</code>",
    "<code>started_at DESC</code>": "<code>start_at DESC</code>",
    "<code>journal_mode</code>": "<code>journal_mode</code>",
    "<code>WAL</code>": "<code>WAL</code>",
    "<code>foreign_keys</code>": "<code>clave_externa</code>",
    "<code>ON</code>": "<code>EN</code>",
    "<code>busy_timeout</code>": "<code>tiempo_de_espera_ocupado</code>",
    "<code>5000</code>": "<code>5000</code>",
    "Export all data (sessions, agents, events, tokens, workflows, runs, alert rules, pricing) as one versioned JSON download":
      "Exporta todos los datos (sesiones, agentes, eventos, tokens, flujos de trabajo, ejecuciones, reglas de alerta, precios) como una descarga JSON versificada",
    "Restore an export bundle (multipart <code>file</code> or JSON <code>{ path }</code>) — idempotent, non-destructive; consolidate machines":
      "Restaurar un paquete de exportación (archivo <code>file</code> multipart o JSON <code>{ path }</code>) - idempotente, no destructivo; consolidar máquinas",
    "<code>ws://localhost:4820/ws</code>": "<code>ws://localhost:4820/ws</code>",
    "<code>&lt;proj&gt;/&lt;sid&gt;.jsonl</code>": "<code>&lt;proj&gt;/&lt;sid&gt;.jsonl</code>",
    "<code>&lt;proj&gt;/&lt;sid&gt;/subagents/agent-*.jsonl</code>":
      "<code>&lt;proj&gt;/&lt;sid&gt;/subagents/agent-*.jsonl</code>",
    "<code>&lt;proj&gt;/subagents/&lt;sid&gt;/agent-*.jsonl</code>":
      "<code>&lt;proj&gt;/subagents/&lt;sid&gt;/agent-*.jsonl</code>",
    "<code>&lt;root&gt;/&lt;sid&gt;.jsonl</code>": "<code>&lt;root&gt;/&lt;sid&gt;.jsonl</code>",
    "<code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>":
      "<code>.zip</code>, <code>.tar</code>, <code>.tar.gz</code>, <code>.tgz</code>",
    "<code>any.jsonl.gz</code>": "<code>any.jsonl.gz</code>",
    "<code>CCAM_IMPORT_MAX_BYTES</code>": "<code>CCAM_IMPORT_MAX_BYTES</code>",
    "1 GB": "1 GB",
    "<code>CCAM_IMPORT_MAX_FILES</code>": "<code>CCAM_IMPORT_MAX_FILES</code>",
    "<code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>": "<code>CCAM_IMPORT_MAX_EXTRACT_BYTES</code>",
    "4 GB": "4 GB",
    '<span class="caption-icon">🛰️</span> <span>Settings → Remote Data Sources — add machines by SSH destination, test connectivity, sync on demand or on a background schedule, and narrow the whole app with the global data-scope selector</span>':
      '<span class="caption-icon">🛰️</span> <span>Configuración → Fuentes de datos remotas — agrega máquinas por destino SSH, prueba la conectividad, sincroniza bajo demanda o en un horario en segundo plano, y reduce toda la aplicación con el selector de alcance de datos global</span>',
    "<code>DASHBOARD_REMOTE_SYNC_MS</code>": "<code>DASHBOARD_REMOTE_SYNC_MS</code>",
    "<code>15000</code>": "<code>15000</code>",
    "<code>DASHBOARD_REMOTE_SYNC_TIMEOUT_MS</code>":
      "<code>DASHBOARD_REMOTE_SYNC_TIMEOUT_MS</code>",
    "<code>600000</code>": "<code>600000</code>",
    "<code>DASHBOARD_REMOTE_TEST_TIMEOUT_MS</code>":
      "<code>DASHBOARD_REMOTE_TEST_TIMEOUT_MS</code>",
    "<code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code>":
      "<code>DASHBOARD_REMOTE_ACTIVE_WINDOW_MS</code>",
    "<code>mcp/src/</code>": "<code>mcp/src/</code>",
    "<code>mcp/build/</code>": "<code>mcp/build/</code>",
    "<code>mcp/README.md</code>": "<code>mcp/README.md</code>",
    "<code>mcp/src/transports/</code>": "<code>mcp/src/transports/</code>",
    "<code>mcp/src/ui/</code>": "<code>mcp/src/ui/</code>",
    "<code>npm run mcp:start|start:http|start:repl|dev|dev:http|dev:repl</code>":
      "<code>npm run mcp:start|start:http|start:repl|dev|dev:http|dev:repl</code>",
    "<code>CLAUDE.md</code>, <code>.claude/rules/</code>":
      "<code>CLAUDE.md</code>, <code>.claude/rules/</code>",
    "<code>.claude/skills/</code>": "<code>.claude/habilidades/</code>",
    "<code>.claude/agents/</code>": "<code>.claude/agents/</code>",
    "<code>AGENTS.md</code>, <code>.codex/rules/default.rules</code>":
      "<code>AGENTES.md</code>, <code>.codex/rules/default.rules</code>",
    "<code>.codex/skills/</code>": "<code>.codex/habilidades/</code>",
    "<code>.codex/agents/</code>": "<code>.codex/agents/</code>",
    "<code>scripts/hook-handler.js</code>": "<code>scripts/hook-handler.js</code>",
    "<code>scripts/install-hooks.js</code>": "<code>scripts/install-hooks.js</code>",
    "<code>scripts/import-history.js</code>": "<code>scripts/import-history.js</code>",
    "<code>server/routes/import.js</code>": "<code>server/routes/import.js</code>",
    "<code>server/lib/archive.js</code>": "<code>server/lib/archive.js</code>",
    "<code>scripts/seed.js</code>": "<code>scripts/seed.js</code>",
    "<code>scripts/clear-data.js</code>": "<code>scripts/clear-data.js</code>",
    "<strong>ccam-analytics</strong>": "<strong>ccam-analytics</strong>",
    "session-report, cost-breakdown, usage-trends, productivity-score":
      "Reporte de sesión, desglose de costos, tendencias de uso, puntuación de productividad",
    "analytics-advisor": "Asesor de análisis",
    "<code>ccam-stats</code>": "<code>ccam-stats</code>",
    "<strong>ccam-cost-guard</strong>": "<strong>ccam-cost-guard</strong>",
    "budget-set, spend-forecast, cost-alert, model-savings, daily-budget-check":
      "Establecer presupuesto, pronosticar gastos, alertas de costos, ahorros de modelos, verificación diaria del presupuesto",
    "budget-sentinel": "Sentinela del presupuesto",
    "<strong>ccam-productivity</strong>": "<strong>ccam-productividad</strong>",
    "daily-standup, weekly-report, sprint-summary, workflow-optimizer":
      "Standup diario, informe semanal, resumen de sprint, optimizador de flujo de trabajo",
    "productivity-coach": "Entrenador de productividad",
    "<strong>ccam-devtools</strong>": "<strong>ccam-devtools</strong>",
    "session-debug, hook-diagnostics, data-export, health-check":
      "Depuración de sesiones, diagnóstico de enlaces, exportación de datos, verificación de salud",
    "issue-triager": "Triagista de problemas",
    "<code>ccam-doctor</code>, <code>ccam-export</code>":
      "<code>ccam-doctor</code>, <code>ccam-export</code>",
    "<strong>ccam-insights</strong>": "<strong>insights de ccam</strong>",
    "pattern-detect, anomaly-alert, optimization-suggest, session-compare":
      "Detección de patrones, alerta de anomalías, sugerencias de optimización, comparación de sesiones",
    "insights-advisor": "Asesor de conocimientos",
    "<strong>ccam-sessions</strong>": "<strong>sesiones ccam</strong>",
    "session-search, session-timeline, transcript-replay, cwd-rollup, session-cleanup":
      "Búsqueda de sesiones, cronología de sesiones, reproducción de transcripciones, acumulación de directorio actual, limpieza de sesiones",
    "session-investigator": "Investigador de sesión",
    "<strong>ccam-workflows</strong>": "<strong>ccam-workflows</strong>",
    "dag-map, delegation-audit, concurrency-report, error-propagation, fleet-runs":
      "Mapa dag, auditoría de delegación, informe de concurrencia, propagación de errores, ejecuciones de flota",
    "orchestration-analyst": "Analista de orquestación",
    "<strong>ccam-quality</strong>": "<strong>Calidad ccam</strong>",
    "error-scan, api-error-report, hook-failure-audit, slo-check, regression-alert":
      "Escaneo de errores, informe de errores API, auditoría de fallos de enlaces, verificación de SLA, alerta de regresión",
    "reliability-engineer": "Ingeniero de fiabilidad",
    "<strong>ccam-config</strong>": "<strong>ccam-config</strong>",
    "config-audit, memory-review, skill-inventory, mcp-audit, hook-inventory":
      "Auditoría de configuración, revisión de memoria, inventario de habilidades, auditoría de mcp, inventario de ganchos",
    "config-auditor": "Auditor de configuración",
    "<strong>ccam-dashboard</strong>": "<strong>Panel de control ccam</strong>",
    "dashboard-status, quick-stats": "Estado del panel de control, estadísticas rápidas",
    "<code>thinking_blocks</code>, <code>turn_count</code>, <code>total_turn_duration_ms</code>, <code>usage_extras</code> (service_tier, speed, inference_geo)":
      "<code>thinking_blocks</code>, <code>turn_count</code>, <code>total_turn_duration_ms</code>, <code>usage_extras</code> (tier_de_servicio, velocidad, inference_geo)",
    "PreToolUse, PostToolUse, Stop, SubagentStop, SessionStart, SessionEnd, Notification, Compaction, APIError, TurnDuration":
      "PreToolUse, PostToolUse, Stop, SubagentStop, SessionStart, SessionEnd, Notification, Compaction, APIError, TurnDuration",
    Model: "modelo",
    "<code>data.model.display_name</code>": "<code>data.model.display_name</code>",
    User: "usuario",
    "<code>$USERNAME</code> / <code>$USER</code>": "<code>$USERNAME</code> / <code>$USER</code>",
    "<code>data.workspace.current_dir</code>": "<code>data.workspace.current_dir</code>",
    "<code>git symbolic-ref --short HEAD</code>": "<code>git symbolic-ref --short HEAD</code>",
    "<code>data.context_window.used_percentage</code>":
      "<code>data.context_window.used_percentage</code>",
    "<code>data.context_window.current_usage</code>":
      "<code>data.context_window.current_usage</code>",
    "<code>data.cost.total_cost_usd</code>": "<code>data.cost.total_cost_usd</code>",
    '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — the <code>/cc-config</code> page: 12 tabs (skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, keybindings, settings, memory), a scope filter (user / project / all), search, and safe edit affordances on the low-risk surfaces</span>':
      '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — la página <code>/cc-config</code>: 12 pestañas (habilidades, subagentes, comandos de guión, estilos de salida, complementos, mercados, servidores MCP, ganchos, asociaciones de teclas, configuraciones, memoria), un filtro de alcance (usuario / proyecto / todo), búsqueda y posibilidades de edición segura en superficies de bajo riesgo</span>',
    '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Skills</strong> — the Skills tab lists every discovered skill (user, project, and plugin) with description and source, is searchable, and opens any <code>SKILL.md</code> for a timestamp-backed edit via the shared editor modal</span>':
      '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Habilidades</strong> — la pestaña Habilidades enumera todas las habilidades descubiertas (usuario, proyecto y complemento) con descripción y fuente, es buscable y abre cualquier <code>SKILL.md</code> para una edición respaldada por una hora y fecha a través del modal del editor compartido</span>',
    "A first-class <strong>configuration inspector</strong> at <code>/cc-config</code> that reads every surface Claude Code knows about — skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts — from the same on-disk layout the CLI uses. The Overview tab summarizes counts and filesystem roots; every other tab drills into one surface with search, scope filtering (<b>user</b> / <b>project</b> / <b>all</b>), and — where safe — create / edit / delete with <strong>mandatory timestamped backups</strong> before every mutation. A <code>cc-watcher</code> on <code>CLAUDE_HOME</code> plus <code>cc_config_changed</code> WebSocket messages keep the page live when files change outside the dashboard.":
      "Un <strong>inspector de configuración</strong> de primera clase en <code>/cc-config</code> que lee todas las superficies sobre las que Claude Code tiene conocimiento, como habilidades, subagentes, comandos de guión, estilos de salida, plugins, mercados, servidores MCP, ganchos, configuraciones, memoria, asociaciones de teclas y scripts de línea de estado, desde el mismo diseño en disco que utiliza la CLI. La pestaña Resumen resume los conteos y las raíces del sistema de archivos; cada otra pestaña profundiza en una superficie con búsqueda, filtrado de alcance (<b>usuario</b> / <b>proyecto</b> / <b>todo</b>) y, cuando sea seguro, crea / edita / elimina con <strong>copias de seguridad obligatorias con hora y fecha</strong> antes de cada mutación. Un <code>cc-watcher</code> en <code>CLAUDE_HOME</code> más mensajes WebSocket de <code>cc_config_changed</code> mantienen la página activa cuando los archivos cambian fuera del panel de control.",
    "<strong>Skills</strong>, <strong>Subagents</strong>, <strong>Slash commands</strong>, <strong>Output styles</strong>, <strong>Plugins</strong> (with per-plugin <code>contributes</code> counts and <code>plugin.json</code> metadata), <strong>Marketplaces</strong>, <strong>MCP servers</strong>, <strong>Hooks</strong> (plus <code>~/.claude/hooks/</code> script listing), <strong>Keybindings</strong>, <strong>Settings</strong> (Current configuration summary + per-file JSON), and <strong>Memory</strong> (user/project <code>CLAUDE.md</code> + per-project auto-memory). Statusline config and script previews live under Settings.":
      "<strong>Habilidades</strong>, <strong>Subagentes</strong>, <strong>Comandos de corte</strong>, <strong>Estilos de salida</strong>, <strong>Complementos</strong> (con contadores de <code>contributes</code> por complemento y metadatos de <code>plugin.json</code>), <strong>Mercados</strong>, <strong>Servidores MCP</strong>, <strong>Ganchos</strong> (además de la lista de scripts <code>~/.claude/hooks/</code>), <strong>Asignaciones de teclas</strong>, <strong>Configuración</strong> (resumen de la configuración actual + JSON por archivo) y <strong>Memoria</strong> (<code>CLAUDE.md</code> del usuario/proyecto + memoria automática por proyecto). La configuración de la línea de estado y las vistas previas de los scripts están disponibles en Configuración.",
    '<code>PUT</code> / <code>DELETE /api/cc-config/file</code> handles skills, agents, commands, output-styles, <code>CLAUDE.md</code>, and per-project auto-memory (<code>scope: "auto-memory"</code>). Writes are <strong>atomic</strong> (temp file + rename), names are validated against a strict regex, resolved paths are containment-checked, and content is capped at 256&nbsp;KiB. Every overwrite or delete creates a backup under <code>cc-config-backups/</code> — outside the directories Claude Code scans — so deleted artifacts cannot reappear as ghost entries.':
      '<code>PUT</code> / <code>DELETE /api/cc-config/file</code> maneja habilidades, agentes, comandos, estilos de salida, <code>CLAUDE.md</code> y memoria automática por proyecto (<code>scope: "auto-memory"</code>). Las escrituras son <strong>atómicas</strong> (archivo temporal + renombrar), los nombres se validan contra una regla de expresión regular estricta, los rutas resueltas se verifican contra el contenedor y el contenido se limita a 256&nbsp;KiB. Cada sobrescritura o eliminación crea una copia de seguridad en <code>cc-config-backups/</code> — fuera de los directorios que Claude Code escanea — por lo que los artefactos eliminados no pueden reaparecer como entradas fantasma.',
    "The Keybindings tab includes a structured inline editor for <code>~/.claude/keybindings.json</code>. Add or remove contexts and bindings, edit keys and actions in place, and save via <code>PUT /api/cc-config/keybindings</code>. The server read-modify-writes the file: top-level metadata (<code>$schema</code>, <code>$docs</code>) is preserved, duplicate contexts/keys are rejected, and the file is backed up first. Unlike <code>settings.json</code>, the live CLI does not rewrite keybindings mid-session, so dashboard edits are safe.":
      "La pestaña de Asignaciones de teclas incluye un editor estructurado inline para <code>~/.claude/keybindings.json</code>. Agregue o elimine contextos y asignaciones, edite teclas y acciones en el lugar, y guarde a través de <code>PUT /api/cc-config/keybindings</code>. El servidor lee, modifica y escribe el archivo: se preservan los metadatos de nivel superior (<code>$schema</code>, <code>$docs</code>), se rechazan los contextos/teclas duplicados y el archivo se copia de seguridad primero. A diferencia de <code>settings.json</code>, la CLI en vivo no reescribe las asignaciones de teclas en medio de la sesión, por lo que las ediciones del panel de control son seguras.",
    "The Memory tab surfaces user + project <code>CLAUDE.md</code> and the per-project file-based memory store — every <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+). Files are grouped by project in collapsible sections, searchable, and editable. Clickable index links in <code>MEMORY.md</code> jump to (scroll + highlight) the matching fact file.":
      "La pestaña Memoria muestra el usuario + proyecto <code>CLAUDE.md</code> y el almacenamiento de memoria basado en archivos por proyecto, todos los <code>*.md</code> bajo <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (un índice <code>MEMORY.md</code> más un archivo por hecho recordado, a menudo más de 100). Los archivos se agrupan por proyecto en secciones colapsables, buscables y editable. Los enlaces de índice clicables en <code>MEMORY.md</code> saltan al archivo de hecho correspondiente (desplazamiento + resaltado) para buscar.",
    "<strong>Plugins</strong>, <strong>MCP servers</strong>, <strong>hooks-in-settings</strong>, and live <strong>settings.json</strong> files are intentionally not writable from the dashboard — they race with the running <code>claude</code> CLI. Those tabs show explainer banners with copy-able <code>claude plugin …</code>, <code>claude mcp …</code>, and <code>claude config …</code> commands instead of edit buttons. Secret-like settings keys are redacted server-side.":
      "Los <strong>complementos</strong>, los <strong>servidores MCP</strong>, los <strong>ganchos configurados</strong> y los archivos <strong>settings.json</strong> en vivo no se pueden editar intencionadamente desde el panel de control; compiten con la CLI <code>claude</code> en ejecución. Esas pestañas muestran banners explicativos con comandos <code>claude plugin …</code>, <code>claude mcp …</code> y <code>claude config …</code> que se pueden copiar en lugar de botones de edición. Las claves de configuración similares a secretos se ocultan en el servidor.",
    "Successful mutations broadcast <code>cc_config_changed</code> over the WebSocket so every open <code>/cc-config</code> tab refetches without polling. A filesystem watcher on <code>CLAUDE_HOME</code> (<code>cc-watcher.js</code>) detects external edits (e.g. you changed a skill in your editor) and triggers the same refresh path. The Backups modal lists every timestamped copy and builds <code>mv</code> restore commands.":
      "Las mutaciones exitosas transmiten <code>cc_config_changed</code> a través de WebSocket, por lo que cada pestaña <code>/cc-config</code> abierta se actualiza de nuevo sin consultar. Un observador de sistema de archivos en <code>CLAUDE_HOME</code> (<code>cc-watcher.js</code>) detecta ediciones externas (por ejemplo, has cambiado una habilidad en tu editor) y activa el mismo camino de actualización. La modalidad Copias de seguridad enumera todas las copias con fecha y hora y construye comandos de restauración <code>mv</code>.",
    "How to change (if read-only)": "Cómo cambiar (si es de solo lectura)",
    "Skills · Subagents · Commands · Output styles":
      "Habilidades · Subagentes · Comandos · Estilos de salida",
    "Create / edit / delete (<code>PUT</code>/<code>DELETE /file</code>)":
      "Crear / editar / eliminar (<code>PUT</code>/<code>DELETE /file</code>)",
    "<code>CLAUDE.md</code> · auto-memory <code>*.md</code>":
      "<code>CLAUDE.md</code> · memoria automática <code>*.md</code>",
    "<code>keybindings.json</code>": "<code>keybindings.json</code>",
    "Structured inline editor (<code>PUT /keybindings</code>)":
      "Editor inline estructurado (<code>PUT /keybindings</code>)",
    "Plugins · marketplaces": "Plugins · mercados",
    "Read-only inventory": "Inventario de lectura única",
    "<code>claude plugin install|uninstall …</code>":
      "<code>instalar o desinstalar el complemento claudie...</code>",
    "MCP servers": "Servidores MCP",
    "<code>claude mcp add|remove …</code>": "<code>claude mcp agregar|eliminar ...</code>",
    "Hooks · settings.json": "Ganchos · settings.json",
    "Read-only (redacted secrets)": "Solo de lectura (secretos redactados)",
    "<code>claude config</code> · <code>/config</code> · <code>$EDITOR</code>":
      "<code>claude config</code> · <code>/config</code> · <code>$EDITOR</code>",
    "Filesystem roots + aggregate counts across every surface":
      "Raíces del sistema de archivos + contadores agregados en todas las superficies",
    "Per-tab discovery lists; most accept <code>?scope=user|project|all</code>":
      "Listas de descubrimiento por tabla; la mayoría acepta <code>?scope=user|project|all</code>",
    "Single-file viewer (path-contained to allowed roots)":
      "Visualizador de archivo único (ruta contenida en las raíces permitidas)",
    "Create/overwrite a text artifact — backs up first, atomic write":
      "Crear/sobreescribir un artefacto de texto: haga una copia de seguridad primero, escritura atómica",
    "Backup-then-delete (skill dirs backed up whole)":
      "Hacer una copia de seguridad y luego eliminar (copias de seguridad completas de las carpetas de habilidades)",
    "Structured overwrite of <code>keybindings.json</code> — preserves metadata, backs up first":
      "Sobrascritura estructurada de <code>keybindings.json</code> — preserva los metadatos, hace una copia de seguridad primero",
    "Timestamped backup listing for the recovery modal":
      "Lista de copias de seguridad con hora y fecha para la modalidad de recuperación",
    "Backups live outside Claude Code's scan paths":
      "Las copias de seguridad están en vivo fuera de los caminos de escaneo de Claude Code",
    "Every mutation writes a timestamped copy under <code>&lt;root&gt;/cc-config-backups/&lt;type&gt;/</code> (or <code>&lt;memory-dir&gt;/.cc-config-backups/auto-memory/</code> for per-project memory files) <em>before</em> touching the original. Skill folders are copied whole so bundled assets survive in the backup. The UI's Backups modal shows every copy and prints a one-line <code>mv</code> restore command.":
      "Cada mutación escribe una copia con fecha y hora bajo <code>&lt;root&gt;/cc-config-backups/&lt;type&gt;/</code> (o <code>&lt;memory-dir&gt;/.cc-config-backups/auto-memory/</code> para archivos de memoria por proyecto) <em>antes</em> de tocar el original. Las carpetas de habilidades se copian enteras, por lo que los activos empaquetados sobreviven en la copia de seguridad. La modalidad Copias de seguridad de la interfaz de usuario muestra cada copia e imprime un comando de restauración <code>mv</code> de una línea.",
    "Do not edit settings.json from here": "No edites settings.json desde aquí",
    "The live <code>claude</code> process rewrites <code>settings.json</code> and <code>~/.claude.json</code> constantly (model changes, plugin toggles, MCP registration). A dashboard write would race and lose state — or worse, break hook ingestion. Use <code>claude config set …</code>, <code>/config</code>, or hand-edit only when no <code>claude</code> process is running.":
      "El proceso <code>claude</code> en vivo reescribe constantemente <code>settings.json</code> y <code>~/.claude.json</code> (cambios de modelo, activación y desactivación de complementos, registro de MCP). Una escritura en el panel de control podría competir y perder el estado, o peor aún, romper la ingestión de ganchos. Utilice <code>claude config set …</code>, <code>/config</code> o edite manualmente solo cuando no esté ejecutando ningún proceso <code>claude</code>.",
    Slack: "ausencia de tensión",
    Discord: "discordancia",
    "Microsoft Teams": "Microsoft Teams",
    "Google Chat": "Chat de Google",
    "Mattermost · Rocket.Chat": "Mattermost · Rocket.Chat",
    Telegram: "telegrama",
    PagerDuty: "PagerDuty",
    Opsgenie: "Opsgenie",
    "Splunk On-Call": "Splunk en servicio de emergencia",
    "Zapier · Make · n8n · Pipedream · generic": "Zapier · Make · n8n · Pipedream · genérico",
    "<code>GET /api/updates/status</code>": "<code>GET /api/updates/status</code>",
    "<code>POST /api/updates/check</code>": "<code>POST /api/updates/check</code>",
    "<code>http://localhost:5173</code>": "<code>http://localhost:5173</code>",
    "<code>http://localhost:4820</code>": "<code>http://localhost:4820</code>",
    "<code>~/.claude:/root/.claude:ro</code>": "<code>~/.claude:/root/.claude:ro</code>",
    "<code>agent-monitor-data:/app/data</code>": "<code>agent-monitor-data:/app/data</code>",
    "<strong>server-deps</strong>": "<strong>dependencias del servidor</strong>",
    "<strong>client-build</strong>": "<strong>construcción del cliente</strong>",
    "<strong>runtime</strong>": "<strong>tiempo de ejecución</strong>",
    "&lt; 200ms": "< 200 ms",
    "&lt; 50ms": "< 50 ms",
    "200 KB / 63 KB gzip": "200 KB / 63 KB gzip",
    "CSS: 17 KB / 4 KB gzip": "CSS: 17 KB / 4 KB gzip",
    "&lt; 5ms": "&lt; 5 ms",
    "~50,000 inserts/sec": "~50.000 inserciones/seg.",
    "~1M rows": "~1M filas",
    "~30 MB": "~30 MB",
    "~15 MB": "~15 MB",
    Area: "área",
    Approach: "planteamiento",
    "<strong>SQLite</strong> (better-sqlite3 / node:sqlite)":
      "<strong>SQLite</strong> (better-sqlite3 / node:sqlite)",
    "<strong>Express</strong>": "<strong>Express</strong>",
    "<strong>ws</strong>": "<strong>ws</strong>",
    "<strong>React 19</strong>": "<strong>React 19</strong>",
    "<strong>Vite 7</strong>": "<strong>Vite 7</strong>",
    "<strong>Tailwind CSS</strong>": "<strong>Tailwind CSS</strong>",
    "<strong>React Router 8</strong>": "<strong>React Router 8</strong>",
    "<strong>Lucide React</strong>": "<strong>Lucide React</strong>",
    "<strong>TypeScript Strict</strong>": "<strong>TypeScript estricto</strong>",
    "<strong>D3.js + d3-sankey</strong>": "<strong>D3.js + d3-sankey</strong>",
    "<strong>Python (statusline)</strong>": "<strong>Python (statusline)</strong>",
    README: "README",
  },
  plain: {
    es: {
      "Run Agent": "Ejecutar agente",
      "Agent Config": "Configuración de agentes",
      "ccam CLI": "CLI de ccam",
      "12 discovery tabs": "12 pestañas de descubrimiento",
      "Safe mutations": "Modificaciones seguras",
      "Keybindings editor": "Editor de combinaciones de teclas",
      "Memory store": "Almacén de memoria",
      "Read-only by design": "Solo lectura por diseño",
      "Live refresh": "Actualización en tiempo real",
      "Editable vs read-only": "Editable frente a solo lectura",
      "API endpoints": "Puntos finales de la API",
      "Four Grafana boards": "Cuatro paneles de Grafana",
      "Prometheus console": "Consola de Prometheus",
      "Recording rules": "Reglas de registro",
      "Scrape & auth guard": "Extracción y protección de autenticación",
      "Remote Data Sources": "Fuentes de datos remotas",
      "How It Works": "Cómo funciona",
      Setup: "configuración",
      "Data Scope": "Ámbito de datos",
      "Live Status": "Estado en vivo",
      "Security Model": "Modelo de seguridad",
      "API Endpoints": "Puntos finales de API",
      Metrics: "métrica",
      "WebSocket Events": "Eventos WebSocket",
      CLI: "CLI",
      "System Overview": "Visión general del sistema",
      "What's Included": "Qué está incluido",
      "Live Dashboard": "Panel de control en vivo",
      "Kanban Board": "Tablero Kanban",
      "Sessions Table": "Tabla de sesiones",
      "Session Detail": "Detalles de la sesión",
      "Alerts & Webhooks": "Alertas y Webhooks",
      "Desktop App (macOS & Windows)": "Aplicación para escritorio (macOS y Windows)",
      "Activity Feed": "Feed de actividad",
      Analytics: "análisis",
      "WebSocket Push": "Push WebSocket",
      Statusline: "Línea de estado",
      "History Import": "Importación de historial",
      "Transcript Cache": "Cache de transcripciones",
      "Bounded Cache Memory": "Memoria de caché limitada",
      "Constant-Time Sweep": "Recorrido de tiempo constante",
      "Subagent Hierarchy": "Jerarquía de subagentes",
      "Cost Tracking": "Seguimiento de costos",
      "Settings & Management": "Configuración y gestión",
      "Local MCP Server": "Servidor MCP local",
      "Claude + Codex Extensions": "Extensión de Claude + Codex",
      "Workflow Graphs": "Gráficos de flujo de trabajo",
      "Workflow Analytics": "Análisis de flujo de trabajo",
      "Session Drill-In": "Entrenamiento de la sesión",
      "Browser Notifications": "Notificaciones del navegador",
      "Docker Deployment": "Implementación de Docker",
      "Plugin Marketplace": "Mercado de complementos",
      "Run Claude": "Corra Claude",
      "Claude Config Explorer": "Explorador de configuración de Claude",
      "Responsive Design": "Diseño Responsivo",
      "Concurrency Timeline": "Cronología de concurrencia",
      "VS Code Extension": "Extensión de VS Code",
      "Error Propagation Map": "Mapa de propagación de errores",
      "Progressive Web App": "Aplicación web progresiva",
      "Fresh-by-Default Caching": "Caching fresco por defecto",
      "Auto-Reload on Update": "Recarga automática en la actualización",
      Screenshots: "Capturas de pantalla",
      "GitHub Star History": "Historial de estrellas de GitHub",
      "Hook Events Captured": "Eventos de captura de gancho",
      "Quick Start": "Inicio rápido",
      Clone: "clon",
      Install: "instalar",
      Start: "inicio",
      "Use Claude": "Utiliza Claude",
      "Alternative: Docker / Podman": "Alternativa: Docker / Podman",
      "Optional: Enable MCP and Agent Extensions":
        "Opcional: Habilitar las extensiones MCP y Agent",
      Verification: "constatación",
      Configuration: "configuración",
      "Environment Variables": "Variables del entorno",
      "Hook Configuration": "Configuración del gancho",
      "Scripts Reference": "Referencia de scripts",
      "System Architecture": "Arquitectura del sistema",
      "Agent State Machine": "Máquina de Estado del Agente",
      "Session State Machine": "Máquina de estado de sesión",
      "Data Flow": "Flujo de datos",
      "Event Ingestion Pipeline": "Pipeline de ingestión de eventos",
      "Client Data Loading Pattern": "Patrón de carga de datos del cliente",
      "Server Architecture": "Arquitectura del servidor",
      "Server Modules": "Módulos del servidor",
      "Client Architecture": "Arquitectura del cliente",
      "Client Routes": "Rutas del cliente",
      "Key Client Modules": "Módulos clave del cliente",
      "PWA & Service Worker": "PWA y trabajador de servicio",
      "State Management": "Gestión estatal",
      "Database Design": "Diseño de bases de datos",
      Indexes: "Índices",
      "SQLite Configuration": "Configuración de SQLite",
      "API Reference": "Referencia de API",
      Health: "bienestar",
      Sessions: "sesión",
      Agents: "Agentes",
      "Events, Stats, Analytics": "Eventos, estadísticas, análisis",
      "Hooks Ingestion": "Ingestión de ganchos",
      Pricing: "fijación de precios",
      Settings: "Configuración",
      "Import History": "Historial de importaciones",
      Workflows: "Flujos de trabajo",
      Alerts: "Alertas",
      Webhooks: "Webhooks",
      "WebSocket Protocol": "Protocolo WebSocket",
      "Message Envelope": "Envelope de mensaje",
      "Hook Integration": "Integración de ganchos",
      "Hook Handler Design": "Diseño del manipulador de ganchos",
      "Hook Installation Flow": "Flujo de instalación del gancho",
      "Import Pipeline": "Pipeline de importación",
      "Three Modes, One Pipeline": "Tres modos, un solo flujo",
      "Upload Request Sequence": "Secuencia de solicitud de carga de archivos",
      "Idempotence & Cost Accuracy": "Idempotencia y precisión de costos",
      "Supported Source Layouts": "Diseños de fuentes compatibles",
      "Safety Model": "Modelo de seguridad",
      "WebSocket Progress Events": "Eventos de progreso de WebSocket",
      "MCP & Agent Extensions": "Extensión de MCP y agente",
      "Local MCP Server Runtime": "Tiempo de ejecución del servidor MCP local",
      "Agent Extension Layout": "Diseño de extensión del agente",
      "Root Helper Scripts": "Scripts de ayuda para root",
      Installation: "instalación",
      "Available Plugins": "Plugins disponibles",
      "Skill Usage Examples": "Ejemplos de uso de habilidades",
      "CLI Tools": "Herramientas CLI",
      "Plugin Architecture": "Arquitectura de plugins",
      "Data Model Reference": "Referencia del modelo de datos",
      "Statusline Utility": "Herramienta de línea de estado",
      "Detailed Components": "Componentes detallados",
      "In-Process Architecture": "Arquitectura en proceso",
      "What It Adds": "Lo que agrega",
      "Menu-Bar / Notification-Area (Tray) Icon":
        "Icono de Barra de Menú / Área de Notificaciones ( bandeja)",
      "Native Application Menu": "Menú de aplicaciones nativas",
      "Auto-Start at Login": "Inicio automático al iniciar sesión",
      "Close Hides, Server Stays Up": "Cerrar Oculta, el Servidor Se Mantiene Activado",
      "Runs Alongside the Web Dashboard": "Funciona junto al panel de control web",
      "Single-Instance Lock": "Bloqueo de instancia única",
      "First-Boot Bootstrap": "Bootstrap de arranque inicial",
      "Data Persistence & CLI Reliability": "Persistencia de datos y fiabilidad de la CLI",
      "Port Discovery": "Descubrimiento del puerto",
      "How to Get It": "Cómo conseguirlo",
      "Option A — download the latest GitHub Release (recommended)":
        "Opción A: descargar la última versión de GitHub (recomendada)",
      "Option B — per-commit CI artifact": "Opción B: artefacto CI por compromiso",
      "Option C — build locally": "Opción C: construir localmente",
      macOS: "macOS",
      "Open the DMG": "Abrir el DMG",
      "Drag to Applications": "Arrastrar a Aplicaciones",
      "Clear Quarantine": "Liberación de cuarentena",
      Launch: "lanzamiento",
      Windows: "ventana",
      "Run the Installer": "Ejecutar el instalador",
      "Clear SmartScreen": "Borrar SmartScreen",
      "Settings Page": "Página de ajustes",
      "Model Pricing": "Precio del modelo",
      "Data Management": "gestión de datos",
      "Data Export": "Exportación de datos",
      "System Health": "Estado de salud del sistema",
      "Notification Preferences": "Preferencias de notificaciones",
      "Rule types": "Tipos de reglas",
      "Evaluation engine": "Motor de evaluación",
      "14 first-class providers": "14 proveedores de primera clase",
      "Delivery engine": "Motor de entrega",
      Security: "seguridad",
      "Guided setup": "Configuración guiada",
      "Provider payloads": "Carga útil del proveedor",
      "Update Notifier": "Notificador de actualización",
      "Non-Blocking Detection": "Detección no bloqueante",
      "5-min Scheduler": "Programador de 5 minutos",
      "Situation-Aware Command": "Comando con conciencia de la situación",
      "Two UI Surfaces": "Dos superficies de interfaz de usuario",
      "Soft Failure Semantics": "Semántica de fallos suaves",
      "Dismissal Memory": "Memoria de despido",
      "API Surface": "Superficie de la API",
      "Connection Status": "Estado de la conexión",
      "Internationalization (i18n)": "Internacionalización (i18n)",
      "Namespaced resources": "Recursos con espacio de nombres",
      "Detection & fallback": "Detección y fallback",
      "Locale-aware formatting": "Formato con conciencia del lugar",
      "Technical terms preserved": "Términos técnicos preservados",
      "🐾 Tabby — Reactive Cat Companion": "🐾 Tabby — Compañero de gato reactivo",
      "Reactive Mascot — Eight Moods": "Mascota reactiva — Ocho estados de ánimo",
      "Auto-Surface Speech Bubbles": "Bocadillos automáticos",
      "The ⌘B Panel": "El panel ⌘B",
      "Ask → Run Claude Handoff": "Transición de Ask a Run Claude",
      "Accessibility & Resilience": "Accesibilidad y resiliencia",
      "Deployment Modes": "Modos de despliegue",
      "Container Runtime (Docker / Podman)": "Tiempo de ejecución del contenedor (Docker / Podman)",
      "Docker / Podman": "Docker / Podman",
      "Plain Docker / Podman (no Compose)": "Docker / Podman simple (sin Compose)",
      "Volume Mounts": "Montajes de volumen",
      "Multi-Stage Build": "Construcción de múltiples etapas",
      "Performance Characteristics": "Características del rendimiento",
      Troubleshooting: "localización y corrección de fallos",
      "No sessions appearing after starting Claude Code":
        "No aparecen sesiones después de iniciar Claude Code",
      "Check 1 — Is the server running?": "Verificación 1: ¿Está funcionando el servidor?",
      "Check 2 — Are hooks installed?": "Verificación 2: ¿Se han instalado los ganchos?",
      "Check 3 — Start a new Claude Code session":
        "Verificar 3 - Iniciar una nueva sesión de Claude Code",
      "Check 4 — Is Node.js in PATH?": "Verificar 4: ¿Está Node.js en PATH?",
      "Common Issues": "Problemas comunes",
      "Technology Choices": "Opciones tecnológicas",
      "Workflow Runs": "Ejecuciones del flujo de trabajo",
    },
    zh: {
      "Run Agent": "运行 Agent",
      "Agent Config": "Agent 配置",
      "Remote Data Sources": "远程数据源",
      "How It Works": "工作原理",
      Setup: "设置",
      "Data Scope": "数据范围",
      "Live Status": "实时状态",
      "Security Model": "安全模型",
      "API Endpoints": "API 端点",
      Metrics: "指标",
      "WebSocket Events": "WebSocket 事件",
      CLI: "CLI",
      "System Overview": "系统概览",
      "What's Included": "包含哪些内容",
      "Live Dashboard": "实时仪表盘",
      "Kanban Board": "看板",
      "Sessions Table": "会话表格",
      "Session Detail": "会话详情",
      "Alerts & Webhooks": "告警与 Webhook",
      "Desktop App (macOS & Windows)": "桌面应用（macOS 和 Windows）",
      "Activity Feed": "活动流",
      Analytics: "分析",
      "WebSocket Push": "WebSocket 推送",
      Statusline: "状态栏",
      "History Import": "历史导入",
      "Transcript Cache": "Transcript 缓存",
      "Bounded Cache Memory": "有界的缓存内存",
      "Constant-Time Sweep": "常数时间扫描",
      "Subagent Hierarchy": "子智能体层级",
      "Cost Tracking": "成本跟踪",
      "Settings & Management": "设置与管理",
      "Local MCP Server": "本地 MCP 服务器",
      "Claude + Codex Extensions": "Claude + Codex 扩展",
      "Workflow Graphs": "工作流图",
      "Workflow Analytics": "工作流分析",
      "Session Drill-In": "会话深入",
      "Browser Notifications": "浏览器通知",
      "Docker Deployment": "Docker 部署",
      "Plugin Marketplace": "插件市场",
      "Run Claude": "运行 Claude",
      "Claude Config Explorer": "Claude 配置浏览器",
      "Responsive Design": "响应式设计",
      "Concurrency Timeline": "并发时间线",
      "VS Code Extension": "VS Code 扩展",
      "Error Propagation Map": "错误传播图",
      "Progressive Web App": "渐进式 Web 应用",
      "Fresh-by-Default Caching": "默认保持最新的缓存",
      "Auto-Reload on Update": "更新时自动重新加载",
      Screenshots: "屏幕截图",
      "GitHub Star History": "GitHub Star 历史",
      "Hook Events Captured": "捕获的 Hook 事件",
      "Quick Start": "快速开始",
      Clone: "克隆",
      Install: "安装",
      Start: "启动",
      "Use Claude": "使用 Claude",
      "Alternative: Docker / Podman": "备选方案：Docker / Podman",
      "Optional: Enable MCP and Agent Extensions": "可选：启用 MCP 与 Agent 扩展",
      Verification: "验证",
      Configuration: "配置",
      "Environment Variables": "环境变量",
      "Hook Configuration": "Hook 配置",
      "Scripts Reference": "脚本参考",
      "System Architecture": "系统架构",
      "Agent State Machine": "Agent 状态机",
      "Session State Machine": "会话状态机",
      "Data Flow": "数据流",
      "Event Ingestion Pipeline": "事件摄取流水线",
      "Client Data Loading Pattern": "客户端数据加载模式",
      "Server Architecture": "服务器架构",
      "Server Modules": "服务器模块",
      "Client Architecture": "客户端架构",
      "Client Routes": "客户端路由",
      "Key Client Modules": "关键客户端模块",
      "PWA & Service Worker": "PWA 与 Service Worker",
      "State Management": "状态管理",
      "Database Design": "数据库设计",
      Indexes: "索引",
      "SQLite Configuration": "SQLite 配置",
      "API Reference": "API 参考",
      Health: "健康检查",
      Sessions: "会话",
      Agents: "代理",
      "Events, Stats, Analytics": "事件、统计、分析",
      "Hooks Ingestion": "Hook 摄取",
      Pricing: "定价",
      Settings: "设置",
      "Import History": "导入历史",
      Workflows: "工作流",
      Alerts: "告警",
      Webhooks: "Webhooks",
      "WebSocket Protocol": "WebSocket 协议",
      "Message Envelope": "消息封装",
      "Hook Integration": "Hook 集成",
      "Hook Handler Design": "Hook 处理器设计",
      "Hook Installation Flow": "Hook 安装流程",
      "Import Pipeline": "导入管道",
      "Three Modes, One Pipeline": "三种模式，一条流水线",
      "Upload Request Sequence": "上传请求序列",
      "Idempotence & Cost Accuracy": "幂等性与成本准确性",
      "Supported Source Layouts": "支持的源布局",
      "Safety Model": "安全模型",
      "WebSocket Progress Events": "WebSocket 进度事件",
      "MCP & Agent Extensions": "MCP 与智能体扩展",
      "Local MCP Server Runtime": "本地 MCP 服务器运行时",
      "Agent Extension Layout": "智能体扩展布局",
      "Root Helper Scripts": "根目录辅助脚本",
      Installation: "安装",
      "Available Plugins": "可用插件",
      "Skill Usage Examples": "技能使用示例",
      "CLI Tools": "CLI 工具",
      "Plugin Architecture": "插件架构",
      "Data Model Reference": "数据模型参考",
      "Statusline Utility": "状态栏工具",
      "Detailed Components": "详细组件",
      "In-Process Architecture": "进程内架构",
      "What It Adds": "它带来了什么",
      "Menu-Bar / Notification-Area (Tray) Icon": "菜单栏 / 通知区域（托盘）图标",
      "Native Application Menu": "原生应用菜单",
      "Auto-Start at Login": "登录时自动启动",
      "Close Hides, Server Stays Up": "关闭即隐藏，服务器保持运行",
      "Runs Alongside the Web Dashboard": "与 Web 仪表盘并行运行",
      "Single-Instance Lock": "单实例锁",
      "First-Boot Bootstrap": "首次启动引导",
      "Data Persistence & CLI Reliability": "数据持久化与 CLI 可靠性",
      "Port Discovery": "端口发现",
      "How to Get It": "如何获取",
      "Option A — download the latest GitHub Release (recommended)":
        "选项 A — 下载最新的 GitHub Release（推荐）",
      "Option B — per-commit CI artifact": "选项 B——按提交的 CI 制品",
      "Option C — build locally": "选项 C——本地构建",
      macOS: "macOS",
      "Open the DMG": "打开 DMG",
      "Drag to Applications": "拖入 Applications",
      "Clear Quarantine": "清除隔离属性",
      Launch: "启动",
      Windows: "Windows",
      "Run the Installer": "运行安装程序",
      "Clear SmartScreen": "清除 SmartScreen 拦截",
      "Settings Page": "设置页面",
      "Model Pricing": "模型定价",
      "Data Management": "数据管理",
      "Data Export": "数据导出",
      "System Health": "系统健康",
      "Notification Preferences": "通知偏好",
      "Rule types": "规则类型",
      "Evaluation engine": "评估引擎",
      "14 first-class providers": "14 个一流提供方",
      "Delivery engine": "投递引擎",
      Security: "安全",
      "Guided setup": "引导式设置",
      "Provider payloads": "提供方载荷",
      "Update Notifier": "Update Notifier",
      "Non-Blocking Detection": "非阻塞检测",
      "5-min Scheduler": "5 分钟调度器",
      "Situation-Aware Command": "情境感知命令",
      "Two UI Surfaces": "两个 UI 界面",
      "Soft Failure Semantics": "软失败语义",
      "Dismissal Memory": "忽略记忆",
      "API Surface": "API 接口",
      "Connection Status": "Connection Status",
      "Internationalization (i18n)": "国际化（i18n）",
      "Namespaced resources": "命名空间化的资源",
      "Detection & fallback": "检测与回退",
      "Locale-aware formatting": "区域感知的格式化",
      "Technical terms preserved": "保留技术术语",
      "🐾 Tabby — Reactive Cat Companion": "🐾 Tabby — 响应式猫咪伙伴",
      "Reactive Mascot — Eight Moods": "响应式吉祥物——八种心情",
      "Auto-Surface Speech Bubbles": "自动弹出对话气泡",
      "The ⌘B Panel": "⌘B 面板",
      "Ask → Run Claude Handoff": "Ask → Run Claude 移交",
      "Accessibility & Resilience": "无障碍与韧性",
      "Deployment Modes": "部署模式",
      "Container Runtime (Docker / Podman)": "容器运行时（Docker / Podman）",
      "Docker / Podman": "Docker / Podman",
      "Plain Docker / Podman (no Compose)": "纯 Docker / Podman（不使用 Compose）",
      "Volume Mounts": "卷挂载",
      "Multi-Stage Build": "多阶段构建",
      "Performance Characteristics": "性能特征",
      Troubleshooting: "故障排查",
      "No sessions appearing after starting Claude Code": "启动 Claude Code 后没有出现会话",
      "Check 1 — Is the server running?": "检查 1——服务器在运行吗？",
      "Check 2 — Are hooks installed?": "检查 2——Hook 是否已安装？",
      "Check 3 — Start a new Claude Code session": "检查 3——启动一个新的 Claude Code 会话",
      "Check 4 — Is Node.js in PATH?": "检查 4——Node.js 是否在 PATH 中？",
      "Common Issues": "常见问题",
      "Technology Choices": "技术选型",
      "Workflow Runs": "工作流运行",
    },
    vi: {
      "Run Agent": "Chạy Agent",
      "Agent Config": "Cấu hình Agent",
      "Remote Data Sources": "Nguồn dữ liệu từ xa",
      "How It Works": "Cách hoạt động",
      Setup: "Thiết lập",
      "Data Scope": "Phạm vi dữ liệu",
      "Live Status": "Trạng thái trực tiếp",
      "Security Model": "Mô hình an toàn",
      "API Endpoints": "Điểm cuối API",
      Metrics: "Số liệu",
      "WebSocket Events": "Sự kiện WebSocket",
      CLI: "CLI",
      "System Overview": "Tổng quan hệ thống",
      "What's Included": "Bao gồm những gì",
      "Live Dashboard": "Bảng điều khiển trực tiếp",
      "Kanban Board": "Bảng Kanban",
      "Sessions Table": "Bảng phiên",
      "Session Detail": "Chi tiết phiên",
      "Alerts & Webhooks": "Cảnh báo & Webhook",
      "Desktop App (macOS & Windows)": "Ứng dụng máy tính để bàn (macOS & Windows)",
      "Activity Feed": "Luồng hoạt động",
      Analytics: "Phân tích",
      "WebSocket Push": "Đẩy qua WebSocket",
      Statusline: "Thanh trạng thái",
      "History Import": "Nhập lịch sử",
      "Transcript Cache": "Cache transcript",
      "Bounded Cache Memory": "Bộ nhớ cache có giới hạn",
      "Constant-Time Sweep": "Quét thời gian hằng số",
      "Subagent Hierarchy": "Phân cấp subagent",
      "Cost Tracking": "Theo dõi chi phí",
      "Settings & Management": "Cài đặt & Quản lý",
      "Local MCP Server": "Máy chủ MCP cục bộ",
      "Claude + Codex Extensions": "Tiện ích mở rộng Claude + Codex",
      "Workflow Graphs": "Đồ thị quy trình",
      "Workflow Analytics": "Phân tích quy trình",
      "Session Drill-In": "Đào sâu phiên",
      "Browser Notifications": "Thông báo trình duyệt",
      "Docker Deployment": "Triển khai Docker",
      "Plugin Marketplace": "Chợ Plugin",
      "Run Claude": "Chạy Claude",
      "Claude Config Explorer": "Trình khám phá cấu hình Claude",
      "Responsive Design": "Thiết kế đáp ứng",
      "Concurrency Timeline": "Dòng thời gian đồng thời",
      "VS Code Extension": "Tiện ích mở rộng VS Code",
      "Error Propagation Map": "Bản đồ lan truyền lỗi",
      "Progressive Web App": "Ứng dụng Web Tiến bộ",
      "Fresh-by-Default Caching": "Bộ nhớ đệm luôn mới mặc định",
      "Auto-Reload on Update": "Tự động tải lại khi cập nhật",
      Screenshots: "Ảnh chụp màn hình",
      "GitHub Star History": "Lịch sử Star trên GitHub",
      "Hook Events Captured": "Các sự kiện Hook được ghi nhận",
      "Quick Start": "Bắt đầu nhanh",
      Clone: "Sao chép",
      Install: "Cài đặt",
      Start: "Khởi động",
      "Use Claude": "Dùng Claude",
      "Alternative: Docker / Podman": "Phương án thay thế: Docker / Podman",
      "Optional: Enable MCP and Agent Extensions": "Tùy chọn: Bật MCP và các phần mở rộng Agent",
      Verification: "Xác minh",
      Configuration: "Cấu hình",
      "Environment Variables": "Biến môi trường",
      "Hook Configuration": "Cấu hình Hook",
      "Scripts Reference": "Tham chiếu Scripts",
      "System Architecture": "Kiến trúc hệ thống",
      "Agent State Machine": "Máy trạng thái của Agent",
      "Session State Machine": "Máy trạng thái của phiên",
      "Data Flow": "Luồng dữ liệu",
      "Event Ingestion Pipeline": "Pipeline thu nhận sự kiện",
      "Client Data Loading Pattern": "Mẫu tải dữ liệu phía client",
      "Server Architecture": "Kiến trúc máy chủ",
      "Server Modules": "Các module máy chủ",
      "Client Architecture": "Kiến trúc client",
      "Client Routes": "Các route phía client",
      "Key Client Modules": "Các mô-đun client chính",
      "PWA & Service Worker": "PWA & Service Worker",
      "State Management": "Quản lý trạng thái",
      "Database Design": "Thiết kế cơ sở dữ liệu",
      Indexes: "Chỉ mục",
      "SQLite Configuration": "Cấu hình SQLite",
      "API Reference": "Tài liệu tham khảo API",
      Health: "Tình trạng",
      Sessions: "Phiên",
      Agents: "Agent",
      "Events, Stats, Analytics": "Sự kiện, Thống kê, Phân tích",
      "Hooks Ingestion": "Thu nạp Hook",
      Pricing: "Định giá",
      Settings: "Cài đặt",
      "Import History": "Nhập lịch sử",
      Workflows: "Quy trình làm việc",
      Alerts: "Cảnh báo",
      Webhooks: "Webhooks",
      "WebSocket Protocol": "Giao thức WebSocket",
      "Message Envelope": "Vỏ bọc thông điệp",
      "Hook Integration": "Tích hợp Hook",
      "Hook Handler Design": "Thiết kế bộ xử lý Hook",
      "Hook Installation Flow": "Luồng cài đặt Hook",
      "Import Pipeline": "Đường ống nhập",
      "Three Modes, One Pipeline": "Ba chế độ, một đường ống",
      "Upload Request Sequence": "Trình tự yêu cầu tải lên",
      "Idempotence & Cost Accuracy": "Tính idempotent và độ chính xác chi phí",
      "Supported Source Layouts": "Các bố cục nguồn được hỗ trợ",
      "Safety Model": "Mô hình an toàn",
      "WebSocket Progress Events": "Sự kiện tiến trình WebSocket",
      "MCP & Agent Extensions": "MCP và phần mở rộng tác tử",
      "Local MCP Server Runtime": "Runtime máy chủ MCP cục bộ",
      "Agent Extension Layout": "Bố cục phần mở rộng tác tử",
      "Root Helper Scripts": "Các script trợ giúp ở thư mục gốc",
      Installation: "Cài đặt",
      "Available Plugins": "Các plugin có sẵn",
      "Skill Usage Examples": "Ví dụ sử dụng kỹ năng",
      "CLI Tools": "Công cụ CLI",
      "Plugin Architecture": "Kiến trúc plugin",
      "Data Model Reference": "Tham chiếu mô hình dữ liệu",
      "Statusline Utility": "Tiện ích Statusline",
      "Detailed Components": "Các thành phần chi tiết",
      "In-Process Architecture": "Kiến trúc trong tiến trình",
      "What It Adds": "Những gì nó bổ sung",
      "Menu-Bar / Notification-Area (Tray) Icon":
        "Biểu tượng thanh menu / khu vực thông báo (khay)",
      "Native Application Menu": "Menu ứng dụng gốc",
      "Auto-Start at Login": "Tự động khởi động khi đăng nhập",
      "Close Hides, Server Stays Up": "Đóng để ẩn, máy chủ vẫn chạy",
      "Runs Alongside the Web Dashboard": "Chạy song song với bảng điều khiển web",
      "Single-Instance Lock": "Khóa một thực thể",
      "First-Boot Bootstrap": "Khởi động bootstrap lần đầu",
      "Data Persistence & CLI Reliability": "Tính bền vững của dữ liệu & độ tin cậy của CLI",
      "Port Discovery": "Phát hiện cổng",
      "How to Get It": "Cách lấy nó",
      "Option A — download the latest GitHub Release (recommended)":
        "Tùy chọn A — tải xuống GitHub Release mới nhất (khuyến nghị)",
      "Option B — per-commit CI artifact": "Phương án B — artifact CI theo từng commit",
      "Option C — build locally": "Phương án C — dựng cục bộ",
      macOS: "macOS",
      "Open the DMG": "Mở DMG",
      "Drag to Applications": "Kéo vào Applications",
      "Clear Quarantine": "Xóa thuộc tính cách ly",
      Launch: "Khởi chạy",
      Windows: "Windows",
      "Run the Installer": "Chạy trình cài đặt",
      "Clear SmartScreen": "Vượt qua SmartScreen",
      "Settings Page": "Trang Cài đặt",
      "Model Pricing": "Giá theo mô hình",
      "Data Management": "Quản lý dữ liệu",
      "Data Export": "Xuất dữ liệu",
      "System Health": "Sức khỏe hệ thống",
      "Notification Preferences": "Tùy chọn thông báo",
      "Rule types": "Các loại quy tắc",
      "Evaluation engine": "Bộ máy đánh giá",
      "14 first-class providers": "14 nhà cung cấp hạng nhất",
      "Delivery engine": "Bộ máy gửi",
      Security: "Bảo mật",
      "Guided setup": "Thiết lập có hướng dẫn",
      "Provider payloads": "Payload của nhà cung cấp",
      "Update Notifier": "Update Notifier",
      "Non-Blocking Detection": "Phát hiện không chặn",
      "5-min Scheduler": "Bộ lập lịch 5 phút",
      "Situation-Aware Command": "Lệnh nhận biết tình huống",
      "Two UI Surfaces": "Hai bề mặt giao diện",
      "Soft Failure Semantics": "Ngữ nghĩa lỗi mềm",
      "Dismissal Memory": "Bộ nhớ bỏ qua",
      "API Surface": "Bề mặt API",
      "Connection Status": "Connection Status",
      "Internationalization (i18n)": "Quốc tế hóa (i18n)",
      "Namespaced resources": "Tài nguyên theo không gian tên",
      "Detection & fallback": "Phát hiện & quay lui",
      "Locale-aware formatting": "Định dạng nhận biết locale",
      "Technical terms preserved": "Giữ nguyên thuật ngữ kỹ thuật",
      "🐾 Tabby — Reactive Cat Companion": "🐾 Tabby — Mèo Đồng Hành Phản Ứng",
      "Reactive Mascot — Eight Moods": "Linh vật phản ứng — Tám tâm trạng",
      "Auto-Surface Speech Bubbles": "Tự động bật bong bóng thoại",
      "The ⌘B Panel": "Bảng ⌘B",
      "Ask → Run Claude Handoff": "Chuyển giao Ask → Run Claude",
      "Accessibility & Resilience": "Khả năng tiếp cận & Tính bền bỉ",
      "Deployment Modes": "Các chế độ triển khai",
      "Container Runtime (Docker / Podman)": "Môi trường chạy container (Docker / Podman)",
      "Docker / Podman": "Docker / Podman",
      "Plain Docker / Podman (no Compose)": "Docker / Podman thuần (không dùng Compose)",
      "Volume Mounts": "Gắn kết ổ đĩa",
      "Multi-Stage Build": "Xây dựng nhiều giai đoạn",
      "Performance Characteristics": "Đặc điểm hiệu năng",
      Troubleshooting: "Khắc phục sự cố",
      "No sessions appearing after starting Claude Code":
        "Không có phiên nào xuất hiện sau khi khởi động Claude Code",
      "Check 1 — Is the server running?": "Kiểm tra 1 — Máy chủ có đang chạy không?",
      "Check 2 — Are hooks installed?": "Kiểm tra 2 — Hook đã được cài đặt chưa?",
      "Check 3 — Start a new Claude Code session":
        "Kiểm tra 3 — Khởi động một phiên Claude Code mới",
      "Check 4 — Is Node.js in PATH?": "Kiểm tra 4 — Node.js có trong PATH không?",
      "Common Issues": "Các vấn đề thường gặp",
      "Technology Choices": "Lựa chọn công nghệ",
      "Workflow Runs": "Lần chạy quy trình",
    },
    ko: {
      "Run Agent": "에이전트 실행",
      "Agent Config": "에이전트 구성",
      "Remote Data Sources": "원격 데이터 소스",
      "How It Works": "작동 방식",
      Setup: "설정",
      "Data Scope": "데이터 범위",
      "Live Status": "실시간 상태",
      "Security Model": "보안 모델",
      "API Endpoints": "API 엔드포인트",
      Metrics: "메트릭",
      "WebSocket Events": "WebSocket 이벤트",
      CLI: "CLI",
      "System Overview": "시스템 개요",
      "What's Included": "포함된 기능",
      "Live Dashboard": "실시간 대시보드",
      "Kanban Board": "Kanban 보드",
      "Sessions Table": "세션 테이블",
      "Session Detail": "세션 상세",
      "Alerts & Webhooks": "알림 & Webhook",
      "Desktop App (macOS & Windows)": "데스크톱 앱 (macOS & Windows)",
      "Activity Feed": "활동 피드",
      Analytics: "분석",
      "WebSocket Push": "WebSocket 푸시",
      Statusline: "Statusline",
      "History Import": "히스토리 가져오기",
      "Transcript Cache": "트랜스크립트 캐시",
      "Bounded Cache Memory": "제한된 캐시 메모리",
      "Constant-Time Sweep": "상수 시간 스윕",
      "Subagent Hierarchy": "서브에이전트 계층 구조",
      "Cost Tracking": "비용 추적",
      "Settings & Management": "설정 & 관리",
      "Local MCP Server": "로컬 MCP 서버",
      "Claude + Codex Extensions": "Claude + Codex 확장",
      "Workflow Graphs": "워크플로 그래프",
      "Workflow Analytics": "워크플로 분석",
      "Session Drill-In": "세션 드릴다운",
      "Browser Notifications": "브라우저 알림",
      "Docker Deployment": "Docker 배포",
      "Plugin Marketplace": "플러그인 마켓플레이스",
      "Run Claude": "Claude 실행",
      "Claude Config Explorer": "Claude 설정 탐색기",
      "Responsive Design": "반응형 디자인",
      "Concurrency Timeline": "동시성 타임라인",
      "VS Code Extension": "VS Code 확장",
      "Error Propagation Map": "오류 전파 맵",
      "Progressive Web App": "프로그레시브 웹 앱",
      "Fresh-by-Default Caching": "기본 신선 캐싱",
      "Auto-Reload on Update": "업데이트 시 자동 새로고침",
      Screenshots: "스크린샷",
      "GitHub Star History": "GitHub Star 히스토리",
      "Hook Events Captured": "캡처되는 Hook 이벤트",
      "Quick Start": "빠른 시작",
      Clone: "클론",
      Install: "설치",
      Start: "시작",
      "Use Claude": "Claude 사용",
      "Alternative: Docker / Podman": "대안: Docker / Podman",
      "Optional: Enable MCP and Agent Extensions": "선택 사항: MCP 및 에이전트 확장 활성화",
      Verification: "검증",
      Configuration: "설정",
      "Environment Variables": "환경 변수",
      "Hook Configuration": "Hook 설정",
      "Scripts Reference": "스크립트 참조",
      "System Architecture": "시스템 아키텍처",
      "Agent State Machine": "에이전트 상태 머신",
      "Session State Machine": "세션 상태 머신",
      "Data Flow": "데이터 흐름",
      "Event Ingestion Pipeline": "이벤트 수집 파이프라인",
      "Client Data Loading Pattern": "클라이언트 데이터 로딩 패턴",
      "Server Architecture": "서버 아키텍처",
      "Server Modules": "서버 모듈",
      "Client Architecture": "클라이언트 아키텍처",
      "Client Routes": "클라이언트 라우트",
      "Key Client Modules": "주요 클라이언트 모듈",
      "PWA & Service Worker": "PWA & 서비스 워커",
      "State Management": "상태 관리",
      "Database Design": "데이터베이스 설계",
      Indexes: "인덱스",
      "SQLite Configuration": "SQLite 설정",
      "API Reference": "API 참조",
      Health: "헬스 체크",
      Sessions: "세션",
      Agents: "에이전트",
      "Events, Stats, Analytics": "이벤트, 통계, 분석",
      "Hooks Ingestion": "Hook 수집",
      Pricing: "가격 책정",
      Settings: "설정",
      "Import History": "가져오기 기록",
      Workflows: "워크플로",
      Alerts: "알림",
      Webhooks: "Webhook",
      "WebSocket Protocol": "WebSocket 프로토콜",
      "Message Envelope": "메시지 엔벨로프",
      "Hook Integration": "Hook 통합",
      "Hook Handler Design": "Hook 핸들러 설계",
      "Hook Installation Flow": "Hook 설치 흐름",
      "Import Pipeline": "가져오기 파이프라인",
      "Three Modes, One Pipeline": "세 가지 모드, 하나의 파이프라인",
      "Upload Request Sequence": "업로드 요청 시퀀스",
      "Idempotence & Cost Accuracy": "멱등성 & 비용 정확성",
      "Supported Source Layouts": "지원되는 소스 레이아웃",
      "Safety Model": "안전 모델",
      "WebSocket Progress Events": "WebSocket 진행 이벤트",
      "MCP & Agent Extensions": "MCP & 에이전트 확장",
      "Local MCP Server Runtime": "로컬 MCP 서버 런타임",
      "Agent Extension Layout": "에이전트 확장 레이아웃",
      "Root Helper Scripts": "루트 헬퍼 스크립트",
      Installation: "설치",
      "Available Plugins": "사용 가능한 플러그인",
      "Skill Usage Examples": "스킬 사용 예시",
      "CLI Tools": "CLI 도구",
      "Plugin Architecture": "플러그인 아키텍처",
      "Data Model Reference": "데이터 모델 참조",
      "Statusline Utility": "Statusline 유틸리티",
      "Detailed Components": "세부 구성 요소",
      "In-Process Architecture": "인프로세스 아키텍처",
      "What It Adds": "추가되는 기능",
      "Menu-Bar / Notification-Area (Tray) Icon": "메뉴 바 / 알림 영역(트레이) 아이콘",
      "Native Application Menu": "네이티브 애플리케이션 메뉴",
      "Auto-Start at Login": "로그인 시 자동 시작",
      "Close Hides, Server Stays Up": "닫아도 숨김, 서버는 계속 실행",
      "Runs Alongside the Web Dashboard": "웹 대시보드와 함께 실행",
      "Single-Instance Lock": "단일 인스턴스 잠금",
      "First-Boot Bootstrap": "최초 부팅 부트스트랩",
      "Data Persistence & CLI Reliability": "데이터 지속성 & CLI 안정성",
      "Port Discovery": "포트 검색",
      "How to Get It": "받는 방법",
      "Option A — download the latest GitHub Release (recommended)":
        "방법 A — 최신 GitHub Release 다운로드 (권장)",
      "Option B — per-commit CI artifact": "방법 B — 커밋별 CI 아티팩트",
      "Option C — build locally": "방법 C — 로컬 빌드",
      macOS: "macOS",
      "Open the DMG": "DMG 파일 열기",
      "Drag to Applications": "Applications로 드래그",
      "Clear Quarantine": "격리 속성 제거",
      Launch: "실행",
      Windows: "Windows",
      "Run the Installer": "설치 프로그램 실행",
      "Clear SmartScreen": "SmartScreen 차단 해제",
      "Settings Page": "설정 페이지",
      "Model Pricing": "모델 가격",
      "Data Management": "데이터 관리",
      "Data Export": "데이터 내보내기",
      "System Health": "시스템 상태",
      "Notification Preferences": "알림 설정",
      "Rule types": "규칙 유형",
      "Evaluation engine": "평가 엔진",
      "14 first-class providers": "14개의 퍼스트클래스 프로바이더",
      "Delivery engine": "전달 엔진",
      Security: "보안",
      "Guided setup": "가이드 설정",
      "Provider payloads": "프로바이더 페이로드",
      "Update Notifier": "업데이트 알리미",
      "Non-Blocking Detection": "논블로킹 감지",
      "5-min Scheduler": "5분 스케줄러",
      "Situation-Aware Command": "상황 인식 명령",
      "Two UI Surfaces": "두 가지 UI 화면",
      "Soft Failure Semantics": "소프트 실패 시맨틱스",
      "Dismissal Memory": "닫힘 상태 기억",
      "API Surface": "API 표면",
      "Connection Status": "연결 상태",
      "Internationalization (i18n)": "국제화 (i18n)",
      "Namespaced resources": "네임스페이스화된 리소스",
      "Detection & fallback": "감지 & 폴백",
      "Locale-aware formatting": "로케일 인식 포맷팅",
      "Technical terms preserved": "보존되는 기술 용어",
      "🐾 Tabby — Reactive Cat Companion": "🐾 Tabby — 반응형 고양이 동반자",
      "Reactive Mascot — Eight Moods": "반응형 마스코트 — 여덟 가지 감정",
      "Auto-Surface Speech Bubbles": "말풍선 자동 표시",
      "The ⌘B Panel": "⌘B 패널",
      "Ask → Run Claude Handoff": "Ask → Run Claude 핸드오프",
      "Accessibility & Resilience": "접근성 & 복원력",
      "Deployment Modes": "배포 모드",
      "Container Runtime (Docker / Podman)": "컨테이너 런타임 (Docker / Podman)",
      "Docker / Podman": "Docker / Podman",
      "Plain Docker / Podman (no Compose)": "순수 Docker / Podman (Compose 미사용)",
      "Volume Mounts": "볼륨 마운트",
      "Multi-Stage Build": "멀티 스테이지 빌드",
      "Performance Characteristics": "성능 특성",
      Troubleshooting: "문제 해결",
      "No sessions appearing after starting Claude Code":
        "Claude Code 시작 후 세션이 나타나지 않음",
      "Check 1 — Is the server running?": "확인 1 — 서버가 실행 중인가요?",
      "Check 2 — Are hooks installed?": "확인 2 — Hook이 설치되어 있나요?",
      "Check 3 — Start a new Claude Code session": "확인 3 — 새 Claude Code 세션 시작",
      "Check 4 — Is Node.js in PATH?": "확인 4 — Node.js가 PATH에 있나요?",
      "Common Issues": "일반적인 문제",
      "Technology Choices": "기술 선택",
      "Workflow Runs": "워크플로 실행",
    },
  },
};

Object.assign(window.__WIKI_CONTENT_I18N.zh, {
  '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Skills</strong> — the Skills tab lists every discovered skill (user, project, and plugin) with its description and source, is searchable across the whole set, and opens any skill file for a safe, timestamp-backed edit</span>':
    '<span class="caption-icon">🧩</span> <span><strong>Claude 配置浏览器 · 技能</strong> — 技能标签页会列出发现的所有技能（用户、项目和插件），显示说明与来源，支持全局搜索，并可安全打开任意技能文件进行带时间戳备份的编辑</span>',
  "Matched subagent → <span class=\"status-chip chip-completed\"><span class=\"chip-dot\"></span>Completed</span>. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session's <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent's own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks. The same scan also rebuilds the nested-subagent hierarchy — it repoints each subagent's <code>parent_agent_id</code> to the true spawner recovered from the transcript's Task tool result (<code>toolUseResult.agentId</code>), so subagents that spawn their own subagents nest correctly instead of flattening under main.":
    '匹配的 Subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>已完成</span>。这里特意<strong>不会</strong>清除等待标记，因为后台 Subagent 完成并不能说明用户是否仍在等待。同时会启动一次即发即弃的 JSONL 扫描（<code>scanAndImportSubagents</code>），遍历会话的 <code>subagents/agent-*.jsonl</code> 文件，按 <code>tool_use_id</code> 配对 <code>tool_use</code> ↔ <code>tool_result</code> 区块，并以各 Subagent 自己的 <code>agent_id</code> 发出逐工具的 <code>PreToolUse</code> + <code>PostToolUse</code> 事件，从而呈现 Subagent 内部调用但不会触发 Hook 的工具。同一扫描还会重建嵌套 Subagent 层级：把每个 Subagent 的 <code>parent_agent_id</code> 指向从 Transcript 的 Task 工具结果（<code>toolUseResult.agentId</code>）恢复出的真实派生者，使 Subagent 派生的 Subagent 正确嵌套，而不是全部平铺在主 Agent 下。',
  "System info (DB size, row counts, hook status, server uptime). Data export as one versioned JSON bundle and matching import/restore (<code>POST /api/settings/import</code> via <code>server/lib/data-transfer.js</code> — idempotent, session-atomic, non-destructive; consolidate machines). Session cleanup (abandon stale active sessions, purge old completed sessions). Clear all data. Reset pricing to defaults. Reinstall hooks.":
    "系统信息（数据库大小、行数、Hook 状态、服务器运行时间）。将数据导出为一个带版本的 JSON 包，并支持对应的导入/恢复（通过 <code>server/lib/data-transfer.js</code> 调用 <code>POST /api/settings/import</code>，具备幂等、会话原子性和非破坏性，可合并多台机器）。会话清理（放弃长期停滞的活动会话、清除旧的已完成会话）。清空全部数据。将定价重置为默认值。重新安装 Hook。",
  "Export all data (sessions, agents, events, tokens, workflows, runs, alert rules, pricing) as one versioned JSON download":
    "将所有数据（会话、Agent、事件、Token、工作流、运行、告警规则和定价）导出为一个带版本的 JSON 下载文件",
  '<span class="caption-icon">🛰️</span> <span>Settings → Remote Data Sources — add machines by SSH destination, test connectivity, sync on demand or on a background schedule, and narrow the whole app with the global data-scope selector</span>':
    '<span class="caption-icon">🛰️</span> <span>设置 → 远程数据源 — 通过 SSH 目标添加机器、测试连接、按需或定时后台同步，并使用全局数据范围选择器筛选整个应用</span>',
  '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — the <code>/cc-config</code> page: 12 tabs (skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, keybindings, settings, memory), a scope filter (user / project / all), search, and safe edit affordances on the low-risk surfaces</span>':
    '<span class="caption-icon">🧰</span> <span><strong>Claude 配置浏览器</strong> — <code>/cc-config</code> 页面包含 12 个标签页（技能、Subagent、斜杠命令、输出样式、插件、市场、MCP Server、Hook、快捷键、设置和记忆）、范围筛选（用户 / 项目 / 全部）、搜索，以及低风险配置面的安全编辑入口</span>',
  '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Skills</strong> — the Skills tab lists every discovered skill (user, project, and plugin) with description and source, is searchable, and opens any <code>SKILL.md</code> for a timestamp-backed edit via the shared editor modal</span>':
    '<span class="caption-icon">🧩</span> <span><strong>Claude 配置浏览器 · 技能</strong> — 技能标签页列出发现的所有用户、项目和插件技能及其说明与来源，支持搜索，并可通过共享编辑弹窗打开任意 <code>SKILL.md</code>，在带时间戳备份后进行编辑</span>',
  "A first-class <strong>configuration inspector</strong> at <code>/cc-config</code> that reads every surface Claude Code knows about — skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts — from the same on-disk layout the CLI uses. The Overview tab summarizes counts and filesystem roots; every other tab drills into one surface with search, scope filtering (<b>user</b> / <b>project</b> / <b>all</b>), and — where safe — create / edit / delete with <strong>mandatory timestamped backups</strong> before every mutation. A <code>cc-watcher</code> on <code>CLAUDE_HOME</code> plus <code>cc_config_changed</code> WebSocket messages keep the page live when files change outside the dashboard.":
    "<code>/cc-config</code> 提供一流的<strong>配置检查器</strong>，按照 CLI 使用的同一磁盘布局读取 Claude Code 的所有配置面：技能、Subagent、斜杠命令、输出样式、插件、市场、MCP Server、Hook、设置、记忆、快捷键和状态栏脚本。概览标签页汇总数量和文件系统根目录；其余标签页可通过搜索和范围筛选（<b>用户</b> / <b>项目</b> / <b>全部</b>）深入查看各配置面，并在安全时提供创建、编辑和删除操作；每次变更前都必须创建<strong>带时间戳的备份</strong>。<code>CLAUDE_HOME</code> 上的 <code>cc-watcher</code> 与 <code>cc_config_changed</code> WebSocket 消息会在仪表盘外部文件发生变化时保持页面实时更新。",
  "<strong>Skills</strong>, <strong>Subagents</strong>, <strong>Slash commands</strong>, <strong>Output styles</strong>, <strong>Plugins</strong> (with per-plugin <code>contributes</code> counts and <code>plugin.json</code> metadata), <strong>Marketplaces</strong>, <strong>MCP servers</strong>, <strong>Hooks</strong> (plus <code>~/.claude/hooks/</code> script listing), <strong>Keybindings</strong>, <strong>Settings</strong> (Current configuration summary + per-file JSON), and <strong>Memory</strong> (user/project <code>CLAUDE.md</code> + per-project auto-memory). Statusline config and script previews live under Settings.":
    "<strong>技能</strong>、<strong>Subagent</strong>、<strong>斜杠命令</strong>、<strong>输出样式</strong>、<strong>插件</strong>（含逐插件 <code>contributes</code> 计数和 <code>plugin.json</code> 元数据）、<strong>市场</strong>、<strong>MCP Server</strong>、<strong>Hook</strong>（另含 <code>~/.claude/hooks/</code> 脚本列表）、<strong>快捷键</strong>、<strong>设置</strong>（当前配置摘要和逐文件 JSON）及<strong>记忆</strong>（用户/项目 <code>CLAUDE.md</code> 和逐项目自动记忆）。状态栏配置和脚本预览位于设置下。",
  '<code>PUT</code> / <code>DELETE /api/cc-config/file</code> handles skills, agents, commands, output-styles, <code>CLAUDE.md</code>, and per-project auto-memory (<code>scope: "auto-memory"</code>). Writes are <strong>atomic</strong> (temp file + rename), names are validated against a strict regex, resolved paths are containment-checked, and content is capped at 256&nbsp;KiB. Every overwrite or delete creates a backup under <code>cc-config-backups/</code> — outside the directories Claude Code scans — so deleted artifacts cannot reappear as ghost entries.':
    '<code>PUT</code> / <code>DELETE /api/cc-config/file</code> 处理技能、Agent、命令、输出样式、<code>CLAUDE.md</code> 和逐项目自动记忆（<code>scope: "auto-memory"</code>）。写入是<strong>原子的</strong>（临时文件 + 重命名），名称通过严格正则校验，解析后的路径会进行包含关系检查，内容上限为 256&nbsp;KiB。每次覆盖或删除都会在 Claude Code 扫描目录之外的 <code>cc-config-backups/</code> 中创建备份，因此已删除内容不会作为幽灵条目重新出现。',
  "The Keybindings tab includes a structured inline editor for <code>~/.claude/keybindings.json</code>. Add or remove contexts and bindings, edit keys and actions in place, and save via <code>PUT /api/cc-config/keybindings</code>. The server read-modify-writes the file: top-level metadata (<code>$schema</code>, <code>$docs</code>) is preserved, duplicate contexts/keys are rejected, and the file is backed up first. Unlike <code>settings.json</code>, the live CLI does not rewrite keybindings mid-session, so dashboard edits are safe.":
    "快捷键标签页为 <code>~/.claude/keybindings.json</code> 提供结构化内联编辑器。可添加或删除上下文与绑定、就地编辑按键和操作，并通过 <code>PUT /api/cc-config/keybindings</code> 保存。服务器会读取、修改并写回文件：保留顶层元数据（<code>$schema</code>、<code>$docs</code>），拒绝重复上下文/按键，并先创建备份。与 <code>settings.json</code> 不同，运行中的 CLI 不会在会话中重写快捷键，因此从仪表盘编辑是安全的。",
  "The Memory tab surfaces user + project <code>CLAUDE.md</code> and the per-project file-based memory store — every <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+). Files are grouped by project in collapsible sections, searchable, and editable. Clickable index links in <code>MEMORY.md</code> jump to (scroll + highlight) the matching fact file.":
    "记忆标签页呈现用户和项目的 <code>CLAUDE.md</code>，以及逐项目的文件式记忆存储：<code>~/.claude/projects/&lt;slug&gt;/memory/</code> 下的每个 <code>*.md</code>（一个 <code>MEMORY.md</code> 索引和每条记忆事实一个文件，通常超过 100 个）。文件按项目分组在可折叠区域中，支持搜索和编辑；<code>MEMORY.md</code> 中可点击的索引链接会滚动并高亮对应的事实文件。",
  "<strong>Plugins</strong>, <strong>MCP servers</strong>, <strong>hooks-in-settings</strong>, and live <strong>settings.json</strong> files are intentionally not writable from the dashboard — they race with the running <code>claude</code> CLI. Those tabs show explainer banners with copy-able <code>claude plugin …</code>, <code>claude mcp …</code>, and <code>claude config …</code> commands instead of edit buttons. Secret-like settings keys are redacted server-side.":
    "<strong>插件</strong>、<strong>MCP Server</strong>、<strong>设置内 Hook</strong>和实时 <strong>settings.json</strong> 文件被有意设为不可从仪表盘写入，因为它们会与运行中的 <code>claude</code> CLI 竞争。这些标签页使用说明横幅和可复制的 <code>claude plugin …</code>、<code>claude mcp …</code>、<code>claude config …</code> 命令替代编辑按钮。类似 Secret 的设置键会在服务器端脱敏。",
  "Successful mutations broadcast <code>cc_config_changed</code> over the WebSocket so every open <code>/cc-config</code> tab refetches without polling. A filesystem watcher on <code>CLAUDE_HOME</code> (<code>cc-watcher.js</code>) detects external edits (e.g. you changed a skill in your editor) and triggers the same refresh path. The Backups modal lists every timestamped copy and builds <code>mv</code> restore commands.":
    "成功的变更会通过 WebSocket 广播 <code>cc_config_changed</code>，让每个打开的 <code>/cc-config</code> 标签页无需轮询即可重新获取数据。<code>CLAUDE_HOME</code> 上的文件系统 watcher（<code>cc-watcher.js</code>）会检测外部编辑（例如在编辑器中修改技能）并触发相同刷新路径。备份弹窗列出每个带时间戳的副本，并生成 <code>mv</code> 恢复命令。",
  "How to change (if read-only)": "如何更改（只读时）",
  "Filesystem roots + aggregate counts across every surface":
    "所有配置面的文件系统根目录和汇总计数",
  "Single-file viewer (path-contained to allowed roots)":
    "单文件查看器（路径限制在允许的根目录内）",
  "Create/overwrite a text artifact — backs up first, atomic write":
    "创建或覆盖文本内容——先备份，再原子写入",
  "Timestamped backup listing for the recovery modal": "恢复弹窗中的带时间戳备份列表",
  "Every mutation writes a timestamped copy under <code>&lt;root&gt;/cc-config-backups/&lt;type&gt;/</code> (or <code>&lt;memory-dir&gt;/.cc-config-backups/auto-memory/</code> for per-project memory files) <em>before</em> touching the original. Skill folders are copied whole so bundled assets survive in the backup. The UI's Backups modal shows every copy and prints a one-line <code>mv</code> restore command.":
    "每次变更都会在触碰原文件<em>之前</em>，将带时间戳的副本写入 <code>&lt;root&gt;/cc-config-backups/&lt;type&gt;/</code>（逐项目记忆文件则写入 <code>&lt;memory-dir&gt;/.cc-config-backups/auto-memory/</code>）。技能目录会完整复制，使捆绑资源保留在备份中。UI 的备份弹窗显示每个副本，并输出单行 <code>mv</code> 恢复命令。",
  "Do not edit settings.json from here": "不要在此处编辑 settings.json",
  "The live <code>claude</code> process rewrites <code>settings.json</code> and <code>~/.claude.json</code> constantly (model changes, plugin toggles, MCP registration). A dashboard write would race and lose state — or worse, break hook ingestion. Use <code>claude config set …</code>, <code>/config</code>, or hand-edit only when no <code>claude</code> process is running.":
    "运行中的 <code>claude</code> 进程会持续重写 <code>settings.json</code> 和 <code>~/.claude.json</code>（模型变更、插件开关、MCP 注册）。仪表盘写入会产生竞争并丢失状态，甚至破坏 Hook 摄取。请使用 <code>claude config set …</code>、<code>/config</code>，或仅在没有 <code>claude</code> 进程运行时手动编辑。",
});

Object.assign(window.__WIKI_CONTENT_I18N.vi, {
  '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Skills</strong> — the Skills tab lists every discovered skill (user, project, and plugin) with its description and source, is searchable across the whole set, and opens any skill file for a safe, timestamp-backed edit</span>':
    '<span class="caption-icon">🧩</span> <span><strong>Trình khám phá cấu hình Claude · Skill</strong> — tab Skill liệt kê mọi skill đã phát hiện (người dùng, dự án và plugin) cùng mô tả và nguồn, cho phép tìm kiếm toàn bộ và mở an toàn bất kỳ tệp skill nào để chỉnh sửa sau khi sao lưu có dấu thời gian</span>',
  '<span class="caption-icon">🔥</span> <span><strong>Observability · Prometheus console</strong> — pre-built landing page at <code>/consoles/index.html</code> with live metric cards, session/token tables, and drill-down links into the Graph UI (Prometheus 3.x compatible — queries the HTTP API directly)</span>':
    '<span class="caption-icon">🔥</span> <span><strong>Khả năng quan sát · bảng điều khiển Prometheus</strong> — trang dựng sẵn tại <code>/consoles/index.html</code> với thẻ số liệu trực tiếp, bảng phiên/Token và liên kết đi sâu vào Graph UI (tương thích Prometheus 3.x — truy vấn trực tiếp HTTP API)</span>',
  '<span class="caption-icon">📈</span> <span><strong>Observability · Prometheus Graph</strong> — run PromQL against scraped CCAM series (e.g. <code>sum(ccam_sessions)</code>, <code>ccam_events_total</code>, <code>rate(ccam_tokens_total[5m])</code>) with starter expressions from the CCAM console and recording rules in <code>monitoring/prometheus/ccam-rules.yml</code></span>':
    '<span class="caption-icon">📈</span> <span><strong>Khả năng quan sát · Prometheus Graph</strong> — chạy PromQL trên chuỗi CCAM đã thu thập (ví dụ <code>sum(ccam_sessions)</code>, <code>ccam_events_total</code>, <code>rate(ccam_tokens_total[5m])</code>) bằng các biểu thức mẫu từ bảng điều khiển CCAM và quy tắc ghi trong <code>monitoring/prometheus/ccam-rules.yml</code></span>',
  "Matched subagent → <span class=\"status-chip chip-completed\"><span class=\"chip-dot\"></span>Completed</span>. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session's <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent's own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks. The same scan also rebuilds the nested-subagent hierarchy — it repoints each subagent's <code>parent_agent_id</code> to the true spawner recovered from the transcript's Task tool result (<code>toolUseResult.agentId</code>), so subagents that spawn their own subagents nest correctly instead of flattening under main.":
    'Subagent khớp → <span class="status-chip chip-completed"><span class="chip-dot"></span>Đã hoàn thành</span>. Cố ý <strong>không</strong> xóa cờ chờ vì một Subagent nền hoàn tất không cho biết gì về người dùng. Đồng thời khởi chạy quét JSONL không chờ kết quả (<code>scanAndImportSubagents</code>) qua các tệp <code>subagents/agent-*.jsonl</code> của phiên, ghép khối <code>tool_use</code> ↔ <code>tool_result</code> theo <code>tool_use_id</code>, rồi phát sự kiện <code>PreToolUse</code> + <code>PostToolUse</code> cho từng công cụ dưới <code>agent_id</code> riêng của mỗi Subagent — nhờ đó hiển thị các lệnh gọi công cụ nội bộ vốn không kích hoạt Hook. Lần quét này cũng dựng lại phân cấp Subagent lồng nhau bằng cách trỏ <code>parent_agent_id</code> tới Agent sinh thật được khôi phục từ kết quả công cụ Task trong Transcript (<code>toolUseResult.agentId</code>), để Subagent con lồng đúng thay vì bị dàn phẳng dưới Agent chính.',
  "System info (DB size, row counts, hook status, server uptime). Data export as one versioned JSON bundle and matching import/restore (<code>POST /api/settings/import</code> via <code>server/lib/data-transfer.js</code> — idempotent, session-atomic, non-destructive; consolidate machines). Session cleanup (abandon stale active sessions, purge old completed sessions). Clear all data. Reset pricing to defaults. Reinstall hooks.":
    "Thông tin hệ thống (kích thước DB, số hàng, trạng thái Hook, thời gian hoạt động của server). Xuất dữ liệu thành một gói JSON có phiên bản và nhập/khôi phục tương ứng (<code>POST /api/settings/import</code> qua <code>server/lib/data-transfer.js</code> — lũy đẳng, nguyên tử theo phiên, không phá hủy; hợp nhất nhiều máy). Dọn phiên (đánh dấu bỏ dở phiên hoạt động cũ, xóa phiên hoàn thành cũ). Xóa toàn bộ dữ liệu. Đặt lại giá mặc định. Cài lại Hook.",
  "Export all data (sessions, agents, events, tokens, workflows, runs, alert rules, pricing) as one versioned JSON download":
    "Xuất toàn bộ dữ liệu (phiên, Agent, sự kiện, Token, quy trình, lần chạy, quy tắc cảnh báo và giá) thành một tệp JSON tải xuống có phiên bản",
  '<span class="caption-icon">🛰️</span> <span>Settings → Remote Data Sources — add machines by SSH destination, test connectivity, sync on demand or on a background schedule, and narrow the whole app with the global data-scope selector</span>':
    '<span class="caption-icon">🛰️</span> <span>Cài đặt → Nguồn dữ liệu từ xa — thêm máy bằng đích SSH, kiểm tra kết nối, đồng bộ theo yêu cầu hoặc lịch nền và thu hẹp toàn ứng dụng bằng bộ chọn phạm vi dữ liệu toàn cục</span>',
  '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — the <code>/cc-config</code> page: 12 tabs (skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, keybindings, settings, memory), a scope filter (user / project / all), search, and safe edit affordances on the low-risk surfaces</span>':
    '<span class="caption-icon">🧰</span> <span><strong>Trình khám phá cấu hình Claude</strong> — trang <code>/cc-config</code> có 12 tab (skill, Subagent, lệnh slash, kiểu đầu ra, plugin, marketplace, MCP Server, Hook, phím tắt, cài đặt và bộ nhớ), bộ lọc phạm vi (người dùng / dự án / tất cả), tìm kiếm và thao tác chỉnh sửa an toàn trên các bề mặt rủi ro thấp</span>',
  '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Skills</strong> — the Skills tab lists every discovered skill (user, project, and plugin) with description and source, is searchable, and opens any <code>SKILL.md</code> for a timestamp-backed edit via the shared editor modal</span>':
    '<span class="caption-icon">🧩</span> <span><strong>Trình khám phá cấu hình Claude · Skill</strong> — tab Skill liệt kê mọi skill người dùng, dự án và plugin đã phát hiện cùng mô tả và nguồn, hỗ trợ tìm kiếm và mở bất kỳ <code>SKILL.md</code> nào trong hộp thoại chung để chỉnh sửa sau khi sao lưu có dấu thời gian</span>',
  "A first-class <strong>configuration inspector</strong> at <code>/cc-config</code> that reads every surface Claude Code knows about — skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts — from the same on-disk layout the CLI uses. The Overview tab summarizes counts and filesystem roots; every other tab drills into one surface with search, scope filtering (<b>user</b> / <b>project</b> / <b>all</b>), and — where safe — create / edit / delete with <strong>mandatory timestamped backups</strong> before every mutation. A <code>cc-watcher</code> on <code>CLAUDE_HOME</code> plus <code>cc_config_changed</code> WebSocket messages keep the page live when files change outside the dashboard.":
    "<code>/cc-config</code> cung cấp một <strong>trình kiểm tra cấu hình</strong> hạng nhất, đọc mọi bề mặt Claude Code biết đến — skill, Subagent, lệnh slash, kiểu đầu ra, plugin, marketplace, MCP Server, Hook, cài đặt, bộ nhớ, phím tắt và script statusline — từ đúng bố cục đĩa mà CLI sử dụng. Tab Tổng quan tóm tắt số lượng và thư mục gốc; các tab khác đi sâu vào từng bề mặt với tìm kiếm, lọc phạm vi (<b>người dùng</b> / <b>dự án</b> / <b>tất cả</b>) và, khi an toàn, tạo / sửa / xóa với <strong>bản sao lưu có dấu thời gian bắt buộc</strong> trước mỗi thay đổi. <code>cc-watcher</code> trên <code>CLAUDE_HOME</code> cùng thông báo WebSocket <code>cc_config_changed</code> giữ trang cập nhật khi tệp bị đổi ngoài bảng điều khiển.",
  "<strong>Skills</strong>, <strong>Subagents</strong>, <strong>Slash commands</strong>, <strong>Output styles</strong>, <strong>Plugins</strong> (with per-plugin <code>contributes</code> counts and <code>plugin.json</code> metadata), <strong>Marketplaces</strong>, <strong>MCP servers</strong>, <strong>Hooks</strong> (plus <code>~/.claude/hooks/</code> script listing), <strong>Keybindings</strong>, <strong>Settings</strong> (Current configuration summary + per-file JSON), and <strong>Memory</strong> (user/project <code>CLAUDE.md</code> + per-project auto-memory). Statusline config and script previews live under Settings.":
    "<strong>Skill</strong>, <strong>Subagent</strong>, <strong>lệnh slash</strong>, <strong>kiểu đầu ra</strong>, <strong>plugin</strong> (với số <code>contributes</code> và metadata <code>plugin.json</code> theo plugin), <strong>marketplace</strong>, <strong>MCP Server</strong>, <strong>Hook</strong> (kèm danh sách script <code>~/.claude/hooks/</code>), <strong>phím tắt</strong>, <strong>cài đặt</strong> (tóm tắt cấu hình hiện tại + JSON theo tệp) và <strong>bộ nhớ</strong> (<code>CLAUDE.md</code> người dùng/dự án + bộ nhớ tự động theo dự án). Cấu hình statusline và bản xem trước script nằm trong Cài đặt.",
  '<code>PUT</code> / <code>DELETE /api/cc-config/file</code> handles skills, agents, commands, output-styles, <code>CLAUDE.md</code>, and per-project auto-memory (<code>scope: "auto-memory"</code>). Writes are <strong>atomic</strong> (temp file + rename), names are validated against a strict regex, resolved paths are containment-checked, and content is capped at 256&nbsp;KiB. Every overwrite or delete creates a backup under <code>cc-config-backups/</code> — outside the directories Claude Code scans — so deleted artifacts cannot reappear as ghost entries.':
    '<code>PUT</code> / <code>DELETE /api/cc-config/file</code> xử lý skill, Agent, lệnh, kiểu đầu ra, <code>CLAUDE.md</code> và bộ nhớ tự động theo dự án (<code>scope: "auto-memory"</code>). Ghi là <strong>nguyên tử</strong> (tệp tạm + đổi tên), tên được kiểm tra bằng regex nghiêm ngặt, đường dẫn đã phân giải được kiểm tra phạm vi và nội dung bị giới hạn ở 256&nbsp;KiB. Mỗi lần ghi đè hoặc xóa tạo một bản sao dưới <code>cc-config-backups/</code> — ngoài thư mục Claude Code quét — nên nội dung đã xóa không thể xuất hiện lại như mục ma.',
  "The Keybindings tab includes a structured inline editor for <code>~/.claude/keybindings.json</code>. Add or remove contexts and bindings, edit keys and actions in place, and save via <code>PUT /api/cc-config/keybindings</code>. The server read-modify-writes the file: top-level metadata (<code>$schema</code>, <code>$docs</code>) is preserved, duplicate contexts/keys are rejected, and the file is backed up first. Unlike <code>settings.json</code>, the live CLI does not rewrite keybindings mid-session, so dashboard edits are safe.":
    "Tab Phím tắt có trình chỉnh sửa nội tuyến có cấu trúc cho <code>~/.claude/keybindings.json</code>. Thêm hoặc xóa ngữ cảnh và binding, sửa phím và hành động tại chỗ rồi lưu qua <code>PUT /api/cc-config/keybindings</code>. Server đọc-sửa-ghi tệp: giữ metadata cấp cao nhất (<code>$schema</code>, <code>$docs</code>), từ chối ngữ cảnh/phím trùng và sao lưu trước. Khác với <code>settings.json</code>, CLI đang chạy không ghi lại phím tắt giữa phiên nên chỉnh sửa từ bảng điều khiển là an toàn.",
  "The Memory tab surfaces user + project <code>CLAUDE.md</code> and the per-project file-based memory store — every <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+). Files are grouped by project in collapsible sections, searchable, and editable. Clickable index links in <code>MEMORY.md</code> jump to (scroll + highlight) the matching fact file.":
    "Tab Bộ nhớ hiển thị <code>CLAUDE.md</code> của người dùng và dự án cùng kho bộ nhớ theo tệp của từng dự án — mọi <code>*.md</code> dưới <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (một chỉ mục <code>MEMORY.md</code> và một tệp cho mỗi thông tin đã nhớ, thường hơn 100). Tệp được nhóm theo dự án trong các phần có thể thu gọn, tìm kiếm và chỉnh sửa. Liên kết chỉ mục trong <code>MEMORY.md</code> sẽ cuộn tới và tô sáng tệp thông tin tương ứng.",
  "<strong>Plugins</strong>, <strong>MCP servers</strong>, <strong>hooks-in-settings</strong>, and live <strong>settings.json</strong> files are intentionally not writable from the dashboard — they race with the running <code>claude</code> CLI. Those tabs show explainer banners with copy-able <code>claude plugin …</code>, <code>claude mcp …</code>, and <code>claude config …</code> commands instead of edit buttons. Secret-like settings keys are redacted server-side.":
    "<strong>Plugin</strong>, <strong>MCP Server</strong>, <strong>Hook trong cài đặt</strong> và tệp <strong>settings.json</strong> trực tiếp cố ý không thể ghi từ bảng điều khiển vì chúng cạnh tranh với CLI <code>claude</code> đang chạy. Các tab này hiển thị biểu ngữ giải thích cùng lệnh <code>claude plugin …</code>, <code>claude mcp …</code> và <code>claude config …</code> có thể sao chép thay cho nút sửa. Các khóa giống Secret được che ở server.",
  "Successful mutations broadcast <code>cc_config_changed</code> over the WebSocket so every open <code>/cc-config</code> tab refetches without polling. A filesystem watcher on <code>CLAUDE_HOME</code> (<code>cc-watcher.js</code>) detects external edits (e.g. you changed a skill in your editor) and triggers the same refresh path. The Backups modal lists every timestamped copy and builds <code>mv</code> restore commands.":
    "Thay đổi thành công phát <code>cc_config_changed</code> qua WebSocket để mọi tab <code>/cc-config</code> đang mở tải lại mà không cần polling. Watcher hệ thống tệp trên <code>CLAUDE_HOME</code> (<code>cc-watcher.js</code>) phát hiện chỉnh sửa ngoài (ví dụ đổi skill trong editor) và kích hoạt cùng đường làm mới. Hộp thoại Sao lưu liệt kê mọi bản có dấu thời gian và tạo lệnh khôi phục <code>mv</code>.",
  "How to change (if read-only)": "Cách thay đổi (nếu chỉ đọc)",
  "Filesystem roots + aggregate counts across every surface":
    "Thư mục gốc và số liệu tổng hợp trên mọi bề mặt",
  "Single-file viewer (path-contained to allowed roots)":
    "Trình xem một tệp (đường dẫn bị giới hạn trong thư mục gốc cho phép)",
  "Create/overwrite a text artifact — backs up first, atomic write":
    "Tạo hoặc ghi đè nội dung văn bản — sao lưu trước, ghi nguyên tử",
  "Timestamped backup listing for the recovery modal":
    "Danh sách bản sao lưu có dấu thời gian cho hộp thoại khôi phục",
  "Every mutation writes a timestamped copy under <code>&lt;root&gt;/cc-config-backups/&lt;type&gt;/</code> (or <code>&lt;memory-dir&gt;/.cc-config-backups/auto-memory/</code> for per-project memory files) <em>before</em> touching the original. Skill folders are copied whole so bundled assets survive in the backup. The UI's Backups modal shows every copy and prints a one-line <code>mv</code> restore command.":
    "Mỗi thay đổi ghi một bản sao có dấu thời gian dưới <code>&lt;root&gt;/cc-config-backups/&lt;type&gt;/</code> (hoặc <code>&lt;memory-dir&gt;/.cc-config-backups/auto-memory/</code> cho bộ nhớ theo dự án) <em>trước khi</em> chạm vào bản gốc. Thư mục skill được sao chép toàn bộ để tài nguyên đi kèm còn nguyên trong bản sao lưu. Hộp thoại Sao lưu hiển thị mọi bản và in lệnh khôi phục <code>mv</code> một dòng.",
  "Do not edit settings.json from here": "Không chỉnh sửa settings.json tại đây",
  "The live <code>claude</code> process rewrites <code>settings.json</code> and <code>~/.claude.json</code> constantly (model changes, plugin toggles, MCP registration). A dashboard write would race and lose state — or worse, break hook ingestion. Use <code>claude config set …</code>, <code>/config</code>, or hand-edit only when no <code>claude</code> process is running.":
    "Tiến trình <code>claude</code> trực tiếp liên tục ghi lại <code>settings.json</code> và <code>~/.claude.json</code> (đổi mô hình, bật/tắt plugin, đăng ký MCP). Ghi từ bảng điều khiển sẽ tạo tranh chấp và mất trạng thái, thậm chí làm hỏng việc nhận Hook. Hãy dùng <code>claude config set …</code>, <code>/config</code> hoặc chỉ sửa tay khi không có tiến trình <code>claude</code> nào chạy.",
  '<span class="caption-icon">🔥</span> <span><strong>Prometheus · CCAM console</strong> — static HTML at <code>/consoles/index.html</code> (Prometheus 3.x compatible): live metric cards, session/token tables, and one-click Graph drill-down links that query the Prometheus HTTP API directly</span>':
    '<span class="caption-icon">🔥</span> <span><strong>Prometheus · bảng điều khiển CCAM</strong> — HTML tĩnh tại <code>/consoles/index.html</code> (tương thích Prometheus 3.x): thẻ số liệu trực tiếp, bảng phiên/Token và liên kết Graph một nhấp truy vấn trực tiếp Prometheus HTTP API</span>',
  '<span class="caption-icon">📈</span> <span><strong>Prometheus · Graph</strong> — ad-hoc PromQL against scraped CCAM series; starter expressions ship in the CCAM console and <code>monitoring/README.md</code>, with derived rates in <code>monitoring/prometheus/ccam-rules.yml</code></span>':
    '<span class="caption-icon">📈</span> <span><strong>Prometheus · Graph</strong> — PromQL tùy ý trên chuỗi CCAM đã thu thập; biểu thức mẫu nằm trong bảng điều khiển CCAM và <code>monitoring/README.md</code>, còn tốc độ dẫn xuất ở <code>monitoring/prometheus/ccam-rules.yml</code></span>',
  "<code>monitoring/prometheus/ccam-rules.yml</code> derives convenient rates and aggregates from raw <code>ccam_*</code> series (e.g. <code>rate(ccam_tokens_total[5m])</code>) so Grafana panels and ad-hoc Graph queries stay fast without hand-rolling every expression.":
    "<code>monitoring/prometheus/ccam-rules.yml</code> tạo sẵn tốc độ và tổng hợp tiện dụng từ chuỗi <code>ccam_*</code> thô (ví dụ <code>rate(ccam_tokens_total[5m])</code>) để bảng Grafana và truy vấn Graph tùy ý luôn nhanh mà không phải tự viết từng biểu thức.",
});

Object.assign(window.__WIKI_CONTENT_I18N.ko, {
  "Rootless Docker and Podman stacks cover the dashboard, authenticated MCP, Nginx, Prometheus, and Grafana. Read-only root filesystems, file-backed secrets, health checks, WebSocket proxying, private metrics, backup tooling, and one-writer SQLite safeguards ship together.":
    "루트리스 Docker 및 Podman 스택은 대시보드, 인증된 MCP, Nginx, Prometheus, Grafana를 포괄합니다. 읽기 전용 루트 파일 시스템, 파일 기반 Secret, 상태 검사, WebSocket 프록시, 비공개 메트릭, 백업 도구, 단일 작성자 SQLite 보호 장치가 함께 제공됩니다.",
  '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — searchable, filterable, server-paginated table. Rows with task state add a progress donut beside status; hover or focus opens a detailed task preview. Click any row to drill into session detail</span>':
    '<span class="caption-icon">📂</span> <span><strong>세션</strong> — 검색, 필터링, 서버 페이지네이션을 지원하는 표입니다. 작업 상태가 있는 행은 상태 옆에 진행률 도넛을 표시하며, 마우스를 올리거나 포커스하면 상세 작업 미리보기가 열립니다. 행을 클릭하면 세션 상세로 이동합니다</span>',
  '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Skills</strong> — the Skills tab lists every discovered skill (user, project, and plugin) with its description and source, is searchable across the whole set, and opens any skill file for a safe, timestamp-backed edit</span>':
    '<span class="caption-icon">🧩</span> <span><strong>Claude 구성 탐색기 · 스킬</strong> — 스킬 탭은 발견된 모든 사용자, 프로젝트, 플러그인 스킬과 설명 및 출처를 나열하고 전체 검색을 지원하며, 어떤 스킬 파일이든 타임스탬프 백업 후 안전하게 편집할 수 있도록 엽니다</span>',
  '<span class="caption-icon">🔥</span> <span><strong>Observability · Prometheus console</strong> — pre-built landing page at <code>/consoles/index.html</code> with live metric cards, session/token tables, and drill-down links into the Graph UI (Prometheus 3.x compatible — queries the HTTP API directly)</span>':
    '<span class="caption-icon">🔥</span> <span><strong>관측성 · Prometheus 콘솔</strong> — <code>/consoles/index.html</code>에 제공되는 사전 구성 페이지로, 실시간 메트릭 카드, 세션/Token 표, Graph UI 상세 링크를 포함합니다(Prometheus 3.x 호환 — HTTP API 직접 조회)</span>',
  '<span class="caption-icon">📈</span> <span><strong>Observability · Prometheus Graph</strong> — run PromQL against scraped CCAM series (e.g. <code>sum(ccam_sessions)</code>, <code>ccam_events_total</code>, <code>rate(ccam_tokens_total[5m])</code>) with starter expressions from the CCAM console and recording rules in <code>monitoring/prometheus/ccam-rules.yml</code></span>':
    '<span class="caption-icon">📈</span> <span><strong>관측성 · Prometheus Graph</strong> — CCAM 콘솔의 시작 표현식과 <code>monitoring/prometheus/ccam-rules.yml</code> 기록 규칙을 사용해 수집된 CCAM 시계열에 PromQL을 실행합니다(예: <code>sum(ccam_sessions)</code>, <code>ccam_events_total</code>, <code>rate(ccam_tokens_total[5m])</code>)</span>',
  "Matched subagent → <span class=\"status-chip chip-completed\"><span class=\"chip-dot\"></span>Completed</span>. Deliberately does <strong>not</strong> clear the waiting flag — a backgrounded subagent finishing tells us nothing about the human. Also kicks off a fire-and-forget JSONL scan (<code>scanAndImportSubagents</code>) that walks the session's <code>subagents/agent-*.jsonl</code> files, pairs <code>tool_use</code> ↔ <code>tool_result</code> blocks by <code>tool_use_id</code>, and emits per-tool <code>PreToolUse</code> + <code>PostToolUse</code> events under each subagent's own <code>agent_id</code> — surfaces tool calls that subagents make internally and which never fire any hooks. The same scan also rebuilds the nested-subagent hierarchy — it repoints each subagent's <code>parent_agent_id</code> to the true spawner recovered from the transcript's Task tool result (<code>toolUseResult.agentId</code>), so subagents that spawn their own subagents nest correctly instead of flattening under main.":
    '일치하는 Subagent → <span class="status-chip chip-completed"><span class="chip-dot"></span>완료</span>. 백그라운드 Subagent의 완료만으로 사용자의 상태를 알 수 없으므로 대기 플래그를 의도적으로 <strong>지우지 않습니다</strong>. 또한 세션의 <code>subagents/agent-*.jsonl</code> 파일을 순회하고 <code>tool_use_id</code>로 <code>tool_use</code> ↔ <code>tool_result</code> 블록을 짝지은 뒤 각 Subagent의 <code>agent_id</code> 아래에 도구별 <code>PreToolUse</code> + <code>PostToolUse</code> 이벤트를 내보내는 비동기 JSONL 스캔(<code>scanAndImportSubagents</code>)을 시작합니다. 이로써 Hook을 발생시키지 않는 Subagent 내부 도구 호출도 표시됩니다. 같은 스캔은 Transcript의 Task 도구 결과(<code>toolUseResult.agentId</code>)에서 실제 생성자를 복원해 각 Subagent의 <code>parent_agent_id</code>를 다시 지정하므로, Subagent가 생성한 Subagent가 메인 아래에 평면화되지 않고 올바르게 중첩됩니다.',
  "Protects MCP <code>/mcp</code>, <code>/sse</code>, and <code>/messages</code>; <code>/health</code> remains available to probes":
    "MCP <code>/mcp</code>, <code>/sse</code>, <code>/messages</code>를 보호하며 <code>/health</code>는 상태 검사에 계속 제공됩니다",
  "File-backed dashboard REST and WebSocket token for Docker or Kubernetes secrets":
    "Docker 또는 Kubernetes Secret용 파일 기반 대시보드 REST 및 WebSocket Token",
  "Independent credential for authenticated remote <code>/api/hooks/*</code> ingestion":
    "인증된 원격 <code>/api/hooks/*</code> 수집을 위한 독립 자격 증명",
  "Writable dotenv path used when Settings persists Claude or Codex home overrides":
    "설정에서 Claude 또는 Codex 홈 재정의를 저장할 때 사용하는 쓰기 가능한 dotenv 경로",
  "Optional remote hook destination; non-loopback URLs require HTTPS and a hook token":
    "선택적 원격 Hook 대상이며 루프백이 아닌 URL에는 HTTPS와 Hook Token이 필요합니다",
  "System info (DB size, row counts, hook status, server uptime). Data export as one versioned JSON bundle and matching import/restore (<code>POST /api/settings/import</code> via <code>server/lib/data-transfer.js</code> — idempotent, session-atomic, non-destructive; consolidate machines). Session cleanup (abandon stale active sessions, purge old completed sessions). Clear all data. Reset pricing to defaults. Reinstall hooks.":
    "시스템 정보(DB 크기, 행 수, Hook 상태, 서버 가동 시간). 데이터를 하나의 버전 지정 JSON 번들로 내보내고 대응하는 가져오기/복원 기능을 제공합니다(<code>server/lib/data-transfer.js</code>를 통한 <code>POST /api/settings/import</code> — 멱등성, 세션 원자성, 비파괴적 처리, 여러 머신 통합). 세션 정리(오래 멈춘 활성 세션 포기, 오래된 완료 세션 제거). 모든 데이터 삭제. 가격 기본값 복원. Hook 재설치.",
  "Export all data (sessions, agents, events, tokens, workflows, runs, alert rules, pricing) as one versioned JSON download":
    "모든 데이터(세션, Agent, 이벤트, Token, 워크플로, 실행, 알림 규칙, 가격)를 하나의 버전 지정 JSON 파일로 내보냅니다",
  '<span class="caption-icon">🛰️</span> <span>Settings → Remote Data Sources — add machines by SSH destination, test connectivity, sync on demand or on a background schedule, and narrow the whole app with the global data-scope selector</span>':
    '<span class="caption-icon">🛰️</span> <span>설정 → 원격 데이터 소스 — SSH 대상으로 머신을 추가하고 연결을 테스트하며 요청 시 또는 백그라운드 일정에 따라 동기화하고 전역 데이터 범위 선택기로 앱 전체를 필터링합니다</span>',
  '<span class="caption-icon">🧰</span> <span><strong>Claude Config Explorer</strong> — the <code>/cc-config</code> page: 12 tabs (skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, keybindings, settings, memory), a scope filter (user / project / all), search, and safe edit affordances on the low-risk surfaces</span>':
    '<span class="caption-icon">🧰</span> <span><strong>Claude 구성 탐색기</strong> — <code>/cc-config</code> 페이지는 12개 탭(스킬, Subagent, 슬래시 명령, 출력 스타일, 플러그인, 마켓플레이스, MCP Server, Hook, 키 바인딩, 설정, 메모리), 범위 필터(사용자 / 프로젝트 / 전체), 검색, 저위험 구성의 안전한 편집 기능을 제공합니다</span>',
  '<span class="caption-icon">🧩</span> <span><strong>Claude Config Explorer · Skills</strong> — the Skills tab lists every discovered skill (user, project, and plugin) with description and source, is searchable, and opens any <code>SKILL.md</code> for a timestamp-backed edit via the shared editor modal</span>':
    '<span class="caption-icon">🧩</span> <span><strong>Claude 구성 탐색기 · 스킬</strong> — 스킬 탭은 발견된 모든 사용자, 프로젝트, 플러그인 스킬과 설명 및 출처를 나열하고 검색을 지원하며, 공유 편집 모달에서 어떤 <code>SKILL.md</code>든 타임스탬프 백업 후 편집할 수 있도록 엽니다</span>',
  "A first-class <strong>configuration inspector</strong> at <code>/cc-config</code> that reads every surface Claude Code knows about — skills, subagents, slash commands, output styles, plugins, marketplaces, MCP servers, hooks, settings, memory, keybindings, and statusline scripts — from the same on-disk layout the CLI uses. The Overview tab summarizes counts and filesystem roots; every other tab drills into one surface with search, scope filtering (<b>user</b> / <b>project</b> / <b>all</b>), and — where safe — create / edit / delete with <strong>mandatory timestamped backups</strong> before every mutation. A <code>cc-watcher</code> on <code>CLAUDE_HOME</code> plus <code>cc_config_changed</code> WebSocket messages keep the page live when files change outside the dashboard.":
    "<code>/cc-config</code>의 일급 <strong>구성 검사기</strong>는 CLI와 같은 디스크 레이아웃에서 Claude Code의 모든 구성 영역 — 스킬, Subagent, 슬래시 명령, 출력 스타일, 플러그인, 마켓플레이스, MCP Server, Hook, 설정, 메모리, 키 바인딩, 상태 표시줄 스크립트 — 을 읽습니다. 개요 탭은 수와 파일 시스템 루트를 요약하고 다른 탭은 검색과 범위 필터(<b>사용자</b> / <b>프로젝트</b> / <b>전체</b>)로 각 영역을 자세히 보여 주며, 안전한 경우 모든 변경 전에 <strong>필수 타임스탬프 백업</strong>을 만든 뒤 생성 / 편집 / 삭제를 제공합니다. <code>CLAUDE_HOME</code>의 <code>cc-watcher</code>와 <code>cc_config_changed</code> WebSocket 메시지는 대시보드 외부에서 파일이 바뀌어도 페이지를 실시간으로 유지합니다.",
  "<strong>Skills</strong>, <strong>Subagents</strong>, <strong>Slash commands</strong>, <strong>Output styles</strong>, <strong>Plugins</strong> (with per-plugin <code>contributes</code> counts and <code>plugin.json</code> metadata), <strong>Marketplaces</strong>, <strong>MCP servers</strong>, <strong>Hooks</strong> (plus <code>~/.claude/hooks/</code> script listing), <strong>Keybindings</strong>, <strong>Settings</strong> (Current configuration summary + per-file JSON), and <strong>Memory</strong> (user/project <code>CLAUDE.md</code> + per-project auto-memory). Statusline config and script previews live under Settings.":
    "<strong>스킬</strong>, <strong>Subagent</strong>, <strong>슬래시 명령</strong>, <strong>출력 스타일</strong>, <strong>플러그인</strong>(플러그인별 <code>contributes</code> 수와 <code>plugin.json</code> 메타데이터), <strong>마켓플레이스</strong>, <strong>MCP Server</strong>, <strong>Hook</strong>(<code>~/.claude/hooks/</code> 스크립트 목록 포함), <strong>키 바인딩</strong>, <strong>설정</strong>(현재 구성 요약 + 파일별 JSON), <strong>메모리</strong>(사용자/프로젝트 <code>CLAUDE.md</code> + 프로젝트별 자동 메모리)를 제공합니다. 상태 표시줄 구성과 스크립트 미리보기는 설정 아래에 있습니다.",
  '<code>PUT</code> / <code>DELETE /api/cc-config/file</code> handles skills, agents, commands, output-styles, <code>CLAUDE.md</code>, and per-project auto-memory (<code>scope: "auto-memory"</code>). Writes are <strong>atomic</strong> (temp file + rename), names are validated against a strict regex, resolved paths are containment-checked, and content is capped at 256&nbsp;KiB. Every overwrite or delete creates a backup under <code>cc-config-backups/</code> — outside the directories Claude Code scans — so deleted artifacts cannot reappear as ghost entries.':
    '<code>PUT</code> / <code>DELETE /api/cc-config/file</code>은 스킬, Agent, 명령, 출력 스타일, <code>CLAUDE.md</code>, 프로젝트별 자동 메모리(<code>scope: "auto-memory"</code>)를 처리합니다. 쓰기는 <strong>원자적</strong>이며(임시 파일 + 이름 변경), 이름은 엄격한 정규식으로 검증하고 해석된 경로는 허용 루트 안인지 확인하며 내용은 256&nbsp;KiB로 제한합니다. 덮어쓰기나 삭제마다 Claude Code 검색 경로 밖의 <code>cc-config-backups/</code>에 백업을 만들어 삭제된 항목이 유령 항목으로 다시 나타나지 않게 합니다.',
  "The Keybindings tab includes a structured inline editor for <code>~/.claude/keybindings.json</code>. Add or remove contexts and bindings, edit keys and actions in place, and save via <code>PUT /api/cc-config/keybindings</code>. The server read-modify-writes the file: top-level metadata (<code>$schema</code>, <code>$docs</code>) is preserved, duplicate contexts/keys are rejected, and the file is backed up first. Unlike <code>settings.json</code>, the live CLI does not rewrite keybindings mid-session, so dashboard edits are safe.":
    "키 바인딩 탭에는 <code>~/.claude/keybindings.json</code>용 구조화된 인라인 편집기가 있습니다. 컨텍스트와 바인딩을 추가 또는 제거하고 키와 동작을 직접 편집한 뒤 <code>PUT /api/cc-config/keybindings</code>로 저장합니다. 서버는 파일을 읽고 수정해 다시 쓰며, 최상위 메타데이터(<code>$schema</code>, <code>$docs</code>)를 보존하고 중복 컨텍스트/키를 거부하며 먼저 백업합니다. <code>settings.json</code>과 달리 실행 중인 CLI가 세션 도중 키 바인딩을 다시 쓰지 않으므로 대시보드 편집이 안전합니다.",
  "The Memory tab surfaces user + project <code>CLAUDE.md</code> and the per-project file-based memory store — every <code>*.md</code> under <code>~/.claude/projects/&lt;slug&gt;/memory/</code> (a <code>MEMORY.md</code> index plus one file per remembered fact, often 100+). Files are grouped by project in collapsible sections, searchable, and editable. Clickable index links in <code>MEMORY.md</code> jump to (scroll + highlight) the matching fact file.":
    "메모리 탭은 사용자 및 프로젝트 <code>CLAUDE.md</code>와 프로젝트별 파일 기반 메모리 저장소 — <code>~/.claude/projects/&lt;slug&gt;/memory/</code> 아래의 모든 <code>*.md</code>(<code>MEMORY.md</code> 인덱스와 기억 항목별 파일, 흔히 100개 이상) — 를 표시합니다. 파일은 프로젝트별 접이식 섹션으로 묶이고 검색 및 편집할 수 있습니다. <code>MEMORY.md</code>의 인덱스 링크를 클릭하면 해당 기억 파일로 스크롤하고 강조합니다.",
  "<strong>Plugins</strong>, <strong>MCP servers</strong>, <strong>hooks-in-settings</strong>, and live <strong>settings.json</strong> files are intentionally not writable from the dashboard — they race with the running <code>claude</code> CLI. Those tabs show explainer banners with copy-able <code>claude plugin …</code>, <code>claude mcp …</code>, and <code>claude config …</code> commands instead of edit buttons. Secret-like settings keys are redacted server-side.":
    "<strong>플러그인</strong>, <strong>MCP Server</strong>, <strong>설정 내 Hook</strong>, 실시간 <strong>settings.json</strong> 파일은 실행 중인 <code>claude</code> CLI와 경쟁하므로 대시보드에서 의도적으로 쓸 수 없습니다. 해당 탭은 편집 버튼 대신 복사 가능한 <code>claude plugin …</code>, <code>claude mcp …</code>, <code>claude config …</code> 명령이 있는 설명 배너를 보여 줍니다. Secret과 유사한 설정 키는 서버에서 마스킹합니다.",
  "Successful mutations broadcast <code>cc_config_changed</code> over the WebSocket so every open <code>/cc-config</code> tab refetches without polling. A filesystem watcher on <code>CLAUDE_HOME</code> (<code>cc-watcher.js</code>) detects external edits (e.g. you changed a skill in your editor) and triggers the same refresh path. The Backups modal lists every timestamped copy and builds <code>mv</code> restore commands.":
    "변경이 성공하면 WebSocket으로 <code>cc_config_changed</code>를 브로드캐스트해 열린 모든 <code>/cc-config</code> 탭이 폴링 없이 다시 가져옵니다. <code>CLAUDE_HOME</code>의 파일 시스템 watcher(<code>cc-watcher.js</code>)는 외부 편집(예: 편집기에서 스킬 변경)을 감지해 같은 새로 고침 경로를 실행합니다. 백업 모달은 모든 타임스탬프 사본을 나열하고 <code>mv</code> 복원 명령을 만듭니다.",
  "How to change (if read-only)": "변경 방법(읽기 전용인 경우)",
  "Filesystem roots + aggregate counts across every surface":
    "모든 구성 영역의 파일 시스템 루트와 집계 수",
  "Single-file viewer (path-contained to allowed roots)":
    "단일 파일 뷰어(허용된 루트 안으로 경로 제한)",
  "Create/overwrite a text artifact — backs up first, atomic write":
    "텍스트 항목 생성 또는 덮어쓰기 — 먼저 백업한 뒤 원자적 쓰기",
  "Timestamped backup listing for the recovery modal": "복구 모달용 타임스탬프 백업 목록",
  "Every mutation writes a timestamped copy under <code>&lt;root&gt;/cc-config-backups/&lt;type&gt;/</code> (or <code>&lt;memory-dir&gt;/.cc-config-backups/auto-memory/</code> for per-project memory files) <em>before</em> touching the original. Skill folders are copied whole so bundled assets survive in the backup. The UI's Backups modal shows every copy and prints a one-line <code>mv</code> restore command.":
    "모든 변경은 원본을 건드리기 <em>전에</em> <code>&lt;root&gt;/cc-config-backups/&lt;type&gt;/</code>(프로젝트별 메모리 파일은 <code>&lt;memory-dir&gt;/.cc-config-backups/auto-memory/</code>) 아래에 타임스탬프 사본을 씁니다. 스킬 폴더는 전체 복사되어 번들 자산도 백업에 남습니다. UI 백업 모달은 모든 사본을 표시하고 한 줄짜리 <code>mv</code> 복원 명령을 출력합니다.",
  "Do not edit settings.json from here": "여기서 settings.json을 편집하지 마세요",
  "The live <code>claude</code> process rewrites <code>settings.json</code> and <code>~/.claude.json</code> constantly (model changes, plugin toggles, MCP registration). A dashboard write would race and lose state — or worse, break hook ingestion. Use <code>claude config set …</code>, <code>/config</code>, or hand-edit only when no <code>claude</code> process is running.":
    "실행 중인 <code>claude</code> 프로세스는 <code>settings.json</code>과 <code>~/.claude.json</code>을 계속 다시 씁니다(모델 변경, 플러그인 토글, MCP 등록). 대시보드 쓰기는 경쟁 상태로 데이터를 잃거나 Hook 수집을 망가뜨릴 수 있습니다. <code>claude config set …</code>, <code>/config</code>를 사용하거나 <code>claude</code> 프로세스가 없을 때만 직접 편집하세요.",
  '<span class="caption-icon">🔥</span> <span><strong>Prometheus · CCAM console</strong> — static HTML at <code>/consoles/index.html</code> (Prometheus 3.x compatible): live metric cards, session/token tables, and one-click Graph drill-down links that query the Prometheus HTTP API directly</span>':
    '<span class="caption-icon">🔥</span> <span><strong>Prometheus · CCAM 콘솔</strong> — <code>/consoles/index.html</code>의 정적 HTML(Prometheus 3.x 호환)로 실시간 메트릭 카드, 세션/Token 표, Prometheus HTTP API를 직접 조회하는 원클릭 Graph 상세 링크를 제공합니다</span>',
  '<span class="caption-icon">📈</span> <span><strong>Prometheus · Graph</strong> — ad-hoc PromQL against scraped CCAM series; starter expressions ship in the CCAM console and <code>monitoring/README.md</code>, with derived rates in <code>monitoring/prometheus/ccam-rules.yml</code></span>':
    '<span class="caption-icon">📈</span> <span><strong>Prometheus · Graph</strong> — 수집된 CCAM 시계열에 임시 PromQL을 실행합니다. 시작 표현식은 CCAM 콘솔과 <code>monitoring/README.md</code>에, 파생 비율은 <code>monitoring/prometheus/ccam-rules.yml</code>에 제공됩니다</span>',
  "<code>monitoring/prometheus/ccam-rules.yml</code> derives convenient rates and aggregates from raw <code>ccam_*</code> series (e.g. <code>rate(ccam_tokens_total[5m])</code>) so Grafana panels and ad-hoc Graph queries stay fast without hand-rolling every expression.":
    "<code>monitoring/prometheus/ccam-rules.yml</code>은 원시 <code>ccam_*</code> 시계열에서 편리한 비율과 집계를 생성해(예: <code>rate(ccam_tokens_total[5m])</code>) 모든 표현식을 직접 만들지 않아도 Grafana 패널과 임시 Graph 쿼리를 빠르게 유지합니다.",
  "The OCI runtime is non-root, uses Tini as PID 1, includes Git, OpenSSH, and SQLite, and becomes read-only except for persistent data/config volumes and tmpfs. Docker Compose and Podman Compose use the same file.":
    "OCI 런타임은 비루트로 실행되고 Tini를 PID 1로 사용하며 Git, OpenSSH, SQLite를 포함합니다. 영구 데이터/구성 볼륨과 tmpfs를 제외한 파일 시스템은 읽기 전용입니다. Docker Compose와 Podman Compose는 같은 파일을 사용합니다.",
  "Dashboard, MCP, Nginx, Prometheus, and Grafana bind host loopback by default. Nginx proxies the UI, authenticated API, and WebSocket, while hooks, metrics, and MCP remain blocked at the edge unless explicitly enabled.":
    "대시보드, MCP, Nginx, Prometheus, Grafana는 기본적으로 호스트 루프백에 바인딩됩니다. Nginx는 UI, 인증된 API, WebSocket을 프록시하며 Hook, 메트릭, MCP는 명시적으로 활성화하지 않는 한 엣지에서 차단됩니다.",
  "Run exactly one dashboard container per data volume. Install hooks on the host. Remote hook destinations must use HTTPS plus <code>CCAM_HOOK_TOKEN</code>.":
    "데이터 볼륨마다 대시보드 컨테이너를 정확히 하나만 실행하세요. Hook은 호스트에 설치하고 원격 Hook 대상에는 HTTPS와 <code>CCAM_HOOK_TOKEN</code>을 사용해야 합니다.",
  "The dashboard image runs as UID/GID 1000 with a read-only root filesystem, dropped capabilities, <code>no-new-privileges</code>, health checks, and persistent <code>/app/data</code> plus <code>/app/config</code> volumes. Claude and Codex homes mount read-only at <code>/home/node/.claude</code> and <code>/home/node/.codex</code>.":
    "대시보드 이미지는 UID/GID 1000, 읽기 전용 루트 파일 시스템, 제거된 capability, <code>no-new-privileges</code>, 상태 검사, 영구 <code>/app/data</code> 및 <code>/app/config</code> 볼륨으로 실행됩니다. Claude와 Codex 홈은 <code>/home/node/.claude</code> 및 <code>/home/node/.codex</code>에 읽기 전용으로 마운트됩니다.",
  'The optional <code>agent-runtime</code> image target adds pinned Claude Code and Codex CLIs for container-native Run Agent workflows. Cloud deployments use one Recreate-managed dashboard replica on a retained ReadWriteOnce PVC through Helm, Kustomize, or Terraform-to-existing-Kubernetes. See <a href="../DEPLOYMENT.md">DEPLOYMENT.md</a>.':
    '선택적 <code>agent-runtime</code> 이미지 대상은 컨테이너 네이티브 Agent 실행 워크플로를 위해 버전이 고정된 Claude Code 및 Codex CLI를 추가합니다. 클라우드 배포는 Helm, Kustomize 또는 기존 Kubernetes용 Terraform을 통해 보존된 ReadWriteOnce PVC에 Recreate 방식으로 관리되는 대시보드 복제본 하나를 사용합니다. <a href="../DEPLOYMENT.md">DEPLOYMENT.md</a>를 참조하세요.',
});

Object.assign(window.__WIKI_CONTENT_I18N.es, {
  "Rootless Docker and Podman stacks cover the dashboard, authenticated MCP, Nginx, Prometheus, and Grafana. Read-only root filesystems, file-backed secrets, health checks, WebSocket proxying, private metrics, backup tooling, and one-writer SQLite safeguards ship together.":
    "Las pilas rootless de Docker y Podman incluyen el panel, MCP autenticado, Nginx, Prometheus y Grafana. También incluyen sistemas raíz de solo lectura, secretos respaldados por archivos, comprobaciones de estado, proxy WebSocket, métricas privadas, copias de seguridad y protección de SQLite con un solo escritor.",
  '<span class="caption-icon">📂</span> <span><strong>Sessions</strong> — searchable, filterable, server-paginated table. Rows with task state add a progress donut beside status; hover or focus opens a detailed task preview. Click any row to drill into session detail</span>':
    '<span class="caption-icon">📂</span> <span><strong>Sesiones</strong> — tabla con búsqueda, filtros y paginación del servidor. Las filas con estado de tarea muestran un indicador circular de progreso junto al estado; al pasar el cursor o enfocar se abre una vista previa detallada. Haz clic en cualquier fila para ver los detalles de la sesión</span>',
  "Protects MCP <code>/mcp</code>, <code>/sse</code>, and <code>/messages</code>; <code>/health</code> remains available to probes":
    "Protege los endpoints MCP <code>/mcp</code>, <code>/sse</code> y <code>/messages</code>; <code>/health</code> permanece disponible para las sondas",
  "File-backed dashboard REST and WebSocket token for Docker or Kubernetes secrets":
    "Token REST y WebSocket del panel respaldado por archivo para secretos de Docker o Kubernetes",
  "Independent credential for authenticated remote <code>/api/hooks/*</code> ingestion":
    "Credencial independiente para la recepción remota autenticada en <code>/api/hooks/*</code>",
  "Writable dotenv path used when Settings persists Claude or Codex home overrides":
    "Ruta dotenv escribible que usa Configuración al guardar reemplazos de los directorios de Claude o Codex",
  "Optional remote hook destination; non-loopback URLs require HTTPS and a hook token":
    "Destino remoto opcional para hooks; las URL que no son de loopback requieren HTTPS y un token de hook",
  "The OCI runtime is non-root, uses Tini as PID 1, includes Git, OpenSSH, and SQLite, and becomes read-only except for persistent data/config volumes and tmpfs. Docker Compose and Podman Compose use the same file.":
    "El entorno OCI se ejecuta sin privilegios root, usa Tini como PID 1, incluye Git, OpenSSH y SQLite, y es de solo lectura salvo por los volúmenes persistentes de datos y configuración y tmpfs. Docker Compose y Podman Compose usan el mismo archivo.",
  "Dashboard, MCP, Nginx, Prometheus, and Grafana bind host loopback by default. Nginx proxies the UI, authenticated API, and WebSocket, while hooks, metrics, and MCP remain blocked at the edge unless explicitly enabled.":
    "El panel, MCP, Nginx, Prometheus y Grafana se vinculan por defecto al loopback del host. Nginx actúa como proxy de la interfaz, la API autenticada y WebSocket, mientras que los hooks, las métricas y MCP permanecen bloqueados en el perímetro salvo que se habiliten explícitamente.",
  "Run exactly one dashboard container per data volume. Install hooks on the host. Remote hook destinations must use HTTPS plus <code>CCAM_HOOK_TOKEN</code>.":
    "Ejecuta exactamente un contenedor del panel por volumen de datos. Instala los hooks en el host. Los destinos de hooks remotos deben usar HTTPS junto con <code>CCAM_HOOK_TOKEN</code>.",
  "The dashboard image runs as UID/GID 1000 with a read-only root filesystem, dropped capabilities, <code>no-new-privileges</code>, health checks, and persistent <code>/app/data</code> plus <code>/app/config</code> volumes. Claude and Codex homes mount read-only at <code>/home/node/.claude</code> and <code>/home/node/.codex</code>.":
    "La imagen del panel se ejecuta con UID/GID 1000, sistema de archivos raíz de solo lectura, capacidades eliminadas, <code>no-new-privileges</code>, comprobaciones de estado y volúmenes persistentes en <code>/app/data</code> y <code>/app/config</code>. Los directorios de Claude y Codex se montan como solo lectura en <code>/home/node/.claude</code> y <code>/home/node/.codex</code>.",
  'The optional <code>agent-runtime</code> image target adds pinned Claude Code and Codex CLIs for container-native Run Agent workflows. Cloud deployments use one Recreate-managed dashboard replica on a retained ReadWriteOnce PVC through Helm, Kustomize, or Terraform-to-existing-Kubernetes. See <a href="../DEPLOYMENT.md">DEPLOYMENT.md</a>.':
    'El objetivo de imagen opcional <code>agent-runtime</code> añade versiones fijadas de las CLI de Claude Code y Codex para flujos de ejecución de agentes nativos de contenedor. Los despliegues en la nube usan una réplica del panel administrada con Recreate sobre un PVC ReadWriteOnce conservado mediante Helm, Kustomize o Terraform para un Kubernetes existente. Consulta <a href="../DEPLOYMENT.md">DEPLOYMENT.md</a>.',
  "One SQLite writer": "Un escritor de SQLite",
  "hooks, metrics, MCP blocked by default":
    "hooks, métricas y MCP bloqueados de forma predeterminada",
  "private bearer scrape": "extracción privada con token bearer",
  "password file": "archivo de contraseña",
});

Object.assign(window.__WIKI_CONTENT_I18N.plain.zh, {
  "Hook Type": "Hook 类型",
  "Dashboard Action": "仪表盘操作",
  "What you get": "可获得的内容",
  "Service Worker": "服务工作线程",
  "Color Logic": "颜色逻辑",
  "Port choice": "端口选择",
  "How to change (if read-only)": "如何更改（只读时）",
  "Payload format": "负载格式",
  "URL / credentials": "URL / 凭据",
  "Four Grafana boards": "四个 Grafana 仪表盘",
  "Prometheus console": "Prometheus 控制台",
  "Recording rules": "记录规则",
  "Scrape & auth guard": "抓取与认证防护",
  "When it appears": "出现时机",
  "Loopback port": "回环端口",
  "Why This Over Alternatives": "为何选择此方案",
});

Object.assign(window.__WIKI_CONTENT_I18N.plain.vi, {
  "Hook Type": "Loại hook",
  "Dashboard Action": "Hành động trên bảng điều khiển",
  "What you get": "Nội dung nhận được",
  "Service Worker": "Service Worker",
  "Color Logic": "Quy tắc màu sắc",
  "Port choice": "Lựa chọn cổng",
  "How to change (if read-only)": "Cách thay đổi (nếu chỉ đọc)",
  "Payload format": "Định dạng payload",
  "URL / credentials": "URL / thông tin xác thực",
  "Four Grafana boards": "Bốn bảng Grafana",
  "Prometheus console": "Bảng điều khiển Prometheus",
  "Recording rules": "Quy tắc ghi",
  "Scrape & auth guard": "Bảo vệ thu thập và xác thực",
  "When it appears": "Thời điểm xuất hiện",
  "Loopback port": "Cổng loopback",
  "Why This Over Alternatives": "Vì sao chọn giải pháp này",
});

Object.assign(window.__WIKI_CONTENT_I18N.plain.ko, {
  "Hook Type": "Hook 유형",
  "Dashboard Action": "대시보드 동작",
  "What you get": "제공 기능",
  "Service Worker": "서비스 워커",
  "Color Logic": "색상 규칙",
  "Port choice": "포트 선택",
  "How to change (if read-only)": "변경 방법(읽기 전용인 경우)",
  "Payload format": "Payload 형식",
  "URL / credentials": "URL / 자격 증명",
  "Four Grafana boards": "Grafana 대시보드 4개",
  "Prometheus console": "Prometheus 콘솔",
  "Recording rules": "기록 규칙",
  "Scrape & auth guard": "수집 및 인증 보호",
  "When it appears": "표시 시점",
  "Loopback port": "루프백 포트",
  "Why This Over Alternatives": "다른 대안보다 이 방식을 선택한 이유",
});

Object.assign(window.__WIKI_CONTENT_I18N.plain.es, {
  "Hook Type": "Tipo de hook",
  "Dashboard Action": "Acción del panel",
  "What you get": "Qué obtienes",
  "Service Worker": "Service Worker",
  "Color Logic": "Lógica de colores",
  "Port choice": "Elección del puerto",
  "How to change (if read-only)": "Cómo cambiarlo (si es de solo lectura)",
  "Payload format": "Formato del payload",
  "URL / credentials": "URL / credenciales",
  "Four Grafana boards": "Cuatro paneles de Grafana",
  "Prometheus console": "Consola de Prometheus",
  "Recording rules": "Reglas de registro",
  "Scrape & auth guard": "Protección de extracción y autenticación",
  "When it appears": "Cuándo aparece",
  "Loopback port": "Puerto de loopback",
  "Why This Over Alternatives": "Por qué elegirlo frente a las alternativas",
});

// ─── Sound cues (Settings → Sound) ───
Object.assign(window.__WIKI_CONTENT_I18N.zh, {
  "The dashboard gives you <strong>subtle audio feedback</strong> for live session activity, so you can leave it on a second monitor and still hear when a run finishes or fails. Sound is <strong>on by default</strong> and can be switched off entirely in one click from <strong>Settings → Sound</strong>. Like Tabby, it is a purely additive client-side subscriber to the existing <code>eventBus</code> WebSocket stream: <strong>no server code, no new API routes, no new message types, and no new dependencies</strong>.":
    "仪表盘会为实时会话活动提供<strong>轻柔的音频反馈</strong>，因此你可以把它放在副屏上，仍然能听到某次运行完成或失败。声音<strong>默认开启</strong>，也可以在<strong>设置 → 声音</strong>中一键完全关闭。与 Tabby 一样，它只是现有 <code>eventBus</code> WebSocket 流的一个纯附加的客户端订阅者：<strong>没有服务端代码、没有新的 API 路由、没有新的消息类型，也没有新的依赖</strong>。",
  "There are no <code>.mp3</code> or <code>.wav</code> assets anywhere in the repository and no audio library in <code>package.json</code>. Every cue is generated at play time with the <strong>Web Audio API</strong>: a short list of oscillators (sine or triangle), each with its own exponential gain envelope, mixed through a master gain node and a low-pass filter so the result sits behind your work rather than cutting through it. The whole engine is one file, <code>client/src/lib/sound.ts</code>.":
    "仓库中没有任何 <code>.mp3</code> 或 <code>.wav</code> 资源，<code>package.json</code> 里也没有音频库。每个提示音都在播放时由 <strong>Web Audio API</strong> 生成：一小组振荡器（正弦或三角波），各自带指数增益包络，经主增益节点与低通滤波器混合，使声音退到你的工作之后而不是刺穿它。整个引擎只有一个文件：<code>client/src/lib/sound.ts</code>。",
  "A session finishes responding or closes": "会话回复完成或关闭",
  "A session enters the error state": "会话进入错误状态",
  "Soft falling minor third on a triangle wave": "三角波上轻柔的下行小三度",
  "Claude Code emits a notification event": "Claude Code 发出通知事件",
  "Detuned pair ringing like a small bell": "失谐音对，如同小铃铛",
  "The dashboard WebSocket returns or drops": "仪表盘 WebSocket 恢复或断开",
  "You press a button, link, tab, or switch": "你按下按钮、链接、标签页或开关",
  "Cues live inside a C-major set so overlapping tails never sound dissonant, and every envelope decays exponentially rather than cutting off, which avoids the click of a hard stop.":
    "所有提示音都落在 C 大调音集内，因此叠加的尾音不会不协和；每个包络都以指数衰减而非硬切断，避免了硬停带来的爆音。",
  "<strong>Per-cue cooldown</strong> — the same cue will not repeat within about 350 ms (45 ms for the interaction tick).":
    "<strong>单个提示音冷却</strong> — 同一提示音在约 350 ms 内不会重复（交互滴答声为 45 ms）。",
  "<strong>Global burst budget</strong> — at most 4 cues start within any 1.2 s window, so importing history or reconnecting after a drop never turns a flood of WebSocket messages into a flood of beeps.":
    "<strong>全局突发预算</strong> — 任意 1.2 s 窗口内最多启动 4 个提示音，因此导入历史或断线重连时，大量 WebSocket 消息不会变成一连串提示音。",
  "<strong>Autoplay policy</strong> — nothing plays until your first pointer, key, or touch interaction with the page. Cues before that are silently dropped, not queued.":
    "<strong>自动播放策略</strong> — 在你首次对页面进行指针、按键或触摸交互之前不会播放任何声音。之前的提示音会被静默丢弃，而不是排队。",
  "If the browser has no Web Audio support at all, every call is a safe no-op and the dashboard simply stays silent.":
    "如果浏览器完全不支持 Web Audio，每次调用都是安全的空操作，仪表盘只是保持静音。",
  "<strong>Settings → Sound</strong> has a master toggle, a volume slider, and an individual switch for each cue. Flipping a switch on plays that cue immediately so you can hear exactly what you just enabled, and a preview button plays the completion chime on demand. Preferences persist to <code>localStorage</code> under <code>agent-monitor-sound</code> and take effect instantly across the app — no reload. By default, session start, session complete, session error, notifications, and the interaction tick are on; subagent spawns and connection changes are off because they are the chattiest. The wiring lives in <code>client/src/hooks/useSoundCues.ts</code>, mounted once in <code>client/src/App.tsx</code>.":
    "<strong>设置 → 声音</strong>提供总开关、音量滑块，以及每种提示音的独立开关。打开某个开关会立即播放该提示音，让你清楚听到刚刚启用的是什么；试听按钮可随时播放完成提示音。偏好设置保存在 <code>localStorage</code> 的 <code>agent-monitor-sound</code> 键下，在整个应用内即时生效 — 无需重新加载。默认情况下，会话开始、会话完成、会话出错、通知和交互滴答声为开启；子代理启动和连接变化为关闭，因为它们最为频繁。接线逻辑位于 <code>client/src/hooks/useSoundCues.ts</code>，在 <code>client/src/App.tsx</code> 中挂载一次。",
});

Object.assign(window.__WIKI_CONTENT_I18N.plain.zh, {
  "🔊 Sound Cues": "🔊 声音提示",
  "Sound Cues": "声音提示",
  "Zero-dependency synthesis": "零依赖合成",
  "The cues": "提示音一览",
  "Staying out of your way": "不打扰你的工作",
  Cue: "提示音",
  "When it fires": "触发时机",
  "What it sounds like": "听感",
});

Object.assign(window.__WIKI_CONTENT_I18N.vi, {
  "The dashboard gives you <strong>subtle audio feedback</strong> for live session activity, so you can leave it on a second monitor and still hear when a run finishes or fails. Sound is <strong>on by default</strong> and can be switched off entirely in one click from <strong>Settings → Sound</strong>. Like Tabby, it is a purely additive client-side subscriber to the existing <code>eventBus</code> WebSocket stream: <strong>no server code, no new API routes, no new message types, and no new dependencies</strong>.":
    "Dashboard cung cấp <strong>phản hồi âm thanh tinh tế</strong> cho hoạt động phiên trực tiếp, nên bạn có thể để nó ở màn hình phụ mà vẫn nghe được khi một lần chạy hoàn tất hay thất bại. Âm thanh <strong>bật sẵn theo mặc định</strong> và có thể tắt hoàn toàn chỉ bằng một cú nhấp trong <strong>Settings → Sound</strong>. Giống như Tabby, đây thuần túy là một subscriber phía client bổ sung cho luồng WebSocket <code>eventBus</code> sẵn có: <strong>không có mã máy chủ, không có route API mới, không có loại thông điệp mới và không có phụ thuộc mới</strong>.",
  "There are no <code>.mp3</code> or <code>.wav</code> assets anywhere in the repository and no audio library in <code>package.json</code>. Every cue is generated at play time with the <strong>Web Audio API</strong>: a short list of oscillators (sine or triangle), each with its own exponential gain envelope, mixed through a master gain node and a low-pass filter so the result sits behind your work rather than cutting through it. The whole engine is one file, <code>client/src/lib/sound.ts</code>.":
    "Không có tệp <code>.mp3</code> hay <code>.wav</code> nào trong kho mã và không có thư viện âm thanh nào trong <code>package.json</code>. Mọi tín hiệu đều được tạo ra ngay lúc phát bằng <strong>Web Audio API</strong>: một danh sách ngắn các bộ dao động (sine hoặc triangle), mỗi bộ có envelope gain suy giảm theo hàm mũ riêng, trộn qua một node gain tổng và một bộ lọc thông thấp để kết quả nằm phía sau công việc của bạn thay vì xuyên qua nó. Toàn bộ engine gói gọn trong một tệp: <code>client/src/lib/sound.ts</code>.",
  "A session finishes responding or closes": "Một phiên phản hồi xong hoặc đóng lại",
  "A session enters the error state": "Một phiên chuyển sang trạng thái lỗi",
  "Soft falling minor third on a triangle wave":
    "Quãng ba thứ đi xuống nhẹ nhàng trên sóng triangle",
  "Claude Code emits a notification event": "Claude Code phát ra một sự kiện thông báo",
  "Detuned pair ringing like a small bell": "Cặp âm lệch tần ngân như một chiếc chuông nhỏ",
  "The dashboard WebSocket returns or drops": "WebSocket của dashboard trở lại hoặc mất",
  "You press a button, link, tab, or switch": "Bạn nhấn nút, liên kết, tab hoặc công tắc",
  "Cues live inside a C-major set so overlapping tails never sound dissonant, and every envelope decays exponentially rather than cutting off, which avoids the click of a hard stop.":
    'Các tín hiệu nằm trong tập âm Đô trưởng nên phần đuôi âm chồng lấn không bao giờ nghịch tai, và mọi envelope đều suy giảm theo hàm mũ thay vì cắt đột ngột, tránh tiếng "pop" của một điểm dừng cứng.',
  "<strong>Per-cue cooldown</strong> — the same cue will not repeat within about 350 ms (45 ms for the interaction tick).":
    "<strong>Thời gian chờ cho từng tín hiệu</strong> — cùng một tín hiệu sẽ không lặp lại trong khoảng 350 ms (45 ms với tiếng tích tương tác).",
  "<strong>Global burst budget</strong> — at most 4 cues start within any 1.2 s window, so importing history or reconnecting after a drop never turns a flood of WebSocket messages into a flood of beeps.":
    "<strong>Ngân sách bùng nổ toàn cục</strong> — tối đa 4 tín hiệu bắt đầu trong bất kỳ cửa sổ 1.2 s nào, nên việc nhập lịch sử hay kết nối lại sau khi mất kết nối không bao giờ biến một loạt thông điệp WebSocket thành một loạt tiếng bíp.",
  "<strong>Autoplay policy</strong> — nothing plays until your first pointer, key, or touch interaction with the page. Cues before that are silently dropped, not queued.":
    "<strong>Chính sách autoplay</strong> — không có gì phát ra trước lần tương tác bằng con trỏ, phím hoặc chạm đầu tiên của bạn với trang. Các tín hiệu trước thời điểm đó bị bỏ qua âm thầm chứ không xếp hàng chờ.",
  "If the browser has no Web Audio support at all, every call is a safe no-op and the dashboard simply stays silent.":
    "Nếu trình duyệt hoàn toàn không hỗ trợ Web Audio, mọi lệnh gọi đều là no-op an toàn và dashboard chỉ đơn giản là im lặng.",
  "<strong>Settings → Sound</strong> has a master toggle, a volume slider, and an individual switch for each cue. Flipping a switch on plays that cue immediately so you can hear exactly what you just enabled, and a preview button plays the completion chime on demand. Preferences persist to <code>localStorage</code> under <code>agent-monitor-sound</code> and take effect instantly across the app — no reload. By default, session start, session complete, session error, notifications, and the interaction tick are on; subagent spawns and connection changes are off because they are the chattiest. The wiring lives in <code>client/src/hooks/useSoundCues.ts</code>, mounted once in <code>client/src/App.tsx</code>.":
    "<strong>Settings → Sound</strong> có công tắc tổng, thanh trượt âm lượng và một công tắc riêng cho từng tín hiệu. Bật một công tắc sẽ phát ngay tín hiệu đó để bạn nghe chính xác thứ vừa bật, và nút nghe thử phát tiếng chuông hoàn tất bất cứ lúc nào. Tùy chọn được lưu vào <code>localStorage</code> dưới khóa <code>agent-monitor-sound</code> và có hiệu lực tức thì trên toàn ứng dụng — không cần tải lại. Theo mặc định, phiên bắt đầu, phiên hoàn tất, phiên lỗi, thông báo và tiếng tích tương tác được bật; subagent được tạo và thay đổi kết nối được tắt vì chúng ồn ào nhất. Phần đấu nối nằm tại <code>client/src/hooks/useSoundCues.ts</code>, được gắn một lần trong <code>client/src/App.tsx</code>.",
});

Object.assign(window.__WIKI_CONTENT_I18N.plain.vi, {
  "🔊 Sound Cues": "🔊 Tín hiệu âm thanh",
  "Sound Cues": "Tín hiệu âm thanh",
  "Zero-dependency synthesis": "Tổng hợp không phụ thuộc",
  "The cues": "Các tín hiệu",
  "Staying out of your way": "Không làm phiền bạn",
  Cue: "Tín hiệu",
  "When it fires": "Khi nào phát",
  "What it sounds like": "Nghe như thế nào",
});

Object.assign(window.__WIKI_CONTENT_I18N.ko, {
  "The dashboard gives you <strong>subtle audio feedback</strong> for live session activity, so you can leave it on a second monitor and still hear when a run finishes or fails. Sound is <strong>on by default</strong> and can be switched off entirely in one click from <strong>Settings → Sound</strong>. Like Tabby, it is a purely additive client-side subscriber to the existing <code>eventBus</code> WebSocket stream: <strong>no server code, no new API routes, no new message types, and no new dependencies</strong>.":
    "대시보드는 라이브 세션 활동에 대해 <strong>은은한 오디오 피드백</strong>을 제공하므로, 보조 모니터에 띄워 두어도 실행이 끝났는지 실패했는지 소리로 알 수 있습니다. 사운드는 <strong>기본으로 켜져 있으며</strong> <strong>설정 → 사운드</strong>에서 클릭 한 번으로 완전히 끌 수 있습니다. Tabby처럼 기존 <code>eventBus</code> WebSocket 스트림에 대한 순수하게 추가적인 클라이언트 측 구독자일 뿐입니다: <strong>서버 코드 없음, 새 API 라우트 없음, 새 메시지 타입 없음, 새 의존성 없음</strong>.",
  "There are no <code>.mp3</code> or <code>.wav</code> assets anywhere in the repository and no audio library in <code>package.json</code>. Every cue is generated at play time with the <strong>Web Audio API</strong>: a short list of oscillators (sine or triangle), each with its own exponential gain envelope, mixed through a master gain node and a low-pass filter so the result sits behind your work rather than cutting through it. The whole engine is one file, <code>client/src/lib/sound.ts</code>.":
    "저장소 어디에도 <code>.mp3</code>나 <code>.wav</code> 자산이 없고 <code>package.json</code>에도 오디오 라이브러리가 없습니다. 모든 큐는 재생 시점에 <strong>Web Audio API</strong>로 생성됩니다: 짧은 오실레이터 목록(사인 또는 삼각파)에 각각 지수 게인 엔벨로프를 걸고, 마스터 게인 노드와 로우패스 필터를 거쳐 믹싱하여 결과가 작업을 뚫고 나오는 대신 그 뒤에 자리 잡도록 합니다. 엔진 전체가 <code>client/src/lib/sound.ts</code> 한 파일입니다.",
  "A session finishes responding or closes": "세션이 응답을 마치거나 종료될 때",
  "A session enters the error state": "세션이 오류 상태에 들어갈 때",
  "Soft falling minor third on a triangle wave": "삼각파의 부드러운 하행 단3도",
  "Claude Code emits a notification event": "Claude Code가 알림 이벤트를 낼 때",
  "Detuned pair ringing like a small bell": "작은 종처럼 울리는 디튠된 한 쌍",
  "The dashboard WebSocket returns or drops": "대시보드 WebSocket이 복구되거나 끊길 때",
  "You press a button, link, tab, or switch": "버튼, 링크, 탭, 스위치를 누를 때",
  "Cues live inside a C-major set so overlapping tails never sound dissonant, and every envelope decays exponentially rather than cutting off, which avoids the click of a hard stop.":
    "큐는 다장조 음 집합 안에 머물러 겹치는 잔향이 불협화음이 되지 않으며, 모든 엔벨로프는 딱 끊기지 않고 지수적으로 감쇠하여 하드 스톱 특유의 팝 소리를 피합니다.",
  "<strong>Per-cue cooldown</strong> — the same cue will not repeat within about 350 ms (45 ms for the interaction tick).":
    "<strong>큐별 쿨다운</strong> — 같은 큐는 약 350 ms 이내에 반복되지 않습니다(상호작용 틱은 45 ms).",
  "<strong>Global burst budget</strong> — at most 4 cues start within any 1.2 s window, so importing history or reconnecting after a drop never turns a flood of WebSocket messages into a flood of beeps.":
    "<strong>전역 버스트 예산</strong> — 1.2 s 창 안에서 최대 4개의 큐만 시작되므로, 히스토리를 가져오거나 끊긴 뒤 재연결할 때 쏟아지는 WebSocket 메시지가 쏟아지는 알림음이 되지 않습니다.",
  "<strong>Autoplay policy</strong> — nothing plays until your first pointer, key, or touch interaction with the page. Cues before that are silently dropped, not queued.":
    "<strong>자동재생 정책</strong> — 페이지와의 첫 포인터·키·터치 상호작용 전에는 아무것도 재생되지 않습니다. 그 이전의 큐는 대기열에 쌓이지 않고 조용히 버려집니다.",
  "If the browser has no Web Audio support at all, every call is a safe no-op and the dashboard simply stays silent.":
    "브라우저가 Web Audio를 전혀 지원하지 않으면 모든 호출이 안전한 no-op이 되고 대시보드는 그저 조용할 뿐입니다.",
  "<strong>Settings → Sound</strong> has a master toggle, a volume slider, and an individual switch for each cue. Flipping a switch on plays that cue immediately so you can hear exactly what you just enabled, and a preview button plays the completion chime on demand. Preferences persist to <code>localStorage</code> under <code>agent-monitor-sound</code> and take effect instantly across the app — no reload. By default, session start, session complete, session error, notifications, and the interaction tick are on; subagent spawns and connection changes are off because they are the chattiest. The wiring lives in <code>client/src/hooks/useSoundCues.ts</code>, mounted once in <code>client/src/App.tsx</code>.":
    "<strong>설정 → 사운드</strong>에는 마스터 토글, 볼륨 슬라이더, 그리고 각 큐의 개별 스위치가 있습니다. 스위치를 켜면 해당 큐가 즉시 재생되어 방금 무엇을 켰는지 정확히 들을 수 있고, 미리 듣기 버튼은 완료 차임을 원할 때 재생합니다. 설정은 <code>localStorage</code>의 <code>agent-monitor-sound</code> 키에 저장되며 앱 전체에 즉시 적용됩니다 — 새로고침이 필요 없습니다. 기본적으로 세션 시작, 세션 완료, 세션 오류, 알림, 상호작용 틱이 켜져 있고, 서브에이전트 생성과 연결 변화는 가장 잦기 때문에 꺼져 있습니다. 연결 로직은 <code>client/src/hooks/useSoundCues.ts</code>에 있으며 <code>client/src/App.tsx</code>에서 한 번 마운트됩니다.",
});

Object.assign(window.__WIKI_CONTENT_I18N.plain.ko, {
  "🔊 Sound Cues": "🔊 사운드 큐",
  "Sound Cues": "사운드 큐",
  "Zero-dependency synthesis": "의존성 없는 합성",
  "The cues": "큐 목록",
  "Staying out of your way": "방해하지 않기",
  Cue: "큐",
  "When it fires": "언제 울리는가",
  "What it sounds like": "어떤 소리인가",
});

Object.assign(window.__WIKI_CONTENT_I18N.es, {
  "The dashboard gives you <strong>subtle audio feedback</strong> for live session activity, so you can leave it on a second monitor and still hear when a run finishes or fails. Sound is <strong>on by default</strong> and can be switched off entirely in one click from <strong>Settings → Sound</strong>. Like Tabby, it is a purely additive client-side subscriber to the existing <code>eventBus</code> WebSocket stream: <strong>no server code, no new API routes, no new message types, and no new dependencies</strong>.":
    "El panel ofrece <strong>retroalimentación de audio discreta</strong> para la actividad de sesiones en vivo, de modo que puedes dejarlo en un segundo monitor y aun así oír cuándo una ejecución termina o falla. El sonido está <strong>activado por defecto</strong> y se puede desactivar por completo con un clic desde <strong>Configuración → Sonido</strong>. Como Tabby, es un suscriptor del lado del cliente puramente aditivo al flujo WebSocket <code>eventBus</code> ya existente: <strong>sin código de servidor, sin nuevas rutas de API, sin nuevos tipos de mensaje y sin nuevas dependencias</strong>.",
  "There are no <code>.mp3</code> or <code>.wav</code> assets anywhere in the repository and no audio library in <code>package.json</code>. Every cue is generated at play time with the <strong>Web Audio API</strong>: a short list of oscillators (sine or triangle), each with its own exponential gain envelope, mixed through a master gain node and a low-pass filter so the result sits behind your work rather than cutting through it. The whole engine is one file, <code>client/src/lib/sound.ts</code>.":
    "No hay ningún archivo <code>.mp3</code> ni <code>.wav</code> en el repositorio ni ninguna biblioteca de audio en <code>package.json</code>. Cada señal se genera en el momento de reproducirse con la <strong>Web Audio API</strong>: una lista corta de osciladores (seno o triángulo), cada uno con su propia envolvente de ganancia exponencial, mezclados a través de un nodo de ganancia maestro y un filtro paso bajo para que el resultado se quede detrás de tu trabajo en lugar de atravesarlo. Todo el motor es un solo archivo, <code>client/src/lib/sound.ts</code>.",
  "A session finishes responding or closes": "Una sesión termina de responder o se cierra",
  "A session enters the error state": "Una sesión entra en estado de error",
  "Soft falling minor third on a triangle wave":
    "Tercera menor descendente suave en onda triangular",
  "Claude Code emits a notification event": "Claude Code emite un evento de notificación",
  "Detuned pair ringing like a small bell": "Par desafinado que suena como una campanita",
  "The dashboard WebSocket returns or drops": "El WebSocket del panel vuelve o se cae",
  "You press a button, link, tab, or switch": "Pulsas un botón, enlace, pestaña o interruptor",
  "Cues live inside a C-major set so overlapping tails never sound dissonant, and every envelope decays exponentially rather than cutting off, which avoids the click of a hard stop.":
    "Las señales viven dentro de un conjunto de do mayor, así que las colas superpuestas nunca suenan disonantes, y cada envolvente decae exponencialmente en lugar de cortarse, lo que evita el chasquido de una parada brusca.",
  "<strong>Per-cue cooldown</strong> — the same cue will not repeat within about 350 ms (45 ms for the interaction tick).":
    "<strong>Enfriamiento por señal</strong> — la misma señal no se repite dentro de unos 350 ms (45 ms para el tic de interacción).",
  "<strong>Global burst budget</strong> — at most 4 cues start within any 1.2 s window, so importing history or reconnecting after a drop never turns a flood of WebSocket messages into a flood of beeps.":
    "<strong>Presupuesto global de ráfaga</strong> — como máximo 4 señales comienzan en cualquier ventana de 1.2 s, así que importar el historial o reconectar tras una caída nunca convierte una avalancha de mensajes WebSocket en una avalancha de pitidos.",
  "<strong>Autoplay policy</strong> — nothing plays until your first pointer, key, or touch interaction with the page. Cues before that are silently dropped, not queued.":
    "<strong>Política de autoreproducción</strong> — nada suena hasta tu primera interacción de puntero, tecla o toque con la página. Las señales previas se descartan en silencio, no se encolan.",
  "If the browser has no Web Audio support at all, every call is a safe no-op and the dashboard simply stays silent.":
    "Si el navegador no soporta Web Audio en absoluto, cada llamada es una operación nula segura y el panel simplemente permanece en silencio.",
  "<strong>Settings → Sound</strong> has a master toggle, a volume slider, and an individual switch for each cue. Flipping a switch on plays that cue immediately so you can hear exactly what you just enabled, and a preview button plays the completion chime on demand. Preferences persist to <code>localStorage</code> under <code>agent-monitor-sound</code> and take effect instantly across the app — no reload. By default, session start, session complete, session error, notifications, and the interaction tick are on; subagent spawns and connection changes are off because they are the chattiest. The wiring lives in <code>client/src/hooks/useSoundCues.ts</code>, mounted once in <code>client/src/App.tsx</code>.":
    "<strong>Configuración → Sonido</strong> tiene un interruptor maestro, un control de volumen y un interruptor individual para cada señal. Activar un interruptor reproduce esa señal de inmediato para que oigas exactamente lo que acabas de habilitar, y un botón de vista previa reproduce el tono de finalización cuando quieras. Las preferencias persisten en <code>localStorage</code> bajo <code>agent-monitor-sound</code> y surten efecto al instante en toda la aplicación, sin recargar. Por defecto están activados el inicio de sesión, la finalización de sesión, el error de sesión, las notificaciones y el tic de interacción; la generación de subagentes y los cambios de conexión están desactivados porque son los más ruidosos. La conexión vive en <code>client/src/hooks/useSoundCues.ts</code>, montada una sola vez en <code>client/src/App.tsx</code>.",
});

Object.assign(window.__WIKI_CONTENT_I18N.plain.es, {
  "🔊 Sound Cues": "🔊 Señales de sonido",
  "Sound Cues": "Señales de sonido",
  "Zero-dependency synthesis": "Síntesis sin dependencias",
  "The cues": "Las señales",
  "Staying out of your way": "Sin estorbarte",
  Cue: "Señal",
  "When it fires": "Cuándo suena",
  "What it sounds like": "Cómo suena",
});
